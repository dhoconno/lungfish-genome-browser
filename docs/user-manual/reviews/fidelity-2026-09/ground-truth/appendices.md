# Reality map: appendices

Sources consulted:

- `docs/user-manual/reviews/fidelity-2026-09/cli-help/` (all 46 files, `_root.txt` and `_version.txt` included. `_version.txt` reports `2026.9.13`)
- `Sources/LungfishApp/App/MainMenu.swift`
- `Sources/LungfishApp/App/AppDelegate+ImportCenter.swift`
- `Sources/LungfishApp/App/ProjectLockResolutionDialog.swift`
- `Sources/LungfishApp/App/ProjectLockWarningPresentation.swift`
- `Sources/LungfishApp/App/AppDelegate.swift`
- `Sources/LungfishApp/App/ProjectWriteGatePresenter.swift`
- `Sources/LungfishApp/Views/ImportCenter/ImportCenterViewModel.swift`
- `Sources/LungfishApp/Views/AI/AIAssistantPanel.swift`
- `Sources/LungfishApp/Views/Settings/AIServicesSettingsTab.swift`
- `Sources/LungfishApp/Services/AI/AIToolRegistry.swift`
- `Sources/LungfishApp/Services/AI/AIAssistantService.swift`
- `Sources/LungfishApp/Resources/PrimerSchemes/` (eight `.lungfishprimers` bundles and their `manifest.json` files)
- `Sources/LungfishCore/Models/AppSettings.swift`
- `Sources/LungfishCore/Services/AI/` (`AIProvider.swift`, `AnthropicProvider.swift`, `OpenAIProvider.swift`, `GeminiProvider.swift`, `OpenAIEndpointConfiguration.swift`)
- `Sources/LungfishCore/Services/NCBI/NCBIService.swift`
- `Sources/LungfishCore/Storage/ProjectLock.swift`
- `Sources/LungfishCore/Storage/ProjectLockRecovery.swift`
- `Sources/LungfishIO/Bundles/PrimerSchemeBundle.swift`
- `Sources/LungfishIO/Registry/` (`FormatIdentifier.swift`, `FormatRegistry+BuiltInDescriptors.swift`, `BuiltInFormats.swift`)
- `Sources/LungfishIO/Formats/VCF/VCFReader.swift`
- `Sources/LungfishIO/Formats/` (writer inventory)
- `Sources/LungfishKit/OperationFailureReportStore.swift`
- `Sources/LungfishWorkflow/Conda/CondaRootMutationLock.swift`
- `Sources/LungfishWorkflow/Resources/ManagedTools/third-party-tools-lock.json`
- `Sources/LungfishWorkflow/Resources/Tools/tool-versions.json`

Note on the June 2026 map. `docs/user-manual/reviews/part-ii-fidelity-2026-06-02/ground-truth/appendices.md` states the binary has 43 top-level commands. That count is now wrong. `_root.txt` lists 44, because `tools` was added. Every row reused from the June map was re-verified against the September help dump and the current source.

## cli-reference.md

### Claims

| # | Claim (quoted from the chapter) | Verdict (true, false, changed) | Evidence | Corrected wording |
|---|---|---|---|---|
| 1 | "The Lungfish command-line interface is a single binary, `lungfish`, that mirrors most operations available in the GUI." | true | `cli-help/_root.txt` banner `==== _root ====`, `USAGE: lungfish-cli [--format <format>] ... <subcommand>`. The help tree exposes one binary with 44 subcommands | |
| 2 | "If you build from source, the SwiftPM product is `lungfish-cli`, so invoke `.build/debug/lungfish-cli ...`" | true | Every banner in `cli-help/` prints `USAGE: lungfish-cli ...`, so the built product is named `lungfish-cli` | |
| 3 | "The binary exposes 43 top-level commands." | false | `cli-help/_root.txt` `SUBCOMMANDS:` block lists 44 names: version, convert, analyze, translate, sequence, search, universal-search, extract, fastq, workflow, run-headless, fetch, bundle, project, provision-tools, tools, conda, blast, esviritu, taxtriage, align, msa, tree, assemble, orient, map, import, import-fastq, ops, provenance, bam, variants, gatk, nao-mgs, freyja, nvd, cz-id, metadata, haplotypes, build-db, markdup, primers, genotype, debug | "The binary exposes 44 top-level commands." |
| 4 | The command index table (44 rows expected) | false | The table has 43 rows and omits `tools`. `cli-help/tools.txt` banner `==== tools ====`, `OVERVIEW: Inspect and update managed third-party tools`, subcommand `update (default)` | Add a row for `tools`. It inspects and updates managed third-party tools against the pinned dependency set (`tools update`). |
| 5 | "`bam` \| Operate on bundle-owned BAM tracks (`filter`, `annotate`, `markdup`, `primer-trim`, `adopt-mapping`)." | changed | `cli-help/bam.txt` `==== bam ====` lists seven subcommands: filter, annotate, annotate-best, annotate-cds-best, markdup, primer-trim, adopt-mapping | "`bam` \| Operate on bundle-owned BAM tracks (`filter`, `annotate`, `annotate-best`, `annotate-cds-best`, `markdup`, `primer-trim`, `adopt-mapping`)." |
| 6 | "`conda` \| Manage plugin packs and run Kraken2 (`conda classify`)." | changed | `cli-help/conda.txt` `==== conda ====` lists 16 subcommands, including `db` (database management with seven of its own subcommands) and `extract` (taxonomy read extraction) | "`conda` \| Manage plugin packs, metagenomics databases, and Kraken2 classification (`conda classify`, `conda db`, `conda extract`)." |
| 7 | "`debug` \| Diagnostic commands (env check, container diagnostics, log parser)." | changed | `cli-help/debug.txt` `==== debug ====` lists five subcommands: env (default), container, resource-smoke, fastq-ingest, workflow-log | "`debug` \| Diagnostic commands (env check, container diagnostics, packaged-resource smoke test, FASTQ ingest, log parser)." |
| 8 | "`esviritu` \| EsViritu viral detection (`esviritu detect`)." | changed | `cli-help/esviritu.txt` `==== esviritu ====` lists detect (default), download-db, db-status | "`esviritu` \| EsViritu viral detection and database management (`detect`, `download-db`, `db-status`)." |
| 9 | "`gatk` \| Build or run GATK4 germline-variant commands (10 subcommands)." | true | `cli-help/gatk.txt` `==== gatk ====` lists exactly ten: haplotype-caller, joint-genotype, filter, select, variants-to-table, bqsr, markdup, validate-sam, leftalign, collect-metrics | |
| 10 | "`genotype` \| Inspect and export ONT genotype result bundles (7 subcommands)." | false | `cli-help/genotype.txt` `==== genotype ====` lists eleven: list-samples, list-cohorts, ai-haplotyping, apply-annotations, replay-matrix-annotation, replay-manual-haplotype-assignments, replay-call-overrides, export, export-xlsx, export-pivot-xlsx, export-labkey | "`genotype` \| Inspect, annotate, and export ONT genotype result bundles (11 subcommands)." |
| 11 | "`haplotypes` \| Manage ONT genotyping haplotype definition sets (10 subcommands)." | false | `cli-help/haplotypes.txt` `==== haplotypes ====` lists eleven: list, validate, import, save, bundle-install, bundle-create, bundle-save, bundle-replace-reference, export, duplicate, delete | "`haplotypes` \| Manage ONT genotyping haplotype definition sets (11 subcommands)." |
| 12 | "`taxtriage` \| Run the TaxTriage classification pipeline (`taxtriage run`)." | changed | `cli-help/taxtriage.txt` `==== taxtriage ====` lists run (default) and check-prerequisites | "`taxtriage` \| Run the TaxTriage classification pipeline and check its prerequisites (`run`, `check-prerequisites`)." |
| 13 | "`workflow` \| Run, list, validate, and diff Lungfish workflows." | changed | `cli-help/workflow.txt` `==== workflow ====` lists run (default), builder-run, list, validate, diff | "`workflow` \| Run, run a builder graph, list, validate, and diff Lungfish workflows." |
| 14 | "`bundle` \| Create, inspect, and export reference bundles." | true | `cli-help/bundle.txt` `==== bundle ====` lists info (default), create, extract-annotations, deduplicate-alignments, export, validate, list | |
| 15 | "`tree` \| Infer a phylogenetic tree (IQ-TREE)." | changed | `cli-help/tree.txt` `==== tree ====` lists infer, export, reroot, extract-subtree, relabel | "`tree` \| Infer, export, re-root, subset, and relabel phylogenetic tree bundles (IQ-TREE for inference)." |
| 16 | "`import` \| Import local files into a project (FASTA, VCF, BAM, classifier output, and more)." | true | `cli-help/import.txt` `==== import ====` lists 16 subcommands: bam, vcf, fasta, msa, tree, fastq, geneious, application-export, sample-metadata, kraken2, esviritu, taxtriage, nao-mgs, nvd, cz-id, metadata | |
| 17 | "`lungfish version [--tools]` ... Prints the Lungfish CLI version." | true | `cli-help/version.txt` `==== version ====`, `USAGE: lungfish-cli version [--tools]`, option `--tools  Print the bundled and managed tool version table` | |
| 18 | "`--db` defaults to `nucleotide`; `protein` is also supported." | true | `cli-help/fetch.txt` `==== fetch ncbi ====`, `--db <db>  Database: nucleotide, protein (default: nucleotide)` | |
| 19 | "`--fetch-format` accepts `genbank` (default), `fasta`, `gff3`, or `xml`." | true | `cli-help/fetch.txt` `==== fetch ncbi ====`, `--fetch-format <fetch-format>  Fetch format: genbank, fasta, gff3, xml (default: genbank)` | |
| 20 | "`lungfish fetch ncbi <accession> [--db <database>] [--fetch-format <format>] [--save-to <path>]`" | changed | `cli-help/fetch.txt` `==== fetch ncbi ====`, `USAGE: lungfish-cli fetch ncbi [<options>] <accessions> ...`. The positional is repeatable, and `--api-key` and `--no-retry` also exist | "`lungfish fetch ncbi <accession>... [--db <database>] [--fetch-format <format>] [--save-to <path>] [--api-key <key>] [--no-retry]`. Accessions are repeatable, so one call can fetch several records into one file." |
| 21 | "`lungfish fetch sra search <query>` ... Searches SRA by free-text query or accession." | true | `cli-help/fetch.txt` `==== fetch sra search ====`, `USAGE: lungfish-cli fetch sra search <query> [--limit <limit>] ...` | |
| 22 | "Tries ENA first; falls back to the NCBI SRA Toolkit (`prefetch` + `fasterq-dump`) when ENA refuses. `--use-toolkit` forces the SRA Toolkit path." | true | `cli-help/fetch.txt` `==== fetch sra ====` overview says "Downloads use ENA mirrors for direct HTTP access (no SRA Toolkit required)". `==== fetch sra download ====` option `--use-toolkit  Use SRA Toolkit instead of ENA (requires prefetch/fasterq-dump)` | |
| 23 | "`lungfish fetch genome <assembly-accession> [--name <name>] [--output-dir <dir>] [--fasta-only]`" | changed | `cli-help/fetch.txt` `==== fetch genome ====` also documents `--no-bundle` and `--api-key`, and the positional accepts a nucleotide accession as well as an assembly accession | "`lungfish fetch genome <accession> [--name <name>] [--output-dir <dir>] [--fasta-only] [--no-bundle]`. The accession may be an assembly accession (`GCF_`/`GCA_`) or a nucleotide accession such as `MN908947.3`. `--no-bundle` downloads the files without wrapping them in a `.lungfishref` bundle." |
| 24 | "Accepts assembly accessions like `GCF_009858895.2`. Includes FASTA plus GFF3 by default; pass `--fasta-only` to skip annotations." | true | `cli-help/fetch.txt` `==== fetch genome ====` overview lists the bundle contents and `--fasta-only  Download only the FASTA sequence (no annotations, no bundle)` | |
| 25 | "`lungfish fetch ena <subcommand>` ... with `search`, `reads`, and `fasta` subcommands." | true | `cli-help/fetch.txt` `==== fetch ena ====` subcommand block lists search, reads, fasta | |
| 26 | The `fetch` section omits `fetch search` and `fetch sra info`. | false | `cli-help/fetch.txt` `==== fetch ====` lists a top-level `search` subcommand ("Search NCBI databases"), and `==== fetch sra ====` lists `info` ("Get information about an SRA run") | Add: "`lungfish fetch search <query> [--db <database>] [--limit <n>] [--organism <name>]` searches NCBI databases and lists matching accessions. `lungfish fetch sra info <accession>` prints metadata for one SRA run." |
| 27 | "`import` requires a subcommand; the `fasta` subcommand handles FASTA, GenBank, and EMBL. A bare `lungfish import <path>` with no subcommand errors." | true | `cli-help/import.txt` `==== import ====` has no `(default)` marker on any subcommand. `==== import fasta ====` argument is "Path to the input reference (.fa/.fasta/.gb/.embl, optionally .gz/.bgz/.bz2/.xz/.zst)" | |
| 28 | "Imports FASTQ files into the project's `Imports/` folder. Auto-pairs files with `_1`/`_2` or `_R1`/`_R2` suffixes." | unverifiable | `cli-help/import.txt` `==== import fastq ====` confirms the command and its `--samplesheet`, `--project`, `--platform`, `--dry-run` flags, but the help text does not state the destination folder or the pairing suffixes | Settled by reading the importer's destination-path and pairing code under `Sources/LungfishCLI/Commands/ImportCommand*.swift`. |
| 29 | "`lungfish import fastq --samplesheet <csv> --project <path>` ... Imports a paired Illumina CSV sample sheet with `sample`, `r1`, and `r2` columns." | true | `cli-help/import.txt` `==== import fastq ====`, `--samplesheet <samplesheet>  CSV sample sheet with sample,r1,r2 columns and optional metadata columns` | |
| 30 | "`lungfish import-fastq --samplesheet <csv> --project <path>` is an alias for the same command." | true | `cli-help/import-fastq.txt` `==== import-fastq ====` prints an identical option set to `==== import fastq ====` (same `--samplesheet`, `--recipe`, `--quality-binning`, `--platform`, `--clumping-tool`, `--compression` flags) | |
| 31 | "There is no `--reference` flag; the bundle is resolved internally. `-o`/`--output-dir` names the destination project directory." | true | `cli-help/import.txt` `==== import vcf ====`, `USAGE: lungfish-cli import vcf <input-file> [--output-dir <output-dir>] ...` with no `--reference` option listed | |
| 32 | "`lungfish import application-export <kind> <source-path> --project <path>` ... Imports an external application export (such as a Geneious-style project)" | false | `cli-help/import.txt` `==== import application-export ====` lists the accepted kinds as clc-workbench, dnastar-lasergene, benchling-bulk, sequence-design-library, alignment-tree, sequencing-platform-run-folder, phylogenetics-result-set, qiime2-archive, igv-session-track-set. Geneious is not among them. `==== import geneious ====` is its own subcommand | "`lungfish import application-export <kind> <source-path> --project <path>` imports an external application export. `<kind>` is one of `clc-workbench`, `dnastar-lasergene`, `benchling-bulk`, `sequence-design-library`, `alignment-tree`, `sequencing-platform-run-folder`, `phylogenetics-result-set`, `qiime2-archive`, or `igv-session-track-set`. Geneious exports have their own subcommand, `lungfish import geneious <path> --project <path>`." |
| 33 | "`lungfish import nao-mgs <path>`, `lungfish import cz-id <path>`, and `lungfish import nvd <path>` import classifier results from those pipelines into the project's `Imports/` folder." | changed | `cli-help/import.txt` confirms all three subcommands, but their `-o, --output-dir` help reads "Output project/import directory (default: current directory)", not a fixed `Imports/` folder. The same section also omits `import kraken2`, `import esviritu`, and `import taxtriage` | "`lungfish import nao-mgs <path>`, `lungfish import cz-id <path>`, `lungfish import nvd <path>`, `lungfish import kraken2 <path>`, `lungfish import esviritu <path>`, and `lungfish import taxtriage <path>` import classifier results from those pipelines. `-o`/`--output-dir` names the destination project or import directory and defaults to the current directory." |
| 34 | "`lungfish bundle create --fasta <path> [--annotation <path>...] --name <name> [--output-dir <dir>] [--compress]`" | false | `cli-help/bundle.txt` `==== bundle create ====`, `USAGE: lungfish-cli bundle create [<options>] --fasta <fasta> --name <name> --output-dir <output-dir>`. `--output-dir` is required, and the help also lists `--identifier`, `--bundle-description`, `--organism`, `--assembly`, and `--variant` | "`lungfish bundle create --fasta <path> --name <name> --output-dir <dir> [--annotation <path>...] [--variant <path>...] [--identifier <id>] [--bundle-description <text>] [--organism <name>] [--assembly <name>] [--compress]`. All three of `--fasta`, `--name`, and `--output-dir` are required." |
| 35 | "`--annotation` accepts one or more GFF3, GTF, or BED files. `--compress` bgzips the FASTA inside the bundle." | true | `cli-help/bundle.txt` `==== bundle create ====` overview line "Optional: Annotation files (GFF3, GTF, BED), variant files (VCF)". Option `--compress  Compress FASTA with bgzip` | |
| 36 | "`lungfish bundle list` ... Lists every reference bundle in the project's `Reference Sequences/` folder." | false | `cli-help/bundle.txt` `==== bundle list ====`, `OVERVIEW: List bundle contents`, `USAGE: lungfish-cli bundle list <bundle-path> [--tracks] [--files]`, argument "Path to the .lungfishref bundle". It lists the files and tracks inside one named bundle | "`lungfish bundle list <bundle> [--tracks] [--files]` lists the files and tracks inside one `.lungfishref` bundle. `--tracks` shows tracks only and `--files` shows files only." |
| 37 | "`lungfish bundle export <bundle> --format container --output <image.oci.tar> [--plugin-pack <name>...]` ... Exports a deterministic OCI-layout tarball" | true | `cli-help/bundle.txt` `==== bundle export ====`, `USAGE: lungfish-cli bundle export <bundle-path> --format <format> --output <output> [--plugin-pack <plugin-pack> ...] [--quiet]`, overview "Exports a Lungfish bundle as a deterministic OCI layout tarball" | |
| 38 | "`--bundle`, `--track` (an annotation track id or name), and `--output-bundle` are all required." | true | `cli-help/bundle.txt` `==== bundle extract-annotations ====`, `USAGE: lungfish-cli bundle extract-annotations [<options>] --bundle <bundle> --track <track> --output-bundle <output-bundle>` | |
| 39 | "`--feature-type` selects which feature type to pull (default `gene`)" | true | `cli-help/bundle.txt` `==== bundle extract-annotations ====`, `--feature-type <feature-type>  Feature type to extract (default: gene)` | |
| 40 | "The full `bundle` group also includes `info`, `validate`, `deduplicate-alignments`, and `create`/`list`/`export` shown above" | true | `cli-help/bundle.txt` `==== bundle ====` subcommand block matches exactly | |
| 41 | "Runs the configured mapper (default minimap2)." | true | `cli-help/map.txt` `==== map ====`, `--mapper <mapper>  Mapper: minimap2, bwa-mem2, bowtie2, bbmap (default: minimap2)` | |
| 42 | The `map` signature omits `--mapper`, `--secondary`, `--no-supplementary`, and `--min-mapq`. | false | `cli-help/map.txt` `==== map ====` lists all four | Add to the signature: `[--mapper <minimap2\|bwa-mem2\|bowtie2\|bbmap>] [--secondary] [--no-supplementary] [--min-mapq <int>]`, and note that `--min-mapq` defaults to 0, `--secondary` keeps secondary alignments in the normalized BAM, and `--no-supplementary` drops supplementary ones. |
| 43 | "The common `--preset` values are `sr` (Illumina short reads), `map-ont` (Nanopore), `map-hifi` (PacBio HiFi), and `map-pb` (PacBio CLR); minimap2 also accepts `asm5` and `splice`, and the BBMap path adds `bbmap-standard` and `bbmap-pacbio`." | true | `cli-help/map.txt` `==== map ====`, `--preset <preset>  Mode/preset. minimap2: sr, asm5, splice, map-ont, map-hifi, map-pb. bbmap: bbmap-standard, bbmap-pacbio.` | |
| 44 | "Read-group fields default to the sample name, except `--rg-pl`, which defaults from the selected preset." | true | `cli-help/map.txt` `==== map ====`: `--rg-id`, `--rg-sm`, `--rg-lb`, `--rg-pu` all say "(default: sample name)". `--rg-pl` says "(default: mapper preset platform)" | |
| 45 | "`-o` names the output directory." | true | `cli-help/map.txt` `==== map ====`, `-o, --output-dir <output-dir>  Output directory (default: mapping-<id> next to input)` | |
| 46 | "`lungfish bam adopt-mapping --bundle <bundle> --mapping-result <dir> --name <name> [--track-id <id>]` ... `--name` is required" | true | `cli-help/bam.txt` `==== bam adopt-mapping ====`, `USAGE: lungfish-cli bam adopt-mapping [<options>] --bundle <bundle> --mapping-result <mapping-result> --name <name>` | |
| 47 | "`lungfish bam primer-trim --bundle <bundle> --alignment-track <id> --scheme <path> [--name <name>]`" | false | `cli-help/bam.txt` `==== bam primer-trim ====`, `USAGE: lungfish-cli bam primer-trim [<options>] --bundle <bundle> --alignment-track <alignment-track> --scheme <scheme> --name <name>`. `--name` is required, and the help also lists `--target-reference`, `--ivar-min-quality` (default 20), `--ivar-min-length` (default 30), `--ivar-sliding-window` (default 4), and `--ivar-primer-offset` | "`lungfish bam primer-trim --bundle <bundle> --alignment-track <id> --scheme <path> --name <name> [--target-reference <sn>] [--ivar-min-quality <int>] [--ivar-min-length <int>] [--ivar-sliding-window <int>] [--ivar-primer-offset <int>]`. `--name` is required. The iVar trim options default to quality 20, length 30, and a 4-base sliding window." |
| 48 | "`lungfish bam annotate --bundle <bundle> --alignment-track <id> --output-track-name <name>` ... Converts mapped reads in an alignment track to a bundle annotation track." | true | `cli-help/bam.txt` `==== bam annotate ====`, `OVERVIEW: Convert mapped reads to annotations in a reference bundle`, `USAGE: lungfish-cli bam annotate [<options>] --bundle <bundle> --alignment-track <alignment-track> --output-track-name <output-track-name>` | |
| 49 | "`lungfish markdup <path> [--force] [--sort-threads <n>] [--deduplicated-bundle <path>]` ... The argument is a single positional path to a BAM file or a directory of BAMs" | true | `cli-help/markdup.txt` `==== markdup ====`, `USAGE: lungfish-cli markdup [<options>] <path>`, argument "Path to a BAM file or a directory containing BAMs", options `--force`, `--sort-threads` (default 4), `--deduplicated-bundle` | |
| 50 | "The same core operation is available as `lungfish bam markdup`, which takes `<path>` plus `--force` and `--sort-threads` but does not offer `--deduplicated-bundle`." | true | `cli-help/bam.txt` lists `markdup` as a `bam` subcommand. `cli-help/markdup.txt` shows `--deduplicated-bundle` only on the top-level form | |
| 51 | "`--caller <ivar\|lofreq\|medaka\|bcftools\|clair3>`" | true | `cli-help/variants.txt` `==== variants call ====`, `--caller <caller>  Variant caller: lofreq, ivar, medaka, bcftools, clair3` | |
| 52 | The `variants call` flag table omits `--min-depth`, `--ivar-consensus-af`, `--ivar-merge-af-threshold`, `--ivar-bad-quality-threshold`, and `--ivar-no-ignore-strand-bias`. | false | `cli-help/variants.txt` `==== variants call ====` lists all five, with defaults 0.75, 0.25, and 20 for the three iVar thresholds | Add rows: `--min-depth <int>` minimum depth threshold. `--ivar-consensus-af <float>` allele frequency above which an iVar haplotype counts as consensus (default 0.75). `--ivar-merge-af-threshold <float>` maximum allele frequency distance for merging adjacent iVar SNPs (default 0.25). `--ivar-bad-quality-threshold <int>` iVar ALT_QUAL below this fails the bq filter (default 20). `--ivar-no-ignore-strand-bias` applies the iVar strand-bias filter, which is off by default for amplicon data. |
| 53 | "`--min-af <float>` \| Minimum allele frequency threshold (iVar default: 0.05)." | changed | `cli-help/variants.txt` `==== variants call ====` prints `--min-af <min-af>  Minimum allele frequency threshold` with no default. The 0.05 figure comes from the iVar step, not from the flag's own default | "`--min-af <float>` \| Minimum allele frequency threshold. The flag has no CLI default; when it is omitted the iVar pipeline uses its own 0.05 threshold." |
| 54 | "`--name <name>` \| Output track name." | changed | `cli-help/variants.txt` `==== variants call ====`, `--name, --output-track-name <name>  Display name for the created variant track` | "`--name`, `--output-track-name` \| Display name for the created variant track." |
| 55 | "`--extra-args <args>` \| Additional caller options forwarded to the selected caller." | changed | `cli-help/variants.txt` `==== variants call ====`, `--extra-args, --advanced-options <extra-args>` | "`--extra-args`, `--advanced-options` \| Additional caller arguments, written exactly as they should be passed to the underlying tool." |
| 56 | "The `variants` group also includes `phase` (build a GATK HaplotypeCaller plus WhatsHap phasing plan), `extract-sample`, and `query`." | true | `cli-help/variants.txt` `==== variants ====` lists call, phase, extract-sample, query, and `==== variants phase ====` overview reads "Construct a phase-aware GATK HaplotypeCaller plus WhatsHap command plan" | |
| 57 | "`lungfish conda classify <fastq...> --db <name> [--preset <preset>] [--paired] [--profile] [-o <dir>]`" | changed | `cli-help/conda.txt` `==== conda classify ====` also lists `--recursive`, `--confidence`, `--min-hit-groups`, `--memory-mapping`, `--quick`, `--bracken-read-length` (default 150), `--bracken-level`, `--bracken-threshold` (default 10), and `--extra-args` | "`lungfish conda classify <fastq...> --db <name> [--preset <preset>] [--paired] [--recursive] [--profile] [--confidence <float>] [--min-hit-groups <n>] [--memory-mapping] [--quick] [--bracken-read-length <n>] [--bracken-level <D\|P\|C\|O\|F\|G\|S>] [--bracken-threshold <n>] [--extra-args <args>] [-o <dir>]`" |
| 58 | "`--preset` accepts `sensitive`, `balanced` (default), or `precise`." | true | `cli-help/conda.txt` `==== conda classify ====`, `--preset <preset>  Sensitivity preset: sensitive, balanced, precise (default: balanced)` | |
| 59 | "`--profile` chains Bracken abundance profiling." | true | `cli-help/conda.txt` `==== conda classify ====`, `--profile  Run Bracken abundance profiling after classification` | |
| 60 | "`-i`/`--input` takes one or two FASTQ files; `-s`/`--sample` names the sample (required)." | true | `cli-help/esviritu.txt` `==== esviritu detect ====`, `USAGE: lungfish-cli esviritu detect [<options>] --sample <sample>`, `-i, --input <input>  Input FASTQ file(s). Provide two files for paired-end.` | |
| 61 | "There is no `esviritu run`." | true | `cli-help/esviritu.txt` `==== esviritu ====` subcommand block lists only detect, download-db, db-status | |
| 62 | The `esviritu` section omits `download-db` and `db-status`. | false | `cli-help/esviritu.txt` `==== esviritu ====` lists both, and the group overview says "Download the database: lungfish esviritu download-db" | Add: "`lungfish esviritu download-db` downloads the EsViritu viral reference database, and `lungfish esviritu db-status` reports whether it is installed." |
| 63 | "`--output` is required. `--platform` defaults to `illumina`; `--confidence` defaults to `0.2`. There is no `--reads` or `--profile` flag." | true | `cli-help/taxtriage.txt` `==== taxtriage run ====`, `USAGE: lungfish-cli taxtriage run [<options>] --output <output>`. `--platform` "(default: illumina)". `--confidence` "(default: 0.2)". No `--reads` or `--profile` in the option list | |
| 64 | The `taxtriage run` signature omits `--top-hits`, `--rank`, `--skip-assembly`, `--skip-krona`, `--max-memory`, `--max-cpus`, `--nf-profile`, `--revision`, and `--recursive`. | false | `cli-help/taxtriage.txt` `==== taxtriage run ====` lists all of them, with `--top-hits` default 10, `--rank` default S, `--max-memory` default 16.GB, and `--nf-profile` default docker | Add a note listing the tuning flags: `--top-hits <n>` (default 10), `--rank <D\|P\|C\|O\|F\|G\|S>` (default S), `--skip-assembly` / `--no-skip-assembly`, `--skip-krona`, `--max-memory <nextflow-size>` (default 16.GB), `--max-cpus <n>`, `--nf-profile <profile>` (default docker), `--revision <ref>`, and `--recursive`. |
| 65 | "`lungfish nao-mgs summary <input-path>` ... Prints a quick summary of a NAO-MGS surveillance result" | true | `cli-help/nao-mgs.txt` `==== nao-mgs summary ====`, `OVERVIEW: Display summary of NAO-MGS results`, argument "Path to virus_hits_final.tsv(.gz) or results directory" | |
| 66 | "`lungfish nao-mgs import <input-path> [-o <dir>] [--sample-name <name>] [--min-bitscore <float>]` to write a standalone JSON summary outside a project." | true | `cli-help/nao-mgs.txt` `==== nao-mgs import ====`, `OVERVIEW: Import NAO-MGS results as a standalone JSON summary`, options `--sample-name`, `-o, --output-dir`, `--min-bitscore` (default 0.0) | |
| 67 | "All four of `--kreport`, `--kraken-output`, `--source`, and `--taxid` are required; `--reads` sets the subsample size (default 20)." | true | `cli-help/blast.txt` `==== blast verify ====`, `USAGE: lungfish-cli blast verify [<options>] --kreport <kreport> --source <source> --kraken-output <kraken-output> --taxid <taxid>`. `--reads` "(default: 20)" | |
| 68 | The `blast verify` description omits `--max-concurrent`, `--include-children`, and `--extra-args`. | false | `cli-help/blast.txt` `==== blast verify ====` lists all three, with `--max-concurrent` default 1 | Add: "`--max-concurrent <n>` caps in-flight BLAST submissions for this process (default 1), `--include-children` adds reads classified to descendant taxa, and `--extra-args` passes BLAST URL API parameters as `KEY=VALUE` tokens." |
| 69 | "This is not a free-form `blast <sequence>` and does not take `--database`." | true | `cli-help/blast.txt` `==== blast ====` exposes only `verify`, and `==== blast verify ====` has no `--database` option | |
| 70 | "You must pick exactly one mode; omitting it fails with `Error: Validation failed: Exactly one of --by-id, --by-region, --by-db, or --by-classifier must be specified`." | changed | `cli-help/extract.txt` `==== extract reads ====` overview states "Exactly one of --by-id, --by-region, --by-db, or --by-classifier must be specified", which confirms the rule. The exact error string is not printed in the help dump | Keep the rule and drop the verbatim error string, or verify the string by running the command. |
| 71 | "`--bundle` here is a boolean flag that wraps the output in a `.lungfishfastq` bundle, not a way to point at a taxonomy bundle." | true | `cli-help/extract.txt` `==== extract reads ====`, `--bundle  Wrap output in a .lungfishfastq bundle` | |
| 72 | "The `--taxon` flag applies to `--by-classifier --tool kraken2`; for the other tools (`esviritu`, `taxtriage`, `naomgs`, `nvd`) select reads with `--accession` instead." | true | `cli-help/extract.txt` `==== extract reads ====`, `--taxon <taxon>  Taxonomy ID (repeatable, for --by-classifier --tool kraken2)` and `--accession <accession>  Reference accession / contig name (repeatable, for --by-classifier)` | |
| 73 | "By default it keeps unmapped reads (samtools `-F 0x400`); pass `--exclude-unmapped` to apply the stricter `-F 0x404` filter that also drops unmapped mates." | true | `cli-help/extract.txt` `==== extract reads ====`, `--exclude-unmapped  Exclude unmapped reads (samtools -F 0x404 instead of -F 0x400) for --by-region` | |
| 74 | "`lungfish assemble <fastq...> [--assembler <tool>] [--read-type <type>] [--profile <profile>] [--extra-args <args>] [--output <path>]`" | changed | `cli-help/assemble.txt` `==== assemble ====` also lists `--project-name`/`--name`, `--paired`, `--memory-gb`/`--memory`, `--min-contig-length`, and `--extra-arg` (repeatable), and states `--assembler` defaults to spades | "`lungfish assemble <fastq...> [--assembler <tool>] [--read-type <type>] [--paired] [--profile <profile>] [--memory-gb <n>] [--min-contig-length <n>] [--name <name>] [--extra-args <args>] [--extra-arg <arg>...] [-o <dir>]`. `--assembler` defaults to `spades`, and `--paired` binds exactly two input files as mates of one sample." |
| 75 | "Exactly one of the two is required; supplying a bare path like `extract contigs my-assembly/` fails with `Error: Specify exactly one of --assembly or --contigs`." | changed | `cli-help/extract.txt` `==== extract contigs ====`, `USAGE: lungfish-cli extract contigs <options>` with no positional argument, so a bare path is rejected. The exact error string is not printed in the dump | Keep the rule and drop the verbatim error string, or verify the string by running the command. |
| 76 | "Name each contig with a repeatable `--contig` flag" | changed | `cli-help/extract.txt` `==== extract contigs ====` also lists `--contig-file  Text file with one contig name per line (repeatable)` and `--line-width` (default 60) | "Name each contig with a repeatable `--contig` flag, or list them one per line in a file passed to a repeatable `--contig-file`. `--line-width` sets the output FASTA line width (default 60)." |
| 77 | "The `fastq` group has 30-plus subcommands in total" | changed | `cli-help/fastq.txt` `==== fastq ====` subcommand block lists 44 names, including five `12s-*` ones (`12s-reference-metadata`, `12s-reference-bundle`, `12s-match`, `12s-export`, `12s-export-unresolved`) | "The `fastq` group has 44 subcommands in total, including a five-command 12S metabarcoding lane." |
| 78 | "The only other options are `-o`/`--output`, `--force`, and `--compress`; there is no `--seed` flag." | true | `cli-help/fastq.txt` `==== fastq subsample ====`, `USAGE: lungfish-cli fastq subsample <input> [--proportion <proportion>] [--count <count>] --output <output> [--force] [--compress]` | |
| 79 | "`lungfish fastq length-filter <input> -o <path> --min <int> [--max <int>]`" | changed | `cli-help/fastq.txt` `==== fastq length-filter ====`, `USAGE: lungfish-cli fastq length-filter <input> [--min <min>] [--max <max>] --output <output> [--force] [--compress]`. `--min` is optional too | "`lungfish fastq length-filter <input> -o <path> [--min <int>] [--max <int>]`. Both bounds are optional, so you can cap length without setting a floor." |
| 80 | "`lungfish fastq qc-summary <input...> -o <path>` ... Computes a JSON QC summary for one or more FASTQ files" | true | `cli-help/fastq.txt` `==== fastq qc-summary ====`, `USAGE: lungfish-cli fastq qc-summary <inputs> ... --output <output> [--force] [--compress]` | |
| 81 | "The database is selected by identifier with `--database-id`, not a path." | true | `cli-help/fastq.txt` `==== fastq scrub-human ====`, `--database-id <database-id>  Human read removal database identifier` | |
| 82 | "`lungfish fastq orient <input> -o <path> --reference <path>` ... The standalone top-level `lungfish orient <input> --reference <path>` runs the same operation outside the `fastq` group." | changed | The two commands differ. `cli-help/fastq.txt` `==== fastq orient ====` takes `-o, --output` (a file) and `--db-mask`. `cli-help/orient.txt` `==== orient ====` takes `-o, --output-dir` (a directory), `--mask`, and adds `--save-unoriented` | "`lungfish fastq orient <input> -o <path> --reference <path> [--word-length <n>] [--db-mask <method>] [--extra-args <args>]` writes a single oriented FASTQ. The standalone `lungfish orient <input> --reference <path> [-o <dir>] [--word-length <n>] [--mask <dust\|none>] [--save-unoriented]` runs the same vsearch orientation but writes into an output directory and can save unoriented reads separately. Both default the word length to 12 and the masking mode to `dust`." |
| 83 | "`lungfish fastq materialize <bundle> -o <path>` ... Materializes a virtual `.lungfishfastq` subset, trim, or demux bundle into a full FASTQ file on disk." | true | `cli-help/fastq.txt` `==== fastq materialize ====` overview: "Reads the derived bundle manifest, resolves the root FASTQ, and applies payload-specific materialization (subset read IDs, trim positions, or copy full payload)" | |
| 84 | "Executed runs require at least one `--expected-output` ... `--dry-run` and `--prepare-only` do not require it." | true | `cli-help/workflow.txt` `==== workflow run ====`, `--expected-output  Final output bundle or file path; required for executed runs and repeatable for every scientific output that must receive provenance` | |
| 85 | "`nf-core/viralrecon` and `viralrecon` are accepted for the Viral Recon adapter" | true | `cli-help/workflow.txt` `==== workflow run ====` overview: "The only built-in nf-core workflow supported by this command is nf-core/viralrecon, also accepted as viralrecon." | |
| 86 | "Useful viralrecon flags include `--results-dir`, `--expected-output`, `--version`, `--workdir`, `--param key=value`, `--cpus`, `--memory`, `--resume`, `--dry-run`, and `--prepare-only`." | changed | `cli-help/workflow.txt` `==== workflow run ====` confirms all ten and also lists `--executor` (default docker), `--params-file`, `--bundle-root`, `--bundle-path`, `--repeat-from`, and `--timeout` | Add `--executor <docker\|conda\|local>` (default `docker`), `--params-file <json-or-yaml>`, and `--timeout <minutes>` to the list. |
| 87 | "With `--nf-core`, lists the supported Viral Recon pipeline. Without the flag, prints a usage hint; it does not inventory project workflows." | changed | `cli-help/workflow.txt` `==== workflow list ====` confirms `--nf-core  List the supported nf-core Viral Recon pipeline`. The behavior without the flag is not stated in the help dump | Keep the `--nf-core` sentence. The no-flag behavior is settled by reading `Sources/LungfishCLI/Commands/WorkflowCommand*.swift`. |
| 88 | "`lungfish workflow validate <file>` ... Checks a local Nextflow file or Snakefile without running it. YAML workflow definitions are not accepted by this command." | changed | `cli-help/workflow.txt` `==== workflow validate ====` argument is "Workflow file to validate". The help does not name the accepted file types or exclude YAML | Keep the first sentence. The YAML exclusion is settled by reading the validator source. |
| 89 | "`lungfish workflow diff <old.lungfishflow> <new.lungfishflow> [--format text\|json\|tsv]`" | true | `cli-help/workflow.txt` `==== workflow diff ====`, `USAGE: lungfish-cli workflow diff <first> <second> [--format <format>]`, `--format` values text, json, tsv | |
| 90 | The `workflow` section omits `builder-run`. | false | `cli-help/workflow.txt` `==== workflow ====` lists `builder-run  Run a native Workflow Builder graph` | Add: "`lungfish workflow builder-run <graph>` runs a native Workflow Builder graph from the command line." |
| 91 | "Installs one or more plugin packs into `~/.lungfish/conda`. `--pack` is a boolean mode toggle; the pack names are positional arguments after it" | true | `cli-help/conda.txt` `==== conda ====` overview "Tools are stored in /Users/dho/.lungfish/conda". `==== conda install ====`, `--pack  Install a plugin pack instead of individual packages` with `<packages>` positional | |
| 92 | "`lungfish conda lock --pack <name> --output <requested-environment.json>` ... It is unresolved: it does not contain a complete installed artifact inventory or claim compatibility with conda-lock." | true | `cli-help/conda.txt` `==== conda lock ====`, `OVERVIEW: Export a requested environment specification for a plugin pack (not a resolved lock)`, `-o, --output <output>  Requested specification JSON output path`. The group overview repeats "This is not a resolved artifact lock" | |
| 93 | "`lungfish conda install --from-lockfile <file>` ... Exact reconstruction is currently unsupported and fails before creating or changing an environment." | true | `cli-help/conda.txt` `==== conda install ====`, `--from-lockfile <from-lockfile>  Unsupported exact reconstruction input; requested specifications cannot be installed as resolved locks` | |
| 94 | "`lungfish conda list` ... Lists installed packs and their versions." | false | `cli-help/conda.txt` `==== conda list ====`, `OVERVIEW: List installed packages in an environment`, `-e, --env <env>  Environment name (lists all envs if omitted)` | "`lungfish conda list [--env <name>]` lists the packages installed in one conda environment, or in every environment when `--env` is omitted." |
| 95 | "`lungfish conda remove <environment...>` ... This takes positional environment names, not `--pack`" | true | `cli-help/conda.txt` `==== conda ====`, `remove  Remove a conda environment and its tools` | |
| 96 | "`lungfish conda search <query>` ... Searches the bioconda index for available packs." | false | `cli-help/conda.txt` `==== conda search ====`, `OVERVIEW: Search for packages in bioconda and conda-forge`, `USAGE: lungfish-cli conda search <query>` | "`lungfish conda search <query>` searches bioconda and conda-forge for available packages. To list plugin packs instead, use `lungfish conda packs`." |
| 97 | "`lungfish conda setup` ... Downloads and installs micromamba into the managed conda root." | true | `cli-help/conda.txt` `==== conda ====`, `setup  Download and set up micromamba` | |
| 98 | "`lungfish conda run [--env <name>] <tool> [args...]` ... The environment defaults to the tool name" | true | `cli-help/conda.txt` `==== conda run ====`, `-e, --env <env>  Environment name (default: tool name)`, argument `<tool-and-args>  Tool name followed by its arguments` | |
| 99 | "`lungfish conda packs` ... Lists the available built-in plugin packs with their ids and bundled packages." | true | `cli-help/conda.txt` `==== conda packs ====`, `OVERVIEW: List available plugin packs` | |
| 100 | "`lungfish conda envs` ... Lists installed conda environments with package counts and on-disk sizes." | changed | `cli-help/conda.txt` `==== conda ====`, `envs  List conda environments`. The help confirms the command but not the counts or sizes | Keep the first clause. The column detail is settled by running the command or reading `Sources/LungfishCLI/Commands/CondaCommand*.swift`. |
| 101 | The plugin-pack section omits `conda export-pack`, `conda offline-export`, `conda offline-install`, `conda db`, and `conda extract`. | false | `cli-help/conda.txt` `==== conda ====` lists all five. `conda db` has its own seven subcommands (list, info, download, remove, recommend, update, install-managed) and `conda extract` filters Kraken2-classified reads out of FASTQs | Add: "`lungfish conda offline-export --pack <id> --output <dir>` and `lungfish conda offline-install <pack-directory> [--conda-root <dir>] [--overwrite]` move environments between machines without network access. `lungfish conda db <subcommand>` manages metagenomics reference databases with `list`, `info`, `download`, `remove`, `recommend`, `update`, and `install-managed`. `lungfish conda extract --kraken-output <file> --source <fastq>... --output <fastq>... --taxid <id>...` pulls Kraken2-classified reads out of a FASTQ." |
| 102 | "`--per-sequence` adds all per-record summaries, `--no-gc` skips the GC calculation ... and `--length-distribution` adds a length histogram." | true | `cli-help/analyze.txt` `==== analyze stats ====`, `--per-sequence  Show statistics per sequence`, `--gc/--no-gc  Calculate GC content (use --no-gc to skip) (default: --gc)`, `--length-distribution  Show length distribution` | |
| 103 | "`stats` is the default subcommand, so `lungfish analyze <input>` runs it. `analyze` also exposes `composition` and `validate`." | true | `cli-help/analyze.txt` `==== analyze ====`, `stats (default)`, plus composition and validate | |
| 104 | "Codons use frame +1 independently for each record; incomplete terminal codons and windows containing ambiguous symbols are excluded from frequency denominators." | true | `cli-help/analyze.txt` `==== analyze composition ====` overview: "Codons use frame +1 of each record; incomplete terminal codons and windows containing ambiguous symbols are excluded from frequency denominators." | |
| 105 | "`--alphabet` overrides the alphabet, which otherwise auto-detects from the file extension (`.faa` is protein, everything else DNA)." | changed | `cli-help/analyze.txt` `==== analyze composition ====`, `--alphabet <alphabet>  Sequence alphabet: dna, rna, protein (default: auto-detect from file extension)`. The help confirms the flag and its auto-detection but not the `.faa` rule | Keep the flag description. The `.faa` mapping is settled by reading the alphabet-detection code. |
| 106 | "`lungfish analyze validate <file>... [--strict]` ... Checks that one or more files are well-formed for their detected format, across FASTA, FASTQ, GenBank, GFF3, VCF, and BED." | changed | `cli-help/analyze.txt` `==== analyze validate ====`, `USAGE: lungfish-cli analyze validate <files> ... [--strict]`. The repeatable positional and `--strict` are confirmed. The six-format list is not printed | Keep the signature. The format list is settled by reading the validator source. |
| 107 | "Frames 1 to 3 are forward, 4 to 6 are reverse complement; all six frames are translated by default. `--table` selects an NCBI genetic-code table (default 1)." | true | `cli-help/translate.txt` `==== translate ====` overview "Reading frames 1-3 are forward strand, 4-6 are reverse complement (frames -1, -2, -3). By default, all 6 frames are translated.". `--table` "(default: 1)" | |
| 108 | The `translate` signature omits `--trim-to-stop`, `--no-stop-asterisk`, and `--longest-orf`. | false | `cli-help/translate.txt` `==== translate ====` lists all three | Add: "`--trim-to-stop` stops translation at the first stop codon, `--no-stop-asterisk` omits stop-codon asterisks, and `--longest-orf` outputs only the longest open reading frame per sequence per frame." |
| 109 | "The GUI `Cmd-Shift-T` action opens the FASTQ/FASTA operations dialog for the selected visible region" | true | `Sources/LungfishApp/App/MainMenu.swift:633-637` adds "Translate\u{2026}" with `keyEquivalent: "t"` and `keyEquivalentModifierMask = [.command, .shift]` in the Sequence menu | |
| 110 | "`lungfish sequence annotate-orfs <bundle> [--frames <list>] [--table <id>]`" | changed | `cli-help/sequence.txt` `==== sequence annotate-orfs ====` also lists `--sequence`, `--start`, `--end`, `--min-length` (default 100), `--include-partial`, and `--allow-alternative-starts`. `--frames` defaults to `+1,+2,+3,-1,-2,-3` | "`lungfish sequence annotate-orfs <bundle> [--sequence <name>] [--start <n>] [--end <n>] [--frames <list>] [--table <id>] [--min-length <nt>] [--include-partial] [--allow-alternative-starts]`. `--frames` defaults to all six (`+1,+2,+3,-1,-2,-3`), `--min-length` to 100 nucleotides, and the coordinates are 0-based with an exclusive end." |
| 111 | "The `sequence` group also includes `delete-annotations` and `delete-annotation-track`" | true | `cli-help/sequence.txt` `==== sequence ====` lists annotate-orfs, delete-annotations, delete-annotation-track | |
| 112 | "`--limit` caps the result count (default 200)." | true | `cli-help/universal-search.txt` `==== universal-search ====`, `--limit <limit>  Maximum number of results (default: 200)` | |
| 113 | "The index builds on first use and rebuilds with `--reindex`." | true | `cli-help/universal-search.txt` `==== universal-search ====`, `--reindex  Force index rebuild before querying` | |
| 114 | "Searches datasets and analysis artifacts within a single project: FASTQ datasets, reference and VCF metadata, classification results, EsViritu detections, and flattened JSON manifests." | true | `cli-help/universal-search.txt` `==== universal-search ====` overview lists exactly those five kinds | |
| 115 | "Recognized field tokens are `type:<kind>` ... Date bounds use `date>=YYYY-MM-DD` ..." | changed | `cli-help/universal-search.txt` shows example queries using `type:fastq_dataset`, `role:air_sample`, `virus:HKU1`, `sample:patient42`, and `date>=2025-01-01`, which corroborates the syntax. The full token grammar is not printed | Keep the paragraph. The exact matching rules (exact for `role`, substring for the rest) are settled by reading the query parser. |
| 116 | "`--stats` adds diagnostics: query and total timing, indexed entity and attribute counts, and per-kind counts." | true | `cli-help/universal-search.txt` `==== universal-search ====`, `--stats  Include indexing/query timing and entity-count diagnostics` | |
| 117 | "`lungfish align mafft <fasta...> --project <path> [--output <path>] [--name <name>] [--strategy <strategy>]`" | changed | `cli-help/align.txt` `==== align mafft ====` also lists `--output-order`, `--sequence-type`, `--adjust-direction`, `--symbols`, `--allow-nondeterministic-threads`, `--allow-fastq-assembly-inputs`, `--sequence` (repeatable), `--extra-mafft-options`, and `--extra-args` | "`lungfish align mafft <fasta...> --project <path> [--output <path>] [--name <name>] [--strategy <strategy>] [--output-order <input\|aligned>] [--sequence-type <auto\|nucleotide\|protein>] [--adjust-direction <off\|fast\|accurate>] [--symbols <strict\|any>] [--sequence <name>...] [--extra-mafft-options <args>]`. `--sequence` is repeatable and restricts the alignment to the named records." |
| 118 | "`mafft` is the default subcommand, so `lungfish align <fasta...> --project <path>` runs it." | true | `cli-help/align.txt` `==== align ====`, `mafft (default)` | |
| 119 | "`--strategy` accepts `auto`, `linsi`, `ginsi`, `einsi`, `fftns2`, or `parttree`." | true | `cli-help/align.txt` `==== align mafft ====`, `--strategy <strategy>  MAFFT strategy: auto, linsi, ginsi, einsi, fftns2, parttree (default: auto)` | |
| 120 | "The ten subcommands are `haplotype-caller`, `joint-genotype`, `filter`, `select`, `variants-to-table`, `bqsr`, `markdup`, `validate-sam`, `leftalign`, and `collect-metrics`." | true | `cli-help/gatk.txt` `==== gatk ====` subcommand block matches exactly, in that order | |
| 121 | "Builds reproducible GATK4 command lines by default; pass `--execute` on a subcommand to run GATK through the managed `gatk-core` environment" | true | `cli-help/gatk.txt` `==== gatk ====` overview: "Builds reproducible GATK command lines by default. Pass --execute on a subcommand to run GATK through the managed gatk-core environment and write final-location provenance." | |
| 122 | "`lungfish freyja demix --variants <tsv> --depths <tsv> --output-dir <dir> [--execute] [--sample <name>]` ... By default it writes and prints the plan without running Freyja; `--execute` runs it through the wastewater-surveillance tool pack." | true | `cli-help/freyja.txt` `==== freyja demix ====`, `--execute  Run Freyja through the wastewater-surveillance tool pack.`. `--dry-run  Write and print the command plan without running Freyja.` | |
| 123 | "`lungfish nvd summary <path>` / `lungfish nvd import <path> [-o <dir>]` ... `summary` is the default subcommand." | true | `cli-help/nvd.txt` `==== nvd ====` lists `import` and `summary (default)` | |
| 124 | "Summarizes or imports a CZ-ID taxon report (a TSV, a ZIP export, or an extracted export folder). These commands read existing CZ-ID outputs; they do not submit data to CZ-ID." | true | `cli-help/cz-id.txt` `==== cz-id ====` overview: "This command imports existing CZ-ID outputs; it does not run or submit data to CZ-ID." | |
| 125 | "Subcommands are `get` (default), `set`, `import`, `export`, and `export-biosample`. Per-bundle metadata lives in `metadata.csv` inside each bundle; folder-level metadata lives in `samples.csv` at the folder root." | true | `cli-help/metadata.txt` `==== metadata ====` subcommand block matches. Overview says "Per-bundle metadata is stored in `metadata.csv` inside each bundle. Folder-level metadata is stored in `samples.csv` at the folder root." | |
| 126 | "The ten subcommands cover `list`, `validate`, `import`, `save`, `export`, `duplicate`, `delete`, and three bundle-management subcommands" | false | `cli-help/haplotypes.txt` `==== haplotypes ====` lists eleven, with four bundle-management subcommands: bundle-install, bundle-create, bundle-save, bundle-replace-reference | "The eleven subcommands cover `list`, `validate`, `import`, `save`, `export`, `duplicate`, `delete`, and four bundle-management subcommands (`bundle-install`, `bundle-create`, `bundle-save`, `bundle-replace-reference`)." |
| 127 | "definitions are project-scoped and sourced from the project's `.lungfishmhcref` bundles" | changed | `cli-help/haplotypes.txt` `==== haplotypes ====` overview: "Definitions may be stored as project JSON definition files or embedded in project .lungfishmhcref reference bundles". `==== haplotypes list ====` has `--scope <scope>  project (the only supported scope)` and `--include-reference-bundles` | "Definitions are project-scoped. They may live as project JSON definition files or be embedded in the project's `.lungfishmhcref` reference bundles, and `haplotypes list --include-reference-bundles` shows the embedded ones." |
| 128 | "`lungfish build-db <taxtriage\|esviritu\|kraken2> <results-path> [--force]`" | changed | `cli-help/build-db.txt` `==== build-db taxtriage ====` also lists `--no-cleanup` | "`lungfish build-db <taxtriage\|esviritu\|kraken2> <results-path> [--force] [--no-cleanup]`. `--no-cleanup` keeps the intermediate files the build produced." |
| 129 | "The seven subcommands are `list-samples`, `list-cohorts`, `apply-annotations`, `export`, `export-xlsx`, `export-pivot-xlsx`, and `export-labkey`." | false | `cli-help/genotype.txt` `==== genotype ====` lists eleven. The chapter omits `ai-haplotyping`, `replay-matrix-annotation`, `replay-manual-haplotype-assignments`, and `replay-call-overrides` | "The eleven subcommands are `list-samples`, `list-cohorts`, `ai-haplotyping`, `apply-annotations`, `replay-matrix-annotation`, `replay-manual-haplotype-assignments`, `replay-call-overrides`, `export`, `export-xlsx`, `export-pivot-xlsx`, and `export-labkey`." |
| 130 | "Read-only subcommands print to stdout; the rest merge into the annotation sidecar beside the bundle's `genotype-result.json` without modifying pipeline output." | changed | `cli-help/genotype.txt` `==== genotype ====` overview confirms the sidecar (`annotations.json`) sits next to `genotype-result.json`, but adds "The `ai-haplotyping` subcommand is a scientific write workflow: it appends a versioned AI haplotype analysis revision with provenance and review metadata" | "Read-only subcommands print to stdout, and the annotation subcommands merge into the `annotations.json` sidecar beside the bundle's `genotype-result.json` without modifying pipeline output. `ai-haplotyping` is the exception: it is a scientific write workflow that appends a versioned AI haplotype analysis revision with its own provenance and review metadata." |
| 131 | "`export-pivot-xlsx` copies the bundle's current workbook and filters only its pivot sheet by `--min-reads` and `--min-percent` (with `--percent-basis viewed-locus` or `sample-retained`); it writes a pivot-only workbook only when the bundle has no workbook." | true | `cli-help/genotype.txt` `==== genotype export-pivot-xlsx ====`, `--source-workbook` help: "The export is that workbook with only the pivot sheet changed; when the bundle has no workbook a pivot-only workbook is written instead.". `--percent-basis` values sample-retained, viewed-locus (default sample-retained) | |
| 132 | "Reads Lungfish provenance from a bundle or output directory, preferring the root `.lungfish-provenance.json` sidecar and falling back to bundle roll-ups under `provenance/`." | changed | `cli-help/provenance.txt` `==== provenance bibliography ====` argument is "Bundle or output directory containing Lungfish provenance". The preference order is not printed in the help | Keep the first clause. The sidecar-preference order is settled by reading `Sources/LungfishCLI/Commands/ProvenanceCommand*.swift`. |
| 133 | "`lungfish provenance export <input> --format shell\|python\|nextflow\|snakemake\|methods\|json --output <dir>`" | true | `cli-help/provenance.txt` `==== provenance export ====`, `-f, --format, --export-format <format>  Export format: shell, python, nextflow, snakemake, methods, json`. `--output <output>  Output directory for the export bundle` | |
| 134 | "By default Lungfish expects `<artifact>.signature.json` and `<artifact>.pub` beside the artifact." | true | `cli-help/provenance.txt` `==== provenance verify ====`, `--signature  Signature artifact path; defaults to <sidecar>.signature.json`. `--public-key  Public key artifact path; defaults to <sidecar>.pub` | |
| 135 | "There is not currently a `lungfish provenance show` command" | true | `cli-help/provenance.txt` `==== provenance ====` lists only bibliography, export, verify | |
| 136 | "The input is positional and the output file is named by `--to` (required); there is no `--in`/`--out`." | true | `cli-help/convert.txt` `==== convert ====`, `USAGE: lungfish-cli convert [<options>] <input> --to <to>`, `--to <to>  Output file path (required)` | |
| 137 | "`--to-format` accepts `fasta` (default), `genbank`, `gff3`, or `fastq`." | true | `cli-help/convert.txt` `==== convert ====`, `--to-format <to-format>  Output format: fasta, genbank, gff3, fastq (default: fasta)` | |
| 138 | "The input format is auto-detected from the extension across FASTA (`.fa`, `.fasta`, `.fna`, `.faa`), GenBank (`.gb`, `.gbk`, `.genbank`), FASTQ (`.fastq`, `.fq`), and `.lungfishref` bundles; a trailing `.gz` is stripped before detection" | changed | `cli-help/convert.txt` `==== convert ====` overview says "Format is auto-detected from input file extension or can be specified", but does not enumerate the extensions or the `.gz` rule | Keep the auto-detection statement. The extension list and the `.gz` stripping are settled by reading `Sources/LungfishIO/Registry/FileTypeUtility.swift` and the convert command source. |
| 139 | "Input and output must be different files" is missing from the chapter. | false | `cli-help/convert.txt` `==== convert ====` overview: "Input and output must be different files; in-place, symlink and hard-link aliases are rejected." | Add: "The input and output must be different files. In-place conversions and symlink or hard-link aliases of the input are rejected." |
| 140 | "`lungfish search <pattern> --in <path>` ... Searches a FASTA or FASTQ for sequence patterns." | false | `cli-help/search.txt` `==== search ====`, `USAGE: lungfish-cli search [<options>] <input> <pattern>` with arguments "Input file (FASTA format)" and "Search pattern (exact sequence, IUPAC motif, or regex)". There is no `--in` flag, the order is input then pattern, and the input is FASTA | "`lungfish search <input> <pattern> [--regex] [--iupac] [--max-mismatches <n>] [--forward-only] [--case-sensitive] [-o <path>]` searches a FASTA for an exact string, an IUPAC motif, or a regular expression, and writes BED-format hits (chrom, start, end, name, score, strand). Both strands are searched by default for nucleotide sequences." |
| 141 | "Subcommands are `actions`, `describe`, `annotate`, `export`, `consensus`, `extract`, `mask`, `trim`, and `distance`." | true | `cli-help/msa.txt` `==== msa ====` subcommand block matches exactly, in that order | |
| 142 | "Annotation editing lives one level deeper: `lungfish msa annotate add`, `msa annotate edit`, `msa annotate delete`, and `msa annotate project`." | true | `cli-help/msa.txt` `==== msa annotate ====` lists add, edit, delete, project | |
| 143 | "`lungfish tree infer iqtree <msa-bundle> --project <project> --output <name>` ... The `iqtree` subcommand is required, the MSA bundle path is positional, and `--project` and `--output` are both mandatory. There is no `--msa` or `--out` flag." | changed | `cli-help/tree.txt` `==== tree infer ====` marks `iqtree (default)`, so the subcommand name may be omitted. The rest holds: `USAGE: lungfish-cli tree infer iqtree [<options>] <msa-bundle-path> --project <project> --output <output>` | "`lungfish tree infer iqtree <msa-bundle> --project <project> --output <name>`. `iqtree` is the default subcommand, so `lungfish tree infer <msa-bundle> ...` runs it. The MSA bundle path is positional, and `--project` and `--output` are both mandatory. There is no `--msa` or `--out` flag." |
| 144 | "Tune the run with `--model` (default `MFP`), `--bootstrap`, and `--alrt`." | changed | `cli-help/tree.txt` `==== tree infer iqtree ====` confirms all three (`--model` default MFP) and adds `--rows`, `--columns`, `--name`, `--sequence-type` (default auto), `--seed` (default 1), `--safe`, and `--keep-identical` | "Tune the run with `--model` (default `MFP`), `--bootstrap`, `--alrt`, `--sequence-type` (default `auto`), and `--seed` (default 1). `--rows` and `--columns` restrict the inference to named rows or 1-based aligned column ranges, `--safe` enables IQ-TREE's safe numerical mode, and `--keep-identical` keeps identical sequences in the analysis." |
| 145 | The `tree` section omits `export`, `reroot`, `extract-subtree`, and `relabel`. | false | `cli-help/tree.txt` `==== tree ====` lists all four alongside `infer` | Add: "The `tree` group also exposes `export` (export tree bundle payloads with provenance), `reroot`, `extract-subtree` (extract a selected clade as a new `.lungfishtree` bundle), and `relabel` (relabel tips from the bundle's `metadata.tsv`)." |
| 146 | "Reports the host environment: macOS version, CPU cores, memory, and a Container Support line that states whether Apple Containerization is available (macOS 26 or later). `--check-tools` probes common bioinformatics tools on `PATH`; `--tool` checks a single named tool. This is the default `debug` subcommand." | changed | `cli-help/debug.txt` `==== debug ====` marks `env (default)`. `==== debug env ====` confirms `--check-tools  Check bioinformatics tools availability` and `--tool  Check specific tool`. The reported field list is not printed in the help | Keep the flag and default-subcommand sentences. The reported fields are settled by running the command or reading `Sources/LungfishCLI/Commands/DebugCommand*.swift`. |
| 147 | "`--test-image` overrides the image reference (default `docker.io/condaforge/miniforge3:latest`, which must have arm64/linux support)." | true | `cli-help/debug.txt` `==== debug container ====`, `--test-image <test-image>  Image to use for testing (must have arm64/linux support) (default: docker.io/condaforge/miniforge3:latest)` | |
| 148 | "The remaining `debug` subcommands are `fastq-ingest` and `workflow-log`." | false | `cli-help/debug.txt` `==== debug ====` also lists `resource-smoke  Verify packaged runtime resources without launching the app UI` | "The remaining `debug` subcommands are `resource-smoke`, `fastq-ingest`, and `workflow-log`." |
| 149 | "Every command accepts these flags." (global flag table) | changed | `cli-help/_root.txt` `OPTIONS:` block lists `--format`, `-v/--verbose`, `-q/--quiet`, `--progress`, `--no-progress`, `--debug`, `--log-file`, `--no-color`, `-t/--threads`, `--version`, `-h/--help`. `--project` is not a global flag. It is a per-command option on `import fastq`, `align mafft`, `tree infer iqtree`, and others. `--format`, `--progress`, and `--no-progress` are missing from the table | Replace `--project <path>` with `--format <text\|json\|tsv>` (default `text`), and add `--progress` / `--no-progress` (progress bar, auto-detected from the TTY by default). Note that `--project` is a per-command option, not a global one. |
| 150 | "`--verbose` \| Increase output verbosity (repeatable: `-v`, `-vv`, `-vvv`)" | true | `cli-help/_root.txt`, `-v, --verbose  Increase output verbosity (can be repeated: -v, -vv, -vvv)` | |
| 151 | "`--threads <n>` \| Number of threads (default: auto)" | true | `cli-help/_root.txt`, `-t, --threads <threads>  Number of threads to use (default: auto)` | |

### Missing from this chapter

| Feature or setting | Evidence it exists |
|---|---|
| `lungfish tools update [--plan] [--apply --yes] [--json] [--required-only] [--include-databases] [--storage-root <dir>]`, the whole `tools` command group | `cli-help/tools.txt` `==== tools ====` and `==== tools update ====`, including its documented exit codes 0, 10, 2, and 1 |
| `lungfish fetch search <query> [--db <database>] [--limit <n>] [--organism <name>]` | `cli-help/fetch.txt` `==== fetch search ====` |
| `lungfish fetch sra info <accession>` | `cli-help/fetch.txt` `==== fetch sra ====` subcommand block |
| `lungfish fetch genome --no-bundle` | `cli-help/fetch.txt` `==== fetch genome ====` |
| `lungfish import msa`, `import tree`, `import sample-metadata`, `import metadata`, `import kraken2`, `import esviritu`, `import taxtriage`, `import geneious` | `cli-help/import.txt` `==== import ====` subcommand block |
| `lungfish import fastq --recipe <vsp2\|wgs\|hifi\|none>`, `--quality-binning <illumina4\|eightLevel\|none>`, `--log-dir`, `--dry-run`, `--no-optimize-storage`, `--clumping-tool`, `--compression`, `--force`, `--recursive` | `cli-help/import.txt` `==== import fastq ====` |
| `lungfish bam annotate-best` and `lungfish bam annotate-cds-best` | `cli-help/bam.txt` `==== bam ====` subcommand block |
| `lungfish bam filter` | `cli-help/bam.txt` `==== bam ====`, "Derive a filtered BAM alignment track from a bundle or mapping analysis directory" |
| `lungfish bam annotate --output-track-id`, `--primary-only`, `--include-sequence`, `--include-qualities`, `--replace` | `cli-help/bam.txt` `==== bam annotate ====` |
| `lungfish workflow builder-run` | `cli-help/workflow.txt` `==== workflow ====` |
| `lungfish workflow run --params-file`, `--timeout`, `--executor` default `docker` | `cli-help/workflow.txt` `==== workflow run ====` |
| `lungfish conda offline-export`, `conda offline-install`, `conda export-pack` | `cli-help/conda.txt` `==== conda ====` subcommand block. The CI appendix uses `offline-export` and `offline-install`, so the CLI reference should list them |
| `lungfish conda db` and its seven subcommands (`list`, `info`, `download`, `remove`, `recommend`, `update`, `install-managed`) | `cli-help/conda.txt` `==== conda db ====` |
| `lungfish conda extract --kraken-output --source --output --taxid [--include-children] [--kreport] [--no-read-pairs]` | `cli-help/conda.txt` `==== conda extract ====` |
| `lungfish esviritu download-db` and `esviritu db-status` | `cli-help/esviritu.txt` `==== esviritu ====` |
| `lungfish taxtriage check-prerequisites` | `cli-help/taxtriage.txt` `==== taxtriage ====` |
| `lungfish debug resource-smoke` | `cli-help/debug.txt` `==== debug ====` |
| `lungfish tree export`, `tree reroot`, `tree extract-subtree`, `tree relabel` | `cli-help/tree.txt` `==== tree ====` |
| `lungfish genotype ai-haplotyping` and its provider, model, Azure-endpoint, chunking, and review-scope options | `cli-help/genotype.txt` `==== genotype ai-haplotyping ====` |
| `lungfish genotype replay-matrix-annotation`, `replay-manual-haplotype-assignments`, `replay-call-overrides` | `cli-help/genotype.txt` `==== genotype ====` |
| `lungfish genotype export` (the generic `--export-format xlsx\|csv\|tsv` view-projection export, distinct from `export-xlsx`) | `cli-help/genotype.txt` `==== genotype export ====` |
| `lungfish haplotypes bundle-install`, `bundle-create`, `bundle-save`, `bundle-replace-reference` | `cli-help/haplotypes.txt` `==== haplotypes ====` |
| The five `fastq 12s-*` subcommands (`12s-reference-metadata`, `12s-reference-bundle`, `12s-match`, `12s-export`, `12s-export-unresolved`) | `cli-help/fastq.txt` `==== fastq ====` subcommand block |
| `lungfish fastq genotype`, `genotype-cohort`, `ont-genotype`, `ont-barcode-genotype`, `full-length-ont-mhc-genotype`, `update-current-workbook`, `mhc-reference-bundle` | `cli-help/fastq.txt` `==== fastq ====` subcommand block |
| `lungfish fastq pbaa-cluster`, `savont-cluster`, `scout`, `import-ont`, `ont-fluidigm-samples`, `ont-pacbio-barcode-demux` | `cli-help/fastq.txt` `==== fastq ====` subcommand block |
| `lungfish fastq deacon-ribo`, `sequence-filter`, `search-motif`, `reverse-complement`, `translate` | `cli-help/fastq.txt` `==== fastq ====` subcommand block |
| `lungfish provision-tools --arch`, `--force-rebuild`, `--list-tools`, `--status` | `cli-help/provision-tools.txt` `==== provision-tools ====` |
| `lungfish variants extract-sample` and `variants query` are named but never given a signature | `cli-help/variants.txt` `==== variants ====` |
| `lungfish msa columns` (surfaced by `msa extract` and `msa trim`) | `cli-help/msa.txt` shows `columns` in the subcommand extraction |

### Screenshots

| Marker or planned shot | Still valid? | Why |
|---|---|---|
| (none) | not applicable | The chapter's front matter has `shots: []` and no `planned_shots` key, and the body contains no `<!-- SHOT -->` or `<!-- planned: -->` markers. A pure command reference does not need screenshots |

## 06-running-in-ci.md

Arbiters: `cli-help/run-headless.txt`, `cli-help/workflow.txt`, `cli-help/conda.txt`, `Sources/LungfishWorkflow/Conda/CondaRootMutationLock.swift`, `Sources/LungfishWorkflow/Resources/ManagedTools/third-party-tools-lock.json`, `Sources/LungfishCore/Storage/ManagedStorageConfigStore.swift`.

### Claims

| # | Claim (quoted from the chapter) | Verdict (true, false, changed) | Evidence | Corrected wording |
|---|---|---|---|---|
| 1 | "The explicit CI entry point is `lungfish run-headless <workflow> ...`, a discoverable alias for `lungfish workflow run --quiet <workflow> ...`." | true | `cli-help/run-headless.txt` `==== run-headless ====` overview: "Thin alias for `lungfish workflow run --quiet <workflow> ...`. Use `lungfish workflow run --help` for the full workflow run option set." | |
| 2 | "Pass the same workflow flags after the workflow path, including `--expected-output` for executed scientific runs." | true | `cli-help/run-headless.txt`, argument `<workflow-run-arguments>  Additional workflow run options passed through after the workflow argument`. `cli-help/workflow.txt` `==== workflow run ====`, `--expected-output  ... required for executed runs` | |
| 3 | "Lungfish CLI workflows run without a display server." | changed | Nothing in the help dump asserts this, and `cli-help/workflow.txt` `==== workflow run ====` says the opposite kind of dependency exists: "Run a Nextflow or Snakemake workflow with the specified parameters. Workflows are executed using Apple Containerization (macOS 26+)." A headless run still needs a container runtime | "Lungfish Genome Explorer CLI workflows run without the app's window, so a CI runner needs no display. They do still need a container runtime or a conda executor, chosen with `--executor`." |
| 4 | "Every headless run writes the same provenance sidecars as the app: tool names and versions, argv, resolved options, runtime identity, input and output paths, checksums, file sizes, exit status, wall time, and useful stderr." | changed | `cli-help/workflow.txt` `==== workflow run ====` confirms the sidecar mechanism through `--expected-output` ("required for executed runs and repeatable for every scientific output that must receive provenance"), but the ten-field list is not corroborated by any file read | Keep the first clause. The field list is settled by reading the provenance writer, and it must match the one in power-user-notes.md and file-formats.md. |
| 5 | "Keep the resulting `.lungfish*` bundle or run directory as a CI artifact when the job produces scientific output." | true | `cli-help/workflow.txt` `==== workflow run ====`, `--bundle-root  Directory where the .lungfishrun bundle should be created` and `--bundle-path  Exact .lungfishrun bundle path to create or update` | |
| 6 | "`lungfish conda offline-export --pack classification --output .ci/lungfish-conda-packs/classification`" | false | There is no pack named `classification`. The lock's ten `packID` values are `read-mapping`, `full-length-mhc-genotyping`, `variant-calling`, `gatk-core`, `phasing`, `assembly`, `multiple-sequence-alignment`, `phylogenetics`, `metagenomics`, and `wastewater-surveillance`. `cli-help/conda.txt` `==== conda offline-export ====` requires `--pack  Built-in tool pack ID to export` | Use a real pack ID. For a Kraken2 classification job that is `metagenomics`: `lungfish conda offline-export --pack metagenomics --output .ci/lungfish-conda-packs`. Update every later occurrence of `classification` in both CI examples to match. |
| 7 | "`--output .ci/lungfish-conda-packs/classification`" names the pack directory itself. | changed | `cli-help/conda.txt` `==== conda offline-export ====`, `-o, --output <output>  Directory where the offline pack directory will be written`. The value is the parent directory, and the pack directory is created inside it | "`--output` names the directory the pack directory will be written into, so pass `.ci/lungfish-conda-packs` and let the command create the pack folder under it." |
| 8 | "`lungfish conda offline-install .ci/lungfish-conda-packs/classification --conda-root \"$LUNGFISH_CONDA_ROOT\"`" | changed | `cli-help/conda.txt` `==== conda offline-install ====`, `USAGE: lungfish-cli conda offline-install <pack-directory> [--conda-root <conda-root>] [--overwrite]`, argument "Path to an offline pack directory created by 'conda offline-export'". The shape is right. Only the pack name is wrong (see row 6), and `--overwrite` is missing | "`lungfish conda offline-install .ci/lungfish-conda-packs/metagenomics --conda-root \"$LUNGFISH_CONDA_ROOT\" --overwrite`. `--overwrite` replaces environments with matching names, which a re-run of a cached CI job will hit." |
| 9 | "`export LUNGFISH_CONDA_ROOT=\"$RUNNER_TEMP/lungfish-conda\"`" | true | `Sources/LungfishCore/Storage/ManagedStorageConfigStore.swift:185` reads `environment["LUNGFISH_CONDA_ROOT"]` as an override, and `:126` reports it back. `Sources/LungfishApp/App/DebugDependencySharing.swift:218` checks the same variable | |
| 10 | "The offline install and export commands take the same `<conda-root>/.install.lock` used by interactive plugin installs." | true | `CondaRootMutationLock.swift:22`, `public static let filename = ".install.lock"` | |
| 11 | "If a second process is already mutating the root, Lungfish prints `waiting for conda lock held by pid <n>` and blocks until the first operation exits." | true | `CondaRootMutationLock.swift:95`, `waitMessageWriter("waiting for conda lock held by pid \(pid)")` | |
| 12 | "On shared read-only roots, mutation commands fail with `conda root is read-only; reinstall as the admin user`." | true | `CondaRootMutationLock.swift:12`, `return "conda root is read-only; reinstall as the admin user"` | |
| 13 | "`lungfish run-headless workflows/classify-sample.yaml --input samplesheet.csv --results-dir outputs/classify-sample --expected-output outputs/classify-sample`" | false | `cli-help/workflow.txt` `==== workflow run ====`, argument `<workflow>  Workflow file (*.nf, Snakefile) or supported nf-core workflow: nf-core/viralrecon`. A `.yaml` workflow file is not an accepted input, and cli-reference.md independently states that "YAML workflow definitions are not accepted" | Use a real workflow argument in all three examples: either a Nextflow file (`pipeline.nf`), a `Snakefile`, or `nf-core/viralrecon`. For the viralrecon path, note that it requires exactly one `--input` samplesheet. |
| 14 | "`runs-on: macos-26`" in the GitHub Actions example. | changed | `cli-help/workflow.txt` `==== workflow ====` says "Requires macOS 26 or later for container support", so pinning macOS 26 is right in spirit. Whether a runner label `macos-26` exists is a GitHub fact outside this repository | Keep the intent and note why: container support needs macOS 26 or later. Verify the runner label against GitHub's current image list before publishing. |
| 15 | "`xcode: \"26.0.0\"`" in the CircleCI example. | unverifiable | Same class of claim as row 14, external to this repository | Verify against CircleCI's macOS image list before publishing. |
| 16 | "`path: outputs/ **/*.lungfish-provenance.json`" as the upload-artifact paths. | true | The `.lungfish-provenance.json` filename is corroborated by `cli-help/ops.txt` `==== ops stats ====`, whose argument is "Project or bundle directory containing .lungfish-provenance.json sidecars" | |
| 17 | "CircleCI uses separate `restore_cache` and `save_cache` steps." | true | External CI fact, correctly stated, and consistent with the example given | |
| 18 | "Do not cache a mutable live conda root across CI jobs unless the cache is restored read-only and managed by an admin process." | true | Corroborated by the read-only error path at `CondaRootMutationLock.swift:12` and the mutation lock at `:22`, which together show a live root is a mutable, lock-guarded resource | |
| 19 | "offline packs are portable artifacts with their own provenance" | changed | `cli-help/conda.txt` `==== conda offline-export ====` confirms portability ("Export installed conda environments for offline transfer") but says nothing about provenance for the pack itself | Keep the portability claim. The pack's own provenance is settled by reading the offline-export writer. |
| 20 | "use `workflow run` directly when you prefer the full command name; use `run-headless` in CI scripts" | true | `cli-help/run-headless.txt` overview confirms the two are the same command with `--quiet` prefilled | |

### Missing from this chapter

| Feature or setting | Evidence it exists |
|---|---|
| `--executor <docker\|conda\|local>`, which decides whether the CI job needs a container runtime at all. This is the single most important CI flag and the chapter never names it. Its default is `docker` | `cli-help/workflow.txt` `==== workflow run ====`, `--executor  Execution profile for nf-core workflows: docker, conda, or local (default: docker)` |
| `--dry-run` and `--prepare-only`, the two ways to validate a workflow in CI without paying for a full run. `--prepare-only` creates the run bundle and command preview without launching Nextflow | `cli-help/workflow.txt` `==== workflow run ====` |
| `--timeout <minutes>`, which is how a CI job stops a hung workflow rather than waiting for the runner's own timeout | `cli-help/workflow.txt` `==== workflow run ====` |
| `--resume`, which pairs with a cached work directory to restart a failed run | `cli-help/workflow.txt` `==== workflow run ====` |
| `lungfish tools update --plan`, which exits 10 when work is pending. That exit code is designed for exactly this job, letting a CI step assert the runner matches the pinned dependency set before any scientific work starts | `cli-help/tools.txt` `==== tools update ====`, "Exit codes: 0 nothing to do, or the update was applied; 10 work is pending (--plan only); 2 usage error, such as --apply without --yes; 1 an item failed to install" |
| `lungfish debug env --check-tools` and `lungfish debug container`, the two preflight checks a CI job should run before a workflow | `cli-help/debug.txt` `==== debug env ====` and `==== debug container ====` |
| `LUNGFISH_STORAGE_ROOT`, the second storage override beside `LUNGFISH_CONDA_ROOT` | `Sources/LungfishCore/Storage/ManagedStorageConfigStore.swift:138`, which checks both keys together |
| `NCBI_API_KEY`, which a CI job that fetches from NCBI needs to avoid rate limiting | `Sources/LungfishCore/Services/NCBI/NCBIService.swift:89`. `Sources/LungfishCLI/Commands/FetchCommand.swift:461` |
| `--format json` on every command, which is how a CI step parses a result rather than scraping text | `cli-help/_root.txt` `OPTIONS:`, `--format <format>  Output format: text, json, tsv (default: text)` |
| The ten real pack IDs a CI author must choose from when writing an `offline-export` step | Lock `packTools[].packID` values |
| `lungfish conda export-pack`, the third offline-transfer command beside `offline-export` and `offline-install` | `cli-help/conda.txt` `==== conda ====` |
| `lungfish conda db download` and `conda db install-managed`, which stage the reference databases a classification workflow needs. An offline conda pack carries tools, not databases, so a CI job that classifies reads needs both | `cli-help/conda.txt` `==== conda db ====` |

### Screenshots

| Marker or planned shot | Still valid? | Why |
|---|---|---|
| (none) | not applicable | Front matter has `shots: []`, no `planned_shots` key, and the body carries no markers. A CI chapter is YAML and shell, and a screenshot of a CI web interface would go stale on the vendor's next redesign |

## ai-assistant.md

Arbiters: `Sources/LungfishCore/Services/AI/` (providers), `Sources/LungfishApp/Services/AI/AIToolRegistry.swift` (callable tools), `Sources/LungfishApp/Services/AI/AIAssistantService.swift` (system prompt, gating, fallback), `Sources/LungfishApp/Views/AI/AIAssistantPanel.swift` (panel chrome), `Sources/LungfishApp/Views/Settings/AIServicesSettingsTab.swift` (settings), `Sources/LungfishCore/Models/AppSettings.swift` (defaults).

### Claims

| # | Claim (quoted from the chapter) | Verdict (true, false, changed) | Evidence | Corrected wording |
|---|---|---|---|---|
| 1 | "The AI Assistant is a chat panel inside Lungfish that answers questions about the dataset you have open." | true | `AIAssistantPanel.swift:37-48`, "Manages the floating AI assistant panel window", `AIAssistantWindowController: NSWindowController` building an `NSPanel`. The input bar, message bubbles, and scroll view are laid out at `AIAssistantPanel.swift:176-238` | |
| 2 | "it reads the state of the active viewer (the loaded bundle, the organism, the region you are viewing, and the rows selected in the variant or sample tables)" | true | `AIToolRegistry.swift:168` `get_current_view`, `:176` `get_variant_table_context` (with a `selection_scope` parameter), `:198` `get_sample_table_context` (with `selection_scope` and `visible_only`). `AIAssistantService.swift:659-678` branches the system prompt on a detected macaque, human, or mouse dataset | |
| 3 | "the assistant does not include a model, and Lungfish does not run one for you. You connect your own account with an external AI provider by entering an API key" | true | `AIServicesSettingsTab.swift:129-168` has one API-key field per provider, and `AIServicesSettingsTab.swift:73-77` loads them from `KeychainSecretStorage`. `Sources/LungfishCore/Services/AI/` contains only `AnthropicProvider.swift`, `OpenAIProvider.swift`, and `GeminiProvider.swift`, all remote HTTP clients, with no local-model path | |
| 4 | "Any supported provider works, and the panel stays neutral about which one you pick." | true | `AIAssistantService.swift:257` builds the fallback order as `[preferred] + [.anthropic, .openAI, .gemini].filter { $0 != preferred }`, so any of the three can be preferred | |
| 5 | "Lungfish supports three: Anthropic, OpenAI, and Google Gemini." | changed | The three provider identifiers are correct (`AIAssistantService.swift:257`, and the three provider files). The chapter never mentions that OpenAI can be pointed at an Azure endpoint, which `AIServicesSettingsTab.swift:171-175` exposes as an "Azure AI" section with a "Use Azure AI-hosted endpoint" toggle, backed by `AppSettings.swift:303-309` (`openAIHostedEndpointEnabled`, `openAIHostedEndpointKind` defaulting to `"azure"`, `openAIHostedEndpoint`) and `Sources/LungfishCore/Services/AI/OpenAIEndpointConfiguration.swift` | "Lungfish Genome Explorer supports three providers: Anthropic, OpenAI, and Google Gemini. The OpenAI path can also be pointed at a model you have deployed behind an Azure AI or Azure OpenAI endpoint, using the Azure AI section of the same settings tab." |
| 6 | "Open **Lungfish > Settings** and go to the **AI Services** tab." | true | `Sources/LungfishApp/App/MainMenu.swift:109-113` puts `Settings...` (Cmd-,) in the application menu, whose title is the app short name. `AIServicesSettingsTab.swift` is the tab | |
| 7 | "Choose your preferred provider, paste your API key, and enable AI services." | changed | The control is a toggle labeled "Enable AI-powered search" (`AIServicesSettingsTab.swift:110`), and the picker is labeled "Default provider:" (`AIServicesSettingsTab.swift:118`), with options "Anthropic Claude", "OpenAI", and "Google Gemini" (`:119-121`) | "Turn on **Enable AI-powered search**, choose a **Default provider**, and paste your API key into that provider's section." |
| 8 | "Lungfish stores the key in the system keychain, not in your project." | true | `AIServicesSettingsTab.swift:73-77` reads through `KeychainSecretStorage.shared.retrieve(forKey:)` for `openAIAPIKey`, `anthropicAPIKey`, and `geminiAPIKey`. `:215-221` writes them back with a debounced `debouncedStore(_:forKey:task:)`, and `:195` shows "Saving API key changes to Keychain…" | |
| 9 | "The assistant uses your preferred provider first, then falls back through the others in turn" | true | `AIAssistantService.swift:257`, `let fallbackOrder: [AIProviderIdentifier] = [preferred] + [.anthropic, .openAI, .gemini].filter { $0 != preferred }`, then `:314-328` walks the resolved list and moves on when `shouldFallback(for: providerError)` | |
| 10 | "A provider whose key is missing or out of credit is skipped rather than tried." | true | `AIAssistantService.swift:268-270`, `let validated = try await filterValidProviders(providers)` throwing "No configured AI provider has a valid API key with available credits." when none survive | |
| 11 | "If you have not chosen a preference, Anthropic is the default." | true | `AppSettings.swift:300`, `public var preferredAIProvider: String = "anthropic"` | |
| 12 | "If no key is set, the panel will tell you AI services are disabled and point you back to this tab." | changed | `AIAssistantService.swift:91-93` guards on `AppSettings.shared.aiSearchEnabled` and returns "AI Assistant is disabled. Enable it in Settings > AI Services." That message is about the toggle, not about a missing key. A missing key instead produces the "No configured AI provider has a valid API key with available credits." error from `:270` | "If the **Enable AI-powered search** toggle is off, the panel replies that the AI Assistant is disabled and points you at Settings > AI Services. If the toggle is on but no provider has a usable key, it reports that no configured provider has a valid API key with available credits." |
| 13 | "Nothing you type is sent anywhere until a valid key is in place." | true | `AIAssistantService.swift:91` returns before any provider is resolved when the feature is off, and `:268-270` throws before any request is dispatched when no provider validates | |
| 14 | "Choose **View > AI Assistant**." | true | `Sources/LungfishApp/App/MainMenu.swift:481-487` adds "AI Assistant" to the View menu with `keyEquivalent: "a"` and mask `[.command, .shift]` | |
| 15 | "The panel opens as a floating window beside your main window and stays visible while you work." | true | `AIAssistantPanel.swift:37`, "Manages the floating AI assistant panel window", and `:48` `let panel = NSPanel(` | |
| 16 | "A short welcome message greets you, along with a set of suggested questions drawn from whatever is currently loaded, so you can click one to get started instead of typing." | true | `AIAssistantPanel.swift:451-453` `addWelcomeMessage()` calling `service.welcomeMessage()`. `:133` `suggestedQueriesContainer`, `:371` `refreshSuggestedQueries()`, `:381-382` shows the container and the welcome message when there are no messages, and `AIAssistantAccessibilityID.suggestedQueryButton(_:)` at `:23-24` makes each suggestion a button | |
| 17 | "Type a question into the field at the bottom and press Return. A thinking indicator appears while your provider responds." | true | `AIAssistantPanel.swift:176` lays the input bar out last in the vertical stack. `:481-500` `showThinkingIndicator()` inserts an `NSProgressIndicator` tagged `ai-assistant-thinking-indicator`, and `:510-519` hides it | |
| 18 | "The answer follows, and you can copy it with the button on the reply." | true | `AIAssistantPanel.swift:686` `private var copyButton: NSButton?`, `:742` sets its image to the `doc.on.doc` symbol with tooltip "Copy response" at `:746`, and `:910-912` flips it to a checkmark and back after a copy | |
| 19 | "The **Clear** button at the top of the panel wipes the current conversation and starts fresh. It removes every message, restores the welcome message, and brings back the suggested questions" | true | `AIAssistantPanel.swift:285` `clearButton.title = "Clear"`. The reset path at `:371-386` calls `refreshSuggestedQueries()`, clears the bubbles, unhides `suggestedQueriesContainer`, and calls `addWelcomeMessage()` | |
| 20 | "Clearing is local to the panel and changes nothing in your project." | true | The clear path at `AIAssistantPanel.swift:371-403` touches only view state and the service's message list. No project or bundle write appears in it | |
| 21 | "Within a single request it reads the active viewer state, searches your genome data for genes and variants, reports variant statistics and gene details, moves the browser view to a gene or region you ask about, and searches PubMed for related literature." | changed | `AIToolRegistry.swift:276-296` dispatches eleven tools: `search_genes`, `search_variants`, `get_variant_statistics`, `get_gene_details`, `get_variant_table_context`, `get_sample_table_context`, `get_current_view`, `navigate_to_gene`, `navigate_to_region`, `list_chromosomes`, `search_pubmed`. The sentence covers ten of them and omits `list_chromosomes` | Add "lists the chromosomes or contigs in the loaded reference" to the sentence, so all eleven tools are covered. |
| 22 | "it also volunteers practical wet-lab follow-ups on its own: candidate assays such as expression, protein, or functional readouts, and species-appropriate reagents, prioritizing antibodies validated in your dataset's organism and flagging when only cross-reactive ones are likely." | true | `AIAssistantService.swift:562-565`, system-prompt rule 11: "When discussing a genomic feature (gene, variant, region), include practical wet-lab follow-up options (e.g., expression assays, protein assays, functional assays) and species-appropriate reagents. Prefer species-validated monoclonal antibodies or explicitly note when only cross-reactive antibodies are likely." `:659-678` branches that guidance for macaque, human, and mouse datasets | |
| 23 | "Treat those reagent suggestions as leads to confirm against vendor datasheets and current literature, never as validated picks." | true | `AIAssistantService.swift:565` continues "State that reagent …", the prompt's own caveat clause, and `:694-696` repeats the species-aware prioritization framing | |
| 24 | "it does not edit files, run analysis workflows or classifiers, import or delete bundles, or make changes on your behalf beyond moving the view. Its lookups are read-only." | true | `AIToolRegistry.swift:276-296` lists all eleven callable tools. Nine read, two navigate (`navigate_to_gene`, `navigate_to_region`). None writes a file, launches a workflow, or mutates a bundle | |
| 25 | "Because it changes nothing, its answers are not written into your project's provenance record" | true | No provenance write appears in `AIToolRegistry.swift` or in the request path of `AIAssistantService.swift` | |
| 26 | "and it searches PubMed for related literature." | true | `AIToolRegistry.swift:256-260` defines `search_pubmed` with a `query` and a `max_results` parameter ("default 5, max 10"). `:841-864` builds NCBI eutils requests with `db=pubmed` | |

### Missing from this chapter

| Feature or setting | Evidence it exists |
|---|---|
| The Azure AI section of the settings tab: the "Use Azure AI-hosted endpoint" toggle, the endpoint URL field, and the deployment-name field | `AIServicesSettingsTab.swift:171-175`. `AppSettings.swift:303-309`. `Sources/LungfishCore/Services/AI/OpenAIEndpointConfiguration.swift` |
| The per-provider model pickers, which let the reader choose a specific model rather than the provider default | `AIServicesSettingsTab.swift:136-168` (`aiAnthropicModelPicker`, `aiOpenAIModelPicker`, `aiGeminiModelPicker`), with the Gemini options enumerated at `:52-59` |
| The "remove all keys from Keychain" action and its confirmation copy | `AIServicesSettingsTab.swift:239`, "This will remove the three AI provider keys from Keychain. You will need to re-enter them to use AI features." |
| The `list_chromosomes` tool | `AIToolRegistry.swift:248`, `:294` |
| The `selection_scope` and `visible_only` parameters, which decide whether the assistant reads the selected rows or every visible row | `AIToolRegistry.swift:180-215` |
| The multi-step tool loop and its ceiling. A question that needs many lookups can hit a maximum analysis-step limit and return without a final answer | `AIAssistantService.swift:212-214`, "I reached the maximum analysis steps without a final text response. Please try again with a narrower query, fewer requested actions, or a different AI provider." |
| The privacy consequence of fallback. A failed request may already have reached one provider before a second provider receives the fallback request | `AIAssistantService.swift:72`, "A failed request may already have reached one provider before another provider receives the fallback request." |
| The panel refuses a second question while one is in flight | `AIAssistantService.swift:95-98`, "Please wait for the current request to complete." |
| The species-detection behavior. The assistant's assay and reagent guidance changes depending on whether it reads the dataset as macaque, human, or mouse | `AIAssistantService.swift:659-678` |
| `lungfish genotype ai-haplotyping`, the other place an AI provider is used, with its own `--provider`, `--model`, `--azure-openai-endpoint`, and `--azure-openai-deployment` flags | `cli-help/genotype.txt` `==== genotype ai-haplotyping ====` |

### Screenshots

| Marker or planned shot | Still valid? | Why |
|---|---|---|
| `planned_shots: ai-assistant-panel`, captioned "The AI Assistant panel docked beside a dataset viewport with a question about the active result and its answer." | changed | The panel is a floating `NSPanel` (`AIAssistantPanel.swift:37-48`), not a docked pane. Reword the caption to "floating beside" rather than "docked beside". The rest of the shot is right and the marker sits correctly at the end of "What it is" |
| `<!-- planned: ai-assistant-panel -->` marker placement | true | It sits after the "What it is" section, which is where a reader first needs to see the panel |
| `planned_shots: ai-assistant-provider-setup`, captioned "The provider settings where a bring-your-own-key credential is entered for the AI Assistant." | changed | Still the right shot, but the corrected procedure now names three controls the caption should show: the "Enable AI-powered search" toggle, the "Default provider:" picker, and one provider's key field (`AIServicesSettingsTab.swift:110`, `:118`, `:129-141`). Frame the shot to include all three, and consider a second shot for the Azure AI section |
| `<!-- planned: ai-assistant-provider-setup -->` marker placement | true | It sits at the end of "Connecting a bring-your-own-key provider", immediately after the text it illustrates |

## bibliography.md

Arbiters: `Sources/LungfishWorkflow/Resources/ManagedTools/third-party-tools-lock.json` (the definitive list of what LGE ships or manages) and `cli-help/provenance.txt`. Citations were checked for whether the tool is in the lock and whether the named publication or repository is that tool's canonical reference. No web fetch was performed, so DOI resolution was not tested.

### Claims

| # | Claim (quoted from the chapter) | Verdict (true, false, changed) | Evidence | Corrected wording |
|---|---|---|---|---|
| 1 | "This appendix collects canonical citations for upstream tools that Lungfish ships, manages, wraps, or commonly references in workflow provenance." | true | The lock's `tools`, `packTools`, and `pipelines` arrays are exactly that set, and 22 of the chapter's 26 rows map onto it | |
| 2 | "`lungfish provenance bibliography <bundle>`" | true | `cli-help/provenance.txt` `==== provenance bibliography ====`, `USAGE: lungfish-cli provenance bibliography <bundle>`, argument "Bundle or output directory containing Lungfish provenance" | |
| 3 | "The command reads the root `.lungfish-provenance.json` sidecar when present, otherwise it looks for a bundle roll-up such as `provenance/bundle.lungfish-provenance.json`." | unverifiable | `cli-help/provenance.txt` `==== provenance bibliography ====` names only the argument, not the lookup order | Settled by reading the bibliography command in `Sources/LungfishCLI/Commands/ProvenanceCommand*.swift`. |
| 4 | "It matches tool names with a local alias table, prints known citations, and lists unmatched tools so you can add any lab-specific scripts manually." | changed | The final "Tools without known citations" behavior is restated at the end of the chapter, and the two agree. The alias table is not visible in the help | Settled by reading the same source as row 3. |
| 5 | "micromamba \| Mamba and micromamba package managers. \| https://mamba.readthedocs.io/" | true | The lock's `bootstrap.micromamba` pins version 2.9.0-0, and `Sources/LungfishWorkflow/Resources/Tools/tool-versions.json` gives `sourceUrl` `https://github.com/mamba-org/mamba`. micromamba has no canonical paper, so a project page is the right fallback under the chapter's own stated rule | |
| 6 | "Nextflow \| Di Tommaso P, Chatzou M, Floden EW, et al. Nextflow enables reproducible computational workflows. Nature Biotechnology. 2017. \| 10.1038/nbt.3820" | true | Lock `tools[]` entry `id: nextflow`, version 26.04.6. This is Nextflow's canonical paper | |
| 7 | "Snakemake \| Moelder F, Jablonski KP, Letcher B, et al. Sustainable data analysis with Snakemake. F1000Research. 2021. \| 10.12688/f1000research.29032.2" | true | Lock `tools[]` entry `id: snakemake`, version 9.25.2. This is the current canonical Snakemake paper | |
| 8 | "BBTools \| Bushnell B. BBTools software package. Joint Genome Institute. \| https://sourceforge.net/projects/bbmap/" | true | Lock `tools[]` entry `id: bbtools`, version 40.02. BBTools has no canonical paper, so the project page is correct under the chapter's rule | |
| 9 | "fastp \| Chen S, Zhou Y, Chen Y, Gu J. fastp: an ultra-fast all-in-one FASTQ preprocessor. Bioinformatics. 2018. \| 10.1093/bioinformatics/bty560" | true | Lock `tools[]` entry `id: fastp`, version 1.3.6. This is fastp's canonical paper | |
| 10 | "Deacon \| Deacon host-depletion toolkit. \| https://github.com/bede/deacon" | true | Lock `tools[]` entry `id: deacon`, version 0.16.0. A repository link is the right fallback for a tool with no paper | |
| 11 | "SAMtools, BCFtools, HTSlib \| Danecek P, Bonfield JK, Liddle J, et al. Twelve years of SAMtools and BCFtools. GigaScience. 2021. \| 10.1093/gigascience/giab008" | true | Lock `tools[]` entries `samtools`, `bcftools`, and `htslib`, all at 1.24. This paper is the canonical joint citation for all three | |
| 12 | "SeqKit \| Shen W, Le S, Li Y, Hu F. SeqKit: a cross-platform and ultrafast toolkit for FASTA/Q file manipulation. PLOS ONE. 2016. \| 10.1371/journal.pone.0163962" | true | Lock `tools[]` entry `id: seqkit`, version 2.13.0. Canonical paper | |
| 13 | "Cutadapt \| Martin M. Cutadapt removes adapter sequences from high-throughput sequencing reads. EMBnet.journal. 2011. \| 10.14806/ej.17.1.200" | true | Lock `tools[]` entry `id: cutadapt`, version 5.2. Canonical paper | |
| 14 | "VSEARCH \| Rognes T, Flouri T, Nichols B, Quince C, Mahe F. VSEARCH: a versatile open source tool for metagenomics. PeerJ. 2016. \| 10.7717/peerj.2584" | true | Lock `tools[]` entry `id: vsearch`, version 2.31.0. Canonical paper | |
| 15 | "pigz \| Adler M. pigz: a parallel implementation of gzip. \| https://zlib.net/pigz/" | true | Lock `tools[]` entry `id: pigz`, version 2.8. Project page is the right fallback | |
| 16 | "SRA Tools \| NCBI Sequence Read Archive Toolkit. \| https://github.com/ncbi/sra-tools" | true | Lock `tools[]` entry `id: sra-tools`, version 3.4.1. Repository is the right fallback | |
| 17 | "UCSC bigBed/bigWig tools \| Kent WJ, Zweig AS, Barber G, Hinrichs AS, Karolchik D. BigWig and BigBed: enabling browsing of large distributed datasets. Bioinformatics. 2010. \| 10.1093/bioinformatics/btq351" | changed | Lock `tools[]` entry is `id: ucsc-bedgraphtobigwig`, version 482, with a single executable `bedGraphToBigWig`. Only that one utility ships, not a "bigBed/bigWig tools" suite. The paper is the right citation for it | "UCSC bedGraphToBigWig \| Kent WJ, Zweig AS, Barber G, Hinrichs AS, Karolchik D. BigWig and BigBed: enabling browsing of large distributed datasets. Bioinformatics. 2010. \| 10.1093/bioinformatics/btq351" |
| 18 | "nf-core/viralrecon \| Patel H, Varona S, Monzon S, et al. nf-core/viralrecon ... \| 10.5281/zenodo.3901628" | true | Lock `pipelines[]` entry `id: nf-core-viralrecon`, `repository: nf-core/viralrecon`, `revision: 3.0.0`. The Zenodo concept DOI is the pipeline's canonical citation | |
| 19 | "iVar \| Grubaugh ND, Gangavarapu K, Quick J, et al. An amplicon-based sequencing framework ... Genome Biology. 2019. \| 10.1186/s13059-018-1618-7" | true | Lock `packTools[]` entry `id: ivar`, version 1.4.4, `packID: variant-calling`. Canonical paper | |
| 20 | "LoFreq \| Wilm A, Aw PPK, Bertrand D, et al. LoFreq ... Nucleic Acids Research. 2012. \| 10.1093/nar/gks918" | true | Lock `packTools[]` entry `id: lofreq`, version 2.1.5. Canonical paper | |
| 21 | "Medaka \| Oxford Nanopore Technologies. Medaka sequence correction and consensus toolkit. \| https://github.com/nanoporetech/medaka" | true | Lock `packTools[]` entry `id: medaka`, version 2.2.2. Medaka has no paper, so the repository is correct | |
| 22 | "Kraken 2 \| Wood DE, Lu J, Langmead B. Improved metagenomic analysis with Kraken 2. Genome Biology. 2019. \| 10.1186/s13059-019-1891-0" | true | Lock `packTools[]` entry `id: kraken2`, version 2.17.1, `packID: metagenomics`. Canonical paper | |
| 23 | "minimap2 \| Li H. Minimap2: pairwise alignment for nucleotide sequences. Bioinformatics. 2018. \| 10.1093/bioinformatics/bty191" | true | Lock `packTools[]` entry `id: minimap2`, version 2.31, `packID: read-mapping`. Canonical paper | |
| 24 | "BWA \| Li H, Durbin R. Fast and accurate short read alignment with Burrows-Wheeler transform. Bioinformatics. 2009. \| 10.1093/bioinformatics/btp324" | changed | Lock `packTools[]` entry is `id: bwa-mem2`, version 2.3, not BWA. The 2009 paper is BWA's, and BWA-MEM2 has its own citation (Vasimuddin et al., IPDPS 2019) | "BWA-MEM2 \| Vasimuddin Md, Misra S, Li H, Aluru S. Efficient architecture-aware acceleration of BWA-MEM for multicore systems. IEEE IPDPS. 2019. \| 10.1109/IPDPS.2019.00041". Keep the Li and Durbin 2009 BWA paper as a secondary citation for the underlying algorithm. |
| 25 | "Bowtie 2 \| Langmead B, Salzberg SL. Fast gapped-read alignment with Bowtie 2. Nature Methods. 2012. \| 10.1038/nmeth.1923" | true | Lock `packTools[]` entry `id: bowtie2`, version 2.5.5, `packID: read-mapping`. Canonical paper | |
| 26 | "BEDTools \| Quinlan AR, Hall IM. BEDTools ... Bioinformatics. 2010. \| 10.1093/bioinformatics/btq033" | changed | BEDTools is not in the lock. It appears in neither `tools` nor `packTools`. If it reaches a run at all, it does so inside a viralrecon container image, not through LGE's own dependency set | Move it to a clearly labeled section for tools that reach a run only through an external pipeline's containers, or drop it. The chapter must not imply LGE manages it. |
| 27 | "MAFFT \| Katoh K, Misawa K, Kuma K, Miyata T. MAFFT ... Nucleic Acids Research. 2002. \| 10.1093/nar/gkf436" | changed | Lock `packTools[]` entry `id: mafft`, version 7.526, `packID: multiple-sequence-alignment`. The 2002 paper is the original MAFFT. The version-7 citation (Katoh and Standley, MBE 2013) is the one MAFFT 7 asks users to cite | "MAFFT \| Katoh K, Standley DM. MAFFT multiple sequence alignment software version 7: improvements in performance and usability. Molecular Biology and Evolution. 2013. \| 10.1093/molbev/mst010". Keep the 2002 paper as the original-method citation. |
| 28 | "IQ-TREE \| Nguyen LT, Schmidt HA, von Haeseler A, Minh BQ. IQ-TREE ... Molecular Biology and Evolution. 2015. \| 10.1093/molbev/msu300" | changed | Lock `packTools[]` entry `id: iqtree`, version 3.1.3, `packID: phylogenetics`. The 2015 paper is IQ-TREE 1. IQ-TREE 2 has its own paper (Minh et al., MBE 2020, 10.1093/molbev/msaa015), and a version-3 citation may exist | "IQ-TREE \| Minh BQ, Schmidt HA, Chernomor O, et al. IQ-TREE 2: new models and efficient methods for phylogenetic inference in the genomic era. Molecular Biology and Evolution. 2020. \| 10.1093/molbev/msaa015". Check whether IQ-TREE 3 has published its own citation before finalizing. |
| 29 | "MultiQC \| Ewels P, Magnusson M, Lundin S, Kaller M. MultiQC ... Bioinformatics. 2016. \| 10.1093/bioinformatics/btw354" | changed | MultiQC is not in the lock. Like BEDTools, it reaches a run only through the viralrecon pipeline's own containers | Move to the external-pipeline section described in row 26, or drop. |
| 30 | "Pangolin \| O'Toole A, Scher E, Underwood A, et al. ... Virus Evolution. 2021. \| 10.1093/ve/veab064" | changed | Pangolin is not in the lock | Move to the external-pipeline section, or drop. |
| 31 | "Nextclade \| Aksamentov I, Roemer C, Hodcroft EB, Neher RA. Nextclade ... Journal of Open Source Software. 2021. \| 10.21105/joss.03773" | changed | Nextclade is not in the lock. file-formats.md separately claims Nextclade can be the aligner behind a `.lungfishmsa` bundle, which is also wrong: MAFFT is the only aligner LGE ships | Move to the external-pipeline section, or drop, and fix the matching claim in file-formats.md. |
| 32 | "It is intentionally offline and conservative: when a tool has no DOI-backed canonical publication, cite the project page or source repository" | true | The chapter follows its own rule consistently for micromamba, BBTools, Deacon, pigz, SRA Tools, and Medaka, all of which lack canonical papers and all of which are cited by link | |
| 33 | "Do not copy this table alone into a methods section. Combine the citation list with the bundle provenance so the methods text names both the publication and the exact executable version used in your run." | true | `cli-help/provenance.txt` `==== provenance export ====` offers a `methods` format for exactly this, and `==== provenance bibliography ====` is the citation half | |
| 34 | "`lungfish provenance bibliography` prints a \"Tools without known citations\" section" | unverifiable | Not printed in the help dump | Settled by reading the bibliography command, or by running it against a fixture bundle. |

### Missing from this chapter

| Feature or setting | Evidence it exists |
|---|---|
| Trim Galore, in the always-installed tool set | Lock `tools[]` entry `id: trim_galore`, version 2.3.0. Cite Krueger F, Trim Galore, https://github.com/FelixKrueger/TrimGalore |
| Bracken, which runs whenever `conda classify --profile` is used | Lock `packTools[]` entry `id: bracken`, version 1.0.0, `packID: metagenomics`. Cite Lu J, Breitwieser FP, Thielen P, Salzberg SL. Bracken: estimating species abundance in metagenomics data. PeerJ Computer Science. 2017. 10.7717/peerj-cs.104 |
| EsViritu, a whole classification lane the manual documents | Lock `packTools[]` entry `id: esviritu`, version 1.3.3, `packID: metagenomics` |
| RiboDetector, behind `lungfish fastq deacon-ribo` | Lock `packTools[]` entry `id: ribodetector`, version 0.3.3, `packID: metagenomics` |
| BLAST, behind `lungfish blast verify` and the full-length MHC genotyping lane | Lock `packTools[]` entry `id: blast`, version 2.16.0, `packID: full-length-mhc-genotyping`. Cite Camacho C, Coulouris G, Avagyan V, et al. BLAST+: architecture and applications. BMC Bioinformatics. 2009. 10.1186/1471-2105-10-421 |
| Savont, the clustering tool behind full-length ONT MHC genotyping | Lock `packTools[]` entry `id: savont`, version 0.6.3, `packID: full-length-mhc-genotyping` |
| Clair3, a variant caller the CLI reference documents by name | Lock `packTools[]` entry `id: clair3`, version 2.0.2, `packID: variant-calling`. Cite Zheng Z, Li S, Su J, et al. Symphonizing pileup and full-alignment for deep learning-based long-read variant calling. Nature Computational Science. 2022. 10.1038/s43588-022-00387-x |
| GATK4, an entire ten-subcommand CLI group | Lock `packTools[]` entry `id: gatk4`, version 4.6.2.0, `packID: gatk-core`. Cite McKenna A, Hanna M, Banks E, et al. The Genome Analysis Toolkit. Genome Research. 2010. 10.1101/gr.107524.110 |
| WhatsHap, the phasing half of `lungfish variants phase` | Lock `packTools[]` entry `id: whatshap`, version 2.3, `packID: phasing`. Cite Martin M, Patterson M, Garg S, et al. WhatsHap: fast and accurate read-based phasing. bioRxiv. 2016. 10.1101/085050 |
| Freyja, which has its own CLI command | Lock `packTools[]` entry `id: freyja`, version 2.0.3, `packID: wastewater-surveillance`. Cite Karthikeyan S, Levy JI, De Hoff P, et al. Wastewater sequencing reveals early cryptic SARS-CoV-2 variant transmission. Nature. 2022. 10.1038/s41586-022-05049-6 |
| All five assemblers, which the assembly chapter and CLI reference both document | Lock `packTools[]` entries under `packID: assembly`: spades 4.3.0, megahit 1.2.9, skesa 2.5.1, flye 2.9.6, hifiasm 0.25.0 |
| TaxTriage, a pinned pipeline with its own CLI command | Lock `pipelines[]` entry `id: taxtriage`, `repository: jhuapl-bio/taxtriage`, `releaseVersion: v3.3.8` |
| pysam and openpyxl, which produce scientific output (BAM parsing and XLSX workbooks) and belong in a methods section | Lock `tools[]` entries `id: pysam` 0.24.0 and `id: openpyxl` 3.1.5 |
| The reference databases a methods section must cite by version, not just the tools that read them | Lock `databases[]`: nine Kraken2 databases at `20260626`, `esviritu-viral-v3` at `v3.2.4`, `deacon-panhuman` at `panhuman-1`, `human-scrubber` at `20260706v2`, and the NCBI taxonomy at `live` |
| A statement of what version of the citation list this is. A bibliography for a release should name the dependency set it matches | Lock top-level `dependencySet` 2026.2 and `version` 2026.9.13 |

### Screenshots

| Marker or planned shot | Still valid? | Why |
|---|---|---|
| (none) | not applicable | Front matter has `shots: []`, no `planned_shots` key, and the body carries no markers. A citation table needs none |

## file-formats.md

Arbiters: `Sources/LungfishIO/Registry/FormatIdentifier.swift` (32 format identifiers), `Sources/LungfishIO/Registry/FormatRegistry+BuiltInDescriptors.swift` (per-format `canRead` / `canWrite` and the detect-only notes), `Sources/LungfishIO/Registry/BuiltInFormats.swift`, `Sources/LungfishIO/Formats/` (the writer inventory), `Sources/LungfishIO/Formats/VCF/VCFReader.swift`.

### Claims

| # | Claim (quoted from the chapter) | Verdict (true, false, changed) | Evidence | Corrected wording |
|---|---|---|---|---|
| 1 | "FASTA \| `.fa`, `.fasta`, `.fna` \| Nucleotide or protein sequence" | changed | `FormatRegistry+BuiltInDescriptors.swift:19-23`, the FASTA descriptor's `extensions: ["fa", "fasta", "fna", "faa", "ffn", "frn", "fas"]`, which is seven, not three | "FASTA \| `.fa`, `.fasta`, `.fna`, `.faa`, `.ffn`, `.frn`, `.fas` \| Nucleotide or protein sequence" |
| 2 | "FASTQ \| `.fastq`, `.fq`, `.fastq.gz` \| Reads with per-base quality" | true | `FormatRegistry+BuiltInDescriptors.swift:33-38`, `extensions: ["fq", "fastq"]`, `capabilities: [.nucleotideSequence, .qualityScores]`. The `.gz` variant is handled by the compression layer, not by a separate descriptor | |
| 3 | "GenBank \| `.gb`, `.gbk` \| Annotated sequence record" | changed | `FormatRegistry+BuiltInDescriptors.swift:47-51`, `extensions: ["gb", "gbk", "genbank", "gbff"]`, which is four, not two | "GenBank \| `.gb`, `.gbk`, `.genbank`, `.gbff` \| Annotated sequence record" |
| 4 | "GFF3 \| `.gff`, `.gff3` \| Hierarchical features" | true | `BuiltInFormats.swift:182-185`, GFF3 descriptor with `extensions: ["gff", "gff3"]` | |
| 5 | "GTF \| `.gtf` \| Gene transfer format" | true | `BuiltInFormats.swift:220-223`, GTF descriptor with `extensions: ["gtf"]` | |
| 6 | "Lungfish converts GTF to GFF3 on import." | unverifiable | Both formats are registered independently (`FormatRegistry+BuiltInDescriptors.swift:64`, `:78`). Whether an import rewrites GTF into GFF3 is decided in the annotation-import path, not in the registry | Settled by reading the annotation importer. |
| 7 | "Lungfish always reads and writes sorted, indexed BAM, and it never keeps SAM as a deliverable. When a tool emits SAM, Lungfish runs it through `samtools sort` plus `samtools index` and then deletes the intermediate file." | changed | Both SAM (`FormatRegistry+BuiltInDescriptors.swift:139`) and BAM (`:154`) are registered, so SAM is readable. The sort-index-delete pipeline is a workflow convention rather than a registry fact | Keep the convention, and attribute it to the mapping pipeline rather than to format support. SAM is a format Lungfish Genome Explorer can read. It is simply never left behind as an output. |
| 8 | "VCF \| `.vcf` \| Variant call format" and "BGZipped VCF \| `.vcf.gz`" and "Tabix index \| `.tbi`" | true | `FormatRegistry+BuiltInDescriptors.swift:108` (VCF), `:122` (BCF). `FormatIdentifier.swift:248` (`tbi`). The `.gz` variant is the compression layer over VCF | |
| 9 | "Lungfish reads VCF 4.0, 4.1, 4.2, 4.3, and 4.4 only ... A VCFv3 file has to be converted to VCF 4.x with an external converter before import." | true | `VCFReader.swift:490`, `if fileFormat.hasPrefix("VCFv3.")`, and `:826` returns "VCFv3 is not supported. Convert to VCF 4.x with bcftools convert or vcf-convert (vcftools) before importing." The reader's default `fileFormat` is `"VCFv4.3"` (`:272`, `:419`) | |
| 10 | "Lungfish consumes VCF from `bcftools`, `lofreq`, and `ivar variants` (once the converter has run)." | true | `cli-help/variants.txt` `==== variants call ====` lists lofreq, ivar, medaka, bcftools, clair3 as callers, all of which produce VCF or are converted to it | |
| 11 | "Variants produced inside Lungfish live in the reference bundle's `variants/` subdirectory ... not in a standalone bundle." | true | `cli-help/bundle.txt` `==== bundle create ====` has a `--variant  Variant file(s) to include` option that folds VCFs into the reference bundle, and no `.lungfishvcf` or similar bundle extension exists in `FormatIdentifier.swift` | |
| 12 | "Newick \| `.nwk`, `.tree`, `.treefile` \| Tree topology with branch lengths" | unverifiable | `FormatIdentifier.swift` registers no Newick identifier. Trees are handled through the `.lungfishtree` bundle and `cli-help/import.txt` `==== import tree ====` rather than the format registry | Settled by reading the tree importer's accepted extensions. Note in the chapter that Newick is not a registry format. It is read by the tree importer. |
| 13 | "Lungfish reads Newick produced by IQ-TREE, FastTree, or RAxML" | unverifiable | Only IQ-TREE is in the tool lock (`packTools` entry `iqtree` 3.1.3). FastTree and RAxML appear nowhere in the lock, so the claim is about Newick compatibility rather than about bundled tools | Reword to "Lungfish Genome Explorer reads standard Newick, whatever produced it. IQ-TREE is the inference tool it ships." |
| 14 | The bundle-type table lists ten bundle kinds. | changed | `FormatIdentifier.swift:324-338` registers only three Lungfish bundle identifiers in the format registry: `lungfishRef`, `lungfish12SRef`, and `lungfishMHCRef`. The other seven are real bundle kinds, evidenced elsewhere (`cli-help/msa.txt` for `.lungfishmsa`, `cli-help/tree.txt` for `.lungfishtree`, `cli-help/genotype.txt` for `.lungfishgenotype`, `cli-help/primers.txt` for `.lungfishprimers`, `cli-help/fastq.txt` `==== fastq materialize ====` for `.lungfishfastq`), but they are not registry formats | Keep the table. Add a note that only `.lungfishref`, `.lungfish12sref`, and `.lungfishmhcref` are registered in the format registry. The rest are bundle kinds recognized by their own commands. |
| 15 | The bundle-type table omits `.lungfishgenotype`, `.lungfishrun`, and `.lungfishflow`. | false | `cli-help/genotype.txt` `==== genotype ====` operates on `.lungfishgenotype` bundles. `cli-help/workflow.txt` `==== workflow run ====` creates `.lungfishrun` bundles via `--bundle-root` and `--bundle-path`. `cli-help/workflow.txt` `==== workflow diff ====` reads `.lungfishflow` bundles. The chapter mentions `.lungfishflow` only in a trailing paragraph and never lists it in the table | Add three rows: Genotype result \| `.lungfishgenotype` \| ONT genotype calls, an annotation sidecar, and an XLSX workbook. Workflow run \| `.lungfishrun` \| A recorded workflow execution with its typed configuration and provenance. Workflow definition \| `.lungfishflow` \| A saved workflow graph as JSON. |
| 16 | "Every bundle carries a `manifest.json` at the root that names the bundle, declares its kind and version, and lists the files inside." | changed | True for the reference bundle and the primer bundle (`PrimerSchemeBundle.swift:100-113`), but the primer manifest has no `kind`, no `version`, and no `files` map. Its keys are `schema_version`, `name`, `display_name`, `description`, `organism`, `reference_accessions`, `primer_count`, `amplicon_count`, `source`, `source_url`, `version`, `created`, `imported`, `attachments` | Soften to "Every bundle carries a `manifest.json` at the root. The keys differ by bundle kind, so read the manifest rather than assuming a shared shape. The primer-scheme manifest, for instance, uses `schema_version` and has no `files` map." |
| 17 | The minimal manifest sample block (`kind`, `version`, `name`, `files`) | changed | No manifest in the tree matches this shape. The primer manifest uses `schema_version` and a flat field list with no `kind` and no `files`. The sample is illustrative rather than real | Label the sample plainly as illustrative, or replace it with a real manifest read from a shipped bundle. The chapter already hedges in "Manifest schema" ("painted in broad strokes"). The same hedge belongs here. |
| 18 | "the text is UTF-8 with LF line endings, with JSON pretty-printed at a two-space indent" | true | The shipped `manifest.json` files under `Sources/LungfishApp/Resources/PrimerSchemes/` are UTF-8, LF, and two-space indented | |
| 19 | "The current release ships the `QIASeqDIRECT-SARS2` built-in scheme." | false | `Sources/LungfishApp/Resources/PrimerSchemes/` holds eight built-in schemes, all with `"source": "built-in"` | "The current release ships eight built-in schemes, including `QIASeqDIRECT-SARS2` and four ARTIC versions. See [Primer Scheme Bundles](primer-schemes.md#appendix-primer-schemes) for the full list." |
| 20 | "Import ARTIC, midnight, vendor, or lab schemes through `File > Import Center > Primer Scheme`" | changed | ARTIC and Midnight already ship (`ARTIC-nCoV-2019-V3`, `ARTIC-SARS-CoV-2-V4`, `ARTIC-SARS-CoV-2-V4.1`, `ARTIC-SARS-CoV-2-V5.3.2`, `Midnight-1200-V1`), so they are not examples of what you must import. The Import Center path itself is right (`ImportCenterViewModel.swift:137`, `:931`) | "Import vendor or lab schemes through `File > Import Center > Primer Scheme`. The ARTIC and Midnight schemes already ship." |
| 21 | "The `.lungfishprimers` bundle ... `primers.bed`, `primers.fasta` (optional), `PROVENANCE.md`" | true | `PrimerSchemeBundle.swift:144-146` resolves those three paths, and `:133-134` makes the BED and the provenance file required | |
| 22 | "Virtual bundles (subset, trim, demux) keep only a small `preview.fastq` on disk and rebuild the full reads on demand. Run `lungfish fastq materialize <bundle> -o <path>` to write the full FASTQ out." | true | `cli-help/fastq.txt` `==== fastq materialize ====` overview: "Reads the derived bundle manifest, resolves the root FASTQ, and applies payload-specific materialization (subset read IDs, trim positions, or copy full payload). Produces a single output FASTQ file." The `preview.fastq` filename is not in the help, but the virtual-bundle mechanism is | |
| 23 | "Per-bundle PHA4GE-aligned metadata lives in `metadata.csv` inside the bundle, and you manage it with `lungfish metadata`." | true | `cli-help/metadata.txt` `==== metadata ====` overview: "Per-bundle metadata is stored in `metadata.csv` inside each bundle. Folder-level metadata is stored in `samples.csv` at the folder root." | |
| 24 | "`.lungfish12sref` ... Produced by `lungfish fastq 12s-reference-bundle`." | true | `cli-help/fastq.txt` `==== fastq ====`, `12s-reference-bundle  Create a .lungfish12sref bundle for 12S amplicon matching`. `FormatIdentifier.swift:331` registers `lungfish12SRef` | |
| 25 | "`.lungfishmhcref` ... Produced by `lungfish fastq mhc-reference-bundle`, and consumed by the `haplotypes` command" | true | `cli-help/fastq.txt` `==== fastq ====`, `mhc-reference-bundle  Create a .lungfishmhcref bundle for MHC amplicon genotyping`. `cli-help/haplotypes.txt` `==== haplotypes ====` overview names `.lungfishmhcref` bundles. `FormatIdentifier.swift:338` registers `lungfishMHCRef` | |
| 26 | "`.lungfishhaplotypedef` \| Holds an ONT genotyping haplotype definition set. Managed with the `lungfish haplotypes` command." | unverifiable | `cli-help/haplotypes.txt` `==== haplotypes ====` describes definitions as "project JSON definition files or embedded in project .lungfishmhcref reference bundles" and never names a `.lungfishhaplotypedef` extension. `FormatIdentifier.swift` registers no such identifier | Verify the extension exists. If the definitions are plain project JSON files, drop the row and say so. |
| 27 | "`.lungfishtax` ... Only the CZ-ID import path produces this bundle (`lungfish cz-id import` or `File > Import Center > CZ-ID`)." | changed | `cli-help/cz-id.txt` `==== cz-id import ====` reads "Import a CZ-ID taxon report TSV into a Lungfish result directory" and never names a `.lungfishtax` extension. `ImportCenterViewModel.swift:930` confirms the Import Center entry titled "CZ-ID" | Verify the `.lungfishtax` extension against the CZ-ID importer, or describe the output as a Lungfish result directory, which is what the CLI help calls it. |
| 28 | "It is not the storage format for Kraken2, EsViritu, TaxTriage, or NAO-MGS results, which the taxonomy viewport reads from their own result directories and SQLite databases." | true | `cli-help/build-db.txt` `==== build-db ====` builds SQLite databases from taxtriage, esviritu, and kraken2 result directories. `cli-help/nao-mgs.txt` reads `virus_hits_final.tsv(.gz)` from a result directory | |
| 29 | "Provenance records which aligner ran (MAFFT, MUSCLE, Nextclade) and with what parameters." | false | The tool lock pins exactly one aligner, `mafft` 7.526 under the `multiple-sequence-alignment` pack. MUSCLE and Nextclade are absent from the lock, and `cli-help/align.txt` exposes only `align mafft` | "Provenance records which aligner ran and with what parameters. MAFFT is the aligner Lungfish Genome Explorer ships, through `lungfish align mafft`." |
| 30 | The `.lungfishmsa` layout block shows `alignment.fasta`, `alignment.fasta.fai`, `metadata.tsv`, `manifest.json`, `provenance/`. | changed | `cli-help/tree.txt` `==== tree relabel ====` confirms a bundle-level `metadata.tsv`, and `cli-help/msa.txt` confirms the bundle kind, but the exact filenames inside are not printed in the help dump | Verify the internal filenames against the MSA bundle writer, or label the layout as typical rather than fixed. |
| 31 | "`lungfish provenance export` to produce an audit-ready report" | true | `cli-help/provenance.txt` `==== provenance export ====`, formats shell, python, nextflow, snakemake, methods, json | |
| 32 | The provenance sidecar sample block (`workflow`, `version`, `command`, `inputs`, `outputs`, `runtime`, `tool`, `steps`) | changed | The same sample appears in power-user-notes.md with an added `schema_version: 2`. Neither version is corroborated by any file in the tree that was read | Keep one canonical sample and mark it as illustrative, or replace both with a real sidecar. The two chapters must not disagree about whether `schema_version` is present. |
| 33 | "The `inputs[]` and `outputs[]` arrays carry a SHA-256 checksum and a byte size for every file." | changed | The sample shows `sha256` and `bytes` keys, and `cli-help/tools.txt` and `cli-help/provenance.txt` treat provenance as checksum-bearing, but no read file confirms the key names | Settled by reading the provenance writer in `Sources/LungfishCore` or `Sources/LungfishWorkflow`. |
| 34 | "macOS Finder treats the folder as a single document" | unverifiable | This requires a document-type registration in the app's `Info.plist`, which was not read | Settled by reading the bundle's `Info.plist` document type declarations. |

### Missing from this chapter

| Feature or setting | Evidence it exists |
|---|---|
| BigWig and BigBed, both registered and both detect-only. Lungfish Genome Explorer recognizes these files but cannot read their contents in process, which is exactly the kind of limit a formats appendix must state | `FormatRegistry+BuiltInDescriptors.swift:192-195` ("Binary coverage/signal format (detection only; in-process reader unavailable)") and `:208-211` ("Binary annotation format (detection only; in-process reader unavailable)") |
| BedGraph, a registered coverage format | `FormatRegistry+BuiltInDescriptors.swift:224`. `FormatIdentifier.swift:218` |
| CRAM, a registered alignment format alongside SAM and BAM | `FormatRegistry+BuiltInDescriptors.swift:173`. `FormatIdentifier.swift:156`. `cli-help/import.txt` `==== import bam ====` accepts "the BAM or CRAM file" |
| BCF, the binary form of VCF | `FormatRegistry+BuiltInDescriptors.swift:122`. `FormatIdentifier.swift:195` |
| EMBL, accepted by `import fasta` | `FormatIdentifier.swift:126`. `cli-help/import.txt` `==== import fasta ====` argument "(.fa/.fasta/.gb/.embl, optionally .gz/.bgz/.bz2/.xz/.zst)" |
| 2bit, a registered sequence format | `FormatIdentifier.swift:133` |
| CSI, the second BAM/VCF index format beside BAI and TBI | `FormatIdentifier.swift:241` |
| The five compression suffixes `import fasta` accepts, which are `.gz`, `.bgz`, `.bz2`, `.xz`, `.zst`. The chapter mentions only `.gz` | `cli-help/import.txt` `==== import fasta ====` |
| The document formats the registry carries, which matter for attachments and exports. They are PDF, plain text, Markdown, CSV, and TSV | `FormatRegistry+BuiltInDescriptors.swift:271`, `:289`, `:305`, `:321`, `:337`. `FormatIdentifier.swift:257-285` |
| The image formats the registry carries, which are PNG, JPEG, TIFF, and SVG | `FormatRegistry+BuiltInDescriptors.swift:355`, `:372`, `:389`, `:406`. `FormatIdentifier.swift:294-315` |
| Which formats Lungfish Genome Explorer can write, as distinct from read. Each descriptor carries `canRead` and `canWrite` flags, and only FASTQ has a dedicated writer in `Sources/LungfishIO/Formats/` | `FormatRegistry+BuiltInDescriptors.swift:25-26` (`canRead: true, canWrite: true`), and `Sources/LungfishIO/Formats/FASTQ/FASTQWriter.swift` plus `FASTQAtomicFileWriter.swift` are the only writer files in the Formats tree |
| The NCBI BioSample export writer, which produces a submission TSV from folder metadata | `Sources/LungfishIO/Formats/FASTQ/NCBIBioSampleExporter.swift`. `cli-help/metadata.txt` `==== metadata ====`, `export-biosample  Export folder metadata as NCBI BioSample submission TSV` |
| The `uiCategory` grouping the registry assigns each format, which is what decides where a format appears in a file picker | `FormatRegistry+BuiltInDescriptors.swift:26` (`uiCategory: .sequence`) and the same key on every descriptor |
| The OCI-layout tarball a bundle export produces, with its `oci-layout`, `index.json`, manifest, config, and layer files. The chapter's bundle table has no row for it | `cli-help/bundle.txt` `==== bundle export ====`. Power-user-notes.md describes the same artifact |

### Screenshots

| Marker or planned shot | Still valid? | Why |
|---|---|---|
| (none) | not applicable | Front matter has `shots: []`, no `planned_shots` key, and the body carries no markers. The chapter's illustrations are code blocks showing file layouts and record shapes, which serve the reader better than screenshots would |

## keyboard-shortcuts.md

Arbiter: `Sources/LungfishApp/App/MainMenu.swift`. Every `keyEquivalent` and `keyEquivalentModifierMask` in that file was extracted and matched against the chapter. An item with no explicit mask uses AppKit's implicit `[.command]`.

### Claims

| # | Claim (quoted from the chapter) | Verdict (true, false, changed) | Evidence | Corrected wording |
|---|---|---|---|---|
| 1 | "A reference of every keyboard shortcut available in Lungfish on macOS." | false | The chapter omits Hide Others (Cmd-Opt-H), Content Text Size Larger/Smaller/Default (Cmd-Opt-+ / Cmd-Opt-- / Cmd-Opt-0), and New Window for Current Project (Cmd-Opt-N). See the Missing table below | "A reference of the keyboard shortcuts available in Lungfish Genome Explorer on macOS." |
| 2 | "New Project \| Cmd-N" | true | `Sources/LungfishApp/App/MainMenu.swift:168-172`, `withTitle: "New Project"`, `keyEquivalent: "n"`, no explicit mask | |
| 3 | "Open Project \| Cmd-O" | changed | `Sources/LungfishApp/App/MainMenu.swift:175-179`, `withTitle: "Open Project Folder..."`, `keyEquivalent: "o"` | "Open Project Folder \| Cmd-O" |
| 4 | "Save Project \| Cmd-S" | false | `Sources/LungfishApp/App/MainMenu.swift` contains no menu item with the word "Save" and no `keyEquivalent: "s"` outside line 440, which is the View menu's Show Sidebar (Cmd-Ctrl-S). The File menu instead carries "About Saving…" with no shortcut at `Sources/LungfishApp/App/MainMenu.swift:197-201` | Remove the row. Lungfish Genome Explorer writes changes as they happen, so there is no Save command and no Cmd-S. The File menu explains this under "About Saving…". |
| 5 | "Close Window \| Cmd-W" | changed | `Sources/LungfishApp/App/MainMenu.swift:192-196`, `withTitle: "Close"`, `keyEquivalent: "w"` | "Close \| Cmd-W" |
| 6 | "Quit Lungfish \| Cmd-Q" | true | `Sources/LungfishApp/App/MainMenu.swift:150-154`, `withTitle: "Quit \(appIdentity.fullName)"`, `keyEquivalent: "q"` | |
| 7 | "Hide Lungfish \| Cmd-H" | true | `Sources/LungfishApp/App/MainMenu.swift:129-133`, `withTitle: "Hide \(appIdentity.fullName)"`, `keyEquivalent: "h"` | |
| 8 | "Minimize Window \| Cmd-M" | changed | `Sources/LungfishApp/App/MainMenu.swift:902-906`, `withTitle: "Minimize"`, `keyEquivalent: "m"` | "Minimize \| Cmd-M" |
| 9 | "Show or Hide Sidebar \| Cmd-Ctrl-S" | true | `Sources/LungfishApp/App/MainMenu.swift:436-442`, `withTitle: "Show Sidebar"`, `keyEquivalent: "s"`, `keyEquivalentModifierMask = [.command, .control]`, with a comment saying the title flips to "Hide Sidebar" | |
| 10 | "Show or Hide Inspector \| Cmd-Opt-I" | true | `Sources/LungfishApp/App/MainMenu.swift:446-452`, `withTitle: "Show Inspector"`, `keyEquivalent: "i"`, mask `[.command, .option]` | |
| 11 | "Operations Panel \| Cmd-Shift-P" | changed | `Sources/LungfishApp/App/MainMenu.swift:866-873`, `withTitle: "Show Operations Panel"`, `keyEquivalent: "p"`, mask `[.command, .shift]`. It lives in the Operations menu, not the View menu, so the row is filed under the wrong heading | "Show Operations Panel \| Cmd-Shift-P". Move it out of the View table into its own Operations section, because the item is in the Operations menu. |
| 12 | "Document Inspector \| Cmd-Opt-D" | true | `Sources/LungfishApp/App/MainMenu.swift:471-477`, `withTitle: "Document Inspector"`, `keyEquivalent: "d"`, mask `[.command, .option]` | |
| 13 | "AI Assistant Panel \| Cmd-Shift-A" | changed | `Sources/LungfishApp/App/MainMenu.swift:481-487`, `withTitle: "AI Assistant"`, `keyEquivalent: "a"`, mask `[.command, .shift]` | "AI Assistant \| Cmd-Shift-A" |
| 14 | "Enter Full Screen \| Cmd-Ctrl-F" | true | `Sources/LungfishApp/App/MainMenu.swift:606-613`, `withTitle: "Enter Full Screen"`, `keyEquivalent: "f"`, mask `[.command, .control]` | |
| 15 | "The Sidebar uses Control, not Shift, following the macOS standard chord." | true | `Sources/LungfishApp/App/MainMenu.swift:442`, `sidebarItem.keyEquivalentModifierMask = [.command, .control]`, with the source comment "Sidebar toggle (Control-Command-S per macOS standard)" | |
| 16 | "Focus Viewer \| Cmd-Opt-F" | true | `Sources/LungfishApp/App/MainMenu.swift:454-460`, `withTitle: "Focus Viewer"`, `keyEquivalent: "f"`, mask `[.command, .option]` | |
| 17 | "Restore Side Panes \| Cmd-Ctrl-Opt-F" | true | `Sources/LungfishApp/App/MainMenu.swift:462-468`, `withTitle: "Restore Side Panes"`, `keyEquivalent: "f"`, mask `[.command, .control, .option]` | |
| 18 | "Zoom In \| Cmd-+" | true | `Sources/LungfishApp/App/MainMenu.swift:541-545`, `withTitle: "Zoom In"`, `keyEquivalent: "+"`, no explicit mask | |
| 19 | "Zoom Out \| Cmd--" | true | `Sources/LungfishApp/App/MainMenu.swift:547-551`, `withTitle: "Zoom Out"`, `keyEquivalent: "-"`, no explicit mask | |
| 20 | "Zoom to Fit \| Cmd-0" | true | `Sources/LungfishApp/App/MainMenu.swift:553-557`, `withTitle: "Zoom to Fit"`, `keyEquivalent: "0"`, no explicit mask | |
| 21 | "Zoom Reset (10kb) \| Cmd-1" | true | `Sources/LungfishApp/App/MainMenu.swift:559-563`, `withTitle: "Zoom Reset (10kb)"`, `keyEquivalent: "1"`, no explicit mask | |
| 22 | "In a sequence view, Show as RNA is Cmd-Shift-U." | changed | `Sources/LungfishApp/App/MainMenu.swift:587-593`, `withTitle: "Show as RNA (U instead of T)"`, `keyEquivalent: "u"`, mask `[.command, .shift]`. The chord is right, the menu title is abbreviated | "In a sequence view, Show as RNA (U instead of T) is Cmd-Shift-U." |
| 23 | "In the taxonomy viewport, Cmd-Shift-Right Arrow expands all nodes and Cmd-Shift-Left Arrow collapses them." | true | `Sources/LungfishApp/App/MainMenu.swift:570-583`, "Expand All" with `NSRightArrowFunctionKey` and "Collapse All" with `NSLeftArrowFunctionKey`, both masked `[.command, .shift]`, targeting `TaxonomyViewController` | |
| 24 | "Import Center \| Cmd-Shift-I" | true | `Sources/LungfishApp/App/MainMenu.swift:206-212`, `withTitle: "Import Center\u{2026}"`, `keyEquivalent: "i"`, mask `[.command, .shift]` | |
| 25 | "Plugin Manager \| Cmd-Shift-B" | true | `Sources/LungfishApp/App/MainMenu.swift:773-778`, `withTitle: "Plugin Manager\u{2026}"`, `keyEquivalent: "b"`, mask `[.command, .shift]`, with the source comment "Cmd-Shift-B for Bioconda" | |
| 26 | "Settings \| Cmd-," | true | `Sources/LungfishApp/App/MainMenu.swift:109-113`, `title: "Settings..."`, `keyEquivalent: ","`, no explicit mask | |
| 27 | "Import and tools" groups Import Center, Plugin Manager, and Settings under one heading. | changed | Import Center is in the File menu (`Sources/LungfishApp/App/MainMenu.swift:206`), Plugin Manager is in the Tools menu (`Sources/LungfishApp/App/MainMenu.swift:773`), and Settings is in the application menu (`Sources/LungfishApp/App/MainMenu.swift:109`). Three different menus | Split the table by menu, or say plainly that Import Center is in File, Plugin Manager is in Tools, and Settings is in the Lungfish Genome Explorer menu. |
| 28 | "Go to Location \| Cmd-L" | true | `Sources/LungfishApp/App/MainMenu.swift:641-645`, `withTitle: "Go to Location\u{2026}"`, `keyEquivalent: "l"`, no explicit mask | |
| 29 | "Go to Gene \| Cmd-Option-G" | true | `Sources/LungfishApp/App/MainMenu.swift:648-654`, `withTitle: "Go to Gene\u{2026}"`, `keyEquivalent: "g"`, mask `[.command, .option]`, with the source comment "so Command-Shift-G remains Find Previous" | |
| 30 | "Extract Visible Region \| Cmd-Shift-E" | true | `Sources/LungfishApp/App/MainMenu.swift:665-671`, `withTitle: "Extract Visible Region\u{2026}"`, `keyEquivalent: "e"`, mask `[.command, .shift]` | |
| 31 | "Copy Visible Region as FASTA \| Cmd-Shift-C" | true | `Sources/LungfishApp/App/MainMenu.swift:658-664`, `withTitle: "Copy Visible Region as FASTA"`, `keyEquivalent: "c"`, mask `[.command, .shift]` | |
| 32 | "Translate \| Cmd-Shift-T" | true | `Sources/LungfishApp/App/MainMenu.swift:633-637`, `withTitle: "Translate\u{2026}"`, `keyEquivalent: "t"`, mask `[.command, .shift]` | |
| 33 | "Reverse Complement \| Cmd-Shift-R" | true | `Sources/LungfishApp/App/MainMenu.swift:627-631`, `withTitle: "Reverse Complement\u{2026}"`, `keyEquivalent: "r"`, mask `[.command, .shift]` | |
| 34 | "Lungfish writes outputs of Extract operations into the project's `Reference Sequences/` folder" | unverifiable | `Sources/LungfishApp/App/MainMenu.swift:669` wires the item to `SequenceMenuActions.extractSelection(_:)`. The destination folder is decided inside that action, not in the menu | Settled by reading `SequenceMenuActions.extractSelection` and its downstream extraction service. |
| 35 | "Undo \| Cmd-Z" | true | `Sources/LungfishApp/App/MainMenu.swift:347-351`, `keyEquivalent: "z"`, no explicit mask | |
| 36 | "Redo \| Cmd-Shift-Z" | true | `Sources/LungfishApp/App/MainMenu.swift:353-359`, `keyEquivalent: "z"`, mask `[.command, .shift]` | |
| 37 | "Cut \| Cmd-X" | true | `Sources/LungfishApp/App/MainMenu.swift:363-367`, `keyEquivalent: "x"` | |
| 38 | "Copy \| Cmd-C" | true | `Sources/LungfishApp/App/MainMenu.swift:369-373`, `keyEquivalent: "c"` | |
| 39 | "Paste \| Cmd-V" | true | `Sources/LungfishApp/App/MainMenu.swift:375-379`, `keyEquivalent: "v"` | |
| 40 | "Select All \| Cmd-A" | true | `Sources/LungfishApp/App/MainMenu.swift:387-391`, `keyEquivalent: "a"` | |
| 41 | "Find \| Cmd-F" | true | `Sources/LungfishApp/App/MainMenu.swift:399-403`, `keyEquivalent: "f"`, inside the Find submenu created at `Sources/LungfishApp/App/MainMenu.swift:397` | |
| 42 | "Find Next \| Cmd-G" | true | `Sources/LungfishApp/App/MainMenu.swift:405-409`, `keyEquivalent: "g"` | |
| 43 | "Find Previous \| Cmd-Shift-G" | true | `Sources/LungfishApp/App/MainMenu.swift:411-417`, `keyEquivalent: "g"`, mask `[.command, .shift]` | |
| 44 | "Lungfish Help \| Cmd-?" | changed | `Sources/LungfishApp/App/MainMenu.swift:984-988`, `withTitle: "Lungfish Genome Explorer Help"`, `keyEquivalent: "?"`, no explicit mask | "Lungfish Genome Explorer Help \| Cmd-?" |
| 45 | "`Cmd-?` opens this user manual in the default browser, scoped to the chapter that matches the current viewport when possible." | unverifiable | `Sources/LungfishApp/App/MainMenu.swift:986` wires the item to `HelpMenuActions.showLungfishHelp(_:)`. Whether that opens a browser and whether it is viewport-scoped is decided inside that action | Settled by reading `HelpMenuActions.showLungfishHelp`. |
| 46 | "`Cmd-Shift-letter` usually opens or toggles a panel (Operations, AI Assistant, Plugin Manager, Import Center)." | true | All four confirmed above at `Sources/LungfishApp/App/MainMenu.swift:870`, `485`, `777`, `210` | |
| 47 | "`Cmd-Opt-letter` targets inspectors (Inspector, Document Inspector)." | changed | Inspector (`Sources/LungfishApp/App/MainMenu.swift:450`) and Document Inspector (`475`) hold, but `Cmd-Opt-letter` also covers Focus Viewer (`458`), Go to Gene (`652`), Hide Others (`141`), New Window for Current Project (`920`), and the three Content Text Size items (`509`, `520`, `531`) | "`Cmd-Opt-letter` targets the inspectors and a few window-level commands: Show Inspector, Document Inspector, Focus Viewer, Go to Gene, and New Window for Current Project." |
| 48 | "enter the exact menu title (for example, `Reverse Complement`)" | changed | The macOS App Shortcuts mechanism is real, but the example title is wrong. `Sources/LungfishApp/App/MainMenu.swift:629` sets the title to `"Reverse Complement\u{2026}"`, so the string entered in System Settings has to carry the trailing ellipsis character | "Enter the exact menu title, including any trailing ellipsis. `Reverse Complement…` and `Translate…` both end in an ellipsis character, so an entry that omits it will not match." |
| 49 | "VoiceOver reads every menu item and shortcut. To hear the full menu bar, press `Ctrl-Opt-M`." | true | This is a macOS VoiceOver system shortcut, not a Lungfish Genome Explorer binding, and `Sources/LungfishApp/App/MainMenu.swift` accordingly defines no `m` chord with `[.control, .option]` | |

### Missing from this chapter

| Feature or setting | Evidence it exists |
|---|---|
| Hide Others, Cmd-Opt-H | `Sources/LungfishApp/App/MainMenu.swift:135-141`, `withTitle: "Hide Others"`, `keyEquivalent: "h"`, mask `[.command, .option]` |
| Content Text Size Larger, Cmd-Opt-+ | `Sources/LungfishApp/App/MainMenu.swift:503-509`, `withTitle: "Larger"`, `keyEquivalent: "+"`, mask `[.command, .option]`. The source comment says Option separates these app-wide accessibility commands from the viewport zoom commands |
| Content Text Size Smaller, Cmd-Opt-- | `Sources/LungfishApp/App/MainMenu.swift:514-520`, `withTitle: "Smaller"`, `keyEquivalent: "-"`, mask `[.command, .option]` |
| Content Text Size Default, Cmd-Opt-0 | `Sources/LungfishApp/App/MainMenu.swift:525-531`, `withTitle: "Default"`, `keyEquivalent: "0"`, mask `[.command, .option]` |
| New Window for Current Project, Cmd-Opt-N | `Sources/LungfishApp/App/MainMenu.swift:916-922`, `keyEquivalent: "n"`, mask `[.command, .option]` |
| The Content Text Size submenu is the accessibility answer to "the type is too small", which the Accessibility section never mentions | `Sources/LungfishApp/App/MainMenu.swift:492-535`, the whole `contentTextSizeMenu` block with its accessibility labels |
| Reset View Settings to Defaults, no shortcut | `Sources/LungfishApp/App/MainMenu.swift:597-602` |
| Add Annotation… and Find ORFs…, no shortcut | `Sources/LungfishApp/App/MainMenu.swift:675-687` |
| Clear Completed and Cancel All Operations, no shortcut | `Sources/LungfishApp/App/MainMenu.swift:876-890` |
| Open Recent, About Saving…, and the Export and Provenance submenus, no shortcut | `Sources/LungfishApp/App/MainMenu.swift:182-187`, `197-201`, `216`, `260` |
| About and Check for Updates…, no shortcut | `Sources/LungfishApp/App/MainMenu.swift:93-104` |
| Zoom and Bring All to Front, no shortcut | `Sources/LungfishApp/App/MainMenu.swift:908-912`, `925-929` |
| Workflow Library… and Search Online Databases, no shortcut | `Sources/LungfishApp/App/MainMenu.swift:766-771`, `740` |
| Getting Started and VCF Variants Guide, no shortcut | `Sources/LungfishApp/App/MainMenu.swift:993-1000` |

### Screenshots

| Marker or planned shot | Still valid? | Why |
|---|---|---|
| (none) | not applicable | Front matter has `shots: []`, no `planned_shots` key, and the body carries no markers. A chord table does not need a screenshot, though a shot of the System Settings App Shortcuts pane would help the Customizing shortcuts section |

## power-user-notes.md

Arbiters: `cli-help/variants.txt`, `cli-help/conda.txt`, `cli-help/ops.txt`, `cli-help/bundle.txt`, `cli-help/fastq.txt`, `cli-help/_root.txt`, `Sources/LungfishWorkflow/Resources/ManagedTools/third-party-tools-lock.json`, `Sources/LungfishKit/OperationFailureReportStore.swift`.

### Claims

| # | Claim (quoted from the chapter) | Verdict (true, false, changed) | Evidence | Corrected wording |
|---|---|---|---|---|
| 1 | "The numbers and flag values match dependency set 2026.2." | true | The lock's top-level `dependencySet` is `2026.2` | |
| 2 | "The iVar variant caller in Lungfish is a two-process pipeline. First `samtools mpileup` ... That pileup pipes straight into `ivar variants`." | changed | The two tools are both pinned (`tools[]` `samtools` 1.24, `packTools[]` `ivar` 1.4.4), and `cli-help/variants.txt` `==== variants call ====` exposes iVar-specific flags that imply a pileup stage. The two-process structure is not printed in any help text | Settled by reading the iVar pipeline in `Sources/LungfishWorkflow`. The claim is plausible and internally consistent with file-formats.md, which shows the same two `steps[]` entries. |
| 3 | The canonical mpileup flag block `-aa -A -d 600000 -B -Q 20 -q 0 -f reference.fasta` | unverifiable | These exact flags appear nowhere in the help dump. They are pipeline internals | Settled by reading the mpileup invocation in the iVar pipeline source. This is the chapter's central claim and must be verified before publication. |
| 4 | "`-d 600000` \| Raise depth cap from 8000 (default) to 600000" | unverifiable | Same as row 3. The 8000 default is a samtools fact, not a Lungfish Genome Explorer one | Settled by reading the pipeline source and the samtools 1.24 manual. |
| 5 | The canonical `ivar variants -p variants -q 20 -t 0.05 -m 10 -r reference.fasta -g annotations.gff3` block | changed | `cli-help/variants.txt` `==== variants call ====` shows `--min-af` mapping to iVar's `-t` and `--min-depth` mapping to its `-m`, which corroborates the shape. The literal default values 20, 0.05, and 10 are not printed | Settled by reading the pipeline source. Note that `--min-af` and `--min-depth` override `-t` and `-m` from the CLI. |
| 6 | "`-t <float>` \| Minimum allele frequency threshold \| 0.05 (overridable in the dialog)" | changed | `cli-help/variants.txt` `==== variants call ====`, `--min-af <min-af>  Minimum allele frequency threshold` is the CLI override, so it is overridable from the command line too, not only from the dialog | "`-t <float>` \| Minimum allele frequency threshold \| 0.05, overridable with `--min-af` on the CLI or in the dialog" |
| 7 | "The `-g` flag is the switch that turns on codon-merge." | unverifiable | Not stated in the help dump | Settled by reading the iVar TSV-to-VCF converter. |
| 8 | The codon-merge worked example (28881 G→A and 28882 G→A merging into `28881 GG AA`, with 28883 keeping its own row) | unverifiable | A biological worked example with no code counterpart in the help dump | Settled by reading the converter's merge rule, or by running the pipeline against a fixture. |
| 9 | "run `lungfish variants phase` to build a GATK HaplotypeCaller plus WhatsHap command plan" | true | `cli-help/variants.txt` `==== variants phase ====`, `OVERVIEW: Construct a phase-aware GATK HaplotypeCaller plus WhatsHap command plan`, with `--execute`, `--dry-run`, `--reference`, `--bam`, `--output-vcf` | |
| 10 | "once the `gatk-core` and `phasing` packs are installed" | true | Lock `packTools[]`: `gatk4` 4.6.2.0 under `packID: gatk-core`. `whatshap` 2.3 under `packID: phasing` | |
| 11 | "`lofreq indelqual --dindel -f reference.fasta in.bam -o indelqual.bam`" | unverifiable | LoFreq is pinned (`packTools[]` `lofreq` 2.1.5 under `variant-calling`), but the indelqual step is not in the help dump | Settled by reading the LoFreq pipeline source. |
| 12 | "`lofreq call-parallel --pp-threads 4 --no-default-filter -f reference.fasta -o variants.vcf.gz indelqual.bam`" | unverifiable | Same as row 11 | Settled by reading the LoFreq pipeline source. |
| 13 | "The Lungfish convention is to hand LoFreq the un-trimmed BAM" | changed | `cli-help/variants.txt` `==== variants call ====` describes `--ivar-primer-trimmed` as an iVar-only confirmation and offers no LoFreq equivalent, which is consistent with the convention. The convention itself is not stated anywhere in the help | Keep it as a stated convention rather than as tool behavior. |
| 14 | The provenance sidecar sample block with `"schema_version": 2` | changed | The same sample appears in file-formats.md without `schema_version`. Neither is corroborated by a file read, and the two chapters contradict each other | Reconcile the two samples against a real sidecar. They must agree. |
| 15 | "`\"version\": \"0.5.0-alpha11\"`" in the sidecar sample | changed | `cli-help/_version.txt` reports `2026.9.13`. The alpha11 string is stale by many releases | Update the sample's `version` to `2026.9.13`. |
| 16 | "`\"tool_version\": \"samtools 1.21\"`" in the `steps[]` sample | false | The lock pins `samtools` at `1.24` | Update to `"samtools 1.24"`. |
| 17 | "`\"version\": \"1.4.4\"`" for ivar in the `tool` block | true | Lock `packTools[]` entry `id: ivar`, `version: 1.4.4`, `packID: variant-calling` | |
| 18 | "`\"plugin_pack\": \"variant-calling\"`" | true | Lock `packTools[]` entries for lofreq, ivar, medaka, and clair3 all carry `packID: variant-calling` | |
| 19 | "`\"plugin_pack_version\": \"0.3.2\"`" | unverifiable | The lock has no per-pack version field. Packs are identified by `packID` and their tools carry individual versions | Verify whether a pack version exists at all. If packs are versioned only by the lock's `dependencySet` (2026.2) and `version` (2026.9.13), say so and drop the invented 0.3.2. |
| 20 | "`lungfish ops stats /path/to/project-or-bundle`" | true | `cli-help/ops.txt` `==== ops stats ====`, `USAGE: lungfish-cli ops stats <project>`, argument "Project or bundle directory containing .lungfish-provenance.json sidecars" | |
| 21 | "It walks every `.lungfish-provenance.json` sidecar it can find, skips the failed and cancelled runs, and reports the completed run count, the total wall time, the average wall time per operation name, and the single largest peak-RAM figure any step recorded." | changed | `cli-help/ops.txt` `==== ops stats ====` confirms the sidecar walk and the subject ("Summarize runtime and peak RAM from provenance sidecars"). The skip rule and the four reported figures are not printed | Keep the first clause. The skip rule and the reported columns are settled by running the command or reading `Sources/LungfishCLI/Commands/OpsCommand.swift`. |
| 22 | "Recent Lungfish builds also stamp each step with `peakMemoryBytes`" | changed | `cli-help/ops.txt` confirms peak RAM is recorded and summarized, but the field name `peakMemoryBytes` is not printed | Settled by reading the provenance writer. The camelCase name also sits oddly beside the snake_case keys in the same sample block, which is worth checking. |
| 23 | "An OCI image artifact from `lungfish bundle export <bundle> --format container --output <bundle>.oci.tar`" | true | `cli-help/bundle.txt` `==== bundle export ====`, "Exports a Lungfish bundle as a deterministic OCI layout tarball", with `--format`, `-o/--output`, and `--plugin-pack` | |
| 24 | "A conda lockfile from `lungfish conda lock --pack <name> --output lockfile.yml`" | false | `cli-help/conda.txt` `==== conda lock ====`, `OVERVIEW: Export a requested environment specification for a plugin pack (not a resolved lock)`, `-o, --output <output>  Requested specification JSON output path`. The output is JSON, not YAML, and it is explicitly not a resolved lock | "A requested environment specification from `lungfish conda lock --pack <name> --output requested-environment.json`. It is a JSON record of what was asked for, not a resolved artifact lock, so it does not by itself guarantee a byte-identical rebuild." |
| 25 | "`lungfish conda lock --pack <name> --output lockfile.yml` writes a conda-lock-compatible YAML file for a built-in plugin pack." | false | `cli-help/conda.txt` `==== conda ====` group overview: "Use 'lungfish conda lock --pack <pack> --output lockfile.yml' to write a Lungfish requested environment specification (JSON). This is not a resolved artifact lock; exact --from-lockfile reconstruction is unsupported." The chapter's own cli-reference.md states the same. This appendix contradicts both | "`lungfish conda lock --pack <name> --output requested-environment.json` writes a Lungfish requested environment specification in JSON. It records the pack identity, the requested packages, the target platforms, the channels, and the post-install hooks. It is not conda-lock compatible and it is not a resolved artifact inventory." |
| 26 | "Inside are the pack ID, the channels, the platforms, a content hash, and one package record for every pinned requirement." | changed | `cli-help/conda.txt` `==== conda lock ====` does not enumerate the JSON's keys. cli-reference.md gives a different list, namely "requested environment names, all packages, target platforms, channels, supported source overlays, and post-install-hook commands and metadata" | Reconcile with cli-reference.md, and verify against a real exported file. The two chapters currently disagree. |
| 27 | "Reinstall from it with: `lungfish conda install --from-lockfile lockfile.yml`" | false | `cli-help/conda.txt` `==== conda install ====`, `--from-lockfile  Unsupported exact reconstruction input; requested specifications cannot be installed as resolved locks`. The command fails rather than reinstalling | "There is no reinstall-from-lock path today. `lungfish conda install --from-lockfile` exists only to refuse the operation, and it fails before creating or changing an environment. For a reproducible rebuild, use the OCI export or an offline conda pack instead." |
| 28 | "For bit-identical reproduction across machines, pair the provenance sidecar with one of: an OCI image artifact ... a conda lockfile ... a Snakemake / Nextflow export" | changed | With row 27 corrected, the conda-lockfile option does not deliver reproduction at all, so the three-item list overstates the reader's choices | "For bit-identical reproduction across machines, pair the provenance sidecar with an OCI image artifact from `lungfish bundle export --format container`, or with an offline conda pack from `lungfish conda offline-export`. A Snakemake or Nextflow export helps only when it carries lockfile or container references." |
| 29 | "Both commands leave a `.lungfish-provenance.json` beside their output or conda root." | changed | cli-reference.md states this for `conda lock` ("Export provenance is retained in `<output>.lungfish-provenance.json` for each exported document"), which is consistent. The install half is moot once row 27 is corrected | Keep the `conda lock` half and drop the install half. |
| 30 | The per-tool determinism table rows for `samtools sort`, `samtools index`, `samtools mpileup`, `ivar variants` | changed | All the named tools are pinned in the lock, so they exist. The determinism verdicts are upstream tool properties, not Lungfish Genome Explorer behavior, and none is stated in the help | Keep the table and label it as upstream tool behavior at the pinned versions, which is what it is. |
| 31 | "`minimap2` \| Mostly \| Multi-threading can produce non-bit-identical CIGAR strings ... Pin `--threads 1` for strict determinism." | changed | `cli-help/_root.txt` confirms the global `-t, --threads` flag exists, so the advice is actionable. The minimap2 behavior is upstream | Keep it. Label it as upstream behavior at minimap2 2.31, the pinned version. |
| 32 | "`spades` / `flye` / `hifiasm` \| No \| Multi-threaded assembly graphs traversed non-deterministically" | changed | All three are pinned (`packTools[]` spades 4.3.0, flye 2.9.6, hifiasm 0.25.0 under `packID: assembly`). The chapter's own assembler list elsewhere also includes megahit 1.2.9 and skesa 2.5.1, which the table omits | Add rows for MEGAHIT and SKESA, or say the row covers every assembler in the `assembly` pack. |
| 33 | "samtools tightened indel realignment in 1.20 and up" | changed | The lock pins samtools 1.24, so the reader is already past 1.20. The sentence reads as forward-looking advice about a version boundary now behind us | "The `tool.version` field in each sidecar is how a re-runner catches that drift. This release pins samtools 1.24, so a sidecar recording an earlier samtools came from a different installation." |
| 34 | "Apple Containers \| macOS 26+, arm64 \| Default on supported Macs" | changed | `cli-help/workflow.txt` `==== workflow ====` says "Requires macOS 26 or later for container support" and `cli-help/debug.txt` `==== debug container ====` is Apple Container diagnostics, both of which corroborate the runtime. But `cli-help/workflow.txt` `==== workflow run ====` sets `--executor` default to `docker`, not to Apple Containers | "Apple Containers is available on macOS 26 and later on Apple Silicon. It is not the `workflow run` default, which is `docker`; select it with `--executor`." |
| 35 | "Inside sit the bundle payload files, the pinned plugin-pack metadata, and the standard OCI furniture: `oci-layout`, `index.json`, the manifest, the config, the layer tar, and a `.lungfish-provenance.json`." | changed | `cli-help/bundle.txt` `==== bundle export ====` confirms the deterministic OCI layout and the `--plugin-pack` pinning. The individual filenames are not printed | Settled by reading the OCI exporter, or by exporting a fixture bundle and listing the tarball. |
| 36 | "Threading helps these operations: `lungfish map` ..., `lungfish bam primer-trim` ..., `lungfish variants call --caller lofreq` ..., `lungfish assemble` ..., and `lungfish conda classify` (Kraken2)." | true | Each of those five commands lists `-t, --threads` in its help. See `cli-help/map.txt`, `cli-help/bam.txt` `==== bam primer-trim ====`, `cli-help/variants.txt` `==== variants call ====`, `cli-help/assemble.txt`, `cli-help/conda.txt` `==== conda classify ====` | |
| 37 | "These stay single-threaded: `lungfish bundle create`, `lungfish import-fastq`" | changed | Both commands do list `-t, --threads` in their help (`cli-help/bundle.txt` `==== bundle create ====`, `cli-help/import-fastq.txt`), because it is a global option. Whether they actually use it is a source question | Reword to say the flag is accepted globally but has no effect on these commands, and verify which ones actually consume it. |
| 38 | "`lungfish fastq subsample` has no `--seed` flag ... The available options are `--proportion`, `--count`, `-o`/`--output`, `--force`, and `--compress`." | true | `cli-help/fastq.txt` `==== fastq subsample ====`, `USAGE: lungfish-cli fastq subsample <input> [--proportion <proportion>] [--count <count>] --output <output> [--force] [--compress]`, exactly those five, with no `--seed` | |
| 39 | "The `--count` path picks an exact number of reads through a deterministic two-pass selection" | unverifiable | The help confirms `--count  Number of reads to keep` but says nothing about the selection algorithm | Settled by reading the subsample implementation. |
| 40 | The Operations Panel disclosure field table (Status, Started / finished, Wall time, Command, Steps, Stderr, Provenance, Re-run as CLI) | unverifiable | No GUI source read covers the Operations Panel row disclosure | Settled by reading the Operations Panel row view in `Sources/LungfishKit`. |
| 41 | "Stderr \| Last 100 lines of the operation's stderr" | unverifiable | Same as row 40. The 100-line figure is precise and load-bearing, so it needs a citation | Settled by reading the Operations Panel row view. |
| 42 | "When an operation fails, read the stderr disclosure first." | changed | This is right but incomplete now. `OperationFailureReportStore.swift:10-18` writes a failure report to disk as the failure happens, "so the first debugging step be 'read the log file' instead of 'drive the GUI'" | "When an operation fails, read the stderr disclosure. Lungfish Genome Explorer also writes a failure report to `~/Library/Logs/<app name>/Operations/Failures` as the failure happens, so you can read it later without the app running." |
| 43 | "`lungfish variants call --caller ivar --extra-args \"--gff annotations.gff3 --pass_only\"`" | changed | `cli-help/variants.txt` `==== variants call ====` confirms `--extra-args, --advanced-options`, so the flag is real. The example's `--gff` duplicates what the bundle already supplies through `-g`, which the chapter itself describes | Pick an example flag the pipeline does not already set, so the reader does not learn to fight the wrapper. |
| 44 | "Whatever you pass to `--extra-args` is split and appended to the underlying command verbatim." | true | `cli-help/variants.txt` `==== variants call ====`, "Additional caller arguments, written exactly as they should be passed to the underlying tool". `cli-help/map.txt` and `cli-help/assemble.txt` use the same wording | |
| 45 | "`source ~/.lungfish/conda/envs/ivar/bin/activate`" | changed | `cli-help/conda.txt` `==== conda ====` confirms the root is `~/.lungfish/conda`, and the lock gives ivar the environment name `ivar` via its `packTools[]` entry. But there is a supported way to do this without hand-activating, which is `lungfish conda run --env ivar ivar variants ...` | "The supported escape hatch is `lungfish conda run [--env <name>] <tool> [args...]`, which runs the tool from its managed environment and passes stdout, stderr, and the exit code straight through. It still bypasses provenance recording, so use it sparingly." |
| 46 | "A later `lungfish bam adopt-mapping` will have no way to confirm the BAM came from the pipeline it expected." | changed | `cli-help/bam.txt` `==== bam adopt-mapping ====` argument is "Path to the mapping analysis directory produced by `lungfish map`", which supports the concern. The precise failure mode is not stated | Keep the warning. It follows from the command requiring a `lungfish map` result directory rather than a loose BAM. |

### Missing from this chapter

| Feature or setting | Evidence it exists |
|---|---|
| The five iVar tuning flags the CLI exposes, which are exactly this chapter's subject. They are `--ivar-consensus-af` (default 0.75), `--ivar-merge-af-threshold` (default 0.25), `--ivar-bad-quality-threshold` (default 20), `--ivar-no-ignore-strand-bias`, and `--min-depth` | `cli-help/variants.txt` `==== variants call ====` |
| `--ivar-no-ignore-strand-bias` is the direct answer to the chapter's own LoFreq strand-bias section. iVar's strand-bias filter is off by default for amplicon data and this flag turns it on | `cli-help/variants.txt` `==== variants call ====`, "Apply iVar strand-bias filter (off by default for amplicon data)" |
| The four primer-trim tuning flags, which sit upstream of every iVar call this chapter describes. They are `--ivar-min-quality` (20), `--ivar-min-length` (30), `--ivar-sliding-window` (4), `--ivar-primer-offset` | `cli-help/bam.txt` `==== bam primer-trim ====` |
| `--target-reference` on `bam primer-trim`, the fix for a scheme whose accession does not match the BAM's `@SQ` SN | `cli-help/bam.txt` `==== bam primer-trim ====` |
| `lungfish conda offline-export` and `offline-install`, the reproducibility path that actually works, unlike the lockfile path this chapter recommends | `cli-help/conda.txt` `==== conda offline-export ====`, `==== conda offline-install ====` |
| The conda mutation lock at `<conda-root>/.install.lock` and its two user-visible messages, which a power user driving concurrent installs will hit | `Sources/LungfishWorkflow/Conda/CondaRootMutationLock.swift:22`, `:95`, `:12` |
| `lungfish tools update --plan` and its exit code 10, the machine-readable way to assert an installation matches the pinned dependency set | `cli-help/tools.txt` `==== tools update ====` |
| Failure reports on disk at `~/Library/Logs/<app name>/Operations/Failures`, capped at 50 reports with older ones pruned on each write | `OperationFailureReportStore.swift:20-22` (`defaultRetentionLimit = 50`), `:56-77` (the path construction), `:10-18` (the rationale) |
| Failure reports are keyed on app identity, so a Debug build's reports never interleave with a shipped build's | `OperationFailureReportStore.swift:52-54`, "keyed on app identity so a Debug build never interleaves its reports with a shipped build's" |
| A failed write of a failure report is swallowed rather than raised, so an absent report is not itself a signal | `OperationFailureReportStore.swift:73-78`, "Returns nil for operations that did not fail, and also for any I/O error" |
| `lungfish variants query`, a per-sample smart filter over bundle variants, and `lungfish variants extract-sample` | `cli-help/variants.txt` `==== variants ====` |
| `--format json` on `ops stats`, which is how a power user pipes cost data into a script | `cli-help/ops.txt` `==== ops stats ====` |

### Screenshots

| Marker or planned shot | Still valid? | Why |
|---|---|---|
| (none) | changed | Front matter has `shots: []`, no `planned_shots` key, and the body carries no markers. The "Operations Panel as a debug tool" section describes an eight-field disclosure in prose and would be far clearer with one screenshot of an expanded failed row showing the stderr and command fields |

## primer-schemes.md

Arbiters: `Sources/LungfishApp/Resources/PrimerSchemes/` (eight bundles), `Sources/LungfishIO/Bundles/PrimerSchemeBundle.swift`, `cli-help/primers.txt`, `Sources/LungfishApp/Views/ImportCenter/ImportCenterViewModel.swift`.

### Claims

| # | Claim (quoted from the chapter) | Verdict (true, false, changed) | Evidence | Corrected wording |
|---|---|---|---|---|
| 1 | "Lungfish keeps amplicon primer schemes as `.lungfishprimers` bundles in a project's `Primer Schemes/` folder." | true | `cli-help/primers.txt` `==== primers import ====`, `--project <project>  Optional Lungfish project; relative output is written under Primer Schemes/`. `Sources/LungfishApp/App/DocumentManager.swift:235` creates a "Primer Schemes" folder at project creation | |
| 2 | "The current release ships a single built-in scheme in the app resources." | false | `Sources/LungfishApp/Resources/PrimerSchemes/` holds eight `.lungfishprimers` bundles, named ARTIC-nCoV-2019-V3, ARTIC-SARS-CoV-2-V4, ARTIC-SARS-CoV-2-V4.1, ARTIC-SARS-CoV-2-V5.3.2, Midnight-1200-V1, NEB-VarSkip-vss1, NEB-VarSkip-Long-vsl1, and QIASeqDIRECT-SARS2. Every one has `"source": "built-in"` in its `manifest.json` | "The current release ships eight built-in schemes in the app resources. All eight target SARS-CoV-2 with canonical accession `MN908947.3` and equivalent accession `NC_045512.2`." Follow with a table of the eight. They are ARTIC SARS-CoV-2 V3 (`ARTIC-nCoV-2019-V3`, 218 primers, 98 amplicons), ARTIC SARS-CoV-2 V4 (`ARTIC-SARS-CoV-2-V4`, 198, 99), ARTIC SARS-CoV-2 V4.1 (`ARTIC-SARS-CoV-2-V4.1`, 209, 99), ARTIC SARS-CoV-2 V5.3.2 (`ARTIC-SARS-CoV-2-V5.3.2`, 192, 96), Midnight 1200 bp V1 (`Midnight-1200-V1`, 58, 29), NEB VarSkip Short v1 (`NEB-VarSkip-vss1`, 148, 74), NEB VarSkip Long v1 (`NEB-VarSkip-Long-vsl1`, 50, 29), QIAseq Direct SARS-CoV-2 with Booster A (`QIASeqDIRECT-SARS2`, 563, 223). |
| 3 | "Its `name` is `QIASeqDIRECT-SARS2` and its `display_name` is \"QIAseq Direct SARS-CoV-2 with Booster A\"." | true | `Sources/LungfishApp/Resources/PrimerSchemes/QIASeqDIRECT-SARS2.lungfishprimers/manifest.json`, `"name": "QIASeqDIRECT-SARS2"`, `"display_name": "QIAseq Direct SARS-CoV-2 with Booster A"` | |
| 4 | "It declares `primer_count` 563 and `amplicon_count` 223" | true | Same manifest, `"primer_count": 563`, `"amplicon_count": 223` | |
| 5 | "with canonical accession `MN908947.3` and equivalent accession `NC_045512.2`, and its `source` is `built-in`" | true | Same manifest, `"reference_accessions": [{"accession": "MN908947.3", "canonical": true}, {"accession": "NC_045512.2", "equivalent": true}]`, `"source": "built-in"` | |
| 6 | The bundle layout block shows `manifest.json`, `primers.bed`, `primers.fasta` (optional), `attachments/` (optional), `PROVENANCE.md`, and `.lungfish-provenance.json`. | changed | `PrimerSchemeBundle.swift:144-146` resolves exactly three paths, which are `primers.bed`, `primers.fasta`, and `PROVENANCE.md`, and its `LoadError` cases (`:133-134`) make `primers.bed` and `PROVENANCE.md` required. The eight built-in bundles contain only `PROVENANCE.md`, `manifest.json`, and `primers.bed`, with no `.lungfish-provenance.json` and no `primers.fasta` | Keep the layout for imported bundles, but say that `.lungfish-provenance.json` appears only on bundles the importer wrote. The eight built-in schemes ship with `manifest.json`, `primers.bed`, and `PROVENANCE.md` only. |
| 7 | "`primers.bed` is required." | true | `PrimerSchemeBundle.swift:133`, `case .missingBED: return "Bundle is missing primers.bed."` | |
| 8 | "`primers.fasta` is optional" | true | `PrimerSchemeBundle.swift:118`, `public let fastaURL: URL?`, the only optional of the three file properties, and no `missingFASTA` error case exists | |
| 9 | The manifest table omits `imported` and `attachments`. | false | `PrimerSchemeBundle.swift:15-16` declares `public let imported: Date?` and `public let attachments: [AttachmentEntry]?`, coded as `imported` and `attachments` at `:110-112`. `AttachmentEntry` (`:87-95`) carries a `path` and an optional `description` | Add two rows. `imported` is the timestamp written when an existing scheme was imported into a project, as distinct from `created`. `attachments` is an array of `{path, description}` objects naming the extra files copied under `attachments/`. |
| 10 | "`schema_version` \| Manifest schema version (currently `1`)." | true | All eight built-in manifests carry `"schema_version": 1`. `PrimerSchemeBundle.swift:100` maps it from `schema_version` | |
| 11 | "`amplicon_count` \| Distinct amplicon names inferred from BED column 4 after stripping `_LEFT` and `_RIGHT`." | changed | The manifest field exists and is coded at `PrimerSchemeBundle.swift:107`. The inference rule is not in this file. It lives in the importer | Keep the field description. The `_LEFT` / `_RIGHT` stripping rule is settled by reading the primer-scheme importer. |
| 12 | "`source` \| Provenance of the scheme: `built-in` for the shipped scheme." | changed | `PrimerSchemeBundle.swift:12` declares `source` as an optional free-form `String?`, and all eight shipped bundles use `"built-in"`. The singular "the shipped scheme" is wrong now that there are eight | "`source` \| Where the scheme came from. All eight shipped schemes use `built-in`." |
| 13 | "`reference_accessions` is an array of objects, not a flat list of strings. Each object carries an `accession` and a boolean role flag" | true | `PrimerSchemeBundle.swift:60-84`, `ReferenceAccession` with `accession: String`, `canonical: Bool = false`, `equivalent: Bool = false`, and a custom decoder that defaults both flags to false when absent | |
| 14 | "The canonical accession is the one the BED coordinates are defined against." | true | `PrimerSchemeBundle.swift:50-54`, `canonicalAccession` returns the first accession whose `canonical` flag is set, falling back to the first entry | |
| 15 | "The equivalent accessions let the resolver match an alignment reference that names the same sequence differently." | true | `PrimerSchemeBundle.swift:56-58`, `equivalentAccessions` returns every accession whose `canonical` flag is false. `cli-help/bam.txt` `==== bam primer-trim ====`, `--target-reference  Override the @SQ SN used to resolve the primer scheme (defaults to the primer scheme's canonical accession)` | |
| 16 | "BED coordinates are zero-based and half-open." | true | This is the BED specification, and the shipped `primers.bed` files follow it | |
| 17 | "`lungfish primers import --bed primers.bed --fasta reference.fasta --output MyScheme.lungfishprimers --reference-accession MN908947.3 --display-name \"My Scheme\"`" | changed | `cli-help/primers.txt` `==== primers import ====` confirms every flag, but `--fasta` is described as "Optional primer FASTA to copy into the bundle", not a reference FASTA. The example filename `reference.fasta` mislabels it | Rename the example argument to `primers.fasta`, and say that `--fasta` copies a primer FASTA into the bundle rather than a reference sequence. |
| 18 | "If `--output` is relative and you supply `--project`, Lungfish writes the bundle under `<project>/Primer Schemes/`. Without `--project`, the relative output path resolves from the current directory." | true | `cli-help/primers.txt` `==== primers import ====`, `--project <project>  Optional Lungfish project; relative output is written under Primer Schemes/` | |
| 19 | "The `.lungfishprimers` suffix is added for you when you leave it off." | changed | `cli-help/primers.txt` describes `--output` as "Output .lungfishprimers bundle name or path", which is consistent with a name being accepted, but the suffix-appending behavior is not stated | Keep the sentence. It is settled by reading the primers importer. |
| 20 | "Two optional arguments repeat as often as you need them: `--equivalent-accession <accession>` ... and `--attachment <path>`" | true | `cli-help/primers.txt` `==== primers import ====`, `--equivalent-accession  Additional equivalent reference accession. Repeatable.`. `--attachment  Extra documentation file to copy under attachments/. Repeatable.` | |
| 21 | "`--reference-accession` names the canonical reference accession." | changed | `cli-help/primers.txt` adds a default the chapter omits, reading "Canonical reference accession. Defaults to the first BED column." | "`--reference-accession` names the canonical reference accession. Leave it off and the importer takes the value from the BED file's first column." |
| 22 | "`--display-name` sets the label shown in pickers." | changed | `cli-help/primers.txt`, `--display-name  Human-readable scheme name. Defaults to the output stem.` | "`--display-name` sets the label shown in pickers, and defaults to the output filename's stem." |
| 23 | "Choose `File > Import Center`, then select `Primer Scheme`." | true | `Sources/LungfishApp/App/MainMenu.swift:206-212` puts "Import Center…" in the File menu. `ImportCenterViewModel.swift:137` declares the `primerScheme` action and `:931` gives it the display title "Primer Scheme" | |
| 24 | "Run the import. Lungfish writes `Primer Schemes/<name>.lungfishprimers`, copies the files, writes `manifest.json`, and adds both human-readable and machine-readable provenance." | changed | `ImportCenterViewModel.swift:598` routes the action through `.wizardSheet(action: .primerScheme)` and `:803` dispatches to `appDelegate.launchPrimerSchemeImport(request)`, and `:685` notes "Primer scheme import runs via the wizard sheet". The written artifacts are decided inside that importer | Keep the destination path, which the CLI help corroborates. The exact file set written is settled by reading `launchPrimerSchemeImport`. |
| 25 | "Reopen the Primer Trim dialog or Viral Recon wizard. The scheme appears alongside built-in schemes." | true | `Sources/LungfishApp/Views/BAM/PrimerSchemePickerView.swift` and `Sources/LungfishApp/Views/Mapping/ViralReconWizardSheet.swift` both reference primer schemes, and the eight built-in bundles live in app resources so a project-local bundle joins them in the same picker | |
| 26 | "Commands that consume primer schemes take the bundle path: `lungfish bam primer-trim --bundle ... --alignment-track <track-id> --scheme \"Primer Schemes/MyScheme.lungfishprimers\"`" | changed | `cli-help/bam.txt` `==== bam primer-trim ====` confirms `--scheme  Path to the .lungfishprimers bundle directory`, but the usage line makes `--name` required too, and the chapter's example omits it | Add `--name "<track name>"` to the example. `bam primer-trim` requires `--bundle`, `--alignment-track`, `--scheme`, and `--name`. |

### Missing from this chapter

| Feature or setting | Evidence it exists |
|---|---|
| Seven of the eight built-in schemes, namely the four ARTIC versions, Midnight 1200 bp V1, and both NEB VarSkip schemes, with their primer and amplicon counts | `Sources/LungfishApp/Resources/PrimerSchemes/` and the `manifest.json` in each bundle |
| The `imported` manifest field, distinct from `created` | `PrimerSchemeBundle.swift:15`, `:110` |
| The `attachments` manifest array and its `{path, description}` shape | `PrimerSchemeBundle.swift:16`, `:87-95`, `:112` |
| `PROVENANCE.md` is required, not optional. A bundle without it fails to load | `PrimerSchemeBundle.swift:134`, `case .missingProvenance: return "Bundle is missing PROVENANCE.md."` |
| The four load failures the reader may hit, which are missing manifest, missing BED, missing provenance, and an unparseable manifest | `PrimerSchemeBundle.swift:120-131`, `LoadError` cases `missingManifest`, `missingBED`, `missingProvenance`, `invalidManifest(underlying:)` |
| The manifest decoder tolerates missing `canonical` and `equivalent` flags, defaulting both to false, so a hand-written accession list with neither flag is accepted but resolves through the first-entry fallback | `PrimerSchemeBundle.swift:76-84` and `:50-54` |
| `bam primer-trim --target-reference`, which overrides the `@SQ` SN used to resolve the scheme when a BAM names the reference differently. This is the fix for the chapter's own warning about a scheme trimming zero primers | `cli-help/bam.txt` `==== bam primer-trim ====` |
| The four iVar trim parameters a custom scheme may need tuned, which are `--ivar-min-quality` (default 20), `--ivar-min-length` (default 30), `--ivar-sliding-window` (default 4), `--ivar-primer-offset` | `cli-help/bam.txt` `==== bam primer-trim ====` |

### Screenshots

| Marker or planned shot | Still valid? | Why |
|---|---|---|
| (none) | changed | Front matter has `shots: []`, no `planned_shots` key, and the body carries no markers. The GUI Import Procedure is a five-step click path through a wizard sheet (`ImportCenterViewModel.swift:598`, `:803`) and would benefit from one shot of the Import Center with Primer Scheme selected, and one of the wizard's name and accession fields. A shot of the scheme picker showing all eight built-in schemes would also fix the reader's mental model that only one ships |

## shared-projects.md

Arbiters: `Sources/LungfishCore/Storage/ProjectLock.swift`, `Sources/LungfishCore/Storage/ProjectLockRecovery.swift`, `Sources/LungfishApp/App/ProjectLockResolutionDialog.swift`, `Sources/LungfishApp/App/ProjectLockWarningPresentation.swift`, `Sources/LungfishApp/App/AppDelegate.swift`, `Sources/LungfishApp/App/ProjectWriteGatePresenter.swift`, `cli-help/project.txt`.

### Claims

| # | Claim (quoted from the chapter) | Verdict (true, false, changed) | Evidence | Corrected wording |
|---|---|---|---|---|
| 1 | "the `lungfish project lock`, `unlock`, and `migrate` commands" | true | `cli-help/project.txt` `==== project ====` lists exactly lock, unlock, migrate | |
| 2 | "The advanced CLI commands write a machine-readable file at `.lungfish/project.lock` inside the project." | true | `ProjectLock.swift:108-111`, `ProjectLockManager.lockURL(for:)` appends `".lungfish"` as a directory then `"project.lock"` as a file | |
| 3 | "The GUI reads the same metadata on project open so teams get a consistent warning surface across CLI and app workflows." | true | `ProjectLockResolutionDialog.swift:44-59` builds its message and informative text from the same `ProjectLockStatus` cases (`.active`, `.corrupted`, `.stale`, `.unknown`), and `ProjectLockWarningPresentation.swift:20-30` reads `record.user`, `record.host`, `record.pid`, `record.createdAt`, `record.mode`, and `record.toolName` straight off the CLI-written record | |
| 4 | "the commands below invoke the CLI as `lungfish`, matching the command name the help text uses" | false | Every banner in the help dump prints `USAGE: lungfish-cli ...`, including `cli-help/project.txt` `==== project lock ====`, `USAGE: lungfish-cli project lock <project-path> ...`. The help text uses `lungfish-cli`, not `lungfish` | "The commands below invoke the CLI as `lungfish`, the name an installed release puts on `PATH`. The help text itself prints `lungfish-cli`, which is the SwiftPM product name." |
| 5 | "`lungfish project lock ~/Projects/SARS-CoV-2.lungfish --mode exclusive`" | true | `cli-help/project.txt` `==== project lock ====`, `USAGE: lungfish-cli project lock <project-path> [--mode <mode>] [--force] ...`, `--mode <mode>  Lock mode to record for tools and GUI clients (default: exclusive)` | |
| 6 | "The command creates `.lungfish/project.lock` with exclusive file creation so two lock attempts cannot both win the same project." | unverifiable | `ProjectLock.swift:108-111` gives the path, and `ProjectLockManager` is the writer, but the `O_EXCL`-style creation flag is not visible in the region read | Settled by reading `ProjectLockManager`'s write path in `ProjectLock.swift`. |
| 7 | "The record contains the tool name, LGE CLI version, project path, user, host, process id, process start time when available, current working directory, creation time, and lock mode." | changed | `ProjectLock.swift:35-47` declares `schemaVersion`, `toolName`, `appVersion`, `projectPath`, `mode`, `user`, `host`, `machineIdentifier`, `pid`, `processStartTime`, `cwd`, `createdAt`. The list omits `schemaVersion` and `machineIdentifier` | "The record contains the schema version, the tool name, the LGE CLI version, the project path, the lock mode, the user, the host, a machine identifier, the process id, the process start time when available, the current working directory, and the creation time." |
| 8 | The sample lock JSON block (`appVersion`, `createdAt`, `cwd`, `host`, `mode`, `pid`, `processStartTime`, `projectPath`, `schemaVersion`, `toolName`, `user`) | changed | The eleven keys shown are all real (`ProjectLock.swift:36-47`), but `machineIdentifier` is missing from the sample. The sample's `appVersion` value `"lungfish-cli 0.5.0-alpha11"` is also stale against `cli-help/_version.txt`, which reports `2026.9.13` | Add `"machineIdentifier": "<opaque id>",` to the sample, and update `appVersion` to a current value such as `"lungfish-cli 2026.9.13"`. |
| 9 | "`schemaVersion`: 1" | true | `ProjectLock.swift:85`, `ProjectLockRecord.current(...)` hard-codes `schemaVersion: 1` | |
| 10 | "The `mode` value is intentionally a label, not a permission system." | true | `cli-help/project.txt` `==== project lock ====`, `--mode <mode>  Lock mode to record for tools and GUI clients`, a free-form string with no enumerated values and no enforcement | |
| 11 | "The CLI process that created the record exits as soon as `lungfish project lock` returns, so the recorded `pid` is the short-lived CLI process" | true | `ProjectLock.swift:80`, `let pid = Int(ProcessInfo.processInfo.processIdentifier)` records the CLI process itself, and the command has no daemon or wait mode in `cli-help/project.txt` | |
| 12 | "If a lock already exists and the recorded process is still live on this host, `lungfish project lock` refuses to replace it." | true | `ProjectLock.swift:232-233`, the staleness test returns true only when `record.processStartTime.isEmpty` or `ProjectProcessInspector.processStartTime(for: record.pid) == record.processStartTime`, so a live matching process is not stale. `cli-help/project.txt` `--force  Replace an active lock without stale-owner checks` confirms the refusal exists | |
| 13 | "If the lock points at a local process that has exited, the command treats it as stale and replaces it." | true | `ProjectLock.swift:211-213` compares the current start time for that pid against the recorded one and treats a mismatch as a different process. `ProjectLockStatus.stale` at `:10` is the resulting state | |
| 14 | "Locks from another host (where this machine cannot tell whether the process is still alive) are treated as active unless you pass `--force`." | changed | `ProjectLockStatus` (`ProjectLock.swift:8-13`) has a distinct `.unknown` case for exactly this situation, and `ProjectLockResolutionDialog.swift:55-59` gives `.unknown` its own copy ("Lungfish could not confirm whether a previous session is still using this project"). It is treated as blocking, not as `.active` | "A lock from another host, where this machine cannot tell whether the process is still alive, is recorded as unknown rather than active. Unknown locks block writes the same way active ones do, and clearing one takes `--force`." |
| 15 | "`unlock` removes a lock owned by the current process, or a stale local lock owned by the current user." | changed | `cli-help/project.txt` `==== project unlock ====`, `--force  Remove the lock even when it belongs to another user or process` implies the un-forced path refuses those two cases. The precise ownership test is in `ProjectLockManager`, not in the region read | Keep the sentence. The exact ownership predicate is settled by reading `ProjectLockManager`'s unlock path. |
| 16 | "If the lock belongs to another user, an active different process, or an unknown or remote host, the command refuses unless `--force` is supplied" | true | `cli-help/project.txt` `==== project unlock ====`, `--force  Remove the lock even when it belongs to another user or process` | |
| 17 | "A forced unlock removes the file; it does not stop the other process." | true | Nothing in `ProjectLock.swift` or `cli-help/project.txt` signals or terminates the recorded process. The command only manipulates the lock file | |
| 18 | "`lungfish project migrate ~/Projects/SARS-CoV-2.lungfish`" and "For automation, request JSON: `--format json`" | true | `cli-help/project.txt` `==== project migrate ====`, `USAGE: lungfish-cli project migrate <project-path> [--dry-run] [--format <format>] ...`, `--format` values text, json, tsv | |
| 19 | "The current implementation is conservative: it scans bundles, reports the schema versions it can see, leaves bundles whose manifests are already current untouched, and refuses to rewrite unsupported legacy schemas until a real transformer exists." | true | `cli-help/project.txt` `==== project migrate ====` overview: "The migration command is intentionally conservative. It scans Lungfish bundles inside a project, leaves current-version bundles untouched, and reports unsupported legacy schemas without mutating data until a schema-specific transformer exists." | |
| 20 | "The current migration report lists manifest-backed Lungfish bundle directories that contain a `manifest.json`; in practice this currently covers `.lungfishref` reference bundles. FASTQ-derived bundle directories that use `analyses-manifest.json` or `derived.manifest.json` are not part of this migration scan." | unverifiable | `cli-help/project.txt` does not name the scanned manifest filenames or the excluded ones | Settled by reading the migration scanner in `Sources/LungfishCLI/Commands/ProjectCommand*.swift`. |
| 21 | "`1.0` reference bundles that already carry a `browser_summary` field in their manifest are reported as `current` with action `none` ... with `--dry-run`, the report flags them as `migration-available` with action `dry-run-synthesize-browser-summary`" | unverifiable | The status and action strings are not printed in `cli-help/project.txt` | Settled by reading the migration report model in `Sources/LungfishCLI/Commands/ProjectCommand*.swift`. |
| 22 | "LGE synthesizes the `browser_summary` field, backs up the original manifest under `.lungfish/migrations/`, and writes migration provenance describing the change." | unverifiable | The `.lungfish/migrations/` path does not appear in `ProjectLock.swift` or the help dump | Settled by reading the migration writer. |
| 23 | "Unsupported legacy bundles are reported as `unsupported` with action `report-only` or `dry-run-report`. They are not renamed or changed." | changed | The "not renamed or changed" half is directly supported by `cli-help/project.txt` `==== project migrate ====` ("reports unsupported legacy schemas without mutating data"). The two action strings are not printed anywhere in the dump | Keep the behavioral sentence. Verify or drop the two literal action strings. |
| 24 | "when a migration actually rewrites or wraps scientific data, it must preserve existing provenance sidecars and write new migration provenance describing the workflow or tool name and version, options, inputs, outputs, checksums, file sizes, runtime identity, exit status, stderr when useful, and wall time" | changed | This states a project rule rather than an observed behavior, and the help dump neither confirms nor denies the field list | Mark it plainly as the project's provenance requirement for future migrations, not as a description of what the current command writes. |
| 25 | "When the GUI opens a project with active or unknown lock metadata, it marks the window `Read Only`, shows a persistent project-lock banner, and blocks project-writing workflows" | true | `AppDelegate.swift:673` appends `" (Read Only)"` to the window title when `controller.projectSession.isReadOnlyRecommended`. `ProjectLockWarningPresentation.swift:16-18` builds a banner titled "Project opened read-only" whose detail ends "Project-writing workflows are blocked to protect shared storage.". `ProjectWriteGatePresenter.swift:22` names the block alert "Project Is Open Read Only" | |
| 26 | "Treat `.lungfish/project.lock` as the source of truth for advanced command-line maintenance." | true | `ProjectLock.swift:108-111` is the single path both the CLI and the GUI read | |

### Missing from this chapter

| Feature or setting | Evidence it exists |
|---|---|
| The GUI's project-lock resolution dialog and its three choices. On opening a locked project the reader is offered "Open Read-Only", "Cancel", and, when the lock is stale, corrupted, or unknown, "Recover and Open" | `ProjectLockResolutionDialog.swift:10` (`case readOnly, cancel, recover`), `:63-66` (the three buttons), `:75-76` (the return mapping) |
| The second confirmation before recovery, which warns that recovering a lock while another session is writing can damage the project | `ProjectLockResolutionDialog.swift:84-88`, "Before continuing, ensure this project is closed in other Lungfish versions, in the CLI, and on other computers. Recovering a lock while another session is writing can damage the project. Lungfish will retain an archive of the existing lock for review." |
| Lock recovery archives the displaced lock rather than deleting it, and writes a recovery record beside it | `ProjectLockRecovery.swift:38-41`, `ProjectLockRecoveryResult` carrying `archiveURL` and `recoveryRecordURL`. `:61` `recover(snapshot:reason:)` |
| The fourth lock status, `corrupted`, and what the reader sees when the lock file cannot be parsed | `ProjectLock.swift:12` (`case corrupted`), `:15-27` (`ProjectLockCorruption` with its `lockURL` and `reason`), `ProjectLockResolutionDialog.swift:51-52`, `ProjectLockWarningPresentation.swift:36-38` |
| The exact banner text the reader will see, which names the lock owner, host, pid, and creation time | `ProjectLockWarningPresentation.swift:29-31`, "\(record.toolName) has an \(lockStatus.rawValue) \(record.mode) lock from \(record.user)@\(record.host) pid \(record.pid), created \(createdAt). Project-writing workflows are blocked to protect shared storage." |
| `machineIdentifier`, the field that lets LGE tell a same-machine lock from a remote one even when host names collide | `ProjectLock.swift:43`, `:93` (`ProjectLockMetadata.machineIdentifier`) |
| `project lock --force`, which the chapter mentions only for `unlock` | `cli-help/project.txt` `==== project lock ====`, `--force  Replace an active lock without stale-owner checks` |
| `project migrate --format tsv` as a third output shape | `cli-help/project.txt` `==== project migrate ====`, `--format` values text, json, tsv |

### Screenshots

| Marker or planned shot | Still valid? | Why |
|---|---|---|
| (none) | changed | Front matter has `shots: []` and `planned_shots: []`, and the body carries no markers. The chapter would be clearer with two. Add the project-lock resolution dialog with its three buttons (`ProjectLockResolutionDialog.swift:63-66`) and the read-only banner over an open project (`ProjectLockWarningPresentation.swift:16-31`). Both are GUI surfaces the chapter describes in prose and never shows |

## tool-versions.md

Arbiters: `Sources/LungfishWorkflow/Resources/ManagedTools/third-party-tools-lock.json` (`version` 2026.9.13, `dependencySet` 2026.2, 17 `tools`, 23 `packTools`, 16 `databases`, 2 `pipelines`, 2 `managedData`, and a `bootstrap.micromamba` block) and `Sources/LungfishWorkflow/Resources/Tools/tool-versions.json` (micromamba 2.9.0-0). This appendix is the most stale of the eleven, with every managed-tool version but four wrong.

### Claims

| # | Claim (quoted from the chapter) | Verdict (true, false, changed) | Evidence | Corrected wording |
|---|---|---|---|---|
| 1 | "The live `version --tools` table has five columns: Tool, Version, Source, Environment, and Executables." | unverifiable | `cli-help/version.txt` confirms `--tools  Print the bundled and managed tool version table` but does not print the table's columns | Settled by running `lungfish version --tools` or reading the printer in `Sources/LungfishCLI/Commands/VersionCommand.swift`. |
| 2 | "The License and Source URL values shown in this appendix come from the managed-tool lock file's per-tool metadata" | true | Every entry in the lock's `tools` and `packTools` arrays carries `license` and `sourceUrl` keys alongside `id`, `environment`, `packageSpec`, `executables`, and `version` | |
| 3 | "micromamba \| 2.0.5-0 \| bundled \| BSD-3-Clause \| `micromamba`" | false | `Sources/LungfishWorkflow/Resources/Tools/tool-versions.json` records `"version": "2.9.0-0"` for micromamba, and the lock's `bootstrap.micromamba.version` is also `2.9.0-0`. The BSD-3-Clause license and the single `micromamba` executable are correct | "micromamba \| 2.9.0-0 \| bundled \| BSD-3-Clause \| `micromamba`" |
| 4 | "Nextflow \| 25.10.4 \| managed \| `nextflow` \| Apache-2.0 \| `nextflow`" | false | Lock `tools[]` entry `id: nextflow`, `version: 26.04.6`, `environment: nextflow`, `license: Apache-2.0`, `executables: ["nextflow"]` | "Nextflow \| 26.04.6 \| managed \| `nextflow` \| Apache-2.0 \| `nextflow`" |
| 5 | "Snakemake \| 9.19.0 \| managed \| `snakemake` \| MIT \| `snakemake`" | false | Lock `tools[]` entry `id: snakemake`, `version: 9.25.2` | "Snakemake \| 9.25.2 \| managed \| `snakemake` \| MIT \| `snakemake`" |
| 6 | "BBTools \| 39.80 \| managed \| `bbtools` \| BSD-3-Clause-LBNL \| `clumpify.sh`, `bbduk.sh`, `bbmerge.sh`, `repair.sh`, `tadpole.sh`, `reformat.sh`, `bbmap.sh`, `mapPacBio.sh`, `java`" | false | Lock `tools[]` entry `id: bbtools`, `version: 40.02`. The license and the nine executables are correct and in the same order | "BBTools \| 40.02 \| managed \| `bbtools` \| BSD-3-Clause-LBNL \| (executables unchanged)" |
| 7 | "Fastp \| 1.3.2 \| managed \| `fastp` \| MIT \| `fastp`" | false | Lock `tools[]` entry `id: fastp`, `version: 1.3.6` | "fastp \| 1.3.6 \| managed \| `fastp` \| MIT \| `fastp`" |
| 8 | "Deacon \| 0.15.0 \| managed \| `deacon` \| MIT \| `deacon`" | false | Lock `tools[]` entry `id: deacon`, `version: 0.16.0` | "Deacon \| 0.16.0 \| managed \| `deacon` \| MIT \| `deacon`" |
| 9 | "Samtools \| 1.23.1 \| managed \| `samtools` \| MIT \| `samtools`" | false | Lock `tools[]` entry `id: samtools`, `version: 1.24` | "SAMtools \| 1.24 \| managed \| `samtools` \| MIT \| `samtools`" |
| 10 | "BCFtools \| 1.23.1 \| managed \| `bcftools` \| GPL \| `bcftools`" | false | Lock `tools[]` entry `id: bcftools`, `version: 1.24` | "BCFtools \| 1.24 \| managed \| `bcftools` \| GPL \| `bcftools`" |
| 11 | "HTSlib \| 1.23.1 \| managed \| `htslib` \| MIT \| `bgzip`, `tabix`" | false | Lock `tools[]` entry `id: htslib`, `version: 1.24`. License and both executables are correct | "HTSlib \| 1.24 \| managed \| `htslib` \| MIT \| `bgzip`, `tabix`" |
| 12 | "SeqKit \| 2.13.0 \| managed \| `seqkit` \| MIT \| `seqkit`" | true | Lock `tools[]` entry `id: seqkit`, `version: 2.13.0`, `license: MIT`, `executables: ["seqkit"]` | |
| 13 | "Cutadapt \| 5.2 \| managed \| `cutadapt` \| MIT \| `cutadapt`" | true | Lock `tools[]` entry `id: cutadapt`, `version: 5.2`, `license: MIT` | |
| 14 | "VSEARCH \| 2.30.5 \| managed \| `vsearch` \| GPL-3.0-or-later OR BSD-2-Clause \| `vsearch`" | false | Lock `tools[]` entry `id: vsearch`, `version: 2.31.0`. The dual license string is correct | "VSEARCH \| 2.31.0 \| managed \| `vsearch` \| GPL-3.0-or-later OR BSD-2-Clause \| `vsearch`" |
| 15 | "pigz \| 2.8 \| managed \| `pigz` \| Zlib \| `pigz`" | true | Lock `tools[]` entry `id: pigz`, `version: 2.8`, `license: Zlib` | |
| 16 | "SRA Tools \| 3.4.1 \| managed \| `sra-tools` \| Public Domain \| `prefetch`, `fasterq-dump`" | true | Lock `tools[]` entry `id: sra-tools`, `version: 3.4.1`, `license: Public Domain`, `executables: ["prefetch", "fasterq-dump"]` | |
| 17 | "UCSC bedGraphToBigWig \| 482 \| managed \| `ucsc-bedgraphtobigwig` \| UCSC (see genome.ucsc.edu/license) \| `bedGraphToBigWig`" | true | Lock `tools[]` entry `id: ucsc-bedgraphtobigwig`, `version: 482`, `license: "Varies; see https://genome.ucsc.edu/license"` | |
| 18 | "pysam \| 0.24.0 \| managed \| `pysam` \| MIT \| `python`" | true | Lock `tools[]` entry `id: pysam`, `version: 0.24.0`, `license: MIT`, `executables: ["python"]` | |
| 19 | "openpyxl \| 3.1.5 \| managed \| `openpyxl` \| MIT \| `python`" | true | Lock `tools[]` entry `id: openpyxl`, `version: 3.1.5`, `license: MIT`, `executables: ["python"]` | |
| 20 | The Managed Tools table has 16 rows. | false | The lock's `tools` array has 17 entries. The chapter omits `trim_galore` (version 2.3.0, environment `trim_galore`, license GPL-3.0-only, executable `trim_galore`) | Add a row: "Trim Galore \| 2.3.0 \| managed \| `trim_galore` \| GPL-3.0-only \| `trim_galore`". It is the default clumping tool option in `import fastq --clumping-tool trim-galore`. |
| 21 | "Tools such as Clair3, WhatsHap, and Freyja are not in the bundled managed-tool lock and do not appear in `lungfish version --tools`." | false | All three are in the lock, in its `packTools` array, as `clair3` 2.0.2 under `variant-calling`, `whatshap` 2.3 under `phasing`, `freyja` 2.0.3 under `wastewater-surveillance`. They are in a different array from `tools`, but the same lock file | "Plugin-pack tools such as Clair3, WhatsHap, and Freyja are pinned in the same lock file, under its `packTools` list rather than its always-installed `tools` list. They are installed on demand with `lungfish conda install --pack <name>`, so a fresh machine has none of them until the pack is installed. The Pack Tools table below gives the pinned versions." |
| 22 | "They are provided by separate plugin packs (for example the GATK, phasing, and wastewater-surveillance packs)" | changed | The pack IDs in the lock are `read-mapping`, `full-length-mhc-genotyping`, `variant-calling`, `gatk-core`, `phasing`, `assembly`, `multiple-sequence-alignment`, `phylogenetics`, `metagenomics`, and `wastewater-surveillance`. The GATK pack is named `gatk-core`, not "GATK" | "They are provided by separate plugin packs. The ten pack IDs are `read-mapping`, `full-length-mhc-genotyping`, `variant-calling`, `gatk-core`, `phasing`, `assembly`, `multiple-sequence-alignment`, `phylogenetics`, `metagenomics`, and `wastewater-surveillance`." |
| 23 | "nf-core/viralrecon \| 3.0.0 \| Add `--version <tag>` to a full `lungfish workflow run nf-core/viralrecon ...` command, or use the Viral Recon wizard version field" | true | Lock `pipelines[]` entry `id: nf-core-viralrecon`, `repository: nf-core/viralrecon`, `revision: 3.0.0`, `releaseVersion: 3.0.0`. `cli-help/workflow.txt` `==== workflow run ====`, `--version <version>  nf-core workflow version or tag` | |
| 24 | The Supported Workflow Pins table has one row. | false | The lock's `pipelines` array has two entries. The second is `id: taxtriage`, `repository: jhuapl-bio/taxtriage`, `revision: e10bfebda32a62711f38a4e23ab03b61725a9675`, `releaseVersion: v3.3.8` | Add a row: "jhuapl-bio/taxtriage \| v3.3.8 (commit `e10bfebd`) \| Add `--revision <ref>` to `lungfish taxtriage run`". The lock pins the exact commit, not just the release tag. |
| 25 | "The numbers and flag values match dependency set 2026.2." (stated in power-user-notes. This appendix never names its dependency set) | changed | The lock's top-level `dependencySet` is `2026.2` and its `version` is `2026.9.13` | Add a line to "What it is": "This appendix reflects dependency set 2026.2 as pinned by release 2026.9.13." |

### Missing from this chapter

| Feature or setting | Evidence it exists |
|---|---|
| Trim Galore 2.3.0, environment `trim_galore`, GPL-3.0-only | Lock `tools[]` entry `id: trim_galore` |
| The whole Pack Tools table. The lock pins 23 pack tools across 10 packs and the appendix names none of their versions | Lock `packTools[]`: read-mapping (minimap2 2.31, bwa-mem2 2.3, bowtie2 2.5.5), full-length-mhc-genotyping (savont 0.6.3, blast 2.16.0), variant-calling (lofreq 2.1.5, ivar 1.4.4, medaka 2.2.2, clair3 2.0.2), gatk-core (gatk4 4.6.2.0), phasing (whatshap 2.3), assembly (spades 4.3.0, megahit 1.2.9, skesa 2.5.1, flye 2.9.6, hifiasm 0.25.0), multiple-sequence-alignment (mafft 7.526), phylogenetics (iqtree 3.1.3), metagenomics (kraken2 2.17.1, bracken 1.0.0, esviritu 1.3.3, ribodetector 0.3.3), wastewater-surveillance (freyja 2.0.3) |
| The whole Databases table. The lock pins 16 databases with their own versions | Lock `databases[]`: nine Kraken2 databases at `20260626` (standard, standard-8, standard-16, pluspf, pluspf-8, pluspf-16, viral, minus-b) plus eupathdb46 at `20230407`. Esviritu-viral-v3 at `v3.2.4`. Ncbi-taxonomy at `live`. Kraken2-special-silva and kraken2-special-greengenes at `kraken2-special-v1`. Human-scrubber at `20260706v2`. Deacon-panhuman at `panhuman-1`. Deacon-ribokmers at `bbmap-ribokmers-k31w15` |
| The two managed data sets, which are neither tools nor databases in the usual sense | Lock `managedData[]`: `deacon-panhuman` ("Human Read Removal Data") and `deacon-ribokmers` ("Ribosomal RNA Removal Data") |
| `lungfish tools update --plan` as the way to see what this machine is missing against the pinned set, and its exit code 10 for "work is pending" | `cli-help/tools.txt` `==== tools update ====` |
| The `retiredEnvironments` list, which is currently empty but is the lock's record of environments removed from the pinned set | Lock top-level key `retiredEnvironments: []` |
| `dependencySetDate`, the date the pinned set was cut | Lock top-level key `dependencySetDate` |

### Screenshots

| Marker or planned shot | Still valid? | Why |
|---|---|---|
| (none) | not applicable | Front matter has `shots: []`, no `planned_shots` key, and the body carries no markers. A version table is text, and a screenshot of it would go stale faster than the table itself |

## troubleshooting.md

Arbiters: `Sources/LungfishKit/OperationFailureReportStore.swift`, `Sources/LungfishCore/Services/NCBI/NCBIService.swift`, `Sources/LungfishCore/Storage/ManagedStorageConfigStore.swift`, `Sources/LungfishWorkflow/Conda/CondaRootMutationLock.swift`, `Sources/LungfishWorkflow/Resources/ManagedTools/third-party-tools-lock.json`, and the cli-help dump.

### Claims

| # | Claim (quoted from the chapter) | Verdict (true, false, changed) | Evidence | Corrected wording |
|---|---|---|---|---|
| 1 | "Lungfish runs most bioinformatics tools out of per-tool conda environments under `~/.lungfish/conda`." | true | `cli-help/conda.txt` `==== conda ====` overview: "Each tool is installed in its own isolated environment to prevent dependency conflicts. Tools are stored in /Users/dho/.lungfish/conda." | |
| 2 | "Operation fails with `command not found: minimap2` \| Pack `read-mapping` not installed" | true | Lock `packTools[]` entry `id: minimap2`, `version: 2.31`, `packID: read-mapping`. `cli-help/map.txt` `==== map ====` says "Install the tools through the read-mapping plugin pack (`lungfish conda install read-mapping`)" | |
| 3 | "Operation fails with `command not found: ivar` \| Pack `variant-calling` not installed" | true | Lock `packTools[]` entry `id: ivar`, `packID: variant-calling` | |
| 4 | "`lungfish conda install --pack read-mapping variant-calling`" | true | `cli-help/conda.txt` `==== conda install ====`, `--pack  Install a plugin pack instead of individual packages` with `<packages>` as repeatable positionals. Both pack IDs exist in the lock | |
| 5 | "Lungfish's `conda install` is idempotent: it does a hash check and exits without re-downloading if the pack is current." | unverifiable | `cli-help/conda.txt` `==== conda install ====` says nothing about idempotence or a hash check | Settled by reading the conda installer. |
| 6 | "If your lab uses shared tool storage, set `LUNGFISH_CONDA_ROOT=/path/to/shared/conda` in the shell that runs Lungfish" | true | `ManagedStorageConfigStore.swift:185` reads `environment["LUNGFISH_CONDA_ROOT"]` as an override, and `:126` reports the resolved root back | |
| 7 | "Confirm there is at least 5 GB of free space on the drive holding the conda root" | unverifiable | No size threshold appears in the source or help. The pack contents vary widely by pack | Either drop the number or derive it from a real pack's on-disk size. |
| 8 | "A firewall or proxy that blocks bioconda will hang the solver indefinitely; on a corporate or institutional network, ask the network team about an HTTPS proxy and set `HTTPS_PROXY` in the shell that runs Lungfish." | changed | `HTTPS_PROXY` appears nowhere in `Sources/`, so it is honored (if at all) by micromamba and the network stack rather than by Lungfish Genome Explorer | Keep the advice, and say plainly that `HTTPS_PROXY` is read by micromamba and the underlying network stack, not by LGE itself. |
| 9 | "If a classifier asks for a database that the Plugin Manager does not list, the database pack is separate from the tool pack. EsViritu, TaxTriage, and Kraken2 each have their own database pack, installed the same way: `lungfish conda install --pack <database-name>`." | false | Databases are not packs. The lock keeps them in a separate `databases` array with 16 entries and their own identifiers (`kraken2-viral`, `esviritu-viral-v3`, and so on), and `--pack` takes a `packID` from the ten-value `packTools` list. Databases are managed by `lungfish conda db` and, for EsViritu, `lungfish esviritu download-db` | "Databases are managed separately from tool packs. Use `lungfish conda db list` to see what is available and installed, `lungfish conda db recommend` to see what suits this machine, and `lungfish conda db download <id>` to fetch one. EsViritu has its own shortcut, `lungfish esviritu download-db`, and `lungfish esviritu db-status` reports whether it is installed." |
| 10 | "Lungfish retries HTTP 429 responses automatically up to five times, starting with a 5-second wait and doubling to a 5-minute cap." | true | `NCBIService.swift:31-36`, `rateLimitDefaults = NCBIRetryPolicy(enabled: true, maxRetries: 5, initialBackoffSeconds: 5, maxBackoffSeconds: 300)` | |
| 11 | "register for an NCBI API key ... and set `NCBI_API_KEY` in the shell that runs Lungfish" | true | `NCBIService.swift:89`, `return normalized(environment["NCBI_API_KEY"])`. `Sources/LungfishCLI/Commands/FetchCommand.swift:461` reads the same key | |
| 12 | "Use `lungfish fetch ncbi --no-retry ...` only for scripts that intentionally fail fast." | true | `cli-help/fetch.txt` `==== fetch ncbi ====`, `--no-retry  Do not retry HTTP 429 rate-limit responses`. `NCBIService.swift:38-43` defines the `disabled` policy with `maxRetries: 0` | |
| 13 | "Provenance sidecars record retry count and backoff timing, and only record whether an API key was provided." | changed | `NCBIService.swift:96-98` redacts the key from logged URLs with a regex over `NCBI_API_KEY=`, which supports the "never records the value" half. The retry-count and backoff-timing fields are not confirmed | Keep the redaction claim. The recorded retry fields are settled by reading the fetch provenance writer. |
| 14 | "the operation row's provenance disclosure shows `Falling back to SRA Toolkit (prefetch + fasterq-dump)`" | changed | `cli-help/fetch.txt` `==== fetch sra ====` confirms the ENA-first design and `--use-toolkit` confirms the toolkit path exists. The literal message string is not printed anywhere in the dump | Keep the behavior and verify or drop the verbatim message. |
| 15 | "Nucleotide accessions need the version suffix (`MN908947.3`, not `MN908947`)." | changed | `cli-help/fetch.txt` `==== fetch ncbi ====` shows both forms in its own examples. `lungfish fetch ncbi NC_002549 --save-to ebola.gb` has no version suffix, and `lungfish fetch ncbi MN908947 NM_000546 --save-to sequences.gb` mixes them | "Give the version suffix when you want a specific record version. NCBI resolves an unversioned accession to its current version, which can change under you." |
| 16 | "Assembly accessions go through `lungfish fetch genome`, not `lungfish fetch ncbi`." | changed | `cli-help/fetch.txt` `==== fetch genome ====` accepts both, saying "Assembly accessions (GCF_/GCA_) are resolved through the assembly database. Any other accession is fetched from nucleotide, which returns the exact record requested" | "Assembly accessions go through `lungfish fetch genome`. That command also accepts a nucleotide accession, and it warns why the distinction matters: asking the assembly database for a nucleotide accession returns the linked assembly instead, under a different sequence name." |
| 17 | "verify the preset matches the platform (`sr` for Illumina, `map-ont` for Nanopore, `map-hifi` for PacBio HiFi)" | true | `cli-help/map.txt` `==== map ====`, `--preset  Mode/preset. minimap2: sr, asm5, splice, map-ont, map-hifi, map-pb.` | |
| 18 | "Re-run it manually: `lungfish bam adopt-mapping --bundle <bundle> --mapping-result <dir> --name <track-name>`. The `--name` option is required." | true | `cli-help/bam.txt` `==== bam adopt-mapping ====`, `USAGE: lungfish-cli bam adopt-mapping [<options>] --bundle <bundle> --mapping-result <mapping-result> --name <name>` | |
| 19 | "The CLI flag is `--ivar-primer-trimmed`." | true | `cli-help/variants.txt` `==== variants call ====`, `--ivar-primer-trimmed  Confirm the BAM was primer-trimmed before iVar calling` | |
| 20 | "The Variant Calling dialog auto-confirms when the primer-trim sidecar is present." | unverifiable | The CLI flag's help does not describe GUI auto-confirmation | Settled by reading the variant-calling dialog state. |
| 21 | "re-create the bundle with `--annotation` pointing at the GFF3 file" | true | `cli-help/bundle.txt` `==== bundle create ====`, `--annotation  Annotation file(s) to include` | |
| 22 | "If a project opens empty, the project's `manifest.json` may be corrupted." | changed | This may be true, but the far more common cause is now a project lock, which the GUI handles with its own dialog and read-only banner. That path is not in this table at all | Add a lock row to the Project and file integrity table before the manifest row. See the Missing table below. |
| 23 | "Missing index files (`.fai`, `.bai`, `.tbi`) regenerate automatically when Lungfish needs them ... the underlying tools are `samtools faidx`, `samtools index`, and `tabix`." | true | The lock pins `samtools` 1.24 with executable `samtools`, and `htslib` 1.24 with executables `bgzip` and `tabix`, so all three tools ship | |
| 24 | "The Standard database needs roughly 50 GB. The PlusPF database needs roughly 80 GB. On a 16 GB MacBook, only the Viral database (about 500 MB) fits." | changed | The lock lists `kraken2-standard`, `kraken2-standard-8`, `kraken2-standard-16`, `kraken2-pluspf`, `kraken2-pluspf-8`, `kraken2-pluspf-16`, and `kraken2-viral`, all at version `20260626`. The `-8` and `-16` variants are precisely the capped-memory builds a 16 GB machine can run, and the chapter never mentions them | "The full Standard and PlusPF databases need far more RAM than a laptop has. Both ship in capped variants, `Standard-8` and `Standard-16`, `PlusPF-8` and `PlusPF-16`, whose numbers are their approximate memory ceilings in gigabytes. On a 16 GB machine, use `Standard-8` or the Viral database. Run `lungfish conda db recommend` to see what this machine can handle." |
| 25 | "Kraken2 loads the entire database into RAM." | true | This is upstream Kraken2 behavior, and the existence of the `-8` and `-16` capped builds in the lock's `databases` array is the direct consequence | |
| 26 | "For variant calling on amplicon data, the chapter on iVar mentions that Lungfish raises the mpileup depth cap to 600,000." | changed | This forwards to power-user-notes.md, whose own mpileup flag block is itself unverified against source (see that chapter's rows 3 and 4) | Verify the 600,000 figure once, in power-user-notes.md, and let this chapter cite it. |
| 27 | "iCloud Drive synchronization can corrupt project folders mid-write" | unverifiable | No iCloud-specific handling appears in the source read | Settled by testing, or reframe as a general caution about eventually-consistent sync services rather than a specific corruption claim. |
| 28 | "NFS-mounted lab storage works but file locking can fail on some configurations, leading to \"could not acquire lock on manifest.json\" errors." | changed | The project lock is at `.lungfish/project.lock` (`ProjectLock.swift:108-111`), not on `manifest.json`, and the conda mutation lock is at `<conda-root>/.install.lock` (`CondaRootMutationLock.swift:22`). No lock is taken on `manifest.json`. The MEMORY note about exFAT AppleDouble sidecars also suggests "needs file locks" is often a misreport | "On network storage, lock failures surface against `.lungfish/project.lock` for a project or `<conda-root>/.install.lock` for a plugin install, not against `manifest.json`. A reported locking failure is often a metadata-sidecar problem rather than a real lock problem, so check for stray dot-underscore files before changing mount options." |
| 29 | "For team workflows, the right answer is currently: one project per researcher per analysis, on local disk. Multi-user shared projects are not yet supported." | false | Shared projects are supported, and the manual has a chapter about them. `cli-help/project.txt` exposes `lock`, `unlock`, and `migrate`. `ProjectLockResolutionDialog.swift` and `ProjectLockWarningPresentation.swift` implement the GUI side. And shared-projects.md documents the whole workflow | "For team workflows, coordinate with project locks. See [Shared Projects](shared-projects.md), which covers `lungfish project lock`, the read-only banner the app shows when someone else holds a lock, and how to recover a stale one." |
| 30 | "Lungfish reads older bundle versions transparently but will refuse to write to a bundle version older than the current." | unverifiable | `cli-help/project.txt` `==== project migrate ====` confirms current bundles are left untouched and unsupported ones are reported without mutation, but says nothing about a write refusal | Settled by reading the bundle writer's version gate. |
| 31 | "`lungfish project migrate <project>` scans bundles, reports the schema versions it can see, and synthesizes the current reference-bundle fields where it safely can. Add `--dry-run` to scan without writing." | true | `cli-help/project.txt` `==== project migrate ====` overview and `--dry-run  Report planned actions without modifying files` | |
| 32 | "The plugin pack versions (`lungfish conda list`)" | changed | `cli-help/conda.txt` `==== conda list ====` lists packages in an environment, not pack versions | "The installed tool versions (`lungfish version --tools` for the managed set, `lungfish conda list` for what is inside each environment, and `lungfish conda envs` for which environments exist)." |
| 33 | "For complex failures that involve multiple operations, the project's `.lungfish/logs/cli.log` carries the full transcript." | false | No such path exists. `.lungfish/logs` and `cli.log` appear nowhere in `Sources/`. The real on-disk record is `~/Library/Logs/<app name>/Operations/Failures`, written by `OperationFailureReportStore` | "Failed operations write a report to `~/Library/Logs/<app name>/Operations/Failures` as the failure happens, so you can read it after quitting the app. The store keeps the most recent 50 reports and prunes older ones on each write. Attach the relevant report to the bug." |
| 34 | "Do not include the project's data files; the log alone usually has enough context." | true | Consistent with the failure report's design, which captures the operation's command, status, and stderr rather than the data | |
| 35 | "The Lungfish app version (Lungfish menu > About Lungfish)" | changed | `Sources/LungfishApp/App/MainMenu.swift:93-97` adds `"About \(appIdentity.fullName)"` to the application menu, whose title is the app's short name | "The app version (the Lungfish Genome Explorer menu, then About Lungfish Genome Explorer, or `lungfish version` from a terminal)." |
| 36 | "File bug reports through the GitHub repository linked from the Lungfish Help menu" | changed | `Sources/LungfishApp/App/MainMenu.swift:975-1000` builds the Help menu with "Lungfish Genome Explorer Help", "Getting Started", and "VCF Variants Guide". No GitHub link is visible in the region read. `cli-help/_root.txt` does print the repository URL: "For more information, see: https://github.com/dhoconno/lungfish-genome-explorer" | Verify a Help-menu GitHub item exists. If it does not, point the reader at the repository URL that `lungfish --help` prints. |

### Missing from this chapter

| Feature or setting | Evidence it exists |
|---|---|
| The whole project-lock failure mode, which is the most likely reason a reader cannot write to a project. The window title gains " (Read Only)", a banner explains who holds the lock, and write operations are blocked | `AppDelegate.swift:673`. `ProjectLockWarningPresentation.swift:16-31`. `ProjectWriteGatePresenter.swift:22`. `ProjectLockResolutionDialog.swift:44-66` |
| The conda mutation lock and its two messages, which a reader running two installs at once will hit. They read "waiting for conda lock held by pid <n>" and "conda root is read-only; reinstall as the admin user" | `CondaRootMutationLock.swift:95`, `:12` |
| `lungfish debug env --check-tools` and `lungfish debug env --tool <name>`, the first diagnostic for any missing-tool error | `cli-help/debug.txt` `==== debug env ====` |
| `lungfish debug container --pull-test`, the first diagnostic for any container-runtime failure, which is what a Nextflow-backed workflow depends on | `cli-help/debug.txt` `==== debug container ====` |
| `lungfish debug resource-smoke`, which verifies packaged runtime resources without launching the app UI | `cli-help/debug.txt` `==== debug ====` |
| `lungfish debug workflow-log`, a log parser for workflow failures, and `lungfish debug fastq-ingest` | `cli-help/debug.txt` `==== debug ====` |
| `lungfish tools update --plan`, which names every install, reinstall, removal, and database update this machine is missing against the pinned set, and exits 10 when work is pending | `cli-help/tools.txt` `==== tools update ====` |
| `lungfish conda db recommend`, which answers "which Kraken2 database fits this machine" directly, and `conda db list` and `conda db info` for what is installed and current | `cli-help/conda.txt` `==== conda db ====` |
| `lungfish esviritu db-status`, the EsViritu-specific database check | `cli-help/esviritu.txt` `==== esviritu ====` |
| `lungfish taxtriage check-prerequisites`, which verifies Nextflow and the container runtime before a TaxTriage run | `cli-help/taxtriage.txt` `==== taxtriage ====` |
| `lungfish analyze validate <files>... [--strict]`, the direct answer to "is this file malformed" | `cli-help/analyze.txt` `==== analyze validate ====` |
| `lungfish bundle validate <bundle>`, the direct answer to "is this bundle intact" | `cli-help/bundle.txt` `==== bundle ====` |
| `lungfish provenance verify`, which detects a sidecar or report that changed after signing | `cli-help/provenance.txt` `==== provenance verify ====` |
| The VCFv3 rejection and its exact remedy, which is a real import failure with a precise fix | `VCFReader.swift:490`, `:826`, "Convert to VCF 4.x with bcftools convert or vcf-convert (vcftools) before importing" |
| The `convert` in-place refusal. Passing the same path as input and output, or a symlink to it, is rejected | `cli-help/convert.txt` `==== convert ====`, "Input and output must be different files; in-place, symlink and hard-link aliases are rejected." |
| BigWig and BigBed are detect-only. A reader who imports one and sees nothing render needs to know the reader is unavailable in process, not that the file is broken | `FormatRegistry+BuiltInDescriptors.swift:192-195`, `:208-211` |
| The `fetch genome` accession trap, which the help itself calls out. Asking the assembly database for a nucleotide accession returns the linked assembly under a different sequence name | `cli-help/fetch.txt` `==== fetch genome ====` |
| `--format json` on any command, which turns an opaque failure into a parseable one | `cli-help/_root.txt` `OPTIONS:` |

### Screenshots

| Marker or planned shot | Still valid? | Why |
|---|---|---|
| (none) | changed | Front matter has `shots: []`, no `planned_shots` key, and the body carries no markers. Two shots would earn their place. One is the expanded Operations Panel row the "Collecting diagnostics" section tells the reader to gather, and the project-lock dialog, which is the failure mode the chapter is currently missing entirely |
