# Reader 1 report: Power User Notes

Persona: sophomore, one genetics course, no lab time, never opened a terminal. I was sent here by another chapter to find one caveat, and I have written down every place I lost the thread.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| What it is, "the argument list underneath it" | "Argument list" is the thing the whole appendix is about and it is never explained. I did not know an argument was a piece of typed text handed to a program. | Gloss argument and argument list in the first paragraph. |
| What it is, "Argument lists are shown in `bash` blocks" | I do not know what bash is. The persona note says I have never opened a terminal, and nothing here tells me these blocks are shell commands rather than a file I edit. | One clause saying a bash block is a line you would type into a terminal. |
| What it is, "the real ones are absolute paths into a scratch folder" | I did not know what an absolute path was or what a scratch folder is, and neither is in the glossary list at the top. | Gloss absolute path once, or say "the full location of a temporary working folder". |
| What it is, "check LGE's choices before you commit a run" | I could not tell what commit means here. I first read it as the git sense I have heard about. | Use "start" or "launch" instead of commit. |
| Reading a command out of the source, "`ViralVariantCallingPipeline.swift:1229` names a file" | I have never looked at source code and I do not know where `Sources/LungfishWorkflow/` is or whether I have it. The chapter assumes I can open the repository. | Say plainly that these citations are for readers with a source checkout and that the sidecar gives the same answer without one. |
| Reading a command out of the source, "small pure functions that take a request" | "Pure function" is a programming term I do not know, and it is load-bearing for the claim that reading tells you the whole command. | Drop the word pure, or gloss it as a function whose output depends only on its input. |
| Reading a command out of the source, "LGE's value wins on any flag you both set" | I did not know what a flag was at this point. Flag first appears here and is used everywhere afterwards. | Gloss flag at this first use as a switch written with a dash. |
| iVar variant calling, "is three processes rather than one" | I could not tell whether process means three separate programs or three stages of one. Both readings fit. | Say "three separate programs run one after another". |
| iVar variant calling, "That pileup pipes into `ivar variants`" | "Pipes into" is terminal vocabulary. I pictured plumbing and could not confirm what actually moved. | Say the output is handed straight to the next program without a file in between. |
| iVar variant calling, "an LGE-internal converter turns that table" | I did not know whether this converter is something I run or something that happens on its own. | One clause saying LGE runs it automatically as part of the same operation. |
| samtools mpileup table, "`-d 600000` Raise the per-position read cap" | I could not judge 600000. The row says the default is 8000 and that deep amplicons pass it, but nothing says what depth my own data would have. | Give the typical depth range of an amplicon run so the two numbers have a scale. |
| samtools mpileup table, "`-Q 20` ... a one-in-a-hundred chance of being wrong" | The jump from 20 to one in a hundred is unexplained. I could not work out the rule. | Add that 30 means one in a thousand, so the pattern is visible. |
| samtools mpileup table, "`-q 0` Impose no mapping-quality floor" | Mapping quality is introduced here with no explanation, and it is a different thing from the base quality one row above. I conflated them. | Gloss mapping quality at first use and say how it differs from base quality. |
| samtools mpileup, "a 20,000-fold amplicon silently reports as 8000-fold" | I do not know what "fold" means as a unit. I guessed coverage depth but was not sure. | Say "20,000-fold coverage, meaning each position is read 20,000 times". |
| ivar variants, "`-p ivar.tsv-prefix`" | The prefix flag appears in the command but is not in any table or sentence, so I could not tell whether it was a setting I control. | Say the prefix is set by LGE and not exposed. |
| ivar variants, "Your own text from `--extra-args` is inserted" | `--extra-args` is used as though I already know it. It is only explained at the very end of the chapter, many pages later. | Introduce `--extra-args` in one sentence at first use, or forward-link to the section that explains it. |
| ivar variants, "On the command line those two are `--min-af` and `--min-depth`" | I did not know there were two different names for one setting, one in the window and one typed. This sentence assumes I know LGE has both a window and a command line. | Say the dialog name and the typed name refer to the same setting. |
| The four iVar tuning flags, "This trips people up, so it is stated plainly" | I had to read the whole section twice. The heading says the flags do not reach iVar, then the text says they are named for iVar, and I could not hold both until the third sentence. | Lead with what the four flags actually do, then say what they do not do. |
| The four iVar tuning flags, "`lungfish-internal ivar-tsv-to-vcf --consensus-af ...`" | I could not tell whether `lungfish-internal` is a program I could run. The name suggests I cannot, but nothing says so. | State that this command exists only inside LGE and cannot be typed. |
| Codon merging, "a single VCF row with a multi-base REF and ALT" | REF and ALT are not glossed here. I met them in a genetics course as reference and alternate but had to guess. | Expand REF and ALT once on first use. |
| Codon merging, "a fixed band from 0.40 to 0.60" | I could not judge why that band exists or what it means biologically. Nothing says what a frequency near 0.5 signifies. | One clause saying a frequency near half suggests a heterozygous or evenly mixed site. |
| Codon merging, "None of this is phase-aware" | Phase is used before phased haplotypes is explained two sentences later, and I did not know what riding the same molecule had to do with it until the end. | Put the plain-language definition of phase before the word phase-aware. |
| LoFreq variant calling, "builds an error model from the base qualities themselves" | I could not follow how base quality becomes an error model or what "outnumber what that error rate alone would produce" is comparing. I read it three times. | Say LoFreq estimates how many wrong bases to expect by chance and flags positions with more than that. |
| The ordinary case, "Your `--extra-args` text lands between `call` and `-f`" | I could not tell why the landing position matters here, since this command has no flag I might duplicate. | Say what it means practically, or move the landing rule to one place. |
| The ordinary case, "a script that expects those is describing a different wrapper" | Wrapper is used without explanation. I did not know LGE was called a wrapper. | Gloss wrapper the first time it appears, in What it is. |
| The ordinary case, "the HG002 chromosome 20 slice" | HG002 is named as though I know it. I do not know it is a reference human sample. | Say HG002 is a well-characterised human reference sample. |
| The ordinary case, "six further steps of LGE's own normalisation" | Normalisation is not explained and the six steps are not listed, so I could not tell what happened to my variants. | Name what normalisation does in one clause, or link to where the six steps are listed. |
| Indel calling, "unless you pass `--call-indels` through `--extra-args`" | I could not perform this. Nothing here shows me where I would type it in the window, and I have never used a terminal. | Say where in the dialog the extra-arguments field sits. |
| Indel calling, "LoFreq's port of the Dindel algorithm" | Dindel is named with no explanation and no glossary entry. | Say it is an older indel-calling method, or drop the name. |
| Indel calling, "an alignment straight out of `samtools sort` carries none" | I did not know sorting was a separate step or that it strips these scores. The chapter has not introduced samtools sort. | Say any ordinary alignment lacks these scores, without naming the tool. |
| LoFreq's version string, "records a `variantCallerVersion` reading `FATAL(...)`" | I could not tell whether seeing FATAL in my own file means my run failed. The next sentence reassures me, but I was alarmed first. | Put "the run is fine" before the error text, not after. |
| Primer trimming, "the sliding-window trim" | The sliding window is named as though defined, but this chapter never defines it. | Gloss the sliding window in one clause, or link to the primer-trimming chapter. |
| Primer trimming, "`--ivar-primer-offset` at 0, which shifts the primer coordinates" | I could not judge when I would need a nonzero offset or how I would discover that I did. | Name the symptom that points at an offset. |
| Primer trimming, "the alignment's own `@SQ SN` header line" | `@SQ SN` is raw file syntax I have never seen and it is not explained. | Say "the line in the alignment file naming the reference". |
| Primer trimming, "trims with exit status 0 and no warning" | Exit status is glossary-linked but the meaning of 0 is not stated here, and 0 sounding like nothing happened confused me. | Say exit status 0 means the program reported success. |
| Mapping, "The four mappers get four different argument shapes" | Four is claimed but five commands follow if I count, and the list shows minimap2, bwa-mem2, bowtie2, and bbmap, so the count is right but I recounted twice. | No fix needed for the count, but naming the four in the sentence would stop the recount. |
| Mapping, "`-R '@RG\tID:...'`" | The backslash-t and the ellipsis meant nothing to me. I did not know whether I fill those in. | Say the ellipsis stands for values LGE fills in automatically. |
| Mapping, "The `-x` preset for minimap2 defaults to `sr`" | Preset and read type are used as though I chose one somewhere. I did not know there was a read-type declaration. | Say where the read type is declared in the window. |
| Mapping, "a BBMap argument of yours is the one that loses" | This inverts the rule from two sentences earlier and I had to read both again to see which way round each tool goes. | A two-column list of tool and who wins would settle it at a glance. |
| Kraken 2, "`--confidence 0.0 --minimum-hit-groups 2`" | Both numbers appear only in the command and are never explained or judged. I do not know what confidence 0.0 means. | Add a short table for these two the way mpileup gets one. |
| Kraken 2, "the abundance re-estimator that runs afterwards" | Abundance re-estimation is not explained. I could not tell what Bracken changes. | One clause saying Bracken converts read counts into species proportions. |
| Kraken 2, "an LGE kreport is eight columns wide" | I could not tell whether this affects me if I only ever look at the window. | Say this matters only if you open the file in another program. |
| Assembly, "SPAdes takes `--isolate`, `--meta`, or `--plasmid`" | The three profiles are never explained. I do not know which fits my sample. | One clause each saying what kind of sample each profile is for. |
| Assembly, "a `nano-hq` profile is passed as `--flye --nano-hq`" | This reads as though flye is passed to itself and I could not parse it. I read it three times. | Show the resulting command instead of describing the substitution. |
| Assembly, "Hifiasm gets `--ont` prepended when the reads are Nanopore" | Nanopore, ONT, nano-hq, and HiFi are all used without a line telling me these are sequencing technologies. | One sentence naming the read technologies before the assembler list. |
| Assembly, "MEGAHIT is given `--no-hw-accel` on hosts where LGE caps its threads" | Hardware acceleration, host, and thread capping all arrive at once. I could not tell whether my Mac is such a host. | Say plainly that this applies on Apple Silicon, if that is what is meant. |
| Assembly, "leaving it automatic can zero out a small assembly" | "Zero out" was ambiguous. I could not tell if it produces no contigs or wrong ones. | Say it can produce no contigs at all. |
| GATK, "`-ERC` selects the reference-confidence mode" | GVCF is introduced by acronym and its expansion never appears. I did not know what the G stands for. | Expand GVCF once. |
| GATK, "LGE never creates the `.dict` sequence dictionary" | I could not perform the fix. The text says I must make it myself and never says how, only that another chapter covers it. | Name the one command in a clause, or say explicitly that the linked chapter gives it. |
| The provenance sidecar, "gathers a bundle's sidecars under a `provenance/` folder" | Three different sidecar locations are given in one sentence and I could not hold them apart. | Split into three short sentences or a three-row table. |
| The full envelope, "carries the following top-level keys" | Top-level key and camelCase are JSON vocabulary I do not have. The JSON block itself is the first JSON I have read. | One sentence saying JSON stores labelled values and a top-level key is an outermost label. |
| The full envelope, "`"workflowVersion": "Lungfish dev (0)"` " | The example says "dev (0)" while the tool version says 2026.9.13, and I could not tell which one is my real version. | Say why the two disagree in the example, or use a shipped build. |
| The full envelope, "`durableReplayArgv`" and "`reproducibleCommand`" | Three near-identical fields hold nearly the same text and nothing says how they differ. | One clause each saying what distinguishes them. |
| The full envelope, "the writer emits an older and a newer spelling" | I could not tell which of the two spellings I should read in my own file. | Say to read the newer one and treat the older as legacy. |
| The workflow-run shape, "Some operations write a second, older shape" | I could not tell how I would know which shape a file in front of me is. | Say which key to look for to tell the two apart. |
| The workflow-run shape, "written as the string `caller-default`" | I read this twice. I could not tell whether `caller-default` means the setting was ignored or that the tool's own default was used. | Say it means the setting never reached the tool. |
| The steps array, "an optional captured `stderr`" | stderr is terminal vocabulary and is not glossed. | Say "the error text the tool printed". |
| The steps array, "`bioconda::samtools=1.24=h36b3a25_1`" | I could not read this string or tell which part is the version and which is the build. | Label the three parts in one clause. |
| Summarising cost, "reports the sidecar count, the completed run count" | This is a terminal command and I cannot run it. Nothing says whether the same numbers appear anywhere in the window. | Say whether the Operations panel shows the same totals. |
| Summarising cost, "a project full of them reports a sidecar count of zero" | I read this three times. The two filenames differ only by a prefix and I kept losing which one is counted. | Show the two names on separate lines and mark which is counted. |
| Summarising cost, "produces an error line and exit status 0" | I could not tell why an error with status 0 is dangerous until the next sentence, and even then I did not know what a script checking status means. | Say a script that only checks the status will believe the command succeeded. |
| What the campaign measured, "produced 104 allele rows for the sample `WD1_S148_L001`" | I could not judge 104. Nothing says whether that is a normal count. | Say what a typical allele row count looks like. |
| What the campaign measured, "differ in `docProps/core.xml`, an embedded timestamp" | I did not know a spreadsheet contained XML files inside it, so the sentence read as a contradiction with "the sheets themselves are byte-identical". | Say an xlsx file is a zip of parts and only the timestamp part differs. |
| What the campaign measured, "It fails most runs with both shipped workarounds active" | "Both shipped workarounds" points back to the assembly section but I could not tell which two were meant without scrolling. | Name the two workarounds again in this sentence. |
| What the campaign measured, "occasionally doubles a small circular genome" | I could not picture a doubled contig or judge whether doubling is common. Occasionally and consistently are given for two tools with no numbers. | Say what fraction of runs doubled in the campaign. |
| What the campaign measured, "the global `--threads` is read before any subcommand sees it" | I did not understand global versus subcommand options and could not tell whether this bug affects the window too. | Say whether the Threads field in the window has the same problem. |
| The consolidated caveat list, "**iVar.** Minimum Allele Frequency and Minimum Depth reach iVar and are recorded as `caller-default` for every other caller" | This sentence carries two unrelated defects and a link, and I lost the subject halfway. I read it three times. | Split into two sentences. |
| The consolidated caveat list, "**Medaka and Clair3.** Every Medaka run fails" | Medaka and Clair3 appear nowhere else in this chapter, so I met them for the first time in a caveat about them being broken. | One clause saying both are Nanopore variant callers. |
| The consolidated caveat list, "**Paths.** A command whose working directory sits under `/private/tmp`" | Working directory, `/private/tmp`, and symlink are all terminal concepts. I could not tell whether I could ever hit this from the window. | Say this affects only typed commands, if that is true. |
| Cross-architecture drift, "different bytes on Intel and on Apple Silicon while staying logically equivalent" | I did not know results could differ in bytes but agree in meaning, and I could not tell whether that is a problem for my work. | Say the biology is the same and only an exact file comparison would notice. |
| Cross-architecture drift, "can change soft-clip boundaries or realignment behaviour" | Soft-clip and realignment arrive with no gloss in a section far from where reads were discussed. | Gloss both, or say "small changes in how reads are trimmed and placed". |
| Pinning an environment, "It does not pin every transitive dependency underneath" | Transitive dependency is unexplained and it is the whole point of the paragraph. | Say a tool depends on other software which depends on more, and only the top layer is pinned. |
| Pinning an environment, "`lungfish-cli conda offline-export --pack <id> --output <dir>`" | I could not perform any of the three commands in this section. The angle brackets are not explained either. | Say angle brackets mark a value you replace. |
| Pinning an environment, "exists only to refuse the operation" | I read this twice. A command that exists in order to fail was not something I could believe on first pass. | Say the command is present but disabled in this release. |
| Pinning an environment, "The subcommand's `--format` collides with the global `--format`" | The global-versus-subcommand distinction returns here and is still not explained. | Explain global options once, early. |
| The conda mutation lock, "takes an exclusive advisory lock on a file" | Advisory lock is glossary-linked but the sentence still assumes I know what locking a file means. | One clause saying a lock is a marker that makes other processes wait. |
| The conda mutation lock, "writes `waiting for conda lock held by pid <n>` to standard error" | pid and standard error are both unexplained. | Say pid is the process number and standard error is where messages print. |
| The conda mutation lock, "never let two CI jobs restore and update the same cached conda root" | CI appears here with no expansion. It is expanded nowhere in this chapter. | Expand continuous integration at first use. |
| Asserting an installation, "`--plan` exits 10 when work is pending" | Four exit codes are given in one paragraph with no list, and I lost track of which was which. | Put the four codes in a small table. |
| Asserting an installation, "`--include-databases` adds the advisory ones" | Required versus advisory databases are distinguished here for the first time with no explanation of either. | One clause saying what makes a database advisory. |
| Debugging from the Operations panel, "**Operations > Show Operations Panel** (Cmd-Shift-P)" | This is the only place in the chapter I could actually follow, and it arrives on page seven. I had assumed until here that none of the chapter applied to me. | Say near the top which sections need a terminal and which do not. |
| Failure reports, "at `~/Library/Logs/<app name>/Operations/Failures`" | The tilde is not explained and I would not know how to reach that folder without a terminal. | Say it is inside your Home folder's Library, and that Reveal in Finder opens it. |
| Failure reports, "a failed write of a report is swallowed rather than raised" | Swallowed and raised are programming words. I guessed but was not sure. | Say the error is discarded rather than reported. |
| Reaching a flag, "both `--extra-args` and the repeatable `--extra-arg` exist on some commands and differ by one letter" | I could not tell which commands have which, or what repeatable means. | Say repeatable means you may give it more than once, and name where to check. |
| Reaching a flag, "it costs you the provenance record, since nothing about the run is written down" | Three escape hatches are ranked by provenance cost, but I could not tell whether losing provenance breaks anything downstream in LGE. | Say what stops working when a file has no sidecar. |
| Querying variants, "applies a per-sample smart filter over bundle variants" | Smart filter is named as though it is a defined feature. I do not know what it is. | Gloss smart filter or link to the chapter that defines it. |
| Querying variants, "reads a `FORMAT DP` field that bcftools does not write" | FORMAT and DP are VCF internals I have never seen, and bcftools is not introduced anywhere in this chapter. | Say DP is the depth field inside a VCF row. |

Three closing lines.

The one thing I learned is that the four flags named after iVar never reach iVar, which explains why I could not reproduce a result by running the tool myself.

The one thing I still could not do is run any of the `lungfish-cli` commands in this chapter, because nothing tells me how to open a terminal or whether the window offers the same thing.

The sentence I liked most is "when this page and a specific run's sidecar disagree, the sidecar wins, because it records what actually happened rather than what the source says usually happens."
