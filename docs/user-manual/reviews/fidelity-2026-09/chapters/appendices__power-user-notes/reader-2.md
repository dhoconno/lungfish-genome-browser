# Reader 2 report: Power User Notes

Persona. Senior undergraduate, two years of wet-lab work, comfortable with library prep and PCR, has never analyzed sequencing data and has never opened a terminal. I came here because another chapter sent me to look up one caveat.

| Location | Issue | Suggested fix |
|---|---|---|
| Front matter, "audience: power-user" | The chapter announces its audience as "power user" but never says what that means or whether I am one. Another chapter sent me here, so I do not know if I am allowed to be reading this. | Open "What it is" with one sentence saying this appendix assumes you work at a command line, and that bench readers should follow the link back to their workflow chapter. |
| What it is, "the argument list underneath it" | "Argument list" is never glossed. I know what a setting in a dialog is. I do not know that a tool is being run by typing words at it, or that those words are called arguments. | Gloss argument at first use as the words typed after a program's name that tell it what to do. |
| What it is, "read out of the LGE source that builds it" | "Source" is undefined. I do not know if this means the app's own code, a manual, or a database, so I cannot tell how trustworthy the flags are. | Say "source code" once and add three words saying it is the app's own program text. |
| What it is, "the release defaults from dependency set 2026.2" | I cannot judge this. I do not know whether "2026.2" is the version of LGE I have, or something separate that I would have to check. Nothing tells me how to find out which dependency set my copy uses. | Add one sentence saying where in the app or the sidecar to read your own dependency set. |
| What it is, "a scratch folder" | Not explained. I do not know if a scratch folder is something I created, something LGE created, or somewhere my results might be lost. | Gloss scratch folder at first use as a temporary working folder LGE creates and deletes. |
| Reading a command out of the source, "`ViralVariantCallingPipeline.swift:1229` names a file under `Sources/LungfishWorkflow/`" | I could not perform this. The text tells me the citation names a file and a line, but never says where that folder is on my computer or how I would open it. I do not have the app's code and would not know where to get it. | Add one sentence saying these citations are for readers who have the LGE source checkout, and that everyone else can skip them. |
| Reading a command out of the source, "small pure functions that take a request and return an array of strings" | Three unfamiliar terms in one sentence. I had to read it twice and still do not know what a pure function or an array is. | Replace with a plain sentence saying the code that builds each command does nothing else, so reading it is enough. |
| Reading a command out of the source, "which means LGE's value wins on any flag you both set" | "Flag" is used here for the first time and never glossed. It reappears about forty times after this. From the tables I eventually guessed a flag is a word starting with a dash, but I guessed. | Gloss flag at first use, before the iVar section, as one of those dash-prefixed words that switches a tool behaviour on or off. |
| iVar variant calling, "An iVar variant call in LGE is three processes rather than one" | "Process" is used in a specific computing sense here. I read it as "a laboratory procedure" the first time and was confused when the next paragraph described piping. | Say "three programs run one after another" instead. |
| iVar variant calling, "That pileup pipes into `ivar variants`" | "Pipes into" is never explained. I know pipetting, not piping. I could not tell whether a file is written in between or not, which matters if I want to inspect the pileup. | Gloss piping once as sending one program's output straight into the next without saving a file. |
| samtools mpileup, "The mpileup arguments are fixed" | I could not act on this section at all. The `bash` block shows a command but the chapter never says where I would type it, and the front matter says I should not need to. I did not know whether this is something I run or only something I read. | Add a lead-in line saying these blocks are shown for reading and checking, not for copying, unless you use the CLI. |
| samtools mpileup table, "`-d 600000` Raise the per-position read cap from the tool default of 8000" | I cannot judge 600000. Nothing tells me what depth my own amplicon run would reach, so I do not know if the old cap would have hurt me or not. | Add the sentence that follows the table's example depth ("a 20,000-fold amplicon") into the table row itself, so the number has a scale beside it. |
| samtools mpileup table, "`-q 0` Impose no mapping-quality floor. Mapping quality is judged later rather than here" | Mapping quality is used without a gloss and is a different thing from the Phred score glossed two rows above. I could not tell the two apart. | Gloss mapping quality at first use as the confidence that a read sits at the right place in the genome, distinct from base quality. |
| samtools mpileup, "makes every allele frequency computed against that pileup wrong in the same direction" | I had to read this twice. "Wrong in the same direction" does not tell me whether frequencies come out too high or too low, which is the thing I would want to know. | Say which direction. |
| ivar variants, "Your own text from `--extra-args` is inserted" | `--extra-args` appears here with no introduction. It is only explained fifteen sections later under "Reaching a flag LGE does not wrap". I read the whole iVar and mapping sections without knowing what it was. | Gloss `--extra-args` at this first use in one clause, or forward-link to the escape-hatch section. |
| ivar variants, "The `-t` value is the Minimum Allele Frequency setting ... On the command line those two are `--min-af` and `--min-depth`" | Three names for one thing in two sentences (`-t`, Minimum Allele Frequency, `--min-af`). I had to reread to work out these are the same control seen from three places. | State the mapping once as a small three-column table rather than in prose. |
| The four iVar tuning flags, "This trips people up, so it is stated plainly" | The heading told me the four flags do not reach iVar, but the section never says where I would set them or why I would want to. As a bench reader I do not know if these appear in a dialog I use. | Say in one sentence whether these four are command-line only or also appear in the Call Variants dialog. |
| The four iVar tuning flags, "`lungfish-internal ivar-tsv-to-vcf --consensus-af ...`" | I could not tell whether `lungfish-internal` is a command I can run. The name suggests not, but the block is formatted exactly like the runnable commands above it. | Add four words saying this command is internal and cannot be run by hand. |
| Codon merging, "Both frequencies inside a fixed band from 0.40 to 0.60 merges" | I cannot judge this band. Nothing says why 0.40 to 0.60 was chosen or what it means biologically, only that no setting controls it. | Add one sentence saying what a pair of frequencies near one half suggests about the underlying molecules. |
| Codon merging, "None of this is phase-aware" | "Phase-aware" is used before "phased haplotypes" is explained, and neither is glossed here. The sentence after it helped, but I had already stalled. | Gloss phase at first use as which of a person's two chromosome copies a change sits on. |
| LoFreq variant calling, "asks whether the alternate reads outnumber what that error rate alone would produce" | I had to read this twice. "Alternate reads" is not glossed, and I could not tell whether this is a statistical test with a p-value or a simple count comparison. | Gloss alternate read at first use and say in three words that this is a statistical test. |
| The ordinary case, "the HG002 chromosome 20 slice" | HG002 is dropped in with no explanation. I do not know if this is a person, a cell line, a public dataset, or a file I am supposed to have. | Gloss HG002 at first use as the public human reference sample used for benchmarking. |
| The ordinary case, "then six further steps of LGE's own normalisation around it" | "Normalisation" is unexplained and the six steps are never listed. Since the point of this section is what LGE really does, leaving six of eight steps unnamed felt like the opposite. | Gloss normalisation in one clause, or say where the six steps are listed. |
| Indel calling, "unless you pass `--call-indels` through `--extra-args`" | I could not perform this. The text never shows a complete example of what I would actually type, so I do not know whether the quoting is `--extra-args --call-indels` or `--extra-args "--call-indels"`. | Show one complete example line. |
| Indel calling, "LoFreq's port of the Dindel algorithm" | Dindel is named without any gloss and is not in the glossary list at the top. I cannot tell if I need to know it. | Drop the name, or gloss it in three words. |
| LoFreq's version string, "`FATAL(lofreq_main.c|main:336): Unrecognized command '--version'`" | Seeing a line beginning FATAL in my own provenance file would have alarmed me. The reassurance "The run is fine" comes after the block, not before it. | Move "the run is fine" to before the code block. |
| Primer trimming, "`-s` is `--ivar-sliding-window` at 4, the window width in bases" | I cannot judge 4. Nothing says what the sliding window does, only how wide it is, so a width of four bases means nothing to me. | Say in one sentence what the sliding window trims and why width matters. |
| Primer trimming, "the alignment's own `@SQ SN` header line" | `@SQ SN` is unexplained. I know a BAM has a header only from vague memory. I could not check my own file for this. | Gloss it as the line in a BAM naming which reference sequence the reads were mapped to. |
| Mapping, "The four mappers get four different argument shapes" | Four are named in the sentence but five commands follow, because BBMap is in the block and not in the count. I read the paragraph twice looking for my mistake. | Make the count match the block. |
| Mapping, "`-R '@RG\tID:...'`" | The `\t` and the trailing `...` are unexplained. I could not tell if the ellipsis is literal or means "something is omitted here". | Say once that `...` in these blocks stands for a value LGE fills in. |
| Mapping, "which for minimap2 means `--secondary=no`" | "Secondary alignments" is glossed nowhere. I do not know what a secondary alignment is or why turning it off is the safe default. | Gloss secondary alignment at first use as an additional lower-scoring placement of the same read. |
| Mapping, "a BBMap argument of yours is the one that loses" | I had to read this twice. "Placed first, ahead of everything LGE sets" and "the one that loses" seem contradictory until you work out that later arguments win. | Say the rule once ("the last value of a repeated flag wins") before giving the per-tool placements. |
| Kraken 2 classification, "`--confidence 0.0 --minimum-hit-groups 2`" | Neither number can be judged. The table format used for mpileup is dropped here, so these two appear with no meaning and no rationale at all. | Give these two the same flag table the mpileup section has. |
| Kraken 2 classification, "so that Bracken, the abundance re-estimator" | Bracken is glossed here in passing but is not in the chapter's glossary list, and the gloss "abundance re-estimator" needs its own gloss for me. | Expand to say Bracken redistributes reads assigned at high taxonomic levels down to species. |
| Kraken 2 classification, "a downstream parser expecting six will misread it" | "Downstream parser" assumes I write or run scripts. As a bench reader who was sent here for one caveat, I could not tell if this affects me. | Say concretely what breaks, for example a spreadsheet import or another tool reading the report. |
| Assembly, "so a `nano-hq` profile is passed as `--flye --nano-hq`" | I had to read this three times. `--flye` looks like a flag given to Flye, but from context it seems to be a flag given to LGE naming Flye. The two levels are never separated. | Distinguish LGE's own options from the wrapped tool's options with a one-line note. |
| Assembly, "MEGAHIT is given `--no-hw-accel` on hosts where LGE caps its threads" | I could not tell whether my Mac is such a host, and "hw-accel" is never expanded. | Expand hardware acceleration once and say plainly that this applies to Apple Silicon Macs. |
| Assembly, "SKESA is pinned to `--min_count 2` ... because leaving it automatic can zero out a small assembly" | "Zero out" is vague. I could not tell whether this means no contigs at all or fewer contigs. | Say whether the output is empty or merely reduced. |
| Assembly, "Two settings the dialog offers are silently ignored on some tools" | This is the caveat I would have been sent here for, and it is the clearest paragraph in the chapter, but it sits in the middle of a section headed only "Assembly". I nearly missed it. | Give it its own subheading naming the two settings. |
| GATK, "`-ERC` selects the reference-confidence mode" | Reference-confidence mode and GVCF are both introduced in the same clause, each explaining the other. I could not get in. | Gloss GVCF first as a VCF that records every position rather than only the variant ones, then say what `-ERC` chooses. |
| GATK, "LGE never creates the `.dict` sequence dictionary GATK requires, so a first run fails until you make it yourself" | I could not perform this. The text tells me I must make a file myself and never says how, or links to a place that says how in that sentence. | Put the forward link on this sentence, not only at the end of the paragraph. |
| The provenance sidecar, "gathers a bundle's sidecars under a `provenance/` folder" | "Bundle" is used throughout the chapter and never glossed, though it seems central. I think it is the project folder LGE makes, but I inferred that. | Gloss bundle at first use. |
| The full envelope, the JSON block | This is the longest block in the chapter and I could not read it. JSON syntax itself is never introduced, so the braces, quotes, and nesting were noise. I could find `wallTimeSeconds` only because it is an English phrase. | Add one sentence before the block saying this is JSON, that each line is a name and a value, and that indentation means containment. |
| The full envelope, "`sha256`" and "`checksumSHA256` holding the same checksum" | Checksum is glossary-linked but the reason for two spellings, and which one I should read, is left implicit. | Say plainly which of the two pairs a new reader should use. |
| The full envelope, "`wallTimeSeconds`: 0.11473691463470459" | I cannot judge this number and the precision is distracting. I could not tell whether this level of detail is meaningful or an artifact. | Note once that wall time is recorded at full float precision and only the leading digits matter. |
| The workflow-run shape, "Some operations write a second, older shape" | I could not act on this. Nothing tells me how to look at a sidecar and know which of the two shapes I am holding. | Name the one key that distinguishes them. |
| The workflow-run shape, "written as the string `caller-default`" | I had to read this twice. It took me a while to see that this records a setting I chose that was then thrown away, which is alarming and is not flagged as such here. | Add one sentence saying a `caller-default` entry means your setting had no effect on that run. |
| The steps array, the whole paragraph | Twelve field names in one unbroken sentence. I gave up partway and could not have found any one of them again. | Break it into a two-column field table like the mpileup one. |
| Summarising cost, "walks a folder and reports the sidecar count" | I could not perform this. The chapter shows `lungfish-cli ops stats "My Project.lungfish"` but never says what a shell is, where I type this, or that `lungfish-cli` is a separate program from the app I clicked on. | State once, early, that `lungfish-cli` is a terminal program and link to the chapter that installs it. |
| Summarising cost, "a project full of them reports a sidecar count of zero" | This is the sharpest trap in the chapter and reads as a throwaway clause inside a list of three limits. A count of zero would have made me think nothing had run. | Give this its own sentence and say plainly that a zero count is usually wrong rather than empty. |
| Summarising cost, "an option it does not recognise produces an error line and exit status 0" | I do not know what an exit status is at this point. It is glossary-linked but the link is the first I see of the concept, and the sentence's whole point rests on it. | Gloss exit status inline here as the number a command reports to say whether it succeeded. |
| Reproducibility, "The MHC genotyping route is deterministic on the evidence of two runs" | I cannot judge this. Two runs is a very small evidence base and the text presents it as a finding without saying so. | Say in three words that two runs is weak evidence. |
| Reproducibility, "MEGAHIT 1.2.9 is not reliable at all on Apple Silicon in this release ... A run that completes is correct" | I had to read this twice. "Not reliable at all" and "a run that completes is correct" seemed to contradict each other until I saw that failure is loud rather than silent. | Say the two halves in one sentence, that it fails visibly rather than producing wrong answers. |
| Reproducibility, "hifiasm does so consistently on the mitochondrial fixture" | "Doubles a small circular genome" is not explained. I could not tell what a doubled contig would look like in my results or why circularity causes it. | Say in one sentence why a circular genome can be assembled twice around. |
| Reproducibility, "The global `--threads` is read before any subcommand sees it, so on `variants phase` it is consumed by the root command" | I could not follow this at all. "Root command", "subcommand", and "consumed" are all unfamiliar, and the sentence is the whole explanation. | Rewrite as a plain statement that on `variants phase` the thread count you give is ignored and one thread is used. |
| The consolidated caveat list, section lead | This is the section I was sent here for and it is the best part of the chapter, but every entry links out to another chapter rather than saying what to do. As a reader chasing one caveat I now have to open a second chapter. | Add the one-line consequence beside each entry, not only the link. |
| The consolidated caveat list, "**Paths.** A command whose working directory sits under `/private/tmp`" | I do not know what a working directory is or how mine could end up under `/private/tmp` without my choosing it. So I cannot tell whether this affects me. | Say what a user action would look like that lands you there. |
| Cross-architecture drift, "different bytes on Intel and on Apple Silicon while staying logically equivalent" | I had to read this twice. "Different bytes but logically equivalent" is exactly the distinction I do not have, and it is the point of the paragraph. | Say the results agree scientifically while the files differ, and that a file comparison is therefore the wrong check. |
| Pinning an environment, "It does not pin every transitive dependency underneath" | "Transitive dependency" is not glossed and carries the whole warning. | Gloss it as the packages your packages themselves need. |
| Pinning an environment, "it is not conda-lock compatible" | Conda is glossary-linked but conda-lock is a separate thing that appears once, undefined. | Drop the name or gloss it. |
| Pinning an environment, "The subcommand's `--format` collides with the global `--format`" | I could not follow this. The idea that one program has two levels of options with the same name was never set up, and this is the second place it bites (the `--threads` case was the first). | Explain global versus subcommand options once, early, and refer back here. |
| The conda mutation lock, "takes an exclusive advisory lock on a file" | Advisory lock is glossary-linked but the sentence needs it immediately. I read the section without knowing whether a lock stops a second process or merely warns it. | Gloss it inline in one clause. |
| The conda mutation lock, "never let two CI jobs restore and update the same cached conda root" | CI appears here with no expansion, and again in the "Next" list. I do not know what it is. | Expand continuous integration at first use. |
| Asserting an installation, "`--plan` exits 10 when work is pending and 0 when nothing is needed" | I had to read this twice because the convention is inverted from what the previous sentence implied. Elsewhere in the chapter 0 means success, and here 0 means nothing to do. | Say explicitly that a nonzero status here is not a failure. |
| Asserting an installation, "`--include-databases` adds the advisory ones and has no effect alongside `--required-only`" | I cannot judge "advisory" databases. Nothing says what they are for or what I lose by deferring one. | Say in one clause what an advisory database provides. |
| Debugging from the Operations panel, "**Operations > Show Operations Panel** (Cmd-Shift-P)" | This is the one part of the chapter I could actually do, and it arrives on page eight. Everything before it assumed a terminal. | Consider moving the Operations panel section earlier, or noting near the top that it is the one GUI section. |
| Failure reports outlive the session, "a failed write of a report is swallowed rather than raised" | "Swallowed" and "raised" are programming terms used without gloss. The sentence after it saved me, but only after a reread. | Say the write failure is ignored on purpose. |
| Reaching a flag LGE does not wrap, "both `--extra-args` and the repeatable `--extra-arg` exist on some commands and differ by one letter" | I could not act on this. It tells me two nearly identical options exist and never says which one to use or what "repeatable" means. | Say which to prefer and what repeatable means in four words. |
| Reaching a flag LGE does not wrap, "a useful example on iVar is a flag the pipeline does not already set, such as `ivar variants -c`" | `-c` is given with no explanation of what it does, in a sentence whose point is that it is useful. | Say what `-c` does. |
| Querying variants, "A per-sample filter clause reads a `FORMAT DP` field that bcftools does not write" | `FORMAT DP` and bcftools both appear undefined here, and bcftools is not in the chapter's tool prose anywhere else even though it is in the front matter. | Gloss `FORMAT DP` as the per-sample depth field, and introduce bcftools once. |
| Whole chapter, "estimated_reading_min: 24" | I spent far longer than 24 minutes and did not finish with understanding. The estimate seems written for a reader who already knows the terms. | Note the estimate assumes command-line familiarity. |

The one thing I learned. LGE keeps a JSON file beside every result that records the exact command it ran, so I never have to trust my memory of which settings I used.

The one thing I still could not do. Look up the assembly caveat I was sent here for and act on it, because the entry tells me two dialog settings are ignored and then sends me to a different chapter to find out what to do about it.

The sentence I liked most. "Omit it and a 20,000-fold amplicon silently reports as 8000-fold, which makes every allele frequency computed against that pileup wrong in the same direction."
