# Reader 4 report: Power User Notes

Reader persona. An undergraduate who used Geneious for one semester. Comfortable
with a graphical sequence viewer, expects every operation to have a dialog, has
never opened a terminal. I was sent here from a workflow chapter to find one
caveat, so I skimmed for my tool and then read the whole page.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "This appendix is where the argument lists live" | I do not know what an argument list is. In Geneious I set values in a dialog and never see a command. The page never says that an argument list is the text form of the settings I already chose in a window. | Gloss "argument list" at first use as the written-out form of the settings a dialog sets. |
| What it is, "Argument lists are shown in `bash` blocks" | I have never opened a terminal, so "bash block" told me nothing and I could not tell whether the grey boxes were something I was meant to type. | Say once that the grey boxes are for reading, not for typing, and that nothing on this page has to be run. |
| What it is, "the real ones are absolute paths into a scratch folder" | "Absolute path" and "scratch folder" are both unexplained, and I could not tell whether the scratch folder is something on my Mac I should look for. | Gloss absolute path once and say the scratch folder is temporary and not something the reader manages. |
| Reading a command out of the source, "A line such as `ViralVariantCallingPipeline.swift:1229`" | The whole section tells me to read LGE's source code. I have never seen Swift and would not know where `Sources/LungfishWorkflow/` is on my machine. The chapter does not say whether a reader can skip this. | Add one sentence saying the source citations are optional and the tables below stand alone. |
| Reading a command out of the source, "Argument builders in LGE are small pure functions" | "Pure function" is a programming term I do not know, and the sentence had to be read twice to see it was reassurance rather than instruction. | Replace the term with a plain statement that the code only assembles text and does not run anything. |
| iVar variant calling, "First `samtools mpileup` walks the alignment" | This section, and every later one, names only command text. My window has a Call Variants dialog. Nothing here tells me which dialog control corresponds to which flag except in scattered later sentences. | Add one line per tool section naming the dialog that produces the command. |
| samtools mpileup table, "`-d 600000` Raise the per-position read cap" | I cannot judge 600000. The next paragraph says 8000 is the tool default and a 20,000-fold amplicon overruns it, but I do not know what "fold" means here or what my own depth is likely to be. | Gloss fold coverage once and say what a typical amplicon run reaches. |
| samtools mpileup table, "`-Q 20` ... a one-in-a-hundred chance of being wrong" | Good gloss, but I could not tell whether 20 is strict or lenient, or whether I would ever want to change it. The row says nothing about changing it and the prose says it is fixed in source only later. | State in the row that this value cannot be changed from the dialog. |
| ivar variants, "Your own text from `--extra-args` is inserted" | `--extra-args` appears here as if already known. It is not explained until the very last section of the chapter, and it appears to be terminal-only with no window route stated. | Gloss `--extra-args` at this first use and say whether any dialog offers it. |
| ivar variants, "On the command line those two are `--min-af` and `--min-depth`" | This is the first time I learned Minimum Allele Frequency has a command-line twin. It made me unsure whether the settings I set in the dialog are the same settings. | Say plainly that the dialog field and the flag are the same setting under two names. |
| The four iVar tuning flags, "`--ivar-consensus-af` (default 0.75)" | Four flags are given with defaults but no dialog equivalents and no statement that they are unreachable from a window. As a dialog-only user I could not tell whether I have ever set them. | Say whether these four have any dialog control, and if not, say so directly. |
| The four iVar tuning flags, "To reproduce an LGE iVar call you need LGE" | I had to read the whole paragraph twice to see that this is good news for me and bad news for someone rebuilding the pipeline by hand. | Lead the paragraph with who is affected. |
| Codon merging, "Both frequencies inside a fixed band from 0.40 to 0.60" | I cannot judge why 0.40 to 0.60 is the band, and "hard-coded with no setting behind it" reads as a complaint I do not have the background to evaluate. | Say in one sentence what a frequency near 0.5 means biologically. |
| LoFreq variant calling, "asks whether the alternate reads outnumber what that error rate alone would produce" | Long sentence, read twice. "Alternate reads" is not glossed anywhere on this page. | Gloss alternate read at first use. |
| The ordinary case, "Your `--extra-args` text lands between `call` and `-f`" | I could not perform this. There is no statement of where a person using windows types extra arguments, or that they cannot. | State the window route for extra arguments, or say there is none. |
| The ordinary case, "the HG002 chromosome 20 slice" | HG002 is introduced with no explanation. I guessed it is a standard human sample but the page never says. | Gloss HG002 once as the standard human reference sample. |
| Indel calling, "unless you pass `--call-indels` through `--extra-args`" | This is exactly the kind of caveat I was sent here for, and it is only reachable by typing in a terminal. The chapter does not acknowledge that a window user simply cannot turn indel calling on. | Say directly that indel calling has no dialog control in this release. |
| Indel calling, "using LoFreq's port of the Dindel algorithm" | Dindel is named and never explained, and I could not tell whether the name matters to me. | Drop the algorithm name or gloss it in three words. |
| LoFreq version string, "records a `variantCallerVersion` reading `FATAL(...)`" | I could not tell where I would ever see this field. The sidecar has not yet been described at this point in the chapter. | Move this note after the sidecar section, or say where the field is visible. |
| Primer trimming, "`-s` is `--ivar-sliding-window` at 4, the window width in bases" | I do not know what a sliding-window trim does, so I cannot judge whether 4 is small or large. | Explain the sliding-window trim in one sentence before the four values. |
| Primer trimming, "which shifts the primer coordinates when a scheme's BED disagrees" | BED is not glossed on this page and is not in the glossary list at the top. | Gloss BED at first use as the plain-text file listing primer positions. |
| Mapping, "minimap2 -a -x sr -t 8 -R '@RG\tID:...' --secondary=no" | Four commands are shown with no per-flag table, unlike the mpileup section which had one. I could not read these at all. | Add a short table for the flags that carry a setting, matching the mpileup section. |
| Mapping, "a BBMap argument of yours is the one that loses" | Read twice. The wording inverts between the two sentences and I lost track of which tool honours my value. | Restate both cases in the same direction, for example always saying which value wins. |
| Kraken 2, "an LGE kreport is eight columns wide and a downstream parser expecting six will misread it" | "Downstream parser" assumes I run my own scripts. I do not. The sentence does not say whether this affects anything I see in the window. | Add a clause saying the window reads the extra columns correctly. |
| Assembly, "SPAdes takes `--isolate`, `--meta`, or `--plasmid`" | Three profiles are named with no explanation of when each applies, and I cannot judge the default. | Say in one clause what each profile is for. |
| Assembly, "Two settings the dialog offers are silently ignored on some tools" | This is the most useful sentence on the page for me and it is buried at the end of a section. It is also the only place a dialog is mentioned in the assembly section. | Promote this to the top of the Assembly section. |
| GATK, "LGE never creates the `.dict` sequence dictionary GATK requires, so a first run fails until you make it yourself" | I cannot make a `.dict` file. The text says to do it and gives no window route and no command, only a link to another chapter. | Say here whether the fix is possible from a window, or state plainly that it is not. |
| The provenance sidecar, "LGE names one `<filename>.lungfish-provenance.json`" | I do not know how to find a file whose name begins with a dot on a Mac, and the chapter never says. | Add one sentence on reaching these files from the window, for example a Reveal command. |
| The full envelope, JSON block | Thirty lines of JSON with no reading guide. I have never seen JSON and could not tell which lines matter. | Say before the block which three or four keys a reader should look at. |
| The full envelope, "spelled in camelCase like every other key" | camelCase is not explained and the warning about `schema_version` assumes I would be writing a program. | Mark this paragraph as being for script writers. |
| The workflow-run shape, "A setting a caller ignores is written as the string `caller-default`" | This is a real caveat that affects me, but it is stated inside a section about a file format I cannot open. I would have missed it. | Repeat this caveat in the consolidated caveat list. |
| Summarising cost, "`lungfish-cli ops stats <project-or-bundle>`" | Terminal only. There is no statement that no window equivalent exists, so I spent time wondering which menu it was under. | Say the Operations panel is the window equivalent, or that there is none. |
| Summarising cost, "a project full of them reports a sidecar count of zero" | Read twice. The naming difference between `.lungfish-provenance.json` and `<filename>.lungfish-provenance.json` is one leading character and I missed it the first time. | Set the two names on separate lines so the difference is visible. |
| Summarising cost, "produces an error line and exit status 0" | I do not know what an exit status is even after the glossary link, and I do not know why zero is wrong here. | Say in one clause that zero normally means success, which is why this is a trap. |
| What the campaign measured, "produced 104 allele rows for the sample `WD1_S148_L001`" | I cannot judge 104. The number is offered as evidence but I have no idea whether 104 is expected. | Say that the point is the two runs matching, not the value. |
| What the campaign measured, "MEGAHIT 1.2.9 is not reliable at all on Apple Silicon" | This affects me directly and it is buried in a reproducibility section I nearly skipped. It also does not say which assembler I should choose instead. | Name the assembler to use instead in the same paragraph. |
| What the campaign measured, "hifiasm does so consistently on the mitochondrial fixture" | "Doubles a small circular genome" needed a second read. I did not know an assembler could report the same sequence twice. | Explain the doubling in one plain sentence. |
| What the campaign measured, "the assembly viewport shows no circularity column, so a doubled contig looks healthy until you check its length" | This is the one caveat I could act on from a window, and it does not say what length I should expect or compare against. | Say to compare the contig length against the known genome length. |
| What the campaign measured, "the global `--threads` is read before any subcommand sees it" | I do not know what a subcommand is, and the sentence is the hardest on the page. | Mark this sentence as terminal-only. |
| The consolidated caveat list, "Every reproducibility defect the 2026-09 campaign found is listed here" | This is the list I was sent to read, and roughly half the entries are about command-line flags I cannot reach. Nothing marks which entries affect a window user. | Mark each entry as window, command line, or both. |
| Consolidated list, "Medaka and Clair3 ... every Medaka run fails in Preview 2026.9.13" | If a whole tool always fails, I would want that in the chapter that offers it, not only here. As written I would only find it by chance. | Add a pointer at the top of the caveat list saying which tools are wholly broken. |
| Consolidated list, "Paths. A command whose working directory sits under `/private/tmp`" | Working directory, `/private/tmp`, and symlink are all unglossed, and I do not know whether my projects live there. | Say whether a normal project folder is ever affected. |
| Pinning an environment, "A plugin pack is a versioned recipe for a per-tool conda environment" | Conda, environment, and transitive dependency all arrive in two sentences. I could not follow why any of this matters to my results. | Open the section with one sentence on why a reader should care. |
| Pinning an environment, "Its companion ... exists only to refuse the operation" | Read three times. A command that exists in order to fail is a surprising idea and the sentence does not prepare me for it. | Lead with the fact that the feature is unfinished. |
| Pinning an environment, "the exporter genuinely builds one with an `oci-layout` file, an `index.json`, config and manifest blobs" | A long list of unexplained artifacts for a feature the same paragraph says I cannot use. | Cut the artifact list or move it after the statement that the feature is unreachable. |
| The conda mutation lock, "A power user driving concurrent installs meets it in one of three ways" | I do not know what a concurrent install is or how I would cause one. The section never says whether ordinary window use can hit this. | State that a single user in one window never meets this. |
| Asserting an installation, "`--plan` exits 10 when work is pending" | Exit codes 10, 2, 1, and 0 are given in one paragraph with no table, and I could not hold them. | Put the four exit codes in a small table. |
| Debugging from the Operations panel, "The Operations panel opens with Operations > Show Operations Panel (Cmd-Shift-P)" | The first window route on the whole page arrives on line 341 of 373. I would have given up long before. | Move the Operations panel section much earlier, since it is the only part a window user can act on. |
| Debugging from the Operations panel, "reproduce with Copy CLI Command in a fresh shell" | "Fresh shell" is unexplained and is the last step of the recommended debugging order, so the order ends somewhere I cannot go. | Give a window-only debugging order alongside this one. |
| Failure reports outlive the session, "at `~/Library/Logs/<app name>/Operations/Failures`" | I do not know how to reach a path starting with a tilde, and the Library folder is hidden on my Mac. | Say to use the Reveal Failure Report command from the panel instead of the path. |
| Reaching a flag LGE does not wrap, "Three escape hatches exist" | All three require a terminal. The section does not say that a window user has none of them. | Open with a sentence saying these are command-line routes only. |
| Reaching a flag, "both `--extra-args` and the repeatable `--extra-arg` exist on some commands and differ by one letter" | Read twice, and I still do not know which commands have which. | Say where the reader can check which spelling a command takes. |
| Querying variants without the window, section title | The title promises the window is optional, but the section is the only place the window is contrasted with a script, and the caveat inside it applies to the window too. | Restate the FORMAT DP caveat in window terms. |
| Querying variants without the window, "reads a `FORMAT DP` field that bcftools does not write, so such a clause matches nothing" | This is a silent wrong answer, and it is the most alarming thing on the page. It is one sentence in the second-to-last section. | Move this to the consolidated caveat list as well. |
| Whole chapter, "task: Look up the exact arguments ..." | Nothing at the top warns that this appendix is almost entirely command line. I was sent here for one caveat and read a great deal that I cannot use. | Add one opening sentence saying which parts a window-only reader should read. |

One thing I learned. LGE adds flags of its own that no dialog shows, so my
results are not the same as running the tool by hand with the same settings.

One thing I still could not do. Turn on indel calling for LoFreq, because the
only route given is typing `--call-indels` in a terminal and the chapter never
says whether a window route exists.

The sentence I liked most. "The assembly viewport shows no circularity column,
so a doubled contig looks healthy until you check its length."
