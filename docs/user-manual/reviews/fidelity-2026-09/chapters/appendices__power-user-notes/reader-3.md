# Reader 3 report, appendices/power-user-notes

Reader persona. Pre-med student. English is my second language. I know biology
words from my textbooks. I am slow with idioms and long sentences. I have never
opened a terminal. I came to this page because another chapter sent me here to
find one caveat.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "The workflow chapters of this manual leave things out on purpose." | I was sent here from another chapter to find one caveat, and there is no map at the top telling me which section holds which caveat. I read the whole page to find my one item. | Add one sentence naming the consolidated caveat list section as the place to start for a reader following a link. |
| What it is, "the argument list underneath it" | "Argument list" is never explained. I do not know what an argument is in this context. I guessed it means the words typed after the tool name, but I was not sure until three sections later. | Gloss "argument" at first use as the text typed after a command that tells the tool what to do. |
| What it is, "the honest limits on repeating a run and getting the same answer" | Long sentence, three ideas joined. I read it twice to see that "the honest limits" attaches to a third thing in the same list as "argument lists" and "the shape of the record". | Split into two sentences. |
| What it is, "Nothing here is secret." | This is a figure of speech, not information. I stopped to ask what would have been secret, and whether some other page is secret. | Cut, or replace with a plain statement of where the text comes from. |
| What it is, "Reading the flags here is faster than opening a sidecar and working backwards" | "Flags" appears for the first time here with no gloss, and "working backwards" is an idiom. I did not know a flag is the same thing as an argument that starts with a dash until the mpileup table. | Gloss "flag" at first use and replace "working backwards" with a literal phrase. |
| What it is, "the release defaults from dependency set 2026.2" | I cannot judge this number. I do not know whether my copy of the app uses 2026.2 or something else, or how I would check. | Say in one sentence how a reader finds which dependency set their own installation uses. |
| What it is, "before you promise anyone a byte-identical rerun" | "Byte-identical" is not glossed here. I understood "identical", but I do not know if a byte-identical result is stricter than a biologically identical result, which turned out to matter a lot later. | Gloss "byte-identical" at first use as the output file being the same file down to every character. |
| Reading a command out of the source, "A line such as `ViralVariantCallingPipeline.swift:1229` names a file under `Sources/LungfishWorkflow/`" | I have never opened a terminal and I do not have the source code. I could not perform this. The section tells me how to read a citation but never says whether I can get the source at all, or whether I should skip this section. | Say plainly that the citations are for readers who have the source checkout, and that everyone else can skip to the next section. |
| Reading a command out of the source, "Argument builders in LGE are small pure functions" | "Pure function" is a programming term I do not know. In my classes "function" means something else. | Gloss or cut "pure". |
| Reading a command out of the source, "so a flag absent from a run is not always a flag LGE never passes" | Double negative inside a comparison. I read this three times and I am still not fully sure of the meaning. | Rewrite as one positive statement. |
| iVar variant calling, "An iVar variant call in LGE is three processes rather than one." | The word "process" is used here for a step of work, but later "A second process that finds the lock held" in the conda section means a running program. I mixed these up. | Use "step" here, and keep "process" for the running program sense. |
| samtools mpileup, "The mpileup arguments are fixed." | I do not know what "the dialog" is, mentioned in the next sentence, because this appendix never says the dialog is the window with settings in the app. | Name the dialog once as the settings window LGE shows before a run. |
| samtools mpileup table, "-d 600000 ... Raise the per-position read cap from the tool default of 8000" | I cannot judge 600000. I know 8000 is the old cap, but nothing tells me whether 600000 is generous, tight, or arbitrary, or what depth my own data would reach. | Add what depth a typical run reaches so the reader can see the headroom. |
| samtools mpileup table, "-Q 20 | Ignore bases whose Phred score is under 20, meaning a one-in-a-hundred chance of being wrong" | Good gloss, but "quality floor" in the next cell is a new metaphor and never glossed. A floor is a surface in my head, not a number. | Say "minimum" instead of "floor" throughout. |
| samtools mpileup, "a 20,000-fold amplicon silently reports as 8000-fold" | I do not know what "fold" means as a unit here. I know "fold" from fold change in gene expression, which is a ratio and different. | Gloss coverage depth and "fold" at first use. |
| ivar variants, "Your own text from `--extra-args` is inserted immediately after the `variants` subcommand" | `--extra-args` appears here with no explanation. It is only explained near the very bottom of the page, in Reaching a flag LGE does not wrap. I could not follow this paragraph on first reading. | Gloss `--extra-args` at this first use, or move the escape-hatch section earlier. |
| ivar variants, "so a `-t` of your own is overridden by LGE's" | I could not judge whether this is good or bad, and the sentence assumes I know what happens when two settings collide. | Add a half-sentence saying the practical result, that your value has no effect. |
| ivar variants, "the flag is dropped and the run continues with no message" | This is the kind of caveat I was sent here for, and it is buried in the middle of a paragraph with no warning marker. I nearly missed it. | Mark silent-failure caveats consistently, for example a bold lead-in word. |
| The four iVar tuning flags do not reach iVar, "This trips people up, so it is stated plainly." | "Trips people up" is an idiom. I understood it only after a moment. | Replace with a literal phrase such as "This is often misunderstood". |
| The four iVar tuning flags do not reach iVar, "`lungfish-internal ivar-tsv-to-vcf --consensus-af ..." | "TSV" is used here and "tab-separated table" earlier, but the page never says they are the same thing. | Say TSV once beside "tab-separated". |
| The four iVar tuning flags, "To reproduce an LGE iVar call you need LGE." | Short and clear, but I could not tell whether this is a limitation I should report to my supervisor or a normal state of affairs. | Say whether this is expected behaviour or a defect. |
| Codon merging, "Both frequencies inside a fixed band from 0.40 to 0.60 merges" | I cannot judge 0.40 to 0.60. Nothing says what a frequency near 0.5 means biologically, so I do not know why this band was chosen. | Say in one sentence why frequencies near one half are the interesting case. |
| Codon merging, "None of this is phase-aware." | "Phase-aware" is used before "phased haplotypes" is glossed, and neither is really explained. I know haplotype from lecture but not "phase" as a verb. | Gloss phase at first use as knowing which changes sit on the same copy of the chromosome. |
| Codon merging, "a merged row asserts a codon boundary and nothing about linkage" | Two terms in one clause, "asserts a codon boundary" and "linkage", and I had to read it twice. Linkage in my genetics course means something related but not identical. | Split into two sentences and gloss linkage in this narrow sense. |
| LoFreq variant calling, "asks whether the alternate reads outnumber what that error rate alone would produce" | Long sentence and the comparison is abstract. I read it twice before I saw that "what that error rate alone would produce" is a count of reads. | Rewrite as a comparison of two counts. |
| The ordinary case is a single call, "so a script that expects those is describing a different wrapper" | "Wrapper" is used repeatedly across the page and never glossed. I could not tell if a wrapper is a program, a file, or a way of speaking. | Gloss "wrapper" at first use. |
| The ordinary case, "The LoFreq sidecar from the HG002 chromosome 20 slice" | HG002 is not explained. I guessed it is a reference human sample, and "slice" is a metaphor I had to guess as well. | Say HG002 is a widely used human reference sample and replace "slice" with "region". |
| Indel calling, "using LoFreq's port of the Dindel algorithm" | "Port" as a noun for software is unknown to me, and Dindel is named without explanation. | Say "LoFreq's version of the Dindel algorithm" and drop or gloss Dindel. |
| Indel calling, "reports far fewer indels than the data holds and says nothing about it" | I cannot judge "far fewer". Half? A tenth? This decides whether the caveat matters for my project. | Give an order of magnitude or say that the size of the gap was not measured. |
| LoFreq's version string is an error message, "Every LoFreq sidecar therefore records a `variantCallerVersion` reading `FATAL(...)`" | I saw the word FATAL and thought my run had crashed. The reassurance "The run is fine" comes only after. | Put "The run is fine" before the error text, not after. |
| Primer trimming, "`-q` is `--ivar-min-quality` at 20, the Phred score floor for the sliding-window trim" | "Sliding window" is not glossed. I do not know what slides or over what. | Gloss sliding window at first use. |
| Primer trimming, "which shifts the primer coordinates when a scheme's BED disagrees with where the primers really sat" | BED appears with no gloss anywhere in this chapter. | Gloss BED as the plain text file listing primer positions. |
| Primer trimming, "Without it a shotgun-derived read passing through an amplicon bundle would vanish." | "Shotgun-derived" is a term I half know and "vanish" is dramatic rather than precise. I could not tell whether the read is deleted from my file or just not counted. | Gloss shotgun sequencing and say precisely what happens to the read. |
| Primer trimming, "A mismatched scheme otherwise trims with exit status 0 and no warning" | "Exit status 0" is glossary-linked but not explained in the text, and I do not know that 0 conventionally means success. To me zero reads as nothing or failure. | State once in the text that exit status 0 means the tool reported success. |
| Mapping, "The four mappers get four different argument shapes" | The code block lists four commands but the paragraph before never says what a mapper does or why there are four choices. This appendix assumes I read the mapping chapter. | Add one sentence saying a mapper places reads onto the reference, and point to the mapping chapter. |
| Mapping, "`-R '@RG\tID:...'`" | The `\t` and the `...` inside a real command confused me. I did not know whether I should type those characters. | Say once that the ellipsis stands for values LGE fills in. |
| Mapping, "which means a BBMap argument of yours is the one that loses" | "Loses" is a metaphor and the sentence is inverted. I read it twice and then had to compare it against the minimap2 sentence to be sure which behaviour is which. | Say plainly that on BBMap, LGE's value is used and yours is ignored. |
| Mapping, "That asymmetry is easy to miss." | "Asymmetry" is abstract here, and after two dense sentences I could not tell which two things were asymmetric. | Name the two things. |
| Kraken 2 classification, "`--memory-mapping` runs the database from disk instead of loading it into RAM" | Four conditional flags are listed in one sentence, and I could not tell how a reader makes any of them happen. Are they settings in a window, or something I type? | Say where each conditional flag comes from, a setting or an input property. |
| Kraken 2, "so that Bracken, the abundance re-estimator that runs afterwards, has what it needs" | "Abundance re-estimator" is dense and Bracken is not otherwise introduced. | Gloss Bracken in plain words. |
| Kraken 2, "an LGE kreport is eight columns wide and a downstream parser expecting six will misread it" | "Downstream parser" assumes I write code that reads files. I do not. I could not tell if this caveat concerns me. | Say who this affects, for example a reader loading the report into another program. |
| Assembly, "the differences between them are larger than the dialog suggests" | I could not perform anything from this. It tells me a difference exists without telling me how to choose. | Point to the assembly chapters for choosing, in this sentence. |
| Assembly, "so a `nano-hq` profile is passed as `--flye --nano-hq`" | I do not know what nano-hq means, and the chapter uses it three times as if it were common knowledge. | Gloss nano-hq as high quality Nanopore reads. |
| Assembly, "MEGAHIT is given `--no-hw-accel` on hosts where LGE caps its threads" | Three unglossed ideas in one clause, hardware acceleration, host, and thread capping. I could not follow it. | Split and gloss, or drop the mechanism and keep the consequence. |
| Assembly, "because leaving it automatic can zero out a small assembly" | "Zero out" is an idiom. I guessed it means no contigs are produced. | Replace with a literal phrase. |
| Assembly, "Two settings the dialog offers are silently ignored on some tools." | This is exactly the kind of caveat I was sent to find, and it is the fourth paragraph of a section I nearly skipped because the heading is just "Assembly". | Give silent-ignore caveats their own subheading. |
| GATK, "`-ERC` selects the reference-confidence mode, which controls whether the output is an ordinary VCF or a GVCF" | GVCF is never glossed and is not in the glossary list at the top. The difference between VCF and GVCF is the whole point of the sentence. | Gloss GVCF at first use. |
| GATK, "LGE never creates the `.dict` sequence dictionary GATK requires, so a first run fails until you make it yourself" | I cannot make it myself. I have never opened a terminal, and this sentence gives me no way to act. | Point directly to the step in the HaplotypeCaller chapter that makes the file. |
| GATK, "The Call Variants dialog's two Thresholds fields are discarded on both GATK entries rather than recorded" | "Both GATK entries" assumes I know there are two GATK options in a menu I have not seen. | Name the two entries. |
| The provenance sidecar, "gathers a bundle's sidecars under a `provenance/` folder" | "Bundle" is used constantly across the whole chapter and never explained here, though it is clearly central. | Gloss bundle at first use in this chapter. |
| The full envelope, JSON block | This is a large block of code with no line-by-line explanation, and the four call-outs after it cover only four keys. I could not tell which of the other twenty keys matter to me. | Say which keys a reader actually needs and which are internal. |
| The full envelope, "`schemaVersion` is `1` and is spelled in camelCase" | "camelCase" is a programming term I do not know. | Gloss or cut. |
| The full envelope, "because the writer emits an older and a newer spelling of each" | "The writer" is an unexplained actor. I did not know if it is a person, a program, or a part of LGE. | Name it as the part of LGE that writes the file. |
| The workflow-run shape, "Some operations write a second, older shape" | I could not tell which operations, other than the later mention of variant calling. So I could not predict what my own file would look like. | List the operations that use the older shape. |
| The workflow-run shape, "A setting a caller ignores is written as the string `caller-default`" | This is an important caveat but the sentence packs the rule and its example together and I read it twice. | Split into rule and example. |
| The steps array, "an optional captured `stderr`" | "stderr" is never expanded. I have never opened a terminal so this word means nothing to me. | Expand as standard error, the text a tool prints about problems. |
| The steps array, "which names the version, the environment, the executable, and the exact conda package build in one string" | Four unglossed nouns in a row. Executable and package build are both unfamiliar. | Gloss executable, or reduce the list. |
| Summarising cost across a project, "`lungfish-cli ops stats <project-or-bundle>`" | Everything in this section requires typing a command in a terminal, which I cannot do, and no equivalent in the app window is mentioned. | Say whether an app-window equivalent exists, or mark the section as terminal-only. |
| Summarising cost across a project, "a project full of them reports a sidecar count of zero" | I read this twice. The two very similar filenames, one with a leading dot and one with a filename before it, are hard to tell apart in running prose. | Set the two filenames on separate lines. |
| Summarising cost across a project, "an option it does not recognise produces an error line and exit status 0" | I now know 0 means success, but the sentence relies on my seeing that success plus an error line is a contradiction. It never says so. | Say explicitly that this makes a failure look like a success. |
| Reproducibility, honestly, "Two of these are LGE's own behaviour and the rest are the wrapped tools behaving as they always do." | "These" points forward to a list that has not started yet. I looked back for the list and found nothing. | Move this sentence after the list, or name the count. |
| What the campaign measured, "both produced 104 allele rows for the sample `WD1_S148_L001`" | I cannot judge 104. I do not know if that number is expected, high, or low, and the sample name means nothing to me. | Say that the number matters only because it matched, not for its own value. |
| What the campaign measured, "The pivot workbook export is not byte-reproducible." | "Pivot workbook" is not explained anywhere in this chapter. I know a workbook is a spreadsheet but not what pivot means here. | Gloss pivot workbook at first use. |
| What the campaign measured, "MEGAHIT 1.2.9 is not reliable at all on Apple Silicon in this release. It fails most runs" | "Most runs" cannot be judged. Sixty percent and ninety-nine percent would lead me to different decisions. | Give the observed rate or say it was not counted. |
| What the campaign measured, "A run that completes is correct." | Very short after a long paragraph, and I read it twice because I could not believe the reassurance came so fast after "not reliable at all". | Join it to the preceding sentence. |
| What the campaign measured, "Flye 2.9.6 occasionally doubles a small circular genome" | "Doubles" is unexplained. I could not picture what a doubled contig is or why a circular genome makes it happen. | Explain in one sentence what doubling means for a circular genome. |
| What the campaign measured, "so on `variants phase` it is consumed by the root command and the run records one thread whatever you asked for" | "Consumed by the root command" is entirely opaque to me. I do not know what a root command is. | Say plainly that the thread count you set is not applied. |
| The consolidated caveat list, "Every reproducibility defect the 2026-09 campaign found is listed here in one sentence apiece" | This is the section I actually needed when another chapter sent me here, and it sits two thirds of the way down the page with no signpost at the top. | Link to this section from the opening. |
| The consolidated caveat list, iVar entry, "recorded as `caller-default` for every other caller with no warning" | Each bold entry packs two or three separate defects into one sentence joined by "and". I had to read every entry twice to separate them. | One defect per sentence. |
| The consolidated caveat list, "Clair3 aborts on the ambient Python" | "Ambient Python" is a phrase I have never seen and cannot guess. | Replace with a plain description, for example the Python already installed on the machine. |
| The consolidated caveat list, "`conda install --pack gatk-core` reports an unknown pack" | I do not know what a pack is here, and "plugin pack" is only glossed much later under Pinning an environment. | Gloss pack before this list, or link forward. |
| The consolidated caveat list, "the Snakemake emitter produces a cycle by listing one BAM as both input and output" | "Emitter" and "cycle" are both unglossed, and Nextflow and Snakemake are named as if I know them. | Gloss what these two exports are for. |
| Cross-architecture and cross-version drift, "Some tools ship processor-specific fast paths" | "Fast paths" is jargon. I read the sentence twice and got the meaning only from the second half. | Replace with plain wording. |
| Cross-architecture and cross-version drift, "A minor release of a wrapped tool can change soft-clip boundaries or realignment behaviour without announcing it." | "Soft-clip boundaries" is unglossed and is the concrete example carrying the whole point. | Gloss soft clipping. |
| Cross-architecture drift, "this release pins samtools 1.24, bcftools 1.24, htslib 1.24, minimap2 2.31, iVar 1.4.4 ..." | Twelve version numbers in one sentence. I could not read it as prose and I could not use it as a table. | Make it a list or a table, or point only to Tool versions. |
| Pinning an environment, "A plugin pack is a versioned recipe for a per-tool conda environment." | Three unfamiliar ideas in one short sentence, versioned, recipe as metaphor, and per-tool environment. Conda is glossary-linked but never explained in the text. | Gloss conda and environment in plain words before this sentence. |
| Pinning an environment, "Three commands sit near this problem and only one of them solves it." | "Sit near this problem" is a figure of speech. I did not know what "near" means for a command. | Replace with a literal phrase. |
| Pinning an environment, "It is a record of what was asked for rather than a resolved artifact inventory" | "Resolved artifact inventory" is three abstract words together and I could not decode it. | Rewrite in plain words. |
| Pinning an environment, "Its companion `lungfish-cli conda install --from-lockfile <file>` exists only to refuse the operation" | A command that exists only to refuse sounded like a joke and I read it twice to check it was serious. | State it plainly, that the command always fails by design. |
| Pinning an environment, "The subcommand's `--format` collides with the global `--format`" | I do not know what a global option is, and "collides" is a metaphor. | Gloss global option and say the result rather than the mechanism. |
| The conda mutation lock, "takes an exclusive advisory lock on a file" | Three terms at once, exclusive, advisory, and lock, and the glossary link does not help me while reading. | Gloss the idea in one plain sentence before naming it. |
| The conda mutation lock, "writes `waiting for conda lock held by pid <n>` to standard error" | "pid" is never expanded and standard error is never explained. | Expand pid and standard error at first use. |
| The conda mutation lock, "never let two CI jobs restore and update the same cached conda root" | CI is used here and in a section title without ever being expanded. | Expand CI at first use. |
| Asserting an installation, "`--plan` exits 10 when work is pending and 0 when nothing is needed" | Four exit numbers appear in one paragraph, 10, 0, 2, and 1, and there is no table. I lost track of which meant what. | Put the exit statuses in a small table. |
| Asserting an installation, "so a CI step running it fails the job whenever the installation has drifted from the pinned set" | "Drifted" and "pinned set" are both metaphors, and the sentence assumes I run automated jobs. | Say who this is for and replace the metaphors. |
| Asserting an installation, "A failure to record the dependency receipt warns on standard error and still exits 0" | "Dependency receipt" appears once, here, with no explanation. | Gloss or cut. |
| Debugging from the Operations panel, "The Operations panel opens with **Operations > Show Operations Panel** (Cmd-Shift-P)" | This is the only part of the chapter I could actually perform, and it appears very late. As a reader who cannot use a terminal I would have started here. | Move the panel section earlier, or signpost it at the top as the no-terminal route. |
| Debugging from the Operations panel, "then reproduce with Copy CLI Command in a fresh shell" | "Fresh shell" is a terminal concept. The one actionable debugging path ends with a step I cannot take. | Say what a reader without a terminal should do instead. |
| Failure reports outlive the session, "at `~/Library/Logs/<app name>/Operations/Failures`" | The tilde is not explained, and I do not know how to open that folder from Finder. | Say once that the tilde means your home folder, and how to reach it. |
| Failure reports outlive the session, "Fifty reports are kept and older ones are pruned on each write" | I can judge fifty, but I could not tell whether fifty is per day or in total, so I do not know how long my report survives. | Say fifty in total, regardless of age. |
| Failure reports outlive the session, "which means an absent report is not itself evidence of anything" | Double negative plus abstraction. I read it twice. | Rewrite positively. |
| Reaching a flag LGE does not wrap, "Three escape hatches exist, in increasing order of how much provenance they cost you." | "Escape hatch" is an idiom, and "cost you provenance" is a metaphor treating a record as money. Both slowed me. | Replace both with literal wording. |
| Reaching a flag LGE does not wrap, "both `--extra-args` and the repeatable `--extra-arg` exist on some commands and differ by one letter" | I read this three times. Two option names differing by a final s, described in running prose, is very hard for me to hold. | Set the two names on separate lines with what each does. |
| Reaching a flag LGE does not wrap, "such as `ivar variants -c`" | The example gives a flag with no explanation of what `-c` does, so it teaches me nothing. | Say what the example flag does. |
| Reaching a flag LGE does not wrap, "a later `lungfish-cli bam adopt-mapping` cannot confirm the alignment came from the pipeline it expected" | Longest sentence in the section, with a subordinate clause about a command I have not met. I read it twice. | Split. |
| Querying variants without the window, "A per-sample filter clause reads a `FORMAT DP` field that bcftools does not write" | `FORMAT DP` is unglossed and is the whole content of the caveat. I could not tell whether this affects my data. | Gloss FORMAT DP as the per-sample depth field. |
| Whole chapter | The chapter is 24 minutes by its own estimate and assumes a terminal throughout, but nothing at the top warns a reader without a terminal which parts they can use. I read a lot that I cannot act on. | Add a one-sentence note at the top saying which sections need a terminal. |

The one thing I learned. The four settings named after iVar do not actually
reach iVar, and that is why running the tool by hand gives a different answer.

The one thing I still could not do. I could not perform any of the command
sections, because every one of them starts from a terminal and no section tells
a reader without a terminal what to do instead.

The sentence I liked most. "Report every position, including those with no
reads", because it says exactly what the flag does in words I already knew.
