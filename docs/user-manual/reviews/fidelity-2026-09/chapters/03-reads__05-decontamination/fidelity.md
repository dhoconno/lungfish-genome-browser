# Fidelity review, 03-reads/05-decontamination.md

Reviewed 2026-09-07 against Preview 2026.9.13 ground truth, the Swift
source, `.build/debug/lungfish-cli`, `parameters.yaml`, the tool lock
manifest, and re-runs of all five subcommands on the chapter's fixtures.

Re-runs were done under
`/private/tmp/claude-501/-Users-dho-Documents-lungfish-genome-explorer/8e8f6de6-4b18-4243-8bb4-601f75d7c63a/scratchpad/fastq-decon/`
using the author's own outputs and provenance sidecars. Every quoted count
was recounted from the FASTQ files themselves rather than from the author's
notes, so the count verdicts below are independent of `NUMBERS.md`.

| Claim | Verdict | Evidence | Correction |
|---|---|---|---|
| Front matter `entry_points` "Tools > Decontamination > Remove Human Reads..." | true | `ToolsMenuModel.swift:66` category title "Decontamination"; `MainMenu.swift:805-812` builds each item as `"\(toolID.title)\u{2026}"`; title at `FASTQOperationDialogState.swift:1964`. | none |
| Front matter "Tools > Decontamination > Remove ribosomal RNA sequences..." | true | Exact title at `FASTQOperationDialogState.swift:1965`, same menu construction. | none |
| Front matter "Tools > Decontamination > Remove Contaminants..." | true | `FASTQOperationDialogState.swift:1966`. | none |
| Front matter "Tools > Decontamination > Low-Complexity Filter..." | true | `FASTQOperationDialogState.swift:1967`. | none |
| Front matter "Tools > Decontamination > Remove Duplicates..." | true | `FASTQOperationDialogState.swift:1968`. | none |
| Front matter "CLI: lungfish-cli fastq scrub-human, deacon-ribo, contaminant-filter, entropy-filter, deduplicate" | true | `cli-help/fastq.txt` banners at lines 1433, 1482, 214, 235, 379. | none |
| `parameters_refs` lists the five ids `fastq.remove-human-reads`, `fastq.remove-ribosomal-rna`, `fastq.remove-contaminants`, `fastq.low-complexity-filter`, `fastq.remove-duplicates` | true | Identical to roster row 18, `DRIFT.md:3744`. All five exist in `parameters.yaml` at lines 517, 551, 592, 642, 697. | none |
| Every setting in the five registry entries has a Settings paragraph with the exact label, default, and allowed values | true | Fourteen registry settings. Retain Reads, Contaminant Mode, K-mer (contaminants), Hamming Distance, Entropy Threshold, Window, K-mer (entropy), Preset, Substitutions, Optical Duplicates, Optical Distance, and Output Strategy each have a paragraph; the four duplicate Output Strategy rows are covered once by "The shared output control", which the chapter announces at the head of the section. Labels, defaults, and allowed values match `parameters.yaml:517-737`. | none |
| `shots` lists `human-scrub-dialog`, `low-complexity-pane`, `remove-duplicates-preset-picker`, each with a caption | true | Body markers at lines 91, 107, 111 are exactly these three ids, one caption each in the front matter. No marker lacks a caption and no caption lacks a marker. | none |
| `fixtures_refs: [hg002-chr20, sarscov2-srr36291587]` | true | Both directories exist under `docs/user-manual/fixtures/`. | none |
| All sixteen `glossary_refs` anchors resolve | true | Each of fastq, deacon, bbduk, clumpify, k-mer, minimizer, hamming-distance, ribosomal-rna, phix, pcr-duplicate, optical-duplicate, shannon-entropy, host-depletion, required-setup-pack, provenance, operations-panel appears exactly once as `{#anchor}` in `GLOSSARY.md`. The body also links `#read`, which resolves but is not declared in `glossary_refs`. | Optionally add `read` to `glossary_refs`, since the body deep-links it in the first paragraph. |
| "LGE offers five operations" | true | `FASTQOperationDialogState.swift:1271` returns exactly the five decontamination tool ids. | none |
| "Remove ribosomal RNA sequences has a Retain Reads control that decides which class of reads is kept" | true | `FASTQOperationToolPanes.swift:454-458`; segment display names non-rRNA, rRNA, Both at `FASTQDerivatives.swift:220-222`. | none |
| Operation table, tool column (Deacon, Deacon, bbduk, bbduk, clumpify) | true | `fastq.txt` OVERVIEW lines for each subcommand; `third-party-tools-lock.json:10` lists `clumpify.sh` and `bbduk.sh` under bbtools. | none |
| Operation table, "Bundled PhiX, or a FASTA you supply" | true | `FASTQDerivativeService+BBTools.swift:38,67-70` uses the PhiX reference bundled inside the managed BBTools resources; `--mode phix, custom` in `fastq.txt:222`. | none |
| Step 2, "The dialog opens with that operation already chosen, so there is no list to pick from inside it" | true | `MainMenu.swift:806-813` sets `item.representedObject = toolID` and dispatches `launchFASTQOperationToolFromMenu`, so the menu item selects the tool. | none |
| Step 3, Inputs section holds **FASTQ Datasets** and **Database** rows | true | `FASTQOperationDialogState.swift:2092-2093` gives `.removeHumanReads` the required kinds `[.fastqDataset, .database]`; titles "FASTQ Datasets" and "Database" at `FASTQOperationDialogState.swift:2225-2229`. | none |
| Step 3, the Database row has "Choose... and Clear buttons beside it" | false | `FASTQOperationToolPanes.swift:208-219`. The first button is labelled "Choose…" only while nothing is selected and becomes "Replace…" once a database is chosen, and the Clear button is drawn only when a selection exists (`if state.auxiliaryInputURL(for: kind) != nil`). Since the managed database is preselected in the described state, the reader sees Replace… and Clear, not Choose… and Clear. | "**Database** shows the managed human database, with Replace... and Clear buttons beside it. The first button reads Choose... while no database is selected." |
| Step 3, "The settings pane below the Inputs section is empty for this operation, because it has no tuning controls of its own" | true | `FASTQOperationToolPanes.swift:699-701` renders only the sentence "This tool uses the dedicated embedded workflow pane or the fixed database chooser above" for `.removeHumanReads`. | The pane is not literally blank, it carries that one explanatory line, which the chapter's own Settings section describes correctly ("the pane says so in place of any controls"). No change needed to the step. |
| "The result lands under `Analyses/<tool>-<timestamp>/` in your project" | false | `MainSplitViewController+GenomicsDisplay.swift:993-994` sets `destinationRoot` to `<project>/Analyses`, and line 1231 assigns `workingDirectory = destinationRoot` for every request that is not an assembly, a Grouped Result, or a demultiplex. The `<tool>-<timestamp>` shape is created only by `AnalysesFolder.createAnalysisDirectory` (line 1218), reached only for `.assemble`. `AnalysesFolder.knownTools` (`AnalysesFolder.swift:24-28`) contains no decontamination tool. Even the Grouped Result path names its folder from the operation title, not a timestamp (`uniqueFASTQOperationOutputDirectory`, lines 1500-1504). | "The result lands directly under `Analyses/` in your project, and the original bundle is untouched." (Flag to the campaign lead: CONSISTENCY.md line 47 states the `Analyses/<tool>-<timestamp>/` rule for every analysis result, which is wrong for FASTQ operations. Settle it there before the chapter is corrected.) |
| "the original bundle is untouched" | true | The input is passed as a read-only argument and outputs are written into `destinationRoot`; nothing in the derivative path writes back to the input bundle. | none |
| "Operations panel, which opens with **Operations > Show Operations Panel** (Cmd-Shift-P)" | true | Matches CONSISTENCY.md line 29. | none |
| "**Remove ribosomal RNA sequences** shows one segmented **Retain Reads** control with the segments non-rRNA, rRNA, and Both. It starts on non-rRNA" | true | `FASTQOperationToolPanes.swift:454-458` with `.pickerStyle(.segmented)`; display names at `FASTQDerivatives.swift:220-222`; default `.nonRRNA` at `FASTQOperationDialogState.swift:249`. | none |
| "**Remove Contaminants** shows a **Contaminant Mode** picker set to PhiX, a **K-mer** field at 31, and a **Hamming Distance** field at 1" | true | `FASTQOperationToolPanes.swift:400-408`; defaults `.phix`, 31, 1 at `FASTQOperationDialogState.swift:2128-2130`. | none |
| "Switching the picker to Custom Reference adds a **Contaminant Reference** row to the Inputs section" | true | `FASTQOperationDialogState.swift:1080-1083` adds `.contaminantReference` only when `removeContaminantsMode == .custom`; input title "Contaminant Reference" at `FASTQOperationDialogState.swift:2237`; pane hint at `FASTQOperationToolPanes.swift:410-415`. | none |
| "**Low-Complexity Filter** shows an **Entropy Threshold** slider at 0.60, with **Window** and **K-mer** fields tucked inside an Advanced disclosure. It needs no reference and no database" | true | `FASTQOperationToolPanes.swift:418-451`, `DisclosureGroup("Advanced")` at line 438; `FASTQOperationDialogState.swift:2083-2089` puts the tool in the `[.fastqDataset]`-only group. | none |
| "**Remove Duplicates** shows a **Preset** picker set to Exact PCR. Five other presets sit behind it, and the last of them, Custom, reveals the individual controls" | true | `FASTQOperationToolPanes.swift:462-476`; six display names at `FASTQOperationToolPanes.swift:1086-1101`; default `.exactPCR` at `FASTQOperationDialogState.swift:2136`. | none |
| "Four of the five operations offer an Output Strategy picker. Remove ribosomal RNA sequences is the one that does not" | true | `FASTQOperationDialogState.swift:2135-2141`, `supportsConfigurableOutput` returns `categoryID != .classification && self != .removeRibosomalRNA`. | none |
| "because it writes one file per retained read class rather than one file per input" | unverifiable | The exclusion is a bare condition in `supportsConfigurableOutput` with no comment giving a reason. The `deacon-ribo` output directory does hold one file per input mate (`riboout/` held `hg002_R1.norrna.fastq` and `hg002_R2.norrna.fastq` for a `--retain norrna` run), which is one file per input, not per class. Settled by asking the author of that condition, or by running the GUI operation with Retain Reads on Both and observing the output count. | Either drop the causal clause or replace it with the observed behaviour, that this operation writes into an output directory rather than to a single output file. |
| Remove Human Reads Settings, "The command line reaches the same setting as `--database-id`" | true | `fastq.txt:1433` block, `--database-id <database-id>  Human read removal database identifier`. | none |
| Retain Reads, "On the command line this is `--retain`" | true | `fastq.txt` `==== fastq deacon-ribo ====`, `--retain <retain>  Read classes to retain: norrna, rrna, or both (default: norrna)`. | none |
| Contaminant Mode, "On the command line this is `--mode`, with `--ref` naming the custom FASTA" | true | `fastq.txt:222-225`. | none |
| K-mer (contaminants) default 31, "On the command line this is `--kmer`" | true | `FASTQOperationDialogState.swift:2129`; `fastq.txt:226` `--kmer <kmer>  K-mer size (default: 31)`. | none |
| Hamming Distance default 1, "On the command line this is `--hdist`" | true | `FASTQOperationDialogState.swift:2130`; `fastq.txt:227`. | none |
| Entropy Threshold "default is 0.60 ... the slider runs from 0.3 to 0.9 in steps of 0.05" and "`--entropy`" | true | `FASTQEntropyFilter.swift` defaults entropy 0.6, minimum 0.3, maximum 0.9, and the slider uses `entropyStep` (`FASTQOperationToolPanes.swift:428-432`); `fastq.txt:250` `--entropy <entropy>  Entropy threshold, 0.3-0.9 (default: 0.6)`. | none |
| Entropy Threshold, "which the pane reports as removing about 4 percent of reads and about 89 percent of tandem-repeat reads on the benchmark dataset" | true | `FASTQOperationToolPanes.swift:435` reads "0.6 removes about 4% of reads and about 89% of tandem-repeat reads on the benchmark dataset." | none |
| Window default 50, "`--window`" | true | `FASTQEntropyFilterDefaults.window` 50; `fastq.txt:252`. | none |
| K-mer (entropy) default 5, "`--kmer`" | true | `FASTQEntropyFilterDefaults.kmer` 5; `fastq.txt:254`. | none |
| Preset, six names, default Exact PCR, "it simply sets `--subs`, `--optical`, and `--dupedist` for you" | true | Names at `FASTQOperationToolPanes.swift:1086-1101`; default at `FASTQOperationDialogState.swift:2136`; `parameters.yaml:706` records the flag as "derived, sets --subs, --optical, and --dupedist"; those three are the only tuning flags `fastq deduplicate` takes (`fastq.txt:387-393`). | none |
| Substitutions default 0, "`--subs`" | true | `FASTQOperationDialogState.swift:2137`; `fastq.txt:388` `--subs <subs>  Substitution tolerance (0=exact, 2=default) (default: 0)`. | none |
| Optical Duplicates "off by default", "`--optical`" | true | `FASTQOperationDialogState.swift:2138` sets false; `fastq.txt:390` is a bare flag. | none |
| Optical Distance default 40, "appears only while Optical Duplicates is on", "`--dupedist`" | true | `FASTQOperationDialogState.swift:2139` sets 40; visibility gate at `FASTQOperationToolPanes.swift:472-475`; `fastq.txt:391` `--dupedist <dupedist>  Pixel distance for optical duplicates (default: 40)`. | none |
| Optical Distance, "Use 12000 on a NovaSeq instead" | unverifiable | Neither the source, the CLI help, nor `parameters.yaml` names 12000 as a NovaSeq figure; `parameters.yaml:729` says "typically 40 for HiSeq and 12000 for NovaSeq" but the registry is itself a manual artifact, not app behaviour. The value is the widely published clumpify guidance. Settled by citing the BBTools documentation in the chapter, or by reading what the Optical NovaSeq preset actually sets. | Cite the source of 12000, or restate as the value the Optical NovaSeq preset uses once that preset's constant is read. |
| Output Strategy, default Per Input, options Per Input and Grouped Result, "This setting has no command-line flag" | true | `FASTQOperationDialogState.swift:2131` `outputStrategyOptions` returns `[.perInput, .groupedResult]`; `parameters.yaml` gives `cli_flag: null` on all four entries; no such flag appears in any of the five `fastq.txt` OPTIONS blocks. | none |
| Deacon quoted block on SRR36291587, both lines exactly as printed | true | Recovered verbatim from `srr_scrubbed_1.fastq.lungfish-provenance.json`: `Deacon v0.16.0; mode: deplete; input: single; options: abs_threshold=2, rel_threshold=0.01, threads=14` and `Retained 85192/85199 sequences (99.992%), 21383192/21384949 bp (99.992%) in 56.12ms.` The chapter truncates the trailing throughput figures, which is a fair excerpt. | none |
| "85,192 of 85,199 retained ... 7 of 85,199 reads were called human and dropped" | true | Recounted: `SRR36291587_1.fastq` 340,796 lines = 85,199 reads; `srr_scrubbed_1.fastq` 340,768 lines = 85,192 reads. Difference 7. | none |
| Deacon quoted block on HG002, "Retained 72/45574 sequences (0.158%), 17778/11331492 bp (0.157%) in 24.44ms." | true | Verbatim in `hg002_scrubbed_R1.fastq.lungfish-provenance.json`. | none |
| "72 of 45,574" and "That is 99.84 percent of the reads called human" | true | Recounted: `hg002_R1.fastq` 182,296 lines = 45,574 reads; `hg002_scrubbed_R1.fastq` 288 lines = 72 reads. 45,502/45,574 = 99.842 percent. | none |
| "SRR36291587 is an amplicon run in which the sequencing targeted the virus by PCR" | true | `fixtures/sarscov2-srr36291587/README.md` describes it as "QIAseq Direct SARS-CoV-2, paired-end Illumina". | none |
| Results table, "Remove ribosomal RNA sequences, non-rRNA, 45,456 kept, 118 removed (0.26%)" | true | Recounted `riboout/hg002_R1.norrna.fastq` at 45,456 reads. 118/45,574 = 0.2589 percent. The paired run's own log reads `Retained 90912/91148 sequences (99.741%)`, which is 45,456 per mate. | none |
| "Remove Contaminants, PhiX, 45,574 kept, 0 removed" | true | `hg002_nophix_R1.fastq` 182,296 lines = 45,574 reads, identical to the input. | none |
| "Remove Contaminants, chr20 as a custom reference, 14 kept, 45,560 removed (99.97%)" | true | `hg002_nocustom_R1.fastq` 56 lines = 14 reads. 45,560/45,574 = 99.9693 percent. | none |
| "Low-Complexity Filter, threshold 0.60, 45,564 kept, 10 removed (0.02%)" | true | `hg002_entropy_R1.fastq` 182,256 lines = 45,564 reads. | none |
| "Remove Duplicates, Exact PCR, 45,076 kept, 498 removed (1.09%)" | true | `hg002_dedup_R1.fastq` 180,304 lines = 45,076 reads. The clumpify log in the sidecar reads `Reads In: 45574`, `Duplicates Found: 498`, `Reads Out: 45076`. 498/45,574 = 1.0928 percent. | none |
| "At its lowest setting of 0.3 the filter removed 0 reads. At the default 0.60 it removed 10. At its highest setting of 0.9 it removed 774, or 1.70 percent" | true | Recounted: `hg002_entropy03_R1.fastq` 45,574 reads (0 removed), `hg002_entropy_R1.fastq` 45,564 (10 removed), `hg002_entropy09_R1.fastq` 44,800 (774 removed). 774/45,574 = 1.6983 percent. | none |
| "subs 2 keeps 44,871" | unverifiable as written | Recounted `hg002_dedup_s2_R1.fastq` at 44,871 reads, so the figure is correct, but the chapter's body never states it. It appears only in the author's notes. | If the figure is meant to be in the chapter, add it beside the Exact PCR row as the Near Duplicate 2 comparison. Otherwise no action. |
| "Just over one percent exact duplicates is normal for a PCR-free library, and a figure above roughly 20 percent would say the library was amplified from too little starting material" | unverifiable | Domain guidance, not app behaviour. Nothing in the source, CLI help, or registry states a 20 percent threshold. Settled by a citation, or by the Bioinformatics Educator standing behind it as field knowledge. | Attribute the 20 percent figure or soften it to a qualitative statement. |
| Provenance sidecar "carries the exact command that ran, the tool version, the tool's own log, and a checksum for every input and output file" | true | `riboout/hg002_R1.norrna.fastq.lungfish-provenance.json` holds `reproducibleCommand`, `toolVersion` 0.16.0, `stderr` with the full Deacon log, and `sha256` on all four file entries, two inputs, the reference index, and the output. | none |
| "the sidecar recorded Deacon 0.16.0, the parameters `absoluteThreshold` 1 and `relativeThreshold` 0, and the full `micromamba run -n deacon deacon filter` command line" | true | Same sidecar: `toolVersion` `0.16.0`, `parameters.absoluteThreshold.value` 1, `parameters.relativeThreshold.value` 0, and `reproducibleCommand` beginning `micromamba run -n deacon deacon filter --deplete -a 1 -r 0.0 ...`. | none |
| "Deacon spends about four of those seconds loading its index" | true | The scrub-human sidecar's stderr reads `Loaded index (k=31, w=15) in 3.90s` against 56.12ms of filtering, and wall time was 4.21s. Note this holds for the panhuman index only. The ribokmers index loaded in 10.68ms. | Optionally scope the sentence to human read removal, since the ribosomal index loads in milliseconds and the surrounding paragraph covers all five operations. |
| "Each of the five operations finishes in a few seconds on this fixture" | true | Wall times from the sidecars ranged 0.20s to 4.21s. | none |
| "Every tool this chapter needs arrives with the Required Setup pack" | true | `third-party-tools-lock.json` top-level `tools` includes `bbtools` (line 10) and `deacon` (line 12); `PluginPack.swift:414-437` builds the Required Setup pack from `lock.tools`. | none |
| "BBTools, which supplies both bbduk and clumpify" | true | `third-party-tools-lock.json:10` lists `clumpify.sh` and `bbduk.sh` among the bbtools executables. | none |
| "No optional pack is needed, and neither is Docker Desktop" | true | All five operations resolve to base-pack tools; none of the five subcommands or their services reference a container runtime. | none |
| "The human database is shown in the Plugin Manager as Human Read Removal Data, and the ribosomal one as Ribosomal RNA Removal Data" | true | `third-party-tools-lock.json:27-28` and 74-75 give exactly those `displayName` values. | none |
| "Both arrive with Required Setup rather than as separate downloads" | unverifiable | The two indexes are `managedData` entries (`third-party-tools-lock.json:27-28`), which is a section distinct from `tools`, and the Required Setup pack is built from `lock.tools` alone (`PluginPack.swift:434`). They install through `DeaconPanhumanDatabaseInstaller` and `DeaconRibokmersDatabaseInstaller` (`DatabaseRegistry.swift:109-146`), and the Welcome window's setup list does surface requirements carrying a `managedDatabaseID` (`WelcomeWindowController.swift:1339`), which suggests they are part of the setup flow. Whether the reader sees them installed as part of Required Setup, or on first use, is not settled by the source. Settled by opening the Plugin Manager on a machine that has never run a decontamination operation and reading the Required Setup section. | Confirm in the app before the sentence stands. |
| "which is why neither appears on the Plugin Manager's Databases tab alongside the classification databases" | true | `PluginManagerView.swift:894-897` builds the Databases tab from `toolOrder: [.kraken2, .esviritu, .taxtriage]` only, so no Deacon index can appear there. The stated cause is a reasonable reading, though the mechanism is the tab's tool filter rather than the install route. | none |
| "the three decontamination databases" | false | There are two managed decontamination databases, `deacon-panhuman` and `deacon-ribokmers` (`third-party-tools-lock.json:27-28`). The PhiX reference is not a database at all, it is a FASTA bundled inside the managed BBTools environment (`FASTQDerivativeService+BBTools.swift:38,67`), and it never appears in the Plugin Manager. | "the two decontamination databases" |
| "On the command line, `lungfish-cli conda db install-managed --list` names them" | true | Ran it. Output is `human-scrubber`, `deacon-panhuman`, `deacon-ribokmers`. It names them by identifier rather than by display name, and it also lists a third, unrelated `human-scrubber` entry. | Optionally note that the command prints identifiers, since the paragraph just introduced the display names. |
| "Download the files ... from the manual's fixtures on GitHub at .../fixtures/hg002-chr20" | true | `HG002.chr20.10.0-10.5Mb_R1.fastq.gz` and `_R2.fastq.gz` are both committed in that directory. | none |
| "The human-read-removal section uses the SRR36291587 SARS-CoV-2 reads instead, and its fixture folder sits beside the first one" | false | The folder exists but holds no reads. `fixtures/sarscov2-srr36291587/README.md` states "The 21.7 MB compressed FASTQ from SRA is **not** committed. Run `./regenerate.sh` to re-derive every artifact from the original accessions." A reader following the chapter's instruction finds a reference FASTA, a GFF3, and two expected VCFs, and no FASTQ. | Say that the SARS-CoV-2 reads are not committed and point the reader at `regenerate.sh` in that folder, or at fetching SRR36291587 through the SRA chapter. |
| CLI block, `scrub-human ... --database-id deacon-panhuman --output ...` | true | `fastq.txt:1436` USAGE. The author's own run used exactly this form. | none |
| CLI block, `deacon-ribo <R1> <R2> --retain norrna --output ribo-filtered/` | true | `fastq.txt:1485` USAGE takes `<inputs> ...` and `-o, --output <output>  Output directory`; the ARGUMENTS line reads "Input FASTA/FASTQ file, or paired R1/R2 FASTQ files". | none |
| CLI block, `contaminant-filter ... --mode phix --kmer 31 --hdist 1 --output ...` | true | `fastq.txt:217` USAGE. | none |
| CLI block, `entropy-filter ... --entropy 0.6 --window 50 --kmer 5 --output ...` | true | `fastq.txt:245` USAGE. | none |
| CLI block, `deduplicate ... --subs 0 --output R1.dedup.fastq.gz --compress` | true | `fastq.txt:382` USAGE. The author's `gzdedup.fastq.gz` run confirms the gzipped output works. | none |
| "Every command accepts a gzipped FASTQ directly" | true | The author's contaminant, entropy, and dedup runs all took `HG002...fastq.gz` input, per the `input` parameter recorded in each sidecar. | none |
| "adding `--compress` writes a gzipped output" | true | `--compress  Compress output with gzip` in all five OPTIONS blocks; `gzdedup.fastq.gz` is a valid gzip stream of 45,076 reads. | none |
| "Every subcommand except `deacon-ribo` writes to a file path and takes `--force`" | true | `--force` and `-o, --output <output>  Output file path (required)` appear in the scrub-human, contaminant-filter, entropy-filter, and deduplicate blocks. `deacon-ribo`'s OPTIONS has neither `--force` nor a file output, only `-o, --output <output>  Output directory`. | none |
| "`--absolute-threshold`, the number of matching minimizers a read needs before it is called ribosomal, which defaults to 1" | true | `fastq.txt` `==== fastq deacon-ribo ====`, "Minimum absolute minimizer hits for an rRNA match (default: 1)". | none |
| "`--relative-threshold`, the share of a read's minimizers that must match, which defaults to 0.0" | true | Same block, "Minimum relative minimizer-hit proportion for an rRNA match (default: 0.0)". | none |
| "It also takes `--format`, choosing whether its report is written as text, json, or tsv" | true | Same block, `--format <format>  Output format: text, json, tsv (default: text)`. | none |
| "`entropy-filter` takes `--threads`, defaulting to 4" | true | `fastq.txt:256`, `--threads <threads>  bbduk thread count (default: 4)`. | none |
| "Three flags exist only on the command line" | false | The sentence introduces `--absolute-threshold`, `--relative-threshold`, `--format`, and `--threads`, which is four flags, and `deacon-ribo` additionally exposes CLI-only `--database-id` and `--threads` of its own (`fastq.txt` `==== fastq deacon-ribo ====`). The count does not match the list that follows it. | "Several flags exist only on the command line." |
| "`scrub-human --remove-reads` is kept for compatibility and is ignored, because Deacon always removes what it matches" | true | `fastq.txt:1447-1449`, "Deprecated compatibility flag; ignored because Deacon always removes matched reads". | none |
| "`deacon-ribo` accepts a paired R1 and R2 together and writes both filtered files into the directory you name, so a paired run there is one command rather than two" | true | The author's paired run wrote `hg002_R1.norrna.fastq` and `hg002_R2.norrna.fastq` into `riboout/` from a single invocation, and its `reproducibleCommand` passes both mates. | none |
| "Pass the kept-read bundle to Tools > Mapping > minimap2..." (the DRIFT correction) | not present | The chapter as written contains no such sentence. Its "Next" section links the Mapping chapter instead, which is a cleaner resolution of DRIFT claim 32. | none |
| "Continue to Subsetting and Extraction ... or go to Mapping Reads to a Reference" | true | Both link targets exist at `chapters/03-reads/06-subsetting-and-extraction.md` and `chapters/04-alignments/01-mapping-reads-to-a-reference.md`. | none |
| Chapter passes the strict manual lint | true | `LUNGFISH_MANUAL_STRICT=1 bash docs/user-manual/build/scripts/lint-chapter.sh` reports "no issues found". | none |

## Notes for the campaign lead

Two of the four false verdicts point outside this chapter.

The `Analyses/<tool>-<timestamp>/` claim is inherited from CONSISTENCY.md
line 47, which states the rule for every analysis result. The source shows
FASTQ operations write into `Analyses/` directly, and only assemblies get
the timestamped subfolder. Every chapter in Part III that repeats the rule
is affected, so settle it in CONSISTENCY.md before correcting this chapter.

The SARS-CoV-2 fixture download instruction fails because the reads are not
committed. The chapter's entire human-read-removal worked example depends on
that fixture, so either the reads need committing or every chapter that
sends a reader to that folder needs the `regenerate.sh` route instead.

One further row records a DRIFT correction (claim 32, the Mapping menu path)
that the rewritten chapter no longer contains, so it carries no verdict.

Counts: 78 true, 5 false, 5 unverifiable.
