# Reader 2 report

Persona. A senior who has spent two years pipetting in a wet lab. I know
library prep and PCR. I have never analyzed data and I have never opened a
terminal.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| What it is, "reference databases and hands back a table" | Reference database is never explained. I know what a reference genome is from a lecture, but I do not know what is in a CZ ID reference database or how big it is. | Gloss reference database at first use as the collection of known sequences the reads are compared against. |
| What it is, "The command-line help states the boundary" | Command-line help is named in the second paragraph, before I have been told what a command line is or that I will ever need one. It made me think I had missed a prerequisite. | Move this sentence after the first mention of the command line in the Procedure, or say the app shows the same wording. |
| What it is, "opens in the same viewport as a Kraken 2 run" | Viewport is used without a gloss and Kraken 2 is named with no explanation. I have not read the Kraken 2 chapter and the prereqs at the top do not list it. | Gloss viewport at first use and add one clause saying Kraken 2 is LGE's own classifier. |
| What it is, "The conversion writes a kreport, the tab-separated summary format Kraken 2 uses" | The gloss tells me the file format but not why I should care that LGE rewrites my file into another tool's format. I read this twice trying to find the reason. | Add that the kreport is what the shared viewport reads, in the same sentence. |
| What it is, "not available under Tools > Classification" | This paragraph tells me where the feature is not, before telling me where it is. I hunted the Tools menu in my head for a while. | State the Import Center location first and the Tools menu absence second. |
| What it is, "So what should you do with this?" | A rhetorical question aimed at me that I could not answer, since I had not yet been told the answer. It broke my reading. | Replace with a plain statement of the two-step workflow. |
| Why you would do this, "the .lungfishtax bundle the import creates" | The file extension appears before the word bundle is glossed, and the gloss arrives two sentences later. I did not know whether .lungfishtax was a file or a folder until then. | Move the bundle gloss ahead of its first use. |
| Why you would do this, "recording the source file's checksum" | The checksum gloss is good but nothing tells me what I would ever do with one. Two people confirming they hold the identical file is abstract to me. | Add one clause naming the concrete use, such as proving to a reviewer the file was not altered. |
| Why you would do this, "the long tail of one-read hits" | Long tail is jargon I have only heard in a business context. I could not tell whether one-read hits are noise I should ignore or real findings. | Say plainly that taxa with one or two reads are usually background. |
| Why you would do this, "This chapter is one of the manual's viral examples" | This sentence talks about the manual's own policy rather than to me. I stopped to work out whether it changed anything I had to do. | Cut it, or fold the reason into the sentence describing the worked example. |
| Before you start, "a report from a real run will be far longer" | Far longer gives me no number. I cannot judge whether my own report of a few hundred rows is normal or whether something went wrong upstream. | Give a typical row-count range for a real CZ ID report. |
| Before you start, "The numbers quoted through this chapter come from a real import of the repository's own small CZ ID test report" | The repository is not a thing I have. This sentence points at a file path I cannot open, so the worked example became something I can read about but not repeat. | Say explicitly that the reader cannot run this exact example and that their own export will behave the same way. |
| Before you start, "Those are tax_id, taxon_name, and rank" | I am not told how to check whether my exported file has these columns. Opening a TSV is not something I have done. | Add one sentence saying the columns are the first line of the file and can be seen in a spreadsheet program. |
| Step 1, "TSV standing for tab-separated values, a plain-text table" | The gloss is fine but I still do not know which of the three shapes CZ ID gives me by default, so I do not know which one I will actually have. | Say which shape CZ ID's download button produces by default. |
| Step 1, "The reference run confirmed all three paths against the same three-row report" | Reference run is used repeatedly without ever being defined. I eventually decided it means the authors tested it, but I read it twice the first time. | Define reference run once in Before you start. |
| Step 3 item 3, "The reference run's preview named the sample Sample-CZ-001, the project Project-42" | This is one very long sentence carrying nine facts. I lost track of which values were examples and which were fixed labels. | Split into a short lead-in and a list of the preview's fields. |
| Step 3 item 3, "The Project row appears only when the export carries a project identifier" | Project identifier is not explained and I do not know whether my export has one or how I would tell. | Say the identifier comes from the CZ ID project the sample was uploaded into. |
| Step 3 item 4, "read the note under step 5 before you trust what it says" | I was told to read a section and simultaneously told not to believe it, with the correction placed after the step. This is the point where I lost confidence in the procedure. | Put the warning inline at step 4 rather than pointing forward. |
| Step 4, "which you open with Operations > Show Operations Panel (Cmd-Shift-P)" | The Operations Panel is glossed by link only, and the text does not say whether it opens on its own or whether I must open it to see progress. | State whether the panel appears automatically when an import starts. |
| Step 4, "The row carries the equivalent command line, so you can copy the command" | I have never opened a terminal, so I cannot tell whether this is something I should do or something I can ignore. | Add that this is optional and only useful for repeating the run later. |
| Step 4, "named from the sample name with any character that is not a letter, a digit, a dot, a hyphen, or an underscore replaced by a hyphen" | Very long clause about a rule I cannot act on. I reread it and it did not change anything I do. | Shorten to a note that spaces and punctuation in the sample name become hyphens. |
| Step 5, "Do the same on the command line" | A step numbered as part of the procedure that I cannot perform at all. I could not tell whether skipping it means I have done the procedure. | Mark this step optional, or move it out of the numbered procedure. |
| Step 5 code block, "/path/to/cz-id-taxon-report.tsv" | Placeholder paths and the backslash line continuations are meaningless to me. I do not know how to turn a file on my desktop into one of these. | Add one sentence saying the placeholders are replaced by the file's own location. |
| Settings, "Three options exist on the command line only" | The Settings section is entirely about flags I cannot use, yet the section reads as required. I could not tell whether the app's import loses anything by not offering them. | Say up front that the app path needs no settings and that the flags below only add records. |
| Settings, "--metadata ... a metadata sidecar being the separate file CZ ID can export" | Sidecar is used as a noun of art. I do not know what CZ ID's metadata file is called on its download page or whether I have one. | Name what the download is called in CZ ID's interface. |
| Settings, "--non-host-fastq ... the reads CZ ID kept after discarding everything matching the host organism" | The gloss is clear, but I do not know why recording a path rather than the reads themselves is the right behavior, or what happens if I later move that file. | Add one clause on what happens to the record if the file is moved or deleted. |
| Reading the results, "Rows counts every taxon row in the report, including the root row" | Root row is introduced here as something CZ ID writes, but the earlier sections never mentioned it. It arrives as a surprise that changes the count I was told to check. | Introduce the root row when the row count is first described, in the preview step. |
| Reading the results, "Pipeline is CZ ID's own pipeline version" | Pipeline version is never explained as a concept. I do not know whether 8.4 is recent, or what changes between versions. | Say a pipeline version identifies which analysis code produced the numbers. |
| What lands in the bundle, "both source and copy reading 3852c1bd..." | A truncated checksum with no explanation of the ellipsis. I could not tell whether the real value is longer or whether the dots are part of it. | Say the value is shortened here and the full value is in the provenance file. |
| What lands in the bundle, "recording the argv, the exit status, the wall time" | Argv, exit status, and wall time are three terminal terms in one clause, none glossed. This sentence stopped me completely. | Replace with the command that ran, whether it succeeded, and how long it took. |
| Reading the results kreport block, "100.00 1200 1200 R 1 root" | The block has six unlabelled columns and the prose explains columns one, four, and five. Columns two and three are never named, and I had to infer them. | Label all six columns before or after the block. |
| Reading the results, "Viruses drew 88 of the sample's 1,200 reads, which is 7.33 percent" | I cannot judge whether 7.33 percent of reads being viral is high, low, or expected for a respiratory sample. The number is explained but not interpreted. | Add one clause on what a typical viral fraction looks like in such a sample. |
| Reading the results, "Only the NT read counts reach the kreport. NR counts, percent identity, alignment length, and e-value stay in the preserved original" | NT and NR are used from the preview onward and never explained. Percent identity and e-value are also unglossed. I do not know which of the two counts I should be quoting. | Gloss NT and NR at first use and say which one the viewport shows. |
| The viewport, "A sunburst chart sits on the left, drawing the root of life at the centre" | I have never seen a sunburst chart and the description does not tell me how to read it. The chapter then sends me to another chapter for the controls. | Add one sentence on what an outer wedge means relative to an inner one. |
| The viewport, "the reference bundle reports 3 taxa for a report holding two real ones" | The count on screen is described as counting something that is not a taxon. I could not tell whether this is a bug I should report or expected behavior. | State plainly that this is expected and not a defect. |
| The viewport, "Reads is the clade count, meaning reads assigned to that taxon or to anything under it" | Clade is used without a gloss. I know it from a phylogenetics lecture but only vaguely, and the sentence depends on it. | Gloss clade as a taxon together with everything beneath it. |
| The viewport, "BLAST Verify and Export stay available" | BLAST is named with no explanation. I have heard of it but I do not know what pressing this button would do to my result. | Add a short gloss or point at the BLAST chapter at this mention. |
| What good looks like, "A count far smaller than expected" | Far smaller is not quantified and I was never told what to expect. Two of the four checks compare against what CZ ID showed in the browser, which I would have to have written down beforehand. | Tell the reader in Before you start to note the row count in CZ ID first. |
| What good looks like, "which happens when an export is assembled by hand from two downloads" | I do not know what assembling an export by hand means, so I cannot tell whether I am at risk of doing it. | Say this only happens if someone combined files manually, and that a single download is safe. |
| Known defect in this build, "verified in the source at AppDelegate+ToolsMenu.swift:860-867" | A source file and line numbers I cannot open and would not understand. It made the defect feel like a note for developers rather than for me. | Drop the file reference from the reader-facing text. |
| On the command line, "The block below reproduces the whole chapter" | Six commands with no per-command instruction on how to run them. Having never opened a terminal I cannot start any of them. | Point to whichever chapter explains opening a terminal, or say this section is for command-line users. |
| On the command line, "--top caps how many taxa are listed and defaults to 20" | The default is given but not whether 20 is enough. I do not know whether my real report would hide things at 20. | Say what happens to taxa beyond the cap and when to raise it. |
| On the command line, "NT RPM is reads per million ... lets you compare a taxon across samples sequenced to different depths" | Sequencing depth is assumed knowledge. I do know it from library prep, but the sentence would fail a reader who does not, and the value 73333.0 is never judged. | Say what an RPM value of this size indicates. |
| On the command line, "The --format json form emits an array of records carrying those fields plus ntPercentIdentity, ntAlignmentLength, ntEValue, and nrRpm" | Four field names in camel case, none of which were explained anywhere in the chapter. I could not judge whether I need them. | Say these are the extra CZ ID measures preserved for later use. |
| On the command line, "The last command is a different subcommand from the one above it" | The distinction between import cz-id and cz-id import is a word-order difference I had to read three times to see. | Set the two forms side by side in a short table. |

Three closing lines.

The one thing I learned. An imported result carries the CZ ID pipeline and
database versions with it, so I can answer a reviewer's question about which
database produced a number.

The one thing I still could not do. Perform any part of the command-line half
of this chapter, which includes step 5 of the numbered procedure.

The sentence I liked most. "The answer lives in a browser tab, in an account,
behind a login, on someone else's schedule."
