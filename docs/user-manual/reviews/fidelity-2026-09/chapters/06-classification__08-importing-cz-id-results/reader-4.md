# Reader 4 report, Importing CZ ID Results

Persona. Undergraduate. One semester of Geneious in a course. Comfortable with a graphical sequence viewer. Expects every operation to have a dialog. Has never opened a terminal.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| What it is, "reads that report. It does not run CZ ID" | I do not know what a "reference database" is here. The sentence before says CZ ID "compares them against reference databases" and I read past it assuming I knew, then hit NT DB and NR DB later and realised I did not. | Gloss reference database at first use in this paragraph as a stored collection of known sequences the reads are compared against. |
| What it is, "The command-line help states the boundary" | Command-line help is quoted as authority in the second paragraph of the chapter. I have never opened a terminal and do not know what "command-line help" is or how anyone reads it. | Cite the boundary from the card description instead, which the chapter already mentions in step 2. |
| What it is, "converts the CZ ID report into its own read classification format" | I had to read this twice. Three formats appear in three sentences (report, read classification format, kreport) and I could not tell whether kreport is a different thing from read classification format or the same thing. | Say in one sentence that the read classification format is written as a kreport file, so the two names name one thing. |
| What it is, "the same viewport as a Kraken 2 run" | Kraken 2 is used as a landmark in the opening section but is never said to be a classifier LGE runs locally. In Geneious everything named is a plugin I could install, so I assumed Kraken 2 was something I had to have installed for this import to work. | Add four words at first mention saying Kraken 2 is a classifier LGE runs itself. |
| What it is, "It is not available under Tools > Classification" | Told what a menu does not contain before I have been told what it does contain. I went and looked for Tools > Classification to check, which was not the point. | Move this paragraph after Before you start, or drop the menu name and say only that the import lives in the Import Center. |
| Why you would do this, "The `.lungfishtax` bundle the import creates" | The extension appears with no reading and no explanation of the leading dot. In Geneious I never see file extensions. I did not know whether `.lungfishtax` is a file I will see in Finder or something hidden. | Say once that this is a folder name ending in `.lungfishtax` that Finder shows as one item. |
| Why you would do this, "a short fingerprint computed from the file's exact bytes" | Checksum is glossed well but I cannot judge one. Later the chapter prints `3852c1bd...` and I do not know what I am supposed to do with a truncated value or whether I am expected to compare it myself. | Say plainly that LGE compares checksums for you and you never type one. |
| Why you would do this, "a spreadsheet with a thousand rows hides its own shape" | Sentence had to be read twice. "Hides its own shape" is a figure of speech I could not resolve on first pass. | Replace with a literal statement that a long table makes it hard to see which taxa dominate. |
| Before you start, "The manual does not ship one" | I was told to export a report from CZ ID's web interface with no indication of where in that interface the export lives. I have no CZ ID account and cannot follow this step at all. | Name the CZ ID page and button, or state that the chapter assumes someone has already handed you the export. |
| Before you start, "`Tests/Fixtures/czid/minimal_taxon_report.tsv`" | A repository path is quoted as the source of every number in the chapter. I do not have a repository and do not know what one is in this context, so I could not tell whether this file is something I can open. | Say that this file ships with the source code and is not part of the installed app. |
| Before you start, "No plugin pack is needed" | Plugin pack is used without explanation. In Geneious a plugin is a thing I install from a menu, so I went looking for the equivalent to confirm I did not need one. | Gloss plugin pack at first use, or cross-reference the chapter that covers it. |
| Before you start, "carries the columns the importer needs. Those are `tax_id`, `taxon_name`, and `rank`" | I am told to check my file for three columns but not how to check. I would have to open a TSV somehow, and the chapter has not said what opens one. | Say the Preview panel does this check for you and the error message is the answer. |
| Procedure step 1, "a plain-text table with one row per line" | TSV is glossed here but the file hint on the card in step 2 also says "taxon report TSV" and the term first reaches me in the frontmatter caption. Minor, but I met the acronym before the gloss. | Move the TSV gloss to the first place the acronym appears in body text. |
| Procedure step 1, "Given an archive or a folder, LGE unpacks it if needed" | Unpacks it where. I could not tell whether a new folder appears next to my download or whether this happens invisibly. | Add that the unpacking is temporary and leaves nothing beside your file. |
| Procedure step 2, "every card is also a drop target for a dragged file" | This is a Geneious habit of mine and I would have used it, but the chapter never says drag and drop actually completes the import or still shows the sheet. | Say that dragging onto the card opens the same sheet with the path already filled in. |
| Procedure step 3.2, "which on a report this size is momentary" | I cannot judge this for my own file. "This size" means three rows. I do not know if a real report takes one second or one minute. | Give a rough time for a report of a few thousand rows. |
| Procedure step 3.3, "3 rows, a source of Taxon report file" | The preview reports a number I cannot judge. What good looks like says to compare it to what CZ ID showed in the browser, but that is 80 lines later and I had already decided the number meant nothing to me. | Add half a sentence here pointing at the row-count check. |
| Procedure step 3.4, "read the note under step 5 before you trust what it says" | Forward reference inside a numbered procedure. I stopped, scrolled down, read the defect note, then had to find my place again. | Put the one-sentence warning inline in step 4 and keep the full note at the end. |
| Procedure step 3.5, "it stays disabled until a path is selected and the scan has finished and succeeded" | Three conditions in one clause. I read it twice to work out whether a successful scan alone was enough. | Split into two sentences. |
| Procedure step 4, "which you open with Operations > Show Operations Panel (Cmd-Shift-P)" | I could not tell whether the panel opens by itself when the import starts or whether I have to open it. Geneious shows progress automatically. | State whether the panel appears on its own. |
| Procedure step 4, "The row carries the equivalent command line" | Equivalent command line means nothing to me and there is nothing I would do with it. It reads as if I am expected to use a terminal. | Say it is there for people who script and can be ignored otherwise. |
| Procedure step 4, "any character that is not a letter, a digit, a dot, a hyphen, or an underscore replaced by a hyphen" | I had to read this twice to understand it describes automatic renaming rather than a rule I must follow when naming my sample. | Lead with "LGE renames the bundle for you" before listing the allowed characters. |
| Procedure step 5, "Do the same on the command line" | Step 5 of a numbered Procedure is a terminal step. I cannot do it and briefly thought the procedure was unfinished without it. | Mark it optional in the heading, or move it out of the numbered procedure entirely. |
| Procedure step 5, "`--project /path/to/project.lungfish`" | I do not know what `/path/to/` means or that I am supposed to substitute it. The backslash at the end of the first line also stopped me. | State once, before the first code block in the manual, that these are placeholders. |
| Settings, "The CZ ID import has no settings." then three flags described | The section says there are no settings and then documents three. As a dialog-first user I could not tell whether I am missing something in the sheet. | Retitle the three as command-line-only options in a separate subsection. |
| Settings --sample-name, "the app path takes this name from the report's own sample column instead of asking you" | This is the first place I learn the app never asks me for a name. I expected a name field in the sheet, the way Geneious asks on every import. | Say this in step 3, where I am looking at the sheet and wondering where the name field is. |
| Settings --metadata, "the reference run confirmed the file is recorded as an input in provenance with its size and checksum rather than copied into the bundle" | Long sentence, and I could not work out the practical consequence. If the file is not copied, does my bundle break when I move the metadata file? | Add that moving or deleting the original later does not affect the bundle. |
| Reading the results, "The reference import printed this to the terminal." | The results section opens with terminal output. I have no terminal, and nothing tells me where to see the same five lines in the app. | Show the Provenance popover first and treat the terminal block as the alternative. |
| Reading the results, "including the `root` row that CZ ID writes to carry the sample's total read count" | I did not know reports contain a root row, and the chapter had not prepared me for a row that is not an organism. | Introduce the root row in What it is, where taxon is defined. |
| Reading the results, "the two database versions are CZ ID's, not LGE's" | I still do not know what NT and NR stand for or how they differ, after five mentions. I cannot judge whether a run using nt but not nr is a problem. | Expand NT and NR at first use as the nucleotide and protein databases. |
| What lands in the bundle, "The reference run's bundle held five files beside a `provenance` folder." | I do not know how to look inside a bundle. If it appears as one item in the sidebar, this whole subsection describes something I cannot see. | Say how to open a bundle in Finder, or state that these files are internal and you never need to open them. |
| What lands in the bundle, "recording the argv, the exit status, the wall time" | Argv, exit status, and wall time are three unexplained terms in one clause. I stopped reading the sentence. | Replace with the command, whether it succeeded, and how long it took. |
| What lands in the bundle, "100.00 1200 1200 R 1 root" | Six unlabelled columns. The prose explains columns one, four, and five, and I never learned what columns two and three are, even though they carry 1200 and 88. | Label all six columns before the block. |
| What lands in the bundle, "Viruses drew 88 of the sample's 1,200 reads, which is 7.33 percent" | 88 of 1200 is 7.33 percent, which checks out, but the row above says root has 1200 out of 1200 at 100 percent and the two virus rows sum to under 11 percent. I could not tell where the other 89 percent went. | Say in one sentence that unclassified and host reads make up the remainder and do not appear as rows. |
| What lands in the bundle, "Only the NT read counts reach the kreport" | I cannot judge what I lose. Percent identity, alignment length, and e-value are named as things left behind, and I do not know what any of them are or whether I will miss them. | Say that these live in the preserved original and name where to open it, or drop the list. |
| The viewport, "drawing the root of life at the centre and one ring per rank outward" | A sunburst is not a Geneious view. I could not picture it from this sentence and the screenshot placeholder is not yet filled. | Say what clicking a wedge does, which is the one thing I would try first. |
| The viewport, "the reference bundle reports 3 taxa for a report holding two real ones" | The app displays a count I am told is misleading. I could not tell whether this is a bug like the destination one or intended. | State plainly whether this is intended behaviour. |
| The viewport, "In a CZ ID import the two are equal for every row" | Reads and Direct are explained clearly, then I am told they are always equal here, so I could not see why the app shows both columns. | Say the columns are shared with local runs where they differ. |
| The viewport, "There is no Bracken column" | Bracken is introduced only to say it is absent. I did not know it existed and now have an unexplained name. | Drop the Bracken sentence from this chapter. |
| The viewport, "BLAST Verify and Export stay available." | Two buttons named with no explanation of what either does or where Export writes to. Extract FASTQ gets a full paragraph and these get four words. | Add one sentence for Export, or link both to their chapters. |
| What good looks like, "check that the row count in the Preview panel matches what CZ ID showed you in the browser" | By the time I read this the import is finished and the sheet is closed. The check is placed after the procedure but must be done during it. | Move the four checks into the procedure steps where each applies. |
| What good looks like, "A count far smaller than expected" | I cannot judge "far smaller" without knowing what CZ ID typically reports. | Give an order of magnitude, such as tens versus thousands of rows. |
| What good looks like, "check that the percent column agrees with what CZ ID reported" | Comparing every percent by hand against a browser tab is not something I could actually do for a thousand-row report. | Say to check only the top two or three wedges. |
| Known defect, "verified in the source at `AppDelegate+ToolsMenu.swift:860-867`" | A source file and line numbers appear in a chapter written for me. I cannot open this and it made me doubt the rest of the section was for me. | Drop the source citation from the reader-facing text. |
| On the command line, entire section | Fifteen lines of terminal commands with no app equivalent. The chapter says this block "reproduces the whole chapter", which suggested the app path I just followed was the lesser one. | Say the section is optional and that everything above it can be done in the app. |
| On the command line, "NT RPM is reads per million, the taxon's read count scaled to a sample of a million reads" | The gloss is clear but the number 73333.0 is not judgeable. I do not know whether that is high, and 73333 per million from 88 reads out of 1200 surprised me. | Say what a high or low RPM looks like, or note that RPM is only meaningful when comparing samples. |
| On the command line, "`lungfish-cli import cz-id` ... `lungfish-cli cz-id import`" | Two commands differing only in word order, and I had to read the paragraph three times to hold the difference. | Put the two in a two-row table. |
| Next, "Read BLAST Verification to confirm one of the taxa" | Confirm against NCBI is offered as a next step but the viewport section said BLAST Verify is available without saying what it verifies. | Say in one clause what BLAST Verify checks. |

Three lines.

The one thing I learned. An imported result opens in the same viewport as a locally run classifier, so I only have to learn one taxonomy view.

The one thing I still could not do. Export a report out of CZ ID, because the chapter assumes I already have one and never says where in CZ ID the export button is.

The sentence I liked most. "Pulling one taxon's reads out into a new bundle needs to know which reads those were, and a taxon report is a summary that does not say."
