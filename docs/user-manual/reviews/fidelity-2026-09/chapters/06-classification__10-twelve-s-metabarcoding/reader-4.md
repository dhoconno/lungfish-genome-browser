# Reader 4 report, 12S Amplicon Metabarcoding

Persona: undergraduate who used Geneious for one semester, comfortable with a graphical sequence viewer, expects every operation to have a dialog, has never opened a terminal.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| What it is, "compared base for base against a deduplicated reference FASTA" | I could not tell whether the read must equal the reference record or merely contain it. The next sentence says "contains a reference sequence exactly", which is a different rule. In Geneious a match is a percent identity, so this all-or-nothing rule is new to me and the two sentences describe it two ways. | State once that the reference record must appear as an exact substring of the read, and drop the "base for base" phrasing. |
| What it is, "So what should you do with this?" | I had to read the paragraph twice. The question is addressed to me but the three preceding sentences were about tool design, so I lost the thread of who was being asked what. | Cut the rhetorical question and begin the sentence with "Run the workflow". |
| Why you would do this, "the HG002 mitochondrial reads" | HG002 is never explained. I do not know if it is a person, a cell line, a file name, or a public dataset. It reappears four times, including in file names, and I could not tell whether I am meant to already have it. | Gloss it at first use as the name of a public reference human sample. |
| Before you start, "Build the demo project first if you have not already" | I could not perform this. The text does not say how to build the demo project or where the command lives. Everything else in the chapter names a menu path, so this one stands out as a step I cannot follow. | Give the menu path for building the demo project, or link the chapter that covers it. |
| Before you start, "a dependency row underneath reading either Ready or Needs install" | The concept of a plugin pack is assumed. Geneious plugins I installed once from a manager and never thought about again. I do not know whether Lungfish Tools is a download, how large it is, or whether it needs a network connection. | Add one sentence saying the pack is downloaded over the network and link the plugin-packs chapter. |
| Before you start, "It expects reads that are already merged" | I could not perform this step. It says to merge with "a read-processing operation" but names no menu item, no dialog, and no chapter. This is the one prerequisite the chapter warns will ruin the run, and it is the least actionable line in the section. | Name the exact menu path for the merge operation and link its chapter. |
| Procedure step 1, "either as a plain FASTA or as a prepared `.lungfish12sref` bundle" | I had no way to judge which I have. I have never seen a `.lungfish12sref` file and the text does not say the demo project ships one, so I did not know whether step 1 applies to me on the worked example. | Say plainly whether the demo project's primate reference is already a bundle, so the reader knows to skip or not. |
| Procedure step 1, "a metadata table in the MIDORI style" | MIDORI is never explained. I do not know if it is a database I download from somewhere or a format I type myself, and the chapter is asking me to supply one before I can build a reference. | Gloss MIDORI at first use as a published 12S reference database and say where the table comes from. |
| Procedure step 1, "naming its common name, its scientific name, its taxon group, and its taxid" | Four columns are named here, but the command-line section later lists seven required columns including `seq_id`, `name_source`, and `taxonomy`. Reading only the dialog section I would build a file that fails. | Make the two lists agree, or say here that the full column list is given later. |
| Procedure step 1, "click **Create 12S Reference...** beside the Reference picker" | The instruction is circular. Step 1 tells me to open the dialog as described in step 2, but step 2 assumes I already have a reference. I read both twice before working out the order. | Reorder so the dialog is opened first, or move reference building into step 2 as a branch. |
| Procedure step 2, "or pick one from the Project Reference menu above it" | I could not tell what makes the app find "candidates in your project", so I did not know whether to expect this menu to be populated or empty. | Say which folder the app scans for reference candidates. |
| Procedure step 3, "The remaining fields are covered one by one in Settings below" | This sends me away mid-procedure. In Geneious every dialog field is explained where I meet it. I read the Settings section, then had to come back and find my place in step 3. | Name the two remaining fields here in one sentence so the reader can run without leaving the procedure. |
| Procedure step 4, "reports four figures separated by vertical bars" | I could not judge these numbers. The order is given but no example line is shown, so I do not know what the text actually looks like on screen or which figure is which when I see it. | Show the literal summary line from the worked example. |
| Procedure step 4, "Refs is how many reference records that species matched" | I did not understand why one species has several reference records. The deduplicated reference was described as collapsing identical sequences, so I expected one record per species. | Add a half sentence saying a species may have several distinct 12S variants in the reference. |
| Procedure step 6, "click **BLAST Verify** in the action bar" | Geneious asked me before sending anything to NCBI. Here the text does not say whether a confirmation appears, and the earlier note that Learn More and View Photo skip confirmation made me wonder if this does too. Sending sample sequence off the machine is something I would want warned about. | Say explicitly whether BLAST Verify sends immediately, and note the data leaves the machine. |
| Procedure step 7, "use the `12s-export-unresolved` command shown at the end" | This is a dead end for me. I have never opened a terminal, the section is labelled optional, and yet the only in-window route to the FASTA is described as taking a file out of "the result bundle", which is not a thing I know how to open. | Say how to reveal the result bundle in Finder so the FASTA can be taken without a terminal. |
| Settings, "the Inspector's **12S Results** section" | The Inspector is never introduced in this chapter. I do not know where it is on screen or how to show it, and six of the fourteen settings live there. | Add one sentence saying where the Inspector is and how to open it. |
| Settings, "The first three sit under the **Target Rows** disclosure and the last two under **Unmatched Reads**" | Three plus two is five, but six controls are then described. I counted twice and still could not place Taxon Groups. | Correct the count and say where Taxon Groups lives. |
| Settings, Min Soft Clip, "how many of the read's own bases must sit on each side" | Soft clip is used as if I know it. The glossary is linked elsewhere in the chapter but not here, and the concept of a read's own bases hanging past a match is not something a Geneious viewer taught me. | Link the soft-clip glossary entry at this first use in prose and give a one-line picture of what is clipped. |
| Settings, Min Soft Clip, "The default is 1 ... Raise it when short partial overlaps are producing matches you do not trust" | I cannot judge this number. There is no range given, no sense of whether 2 or 20 is the next sensible value, and no symptom I could look at to decide. | Give the accepted range and one concrete alternative value. |
| Settings, Read Platform, "The default is Illumina exact" | The picker label and the setting names differ. Step 3 calls them "the Illumina setting" and "the Nanopore setting", here they are "Illumina exact" and "ONT indel-tolerant". I did not know which words are on screen. | Use one set of names throughout and say which appears in the picker. |
| Settings, Max Indels, "it is greyed out in the Illumina setting" | ONT is used as an abbreviation here and expanded only in the sentence before as Oxford Nanopore. I had to look back to connect ONT to Nanopore, and step 3 never uses ONT at all. | Expand ONT once and use it consistently in step 3 and Settings. |
| Settings, "ends with the command-line flag that does the same job" | Every one of fourteen settings ends with a terminal instruction I will never use. Reading the section is roughly a third wasted for a window-only reader, and the advice to skip them still means reading each one to find where it stops. | Move the flags into a compact table in the command-line section instead of into each entry. |
| Reading the results, "110 matched exactly, and 63 were left unresolved, giving a 63.6 percent exact-match rate" | The arithmetic did not work for me. 110 out of 173 is 63.6 percent, but 63 unresolved out of 173 is 36.4 percent, and step 4 said the summary reports "the percent of reads left unresolved". So I could not tell whether the summary shows 63.6 or 36.4. | Say which of the two percentages the summary line displays. |
| Reading the results, "Homo sapiens with 110 exact reads and 100 percent of the sample" | 100 percent contradicts the 63.6 percent exact-match rate two sentences earlier. The definition of % of Sample arrives only in the next paragraph, so at the moment I read it the two numbers looked inconsistent. | Define % of Sample as a share of matched reads before quoting the 100 percent figure. |
| Reading the results, "recorded in its own channel rather than folded silently into the exact-match counts" | I do not know what a channel is or where I would look at it. The sentence promises the result is auditable but gives me nothing to click. | Name the column, file, or view where the reassignments can be seen. |
| Reading the results, "The default policy lets any nonzero lead win" | This is a setting I was never shown. The dialog section lists eight controls and none of them is ambiguity resolution, but the command-line section has `--ambiguity-resolution` with a conservative alternative. So the window version silently uses strict with no way to change it. | Say in Settings that ambiguity resolution is fixed to strict in the dialog. |
| What good looks like, "a well-matched reference against a clean amplicon library usually lands high" | I cannot judge "high". No number is given anywhere, and the only worked figure, 63.6 percent, is then explained as disappointing. So I have no anchor at all for what my own run should reach. | Give a rough target percent, even as a range. |
| What good looks like, "The reads were oriented to match the reference before matching" | This is the biggest gap in the chapter for me. Orientation is presented as essential, the fix is a terminal command, and the Procedure never mentions orienting reads at all. Following the Procedure exactly, my run would lose most of its matches and I would not know why. | Add an orient step to the Procedure with its dialog path, or say in Before you start that orientation is required. |
| What good looks like, "does not also try the reverse complement" | Geneious matches both strands without my ever thinking about it. That LGE does not is a real behavioural difference and the chapter states the fact without flagging it as unexpected. | Say plainly that unlike most sequence viewers the matcher searches one strand only. |
| What good looks like, "Click the information button at the right of the action bar" | The Provenance popover arrives with no introduction in a section about quality checks. I did not know what a provenance record is or why it is under What good looks like rather than in its own step. | Gloss provenance at first use and move this to the Procedure or its own short section. |
| On the command line, "The command's own help text lists only five of those seven" | The chapter tells me the software's own documentation is wrong. I could not judge whether that means the app is unreliable elsewhere too, and nothing tells me whether this is being fixed. | Mark it as a known defect with a version, so the reader knows it is tracked. |
| On the command line, "`--compress` option currently writes plain text rather than gzip" | Same worry. A second acknowledged bug in the section I was told is optional, with no indication whether the window path has the same problem. | Say whether the equivalent window operation is affected. |
| Next, "see [BLAST Verification](06-blast-verification.md)" | Step 6 told me BLAST is covered here, and only at the very end do I learn there is a whole chapter on it. I would have read that first had I known. | Point to the BLAST chapter from step 6 rather than only from Next. |

The one thing I learned is that an exact-match workflow refuses to guess, and that the reads it cannot place are kept for me to judge rather than forced into a species name.

The one thing I still could not do is run the workflow the way the chapter itself says it must be run, because merging pairs and orienting reads are both required and neither has a dialog I was shown.

The sentence I liked most is "A chimera looks exactly like a novel species until something checks it."
