# Reader report: Running Kraken 2

Reader 1, sophomore fresh from a genetics course, no lab time, never opened a terminal.

| Location | What stopped me | What would have helped |
|---|---|---|
| What it is, "Kraken 2 slides a window along each read..." | I could not picture a "sliding window". I know what a substring is but not what the window is sliding over or how wide it is. | One clause saying the window is a fixed run of consecutive bases that moves one base at a time. |
| What it is, "assigns the read to whichever taxon those matches will support" | This is the whole rule of the tool and it is vague. Which taxon do the matches "support" when they disagree? | State the rule as counting, so the taxon with the most matching minimizers wins. |
| What it is, "The result lands as a timestamped folder" | I did not know what "timestamped" means for a folder name until step 3 showed `kraken2-<timestamp>`. | Show the name shape here instead of three pages later. |
| What it is, "So what should you do with this? Run it as a screen" | "Run it as a screen" stopped me cold. My genetics class used "screen" for a genetic screen, which is not this. | Gloss screen as a first quick pass you follow up rather than trust. |
| Why you would do this, "You have a low-yield run" | I do not know what a low-yield run is. Low yield of what, DNA or reads? | Gloss yield at first use. |
| Why you would do this, "how much of the data is even from" | I do not know what "assembling a genome" involves, so I could not judge whether a week is a lot to risk. | A five-word gloss of assembly. |
| Why you would do this, "86,281 paired-end Illumina read pairs" | I do not know what paired-end means. The chapter explains pairs later in step 2 but I met the term here first. | Gloss paired-end at this first mention, not in the procedure. |
| Why you would do this, "86,281 paired-end Illumina read pairs" | This number is 86,281 but step 3 and two later tables say 85,199 read pairs. I could not tell which is the sample size, and I did not know if I had misread. | Use one number, or say plainly why the count dropped. |
| Before you start, "fetch them from the Sequence Read Archive as accession `SRR36291587`" | I have never fetched anything from the SRA and the link points at a whole other chapter. I did not know if I needed an account. | Say whether an account or login is needed before I click away. |
| Before you start, "install Metagenomics if it is not already there" | I could not tell how to know if it is already there. Nothing says what an installed pack looks like. | One clause naming what the installed state shows on that row. |
| Step 1, "Nine Kraken 2 collections are listed" | I did not know what a "collection" is versus a "database". The chapter uses both and I assumed they were different things for a page. | Say once that a collection is one database. |
| Step 1, "Kraken 2 loads the entire database into RAM" | I do not know how much RAM my Mac has or where to look. The banner supposedly tells me, but only about the recommendation. | Name where the Mac reports its memory. |
| Step 1, "either **Standard-8** or **Standard-16** depending on your memory" | I could not perform this step. Nothing tells me which memory size goes with which choice. | Give the two memory numbers, so 8 GB needs this much and 16 GB that much. |
| Step 1, "whether a newer pinned build exists" | "Pinned build" is not defined anywhere I could find. | Gloss pinned as the version LGE has tested. |
| Step 2, "Picking Kraken2 opens the FASTQ/FASTA Operations dialog" | I read this paragraph three times. It explains the dialog title, then the sidebar, then that Classification is a submenu, and I lost track of what I was meant to click. | Split the click instruction from the explanation of why the window has a different name. |
| Step 2, "LGE groups the selected bundles into samples" | I do not know what a bundle is. It is used from step 2 onward and never glossed, and it is not in the glossary list at the top. | Gloss bundle at first use. |
| Step 2, "The three-way sensitivity control is the simple version of two numbers" | I did not know what the two numbers were until the Settings section, so I had to leave the procedure to understand the step. | Name the two numbers here in three words. |
| Step 2, "The selection runs as one classification batch that registers a single entry" | This sentence carries five ideas and I had to read it twice to find the one that mattered to me. | Split it. |
| Step 3, "On the reference run the Viral database finished in 2.6 seconds" | I could not judge whether my own run being far slower means something is wrong. | Say what range is normal on a laptop for a first run. |
| Step 3, "a database already sitting in the operating system's file cache" | I do not know what a file cache is. | Gloss it, or drop it and just say a first run is slower. |
| Step 4, "The action bar's **Extract FASTQ** button" | I did not know what the action bar is or where it sits. It appears again under Reading the results with no more explanation. | Say once where the action bar is on screen. |
| Settings, "Each entry ends with a short sentence naming the command-line flag" | I have never opened a terminal, so every entry ends with a sentence I cannot use, and I could not tell whether I was allowed to skip them. | Say plainly that readers who stay in the app can ignore those closing sentences. |
| Settings, "Confidence:. Sets how much of a read must match" | I do not know how to pick a value. It says raise it when the report is full of implausible species, but I would not know an implausible species from a real one. | Give one worked example of a report that needs a raise. |
| Settings, "**Min hit groups:.** ... one long chance match is much easier to come by" | I could not picture "separate stretches of a read". Separated by how much? | One clause saying the stretches must not touch. |
| Settings, "Threads:. ... will not let you exceed your Mac's core count" | I do not know my core count or whether more threads could hurt anything besides responsiveness. | Say whether raising it risks anything. |
| Settings, "**Bracken.** ... You do not control this one" | I was confused about why a thing I cannot control is listed as a setting. | Label it as a column rather than a setting. |
| Reading the results, "The table's columns are Sample, Taxon Name, Rank, Reads, Direct, Bracken, and a percent column" | Seven columns are named but the example table below shows five with different names, so I could not line them up. | Match the example table's headings to the real ones. |
| Reading the results, "| Viruses | domain | 83,728 | 65 |" | I could not work out where 98.27 percent comes from. 83,728 out of 85,199 read pairs is 98.27, but the counts are labelled reads and the sample size is in pairs, so I did not know if a pair counts as one or two. | State once whether these counts are reads or pairs. |
| Reading the results, "named *Betacoronavirus pandemicum*, which is the formal species name" | The table calls SARS-CoV-2 a subspecies below a species. My genetics course never mentioned a virus subspecies and I could not tell if that was a typo. | One clause saying viral taxonomy uses this rank. |
| Reading the results, "Kraken 2 reports 1,471 reads, or 1.73%, as unclassified" | Unclassified is the most important number in the chapter and it is not a row in the table, so I nearly missed it. | Put it in the table with a note. |
| Reading the results, "Escape zooms out one level and Cmd-0 jumps straight back to the root" | I could not tell whether Escape closes the viewport instead, and I was afraid to try. | Say Escape does not close the window. |
| The same reads a different database, "Species reported | 1 | 1" | Standard-16 named only 3.90 percent of reads yet reported the same one species. I expected a broad database to report more species, and the text does not address this row at all. | One sentence on why the species count did not change. |
| The same reads a different database, "squeezed from a seventy-gigabyte collection down to sixteen by throwing away most of its reference fragments" | I do not know what a reference fragment is or how a database can throw away most of itself and still work. | Gloss reference fragment. |
| What the Sensitivity preset changes, "Sensitive | 85,181 (99.98%) | 18" | Sensitive classified 85,181 reads, which is more than the 83,728 Balanced found, but Balanced covers 98.27 percent while the totals differ. I could not check the arithmetic because I still did not know the denominator. | Print the total once above the table. |
| What the Sensitivity preset changes, "those are chance k-mer matches rather than organisms in the tube" | I could not tell how the author knew these three were fake and phiX was real. It reads like inside knowledge I do not have. | Say the reasoning is that phiX is a known added control. |
| What good looks like, "A few thousand is worth investigating" | A handful is noise and a few thousand is worth investigating, but my report will mostly be numbers in between and the text says I will not know. That left me with no rule I could apply. | Name one threshold as a starting default. |
| What good looks like, "It is not a BLAST e-value, which asks how unlikely" | I have not met an e-value, so a comparison to it explained nothing. | Explain confidence without the comparison. |
| On the command line, "```bash lungfish-cli conda classify \" | I have never opened a terminal and could not tell whether this section was optional for me. | One line saying the section is optional. |
| Next, "go to [BLAST Verification](06-blast-verification.md)" | Three sections told me to verify a hit with BLAST, but BLAST is never explained here, only linked. I finished the chapter unable to check any hit. | One sentence saying what BLAST does. |

The one thing I learned: a read count sitting high in the tree with a Direct figure of zero is a good result, not a vague one, because it means every read got resolved to something more specific below.

The one thing I still could not do: choose between Standard-8 and Standard-16, because the chapter tells me to decide on my memory and never says how much memory each one needs or where to find my own.

The sentence I liked most: "It is the classifier telling you where its knowledge ran out."
