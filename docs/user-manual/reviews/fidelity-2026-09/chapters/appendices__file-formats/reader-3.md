# Reader 3 report, File Formats

Persona. Pre-med student, English is my second language. I know biology
words from textbooks. I have never opened a terminal. I was handed a file
and I came here to find out what it is.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "ordinary folders that macOS shows as a single icon" | I did not understand what this means until much later. A folder that looks like one icon is a strange idea and nothing here tells me why I would care yet. | Add the Finder consequence right here, that double-clicking it opens LGE instead of showing the files. |
| What it is, "This appendix is a lookup table rather than a procedure" | "Lookup table" is a figure of speech here. The chapter is not a table. I read the sentence twice trying to find the table. | Say it is for looking things up one section at a time and is not meant to be read in order. |
| What it is, "how to look inside it from the Terminal application" | The chapter promises Terminal commands but never says how to open Terminal, or what directory I must be in for the commands to work. I could not perform a single command in this chapter. | Add one sentence naming Applications then Utilities then Terminal, and say to drag the project folder onto the window first. |
| What it is, "the demo project at `~/Desktop/lge-docs/...`" | The `~` symbol is not explained. I do not know if I type it or replace it with something. | Gloss `~` as your home folder at first use. |
| The format registry, "assigns a category that decides where the format appears in a file picker" | Long sentence with four verbs in a chain. I had to read it twice to see that one entry does all four things. | Split after the extensions clause. |
| The format registry, "an internal LGE routine can produce directly" | "Routine" is a programming word I do not know in this sense. | Say a part of LGE itself rather than routine. |
| The format registry, "still carry a write flag of false in the registry" | The word "flag" here means a yes or no value, not something you raise. The table uses Yes and No, so the word "false" does not match the table I am looking at. | Use "No" to match the table column. |
| The format registry, "The read and write flags matter more than they look" | This is an idiom. I understood it only from the sentences after. | Say the two columns are easy to misread and explain why. |
| Format registry table, "FASTA index `.fai` Category Index" | The text above says there are five categories plus two more, so seven. The table shows Index as an eighth. I could not reconcile the count. | Name Index in the category list, or say the count covers only file-picker categories. |
| Format registry table, "BAM ... LGE writes No" | I cannot judge this. Earlier the chapter says every alignment reaching a viewport is a sorted BAM. So LGE produces BAM files but the table says No. The explanation is three sections away. | Add a footnote under the table pointing to the alignment section. |
| The format registry, "Three more formats have registry identifiers without a full descriptor entry" | "Registry identifier" and "full descriptor entry" are both new terms in one sentence, and neither is defined. I could not tell what the practical difference is for me. | Say it in effect terms, that LGE knows the file type but will not draw it. |
| The format registry, "so that one reaches the app through a real route" | "Real route" is vague. I do not know what an unreal route would be. | Say EMBL files can actually be imported, unlike the other two. |
| Standard sequence formats, "the human chromosome 20 slice" | "Slice" is a metaphor. I guessed it means a piece of the chromosome, but the name `chr20_10.0-10.5Mb` is never explained either. | Gloss the name once, that it is bases 10.0 to 10.5 million of chromosome 20. |
| Standard sequence formats, "truncated to fit the page" | I did not know if the file itself is short or if lines were cut. It appears three times and I was unsure each time. | Say the lines were shortened for printing and the real lines are longer. |
| Standard sequence formats, "They mark repeat-masked regions" | "Repeat-masked" is explained, but I still do not know whether I should worry about them in my own analysis. | Add one sentence saying you can ignore them for normal work. |
| Standard sequence formats, "`chr20_10.0-10.5Mb 500001 19 50 51`" | The four columns are named but the example has five numbers plus a name. I counted and could not match "four-column" to what I saw. | Say five columns, or say four columns after the name. |
| Standard sequence formats, "the byte offset at which its sequence starts" | "Byte offset" is not explained. I do not know what a byte is in this context or why 19. | Gloss byte offset as how many characters into the file. |
| Standard sequence formats, "Run `samtools faidx` on a FASTA to create one" | I could not do this. There is no full example command, no file name, and no statement of whether samtools is already installed with LGE. | Give one complete command line and say where samtools comes from. |
| Standard sequence formats, "30 means roughly a one in a thousand chance of being wrong" | I can read the number but I cannot judge my own file. The example quality line is full of `1` and `<` characters and nothing connects those characters to a score. | Add one sentence saying which characters are good and which are poor. |
| Standard sequence formats, "recognizes five compression suffixes" | Five suffixes are listed but nothing says what any of them are or that they are all just compressed files. `.zst` I have never seen. | Say all five are compression formats and LGE handles them the same way. |
| Standard annotation formats, "a semicolon-separated list of attributes" | Nine columns are named in one very long sentence. I lost my place around column six and had to restart. | Turn the nine columns into a short list. |
| Standard annotation formats, "a reading-frame phase" | Not explained. I know reading frame from genetics but not what "phase" means as a column value. | Gloss phase in half a sentence or say most readers can ignore it. |
| Standard annotation formats, "`_lf_raw_genbank_location=52070..53062`" | The example line runs off the width and I could not tell where one attribute ended and the next began without counting semicolons. | Note that semicolons separate the attributes in this example. |
| Standard annotation formats, "so a spliced feature keeps its exact definition" | "Spliced feature" assumes I connect RNA splicing to a location string. I could not picture what would be lost. | Say a gene made of several separate pieces. |
| Standard annotation formats, "the fifth gives its pool" | "Pool" for primers is a lab term I have not met. The example shows `1` and I cannot judge it. | Gloss pool as which of two primer mixes the primer belongs to. |
| Standard annotation formats, "the shipped QIAseq scheme" | "Shipped" is used here and in several later places to mean included with the app. I read it as sent to me. | Use "included with LGE". |
| Standard annotation formats, "this is a common source of off-by-one confusion" | "Off-by-one" is jargon. The two conventions are then explained well, but the label came first and confused me. | Say the two formats count the first base differently. |
| Standard annotation formats, "half-open" | Explained in the same sentence, but the term itself never becomes useful and adds a second unfamiliar word to a sentence already carrying "zero-based". | Drop the term and keep only the explanation. |
| Standard alignment formats, "stores only the differences from the reference" | The consequence is stated, that CRAM needs the exact reference, but not the risk. I could not judge whether I should avoid CRAM. | Add one sentence saying a CRAM without its reference cannot be read at all. |
| Standard alignment formats, "runs any tool that emits SAM through `samtools sort`" | "Emits" is an uncommon verb for me and "runs X through Y" is an idiom. I read it twice. | Say tools that produce SAM, and that LGE converts it. |
| Standard alignment formats, "roughly 512-megabase limit a `.bai` can address" | I cannot judge this number against my own data. Is human chromosome 1 over or under it? | Say which real chromosomes exceed it, or that no human chromosome does. |
| Standard alignment formats, "```bash samtools idxstats "LGE Manual Demo.lungfish/..."```" | I could not run this. The path is relative to something the chapter never names, and I do not know what folder my Terminal starts in. | Say to run it from the folder containing the project. |
| Standard alignment formats, "90,990 of 91,203 reads mapped ... what a healthy targeted mapping run looks like" | I cannot judge a different number. 99.8 percent is called healthy but no threshold is given, so with 85 percent I would not know. | Give a rough percentage floor for concern. |
| Standard alignment formats, "`0 + 0 secondary` `55 + 0 supplementary`" | Five output lines are shown and none of the terms primary, secondary, supplementary, or duplicates is explained. The `+ 0` pattern is also unexplained. | Gloss the plus form once and say which lines a beginner should read. |
| Standard variant formats, "declare the file version, the reference contigs" | "Contig" appears here without explanation and is not glossed anywhere in the chapter. | Gloss contig at this first use. |
| Standard variant formats, "`platforms=5;platformnames=Illumina,PacBio,10X,CG,Solid`" | The nine column names are given in the block but the INFO field content is never explained, and I did not know if I needed to understand it. | Say the INFO content differs per file and can be skipped. |
| Standard variant formats, "`QUAL 50`" and later "`225.417`" | Two different quality numbers in two examples with no scale. I cannot judge either. | Say quality scales differ between callers so the numbers are not comparable. |
| Standard variant formats, "bgzip is a block-wise form of gzip that stays randomly accessible" | "Block-wise" and "randomly accessible" are both new. I understood gzip only. The whole sentence carries three definitions at once. | Split into two sentences and say it can be read from the middle. |
| Standard variant formats, "a `.db` SQLite sidecar" | "SQLite" and "sidecar" both appear before either is explained. Sidecar is a metaphor from a motorcycle and did not help me. | Gloss sidecar as a helper file stored next to the main one, at first use. |
| Standard variant formats, "`vc-6edd1356-e900-470c-95a3-eb1e4b5ffcfd`" | I could not tell if I must type this exact string or if mine will differ. It appears in a command I might copy. | Say the identifier is generated and yours will be different. |
| Standard variant formats, "`lungfish-cli bundle create --variant` writes a `.bcf`" | The chapter has not yet said what `lungfish-cli` is or where it lives. It is used as if I already know. | Introduce lungfish-cli in the opening section. |
| Standard tree format, "`(Human_NC_012920.1:0.0601416264,Chimp...` " | The whole tree is one long unbroken line. I could not follow the parentheses on screen even after the instruction to read the innermost first. | Show the same tree indented over several lines beside the raw form. |
| Standard tree format, "The branch lengths are substitutions per site" | The unit is given and the example converted to 3 per hundred, which helped, but I cannot judge whether 0.898 on the other branch is large. | Say plainly that a longer number means more change. |
| What an LGE bundle is, "Read the manifest rather than assuming a common structure" | This tells me what not to do but not how to read a JSON file. The `python3 -m json.tool` command appears only in the last section. | Point forward to the command that prints a manifest. |
| What an LGE bundle is, "snake_case keys" and "camelCase keys" | Neither term is explained. I guessed from the examples but was not sure it mattered. | Gloss both once as two naming styles, and say why it matters when reading a manifest. |
| What an LGE bundle is, "does not mean the bundle is unsupported" | Double negative in a sentence that already has a "so" clause. I read it twice. | State it positively. |
| Bundle table, "`.lungfishgenotype` ONT genotype calls" | "ONT" is never expanded. | Expand to Oxford Nanopore at first use. |
| Bundle table, "`.lungfish12s` A 12S metabarcoding run's species table" | "12S" and "metabarcoding" both appear unexplained. I know ribosomal genes but not this use. | Gloss 12S once as a mitochondrial gene region used to identify species. |
| Bundle table, "`.lungfishtax` A CZ ID taxon report" | CZ ID is not explained anywhere in the chapter. | Say it is an external web service for pathogen sequencing. |
| The reference bundle, "which is why an assembly appears in every reference picker without any conversion step" | Very long sentence covering creation, location, and consequence. I lost the subject. | Split before "An assembly is". |
| The reference bundle, "`sequence.fa.gz.gzi`" | The `.gzi` is explained as addressing compressed blocks, but I do not know what a block is. It builds on the bgzip sentence two sections earlier. | Repeat the one-line gloss here since the chapter says sections stand alone. |
| The reference bundle, "The `annotations/` and `tracks/` folders exist even when empty" | I could not judge whether an empty folder means something went wrong. The sentence says it is normal but does not say so directly. | Say an empty folder is expected and not an error. |
| The reference bundle, "`"total_length": 81706`" | A JSON fragment is shown but nothing says what JSON is or how I would open the file to see it. | Gloss JSON once as a text format for structured data. |
| The reference bundle, "`lungfish-cli bundle info "LGE Manual Demo.lungfish/..."`" | I could not run this. Nothing tells me where the `lungfish-cli` program is or whether it is on my computer after installing LGE. | State where the CLI comes from and how to make the terminal find it. |
| The read dataset bundle, "SRA or ENA" | Two abbreviations, neither expanded. | Expand both at first use. |
| The read dataset bundle, "the meta sidecar records `pairingMode: interleaved`" | I do not know how to see this key. The chapter never shows me how to open a `.json` file until the very last code block. | Point to the JSON command here. |
| The read dataset bundle, "Whatever the Pairing setting said about the files you handed in" | "Handed in" is an idiom and made me think of homework. The sentence also assumes I have seen a Pairing setting in a dialog. | Say the files you imported. |
| The read dataset bundle, "`n50ReadLength`" | Nine statistics keys are listed and only this one is a term I cannot guess. N50 is never explained. | Gloss N50 in half a sentence. |
| The read dataset bundle, "`"gcContent": 0.444`" | I know GC content but cannot judge 0.444 for mitochondrial reads, and `meanQuality` 26.52 is below the 30 mentioned earlier with no comment. | Say whether 26.52 is acceptable, since 30 was the earlier reference point. |
| The read dataset bundle, "Some read bundles are virtual, meaning they hold no reads at all" | The idea is startling and the explanation follows, but "a subset, a trim, or a demultiplex" uses three verbs as nouns in a row. I read it twice. | Say bundles produced by subsetting, trimming, or demultiplexing. |
| The read dataset bundle, "`lungfish-cli fastq materialize ...`" | "Materialize" is used as a verb with no gloss. I guessed it means write the real file. | Gloss materialize at first use. |
| The read dataset bundle, "aligned to the PHA4GE specification for pathogen sample descriptions" | PHA4GE is not expanded and I cannot tell whether this affects what I must fill in. | Expand it and say whether the user has to do anything. |
| The alignment and tree bundles, "positions descended from the same ancestral base line up in the same column" | This is a good definition but it is inside a sentence that also defines the bundle and names the folder. Three ideas, one sentence. | Split the definition into its own sentence. |
| The alignment and tree bundles, "5,053 variable sites, and 2,709 parsimony-informative sites" | "Parsimony-informative" is not explained and I cannot judge either number. | Gloss the term or drop it from the reader-facing sentence. |
| The alignment and tree bundles, "`source.original`" | This filename has no extension and is never explained in either bundle listing. | Say in one clause what it holds. |
| The alignment and tree bundles, "`isRooted` false" | Rooted and unrooted trees are not explained, and the earlier tree section did not mention rooting. | Gloss rooted once, since it changes how the tree is read. |
| The primer scheme bundle, "The shipped schemes have a minimal shape" | "Minimal shape" is vague. I could not tell what is missing compared with a full one until the next paragraph. | Say the included schemes carry only three files. |
| The primer scheme bundle, "the filenames above are fixed by convention rather than declared" | I read this three times. "Declared" refers back to the file map idea which was mentioned once, sections earlier. | Say the manifest does not list the filenames, so they must always be named exactly as shown. |
| The primer scheme bundle, "four ARTIC versions covering nCoV-2019 V3 through SARS-CoV-2 V5.3.2" | The claim is eight schemes but I count one plus four plus one plus two, which is eight only if "the two NEB VarSkip schemes" is exactly two. The ARTIC four are not named individually. | Name all eight in a short list. |
| The result bundles, "the Williams MiSeq genotyping project" | This project has not been introduced. Every other example came from the demo project, so I did not know if I have this one. | Say it is a separate project not included with the manual. |
| The result bundles, "the stderr logs of every tool the run invoked" | "stderr" is a terminal word I do not know. | Say error logs. |
| The result bundles, "the import sheet's Project Destination readout names a path under `Analyses` that nothing writes to" | This tells me the app displays something wrong. I read it twice because the sentence is long and the correction comes at the end. | Lead with the instruction, that the displayed path is wrong and the result is in Classifications. |
| The result bundles, "Haplotype definitions are files inside that folder with the suffix `.lungfishhaplotypedef.json`, not a bundle format of their own" | "Haplotype" is used three times in this paragraph without a gloss, and I only half remember it from genetics. | Gloss haplotype at first use in the chapter. |
| The workflow bundles, "the graph of steps you assembled in the Workflow Builder" | "Graph" here does not mean a chart. That meaning tripped me. | Say the connected sequence of steps. |
| The workflow bundles, "a `manifest.json` declaring its name, version, engine, inputs, and outputs" | "Engine" is unexplained, and Nextflow is given as an example of a pipeline file without being explained either. | Gloss engine as the pipeline program that runs the file. |
| The workflow bundles, "A run started from a bare graph JSON file rather than from a saved bundle" | "Bare" is doing a lot of work here and I could not tell how such a run would start. | Say a graph file that was never saved as a bundle. |
| Provenance sidecars, "The keys are not the ones a general-purpose provenance format would use" | I do not know any provenance format, so this comparison told me nothing and cost me a reread. | Drop the comparison and just say here is the shape. |
| Provenance sidecars, "`"hostOS": "macOS 26.6.2 (arm64)"`" | The JSON block shows seven keys but the sentence lists ten. I looked for `parameters` and `steps` in the block and could not find them. | Say the block is shortened and the last three keys are described below. |
| Provenance sidecars, "`minimumDepth` as `caller-default`" | I cannot judge this. I do not know what minimum depth does, only that it was not overridden. | Say in half a sentence that the tool chose the value. |
| Provenance sidecars, "so the checksum and byte size claim holds for both directions" | I read this three times and still am not sure what claim, or what the two directions are. | Rewrite as one plain statement about inputs and outputs both being recorded. |
| Provenance sidecars, "There is no `schema_version` key in a provenance sidecar" | This is written as a correction to an expectation I never had. It made me wonder what I had missed. | State it positively or move it to a note. |
| Sharing and inspecting bundles, "`zip -r HBB.lungfishref.zip HBB.lungfishref`" | I could not perform this. `-r` is not explained and again I do not know what folder to be in. | Say the flag includes everything inside, and where to run it. |
| Sharing and inspecting bundles, "a deterministic OCI layout tarball" | Three unfamiliar words together. "Deterministic" is explained later in the paragraph but "tarball" and "OCI" are not, and container registries are outside anything I know. | Gloss tarball as a single archive file and say who this format is for. |
| Sharing and inspecting bundles, "each blob addressed by its SHA-256 digest" | "Blob" and "digest" are new, and the checksum definition appeared in the previous section using different words. | Reuse the word checksum here instead of digest. |
| Sharing and inspecting bundles, "In Preview 2026.9.13 that command cannot actually be run" | I had just read a full paragraph explaining a command that does not work. I felt my time was wasted and read the paragraph again to see if I misunderstood. | Put the warning before the description. |
| Sharing and inspecting bundles, "`bcftools view -H chr20_10.0-10.5Mb.lungfishref/variants/vc-6edd1356.vcf.gz`" | This filename is shorter than the one used earlier in the chapter for the same file. I could not tell which is right. | Make the two identical. |
| Sharing and inspecting bundles, "`python3 -m json.tool ...`" | This is the only way the chapter shows for reading a manifest, but manifests are discussed in six earlier sections. Also nothing says python3 is present on a Mac. | Move this command up to the bundle section and say python3 ships with macOS. |

The one thing I learned. A bundle is only a folder, so ordinary tools can
open what is inside it without any special step.

The one thing I still could not do. Run any command in this chapter, because
nothing tells me how to open Terminal, where `lungfish-cli` lives, or which
folder to be in.

The sentence I liked most. "Finding a `preview.fastq` where you expected a
full file is correct rather than a truncated result."
