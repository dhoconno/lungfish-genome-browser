# Reader 1 report — File Formats

Persona: sophomore, one genetics course, no lab time, never opened a terminal.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| What it is — "such as FASTA for sequences and BAM for alignments" | FASTA and BAM are named as if I already know them. This is the first sentence of an appendix I would jump into cold. | Add half a clause saying each is defined in its own section below. |
| What it is — "how to look inside it from the Terminal application" | The chapter tells me Terminal is where I type commands but never how to open it, and I have never opened one. | One sentence saying Terminal is in Applications then Utilities. |
| What it is — "read out of the manual's fixtures and out of the demo project" | I do not know what a fixture is here, and I do not know whether the demo project exists on my machine or how it got to my Desktop. | Gloss fixture once and say where the demo project comes from. |
| The format registry — "assigns a category that decides where the format appears in a file picker" | I could not tell what a file picker is inside LGE or why category placement matters to me. | Say that the category groups the format in the Open dialog. |
| The format registry — "an internal LGE routine can produce directly" | Routine is used as a programming word. I read this twice before deciding it meant a piece of LGE's own code. | Replace routine with a plainer word such as part of LGE. |
| The format registry — "taken from `FormatRegistry+BuiltInDescriptors.swift`" | A source filename appears with no explanation. I would not know where that file is or whether I am supposed to open it. | Say this is a source file inside the app, not something the reader opens. |
| The format registry table — "`.faa`, `.ffn`, `.frn`" | Six extra FASTA extensions appear with no hint of which ones I would actually meet. Protein versus nucleotide is not mentioned anywhere. | Note in a clause that `.faa` holds protein and the rest hold nucleotides. |
| The format registry table — "BAM ... LGE writes: No" | This flatly contradicts the alignment section three pages later, which says every alignment reaching a viewport is a sorted BAM LGE produced. I could not reconcile the two. | Add a footnote to the BAM row pointing at the samtools explanation. |
| The format registry — "Detection only" | Detection only appears in the table before the paragraph that explains it, and the paragraph is above the table. I scrolled back and forth. | Repeat the one-line meaning directly under the table. |
| The format registry — "convert it to bedGraph outside LGE first" | I am told to convert but not with what. I would have no idea where to start. | Name the tool, such as bigWigToBedGraph. |
| The format registry — "registry identifiers without a full descriptor entry" | Descriptor entry is new here and never defined. I could not work out what having an identifier but no descriptor means for me in practice. | Say plainly that LGE names the file type but cannot open it. |
| Standard sequence formats — "the human chromosome 20 slice from the `hg002-chr20` fixture" | I do not know what HG002 is. It recurs through the whole chapter as if it were common knowledge. | One clause saying HG002 is a widely used reference human sample. |
| Standard sequence formats — "stretches a repeat-finding program flagged as repetitive" | I understood the definition but could not judge whether seeing lowercase means something is wrong with my file. | Say directly that lowercase is normal and needs no action. |
| Standard sequence formats — "the byte offset at which its sequence starts" | Byte offset assumes I know what a byte position in a file is. I have never thought of a file as numbered bytes. | Gloss byte offset as how many characters into the file the sequence begins. |
| Standard sequence formats — "Run `samtools faidx` on a FASTA to create one." | This is the first command in the chapter and there is no example of the full line, unlike every later command. The chapter also never says samtools is a separate program I might not have. | Show the full command with a filename, as elsewhere. |
| Standard sequence formats — "30 means roughly a one in a thousand chance" | I was given one anchor point and then shown a quality line of `0<0DD=1<DGHHE` that I cannot map to numbers at all. I could not judge whether that read is good. | Say that the example line's characters are mostly low and what that means. |
| Standard sequence formats — "recognizes five compression suffixes, which are `.gz`, `.bgz`, `.bz2`, `.xz`, and `.zst`" | Five suffixes appear and only `.gz` is familiar. I could not tell whether I should prefer one. | Note that `.gz` is the common case and the rest are accepted. |
| Standard annotation formats — "a reading-frame phase" | Phase is named but not explained, and my genetics course used reading frame only for translation. | Gloss phase as which base starts the codon, or say it is usually a dot. |
| Standard annotation formats — "`_lf_raw_genbank_location=52070..53062;db_xref=GeneID:103344929`" | The example line is wider than the page and wraps mid-attribute, so I could not tell where one attribute ended and the next began on first read. | Truncate the attribute column as the other examples are truncated. |
| Standard annotation formats — "so a spliced feature keeps its exact definition" | Spliced feature is used without a gloss and I could not tell why a start and an end would not be enough. | Add a clause saying a spliced feature has gaps a single start and end cannot record. |
| Standard annotation formats — "the fifth gives its pool" | Pool is used as a primer term I have never met. | Gloss pool as the reaction the primer is mixed into. |
| Standard annotation formats — "MN908947.3" in the primer example | The chapter's other examples are human and macaque. A viral accession appears here with no word saying what organism it is. | Name it as the SARS-CoV-2 reference in the lead-in sentence. |
| Standard annotation formats — "BED and bedGraph are zero-based and half-open" | I read this paragraph three times. The definitions are correct but abstract, and I could not check my understanding against anything. | Add one worked example, such as the first ten bases written both ways. |
| Standard alignment formats — "stores only the differences from the reference" | I understood the idea but not the consequence stated next, that CRAM needs that exact reference. It felt like a trap I would only learn about after losing data. | Add a clause warning that a CRAM without its reference cannot be read. |
| Standard alignment formats — "runs any tool that emits SAM through `samtools sort` and `samtools index`" | Sort and index are named as steps but I do not know what sorting an alignment means or why it must come before indexing. | Say sorting puts reads in coordinate order so an index can be built. |
| Standard alignment formats — "the roughly 512-megabase limit a `.bai` can address" | I have no idea whether any chromosome I would use is over 512 megabases, so I could not judge whether this ever affects me. | Say that no human chromosome exceeds it, so `.bai` is the normal case. |
| Standard alignment formats — the two samtools commands | I am told to run these but not from where. The path starts at `LGE Manual Demo.lungfish` with no parent, and I have never used `cd`. | Say to run them from the folder containing the project. |
| Standard alignment formats — "the number of mapped reads, and the number of unmapped reads" | The output line has five fields including a row starting with `*` that is never explained. | One clause saying the `*` row counts reads that matched nothing. |
| Standard alignment formats — "91,203 reads mapped ... which is what a healthy targeted mapping run looks like" | I was given one healthy example and one failure example, but no threshold. I could not judge a run at 60 percent. | Give a rough percentage band that counts as healthy. |
| Standard alignment formats — "0 + 0 secondary / 55 + 0 supplementary" | Five flagstat lines are quoted and only the total is discussed. Secondary, supplementary, duplicates, and the `+ 0` form all went unexplained. | Say what the `+ 0` means and that the other lines are safe to ignore here. |
| Standard variant formats — "declare the file version, the reference contigs" | Contig appears here for the first time and is never defined in this chapter. | Gloss contig as one continuous stretch of reference sequence. |
| Standard variant formats — "`50 PASS platforms=5;platformnames=Illumina,PacBio,10X,CG,Solid`" | Ten column names are listed in the header row and none of them are explained. QUAL of 50 in one example and 225.417 in another gave me no way to judge either. | Say what QUAL measures and that its scale differs between callers. |
| Standard variant formats — "LGE reads VCF versions 4.0 through 4.4" | I would not know how to find out which version my file is. | Say the version is on the first `##fileformat` line. |
| Standard variant formats — "`DP=62;VDB=0.240996;SGB=-0.693147;MQSBZ=0`" | Four abbreviations appear in the example and none are expanded. DP is the one I would most want. | Expand DP as read depth at that position. |
| Standard variant formats — "a `.db` SQLite sidecar" | SQLite and sidecar both arrive unglossed. Sidecar is later defined only for provenance. | Gloss sidecar once here as a companion file. |
| Standard variant formats — "`lungfish-cli bundle create --variant` writes a `.bcf`" | I could not tell whether this difference will break something for me later or is just trivia. | Say whether a `.bcf` track still opens in the Variants tab. |
| Standard tree format — "The branch lengths are substitutions per site, so the 0.0299 ... means about 3 changes per hundred" | The arithmetic is 0.0299 giving about 3 per hundred, which I followed, but the branch quoted as 0.8982 is far larger and never mentioned. That one looked like an error I could not assess. | Say a word about the long 0.898 branch so the reader is not left wondering. |
| Standard tree format — "the human and chimpanzee sit outside that group" | This tree puts human and chimp outside a group containing gorilla, which contradicts what I was taught. The next paragraph says `isRooted` false, but that is 180 lines later. | Say here that the tree is unrooted so the ordering is not an ancestry claim. |
| What an LGE bundle is — "right-click it and choose **Show Package Contents**" | This is the only click instruction in the chapter and I could not tell whether it is required before the Terminal commands work. | Say the Terminal commands work without doing this. |
| What an LGE bundle is — "snake_case keys such as `format_version` ... camelCase keys such as `schemaVersion`" | Snake case and camel case are named as if standard vocabulary. I had to infer them from the examples. | Gloss both once, or just say underscores versus capitals. |
| What an LGE bundle is — "There is no single key that every manifest carries." | I was told the manifests are inconsistent but not what to do about it. This paragraph left me anxious rather than informed. | Say the practical rule, which is to open the manifest and read it. |
| The bundle table — "ONT genotype calls" | ONT appears only as an abbreviation in the table and is expanded nowhere in the chapter. | Expand ONT at first use as Oxford Nanopore. |
| The bundle table — "A CZ ID taxon report normalized into LGE's classifier schema" | CZ ID, taxon report, normalized, and classifier schema all arrive in one row. I understood none of it. | Gloss CZ ID as an outside classification service in the row's own section. |
| The reference bundle — "`vc-6edd1356-e900-470c-95a3-eb1e4b5ffcfd.vcf.gz`" | The long identifier repeats four times in the tree and twice more in commands, and I could not tell whether I would have to type it. | Say the identifier is generated and can be copied from the Finder. |
| The reference bundle — "the `.gzi` addresses compressed blocks" | Compressed block is not explained until the variant section far below, where bgzip is defined. | Move or repeat the bgzip gloss here at first use. |
| The reference bundle — "`lungfish-cli bundle info`" | This is the first `lungfish-cli` command in the chapter and nothing has said what `lungfish-cli` is or whether it is installed. | One sentence saying `lungfish-cli` ships with LGE and where it lives. |
| The reference bundle — the chromosome table output | The columns Primary and Mitochondrial appear in output with no explanation of what marking a chromosome primary means. | One clause on what Primary flags. |
| The read dataset bundle — "`HG002-chrM.fastq.gz.lungfish-meta.json.lungfish-provenance.json`" | A filename with three stacked suffixes. I read it twice and still was not sure it was not a typo. | Note that provenance names are built by appending, so doubled suffixes are expected. |
| The read dataset bundle — "the meta sidecar records `pairingMode: interleaved`" | Paired-end is used without a gloss, and the sentence after it says my Pairing setting is overridden, which sounded like my choice was ignored. | Gloss paired-end and say the setting still controls how the files are read in. |
| The read dataset bundle — "`n50ReadLength`" | N50 is a term I have never met and it is not glossed here or anywhere in the chapter. | Gloss N50 in one clause. |
| The read dataset bundle — "`gcContent`: 0.444, `meanQuality`: 26.52" | Real numbers are shown but nothing says whether 0.444 or 26.52 is good. Mean quality 26.52 sits below the 30 anchor given earlier, and I could not tell if that is a problem. | Say what range each value normally falls in. |
| The read dataset bundle — "To write the full reads out, materialize the bundle." | Materialize is used as a verb before it means anything to me, and the command uses a bare bundle name with no path when every other command in the chapter uses a full path. | Gloss materialize and show the command with a path. |
| The read dataset bundle — "aligned to the PHA4GE specification" | PHA4GE is an acronym with no expansion. | Expand it once. |
| The alignment and tree bundles — "5,053 variable sites, and 2,709 parsimony-informative sites" | Variable site I could guess. Parsimony-informative I could not, and I have no sense of whether 2,709 out of 17,247 is a lot. | Gloss parsimony-informative and say whether that ratio is typical. |
| The alignment and tree bundles — "`source.original`" | A file with no extension appears in two bundle listings and is never described. | One clause saying what `source.original` holds. |
| The alignment and tree bundles — "a `checksums` map" | Checksum is used here but only defined much later, in the provenance section. | Move the checksum gloss to its first use. |
| The result bundles — "the stderr logs of every tool the run invoked" | Stderr is a terminal word I have never seen. | Say error logs instead, or gloss stderr. |
| The result bundles — "an XLSX workbook named for the run" | XLSX is not glossed. I guessed Excel but the chapter never says so. | Say it is an Excel file. |
| The result bundles — "the import sheet's Project Destination readout names a path under `Analyses` that nothing writes to" | This tells me the app displays a wrong path. I could not tell whether my import failed or whether the display is simply cosmetic. | Say plainly that the import succeeds and only the label is wrong. |
| The workflow bundles — "the graph of steps you assembled in the Workflow Builder" | Graph here means a flow of steps, not a plot. I read it as a chart first. | Say a diagram of connected steps. |
| The workflow bundles — "such as a Nextflow script" | Nextflow appears with no explanation of what it is. | Gloss Nextflow as an outside pipeline system. |
| Provenance sidecars — "`minimumDepth` as `caller-default`" | I could not tell what depth value was actually used, only that I did not set one. That felt like the record does not tell me what happened. | Say how to find the tool's own default. |
| Provenance sidecars — "so the checksum and byte size claim holds for both directions" | I read this sentence three times. Both directions is never explained and I still do not know what claim is being defended. | Rewrite to name what inputs and outputs each get recorded with. |
| Provenance sidecars — "There is no `schema_version` key in a provenance sidecar." | I was not looking for one and did not know I should. This felt written for someone else. | Cut or move to a note for tool builders. |
| Sharing and inspecting bundles — "`zip -r HBB.lungfishref.zip HBB.lungfishref`" | My first real command to type. I do not know what `-r` does or where to run this. | Say `-r` includes the folder's contents, and name the working folder. |
| Sharing and inspecting bundles — "a deterministic OCI layout tarball ... a config blob, a manifest blob, and a single layer blob" | Six unfamiliar words in two sentences. I gave up on this paragraph, then found the next paragraph says the command does not work anyway. | Cut the detail, or put the broken-command warning first. |
| Sharing and inspecting bundles — "In Preview 2026.9.13 that command cannot actually be run." | The paragraph explaining the broken command is longer than the workaround. I nearly missed the one line telling me to use zip. | Lead with the workaround, then explain the defect. |
| Sharing and inspecting bundles — "`python3 -m json.tool HBB.lungfishref/manifest.json`" | Python appears in the last code block with no warning that it is a separate language, and the third command uses a shortened `vc-6edd1356.vcf.gz` that does not match the full identifier used earlier. | Use the full filename and say `python3` comes with macOS. |

One thing I learned: a bundle is just a folder that Finder draws as one icon, so ordinary tools still work on the files inside it.

One thing I still could not do: judge whether any of the real numbers quoted, such as mean quality 26.52 or QUAL 225.417, are good or bad.

The sentence I liked most: "Finding a `preview.fastq` where you expected a full file is correct rather than a truncated result."
