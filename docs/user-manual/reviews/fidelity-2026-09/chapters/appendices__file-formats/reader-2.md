# Reader 2 report, appendices/file-formats

Persona. A senior who has spent two years pipetting. I know library prep and PCR. I have never analyzed data and have never opened a terminal. I read this as someone handed a file who needs to look up what it is.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "which are ordinary folders that macOS shows as a single icon" | I did not know a folder could look like a file. I spent a minute wondering whether this was a metaphor. The explanation of how to actually see inside is 180 lines later. | Add the Show Package Contents pointer here at first mention. |
| What it is, "how to look inside it from the Terminal application" | The Terminal is glossed as where you type commands, but nothing says how to open it, and I do not know where to type these commands from. Every code block after this assumes I already have a prompt open. | Name the one step, Applications then Utilities then Terminal. |
| What it is, "read out of the manual's fixtures" | A fixture is not explained. I could not tell if this is a file I have, a file the manual has, or something in the app. | Gloss fixture at first use or drop the word. |
| What it is, "the demo project at `~/Desktop/lge-docs/...`" | The tilde is not explained. As a reader who has never used a terminal I read it as a literal character in a folder name. | Say the tilde means your home folder. |
| What it is, "so every line quoted here is a line that exists on disk" | Had to read twice. I could not tell whether this was a promise about accuracy or an instruction to me. | Shorten to a plain claim about accuracy. |
| The format registry, "a category that decides where the format appears in a file picker" | File picker is not glossed. I guessed it is the Open dialog but I was guessing. | Gloss file picker at first use. |
| The format registry, "an internal LGE routine can produce directly" | Routine is a programmer's word. The sentence is trying to explain something subtle about write flags and the jargon defeated it. | Replace routine with a plain word. |
| The format registry, "taken from `FormatRegistry+BuiltInDescriptors.swift`" | This is a source code filename. I have no idea what to do with it and it made me feel the appendix was not written for me. | Move the source citation to a footnote or drop it. |
| Registry table, "Detection only" | The column header says LGE reads and the cell says Detection only, which contradicts the header. I had to go back up two paragraphs to work out what it meant. | Add a footnote marker on the cell pointing at the paragraph. |
| Registry table, BAM row "LGE writes: No" | This confused me badly. Later the chapter says every alignment reaching a viewport is a sorted BAM that LGE made. The table says LGE cannot write BAM. I could not reconcile these without effort. | Add one clause to the table caption saying an outside tool writes these. |
| The format registry, "the European counterpart to GenBank" | I do not know what GenBank is yet at this point. It is defined one section later. | Move the GenBank gloss earlier or forward-reference it. |
| The format registry, "The `import fasta` command accepts an EMBL file, so that one reaches the app through a real route" | I could not tell whether `import fasta` is something I type in the Terminal or click in the app. Nothing before this says commands begin with `lungfish-cli`. | Write the command as the reader would type it. |
| Standard sequence formats, "truncated to fit the page" | Used four times. Each time I worried I was looking at an incomplete file rather than a shortened printout. | Say the file continues past what is shown, once, and drop the repeats. |
| Standard sequence formats, "the `hg002-chr20` fixture" | I still do not know where this fixture is or whether I have it. I cannot reproduce anything shown. | Give the path once. |
| Standard sequence formats, `>chr20_10.0-10.5Mb` | I could not judge this name. Is 10.0-10.5Mb part of the standard or something LGE invented for this slice? | One clause saying the name is arbitrary and set by whoever made the file. |
| Standard sequence formats, "a repeat-finding program flagged as repetitive" | Circular. Repeat-finding is defined by repetitive and repetitive is not defined. As a wet lab person I know repeats matter for primer design but this sentence taught me nothing. | Say what a repeat is in one clause. |
| FASTA index, "the byte offset at which its sequence starts" | A byte offset is not glossed. I know what a base is. I do not know what a byte is in this context. | Gloss byte offset in one clause. |
| FASTA index, "500001 19 50 51" | I could not judge any of these numbers. Why is the offset 19 and why do 50 bases take 51 bytes? The 51 is explained but the 19 never is. | Say the 19 counts the header line and its line ending. |
| FASTA index, "Run `samtools faidx` on a FASTA to create one" | This is the only place samtools is named before it is used and it is never introduced. I do not know what samtools is, whether I have it, or whether LGE installs it. | Gloss samtools at first use and say LGE ships it. |
| Standard sequence formats, "30 means roughly a one in a thousand chance of being wrong" | The number I was given is Q30 but the example line shows characters, not numbers. I could not connect the two. I stared at `0<0DD=1<...` and could not tell whether that read was good or bad. | Say the character encodes the score and that reading it by eye is not expected. |
| Standard sequence formats, `@D00360:94:H2YT5BCXX:1:1101:1503:41403` | This is an Illumina read name and I recognize the shape from my sequencing core, but the text does not say the colons carry meaning. | One clause saying the name is instrument-generated. |
| Standard sequence formats, "keeping the original record in a small database inside the bundle" | Database is used with no gloss and nothing says where inside the bundle. The path appears 160 lines later. | Name the file here. |
| Standard sequence formats, "recognizes five compression suffixes" | I know `.gz`. I have never seen `.bgz`, `.xz`, or `.zst` and cannot tell whether these are common or exotic. | No fix needed beyond one clause saying gz is the usual one. |
| Standard annotation formats, "a reading-frame phase" | Phase is named and not explained. I took genetics and I still could not say what goes in that column. | Gloss phase or say the column is usually a dot. |
| Standard annotation formats, `_lf_raw_genbank_location=52070..53062` | The section says this preserves a spliced feature's definition, but the example shown is not spliced, so the example does not demonstrate the point. | Show a spliced example or drop the claim. |
| Standard annotation formats, "`%3B` for a semicolon" | `%3B` does not appear in the example line, so I could not check it. | Use only the code that appears. |
| Standard annotation formats, "the fifth gives its pool" | Pool is not glossed. I have made primer pools at the bench so I guessed, but a sophomore would not. | Gloss pool in one clause. |
| Standard annotation formats, primer BED example `MN908947.3` | This is a SARS-CoV-2 accession dropped in with no label. The chapter uses human data everywhere else, so the switch is jarring and unexplained. | Say in one clause that this is the SARS-CoV-2 reference. |
| Standard annotation formats, "BED and bedGraph are zero-based and half-open" | This is the single most important paragraph for me and it is the densest. I read it four times. The two conventions are stated abstractly with no worked example, so I still cannot convert a coordinate. | Add one worked line showing the same three bases in both conventions. |
| Standard alignment formats, "a further-compressed form that stores only the differences from the reference" | Then the next clause says it needs that exact reference. I understood the words but could not tell what happens if I lose the reference. That is the practical question. | Add one clause saying the file is unreadable without it. |
| Standard alignment formats, "runs any tool that emits SAM through `samtools sort` and `samtools index`" | Sorting and indexing are treated as self-evident. I do not know what a BAM is sorted by or why sorting matters. | One clause saying sorted means ordered by position. |
| Standard alignment formats, "the roughly 512-megabase limit a `.bai` can address" | I could not judge this. Is human chromosome 1 over or under 512 megabases? That is the only thing I would want to know. | Name one real chromosome that crosses the line. |
| Standard alignment formats, the two `samtools` commands | I cannot run these. The paths are relative and nothing says what folder to be in. There is no `cd` step and no explanation of what a working directory is. | Add the one step that gets the reader into the project folder. |
| Standard alignment formats, "gives real numbers" | Had to read twice. I first read it as the commands producing numbers rather than as a claim about the manual's honesty. | Rephrase as plain output description. |
| Standard alignment formats, idxstats output, the `*` row | The `*` line is in the output and is never explained. | One clause saying the star row counts unplaced reads. |
| Standard alignment formats, "91148 + 0 primary ... 55 + 0 supplementary" | Primary, secondary, supplementary, and duplicates are all shown and none are glossed. The paragraph after only uses the total. | Gloss the four terms or show fewer lines. |
| Standard alignment formats, "which is what a healthy targeted mapping run looks like" | 90990 of 91203 is clearly high, but I was not told what number is the floor. Is 80 percent fine? Is 50 percent alarming? | Give a rough threshold. |
| Standard alignment formats, "the reads were mapped against the wrong reference" | This is the one troubleshooting sentence and it does not say what to do next. | Point at the chapter that covers choosing a reference. |
| Standard variant formats, "the reference contigs" | Contig is not glossed anywhere in the chapter. | Gloss contig at first use. |
| Standard variant formats, VCF example rows | The example row stops after INFO but the header line declares FORMAT and HG002 columns. The row does not match its own header and I could not tell if that was truncation or an error. | Say the row is cut at the right edge. |
| Standard variant formats, "QUAL 50 ... PASS" | Then the bcftools output shows QUAL 225.417 for the same position. Two different quality numbers for the same variant, unexplained. I assumed I was misreading. | One clause saying the two files were produced by different callers. |
| Standard variant formats, `DP=62;VDB=0.240996;SGB=-0.693147;MQSBZ=0` | Four codes, none glossed. DP is the only one I would ever look at and it is not called out. | Gloss DP and say the rest are caller internals. |
| Standard variant formats, "LGE reads VCF versions 4.0 through 4.4" | Nothing tells me how to find out which version my file is. The header line `##fileformat=VCFv4.2` is shown above but the connection is not made. | Point back at the fileformat line. |
| Standard variant formats, "a `.db` SQLite sidecar" | SQLite and sidecar both arrive unexplained. Sidecar is glossed 240 lines later under provenance. | Gloss sidecar at first use here. |
| Standard variant formats, "the Variants tab of the table drawer" | Table drawer is app vocabulary I have not met. This appendix is the first place I would look up a file, so it may be my first chapter. | Say it is a panel in the main window. |
| Standard variant formats, "`lungfish-cli bundle create --variant` writes a `.bcf`" | I could not tell whether this matters to me. Two storage shapes for the same thing, and no guidance on which one I will actually have. | Say which route most readers will have used. |
| Standard tree format, "a number after a colon gives a branch length" | I know what a tree is but not what a branch length measures until 6 lines later. The Newick block sits between the two. | Move the substitutions-per-site gloss up. |
| Standard tree format, the Newick block | It is one unbroken 190-character line. I could not find the parentheses the next paragraph tells me to read first. | Break the line or annotate it. |
| Standard tree format, "the human and chimpanzee sit outside that group" | This describes an unrooted tree as if it had a direction. Later the manifest says isRooted is false. As a genetics student I know that matters and the two statements conflict. | Say the tree is unrooted so outside means only that. |
| Standard tree format, "0.8982224217" | The gorilla-macaque branch is thirty times longer than every other branch. That leapt out at me and nothing says whether it is expected or a red flag. | One clause on why that branch is long. |
| What an LGE bundle is, "the manifest's shape is not shared across bundle kinds" | Four exceptions in one paragraph, snake_case and camelCase both used without gloss. I do not know what those words mean and could not use this paragraph at all. | Gloss the two naming styles or drop the terms. |
| What an LGE bundle is, "There is no single key that every manifest carries" | I did not know what a key is in a JSON file. JSON itself is never glossed in this chapter. | Gloss JSON and key at first use. |
| Bundle table, "ONT genotype calls" | ONT is an unexpanded abbreviation. I have heard of nanopore at the bench but never saw the letters. | Expand ONT at first use. |
| Bundle table, "normalized into LGE's classifier schema" | Normalized and schema are both unglossed. I could not tell what CZ ID is either. | Gloss or simplify. |
| What an LGE bundle is, "classifier results ... are not bundles" | Classifier arrives here with no gloss, and the five tool names mean nothing to me. | Gloss classifier in one clause. |
| The reference bundle, "an assembler" | Assembler is not glossed. This is one of the three ways a reference bundle is created so I needed it. | Gloss assembler. |
| The reference bundle, "`.lungfish-provenance.json`" in the tree | The leading dot means the file is hidden and I would not see it in the Finder. Nothing says so. | One clause saying dot-files are hidden. |
| The reference bundle, "the `.fai` addresses sequence positions and the `.gzi` addresses compressed blocks" | Addresses is used in a sense I do not know. | Replace addresses with a plain verb. |
| The reference bundle, "named by a generated track identifier rather than by the caller" | This tells me I cannot identify my own file, then does not tell me how to. The manifest is mentioned but not as the answer. | Say which command maps identifier to caller. |
| The reference bundle, the manifest key list | Fourteen key names in one sentence with no explanation of any but `genome`. I skimmed it, which is the thing I was told not to do. | Cut to the keys a reader would use. |
| The reference bundle, "`total_length": 81706` | I could not judge this. Is 81706 bases a lot for a gene record? Nothing anchors it. | Say it is one gene region. |
| The reference bundle, "The friendlier way to read all of that is the command line" | Reading a JSON file by eye versus typing a command are both terminal work to me, so friendlier did not land. Also this is where the reader learns commands begin with `lungfish-cli`, forty lines after `import fasta` appeared bare. | Introduce the `lungfish-cli` prefix at its first command. |
| The reference bundle, "Features 102" | I could not judge 102 features for one gene. | No fix needed if the number is only illustrative, but say so. |
| The read dataset bundle, "fetch reads from SRA or ENA" | Two unexpanded abbreviations. I have heard SRA once in a seminar. | Expand both at first use. |
| The read dataset bundle, "a read operation such as trimming" | Trimming is not glossed. As a wet lab person I would guess adapter trimming and might guess wrong. | Gloss trimming in one clause. |
| The read dataset bundle, "the meta sidecar records `pairingMode: interleaved`" | I could not act on this. Nothing says how to look at the sidecar, and the earlier `python3 -m json.tool` line is at the very end of the chapter. | Point at the command that prints it. |
| The read dataset bundle, "Whatever the Pairing setting said about the files you handed in" | Had to read twice. It seems to say my setting is ignored, which alarmed me, but I think it means only storage changes. | Say the setting affects reading, not storage. |
| The read dataset bundle, "`n50ReadLength`" | N50 is famously hard and it is dropped in a list with no gloss. | Gloss N50 or drop it from the list. |
| The read dataset bundle, "`gcContent": 0.444` and `"meanQuality": 26.52` | Two numbers I cannot judge. Is 44 percent GC normal? Is mean quality 26.5 acceptable, given the chapter earlier taught me 30 as the reference point? | Say what range is ordinary for each. |
| The read dataset bundle, "Some read bundles are virtual, meaning they hold no reads at all" | This is genuinely alarming to a bench person who thinks their data may be gone. The reassurance comes two sentences later. | Lead with the reassurance. |
| The read dataset bundle, "the operation to replay" | Replay is used in a specialized sense with no gloss. | Say the operation is rerun on demand. |
| The read dataset bundle, `lungfish-cli fastq materialize` | Materialize is used as a verb without gloss. Also the command has no path prefix while the earlier `bundle info` example had a full quoted path, so I do not know which style works. | Gloss materialize and make paths consistent. |
| The read dataset bundle, "aligned to the PHA4GE specification" | Unexpanded abbreviation and an unexplained specification. | Expand or drop. |
| The read dataset bundle, "turns a folder of them into a TSV file you can submit to NCBI's BioSample portal" | NCBI is unexpanded here even though it appeared earlier. TSV is only in the registry table. | Expand NCBI at first use. |
| The alignment and tree bundles, "padded with gap characters" | Gap character is not shown. I do not know it is a dash until I have opened one. | Say the gap character is a dash. |
| The alignment and tree bundles, "`source.original`" | This filename has no extension and is never explained. It appears in both the MSA and tree layouts. | One clause saying it keeps the file as imported. |
| The alignment and tree bundles, "5,053 variable sites, and 2,709 parsimony-informative sites" | Parsimony-informative is not glossed. I could not judge either number against the 17,247 aligned length. | Gloss parsimony-informative and say what fraction is typical. |
| The alignment and tree bundles, "a `checksums` map, and a `fileSizes` map" | Checksum is glossed 60 lines later, under provenance, not here at first use. Map is not glossed at all. | Move the checksum gloss to first use. |
| The alignment and tree bundles, "`internalNodeCount` ... three internal nodes" | Five tips and three internal nodes did not match my count from the Newick line, which shows two nesting levels. I could not resolve it. | Say how internal nodes are counted for an unrooted tree. |
| The primer scheme bundle, "`PROVENANCE.md`" | This is all caps and Markdown, while every other provenance file in the chapter is a dot-prefixed JSON. The inconsistency is not explained. | One clause saying shipped schemes use a text note. |
| The primer scheme bundle, "The manifest uses `schema_version` rather than a version number under some other name" | Read three times. The sentence explains what the key is not called, which told me nothing. | State the key and stop. |
| The primer scheme bundle, "the filenames above are fixed by convention rather than declared" | I could not tell what this means for me. If I rename a file does the bundle break? That is the actual question. | Say renaming breaks the bundle. |
| The primer scheme bundle, "four ARTIC versions covering nCoV-2019 V3 through SARS-CoV-2 V5.3.2" | I count eight schemes claimed but the list names one plus four plus one plus two. That is eight, but the four are not individually named, so I cannot look mine up. | Name all four ARTIC versions. |
| The primer scheme bundle, "**File > Import Center...**" | This is the only app menu path in the chapter and it arrives with no explanation of where the menu bar is or which window must be open. | State which window must be frontmost. |
| The result bundles, "Reading one from the Williams MiSeq genotyping project" | A project name appears from nowhere. Every other example came from the demo project. I do not have this one. | Say the example is from a project the reader does not have. |
| The result bundles, "the stderr logs of every tool the run invoked" | stderr is unglossed jargon. | Say error logs. |
| The result bundles, "an `artifacts/` folder holding projections and workbooks" | Projections is unglossed and I cannot guess it. | Gloss or drop projections. |
| The result bundles, "in a fixed category folder rather than a timestamped one" | I do not know what a timestamped folder would be, since none has been shown. The contrast is with something I have never seen. | Drop the contrast. |
| The result bundles, "In Preview 2026.9.13 the import sheet's Project Destination readout names a path under `Analyses` that nothing writes to" | This is a bug notice and it took two readings to see that. It reads like a description of intended behavior. | Label it as a known problem. |
| The result bundles, "deduplicated 12S reference sequences" | Deduplicated is unglossed, and 12S itself is never explained anywhere in the chapter despite appearing in three bundle kinds. | Gloss 12S at first use. |
| The result bundles, "`.lungfishhaplotypedef.json`, not a bundle format of their own" | Haplotype is not glossed and the sentence's point depends on a distinction I do not have. | Gloss haplotype or cut the sentence. |
| The workflow bundles, "the graph of steps you assembled in the Workflow Builder" | Graph is used in the computer science sense. I read it as a chart first. | Say the connected set of steps. |
| The workflow bundles, "such as a Nextflow script" | Nextflow is unglossed. | Say it is an outside pipeline system. |
| The workflow bundles, "a run started from a bare graph JSON file" | Bare graph JSON file has not been introduced as a thing that exists. | Say a workflow saved as a plain file rather than a bundle. |
| Provenance sidecars, "`<filename>.lungfish-provenance.json`" | The angle brackets are notation I had to infer. | Show one real filename instead. |
| Provenance sidecars, "The keys are not the ones a general-purpose provenance format would use" | I do not know any provenance format, so the contrast is empty and the sentence cost me a re-read. | Cut the comparison. |
| Provenance sidecars, "`hostOS": "macOS 26.6.2 (arm64)"` | arm64 is unglossed. | Drop or gloss. |
| Provenance sidecars, "`minimumDepth` as `caller-default`" | Depth was shown as DP earlier and never glossed. This is the second unexplained appearance. | Gloss depth once, early. |
| Provenance sidecars, "so the checksum and byte size claim holds for both directions" | Read three times and still unsure what claim or which directions. | Rewrite as a plain statement about inputs and outputs. |
| Provenance sidecars, "There is no `schema_version` key in a provenance sidecar" | Another sentence defined by absence. It does not help me do anything. | Cut or replace with what is there. |
| Sharing and inspecting bundles, `zip -r HBB.lungfishref.zip HBB.lungfishref` | I would not have thought to use the Terminal for this. In the Finder I would right-click and choose Compress, which is the step I can actually do. | Give the Finder step first. |
| Sharing and inspecting bundles, "The recipient unzips it and drops the folder into a project" | Drops it where? Into the app window, into the project folder in the Finder, or onto the icon? I could not perform this. | Name the drop target. |
| Sharing and inspecting bundles, "a deterministic OCI layout tarball, which is the standard directory shape a container registry uses" | Three unknowns in one clause. OCI, tarball, and container registry. The gloss explains one word using two more. | Cut this to one sentence or drop it. |
| Sharing and inspecting bundles, "each blob addressed by its SHA-256 digest" | Blob and digest are unglossed, and checksum was already glossed as fingerprint, so I could not tell whether digest is the same thing. | Reuse the word checksum. |
| Sharing and inspecting bundles, "In Preview 2026.9.13 that command cannot actually be run" | The chapter spends a full paragraph teaching me a format and then says the command is broken. I felt I had wasted the reading. | Put the broken-command notice first. |
| Sharing and inspecting bundles, "`vc-6edd1356.vcf.gz`" | This filename is shortened while the same file was written in full earlier. If I copied this line it would fail. | Use the full filename. |
| Sharing and inspecting bundles, "`python3 -m json.tool`" | Python is never mentioned before this. I do not know if I have it or what the flags mean. | Say macOS includes it, or use the CLI instead. |
| Sharing and inspecting bundles, all four example commands | Every one uses a relative path with no `cd` step, so I cannot run any of them, and the earlier `bundle info` example used a full quoted path instead. Two styles, neither runnable by me. | Add the one `cd` line and keep one style. |
| Next, "See [Power User Notes](power-user-notes.md) for the exact flags LGE passes" | Flag is used in two senses in this chapter, the registry read and write flags and command-line flags. I noticed the collision. | Use option for command-line flags. |
| Whole chapter | There is no glossary link and no index of formats by extension. I was handed a file ending in `.gzi` and had to read three sections to find out what it was. | Add a lookup list from extension to section. |
| Whole chapter, estimated_reading_min 26 | It took me far longer. As an appendix I expected to look one thing up, but the sections depend on earlier ones for glosses, which the opening promises they do not. | Either make sections self-contained or drop that promise. |

One thing I learned. A bundle is just a folder wearing a costume, and I can open it with Show Package Contents.

One thing I still could not do. Run any command in this chapter, because nothing tells me how to get the Terminal into the right folder.

The sentence I liked most. "Finding a `preview.fastq` where you expected a full file is correct rather than a truncated result."
