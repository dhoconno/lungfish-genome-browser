# Merged reader report - Appendices: File Formats

Four readers reviewed this chapter. Reader 1 is a sophomore fresh from one genetics course who has never opened a terminal and filed 68 rows. Reader 2 is a senior with two years of wet-lab experience and no data-analysis background who filed 111 rows. Reader 3 is a pre-med student reading English as a second language who filed 87 rows. Reader 4 has used Geneious and expects every operation to have a dialog, and filed 67 rows.

| Location | Issue | Suggested fix | Readers |
|---|---|---|---|
| Whole chapter, every inspection method | The chapter's whole method is Terminal commands, but no passage says how to open Terminal, what folder to be in, or that any menu equivalent exists. All four readers could not perform a single command as printed. | State once, early, how to open Terminal and get into the project folder, and note in each section whether a menu equivalent exists. | 1, 2, 3, 4 |
| Numbers given with no scale, spread across the chapter | Recurring specific numbers, the 512-megabase `.bai` limit, the mapping-health percentage, QUAL scores, GC content and mean quality, and parsimony-informative site counts, are shown with no anchor telling readers whether their own number is normal, high, or a problem. | Give a rough threshold or anchor next to each of these recurring numeric claims. | 1, 2, 3, 4 |
| The format registry, "BAM ... LGE writes: No" | All four readers found the table's "LGE writes: No" for BAM directly contradicts the alignment section, which says every alignment reaching a viewport is a sorted BAM that LGE produced, and could not reconcile the two. | Add a footnote to the BAM row pointing at the alignment section's explanation. | 1, 2, 3, 4 |
| Standard sequence formats, "repeat-finding program flagged as repetitive" / lowercase bases | All four readers understood the definition of repeat-masking but could not tell whether lowercase in their own file means something is wrong, or whether LGE shows it differently on screen. | Say directly that lowercase is normal, needs no action, and state whether the viewport distinguishes it. | 1, 2, 3, 4 |
| Standard sequence formats, "the byte offset at which its sequence starts" | All four readers met "byte offset" with no gloss and could not picture what a byte position in a file means. | Gloss byte offset once as the count of characters from the start of the file. | 1, 2, 3, 4 |
| Standard sequence formats, "Run `samtools faidx` on a FASTA to create one." | All four readers could not perform this, the first command in the chapter: there is no full example line, and nothing says whether samtools is a separate program, whether LGE ships it, or whether importing a FASTA already makes the index. | Show the full command with a filename, and say once, early, whether samtools ships with LGE. | 1, 2, 3, 4 |
| Standard sequence formats, "30 means roughly a one in a thousand chance of being wrong" | All four readers were given the Q30 anchor but could not connect it to the actual quality-line characters shown in the example, and had no way to judge whether that read was good or bad by eye. | Say plainly that the characters encode the score, that reading them by eye is not expected, and that LGE plots them for you. | 1, 2, 3, 4 |
| Standard annotation formats, "a reading-frame phase" | All four readers met "phase" with no gloss, and even readers with a genetics background did not know what value belongs in that column. | Gloss phase in half a sentence, or say most readers can ignore it. | 1, 2, 3, 4 |
| Standard annotation formats, "the fifth gives its pool" | All four readers met "pool" as an unglossed primer term and could not judge the example value. | Gloss pool in half a sentence as which primer mix the primer belongs to. | 1, 2, 3, 4 |
| Standard annotation formats, "BED and bedGraph are zero-based and half-open" | All four readers found this the single most important passage in the section, but its explanation is abstract, is padded with a second unfamiliar term (half-open), and gives no worked example, so none could convert a coordinate. | Add one worked example showing the same bases in both conventions, and say what to type when copying a coordinate into LGE. | 1, 2, 3, 4 |
| Standard alignment formats, "stores only the differences from the reference" (CRAM) | All four readers understood that CRAM stores differences from a reference but were never told the consequence of losing that reference, or what LGE does if it is missing. | State plainly that a CRAM without its reference cannot be read, and say what LGE does in that case. | 1, 2, 3, 4 |
| What an LGE bundle is, "snake_case keys" and "camelCase keys" | All four readers met snake_case and camelCase used as if standard vocabulary, with no gloss, and had to infer the meaning from the examples. | Gloss both naming styles once, in one clause. | 1, 2, 3, 4 |
| The bundle table, "ONT genotype calls" | All four readers met ONT as an abbreviation that is never expanded anywhere in the chapter. | Expand ONT as Oxford Nanopore at first use. | 1, 2, 3, 4 |
| The bundle table, "A CZ ID taxon report normalized into LGE's classifier schema" | All four readers met CZ ID, normalized, and classifier schema arriving together in one table row with none of them explained. | Gloss CZ ID as an outside classification service, and simplify the row to what the bundle holds. | 1, 2, 3, 4 |
| The read dataset bundle, "the meta sidecar records `pairingMode: interleaved`" | All four readers found paired-end and the Pairing-setting override introduced together with no gloss, and the wording sounded like the reader's own choice was being ignored. | Gloss paired-end, and say plainly that the setting affects how files are read in, not whether the bundle can give the two files back. | 1, 2, 3, 4 |
| The read dataset bundle, "`n50ReadLength`" | All four readers met N50 listed among the read statistics with no gloss anywhere in the chapter. | Gloss N50 in one clause. | 1, 2, 3, 4 |
| The read dataset bundle, "To write the full reads out, materialize the bundle." | All four readers met "materialize" used as a verb before it means anything, and found the only route given is a Terminal command with no menu equivalent named. | Gloss materialize, keep command paths consistent, and name a menu command if one exists. | 1, 2, 3, 4 |
| The read dataset bundle, "aligned to the PHA4GE specification" | All four readers met PHA4GE as an unexpanded acronym and could not tell whether it constrains what they type in metadata fields. | Expand PHA4GE at first use and say whether it limits the reader's entries. | 1, 2, 3, 4 |
| The result bundles, "the stderr logs of every tool the run invoked" | All four readers met stderr as an unglossed terminal word. | Say "error logs" instead of stderr, or gloss it. | 1, 2, 3, 4 |
| The result bundles, "the import sheet's Project Destination readout names a path under `Analyses` that nothing writes to" | All four readers read this bug notice as a plain description of intended behavior on first pass, and could not tell whether their import had failed or the display was merely wrong. | Flag it plainly as a known defect in its own short note, stating the import succeeds and the result is under Classifications. | 1, 2, 3, 4 |
| The workflow bundles, "such as a Nextflow script" | All four readers met Nextflow named with no gloss, alongside the equally unglossed term "engine." | Gloss Nextflow in a few words as an outside pipeline system. | 1, 2, 3, 4 |
| Provenance sidecars, "`minimumDepth` as `caller-default`" | All four readers could not tell what actual depth value a tool used when the sidecar only records that no value was overridden. | Say how to find the tool's own default, or whether the real number is recoverable. | 1, 2, 3, 4 |
| Provenance sidecars, "so the checksum and byte size claim holds for both directions" | All four readers read this sentence multiple times and still could not identify what claim was being made or what the two directions were. | Rewrite as one plain statement naming what is recorded for inputs and for outputs. | 1, 2, 3, 4 |
| Provenance sidecars, "There is no `schema_version` key in a provenance sidecar." | All four readers met this fact stated as a correction to an expectation they never had, which left them wondering what they had missed rather than informing them. | State it positively, or move it to a note aimed at tool builders. | 1, 2, 3, 4 |
| Sharing and inspecting bundles, "`zip -r HBB.lungfishref.zip HBB.lungfishref`" | All four readers met this as a Terminal-only instruction with `-r` unexplained, when a Finder route (right-click, Compress) exists and is mentioned only afterward or not at all. | Give the Finder step first, then the command, and explain what `-r` does. | 1, 2, 3, 4 |
| Sharing and inspecting bundles, "a deterministic OCI layout tarball" | All four readers met a dense cluster of unfamiliar terms together, OCI, tarball, blob, container registry, with the gloss for one term relying on two more unglossed terms. | Say in one clause who this format is for, so the reader can skip the paragraph knowingly, and cut the jargon-dense detail. | 1, 2, 3, 4 |
| Sharing and inspecting bundles, "In Preview 2026.9.13 that command cannot actually be run." | All four readers read a full paragraph teaching a command before learning it does not work, and felt the reading was wasted. | Move the broken-command notice to the start of the paragraph, before the explanation. | 1, 2, 3, 4 |
| Sharing and inspecting bundles, shortened filename `vc-6edd1356.vcf.gz` | All four readers noticed this filename is shorter than the full identifier used earlier in the chapter for the same file, and could not tell whether the short form works or is a typo. | Use the full filename consistently everywhere it appears. | 1, 2, 3, 4 |
| Terms glossed only in a later section, not at first use | Across many passages (bundle-looks-like-one-icon, GenBank, the bundle's internal database, SQLite sidecars, branch length, the lungfish-cli prefix, pairingMode, checksums, and the chapter's own promise that sections stand alone), three readers found a term's explanation exists but arrives dozens to hundreds of lines after the term is first used, forcing them to read on faith or scroll back and forth. | Move each gloss to the term's first use, or repeat a short version there, and drop the opening's promise that sections are self-contained if that promise is not kept. | 1, 2, 3 |
| What it is, "read out of the manual's fixtures" / "the `hg002-chr20` fixture" | Three readers met "fixture" with no gloss and could not tell whether it was a file they had, a file the manual had, or something inside the app, and one still did not know where the file lived. | Gloss fixture once at first use as an example file that ships with the manual, and give its path once. | 1, 2, 4 |
| What it is, "the demo project at `~/Desktop/lge-docs/...`" | Three readers met the tilde character with no gloss and did not know whether to type it literally, replace it, or what it represented. | Gloss the tilde once as the reader's home folder. | 2, 3, 4 |
| The format registry, "an internal LGE routine can produce directly" | Three readers met "routine" used as unglossed programming jargon that obscured the point being made about write flags. | Replace "routine" with a plain phrase such as "part of LGE." | 1, 2, 3 |
| The format registry, "registry identifiers without a full descriptor entry" | Three readers met "registry identifier" and "full descriptor entry" arriving together, both new, with no way to tell what the practical difference means for their own file. | State the practical effect plainly: LGE can name the file type but cannot open it as a track. | 1, 3, 4 |
| Standard sequence formats, "recognizes five compression suffixes" | Three readers recognized only `.gz` among the five listed compression suffixes and could not tell whether the others were common or exotic, or whether LGE treats them the same. | Say plainly that all five are compression formats LGE handles the same way, with `.gz` the usual case. | 1, 2, 3 |
| Standard annotation formats, long attribute example line | Three readers found the GenBank-location attribute example runs off the page width with no visual break, so they could not tell where one attribute ended and the next began. | Break the attribute column onto its own line, or note that semicolons separate the attributes. | 1, 3, 4 |
| Standard alignment formats, "runs any tool that emits SAM through `samtools sort` and `samtools index`" | Three readers found sorting and indexing treated as self-evident steps, with no statement of what "sorted" means for a BAM or why it must precede indexing. | Add one clause saying sorted means ordered by position, so an index can be built. | 1, 2, 3 |
| Standard alignment formats, flagstat output lines | Three readers saw primary, secondary, supplementary, duplicates, and the "+ 0" notation all appear in the quoted flagstat output with none of them glossed, though only the total is discussed afterward. | Gloss the "+ 0" form once and say which lines a beginner can safely ignore. | 1, 2, 3 |
| Standard variant formats, "declare the file version, the reference contigs" | Three readers met "contig" with no gloss anywhere in the chapter. | Gloss contig at this first use as one continuous stretch of reference sequence. | 1, 2, 3 |
| Standard variant formats, `DP=62;VDB=0.240996;SGB=-0.693147;MQSBZ=0` | Three readers found four INFO abbreviations shown with none expanded, though DP is the one most readers would want explained. | Gloss DP as read depth and say the rest are caller-internal values that can be skipped. | 1, 2, 3 |
| Standard variant formats, "LGE reads VCF versions 4.0 through 4.4" | Three readers did not know how to find out which VCF version their own file uses, even though the `##fileformat` line shown earlier answers the question. | Point back explicitly at the `##fileformat` line as the place to check. | 1, 2, 4 |
| Standard tree format, the unbroken Newick line | Three readers found the whole Newick tree printed as one long unbroken line, making it impossible to visually locate a specific branch length or follow the nesting as instructed. | Break the line across several indented lines, or annotate which name a given branch length sits after. | 2, 3, 4 |
| Standard tree format, the long 0.898 branch length | Three readers noticed the gorilla-macaque branch length is far larger than the others, with nothing saying whether that is expected or a red flag. | Add one clause explaining why that branch is long. | 1, 2, 3 |
| What an LGE bundle is, "There is no single key that every manifest carries." | Three readers found this fact stated with no practical guidance, leaving them anxious rather than informed, and one noted the surrounding sentence is a double negative. | State the practical rule plainly: open the manifest and read it directly. | 1, 3, 4 |
| The alignment and tree bundles, "`source.original`" | Three readers met this filename, which carries no extension, appearing in two different bundle listings with no description of what it holds. | Add one clause saying what `source.original` holds and that it keeps the file as imported. | 1, 2, 3 |
| The primer scheme bundle, "four ARTIC versions covering nCoV-2019 V3 through SARS-CoV-2 V5.3.2" | Three readers counted the claimed eight schemes but found the four ARTIC versions named only by a range, not individually, so they could not check whether their own scheme was included. | Name all eight schemes individually in a short list. | 2, 3, 4 |
| The result bundles, "Haplotype definitions are files inside that folder with the suffix `.lungfishhaplotypedef.json`, not a bundle format of their own" | Three readers met "haplotype" used repeatedly with no gloss, and could not see what practical difference the file-versus-bundle-format distinction makes. | Gloss haplotype at its first use in the chapter, and state what the distinction changes for the reader. | 2, 3, 4 |
| The workflow bundles, "the graph of steps you assembled in the Workflow Builder" | Three readers first read "graph" as a chart or plot rather than a flow of connected steps. | Say "the connected sequence of steps" instead of "graph." | 1, 2, 3 |
| What it is, "ordinary folders that macOS shows as a single icon" | Two readers found the practical consequence of a bundle looking like one icon missing at first mention, one noting the Finder result (double-clicking opens LGE) and one noting the biggest surprise coming from Geneious, where results live inside the program rather than as folders on disk. | State the Finder consequence and the disk-versus-in-app contrast together at first mention. | 3, 4 |
| The format registry, "taken from `FormatRegistry+BuiltInDescriptors.swift`" | Two readers met a source code filename cited with no explanation of what to do with it, leaving the appendix feeling like it was not written for them. | Move the source citation to a footnote, or drop it. | 1, 2 |
| The format registry, "assigns a category that decides where the format appears in a file picker" | Two readers met "file picker" used with no gloss and had to guess it meant the Open dialog. | Gloss file picker at first use as the Open dialog. | 1, 2 |
| The format registry, "Detection only" | Two readers found "Detection only" appearing in the table before the paragraph that explains it, forcing them to scroll back and forth to connect the two. | Repeat the one-line meaning of "Detection only" directly under the table. | 1, 2 |
| The format registry, "convert it to bedGraph outside LGE first" | Two readers were told to convert a file but given no tool name to do it with, so the step cannot be performed from the text. | Name the specific converting tool, or say plainly the manual does not cover this step. | 1, 4 |
| The format registry, "still carry a write flag of false in the registry" / "a write flag of false" | Two readers found the prose says "flag of false" while the table itself uses the words "Yes" and "No," and the mismatch made them stop to check the two meant the same thing. | Use the table's own words, "No," in the prose instead of "flag of false." | 3, 4 |
| Standard sequence formats, "the human chromosome 20 slice from the `hg002-chr20` fixture" | Two readers did not know what HG002 refers to, since it recurs through the chapter as if it were common knowledge, and one also found "slice" a metaphor that left the coordinate name unexplained. | Add one clause saying HG002 is a widely used reference human sample, and gloss the coordinate name at first use. | 1, 3 |
| Standard sequence formats, "truncated to fit the page" | Two readers were unsure, each of the several times this phrase appeared, whether the underlying file itself was short or only the printed excerpt had been shortened. | Say once that the lines were shortened for printing, and drop the repeated reminders. | 2, 3 |
| Standard annotation formats, "so a spliced feature keeps its exact definition" | Two readers met "spliced feature" used with no gloss and could not picture what a single start and end position would fail to capture. | Add a clause saying a spliced feature is made of several separate pieces that a single start and end cannot record. | 1, 3 |
| Standard annotation formats, "MN908947.3" in the primer example | Two readers found a SARS-CoV-2 accession dropped into an example with no label, jarring after a chapter of human and macaque examples. | Name it as the SARS-CoV-2 reference in the lead-in sentence. | 1, 2 |
| Standard alignment formats, the `*` row in idxstats output | Two readers found the output line beginning with `*` shown with no explanation of what it counts. | Add one clause saying the `*` row counts reads that matched nothing. | 1, 2 |
| Standard variant formats, "the Variants tab of the table drawer" / "table drawer" | Two readers met "table drawer" used as app vocabulary they had not met, with no statement of where it is in the window. | Say plainly that the table drawer is a panel in the main window, and point to where it is introduced. | 2, 4 |
| Standard tree format, "the human and chimpanzee sit outside that group" | Two readers read this phrase as an ancestry claim that seemed to contradict what they had been taught, or as contradicted by the names' position in the text, without the tree's unrooted nature being stated at this point. | Say at this point that the tree is unrooted, so position in the text or in the description carries no ancestry meaning. | 2, 4 |
| What an LGE bundle is, "There is no single key that every manifest carries" / JSON key | Two readers did not know what a "key" meant in a JSON file, since JSON itself is never glossed anywhere in the chapter. | Gloss JSON and "key" together at first use. | 2, 3 |
| The reference bundle, "`lungfish-cli bundle info`" | Two readers met the first `lungfish-cli` command in the chapter with nothing said about what it is, whether it is installed, or whether the app offers the same information another way. | Say in one sentence that `lungfish-cli` ships with LGE, and whether the app shows this same information anywhere else. | 1, 4 |
| The reference bundle, "named by a generated track identifier rather than by the caller" | Two readers were told they cannot identify which caller produced a given variant track from a folder listing, but not how to find out. | Name the command or file that maps a generated track identifier back to its caller. | 2, 4 |
| The reference bundle, "which is why an assembly appears in every reference picker without any conversion step" / "An assembly is the same format in a different place" | Two readers found the point, that assemblies are the same format shown in a different place, buried at the end of a long sentence covering creation, location, and consequence together. | Lead with the consequence, then explain how an assembly gets there. | 3, 4 |
| The read dataset bundle, "fetch reads from SRA or ENA" | Two readers met SRA and ENA as unexpanded abbreviations. | Expand both abbreviations at first use. | 2, 3 |
| The read dataset bundle, "Some read bundles are virtual, meaning they hold no reads at all" | Two readers found this alarming statement precedes its reassurance by two sentences, reading briefly as if their data were missing. | Lead with the reassurance before stating that the bundle holds no reads. | 2, 3 |
| The read dataset bundle, "turns a folder of them into a TSV file you can submit to NCBI's BioSample portal" | Two readers met NCBI unexpanded at this point and found this described as a Terminal-only task for something that sounds routine enough to expect a dialog. | Expand NCBI at first use, and say whether an in-app equivalent exists. | 2, 4 |
| Alignment and tree bundles, "`isRooted` false" | Two readers met rooted versus unrooted trees left unexplained here, though the concept affects how the whole tree section should be read. | Gloss rooted versus unrooted in half a sentence at this point. | 3, 4 |
| The primer scheme bundle, "the filenames above are fixed by convention rather than declared" | Two readers could not tell what this means practically, specifically whether renaming a file would break the bundle. | State plainly that renaming breaks the bundle, since the manifest does not separately list the filenames. | 2, 3 |
| The result bundles, "Reading one from the Williams MiSeq genotyping project" | Two readers met a named project appearing with no introduction, unlike every other example drawn from the shared demo project, and could not tell if they were expected to have it. | State plainly that this example comes from a separate project not included with the manual. | 2, 3 |
| The workflow bundles, "a run started from a bare graph JSON file" | Two readers found "bare graph JSON file" introduced as if already a known concept, with "bare" doing unexplained work. | Say plainly that this is a workflow saved as a plain file rather than as a bundle. | 2, 3 |
| Provenance sidecars, "The keys are not the ones a general-purpose provenance format would use" | Two readers do not know any general-purpose provenance format, so this comparison conveyed nothing and cost a reread. | Drop the comparison and describe the shape directly. | 2, 3 |
| Sharing and inspecting bundles, "`python3 -m json.tool`" | Two readers met Python introduced for the first time in the chapter's last code block, with no statement of whether it comes with macOS. | State that python3 ships with macOS, or move this command earlier and reference it from the bundle sections. | 2, 3 |
| Sharing and inspecting bundles, "each blob addressed by its SHA-256 digest" | Two readers met "blob" and "digest" as new terms, and could not tell whether digest was the same concept as the checksum defined earlier using different wording. | Reuse the word "checksum" instead of introducing "digest." | 2, 3 |
| What it is, "such as FASTA for sequences and BAM for alignments" | One reader met FASTA and BAM named in the opening sentence as if already familiar, with the appendix's cold start giving no signal that each is defined later. | Add half a clause saying each format is defined in its own section below. | 1 |
| What it is, "so every line quoted here is a line that exists on disk" | One reader had to read this claim twice, unsure whether it was a promise about accuracy or an instruction to follow. | Shorten to a plain claim about accuracy. | 2 |
| What it is, "This appendix is a lookup table rather than a procedure" | One reader took "lookup table" as a literal table and searched the page for it before realizing it was a figure of speech. | Say plainly that the appendix is for looking up individual sections, not for reading start to finish. | 3 |
| Whole chapter, no glossary or extension index | One reader found no glossary link and no index from file extension to section, and had to read three sections to identify a `.gzi` file. | Add a lookup list mapping file extensions to the sections that cover them. | 2 |
| The format registry table, "`.faa`, `.ffn`, `.frn`" | One reader met six extra FASTA extensions with no hint of which are protein and which are nucleotide. | Note in a clause that `.faa` holds protein sequence and the rest hold nucleotide sequence. | 1 |
| The format registry, "The `import fasta` command accepts an EMBL file, so that one reaches the app through a real route" | One reader could not tell whether `import fasta` is a Terminal command or an in-app action, since nothing before this point had said commands begin with `lungfish-cli`. | Write the command the way the reader would actually type it, with the `lungfish-cli` prefix shown. | 2 |
| The format registry, "The read and write flags matter more than they look" | One reader read this as an idiom and only understood it from the sentences that followed. | Say plainly that the two columns are easy to misread, and explain why. | 3 |
| Format registry table, "FASTA index `.fai` Category Index" | One reader counted the categories named in the prose as seven but found an eighth, Index, appearing only in the table, and could not reconcile the counts. | Name Index in the category list in the prose, or state that the count covers only file-picker categories. | 3 |
| The format registry, "so that one reaches the app through a real route" | One reader found "real route" vague, with no sense of what an unreal route would look like. | Say plainly that EMBL files can actually be imported, unlike the other two formats mentioned. | 3 |
| Format registry, "treats the file as an import candidate" | One reader could not tell from "import candidate" whether the import would actually succeed. | Replace the phrase with the plain outcome the user will see. | 4 |
| Format registry, "assigns a category that decides where the format appears in a file picker" | One reader could not tell what changes for them if a format sits in the wrong category, such as whether the file would fail to appear when opening. | Say in one sentence what a user actually notices when a format's category differs. | 4 |
| Standard sequence formats, "`chr20_10.0-10.5Mb 500001 19 50 51`" | One reader counted five numbers plus a name in the FASTA index example, but the prose names only four columns, and could not reconcile the counts. | Say five columns, or say four columns come after the sequence name. | 3 |
| Standard sequence formats, `>chr20_10.0-10.5Mb` | One reader could not tell whether the name format `10.0-10.5Mb` is a real standard or something LGE invented for this example. | Add a clause saying the name is arbitrary and chosen by whoever created the file. | 2 |
| Standard sequence formats, Illumina read name `@D00360:94:H2YT5BCXX:1:1101:1503:41403` | One reader recognized the read-name shape from their own sequencing core but the chapter never says the colon-separated fields carry meaning. | Add a clause saying the read name is generated by the sequencing instrument. | 2 |
| Standard annotation formats, example attribute not actually spliced | One reader noticed the example given to demonstrate a spliced feature's location string is not itself spliced, so it does not demonstrate the claim it accompanies. | Show a genuinely spliced example, or drop the claim tied to this example. | 2 |
| Standard annotation formats, "`%3B` for a semicolon" | One reader looked for the `%3B` escape code in the shown example line and could not find it, so could not verify the claim. | Use only escape codes that actually appear in the example shown. | 2 |
| Standard annotation formats, "the shipped QIAseq scheme" | One reader read "shipped" as meaning sent to them personally, rather than meaning included with the app, a meaning repeated several times later. | Replace "shipped" with "included with LGE" throughout. | 3 |
| Standard annotation formats, "a semicolon-separated list of attributes" | One reader lost their place partway through a single long sentence naming all nine GFF columns at once. | Break the nine columns into a short list rather than one long sentence. | 3 |
| Standard annotation formats, semicolons in examples | One reader briefly read the semicolons throughout the GFF examples as a formatting error, since the manual's own prose rules ban semicolons in running text. | No fix needed if intentional, since this applies to code examples rather than prose. Worth confirming that distinction is clear. | 4 |
| Standard alignment formats, "gives real numbers" | One reader first read "gives real numbers" as describing the commands' output rather than as a claim about the manual's honesty, and had to reread. | Rephrase as a plain description of the command's output. | 2 |
| Standard alignment formats, "the reads were mapped against the wrong reference" | One reader found this the chapter's only troubleshooting sentence, and it does not say what to do next. | Point to the chapter section that covers choosing the correct reference. | 2 |
| Alignment formats, mapping pipeline description | One reader found the mapping pipeline described from the app's internal perspective, naming two command-line tools as if the reader would recognize them, rather than describing what the reader experiences. | Rewrite from the reader's perspective: LGE always leaves behind a sorted, indexed BAM. | 4 |
| Standard variant formats, VCF example row truncated | One reader found the shown VCF example row stops after the INFO column even though the header line declares FORMAT and a sample column, and could not tell if that was deliberate truncation or an error. | Say explicitly that the row is cut at the right edge for space. | 2 |
| Standard variant formats, two different QUAL values for one variant | One reader saw QUAL 50 in one example and QUAL 225.417 for what appeared to be the same variant in a later example, with no explanation, and assumed they had misread. | Add a clause saying the two files were produced by different variant callers. | 2 |
| Standard variant formats, "bgzip is a block-wise form of gzip that stays randomly accessible" | One reader met three new ideas, block-wise, randomly accessible, and gzip, packed into a single sentence. | Split into two sentences, and say plainly that a bgzip file can be read starting from the middle. | 3 |
| Standard variant formats, long variant-track identifier | One reader could not tell whether the long generated identifier shown in the example must be typed exactly, or whether their own file would have a different one. | Say plainly that the identifier is generated and the reader's own will differ. | 3 |
| Standard variant formats, "`lungfish-cli bundle create --variant` writes a `.bcf`" | One reader met the `lungfish-cli` tool used here as if already familiar, though it had not yet been introduced anywhere in the chapter at this point. | Introduce `lungfish-cli` in the chapter's opening section before using it. | 3 |
| Variant formats, GATK entries writing to a different path | One reader found that two import routes producing different file shapes are described, but not what the difference means for the reader or whether it is visible in the app. | Say whether the difference between the two routes is visible anywhere in the app. | 4 |
| Variant formats, command line not introduced as an alternative | One reader, coming from a dialogs-only tool, did not know LGE had a command-line option at all when it first appeared, since nothing earlier flagged it as an alternative to the window. | Introduce the command line once in the opening section, and note that a reader can skip every code block. | 4 |
| Variant formats, "converted with an outside tool before import" | One reader found an unnamed outside tool referenced as a required conversion step, which cannot be performed without a name. | Name the specific tool required. | 4 |
| What an LGE bundle is, "right-click it and choose Show Package Contents" | One reader could not tell whether this Finder step, the chapter's only click instruction, is required before the Terminal commands that follow will work. | State plainly that the Terminal commands work without performing this step. | 1 |
| What an LGE bundle is, "classifier results ... are not bundles" | One reader met "classifier" used with no gloss, alongside five unfamiliar tool names. | Gloss classifier in one clause. | 2 |
| What a bundle is, "Only three bundle extensions have identifiers in the format registry" | One reader found this an internal detail about the app with no consequence stated for what the reader would actually notice. | Cut the detail, or state plainly what a user would notice as a result. | 4 |
| The bundle table, "`.lungfish12s` A 12S metabarcoding run's species table" | One reader met "12S" and "metabarcoding" together with neither explained, despite recognizing ribosomal genes generally. | Gloss 12S once as a mitochondrial gene region used to identify species. | 3 |
| The reference bundle, long variant-track identifier repeated | One reader saw a long generated identifier repeat six times across the tree listing and commands, and could not tell whether it would need to be typed by hand. | Say the identifier is generated and can be copied directly from the Finder rather than typed. | 1 |
| The reference bundle, chromosome table "Primary" and "Mitochondrial" columns | One reader met these two column headers in the chromosome table output with no explanation of what marking a chromosome "primary" means. | Add one clause explaining what the Primary flag indicates. | 1 |
| The reference bundle, "an assembler" | One reader met "assembler" with no gloss, despite it being one of three named ways a reference bundle can be created. | Gloss assembler in one clause. | 2 |
| The reference bundle, dot-prefixed hidden files | One reader found a `.lungfish-provenance.json` file shown in a directory tree with no note that the leading dot makes it hidden in the Finder. | Add a clause saying dot-prefixed files are hidden by default in the Finder. | 2 |
| The reference bundle, "the `.fai` addresses sequence positions and the `.gzi` addresses compressed blocks" | One reader met "addresses" used in an unfamiliar technical sense as a verb. | Replace "addresses" with a plainer verb such as "locates" or "points to." | 2 |
| The reference bundle, manifest key list | One reader met fourteen manifest key names listed in one sentence with only `genome` explained, and admitted to skimming past it. | Cut the list down to the keys a reader would actually use. | 2 |
| The reference bundle, "The `annotations/` and `tracks/` folders exist even when empty" | One reader could not tell from the phrasing whether an empty folder is expected behavior or a sign something went wrong. | State directly that an empty folder is expected and not an error. | 3 |
| The read dataset bundle, filename with three stacked suffixes | One reader read a filename carrying three appended suffixes twice, unsure whether it was a typo rather than a deliberate naming pattern. | Note that provenance filenames are built by appending suffixes, so doubled endings are expected. | 1 |
| The read dataset bundle, "a read operation such as trimming" | One reader met "trimming" with no gloss and, from a wet-lab background, guessed it meant adapter trimming without confirmation. | Gloss trimming in one clause. | 2 |
| The read dataset bundle, "the operation to replay" | One reader met "replay" used in a specialized technical sense with no gloss. | Say plainly that the recorded operation is rerun on demand. | 2 |
| The alignment and tree bundles, "padded with gap characters" | One reader was not told the gap character is a dash until opening a file directly. | State plainly that the gap character is a dash. | 2 |
| The alignment and tree bundles, "`internalNodeCount` ... three internal nodes" | One reader's own count of nesting levels from the Newick line did not match the stated internal node count, and could not resolve the discrepancy. | Explain how internal nodes are counted for an unrooted tree. | 2 |
| The alignment and tree bundles, "positions descended from the same ancestral base line up in the same column" | One reader found a genuinely useful definition buried inside a sentence that also defines the bundle and names its folder, three ideas at once. | Split the definition into its own sentence. | 3 |
| The primer scheme bundle, "`PROVENANCE.md`" | One reader noticed this file is all-caps Markdown while every other provenance file in the chapter is a dot-prefixed JSON file, with the inconsistency unexplained. | Add a clause saying shipped primer schemes use a plain text note instead of JSON. | 2 |
| The primer scheme bundle, "The manifest uses `schema_version` rather than a version number under some other name" | One reader read this sentence three times, since it explains only what the key is not called rather than stating the key plainly. | State the key name plainly and stop there. | 2 |
| The primer scheme bundle, "**File > Import Center...**" | One reader met the chapter's only app menu path with no statement of where the menu bar is or which window must be frontmost. | State which window must be open and frontmost for this menu path to work. | 2 |
| The primer scheme bundle, "The shipped schemes have a minimal shape" | One reader found "minimal shape" vague until the next paragraph clarified it. | State plainly that the included schemes carry only three files. | 3 |
| Primer scheme bundle, "ready for the Primer Trim dialog" | One reader found this the one place in the chapter naming a dialog, but with no statement of where to find it in the app's menus. | Name the menu the Primer Trim dialog lives under. | 4 |
| The result bundles, "an XLSX workbook named for the run" | One reader guessed XLSX meant an Excel file but the chapter never confirms it. | State plainly that XLSX is an Excel file. | 1 |
| The result bundles, "deduplicated 12S reference sequences" | One reader met "deduplicated" unglossed, and 12S itself never explained anywhere despite appearing in three different bundle kinds. | Gloss 12S at its first use in the chapter. | 2 |
| The result bundles, "an `artifacts/` folder holding projections and workbooks" | One reader met "projections" with no gloss and could not guess its meaning. | Gloss projections, or drop the term. | 2 |
| The result bundles, "in a fixed category folder rather than a timestamped one" | One reader could not evaluate this contrast since no timestamped folder had been shown anywhere in the chapter. | Drop the contrast to something never shown. | 2 |
| Workflow bundles, developer-facing aside about "nobody writing that dialog" | One reader found a long sentence whose final clause, about no one writing the dialog, is aimed at developers rather than the reader. | Split the sentence and drop the developer-facing aside. | 4 |
| Provenance sidecars, "`<filename>.lungfish-provenance.json`" | One reader had to infer that the angle brackets were notation rather than literal characters to type. | Show one real filename instead of angle-bracket notation. | 2 |
| Provenance sidecars, "hostOS": "macOS 26.6.2 (arm64)" | One reader met "arm64" unglossed in the example. | Drop the term, or gloss it briefly. | 2 |
| Provenance sidecars, JSON block shows fewer keys than the sentence lists | One reader counted seven keys in the shown JSON block against ten named in the surrounding sentence, and could not find two of the named keys anywhere in the block. | Note that the block is shortened and the remaining keys are described afterward. | 3 |
| Sharing and inspecting bundles, "The recipient unzips it and drops the folder into a project" | One reader could not tell where "drops the folder" means: into the app window, into the project folder in the Finder, or onto an icon. | Name the specific drop target. | 2 |
| Next, "See Power User Notes for the exact flags LGE passes" | One reader noticed "flag" is used in two different senses in this chapter, the registry's read and write flags and command-line flags, and the collision was noticeable. | Use "option" for command-line flags to avoid the collision with registry flags. | 2 |
| The format registry, "assigns a category that decides where the format appears in a file picker" | One reader found this single sentence chains four separate actions together, making it hard to see that one registry entry does all four. | Split the sentence after the extensions clause. | 3 |
| Reference bundle, "`sequence.fa.gz` ... `.fai` addresses sequence positions and the `.gzi` addresses compressed blocks" | One reader found three files described here for one sequence, and had to reread the earlier FASTA index section to realize this was an additional compression-specific index rather than a contradiction. | Add a back-reference to the FASTA index section explaining the extra file. | 4 |

One hundred thirty-eight merged rows total, drawn from 333 source rows across the four reader reports: 68 rows from reader 1, 111 from reader 2, 87 from reader 3, and 67 from reader 4.

## Consensus

Forty-seven merged rows were hit by three or more readers.

- The chapter never states how to open Terminal or which folder to work from, so no printed command can be run as written.
- Numbers throughout the chapter, the 512-megabase limit, mapping-health percentages, QUAL scores, GC content, mean quality, and parsimony-informative counts, are given with no anchor for whether a reader's own value is normal.
- The format registry table says LGE cannot write BAM, directly contradicting the alignment section, which says every alignment reaching a viewport is a sorted BAM LGE produced.
- Readers cannot tell whether lowercase bases in their own file are a problem, and the chapter never says whether the viewport shows lowercase differently.
- Byte offset is never glossed anywhere in the chapter.
- The first command in the chapter, samtools faidx, is incomplete and never says whether samtools ships with LGE.
- The Q30 quality anchor is never connected to the actual characters shown in the example quality line.
- Reading-frame phase is never glossed despite appearing as a column value.
- Pool, the primer term, is never glossed.
- The BED and bedGraph zero-based, half-open explanation is abstract and gives no worked example a reader can check against.
- CRAM's dependency on its exact reference file is stated with no warning about what happens if that reference is lost.
- snake_case and camelCase manifest key styles are used as if already familiar vocabulary.
- ONT is never expanded anywhere in the chapter.
- CZ ID, normalized, and classifier schema arrive together in one table row with none of them explained.
- PairingMode and the Pairing setting override are introduced together in a way that sounds like the reader's own choice is being ignored.
- N50 is never glossed anywhere in the chapter.
- Materialize is used as a verb before any gloss, and its only route is a Terminal command with no stated menu alternative.
- PHA4GE is never expanded and its effect on metadata fields is never stated.
- Stderr is used without a gloss.
- The Project Destination path bug reads like a description of intended behavior rather than a known defect.
- Nextflow is named with no gloss.
- MinimumDepth as caller-default gives no way to find the actual value a tool used.
- The checksum and byte size claim about both directions is unclear after multiple readings.
- The missing schema_version key is stated as a correction to an expectation readers never had.
- The zip -r sharing command is Terminal-only when a Finder route exists and is not given first.
- The OCI layout tarball paragraph packs several unglossed terms together with no signal for who the format is meant for.
- The broken-command notice for Preview 2026.9.13 arrives after the paragraph teaching the now-unusable command, wasting the reading.
- A shortened filename late in the chapter does not match the full identifier used earlier for the same file.
- Terms across many passages are glossed only much later in the chapter rather than at their first use, despite the chapter's opening promise that sections stand alone.
- Fixture is used repeatedly with no gloss, and one reader could not find where the example file actually lives.
- The tilde in the demo project path is never glossed as the reader's home folder.
- Routine is used as unglossed programming jargon that obscures the point being made about write flags.
- Registry identifier and full descriptor entry arrive together with no statement of what the difference means practically.
- Five compression suffixes are listed with no statement of which are common and which are exotic.
- The GenBank-location attribute example runs off the page width with no visual break between attributes.
- Sorting and indexing a BAM are treated as self-evident steps with no explanation of what sorted means or why order matters.
- Flagstat output shows primary, secondary, supplementary, duplicates, and a plus-zero notation with none of them glossed.
- Contig is never glossed anywhere in the chapter.
- Four INFO codes are shown in a variant example with none expanded, though DP is the one most readers would want explained.
- Nothing points the reader back to the fileformat line as the way to check their own VCF version.
- The Newick tree is printed as one unbroken line, making it impossible to visually locate a specific branch or follow the nesting as instructed.
- The long branch length on one tree branch is never explained as expected or unusual.
- The claim that no single key appears in every manifest is stated with no practical rule for what to do about it.
- The source.original filename appears in two bundle listings with no description of what it holds.
- The primer scheme bundle claims eight schemes but names the four ARTIC versions only by a range, not individually.
- Haplotype is used repeatedly with no gloss and no stated consequence for the reader.
- Graph is used in the workflow bundle sections to mean a flow of connected steps, and multiple readers read it as a chart on first pass.
