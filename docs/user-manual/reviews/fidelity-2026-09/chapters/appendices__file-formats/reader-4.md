# Reader 4 report, appendices/file-formats

Persona. An undergraduate who used Geneious for a semester in one course. Comfortable with a graphical sequence viewer. Expects every operation to have a dialog. Has never opened a terminal.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "how to look inside it from the Terminal application" | This is the whole chapter's method and I have never opened Terminal. In Geneious I would look inside a file by double-clicking it. Nothing here tells me whether there is a way to see any of this from inside the window instead. | Add one sentence saying which of these inspections have an in-window equivalent and which genuinely require Terminal. |
| What it is, "which is the macOS program where you type commands instead of clicking" | Glosses what Terminal is but never says how to open it, so I cannot run a single command block in this chapter. | Name the path once, Applications then Utilities then Terminal. |
| What it is, "the demo project at `~/Desktop/lge-docs/LGE Manual Demo.lungfish`" | The `~` is not explained and I do not know if this demo project exists on my machine or is something I have to make. | Gloss `~` as your home folder and say in one sentence where the demo project comes from. |
| Format registry, "assigns a category that decides where the format appears in a file picker" | I could not tell what this changes for me. Does a wrong category mean my file will not show up when I go to open it. | Say in one sentence what a user notices when a format is in one category rather than another. |
| Format registry, "a write flag of false in the registry" | The table column says "LGE writes" with Yes and No, but the prose says "flag of false". I read this twice to check they were the same thing. | Use the table's words, No, in the prose. |
| Format registry, "BAM ... LGE reads Yes, LGE writes No" | This alarmed me. The mapping section later says every alignment becomes a sorted BAM, so LGE clearly produces BAM files. I could not reconcile the table with that until the alignment section. | Add a footnote on the BAM row pointing to the alignment section's explanation. |
| Format registry, "convert it to bedGraph outside LGE first" | I do not know what tool does this and there is no name given, so this step cannot be performed from the text. | Name the converting tool, or say the manual does not cover it. |
| Format registry, "Three more formats have registry identifiers without a full descriptor entry" | "registry identifier" and "full descriptor entry" are internal terms I have no way to picture. The sentence is about the app's insides, not about my file. | Say what I observe, that these files can be imported but not opened as a track. |
| Format registry, "treats the file as an import candidate" | "import candidate" is not explained and I could not guess whether that means the import will work. | Replace with the plain outcome for the user. |
| Sequence formats, "the human chromosome 20 slice from the `hg002-chr20` fixture" | "fixture" is a term from the manual's own machinery, not something a student knows. It appears again for `primate-mito`. | Gloss it once as an example file that ships with the manual. |
| Sequence formats, "They mark repeat-masked regions" | I do not know whether Lungfish Genome Explorer shows lowercase differently on screen. Geneious colours bases, so I would expect to see something. | Say in one sentence whether the viewport distinguishes lowercase. |
| Sequence formats, "the byte offset at which its sequence starts" | "byte offset" is not glossed and is the only column I could not picture. | Gloss it once as the count of characters from the start of the file. |
| Sequence formats, "Run `samtools faidx` on a FASTA to create one." | This is an instruction I cannot perform. There is no Terminal for me, and no dialog named. Also samtools is never introduced as a thing that comes with the app or a thing I install. | Say once, early, whether samtools ships inside LGE and whether importing a FASTA makes the `.fai` for me. |
| Sequence formats, "30 means roughly a one in a thousand chance of being wrong" | I can judge 30 now, but the quality line in the example is characters like `0<0DD=1<` and I cannot get from those characters to a number. | Add half a sentence saying the characters encode the numbers and that LGE plots them for you. |
| Sequence formats, "the fourth encodes one Phred quality score per base as a single character" | Reading the example, the quality line looked mostly like low-quality junk to me and I had no way to judge whether that read was good or bad. | Say what a typical good-looking quality line looks like, or say not to judge it by eye. |
| Annotation formats, "a reading-frame phase" | Not explained, and it is the one column of the nine I could not describe back. | Gloss it in half a sentence. |
| Annotation formats, "`_lf_raw_genbank_location=52070..53062`" | The example line was hard to scan because the attributes run together. I read it three times to find where one attribute ended. | Consider breaking the attribute column onto its own line under the example. |
| Annotation formats, "a semicolon-separated list of attributes" | The campaign's own prose rules ban semicolons, so seeing them everywhere in the examples briefly made me think the examples were malformed. Minor, but I did pause. | No fix needed if intentional. Noting the double take. |
| Annotation formats, "the fifth gives its pool" | "pool" is not glossed. I have never done amplicon work and cannot tell if a pool is a number I choose or something the vendor sets. | Gloss pool in half a sentence. |
| Annotation formats, "BED and bedGraph are zero-based and half-open" | This is the clearest thing in the chapter and yet it stops short of what I need. If I type a coordinate I read off a BED file into the LGE search box, do I add one or not. | Say explicitly what to type when you copy a coordinate out of a BED file. |
| Alignment formats, "needs that exact reference to be read back" | I could not tell what happens if I open a CRAM in LGE without its reference. Does it fail, does it ask me, does it show nothing. | Say what the app does in that case. |
| Alignment formats, "The mapping pipeline runs any tool that emits SAM through `samtools sort` and `samtools index`" | This describes the app's internals rather than anything I do, and it uses two command names as if I would recognize them. | Rewrite from my side, that LGE always leaves a sorted indexed BAM. |
| Alignment formats, "the roughly 512-megabase limit a `.bai` can address" | I have no idea which of my references are over that. Human chromosome 1 is about 250 megabases, so I guessed I am fine, but the text made me do that arithmetic. | Add the reassurance that no human or macaque chromosome reaches it. |
| Alignment formats, "```bash samtools idxstats "LGE Manual Demo.lungfish/..."```" | I cannot run this. Worse, the path is relative and the text never says which folder I would have to be in, so even a friend at a terminal could not run it as printed. | Show the command with a full path, or say which folder to start from. |
| Alignment formats, "90,990 of 91,203 reads mapped ... what a healthy targeted mapping run looks like" | I want the rule, not the one example. Is 90 percent healthy, is 50 percent a problem. I cannot judge my own number from this. | Give a rough threshold under which to worry. |
| Variant formats, "A mapped count near zero when the read file is large" | "near zero" is the only guidance and it is the extreme case. Everything between that and 99 percent is unaddressed. | Same fix as above, one threshold. |
| Variant formats, "`50 PASS platforms=5;platformnames=...`" | The QUAL of 50 in the first example and 225.417 in the second look like different scales and I could not tell whether either is good. | Say that QUAL scales differ by caller and is not comparable across files. |
| Variant formats, "LGE reads VCF versions 4.0 through 4.4" | I do not know how to find out which version my VCF is without opening it in something. | Say the version is the first line of the file and name where to see it in the app. |
| Variant formats, "converted with an outside tool before import" | Unnamed tool again, so the step cannot be performed. | Name the tool. |
| Variant formats, "a `.db` SQLite sidecar that the Variants tab of the table drawer uses" | "SQLite" and "table drawer" both arrive unexplained. I do not know where the table drawer is in the window. | Gloss SQLite in three words and point at the chapter that shows the table drawer. |
| Variant formats, "The GATK entries of the Call Variants dialog write to `variants/gatk/<track-id>.vcf.gz`" | This says two routes produce different shapes but not what I should do about it. Does a GATK track behave differently for me, or is this only a note for whoever opens the folder. | Say whether the difference is visible in the app. |
| Variant formats, "`lungfish-cli bundle create --variant` writes a `.bcf`" | The command line appears here without ever having been introduced as an alternative to the window. As a dialogs-only user I did not know LGE had one. | Introduce the command line once in What it is, and say a reader can skip every code block. |
| Tree format, "0.0299 on the cynomolgus macaque branch means about 3 changes per hundred aligned positions" | The Newick string is one unbroken line and I could not find 0.0299 in it without counting characters. | Point at which name it sits after, or wrap the example. |
| Tree format, "the human and chimpanzee sit outside that group" | Read twice. The prose says human and chimp sit outside, but they are the first two names in the string, which looks like inside to me. | Add half a sentence saying position in the text does not mean position in the tree. |
| What a bundle is, "macOS Finder draws as a single icon" | This is the biggest difference from Geneious and it is not flagged as one. In Geneious a result is a row in a folder inside the program. Here my results are files on my disk that pretend to be documents. | State the contrast once, that LGE stores results as real folders on disk rather than inside a program database. |
| What a bundle is, "the manifest's shape is not shared across bundle kinds ... There is no single key that every manifest carries" | I read this paragraph twice and still do not know what it asks of me. It reads like a complaint about the app rather than an instruction. | Say plainly that this matters only if you open manifests yourself. |
| What a bundle is, "snake_case keys such as `format_version` ... camelCase keys such as `schemaVersion`" | Neither snake_case nor camelCase is glossed. I could infer it from the examples but had to stop and do so. | Gloss both in one clause. |
| What a bundle is, "Only three bundle extensions have identifiers in the format registry" | Same internals problem as earlier. Nothing follows from it for me. | Cut, or say what a user would notice. |
| Bundle table, "`.lungfishgenotype` ... ONT genotype calls" | ONT is never expanded anywhere in the chapter. | Expand at first use. |
| Bundle table, "`.lungfishtax` ... normalized into LGE's classifier schema" | "normalized" and "classifier schema" are both unexplained and the row is the only place I meet either. | Simplify the row to what the bundle holds. |
| Reference bundle, "An assembly is the same format in a different place" | Read twice. I expected assemblies to be their own kind of thing, and the sentence's point, that they show up in reference pickers, arrives only at the end. | Lead with the consequence. |
| Reference bundle, "`sequence.fa.gz` ... `.fai` addresses sequence positions and the `.gzi` addresses compressed blocks" | Three files for one sequence, and earlier the chapter said a FASTA index is one file. I had to reread the earlier section to see this was an extra index for compression. | Add a back-reference to the FASTA index section. |
| Reference bundle, "Variant tracks are named by a generated track identifier ... a folder listing does not tell you which caller produced which track" | So how do I find out. The paragraph names the problem and stops. | Name the place in the app or the file that says which caller made a track. |
| Reference bundle, "```bash lungfish-cli bundle info ...```" | Called "the friendlier way", which stung. It is a terminal command, so it is the unfriendly way for me. The manifest keys just above are the only route I have and they are raw JSON. | Say whether the app shows this same information anywhere. |
| Read dataset bundle, "the meta sidecar records `pairingMode: interleaved`" | Interleaved is glossed, which helped, but I could not tell whether this matters to me. If I hand LGE two files and get one back, will an outside tool still accept it. | Say whether the bundle can give the two files back. |
| Read dataset bundle, "`gcContent`: 0.444 ... `meanQuality`: 26.52" | I now know what a Phred score is but not whether 26.52 is acceptable, and 0.444 GC content is a number I cannot judge at all. | Say what range is expected for each on human or macaque data. |
| Read dataset bundle, "`n50ReadLength`" | Not glossed, and it is the one statistic in that list I could not describe. | Gloss it once. |
| Read dataset bundle, "To write the full reads out, materialize the bundle." | The only route given is a terminal command. As a dialogs-only user I am stuck, and the chapter's own promise is that virtual bundles are normal, so this is a step I would actually hit. | Name the menu command that materializes, or say there is none. |
| Read dataset bundle, "aligned to the PHA4GE specification for pathogen sample descriptions" | PHA4GE is never expanded and I do not know if this constrains what I may type in the metadata fields. | Expand once and say whether it limits my entries. |
| Read dataset bundle, "turns a folder of them into a TSV file you can submit to NCBI's BioSample portal" | Terminal only again, for something that sounds like a routine task with a dialog. | Say whether an equivalent exists in the window. |
| Alignment and tree bundles, "5,053 variable sites, and 2,709 parsimony-informative sites" | Parsimony-informative is not explained and I cannot judge either count. | Gloss the term and say what the counts tell you. |
| Alignment and tree bundles, "`isRooted` false" | Not explained, and rooted versus unrooted is exactly the thing I remember being confused about in class. | Gloss in half a sentence. |
| Primer scheme bundle, "ready for the Primer Trim dialog" | This is the one place a dialog is named and it is not said where to find it. | Name the menu it lives under. |
| Primer scheme bundle, "four ARTIC versions covering nCoV-2019 V3 through SARS-CoV-2 V5.3.2" | Four schemes named by a range rather than listed, so I cannot check whether the one my lab uses is present. | List all eight. |
| Result bundles, "the stderr logs of every tool the run invoked" | "stderr" is unexplained. | Gloss as the tool's error output. |
| Result bundles, "the import sheet's Project Destination readout names a path under `Analyses` that nothing writes to, so look under `Classifications`" | I read this three times. A screen telling me the wrong place is exactly the kind of thing I would trust and then get lost by. | Flag it as a known defect in its own short note rather than a clause. |
| Result bundles, "Haplotype definitions are files inside that folder with the suffix `.lungfishhaplotypedef.json`, not a bundle format of their own" | The distinction between a file and a bundle format has no consequence stated for me. | Cut or say what it changes. |
| Workflow bundles, "such as a Nextflow script" | Nextflow is not explained. | Gloss in three words. |
| Workflow bundles, "which is why a package that declares a reference and a reads input shows a reference picker and a reads picker without anyone writing that dialog" | Long sentence, read twice. The clause about nobody writing the dialog is about the developers, not me. | Split the sentence and drop the developer aside. |
| Provenance sidecars, "`minimumDepth` as `caller-default`" | I could not tell what actual number was used, which is what I would want for a methods section. | Say whether the real number is recoverable. |
| Provenance sidecars, "so the checksum and byte size claim holds for both directions" | I could not work out what claim or what directions. This sentence appears to be arguing with something not in the chapter. | Rewrite as what the arrays record. |
| Provenance sidecars, "There is no `schema_version` key in a provenance sidecar." | Told what is absent without ever being told why I would look for it. | Cut, or explain who expects that key. |
| Sharing bundles, "```bash zip -r HBB.lungfishref.zip HBB.lungfishref```" | Terminal, but the very next sentence tells me Finder treats it as one document. In Finder I would right-click and choose Compress, which is not mentioned. | Give the Finder route first and the command second. |
| Sharing bundles, "a deterministic OCI layout tarball, which is the standard directory shape a container registry uses" | Container registry, tarball, blob, and SHA-256 digest all arrive in one paragraph. None mean anything to me and I skipped the paragraph. | Say who this is for in the first clause so I can skip it knowingly. |
| Sharing bundles, "In Preview 2026.9.13 that command cannot actually be run." | An entire paragraph explains a feature and the next paragraph says it does not work. I would rather have been told first. | Move the defect note to the start of that paragraph. |
| Sharing bundles, "`bcftools view -H chr20_10.0-10.5Mb.lungfishref/variants/vc-6edd1356.vcf.gz`" | This filename is shorter than the one printed earlier in the chapter, which was `vc-6edd1356-e900-470c-95a3-eb1e4b5ffcfd.vcf.gz`. I could not tell if the short form works or is a typo. | Make the two match. |
| Whole chapter, dialogs | Coming from Geneious I expect every operation to have a dialog, and this chapter answers almost every "how do I look at this" with a command. I could not tell whether that is because no dialog exists or because the chapter simply prefers commands. | Add one sentence in What it is saying which parts of the chapter a reader who never opens Terminal can still use. |

One thing I learned. A bundle is an ordinary folder that macOS only draws as one icon, so my results live on my disk rather than inside the program the way Geneious keeps them.

One thing I still could not do. Look inside any of these files, because every inspection route in the chapter is a Terminal command and I have never opened Terminal.

The sentence I liked most. "Finding a `preview.fastq` where you expected a full file is correct rather than a truncated result."
