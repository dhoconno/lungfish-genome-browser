# Reader 1 report, 09-genotyping/02-running-genotyping

Persona: sophomore, one intro genetics course, never opened a terminal, never used sequencing software.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "a folder called a genotype result bundle" | I do not know what makes a folder a bundle here versus a normal folder. The idea is only explained much later, under Before you start, where I learn LGE treats a bundle as one row in the sidebar. By then I had already pictured a folder I could open. | Move the one-sentence gloss of "bundle" to its first use here. |
| What it is, "with no substitutions anywhere along it" | "Substitution" was never glossed. My genetics course used it for a base swapped for another base, but the next sentence says "differ by one base", so I could not tell whether these are the same thing or two different tests. | Use one word for one idea and gloss it once. |
| What it is, "23.6 percent of the reads survived, and that is a healthy number" | I have no way to judge this. I am told it is healthy but not why a quarter is good when three quarters were thrown away. The reason arrives two thousand words later under What good looks like. | Add the reason here, that the filter is deliberately strict, in the same sentence. |
| What it is, "far smaller than a mapping workflow would report" | I have not read about a mapping workflow and do not know what number it would report, so the comparison told me nothing. | Drop the comparison or name the chapter that covers mapping. |
| Why you would do this, "present different fragments of the same antigen" | "Antigen" and "present" are used without gloss. I know antigen loosely, but "present" as a verb doing something specific to a fragment is a technical usage I had to read twice. | Gloss "present" in half a sentence. |
| Why you would do this, "a set of PCR primers that copy one short defined stretch" | "PCR" and "primer" are never spelled out or glossed anywhere in the chapter, and primers come back as a real setting in step 6 and three Settings entries. I knew PCR from lecture but not what a primer file would contain. | Gloss "primer" at this first use, since later settings depend on it. |
| Before you start, "the Williams MiSeq genotyping project" | This is the whole worked example, and I cannot get it. The text says so honestly, but then every number in the chapter comes from data I cannot open, so I could not follow along at any step. | Say up front, in the What it is section, that the example is not downloadable. |
| Before you start, "imported as one .lungfishfastq bundle per animal" | I could not perform this step from this text. It sends me to another chapter, which is fine, but I did not know whether I need to finish that chapter before opening the dialog or whether the dialog can import for me. | Say plainly that importing must be finished first. |
| Before you start, "970 rhesus allele sequences... Most of them are 156 bases long" | I could not judge any of these numbers. Is 970 a big library or a small one? I have nothing to compare it against. | Say whether 970 is the whole catalogue or a subset. |
| Before you start, "That 244 figure matters in a way step 3 explains." | I had to hold an unexplained number in my head across two sections. When step 3 arrived I had forgotten which number was the class I one and which was DRB. | Repeat the 244 and 156 figures in step 3 rather than referring back. |
| Before you start, "the class I amplicon" and "the DRB class II amplicon" | Class I, class II, and DRB appear here with no gloss at all. Chapter 1 may cover them, but this chapter treats them as known and then makes DRB the subject of an entire failure mode in step 3 and a check in What good looks like. | Gloss class I, class II, and DRB at this first use. |
| Before you start, "the read-mapping plugin pack" | I do not know what read mapping is at this point. The word "mapping" is used as if familiar here, in step 3, and in step 5, and is only loosely explained under minimap2 in Settings. | Gloss "mapping" the first time it appears. |
| Before you start, "Check both in Tools > Plugin Manager... (Cmd-Shift-B)" | I could not perform this. I do not know what I am checking for, what an installed pack looks like versus a missing one, or what to click to install one. | Say what the manager shows for an installed pack and what to click otherwise. |
| Step 1, "Selecting several at once is the usual case" | I could not perform this. Clicking several rows in a list usually needs a modifier key, and the chapter is careful elsewhere to explain Cmd shortcuts, so its silence here left me guessing. | Name the click, for example Shift-click or Cmd-click. |
| Step 2, "If LGE found any reference bundles inside your project" | I had not put any reference into my project and the chapter never told me to. Step 2 assumes the file is already there, but Before you start only says the library is "handed to you". | Add a sentence saying how the handed-over file gets into the project. |
| Step 3, "with 2x251 chemistry a single MiSeq mate covers roughly 198 bases of insert" | This sentence stopped me completely. I do not know what 2x251 means, what chemistry means here, what a mate is, or what an insert is, and I could not follow the arithmetic that the whole paragraph rests on. | Gloss mate and insert, and say 2x251 means 251 bases read from each end. |
| Step 3, "the ONT path otherwise" | ONT is used as an abbreviation before it is expanded. The What it is section said "Oxford Nanopore", but never wrote the letters ONT next to it, so I did not connect them until step 6. | Expand ONT at its first use. |
| Step 3, "the fix is in the bundles rather than in this window" | I could not act on this. It tells me the fix is elsewhere but not what to do to the bundles. | Name the fix or link the chapter that covers it. |
| Step 4, "the AI preset segment is greyed out unless API access has been configured" | "API access" is not explained and I would not know how to configure it or whether I should. | Say this is optional and name where it is configured. |
| Step 4, "Threads starts at your machine's processor count" | I do not know my machine's processor count or whether the number shown is right. | Say the shown number is already correct and needs no action. |
| Step 5, "Cmd-Shift-P" | The panel is opened by a shortcut, but I do not know whether it opens by itself when I click Run or whether I have to press this. The sentence reads as both. | Say whether the panel opens on its own. |
| Step 5, "took 329 seconds of wall time" | "Wall time" was not glossed. I guessed it means real elapsed time but had to stop and think. | Say "329 seconds of real time" or gloss the phrase. |
| Step 5, "the filtering step... took 14.9 seconds of the 55" | I could not judge why this detail matters to me. It felt like a fact about the author's computer with no action attached. | Say what a much longer filtering step would mean, or cut. |
| Step 6, "which the full-length-mhc-genotyping pack supplies at version 0.6.3" | I do not know whether I need to check this version or whether it just happens. | Say the version is informational. |
| Step 6, "pbAA is a separate operation whose saved output this workflow can reuse" | I do not know what pbAA is and the sentence explains only what it is not. It stopped me because it seemed to describe a feature I might need. | Say pbAA is covered elsewhere, or cut the sentence. |
| Step 6, "They start at 2000 and 4000 bases." | I could not judge these. Whether my amplicon falls between them depends on knowing my amplicon length, which the chapter never tells me how to find out. | Say where to find your amplicon length. |
| Step 7, "lands beside the first as amplicon-genotyping_1 rather than overwriting it" | I had to read this twice because "named category folders rather than the timestamped per-run folders most other tools use" compares against behaviour I have not seen. | Cut the comparison and keep the consequence. |
| Settings, lead-in "ends with the command-line flag that does the same job" | Every entry then finishes with a sentence I was told to skip. Reading seventeen entries each with a discardable last sentence made the section tiring. | Put the flags in a small table instead. |
| Settings, "Min Reads... Be aware of what it does not do in the release tested here" | I had to jump forward to another section to learn the setting is broken, and the pointer does not say what it does not do. This is the one place I would have wanted the bad news immediately. | State plainly here that the setting does not filter on a Genotype only run. |
| Settings, "Threads... it is shared across the clustering and mapping steps" | I could not tell whether this means I should set it differently on that route. | Say no change is needed. |
| Settings, the command-line block, "--mode... the deprecated ont-barcode-demux" | I was told in the Settings lead-in that I could skip command-line sentences, then met eighteen paragraphs of flags before Reading the results. I did not know whether I was allowed to skip the whole block. | Move this block into the command-line section at the end. |
| Settings, "--cdna-threshold... treated as a cDNA sequence rather than a whole genomic one" | cDNA is not glossed and I could not tell whether this affects me. | Gloss cDNA or move the flag out of the reader path. |
| Reading the results, "23.9 percent of 2,854,092 input reads, or 682,927 retained reads" | The What it is section said 23.6 percent, this says 23.9, and I could not tell whether I had misread something or whether these are two different runs. It took a second read to see one is the plate and one is the reproduction. | Label the two figures in both places. |
| Reading the results, "The remaining 682,928 alignments passed." | This is 682,928 here and 682,927 two paragraphs above. I stopped to check whether I had miscounted. | Reconcile the two numbers. |
| Reading the results, "which on a MiSeq plate usually means merging did not happen" | I do not know what I would do about it. The chapter says merging is automatic, so being told it did not happen leaves me stuck. | Say what to do when merging did not happen. |
| Reading the results, "a sorted and indexed BAM of just the retained reads" | BAM, sorted, and indexed are all unglossed. I could not tell whether this is a file I would ever open. | Gloss BAM in half a sentence and say the next chapter uses it. |
| What good looks like, "adapter dimers and off-target product" | Neither term is glossed. I guessed these are laboratory junk but could not be sure. | Gloss both in one clause. |
| What good looks like, "A sample with fewer than a hundred retained reads has failed" | This is the clearest threshold in the chapter, but the paragraph before says to judge against your own assay rather than a fixed number. The two rules seemed to contradict each other. | Say the hundred-read rule is the exception. |
| What good looks like, "Scan the allele names in the report for DRB rows" | I could not perform this. I do not know where in the report the allele names are, or whether "report" means the CSV, the workbook, or the viewport. | Name the sheet or view to look at. |
| What good looks like, "In Preview 2026.9.13" | I do not know which version I have or how to check. | Say where the version is shown. |
| What good looks like, "each of its 67 tool invocations with its own exit status" | "Exit status" is unglossed terminal vocabulary in a section I was told is not the command-line section. | Gloss or cut. |
| Haplotype analysis (placeholder), whole section | A heading with a promise and no content stopped me because I expected the thing I had read about in step 4 and Settings. | Fold this into the Next section as one sentence. |
| On the command line, "hardened file-publishing path that refuses to write inside a world-writable directory" | I could not parse this at all. World-writable, hardened, and file-publishing path are three unknowns in one clause. | Reduce to the advice, which is to keep the output inside your project or home folder. |

Three closing lines.

The one thing I learned: two reads from the same DNA fragment can be joined into one longer piece, and skipping that join can silently erase a whole class of genes from the results.

The one thing I still could not do: get a library file into my project, because the chapter tells me a colleague hands it to me and then assumes it is already inside the project when I open the dialog.

The sentence I liked most: "The failure would be silent, since a locus with no reads simply has no rows."
