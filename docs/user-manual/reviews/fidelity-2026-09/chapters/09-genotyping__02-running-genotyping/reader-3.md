---
role: undergraduate-reader
reader: 3
persona: pre-med student, English as a second language, strong biology vocabulary, no terminal experience
chapter: 09-genotyping/02-running-genotyping
date: 2026-09-07
---

# Reader 3 report on Running Amplicon MHC Genotyping

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "The word that carries the most weight" | Idiom. I read it twice before I understood it means "the most important word". A word does not have weight. | Say the most important word is *matches*. |
| What it is, "with no substitutions anywhere along it" | Substitution is not glossed here. In my genetics course a substitution is a kind of mutation, but here it seems to mean a base that differs between read and library. I was not sure they are the same thing. | Gloss substitution at first use as one base that differs from the library sequence. |
| What it is, "23.6 percent of the reads survived, and that is a healthy number" | I cannot judge this. Nothing before this sentence told me what range is normal, so I only believe it because the text says so. The explanation arrives many pages later in What good looks like. | Say here that the filter is deliberately strict and roughly a quarter surviving is the normal range for this panel. |
| What it is, "far smaller than a mapping workflow would report" | I have not read a mapping chapter yet and do not know what number a mapping workflow reports, so the comparison told me nothing. | Give the comparison number, or drop the comparison. |
| What it is, "So the practical shape of the work is short." | Read twice. "Practical shape" and "is short" together are a metaphor I had to translate. | Say the work has three steps. |
| Why you would do this, "so genotyping the colony before the study starts is routine" | Colony is not explained. I guessed it means the group of monkeys held at the facility, but I was not sure. | Gloss colony at first use. |
| Why you would do this, "a set of PCR primers that copy one short defined stretch" | Primer is used here and again in step 6 and in three Settings entries, and it is never glossed even though the chapter glosses easier words like read and locus. | Gloss primer at first use. |
| Why you would do this, "the sequencer reads that stretch very deeply" | Deeply is used as a technical word here without explanation, and depth is not in the glossary list at the top. | Gloss sequencing depth at first use. |
| Why you would do this, "which chapter 1 calls an allele target" | The chapter is called "What Is MHC Genotyping" everywhere else. "Chapter 1" made me stop and check whether a different chapter was meant. | Name the chapter as it is named in the Next section. |
| Before you start, "the Williams MiSeq genotyping project" | I cannot perform any step with real data, because this project is not downloadable and no substitute is offered. The chapter admits this but leaves me with nothing to open. | Point to a downloadable fixture, or say plainly which earlier chapter's data can be substituted. |
| Before you start, "imported as one `.lungfishfastq` bundle per animal" | I have not imported anything. The link is given but the text does not say whether I must finish that chapter first. | Say the import must be finished before this chapter's steps. |
| Before you start, "970 rhesus allele sequences ... Most of them are 156 bases long" | Numbers I cannot judge. Is 970 a complete catalogue or a subset? Nothing tells me whether a smaller library would be a problem. | Say what a complete library for a species looks like. |
| Before you start, "That 244 figure matters in a way step 3 explains." | Read twice. The sentence points forward but does not say why, so I held the number in my head for two pages without knowing what to do with it. | Say in one clause that a single MiSeq read is too short to cover 244 bases. |
| Before you start, "The miSeq operation needs the `read-mapping` plugin pack" | I do not know how to tell, inside Plugin Manager, whether a pack is installed or not. The text says to check but not what to look for. | Say what an installed pack looks like in the Plugin Manager window. |
| Before you start, "Cmd-Shift-B" | The three-key shortcut is not explained. Cmd-N was explained one paragraph earlier, so I expected the same help and did not get it. | Reuse the same explanation form for the three-key shortcut. |
| Procedure, "Steps 1 through 5 walk the miSeq amplicon route" | "Walk the route" is a figure of speech. I understood after a second reading. | Say steps 1 through 5 cover the miSeq amplicon workflow. |
| Step 1, "Selecting several at once is the usual case" | The text does not say how to select several. I assume a key must be held, but which one is never stated, and I have never used this app. | Say to hold Shift or Command while clicking. |
| Step 1, "Merged means one report file, not pooled reads." | Pooled is not explained. I know pooling from the wet laboratory, where samples are physically mixed, and I was not sure the same meaning applies to a data file. | Gloss what pooled reads would have meant. |
| Step 2, "The Workflow Operations dialog opens" | I do not know what "the Workflow Operations dialog" is. This is its first mention and no earlier chapter is cited for it. | Say in half a sentence that this is the standard window LGE uses to configure and start an operation. |
| Step 2, "picking `26128_ipd-mhc-mamu-2021-07-09.lungfishref` from that menu" | I cannot perform this. That file exists only in the author's project, and the text does not say what my own menu would show or what to do if it is empty. | Say what to do when the Project Reference menu is empty. |
| Step 3, "That caption is a readout rather than a control" | Readout and control are used as interface words, not biology words, and neither is explained. I first read "control" as an experimental control. | Say the caption only reports what LGE decided and cannot be changed. |
| Step 3, "the fix is in the bundles rather than in this window" | I could not perform this. It does not tell me what to change in a bundle or where to do it. | Name the chapter that fixes a bundle's platform metadata. |
| Step 3, "with 2x251 chemistry a single MiSeq mate covers roughly 198 bases of insert" | This sentence stopped me completely. 2x251, chemistry, mate, and insert are four unexplained terms in one clause, and I do not see how 251 becomes 198. | Gloss insert and mate, and say why the usable length is shorter than 251. |
| Step 3, "comfortably more than the 156-base ... comfortably less than the 244-base" | Read twice. The two comparisons run in opposite directions in one sentence and I lost track of which amplicon was safe. | Split into two sentences, one per amplicon. |
| Step 3, "The failure would be silent, since a locus with no reads simply has no rows." | I understood the words but not how I would ever notice. The actual check only appears much later in What good looks like. | Point forward to the DRB check by name. |
| Step 4, "the AI preset segment is greyed out unless API access has been configured" | API access is not explained, and I do not know whether I am supposed to configure it or ignore it. | Say a first-time reader can ignore this segment. |
| Step 4, "Threads starts at your machine's processor count" | Thread and processor count are not explained. I do not know whether a bigger number is better or dangerous. | Gloss thread and say the starting value is right for most people. |
| Step 4, "You can run now and read the Settings section afterwards." | Read twice. I was unsure whether this permits me to skip step 5, since step 5 is the running step. | Say the defaults are ready, so continue to step 5. |
| Step 5, "Cmd-Shift-P" | Same problem as Cmd-Shift-B. The three-key form is never explained anywhere in the chapter. | Explain the three-key form once, at its first appearance. |
| Step 5, "took 329 seconds of wall time" | Wall time is not explained. I guessed it means real elapsed time rather than processor time, but this is a computing term I have never met. | Gloss wall time, or write elapsed time. |
| Step 5, "the filtering step ... took 14.9 seconds of the 55" | I cannot judge this. Nothing says whether a long filtering step would mean something is wrong. | Say these numbers are only to set expectations. |
| Step 6, "which the `full-length-mhc-genotyping` pack supplies at version 0.6.3" | I do not know why the version matters or what to do if my copy shows a different one. | Say no action is needed, the pack supplies the version LGE expects. |
| Step 6, "pbAA is a separate operation whose saved output this workflow can reuse" | Read twice and still unclear. It answers a question I had not asked, and no chapter is cited for pbAA. | Either link the pbAA chapter or remove the sentence. |
| Step 6, "They start at 2000 and 4000 bases." | I cannot judge these. My amplicon length is not something I know, and the text never says how to find it. | Say where to find your amplicon length, for example from the panel's documentation. |
| Step 7, "When the Operations panel row turns green" | This is the first mention that green means finished. I do not know what other colours mean, especially a failure. | Say what a failed run looks like in the row. |
| Step 7, "named category folders rather than the timestamped per-run folders most other tools use" | Read twice. Three modifiers stack before a noun, and I do not know what the other tools do. | Say results are grouped in one folder per workflow instead of one folder per run. |
| Settings, "Those closing sentences belong to the optional command-line section" | Read twice. I could not tell whether I should read the flag sentences or skip them, and the instruction is buried in a long paragraph. | Say the last sentence of each entry is only for command-line readers. |
| Settings, Project Reference, "Names the library of known allele sequences" | The first sentence of the entry runs about 60 words and holds three ideas. I had to read it twice. | Split the first sentence in two. |
| Settings, FASTQ Bundles, "the samples are worked on largest first with several running at once" | I do not know what this means for me or whether I can change it. It is stated but never acted on. | Say this is automatic and needs no action. |
| Settings, Min Reads, "Be aware of what it does not do in the release tested here" | Read twice. The sentence says a problem exists but withholds it, and I had to jump forward to What good looks like to learn what it was. | State the defect in one clause here. |
| Settings, minimap2 arguments, "only when you have a specific argument from the minimap2 documentation" | I have no way to try this. No example argument is given and the minimap2 documentation is not linked. | Link the minimap2 documentation. |
| Settings, Orient Reference, "clusters would otherwise split by strand rather than by allele" | Read twice. Two ideas are joined by "since" across a long sentence and I lost the connection between strand and cluster. | Split into two sentences. |
| Settings, "The command line reaches a further set of controls" | Fourteen flag entries follow and I have never opened a terminal. Nothing here says I may skip them. The reassurance only arrives much later at the start of On the command line. | Move the "this section is optional" sentence to the top of the flag list. |
| Settings, --preset, "the only supported value is `mcm-mhc-miseq`" | MCM appears here and again in the Haplotype placeholder without being spelled out at first use. | Spell out Mauritian cynomolgus macaque at first use of MCM. |
| Settings, --comparison-name, "It defaults to `Illumina-31262`." | I cannot judge this. A default that names someone else's dataset looked to me like an error in the manual. | Say the default names the author's own dataset and should usually be changed. |
| Settings, --cdna-threshold, "treated as a cDNA sequence rather than a whole genomic one" | cDNA is not glossed. I know it from my course, but the chapter glosses much easier words, so I expected it here. | Gloss cDNA at first use. |
| Reading the results, "23.9 percent of 2,854,092 input reads" | Earlier the chapter said 23.6 percent for the same example. I stopped to check whether one number was wrong before seeing that 23.9 is the full plate and 23.6 the reproduction. | Name the plate and the reproduction in the same sentence as their percentages. |
| Reading the results, "agreeing within a third of a percent ... is what a stable assay looks like" | I cannot judge this. No sentence says how much disagreement would be too much. | Give the gap that would worry you. |
| Reading the results, "The remaining 682,928 alignments passed." | This is one larger than the 682,927 retained reads named two paragraphs earlier. I read both twice to be sure I had not misread. | Reconcile the two figures or explain the difference. |
| Reading the results, "produced between 80 and 117 allele rows each" | I cannot judge an allele row count, because I do not know how many alleles a macaque should carry. Chapter 1 may say, but it is not recalled here. | Say roughly how many rows a working animal is expected to produce and why. |
| Reading the results, "row count sample for sample is what determinism looks like here" | Determinism is not glossed, and "sample for sample" is an idiom. Read twice. | Say the same input gives the same output every time. |
| What good looks like, "read it against your own assay rather than against a fixed number" | I have no assay history yet, so this advice cannot be followed on a first run. | Say what a first-time reader should compare against instead. |
| What good looks like, "has a wet-lab problem or a library mismatch" | Wet-lab appears only here and I was not certain it means the bench work rather than the software. | Say laboratory rather than wet-lab. |
| What good looks like, "adapter dimers and off-target product" | Neither is explained. These are the reason a quarter is normal, so the whole explanation rests on words I do not know. | Gloss adapter dimer and off-target product. |
| What good looks like, "with 2, 2, 4, 7, 12, and 76 retained reads" | 76 sits above the "fewer than a hundred" line but is listed among the failures. I read the paragraph twice to see whether that was intentional. | Say why 76 still counts as a failure. |
| What good looks like, "judge low-support rows by reading the read counts in the report yourself" | I could not perform this workaround. It does not say which column holds the count or what count is too low. | Name the column and give a number to compare against. |
| What good looks like, "each of its 67 tool invocations with its own exit status" | Exit status is not explained, and it is a terminal idea in a section the chapter says I can read without a terminal. | Say the record notes whether each step succeeded. |
| Haplotype analysis (placeholder), "A worked example ... will be added in a later release" | A section that exists but says nothing made me stop and check whether I had missed a page. | Remove the placeholder or say what to read meanwhile. |
| On the command line, "runs headless, meaning with no window at all" | Headless is glossed, which helped, but the same paragraph assumes I know what Terminal is, and the link that would explain it comes two sentences later. | Move the Command-Line Reference link to the first sentence. |
| On the command line, code block ending each line with `\` | I cannot run this. The backslashes are never explained and I would not know whether to type them. | Say the backslash continues one command across several lines. |
| On the command line, "a run pointed at a path under `/tmp` fails near the end" | I do not know what `/tmp` is, how I would accidentally point at it, or what world-writable means. | Say to keep the output inside your project folder and drop the technical reason. |

Three closing lines.

The one thing I learned is why read merging must happen before mapping, because a single MiSeq read cannot cover a 244-base DRB amplicon on its own.

The one thing I still could not do is run any part of this chapter, because the example project is not downloadable and no substitute dataset is named.

The sentence I liked most is "A large not-spanned count points at reads that are too short, which on a MiSeq plate usually means merging did not happen."
