---
chapter: appendices/06-running-in-ci
reviewer: undergraduate-reader (reader-2)
persona: Senior, two years at the bench (library prep, PCR), no data analysis, has never opened a terminal
date: 2026-09-07
---

# Reader 2 report on Running in CI

| Location | Issue | Suggested fix |
| --- | --- | --- |
| Front matter, `prereqs: [01-foundations/08-provenance...]` | I do not know whether I am allowed to read this chapter without having read those two. Nothing in the prose tells me. | Add one sentence at the top naming the chapters to read first. |
| What it is, "somebody pushes a change to a shared repository" | "Pushes" and "repository" are never glossed. I have heard of GitHub but I do not know what pushing is or how you do it. | Gloss repository and push at first use. |
| What it is, "runs a set of commands on a fresh machine" | I do not know where this fresh machine is. Is it a computer in my lab or a rented one somewhere. My PI will ask who pays. | Say in one sentence that the machine is rented from a service and is not one you own. |
| What it is, "Its command-line program runs without the app's window" | I have only used the app's window. I do not know what a command-line program is or how you make one run. | Gloss command-line program at first use and point to a chapter that teaches the basics. |
| What it is, "either a container runtime or a set of conda environments" | "Runtime" and "environment" are used as nouns I am supposed to already own. The next sentence explains containers but not runtimes. | Gloss container runtime and conda environment as things, not as concepts. |
| What it is, "decided by the `--executor` flag" | I do not know what a flag is. This is called the single most important choice in the appendix, so I cannot afford to skip it. | Gloss flag at first use, with one example of a word with two dashes in front of it. |
| What it is, "your `PATH`, the list of folders your shell searches" | The gloss uses "shell", which is not glossed. I have never opened a terminal so I do not know what a shell is. | Gloss shell before using it inside the gloss of PATH. |
| What it is, "typing `lungfish-cli` at a fresh prompt" | "Prompt" is another terminal word I do not have, and I cannot tell where I would be typing it. | Gloss prompt, or reword to avoid it. |
| What it is, "the SwiftPM product lands at `.build/debug/lungfish-cli`" | SwiftPM, product, checkout, and building from source are four unexplained things in one sentence. I read it three times. | Cut the build-from-source path to a short aside, or gloss checkout and building from source. |
| What it is, "substitute whichever path your runner actually has" | I could not perform this. I do not know how to find out what path my runner has, and "runner" has not been defined yet. | Define runner before this sentence and say how to check which path exists. |
| What a job needs installed, "an ordinary macOS job that checks out a repository" | "Checks out" is jargon. I read it as inspecting something rather than copying it. | Gloss checkout at first use. |
| What a job needs installed, "the `macos-26` runner label" | I do not know what a label is or where you type it. The template later shows `runs-on: macos-26` but the prose never connects the two. | Point forward to the template line that uses the label. |
| What a job needs installed, "check the label against the current image list" | I cannot perform this. There is no link and I do not know what an image list is or whose it is. | Link to the vendor's runner image list, or name where to look. |
| What a job needs installed, "the pinned dependency set" | The phrase after it explains what a dependency set is but not how I find out which one my release uses. | Say in one sentence how to see the dependency set your install has. |
| What a job needs installed, "exits 10 when work is pending and 0 when there is nothing to do" | Exit status is not glossed here. It is glossed a whole section later, under Running the workflow. I hit the numbers first. | Move the exit status gloss to its first use. |
| What a job needs installed, the `tools update --plan` code block | I cannot judge any of this output. "preserve", "reinstall", "unknown -> bioconda::gatk4=4.6.2.0=py310hdfd78af_1", and "[buildChanged]" mean nothing to me, and no sentence walks through even one line. | Annotate two lines of the block, or say plainly that a reader only needs the exit code. |
| What a job needs installed, "Estimated download: 157.3 MB" | I cannot tell whether 157 MB is small or large for a CI job, or whether it recurs on every run. | Say whether this download is per job or cached, and what an ordinary range looks like. |
| What a job needs installed, "installs what cannot be deferred" | I could not tell what counts as deferrable. The flag says `--required-only` but nothing says what required covers. | Say in one sentence which tools count as required. |
| What a job needs installed, "`lungfish-cli conda install --pack <id>`" | The angle brackets are a convention I had to guess. I did not know whether to type them. | State once that angle brackets mark a value you replace and are not typed. |
| What a job needs installed, "for each plugin pack the tests need" | I do not know what a plugin pack is or how to find out which ones my tests need. "Pack" then runs through the whole chapter. | Gloss plugin pack at first use with a concrete example. |
| What a job needs installed, "`conda db download Viral` for the classification database" | I do not know what a classification database is, how large it is, or whether it downloads on every job. | Add the size and whether it is cached. |
| What a job needs installed, "prints the runner's macOS version, core count, memory" | I do not know how many cores or how much memory a job should have, so this output cannot help me judge anything. | Give a minimum core and memory figure for a typical run. |
| Running the workflow, "a thin alias for `lungfish-cli workflow run --quiet`" | "Alias" is not glossed, and the paragraph then argues from the `argv` field, which is also not glossed. I read the paragraph twice and still did not learn what to type. | Lead with the one-line recommendation and put the alias evidence after it. |
| Running the workflow, "A Nextflow file with a lower-case `.nf` extension" | Nextflow and Snakemake are named as if I know them. The body never says what a workflow engine is. | Gloss workflow engine, Nextflow, and Snakemake at first use. |
| Running the workflow, "the one built-in nf-core pipeline" | nf-core is new here and never explained. I could not tell whether it is a company, a format, or a folder. | Gloss nf-core at first use. |
| Running the workflow, the `run-headless` example block | I could not perform this. The example uses `hello-world-nextflow.lungfishflowpkg/main.nf` but never says where that file is on my machine or how to get it. | Say where the example package ships and how to reach it. |
| Running the workflow, "`--bundle-root ./bundles`" | This flag appears in the example and in both templates but is never explained anywhere in the chapter. | Add it to the five-flag list, or gloss it where it first appears. |
| Running the workflow, "the path of the run bundle it created" | "Run bundle" is listed in the front matter glossary but never defined in the body. I do not know what is inside one or why I want it. | Define run bundle where it first appears. |
| Running the workflow, "That refusal exited 64" | I cannot judge 64. Nothing tells me whether these numbers are LGE's own or a standard, or whether I must memorise any. | Say once that only 0 means success and the other numbers are for diagnosis. |
| Running the workflow, "`--executor` picks the execution profile" | "Execution profile" is a third name for a thing already called a runtime and an environment. Docker has not been introduced. | Use one term throughout, and gloss Docker at first use. |
| Running the workflow, "a job whose workflow takes that route" | I could not tell which route "that route" points back to. I reread the previous two paragraphs. | Name the route instead of using "that route". |
| Running the workflow, "`--resume` continues a repeated run from the last checkpoint" | Checkpoint and work directory are both unexplained, and I cannot tell whether resume is something I should ever turn on. | Gloss work directory, and say plainly that most jobs leave resume off. |
| Running the workflow, dry-run output "Executor: docker" | The block says docker but the prose never says whether the example machine actually had Docker. I could not tell whether this output means it would have worked. | Note that a dry run reports the default without checking the runtime is present. |
| Running the workflow, "`LUNGFISH_CONDA_ROOT` sets the folder holding conda environments" | I could not perform this. There is no example of setting an environment variable outside the YAML templates, and I have never used a terminal. | Show one line of how a variable is set, or point to the template line. |
| Running the workflow, "unkeyed requests are rate limited" | I do not know how to get an NCBI API key or where the job would store it safely. | Say where to get a key and that it belongs in the service's secret store. |
| Running the workflow, "how a CI step reads a result rather than matching text" | I do not know what reading JSON in a CI step involves, so I cannot act on the advice. | Show one short example, or cut to the warning about the two broken commands. |
| Offline packs, "Build it on a machine that already has the pack installed" | I could not perform this. Nothing earlier told me how to install a pack on my own Mac, only how to install one inside a job. | Add the command that installs a pack locally, or cross-reference the chapter that has it. |
| Offline packs, "It names the directory the pack directory is written *into*" | I had to read this three times and then work backwards from the example to be sure. The italics did not help. | Show the flag value and the resulting path side by side in two labelled lines. |
| Offline packs, "a SHA-256 checksum and byte size for every file" | SHA-256 and checksum are unexplained. I do not know what I would do with one. | Gloss checksum in plain terms, as a fingerprint showing a file has not changed. |
| Offline packs, "`export LUNGFISH_CONDA_ROOT="$RUNNER_TEMP/lungfish-conda"`" | `export` and `$RUNNER_TEMP` are both unexplained. I do not know whether `$RUNNER_TEMP` is something I define or something the service gives me. | Say that RUNNER_TEMP is supplied by the CI service, and gloss export. |
| Offline packs, "Available packs: lungfish-tools, read-mapping, ..." | Eight names with no descriptions. I have no way to tell which pack my analysis needs. Only `metagenomics` is ever explained. | Add a short table matching each pack id to the kind of analysis it serves. |
| Offline packs, "For a Kraken2 read-classification job" | Kraken2 is used as a known noun. It is the only concrete science example in the chapter and it is the one I would recognise least. | Gloss Kraken2 in half a sentence. |
| Offline packs, "the Deacon host-depletion index" | Three unknowns at once, and I could not tell whether I need this or it is an aside. | Cut to a cross-reference, or gloss host depletion. |
| Offline packs, "`conda export-pack` ... calls the identical code" | Being told two commands are the same, then given a difference, left me unsure which to use. | State the recommendation first and the equivalence second. |
| GitHub Actions template, "The file below is a template to adapt" | I could not perform this. Nothing says where the file goes in my repository or what it must be named. `.github/workflows/` never appears. | Give the file path and name above the block. |
| GitHub Actions template, "`uses: actions/checkout@v4`" | The YAML uses keys I have never seen (`on`, `jobs`, `steps`, `uses`, `with`, `env`) and none are explained. I cannot tell which lines I must change and which to leave alone. | Mark the lines a reader is expected to edit. |
| GitHub Actions template, "key: lungfish-conda-packs-${{ hashFiles(...) }}" | I could not read this at all. The `${{ }}` syntax and `hashFiles` are not explained. | Add a sentence saying this expression is GitHub's own and can be copied unchanged. |
| GitHub Actions template, "run: lungfish-cli tools update --plan" | The chapter says twice that installed releases do not put `lungfish-cli` on the PATH, but the template types the bare name anyway. This reads as a contradiction. | Add a PATH setup step to the template, or a comment saying the path must be filled in. |
| GitHub Actions template, "`timeout-minutes: 60`" | I cannot judge 60. There is no sense anywhere of how long a real LGE workflow takes. | Give a rough runtime for a typical job. |
| GitHub Actions template, "Install the cached pack" step | On the very first run the cache is empty, and nothing tells me what happens then. | Say what the first run does and how the cache gets filled. |
| GitHub Actions template, "run-headless pipeline.nf" | `pipeline.nf` appears with no explanation of where a reader gets one. The whole chapter assumes I already have a workflow file. | Say plainly that writing the workflow file is out of scope and name the chapter that covers it. |
| CircleCI template, "`xcode: "26.0.0"`" | I could not connect this to the earlier requirement for macOS 26. It looks like a different numbering system. | State that the Xcode image version is how CircleCI selects the macOS version. |
| CircleCI template, "has no equivalent of the `always()` condition" | I could not tell whether this is a problem I must solve or one the template already solved. | Say directly that the template already handles it. |
| CircleCI template, "corrupt it in ways that are hard to see" | I was warned off a thing I did not know was possible, and the warning arrives after both templates. | Move the warning to where caching is first introduced. |
| Keeping the provenance, "the JSON file LGE writes to record one operation" | JSON is used unglossed here and again under `--format json` an earlier section. | Gloss JSON at its first use, which is the earlier one. |
| Keeping the provenance, "with a second copy of the bundle's own record under" | Two provenance records in two places, and I could not work out whether they are the same file or different ones. | Say in one sentence what each of the two records covers. |
| Keeping the provenance, "A CI artifact step that globs for `*.lungfish-provenance.json`" | "Globs" is jargon and the leading asterisk is a pattern convention I had to guess. | Reword without "glob". |
| Keeping the provenance, "the runtime identity (application version, architecture, ...)" | This is one sentence with about fifteen items in it. I lost the thread and had to restart it. | Break the field list out into a short list or a table. |
| Keeping the provenance, "Peak RAM reading `unknown` is expected" | I cannot judge peak RAM. Nothing says what a good or bad value would be if it were reported. | Say what peak RAM would be used for once it is recorded. |
| Keeping the provenance, "check the sidecar's `exitStatus` field" | I could not perform this. No command is shown for reading a field out of the sidecar. | Show the one command or expression that reads the field. |
| What fails, "Six failures account for most red CI jobs" | "Red" is used to mean failed without explanation, and it is the first word of the section's argument. | Say "failed" instead. |
| What fails, "`workflow validate` lowercases the extension first" | `workflow validate` appears here for the first and only time in the chapter and is never explained. | Introduce the command, or move the aside to the defect list. |
| Defects section, "a very small negative number rather than a duration" | I could not tell whether this means my provenance is untrustworthy overall or only that one field. | Say that only that one field is affected. |
| Defects section, "reports Nextflow's version by printing Nextflow's own complaint" | I read this twice before I understood that Nextflow is complaining, not LGE. | Reword so the subject of the complaint is clear on the first read. |
| Whole chapter | The chapter never says who sets up CI or whether a student can do it alone. My lab asked me to set this up, and after reading I still do not know whether I need an account, permissions on the repository, or somebody with terminal experience. | Add a short paragraph near the top saying what access and prior setup the reader must already have. |

I learned that a CI job throws its machine away every time, so anything the job needs has to be installed or restored inside the job itself.

I still could not run a single command, because the chapter never shows me how to open a terminal, how to find `lungfish-cli` on my own Mac, or where the example workflow file comes from.

The sentence I liked most is "a job that passes on a machine nobody has touched is evidence the work does not depend on one person's laptop."
