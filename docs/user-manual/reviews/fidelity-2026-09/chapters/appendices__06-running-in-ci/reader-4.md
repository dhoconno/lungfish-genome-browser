# Reader 4 report: Running in CI

Persona: undergraduate who has used Geneious for one semester, expects a dialog for every operation, has never opened a terminal. A lab has asked me to set this up on a build server.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| What it is, "runs a set of commands on a fresh machine" | I have never run a command on any machine, fresh or not. In Geneious I click Align and a dialog appears. Nothing in this chapter tells me where the commands get typed, or that they get typed at all rather than clicked. I was lost from the first paragraph and never recovered. | Add one sentence at the top saying this appendix is entirely text commands with no dialogs, and name the chapter that teaches the terminal. |
| What it is, "somebody pushes a change to a shared repository" | "Push" and "repository" are used as if I know them. I know a repository as a place data is deposited, like GenBank, which is clearly not the meaning here. | Gloss repository and push at first use. |
| What it is, "a container runtime or a set of conda environments" | Container is glossed one sentence later and conda is glossed too, but "runtime" and "environment" are not, and those are the words the rest of the chapter actually uses. | Gloss runtime and environment where container and conda are glossed. |
| What it is, "decided by the `--executor` flag" | I do not know what a flag is. The chapter uses flag about twenty times after this and never says. | Gloss flag at first use as a word starting with dashes that changes how a command behaves. |
| What it is, "the single most important choice in this appendix" | I am told this is the most important choice and then it is not explained here. When I reach `--executor` later it is one bullet among five and reads no more important than the rest. | Either explain the choice here or drop the claim. |
| What it is, "does not put it on your `PATH`, the list of folders your shell searches" | PATH is glossed using "shell", which is not glossed. I do not know what a shell is or where one lives. | Gloss shell before using it to gloss PATH. |
| What it is, "SwiftPM product lands at `.build/debug/lungfish-cli` inside the checkout" | Three unknowns in one clause. SwiftPM, "product", and "checkout". I also do not know what building from source means or whether my lab would want it. | Say plainly that this path only applies if somebody compiled the program themselves, and gloss checkout. |
| What it is, "substitute whichever path your runner actually has" | I cannot do this. I do not know how to find out what path the runner has, and the chapter never shows me. This blocks every single example that follows. | Show one worked example using the full bundle path so I can see the substitution done once. |
| What a job needs installed, "an ordinary macOS job that checks out a repository" | Second unglossed use of check out, now as a verb. | Gloss once, use consistently. |
| What a job needs installed, "the `macos-26` runner label" | Label is new here. I cannot tell whether I type this somewhere, choose it from a menu, or whether it is already set for me. | Say where the label is written, then point at the template. |
| What a job needs installed, "check the label against the current image list" | I do not know what an image list is or where it is published. There is no link. | Link the vendor image list. |
| What a job needs installed, "exits 10 when work is pending and 0 when there is nothing to do" | Exit status is not explained until the next section, several pages later, and 10 is described as good news in one sentence and as a job failure in another. I had to read this three times to work out that failing here is deliberate. | Move the exit-status gloss to its first use here and say plainly that this step is meant to fail. |
| What a job needs installed, output block "preserve  bracken  local install kept" | The block has four columns with no headers, and I cannot tell whether "preserve" and "reinstall" are things that happened or things that will happen. | Add a sentence naming the columns and saying nothing was changed. |
| What a job needs installed, "pinned bioconda::bracken=1.0.0=1" | The double colon and the second `=1` are unexplained. I cannot tell which part is the version. | Gloss the package coordinate format once. |
| What a job needs installed, "Estimated download: 157.3 MB" | A number I cannot judge. Is that fast, or does it mean my job is now ten minutes longer? | Say roughly what that translates to in job time. |
| What a job needs installed, "`--apply --yes --required-only` installs what cannot be deferred" | I cannot judge which work "cannot be deferred" or what happens to the rest, so I cannot tell whether this line is enough or leaves me half installed. | Say in one sentence what `--required-only` leaves out. |
| What a job needs installed, "`conda install --pack <id>` for each plugin pack the tests need" | Angle brackets appear here for the first time with no explanation, and I have no way to find out which packs my tests need. | Gloss the angle-bracket placeholder convention, and point at the pack list that appears three sections later. |
| Running the workflow, "a thin alias for `workflow run --quiet`" | Alias is unglossed, and the paragraph spends four sentences proving two commands are the same thing before telling me which one to use. I read it twice. | Lead with which one to use in CI, then explain. |
| Running the workflow, "the `argv` field it stores" | argv means nothing to me. | Gloss it or say "the record of the command". |
| Running the workflow, "a file whose name contains `snakefile` in any case" | I do not know what Snakemake or Nextflow are. The front matter lists them as tools but the body never says what a workflow engine is or that I need one. | Gloss workflow engine, Nextflow, and Snakemake in one sentence before the three shapes. |
| Running the workflow, "hello-world-nextflow.lungfishflowpkg/main.nf" | This example package "ships with LGE" but I am not told where it is on disk, so I cannot run the one complete example in the chapter. | Give the path to the shipped example package. |
| Running the workflow, "`--bundle-root ./bundles`" | This flag appears in the worked example and in both templates and is never defined anywhere in the chapter. | Add it to the flag list or gloss it inline. |
| Running the workflow, "Five flags of `workflow run` matter more in CI" | The list covers three separate decisions, and `--timeout` is the odd one because its advice is not to use it. I had to reread to see it was a warning rather than an option. | Split the do-not-use flag out of the list. |
| Running the workflow, "`--dry-run` ... the cheapest possible pull-request check" | Pull request is unglossed and appears again in the YAML template as `pull_request`. | Gloss pull request at first use. |
| Running the workflow, "`--resume` ... survived from the previous job in a cache" | Cache is used here and is central to both templates, and is never glossed. | Gloss cache at first use. |
| Running the workflow, dry-run block "Results: ./results" | The dry run reports `./results` although the earlier real command used `./nf-results`, and I could not tell whether that is a default, a mistake, or a different command being shown. | Show the dry run of the same command, or say which flags were dropped. |
| Running the workflow, "`LUNGFISH_STORAGE_ROOT` sets the managed storage root that everything else lives under" | Managed storage root is defined with itself. I still do not know what lives there or how big it gets. | Say what is stored there in plain words. |
| Running the workflow, "a runner where the home folder is not cached but a scratch folder is" | I do not know what a scratch folder is or how I would know which folders my runner caches. | Gloss scratch folder, and say the templates already pick the right one. |
| Running the workflow, "`NCBI_API_KEY` ... unkeyed requests are rate limited" | I am told to set this and never told how to get one. | Link the NCBI page that issues a key. |
| Offline packs, "The trap is in `--output`. It names the directory the pack directory is written *into*" | This is the clearest sentence in the chapter, but the surrounding example uses pack `metagenomics` while the evidence sentence uses pack `gatk-core`, so I had to map two different names onto one rule. | Use one pack id throughout the section. |
| Offline packs, "alongside its `offline-pack-manifest.json` and its own `.lungfish-provenance.json`" | Manifest is unglossed, and a file whose name starts with a dot is invisible in the Finder, which nobody warns me about. | Gloss manifest, and note that dot-files are hidden in the Finder. |
| Offline packs, "`export LUNGFISH_CONDA_ROOT="$RUNNER_TEMP/lungfish-conda"`" | Three terminal conventions in one line that I cannot read. The word `export`, the `$` variable, and where `RUNNER_TEMP` comes from. | Say in one sentence that this line is only for a GitHub runner and that the templates set it for you. |
| Offline packs, "exited 3 and listed the ones that do" | Exit codes 0, 3, 10, and 64 all appear in this chapter with different meanings and there is no one place listing them. | Add a small table of the exit codes this chapter uses. |
| Offline packs, "For a Kraken2 read-classification job the pack is `metagenomics`" | Kraken2 is named with no gloss and no cross-reference, and this is the only sentence that tells me how to choose a pack. | Gloss Kraken2, and give one more mapping so the choice looks like a rule rather than one case. |
| Offline packs, "the listed eight are not the complete set of what works" | I am left holding a list I have been told is wrong. As a student I would not know whether to trust `gatk-core` or avoid it. | Say plainly that the three extra ids are safe to use. |
| Offline packs, "`conda db install-managed <identifier>` for a managed one such as the Deacon host-depletion index" | Catalogue database, managed database, and host depletion all arrive unglossed in one sentence, and no identifier is shown. | Gloss the two database kinds and show one real identifier. |
| A GitHub Actions template, "The file below is a template to adapt" | I am never told where this file goes. In Geneious everything is a menu, so the idea of a file at a fixed hidden path in a repository is exactly what I do not have. | State the path `.github/workflows/<name>.yml` and that the folder must be created. |
| A GitHub Actions template, "`uses: actions/checkout@v4`" | The YAML uses `uses`, `with`, `key`, `hashFiles`, and `${{ }}` with no explanation, so I can copy it but cannot adjust it, which is what the intro told me to do. | Add one or two sentences naming the parts a reader is expected to change. |
| A GitHub Actions template, "key: lungfish-conda-packs-${{ hashFiles(...) }}" | The cache is restored but nothing in this template ever creates the pack, unlike the CircleCI one which at least saves it. I could not see how the first run ever gets a pack. | Add the export step, or say the pack is committed to the repository beforehand. |
| A GitHub Actions template, "run: lungfish-cli run-headless pipeline.nf" | Every step calls the bare name `lungfish-cli`, which the chapter's own first failure case says will not be found. The template contradicts the advice. | Show the PATH step, or use the full bundle path in the template. |
| A CircleCI template, "`xcode: "26.0.0"`" | I cannot tell whether an Xcode version is the same requirement as macOS 26, which was stated as a hard rule. | Say which Xcode images give macOS 26. |
| Keeping the provenance, "whose expected output was a `.lungfishref` bundle" | Bundle, run bundle, reference bundle, and `.lungfishref` are used near each other and I lost track of which artifact holds what. | Name the artifact kinds once in a short list. |
| Keeping the provenance, "a `files` list giving every input and output a role, a SHA-256 checksum, and a byte size, the exit status, the start and end times" | One sentence, more than a dozen items, no break I could hold onto. I read it three times and still could not say where one item ended. | Break into a short list. |
| Keeping the provenance, "Total wall time    : <1s" and "Peak RAM: unknown" | Wall time is not glossed, and I cannot judge whether a run of under a second means the example is trivial or that something did not run. | Gloss wall time, and say the example workflow does almost no work. |
| Keeping the provenance, "check the sidecar's `exitStatus` field" | I am told to check a field inside a JSON file, with no indication of how a CI step reads a JSON field, and `--format json` was earlier described as unreliable. | Show the one line that reads the field. |
| What fails, "`pipeline.NF` is not seen as Nextflow even though `workflow validate` lowercases the extension first and does see it" | The longest sentence in the chapter and the only one I gave up on. Two commands disagree, and I could not work out which one my job runs. | Split into two sentences and say which command the templates use. |
| Defects found while writing this appendix, "Four rough edges in the 2026.9.13 build" | I do not know whether 2026.9.13 is the version my lab will have, and nothing tells me how to check my own version. | Give the command that prints the installed version. |
| Defects found while writing this appendix, "`wallTimeSeconds` field ... held a very small negative number" | A defect list at the end of an appendix leaves me unsure whether the whole chapter is safe to follow. As a student I would stop here and ask somebody. | Put the reassurance that none of these blocks a working job in its own sentence before the list. |

One thing I learned: a CI job is a throwaway machine, so anything that has to survive must be deliberately saved or uploaded.

One thing I still could not do: run any command in this chapter, because I was never told where commands are typed or how to find the program's path on my own machine.

The sentence I liked most: "The trap is in `--output`."
