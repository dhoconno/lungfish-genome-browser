# Reader report: Running in CI

Reader 3. Pre-med student, English is my second language. Strong on biology words from textbooks, slow with idioms and long sentences. I have never opened a terminal. My lab asked me to set this up.

| Location | Issue | Suggested fix |
|---|---|---|
| Front matter, "task: Run Lungfish Genome Explorer workflows on a continuous integration runner with no window" | "with no window" made me think something was broken. I did not know a program could run without a window until the next section explained it. | Say the program runs from typed commands and shows no screen. |
| What it is, "somebody pushes a change to a shared repository" | "pushes" and "repository" are both new. I have never used version control and I do not know what a push is. | Gloss repository and push at first use. |
| What it is, "That disposability is the whole point" | "disposability" is a hard noun and "the whole point" is an idiom. I had to read the sentence twice. | Say the machine is thrown away on purpose. |
| What it is, "evidence the work does not depend on one person's laptop" | I understood the words but not why a laptop is the problem. I do not know what would go wrong. | Add one clause saying a laptop may have extra software nobody else has. |
| What it is, "Its command-line program runs without the app's window" | "command-line program" is used before it is explained. The explanation of what a command line is never arrives. | Say in one sentence what a command line is. |
| What it is, "a container runtime or a set of conda environments" | "runtime" is not glossed. I now know what a container is, but not a runtime. | Say a container runtime is the program that starts containers. |
| What it is, "decided by the `--executor` flag" | "flag" is used for the first time here with no explanation. I thought of a signal flag. | Say a flag is a word starting with two dashes that you add to a command. |
| What it is, "the single most important choice in this appendix" | The text says this is the most important choice but then does not tell me how to make it. The flag is explained six sections later. | Say here which value to pick if you do not know. |
| What it is, "Installed releases do not put it on your `PATH`, the list of folders your shell searches" | "shell" is used inside the gloss for PATH. I do not know what a shell is, so the gloss did not help. | Gloss shell before using it inside another gloss. |
| What it is, "the SwiftPM product lands at `.build/debug/lungfish-cli`" | I do not know what SwiftPM is or what "build from source" means. This whole sentence was closed to me. | Say this path only applies if you compiled the program yourself. |
| What it is, "So what should you do with this?" | A question addressed to me in the middle of an explanation made me stop and check whether I was being tested. | Remove the question and state the rule. |
| What a job needs installed, "Nothing about LGE is special until the workflow starts." | I could not tell what this sentence wanted me to do. It reads like reassurance but I did not know what I was being reassured about. | Cut it or say the first steps are the same as any other job. |
| What a job needs installed, "checks out a repository" | "checks out" is an idiom. In a library that means borrowing. I guessed it means copying. | Say the job copies the repository onto the machine. |
| What a job needs installed, "This is not a preference." | Very short and abrupt after a long sentence. I read it twice looking for the missing word. | Say the requirement cannot be changed. |
| What a job needs installed, "On GitHub Actions that is the `macos-26` runner label" | GitHub Actions appears here for the first time with no explanation. I do not know if it is the same as CI or a different thing. | Say GitHub Actions is one CI service, and name it at first use. |
| What a job needs installed, "check the label against the current image list" | I do not know what an image list is or where I would find it. I could not perform this step. | Give the place to look. |
| What a job needs installed, "two honest ways to get them" | "honest" applied to a method is a figure of speech I did not follow. I wondered if the other ways were dishonest. | Say there are two ways that work. |
| What a job needs installed, "it exits 10 when work is pending and 0 when there is nothing to do" | Exit numbers appear here, but exit status is only explained two sections later. I did not know what exiting 10 meant. | Explain exit status before the first number. |
| What a job needs installed, "Run against a machine whose environments were out of date it printed this." | The sentence has no comma and I parsed "run against a machine" as a command to me. I read it three times. | Add a comma after "date", or rewrite as a full sentence. |
| Code block, "preserve  bracken  local install kept; pinned bioconda::bracken=1.0.0=1 not applied" | I cannot read any of this line. I do not know what preserve, pinned, or the double colons mean, and nothing in the text walks through it. | Add one sentence explaining what one row of this output says. |
| Code block, "Estimated download: 157.3 MB" | I do not know whether 157.3 MB is a lot for a CI job or normal. | Say roughly how long a download of this size takes. |
| What a job needs installed, "a good deal cheaper than discovering a version drift halfway through a pipeline" | "cheaper" here is not about money, and "version drift" is not glossed. Two unclear things in one clause. | Say it saves time, and gloss version drift. |
| What a job needs installed, "installs what cannot be deferred" | "deferred" is abstract. I do not know what makes one install deferrable and another not. | Say it installs only the tools the run cannot start without. |
| What a job needs installed, "for each plugin pack the tests need" | "plugin pack" appears here for the first time and is not explained, though it is in the glossary list. | Gloss plugin pack at first use in this chapter. |
| What a job needs installed, "`lungfish-cli conda db download Viral`" | I do not know what the Viral database is or whether my lab's job needs it. | Say what this database is for. |
| What a job needs installed, "their flags have a trap in them" | "a trap" made me anxious and I did not know if I should skip ahead to read about it now. | Say the flags are easy to get wrong and are explained below. |
| What a job needs installed, "Two preflight commands" | "preflight" is aviation vocabulary. I guessed from context but was not sure. | Say these are two checks to run first. |
| Running the workflow, "a thin alias for `lungfish-cli workflow run --quiet`" | "thin alias" is jargon. I do not know what makes an alias thin. | Say run-headless is another name for the same command. |
| Running the workflow, "the `argv` field it stores reads `workflow run ... --quiet`" | "argv" is never explained. I could not tell if this detail matters to me. | Gloss argv or move this to a note. |
| Running the workflow, "the signal you want is that the workflow ran quietly" | "the signal you want" is figurative. I could not decide between two commands from this sentence. | Say use run-headless in scripts and workflow run when reading output yourself. |
| Running the workflow, "only three shapes are accepted" | "shapes" for file kinds is a metaphor. I expected geometry. | Say three kinds of workflow argument are accepted. |
| Running the workflow, "a file whose name contains `snakefile` in any case" | "in any case" could mean "anyway" or "any capitalisation". I read it twice and still guessed. | Say upper or lower case both work. |
| Running the workflow, "the one built-in nf-core pipeline" | "nf-core" is never explained. It also appears in the executor flag and the timeout flag. | Gloss nf-core at first use. |
| Code block, "lungfish-cli run-headless hello-world-nextflow.lungfishflowpkg/main.nf \" | The backslash at the end of each line is notation I have never seen. I did not know if I should type it. | Say the backslash lets one command continue on the next line. |
| Running the workflow, "--bundle-root ./bundles" | Three flags appear in this example and only two are explained anywhere. I do not know what bundle-root does. | Add one sentence for bundle-root. |
| Running the workflow, "the path of the run bundle it created" | "run bundle" is used before it is explained. I still do not know what is inside one. | Say a run bundle is a folder holding the record of one run. |
| Running the workflow, "The same command without `--expected-output` refused to start." | I did not understand why the program refuses. The reason is inside the error message, which is hard for me to read. | Say plainly that LGE requires you to name an output. |
| Running the workflow, "That refusal exited 64" | 64 has no meaning to me and no other exit number in this chapter equals 64 for a different reason. | Say 64 means the command was used incorrectly. |
| Running the workflow, "a path that does not match where the pipeline actually writes gives a run that reports success and leaves no provenance behind" | This is the most important warning here and it is at the end of a 40-word sentence. I nearly missed it. | Give it its own short sentence. |
| Running the workflow, "each is one sentence of what it does followed by why a job would set it" | The sentence describes the structure of the list instead of telling me anything. I paused on it. | Cut it. |
| Running the workflow, "picks the execution profile for an nf-core run from `docker`, `conda`, or `local`" | "execution profile" is not glossed, and this is the choice the first section called the most important one. I still cannot choose. | Say which value to pick for a job with no containers. |
| Running the workflow, "the cheapest possible pull-request check" | "pull request" is never explained and "cheapest" is again not money. | Gloss pull request, or say it is the fastest check. |
| Running the workflow, "it is the one flag that formally waives the expected-output requirement" | "formally waives" is legal-sounding language. I read it twice. | Say this flag is the only one that removes the requirement. |
| Running the workflow, "a local run accepts it without enforcing it and an nf-core run rejects it outright" | The flag behaves in three different ways in one sentence and I lost track of which was which. | Split into two sentences. |
| Running the workflow, "worth having only when the engine's work directory survived from the previous job in a cache" | "work directory", "engine", and "cache" all in one clause, none glossed. | Gloss cache, and say the work directory is where the engine keeps partial results. |
| Code block, "ℹ Dry run - workflow would execute with:" | "Parameters: 0" gave me nothing. I do not know whether zero is expected or a sign something is missing. | Say zero parameters is normal for this example. |
| Running the workflow, "on a runner where the home folder is not cached but a scratch folder is" | "scratch folder" is not glossed and the whole clause assumes I know how runner storage works. | Say a scratch folder is temporary space the job can write to. |
| Running the workflow, "LGE reads the two together, so a job that overrides one usually wants to override both." | "reads the two together" is vague. I did not learn what goes wrong if I set only one. | Say what breaks when only one is set. |
| Running the workflow, "unkeyed requests are rate limited and a shared CI address exhausts that allowance quickly" | "rate limited", "allowance", and "shared CI address" are three new ideas in one clause. | Say NCBI blocks too many requests from one address, and a key raises the limit. |
| Running the workflow, "matching text that a later release may reword" | I did not understand what matching text means until I reread the sentence from the start. | Say reading JSON is safer than searching the printed words. |
| Offline packs heading, "and the flag that catches people" | "catches people" is an idiom. I understood it only after reading the section. | Use a plain heading. |
| Offline packs, "Build it on a machine that already has the pack installed." | I do not know how to build one. The step says build it and then shows a command whose flags I cannot judge. | Say the command below does the building. |
| Offline packs, "It names the directory the pack directory is written *into*, not the pack directory itself." | Three uses of "directory" in one sentence, with italics carrying the meaning. I read it four times. | Say --output names the parent folder. |
| Offline packs, "alongside its `offline-pack-manifest.json` and its own `.lungfish-provenance.json`" | The leading dot on the second filename is unexplained, and I do not know if I will see these files. | Say files starting with a dot are hidden. |
| Offline packs, "recording the pack id, the source conda root, the exporting command, and a SHA-256 checksum" | "SHA-256 checksum" is not glossed here and I did not know what it is for. | Say a checksum is a fingerprint that shows a file has not changed. |
| Offline packs, "export LUNGFISH_CONDA_ROOT=\"$RUNNER_TEMP/lungfish-conda\"" | I do not know what the dollar sign does or where RUNNER_TEMP comes from. I could not perform this step. | Say the dollar sign inserts a value the CI service provides. |
| Offline packs, "a re-run of a cached CI job hits immediately" | "hits" with no object. I read it twice. | Say a re-run runs into this at once. |
| Offline packs, "a clean early failure rather than a confusing one" | I could not judge what makes a failure clean. | Say the command stops right away with a list of valid names. |
| Offline packs, "Available packs: lungfish-tools, read-mapping, ..." | Eight names with no descriptions. I cannot tell which one my lab needs, except that one line later tells me metagenomics is for Kraken2. | Add a short purpose for each pack, or point to where they are described. |
| Offline packs, "so the listed eight are not the complete set of what works" | This contradicts the error message the app itself prints. I did not know which to believe. | Say the error message is incomplete and this list is correct. |
| Offline packs, "the Deacon host-depletion index" | Deacon and host depletion are both new. I know what a host is in biology, but not what depleting an index means. | Gloss host depletion. |
| Offline packs, "calls the identical code, differing only in that its `--output` also accepts a `.tar`, `.tgz`, or `.tar.gz` archive path" | I do not know what these three file endings are or why there are three. | Say these are compressed archive formats. |
| GitHub Actions template, "Every flag in it was verified individually against the 2026.9.13 build, but no CI service executed it" | I could not judge how much to trust this file. Verified but not executed left me unsure whether it would run. | Say the file may need edits before it runs. |
| GitHub Actions template, "runs-on: macos-26" | I have never seen YAML. Nothing in the chapter says what this file is called or where I put it in my repository. | Say the file goes in .github/workflows and give the filename. |
| GitHub Actions template, "uses: actions/checkout@v4" | The at sign with a version is notation I do not know, and it appears three times with different numbers. | Say the part after @ is the version. |
| GitHub Actions template, "key: lungfish-conda-packs-${{ hashFiles(...) }}" | The double curly braces and hashFiles are unreadable to me and unexplained. | Say this line is boilerplate you can copy unchanged. |
| GitHub Actions template, "timeout-minutes: 60" | I cannot judge 60. The chapter never says how long a real workflow takes. | Give a typical run time so the ceiling makes sense. |
| GitHub Actions template, "run: lungfish-cli tools update --plan" | The chapter said earlier that installed releases do not put lungfish-cli on the PATH, but every step here types the bare name. I could not reconcile the two. | Show the step that puts the program on the PATH, or say the template assumes you added it. |
| GitHub Actions template, "The `tools update --plan` step fails the job on exit 10" | I did not see anything in the file that says exit 10 fails the job, so I could not verify this claim. | Say CI treats any non-zero exit as a failure. |
| GitHub Actions template, "for the reason the next section explains" | A forward reference in the middle of a template. I stopped and went looking. | Give the reason here in one clause. |
| CircleCI template, "CircleCI splits caching into separate restore and save steps" | CircleCI is introduced with no sentence saying what it is or why I would use it instead of GitHub Actions. | Say CircleCI is a second CI service and name when to pick it. |
| CircleCI template, "xcode: \"26.0.0\"" | I do not know what Xcode has to do with this, or why the macOS version is given as an Xcode version here but as macos-26 in the other file. | Say the Xcode version selects the macOS image. |
| CircleCI template, "Two jobs updating one root at once corrupt it in ways that are hard to see and harder to reproduce." | "hard to see and harder to reproduce" is a rhetorical pattern I had to read twice, and it does not tell me what breaks. | Say the environments become unusable. |
| Keeping the provenance, "the reason to run LGE in CI at all rather than calling the underlying tools directly" | "the underlying tools" is not explained. I do not know what calling them directly would look like. | Say running the bioinformatics programs yourself. |
| Keeping the provenance, "the sidecar was written *inside* the bundle at ..." | The path with a leading dot and a slash is long and I could not see the structure. | Show the folder layout as a small tree. |
| Keeping the provenance, "A CI artifact step that globs for `*.lungfish-provenance.json` beside the output will therefore find nothing." | "globs" is a verb I have never seen and the asterisk is unexplained. | Say a step that searches for files matching that name. |
| Keeping the provenance, "a `files` list giving every input and output a role, a SHA-256 checksum, and a byte size, the exit status, the start and end times, the wall time, and the captured standard error" | This sentence lists thirteen things across 60 words. I lost the thread halfway and could not tell where the files list ended. | Break it into a short list. |
| Keeping the provenance, "the runtime identity (application version, architecture, operating system version, dependency set, and user)" | "runtime identity" and "dependency set" are both unglossed here, and one is inside the parentheses of the other. | Gloss dependency set at first use. |
| Keeping the provenance, "wall time" | Not glossed. I guessed it means real elapsed time but was not certain. | Say wall time is the real time from start to finish. |
| Keeping the provenance, "the captured standard error" | "standard error" is a statistics term I know from my classes, and here it clearly means something else. This confused me badly. | Say it is the text the program printed about problems. |
| Code block, "Peak RAM           : unknown" | The text explains why it is unknown, but I could not tell whether I should worry when it says unknown in my own run. | Say unknown is always expected for local workflows. |
| Keeping the provenance, "`ops stats` exits 0 even when it is given an option it does not understand" | I do not know how I would notice this, since exit 0 is the success value. | Say the step must check the printed text, and say what to look for. |
| Keeping the provenance, "sounds like the natural CI check on a sidecar and is not one" | The negative at the end of the sentence reverses everything before it. I read it twice. | Say provenance verify only works on signed sidecars. |
| Keeping the provenance, "signing is off by default" | Signing is never explained. I do not know what signing a file means or how to turn it on. | Gloss signing and say where it is configured. |
| What fails and why, "Six failures account for most red CI jobs" | "red" for a failed job is jargon from a screen I have never seen. | Say failed jobs. |
| What fails and why, "each has a signature that names it" | "signature" here is a third meaning of the word after the signing section and the signature file in the error message. | Say each failure has a message that identifies it. |
| What fails and why, "a runner that only unpacked the application" | I could not picture what unpacking the application means on a CI machine, and the chapter never shows that step. | Show the step that gets LGE onto the runner. |
| What fails and why, "`pipeline.NF` is not seen as Nextflow even though `workflow validate` lowercases the extension first and does see it" | Two commands behaving differently in one long sentence with a double negative. I read it three times. | Split into two sentences. |
| Defects found, "Four rough edges in the 2026.9.13 build" | "rough edges" is an idiom. I understood it, but the heading calls them defects and the sentence calls them rough edges, so I was unsure how serious they are. | Use one word for both. |
| Defects found, "held a very small negative number rather than a duration" | I do not know how a duration can be negative, and I do not know if this means my timing is wrong. | Say the field is broken and should be ignored. |
| Defects found, "reports Nextflow's version by printing Nextflow's own complaint about the version flag it was given" | Three possessives and a nested clause. I read this twice and still am not sure what appears on screen. | Show the line that actually appears. |

One thing I learned. A CI machine is created fresh for every job and thrown away afterwards, which is why nothing you install stays.

One thing I still could not do. Get `lungfish-cli` onto the runner in the first place. The chapter tells me it is not on the PATH and then every template types its bare name, and no step ever installs it.

The sentence I liked most. "Naming an expected output tells LGE where to look for a finished file, and it does not make the workflow produce one."
