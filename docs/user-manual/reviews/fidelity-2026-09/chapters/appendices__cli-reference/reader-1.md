# Reader 1 report, appendices/cli-reference

Persona: sophomore, one genetics course, never opened a terminal, never used sequencing software. Read as a student sent here to look up one command.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "ships a command-line program alongside its window" | I do not know what a command-line program is or where I would type into one. The chapter names three words it will fix but never says what a terminal is or how to open one. | Add one sentence saying the commands are typed into the macOS Terminal app and where to find it. |
| What it is, "a flag with no value after it is a switch" | "Switch" is used as a term here and again in three later places, but the sentence defines it inside a definition of something else, so I missed it and had to come back. | Give "switch" its own short sentence rather than tucking it into the flag definition. |
| What it is, "the same provenance sidecar files" | I do not know what "sidecar" means as a file. The gloss says "small JSON records", but I do not know what JSON is either. | Say a sidecar is a small extra text file saved next to the result. |
| Finding the binary, "the list of folders your shell searches" | "Shell" is never explained. I do not know what a shell is or whether it is the same thing as the terminal. | Gloss "shell" at this first use. |
| Finding the binary, "add its folder to your `PATH` once" | This is the one thing I actually needed to do and there is no command showing how. I cannot perform this step from the text. | Show the one line to add, or link to a page that does. |
| Finding the binary, "the SwiftPM product lands at `.build/debug/lungfish-cli`" | SwiftPM, product, checkout, and build from source are four unexplained terms in one sentence. | Cut this to "if you built the app yourself" and move the detail to a developer page. |
| Finding the binary, "type that whole path" | The path has a space in "Lungfish Preview.app". Nobody tells me a space breaks the command unless I quote it, and I would have typed it plain and failed. | Show the quoted or escaped path in a code block. |
| Finding the binary, "substitute whichever path you are using" | I read this twice. It means the examples will not work as typed, which is the opposite of what an example promises. | Say plainly that no example below runs until this path is set up. |
| Command index, "The program exposes 44 top-level commands" | I could not judge whether 44 is a lot or a little, and the same number appears again as 44 subcommands for `fastq`, which made me think one was a typo. | Drop one of the two counts or note the coincidence. |
| Command index, `align` row, "into a native `.lungfishmsa` bundle" | Six file extensions appear across this table and none is explained here. | Add one lead-in sentence saying these are LGE's own folder formats and point to File Formats. |
| Command index, `analyze` row, "format validity" | I do not know what makes a sequence file valid or invalid. | Say it checks the file is not corrupt or malformed. |
| Command index, `bam` row, "bundle-owned BAM alignment tracks" | Three unexplained things stacked, namely BAM, bundle-owned, and track. | Gloss BAM and track at first use here rather than assuming the earlier chapters were read. |
| Command index, `build-db` row, "Build a SQLite index" | SQLite is never explained anywhere in the chapter. | Say it builds a small searchable database file. |
| Command index, `assemble` row, "Assemble reads de novo" | Latin term, never glossed. I half-remember it from lecture but could not define it. | Add "assembling reads with no reference to guide them". |
| Global flags, "`--verbose`, `-v` ... Repeat it as `-v`, `-vv`, or `-vvv`" | I could not tell what each extra v gets me or which to pick. | Say roughly what each level adds. |
| Global flags, "`--threads <n>`, `-t` Sets the thread count" | "Thread" is never explained and I could not judge what number to use. | Say a thread is one parallel worker and that the default suits most machines. |
| Global flags, "The default is to show it only when the output is a terminal" | I read this three times. I do not know what it means for output to "be" a terminal. | Say the bar appears when you run the command yourself and not when a script runs it. |
| Global flags, "`--no-color` strips the colour codes" | Spelling is British here while the rest of the chapter is American. It made me stop. | Use one spelling. |
| Global flags, "whatever older copies of this appendix said" | This addresses a reader who has seen an older version. I have not, so the sentence is noise that made me wonder what I missed. | State the rule without the history. |
| Global flags, "`lungfish-cli variants phase --threads 4` records one thread rather than four" | This is a bug notice buried in a flags section. As a first-timer I could not tell whether I was reading how the program works or how it is broken. | Move it to Known defects and cross-reference from here. |
| Global flags, "not bit-identical across thread counts" | "Bit-identical" is unexplained and I could not tell if this matters to me. | Say the results can differ slightly run to run. |
| Downloading records, "Its four subcommands are ... plus `ena`" | Four is stated and then five are listed. I counted twice. | Say five, or explain why `ena` sits outside the count. |
| Downloading records, "`lungfish-cli fetch ncbi <accession>...`" | The whole syntax line is dense and I do not know what the square brackets, angle brackets, pipes, or trailing dots mean. Nothing in the chapter explains this notation. | Add a short key explaining `<value>`, `[optional]`, `a|b`, and `...`. |
| Downloading records, "retrying an HTTP 429 rate-limit response" | HTTP 429 means nothing to me. | Say "when NCBI asks you to slow down". |
| Downloading records, "Accessions are repeatable" | I could not tell whether that means I repeat the flag or list them separated by spaces. There is no example. | Show one command fetching two accessions. |
| Downloading records, "The limit defaults to 20" | I do not know whether 20 results is generous or restrictive for an NCBI search. | Say what a typical search returns. |
| Downloading records, "falls back to the NCBI SRA Toolkit when ENA refuses" | SRA, ENA, and Toolkit are all unglossed, and this is their first appearance in the body. | Gloss SRA and ENA once here. |
| Importing into a project, "A bare `lungfish-cli import <path>` fails" | It says it fails but not what I see when it does, so I would not recognize the failure. | Say what the error message is. |
| Importing into a project, "matching the file's `CHROM` values" | CHROM is a VCF column name I do not know. | Say it matches the chromosome names written in the file. |
| Importing into a project, "`--quality-binning <illumina4\|eightLevel\|none>` defaults to `illumina4`" | I do not know what quality binning is, what the three choices do, or why one is named in camel case while the others are lowercase. | Explain binning in one sentence and make the value names consistent. |
| Importing into a project, "`--recipe <vsp2\|wgs\|hifi\|none>` picks a processing recipe" | VSP2, WGS, and HiFi are three unexplained abbreviations in one flag. | Spell each out once. |
| Importing into a project, "`--clumping-tool <auto\|bbtools\|trim-galore\|none>`" | "Clumping" is not a word I have met and the paragraph does not say what it does. | Say clumping groups similar reads so they compress better. |
| Importing into a project, "`--compression <fast\|balanced\|maximum>`" | I could not judge the tradeoff. Nothing says maximum is slower. | Name the tradeoff in half a sentence. |
| Importing into a project, "`<kind>` is one of `clc-workbench`, ..." | Nine values in one run-on sentence with no line breaks. I lost my place. | Break these into a list or table. |
| Reference bundles, "a sequence, its indices, and any annotation" | "Index" for a sequence file is never explained, though the word appears many times. | Say an index is a helper file that lets tools jump to a position quickly. |
| Reference bundles, "`--compress` bgzips the FASTA inside the bundle" | "bgzip" is used as a verb with no gloss. | Say it compresses the FASTA in a way tools can still read. |
| Reference bundles, first code block | The example uses `NC_012920.1.fasta` but no earlier command in the chapter produces that file, so I cannot follow along. | Show the fetch command that downloads it first. |
| Reference bundles, code block line endings | The lines end in `\` and I do not know what that does. I would have typed the whole thing on one line and wondered. | Note once that the backslash continues a command onto the next line. |
| Reference bundles, "writes a deterministic OCI-layout tarball" | OCI, layout, tarball, and deterministic are four unknowns in one clause. This sentence is the hardest in the chapter. | Rewrite for a reader who has never used containers, or mark it as advanced. |
| Mapping and alignment tracks, "writes a coordinate-sorted, indexed BAM" | I do not know what sorting by coordinate means or why it matters. | Say the reads are stored in position order so tools can seek. |
| Mapping and alignment tracks, "`sr` for Illumina short reads, `map-ont` for Nanopore" | Illumina, Nanopore, PacBio HiFi, and PacBio CLR appear here as if known. I have heard of Illumina only. | Point to the sequencing chapter at first mention. |
| Mapping and alignment tracks, "sets a mapping-quality floor that defaults to 0" | I do not know what mapping quality is or what a good floor would be. The chapter says the default drops nothing but never names a useful value. | Say what mapping quality measures and name a common cutoff. |
| Mapping and alignment tracks, "`--secondary` keeps secondary alignments, `--no-supplementary` drops supplementary ones" | Secondary and supplementary alignments are distinct concepts I do not know, and they sound like synonyms. | Gloss both in one sentence. |
| Mapping and alignment tracks, "Five read-group flags are `--rg-id`, `--rg-sm`, `--rg-lb`, `--rg-pu` ... and `--rg-pl`" | Read group is unexplained and the five two-letter suffixes are opaque. | Say what a read group is and expand each suffix. |
| Mapping and alignment tracks, "overrides the `@SQ` sequence name" | `@SQ` is unexplained BAM header notation. | Say it is the reference name recorded in the file's header. |
| Mapping and alignment tracks, "the file is rewritten in place" | This destroys my original and the sentence goes by without warning me to keep a copy. | Add a caution to back up the BAM first. |
| Calling variants, "Minimum allele frequency. The flag has no default of its own" | Allele frequency in a sequencing sense is not what I learned in genetics, and "no default of its own" followed by "applies its own 0.05" contradicts itself on first read. | Explain allele frequency here and state the effective default plainly. |
| Calling variants, "`--medaka-model <id>` The ONT basecaller model" | ONT and basecaller model are both unexplained, and I would not know where to get an id. | Say where to find the model id. |
| Calling variants, "defaulting to 0.75 ... defaulting to 0.25 ... defaulting to 20" | Three numbers in three sentences and I cannot judge any of them. The chapter never says whether raising or lowering each is more permissive. | Add the direction of each knob. |
| Calling variants, "the ALT_QUAL below which a call fails the bq filter" | ALT_QUAL and "bq filter" are raw tool jargon with no gloss. | Say it is the quality of the alternate base. |
| Calling variants, "since it is off by default for amplicon data" | Strand bias is not explained and I do not know why amplicon data changes the answer. | Gloss strand bias in one sentence. |
| Calling variants, code block, "`--alignment-track <track-id>`" | The example leaves a placeholder I cannot fill. Nothing in the chapter says how to find a track id. | Name the command that lists track ids. |
| Calling variants, "printing it unless `--execute` is given" | A command that prints instead of running surprised me and I read the sentence twice. | Say plainly that the default is a preview. |
| Classification, "`--preset <sensitive\|balanced\|precise>` defaults to `balanced`" | I could not judge which preset to choose or what sensitive trades away. | One clause per preset. |
| Classification, "which matters because the dialog always runs Bracken and this command does not" | "The dialog" is not defined here and Bracken is unexplained. I do not know what Bracken adds. | Say what Bracken does and name the dialog. |
| Classification, "override the numbers behind the preset" | The preset numbers are never printed, so I cannot tell what I would be overriding. | Print the numbers each preset uses. |
| Classification, "`--memory-mapping` runs the database from disk to save memory" | I could not judge whether I need this. Nothing says how much memory a database wants. | Say roughly when this is needed. |
| Classification, "`--bracken-read-length <n>` at 150" | 150 has no unit and no reason. I guessed base pairs. | Add the unit and say it should match your read length. |
| Classification, "`--bracken-level <D\|P\|C\|O\|F\|G\|S>`" | Seven single letters with no key. I worked out from genetics that S is species but not the rest. | Spell out the taxonomic ranks once. |
| Classification, "an installed database such as `Viral` or `Standard-8`" | I do not know what the 8 means or how to see which databases I have. | Say the 8 is a size in gigabytes and name the listing command here. |
| Classification, "`--confidence` defaults to 0.2" | The same flag name is 0.2 here for TaxTriage but unspecified for Kraken 2 above. I could not tell whether they mean the same thing. | Say whether the two confidence values are comparable. |
| Classification, "takes a Nextflow size string and defaults to `16.GB`" | "Nextflow size string" is unexplained and `16.GB` with a dot looks like a typo to me. | Show the format and note the dot is correct. |
| Classification, "`--skip-assembly` is on by default and `--no-skip-assembly` turns assembly back on" | A double negative I read three times before it landed. | Say assembly is off by default and name the flag that enables it. |
| Classification, "reports how many come back independently confirmed" | I could not judge what a good confirmation count is out of the 20 reads it submits. | Say what number would reassure you. |
| Classification, "passes BLAST URL API parameters such as `WORD_SIZE=11`" | I do not know what a URL API parameter is or where the list of them lives. | Link to the BLAST parameter list. |
| Extracting reads and sequences, "1-based inclusive coordinates, matching the samtools faidx convention" | 1-based and inclusive are numbering conventions I have never been taught, and later the chapter uses 0-based exclusive for ORFs without warning me the two differ. | Explain both conventions once and flag that the chapter uses each in places. |
| Extracting reads and sequences, "and an exit status of 3" | Exit status is glossed much later, in the Tool packs section, not at this first use. | Move the gloss to this first mention. |
| Extracting reads and sequences, "and an exit status of 64" | Two different exit numbers for two similar mistakes, and no table of what any number means. | Add a short exit-status table. |
| Extracting reads and sequences, "The output line width defaults to 70" and later "`--line-width` defaults to 60" | Two different defaults for what reads like the same setting, with no explanation. I assumed one was wrong. | Note that the two commands differ on purpose. |
| Extracting reads and sequences, "takes one of four mutually exclusive mode switches" | The four modes are then described in one dense paragraph with nine flags mixed in. I could not extract the one I needed. | Break the four modes into four short paragraphs or a table. |
| Extracting reads and sequences, "`--taxon 2697049`" | A bare number with no explanation of what a taxon id is or where to get one. | Say it is the NCBI taxonomy number and name the organism in the example. |
| Extracting reads and sequences, "so the two produce byte-identical output" | I could not tell why this promise matters to me. | Say it means the script and the window give the same answer. |
| Assembly, "MEGAHIT 1.2.9 fails most runs on Apple Silicon in this release" | "Most runs" gives me no way to judge, and "Apple Silicon" is unexplained though it describes my own laptop. | Say roughly how often, and say Apple Silicon means M-series Macs. |
| Assembly, "a run that does complete is correct" | I read this twice. It is reassurance phrased as a puzzle. | Say the output is trustworthy when the run finishes. |
| Assembly, "Two spellings pass extra arguments, and they differ by one letter" | Naming two near-identical flags without a worked example invites the exact mistake being warned about. | Show both in a one-line example. |
| FASTQ operations, "more than this appendix walks through" | A reference appendix telling me to look elsewhere for a third of itself is the moment I would give up. | Link directly to a generated full list. |
| FASTQ operations, "`{--proportion <p> \| --count <n>}`" | Curly braces appear here for the first time with no explanation, and the pipe inside carries a different meaning from the pipes used in flag value lists. | Cover braces in the notation key. |
| FASTQ operations, "a deterministic two-pass selection" | Two-pass and deterministic together are opaque, and the sentence justifies a missing flag rather than telling me what happens. | Say the same reads come out every time you run it. |
| FASTQ operations, "the arithmetic mean of the Phred scores ... probability-averaged Mean Q" | Phred score is not explained and the two averaging methods are the subtlest point in the chapter, dropped in one sentence. | Explain Phred and give the two averages a short worked contrast. |
| FASTQ operations, "removes human reads with Deacon, choosing the database by identifier" | Deacon is unexplained and I do not know what identifiers exist or how to see them. | Say what Deacon is and name the listing command. |
| FASTQ operations, "Both default the word length to 12 and the masking to `dust`" | Word length and dust masking are unexplained and I could not judge 12. | Gloss both or say the defaults are fine. |
| FASTQ operations, "rebuilds a virtual subset, trim, or demux bundle" | "Virtual bundle" is a concept the chapter never introduces, and demux is an abbreviation. | Say a virtual bundle stores only a preview until it is rebuilt. |
| FASTQ operations, "five lanes worth knowing about by name" | Thirty-plus subcommand names run together across one paragraph. I could not scan for the one I wanted, which is the whole reason I opened this appendix. | Put these in a table with one line each. |
| FASTQ operations, "`12s-match`" | 12S is used five times with no explanation of what the 12S gene is or why it matters. | Gloss 12S at first use. |
| FASTQ operations, "`fastq ont-barcode-genotype` is marked deprecated" | This command is not in the index above nor in the five lanes just given, so it appears from nowhere. | List it where the other subcommands are listed. |
| Sequence utilities, "Nx is the length at which records sorted longest first cover at least x percent" | I read this three times and still could not apply it. It is the only statistic here that needs a picture. | Add a two-sentence worked example on a tiny set of contigs. |
| Sequence utilities, output block, "N50 : 81706 bp" | Every statistic equals the total length because there is one sequence, so the example teaches me nothing about N50. | Use a multi-record file for this example. |
| Sequence utilities, "`lungfish-cli analyze stats NG_000007.3.gb`" | This file appears with no explanation of what it is or how to obtain it, and its `.gb` extension is not glossed. | Name the record and show how to fetch it. |
| Sequence utilities, "purine and pyrimidine totals and GC and AT skew" | Skew is not a term from my genetics course and is not explained. | Gloss skew in one sentence. |
| Sequence utilities, "Codons are counted in frame +1" | Reading frame notation with a plus sign is not explained until the next paragraph, which is too late. | Explain frames before using the notation. |
| Sequence utilities, "symlink or hard-link aliases of the input are rejected" | Symlink and hard link are filesystem terms I do not know. | Say shortcuts to the same file are also rejected. |
| Sequence utilities, "selects an NCBI genetic code and defaults to 1" | I did not know there was more than one genetic code, and the numbering is unexplained until the example. | Say briefly that mitochondria and some organisms use different codes. |
| Sequence utilities, "gives a 318-residue protein for MT-ND1" | I cannot check this without knowing what output to expect, and 318 has no context. | Show the first line of output. |
| Sequence utilities, "finds six matches" | I have no way to check six is right, and the command that produces it is not shown as a runnable block like the others. | Show the command and its output. |
| Sequence utilities, "writes BED-format hits with chromosome, start, end, name, score, and strand" | BED is only in the front-matter glossary list, not glossed in the text. | Gloss BED at this use. |
| Sequence utilities, "The coordinates are 0-based with an exclusive end" | This flatly contradicts the 1-based inclusive rule given earlier for `extract sequence`, with no acknowledgement. I assumed one was a mistake. | Say explicitly that these two commands use different conventions and why. |
| Sequence utilities, "`--min-length` to 100 nucleotides" | I could not judge whether 100 is permissive or strict for finding real genes. | Say what that threshold lets through. |
| Sequence utilities, "Field tokens are `type:<kind>`, `format:<format>` ..." | I have no list of valid `kind` or `format` values, so I cannot write a query. | List the valid kinds or name the command that prints them. |
| Sequence utilities, "numeric comparisons as `key>=n` and its four siblings" | "Four siblings" makes me guess the operators rather than showing them. | List all five. |
| Alignment and phylogenetics, "`--strategy` accepts `auto`, `linsi`, `ginsi`, `einsi`, `fftns2`, and `parttree`" | Six MAFFT strategy names with no hint what any does or when to change from auto. | Say auto is right unless you have a reason, and name that reason. |
| Alignment and phylogenetics, "`--symbols` is `strict` or `any`" | The only flag in the chapter with no default and no explanation at all. | Say what each does and which is default. |
| Alignment and phylogenetics, "`--model` defaults to `MFP`" | MFP is an abbreviation with no expansion. | Expand it once. |
| Alignment and phylogenetics, "`--bootstrap` and `--alrt` set replicate counts" | Bootstrap and aLRT are central to reading a tree and both go unglossed, with no typical count given. | Gloss both and name the usual value. |
| Alignment and phylogenetics, "and `--seed` to 1" | I do not know what a seed does or why it matters that it is fixed. | Say it makes the run repeatable. |
| Alignment and phylogenetics, "`--keep-identical` stops it collapsing identical sequences" | I could not tell whether collapsing is good or bad for my result. | Say why you might want the duplicates kept. |
| Alignment and phylogenetics, "1-based aligned column ranges such as `10-40,55`" | This is a third coordinate convention in the chapter and the comma-range syntax is shown only by example. | State the syntax rule alongside the example. |
| Workflows, "runs a Nextflow file, a Snakefile, or the one supported nf-core pipeline" | Nextflow, Snakefile, and nf-core are three unexplained ecosystems in one clause. | Gloss each once or point to the workflows chapter first. |
| Workflows, "at least one `--expected-output` so LGE knows which artifact must receive focused provenance" | I read this twice and still cannot tell what I should put there, a file or a folder. | Say it names the folder or file the run must produce. |
| Workflows, "re-validates the retained configuration and source identity of a supported bound local run" | The densest sentence in the chapter. Bound, unbound, retained sources, and source identity are all undefined. | Rewrite for a first-time reader or move to an advanced note. |
| Workflows, "prints a two-line usage hint rather than an inventory of your project's workflows" | This documents a disappointment without saying what I should do instead. | Name the command that does list project workflows, or say there is none. |
| Tool packs and managed tools, "`--pack` is a switch that changes how the positional arguments are read" | I had to read the example twice to see that the same words mean different things with and without the flag. This is a trap. | Show the two commands side by side with their results. |
| Tool packs and managed tools, "That is a defect rather than a design choice" | Editorial aside that does not help me act. | Cut it or point at the defect list. |
| Tool packs and managed tools, "Use the Plugin Manager instead, at **Tools > Plugin Manager...**" | This is the first menu path in a chapter that has been pure command line, and it assumes I have the window open. | Say the fix requires the app window. |
| Tool packs and managed tools, "`conda run [--env <name>] <tool> [args...]`" | I do not know what conda is. It appears as a command name many times and is never explained. | Gloss conda at its first appearance in the command index. |
| Tool packs and managed tools, "It is unresolved, meaning it is not a complete installed-artifact inventory" | Three negatives and two unexplained terms. I could not tell what the file is good for. | Say what the lock file can be used for. |
| Tool packs and managed tools, "Zero means there was nothing to do ... 10 means work is pending" | The exit-status numbers 0, 10, 2, 1 are listed out of order, which made the list hard to scan. | Order them numerically. |
| Tool packs and managed tools, "the number a command hands back to say how it finished" | Exit status is glossed here, but the chapter already used exit statuses 3, 64, and 3 again in three earlier sections. | Move this gloss to the first use. |
| Provenance and metadata, "reports a missing signature artifact and exits with a failing status. That is the expected result" | A command that is supposed to fail on a normal install is confusing, and I read it twice. | Say up front that most users will never need this command. |
| Provenance and metadata, "manages PHA4GE-aligned sample metadata" | PHA4GE is an unexplained acronym used twice. | Expand it once. |
| ONT genotyping, "the haplotype definition sets an ONT genotyping run matches against" | ONT is used throughout and is never expanded in this chapter. Haplotype definition set is also undefined. | Expand ONT at first use and gloss the definition set. |
| ONT genotyping, "embedded in the project's `.lungfishmhcref` reference bundles" | MHC is not explained, and this bundle type is not in the earlier extension list. | Gloss MHC and add this format to the list. |
| ONT genotyping, "carries its own provider, model, Azure-endpoint, chunking, and review-scope options" | Five option families named and none documented. This is the only place in the chapter where a command's flags are withheld entirely. | List the flags or say where they are documented. |
| ONT genotyping, "`--percent-basis <viewed-locus\|sample-retained>` choosing the denominator" | I could not tell which denominator I want, and neither term is explained. | Say what each denominator counts. |
| ONT genotyping, "it writes a pivot-only workbook only when the bundle has no workbook of its own" | Conditional behaviour stated once inside a long sentence. I read it twice. | Split into two sentences. |
| Diagnostics, "whether Apple Containerization is available, which needs macOS 26 or later" | I do not know what containerization is or whether I need it. | Say in one clause what it enables. |
| Diagnostics, "probes the common bioinformatics tools on `PATH`" | Back to PATH, which was described once at the start and not since. | Repeat the one-line gloss. |
| What the command line cannot do, "this campaign's chapter reviews recorded four of them" | "This campaign's chapter reviews" is internal process language that means nothing to a reader. | Say four gaps are known. |
| Known defects, "Six problems in this release are worth knowing" | The section is one unbroken paragraph of six defects. As a lookup reader I could not scan it. | Make it a list or table. |
| Known defects, "fails with a provenance publication artifact error and writes no output" | This defect describes a symptom I would not connect to my working directory, and `/private/tmp` versus `/tmp` is a distinction I do not understand. | Say the fix in plain terms, which is to run from an ordinary folder such as Documents. |
| Known defects, "stops with `Empty Kraken2 report` and exit status 64 rather than reporting a fully unclassified result" | I could not tell whether this means my data is bad or the program is broken. | Say the run may have worked and simply found nothing. |
| Next, "the canonical mpileup flags, indelqual handling" | Three unexplained terms in a closing pointer, which made the next chapter sound unreadable. | Describe those pages in plain words. |

The one thing I learned. Every command writes a small record next to its output saying which tool ran, so a typed command documents itself the same way a clicked button does.

The one thing I still could not do. Get `lungfish-cli` to run at all, because the chapter tells me to add a folder to my PATH but never shows the command that does it, and the path it gives has a space in it that I would not have known to quote.

The sentence I liked most. "The help text is the authority whenever this appendix and a running copy disagree."
