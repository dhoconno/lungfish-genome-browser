# Reader 3 report

Persona. Pre-med student, English is my second language, strong on biology
words from textbooks, slow on idioms and long sentences, never opened a
terminal. I came here to look up one command, not to read the whole thing.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| What it is, "ships a command-line program alongside its window" | "Window" is used to mean the graphical app, and "command line" is never defined. I do not know what a command line is, so the whole premise of the appendix is unexplained. | Gloss "command line" in one sentence at the top. |
| What it is, "This appendix is the map of that program." | "Map" is a metaphor. I expected a picture or a diagram and found tables. | Say it lists every command. |
| What it is, "Three words appear on every page of this appendix" | There are no pages in this file, and the three words appear in one paragraph, not everywhere. I looked for pages. | Say "three terms are used throughout". |
| What it is, "a flag with no value after it is a switch" | "Switch" is introduced as a new term inside the gloss of another term. I had to read the sentence twice to see that a switch is a kind of flag. | Give switch its own sentence. |
| What it is, "the two are interchangeable as evidence" | "Interchangeable as evidence" is abstract. Evidence for what, and for whom? | Say a scripted run is documented as fully as a clicked one. |
| Finding the binary, the heading word "binary" | "Binary" is never explained and I only know the word from binary fission and binary numbers. The heading is the first place a beginner looks and it holds the hardest word on the page. | Title the section "Finding the program". |
| Finding the binary, "your `PATH`, the list of folders your shell searches" | "Shell" is used to explain PATH but is itself never explained. The gloss depends on a word I do not have. | Gloss shell before PATH, or avoid the word. |
| Finding the binary, "or add its folder to your `PATH` once" | This is offered as an option but nowhere does the appendix show how. I cannot do it from the text. | Show the one line, or drop the offer. |
| Finding the binary, "the SwiftPM product lands at `.build/debug/lungfish-cli` inside the checkout" | SwiftPM, product, and checkout are three unexplained words in eleven words. I skipped the sentence. | Say "if you built LGE yourself, the program is at this path". |
| Finding the binary, "substitute whichever path you are using" | I do not know how to substitute a path. The installed path contains a space in "Lungfish Preview.app" and nothing says whether that breaks a command. | Show one worked example of the full installed path in a real command. |
| Finding the binary, the `lungfish-cli version` code block | This is the first command in the appendix and I still do not know where to type it. No sentence anywhere says to open the Terminal app. | Add one sentence naming Terminal. |
| Command index, "The program exposes 44 top-level commands." | "Exposes" is used in a sense I have only met in "exposed to a pathogen". I read it twice. | Say "has 44 top-level commands". |
| Command index, "Run `lungfish-cli <command> --help`" | The angle brackets are never explained. I could not tell whether I type them. This blocked every command in the appendix for me. | Say once that angle brackets mark a value you replace and are not typed. |
| Command index, "The help text is the authority whenever this appendix and a running copy disagree." | "A running copy" and "the authority" are both indirect. I understood only on the second reading that the program wins. | Say the program's own help is correct if this page is out of date. |
| Command index, `align` row, "into a native `.lungfishmsa` bundle" | MSA is never expanded anywhere in this appendix, and "bundle" is not defined until much later. The index is the first thing I read. | Expand MSA once. |
| Command index, `bam` row, "bundle-owned BAM alignment tracks" | "Bundle-owned" and "track" are both unexplained here, and this row sits far above the section that defines a bundle. | Move the bundle definition above the index. |
| Command index, `fastq` row, "with 44 subcommands" | The program has 44 top-level commands and `fastq` has 44 subcommands. I assumed one number was a copy of the other and lost confidence in both. | Confirm both numbers, or word one differently. |
| Command index, `run-headless` row, "a CI-friendly alias" | CI is never expanded. I do not know what it is. | Expand CI once. |
| Command index, `provision-tools` row, "the bundled micromamba bootstrap binary" | Four unfamiliar words and no action I recognise. I could not tell whether I would ever need this command. | Say what it does for the user. |
| Global flags, "because they sit on the program itself rather than on any one operation" | "Sit on" is a metaphor for a structure I cannot picture. | Say they go before the subcommand. |
| Global flags, "`--format <text\|json\|tsv>`" | The pipe character is never explained. I did not know whether to type it. | Say once that the pipe means "choose one". |
| Global flags, "Repeat it as `-v`, `-vv`, or `-vvv`." | Nothing says what more verbosity gets me or when three would help. | Say what each level adds. |
| Global flags, "The default is to show it only when the output is a terminal." | I do not know what it means for output to "be a terminal", or what the alternative is. | Say the bar is hidden when output goes to a file. |
| Global flags, "`--threads <n>`, `-t` ... The default is automatic." | I cannot judge a thread count. I do not know what a thread is, whether more is faster, or what number to pick. | Gloss thread and say more threads means faster. |
| Global flags, "whatever older copies of this appendix said" | The appendix argues with its own past versions. As a first-time reader this made me distrust the current page. | Cut the aside and state the rule. |
| Global flags, "it is consumed by the program before any subcommand sees it" | "Consumed" is a metaphor and the sentence runs 38 words. I read it three times and still cannot tell whether `variants phase` is safe to use. | Split into two sentences and say plainly that the flag is ignored there. |
| Global flags, "Several of the wrapped tools are not bit-identical across thread counts." | "Wrapped tools" and "bit-identical" are both new. I could not judge whether this matters for my work. | Say results can differ slightly between runs. |
| Downloading records, "Its four subcommands are `ncbi`, `search`, `sra`, and `genome`, plus `ena`" | Four are named and a fifth is added in the same sentence, so the count is wrong as written. I counted twice. | Say five subcommands. |
| Downloading records, "`--no-retry` stops LGE from retrying an HTTP 429 rate-limit response." | HTTP 429 is a code I have never seen. I cannot tell when I would want this. | Say "when the server asks you to slow down". |
| Downloading records, "`lungfish-cli fetch sra download <accession>`" | Every other `fetch` command here is two words and this one is three. The subcommand list above never shows the third word. | Show the nesting in the list. |
| Downloading records, "It tries ENA first and falls back to the NCBI SRA Toolkit when ENA refuses" | "Falls back" is an idiom and "refuses" makes a database sound like a person. I understood on the second reading. | Say it uses the toolkit if ENA does not have the run. |
| Importing into a project, "A bare `lungfish-cli import <path>` fails." | I do not know what "bare" means here, and no error message or exit status is given, unlike the two `extract` cases later. | Say the command needs the subcommand word. |
| Importing into a project, "`import vcf` resolves the reference internally by matching the file's `CHROM` values against the project's bundles" | This 30-word sentence carries three unexplained ideas, resolve, CHROM, and bundles, and then ends by telling me a flag does not exist. | Split, and gloss CHROM. |
| Importing into a project, the `import fastq` paragraph | Fifteen lines of solid prose listing about fourteen flags, while `variants call` and the global flags both get tables. Looking one flag up meant reading the whole block. | Make it a table like the others. |
| Importing into a project, "`--quality-binning <illumina4\|eightLevel\|none>` defaults to `illumina4`" | Quality binning is never explained and the three values are not explained. I cannot judge the default. | Say what binning does in one sentence. |
| Importing into a project, "`<kind>` is one of `clc-workbench`, ... `igv-session-track-set`" | Nine values in one sentence, none explained. I do not recognise most of these programs. | Make it a list, or drop the ones with a chapter. |
| Reference bundles, "A reference bundle is a `.lungfishref` folder holding a sequence, its indices" | Good definition, but it arrives in section six after `.lungfishref` has already been used four times. | Define the bundle before the command index. |
| Reference bundles, the `bundle create` synopsis line | Nine bracketed options on one unbroken line. On my screen it wrapped into a wall of grey text and I could not find where one option ended. | Break the long synopses, or move them into tables. |
| Reference bundles, "This example builds the human mitochondrial reference from the manual's fixture." | The example uses `NC_012920.1.fasta` but nothing tells me where that file comes from or how to get it. I could not run the example. | Say which command downloads the fixture first. |
| Reference bundles, "`--compress` bgzips the FASTA inside the bundle" | "Bgzips" is used as a verb and never explained. I only know zip files. | Say it compresses the sequence. |
| Reference bundles, "writes a deterministic OCI-layout tarball carrying the bundle payload" | Deterministic, OCI-layout, tarball, and payload are four unknown words in one clause. I gave up on the sentence. | Say what it is for in plain words. |
| Mapping and alignment tracks, "[--preset <preset>]" | The placeholder repeats the flag name and tells me nothing. The real values arrive two sentences later. | Show the values in the synopsis. |
| Mapping and alignment tracks, "plus `asm5` and `splice`" | Six preset values are given, four are explained and the last two are not. | Explain `asm5` and `splice`, or omit them. |
| Mapping and alignment tracks, "Several inputs are treated as one sample's reads, and `--paired` binds exactly two files as that sample's mates, so run the command once per sample." | Three ideas in one 30-word sentence, and the conclusion is the opposite of what the first clause led me to expect. I read it twice. | Split into two sentences. |
| Mapping and alignment tracks, "`--min-mapq` sets a mapping-quality floor that defaults to 0" | Mapping quality is never explained and I cannot judge 0 against any scale. What would a strict value be? | Give the scale and one typical value. |
| Mapping and alignment tracks, "Five read-group flags are `--rg-id`, `--rg-sm`, `--rg-lb`, `--rg-pu` ... and `--rg-pl`" | Read groups are never explained and none of the five abbreviations is expanded. I would not know what to type after any of them. | Expand the five, or link a chapter. |
| Mapping and alignment tracks, "`--secondary` keeps secondary alignments, `--no-supplementary` drops supplementary ones" | Secondary and supplementary are used to define themselves. I still do not know the difference. | Gloss both once. |
| Mapping and alignment tracks, "the file is rewritten in place" | I do not know whether my original BAM is destroyed. This is the only destructive-sounding statement in the appendix and it carries no warning. | Say plainly that the original is overwritten. |
| Calling variants, "`--min-af <float>` ... The flag has no default of its own, and when omitted the iVar pipeline applies its own 0.05 threshold." | "Float" is never explained, and the row tells me there is no default and then gives me a default. I read it three times. | Say the effective default is 0.05 with iVar. |
| Calling variants, "`--medaka-model <id>` ... Both callers require it." | The row is required but the appendix never says where to find a model id and never gives an example. I could not run either caller. | Give one example model id. |
| Calling variants, "`--ivar-bad-quality-threshold` is the ALT_QUAL below which a call fails the bq filter, defaulting to 20" | ALT_QUAL and "bq filter" are both unexplained, and I cannot judge 20 without knowing the scale. | Gloss the quality scale once. |
| Calling variants, "since it is off by default for amplicon data" | Amplicon appears here for the first time with no gloss, inside a clause explaining a double-negative flag name. | Gloss amplicon, and name the flag positively. |
| Calling variants, "`--alignment-track <track-id>`" in the code block | The example asks me to supply a track id but nothing in the appendix tells me how to find one. The example cannot be run as printed. | Name the command that lists track ids. |
| Calling variants, "printing it unless `--execute` is given" | I did not expect a command that prints instead of running. This surprised me and I checked the sentence again. | Say plainly that nothing runs without `--execute`. |
| Classification, "`--preset <sensitive\|balanced\|precise>` defaults to `balanced`" | Sensitive and precise sound like opposites but I cannot tell what I trade between them. | Say what each preset trades. |
| Classification, "`--profile` runs Bracken afterwards, which matters because the dialog always runs Bracken and this command does not." | Bracken is never explained, "the dialog" is not defined in this appendix, and the sentence has three clauses. I read it twice. | Gloss Bracken and split the sentence. |
| Classification, "`--bracken-read-length <n>` at 150 ... `--bracken-threshold <n>` at 10" | Two bare numbers with no units and no scale. I cannot judge either. | Give the units and say when to change them. |
| Classification, "`--memory-mapping` runs the database from disk to save memory" | I do not know how much memory a database needs, so I cannot tell whether I need this flag. | Say roughly when to use it. |
| Classification, "`--confidence` defaults to 0.2" | The Kraken 2 paragraph also has a `--confidence` and gives no default. Two commands, one flag name, one number. I could not tell whether they are the same setting. | Give the Kraken 2 default too. |
| Classification, "`--max-memory` takes a Nextflow size string and defaults to `16.GB`" | "Nextflow size string" is unexplained, and `16.GB` with a dot is a format I would never have guessed. | Say the dot is required and show one more example. |
| Classification, "`--skip-assembly` is on by default and `--no-skip-assembly` turns assembly back on" | Two negatives in a row. I had to work out that the default is no assembly. | State the default as "assembly is off by default". |
| Classification, "reports how many come back independently confirmed" | I cannot judge a confirmation count without knowing how many were sent, and the default `--reads` of 20 arrives only in the next sentence. | Say the count is out of `--reads`. |
| Classification, "`--extra-args` passes BLAST URL API parameters such as `WORD_SIZE=11`" | URL API parameters is unexplained and I would have no idea where to find the list. | Link the parameter list. |
| Extracting reads and sequences, "1-based inclusive coordinates, matching the samtools faidx convention" | 1-based and inclusive are unexplained, and samtools faidx means nothing to me. A later section uses 0-based exclusive coordinates and the contrast is never drawn. | Gloss 1-based inclusive with one worked number. |
| Extracting reads and sequences, "and an exit status of 3" | Exit status is glossed only much later, in the tools section. Here the number 3 appears with no meaning attached. | Move the exit-status gloss to its first use. |
| Extracting reads and sequences, the `--by-id` to `--by-classifier` paragraph | One paragraph of about 130 words covering four mutually exclusive modes and a dozen flags. To look up one mode I had to read all four. | Split into four short entries or a table. |
| Extracting reads and sequences, "so the two produce byte-identical output for the same selection" | "Byte-identical" is not explained and "the two" refers back three clauses. | Say the window and the command give the same reads. |
| Extracting reads and sequences, "and an exit status of 64" | An earlier, similar mistake gives 3 and this one gives 64. Nothing explains why two usage errors get different numbers. | Say what each status means, once. |
| Extracting reads and sequences, "`--bundle` here is a switch rather than a path" | I understood this only because "switch" was defined about 200 lines earlier. Someone looking up this one command would not have read that. | Repeat the one-word gloss. |
| Extracting reads and sequences, "The source and output counts must match" | I could not tell whether this means the number of files or the number of reads. | Say "the same number of files". |
| Assembly, "`--profile` names a curated profile such as `meta-sensitive` or `nano-hq`" | "Curated" is a metaphor and neither profile name is explained. I cannot pick one. | Say what each profile is for. |
| Assembly, "`--memory-gb` sets a memory budget where the assembler supports one" | I have no idea what number to give. Assembly sounds like the most memory-hungry step in the manual and there is no guidance. | Give one typical value. |
| Assembly, "Two spellings pass extra arguments, and they differ by one letter." | This makes me hunt for the letter instead of telling me. I compared the two flags character by character. | Say `--extra-args` takes all of them and `--extra-arg` takes one. |
| Assembly, "MEGAHIT 1.2.9 fails most runs on Apple Silicon in this release." | A serious warning sitting as a bare paragraph with no heading and no marker, in the middle of prose. Someone looking up `assemble` could miss it. | Mark it as a warning. |
| Assembly, "a run that does complete is correct, and rerunning is the only workaround" | I cannot tell how many times to rerun, or how I would know it had failed. | Say what the failure looks like on screen. |
| FASTQ operations, "The common shape is a positional input file" | "Shape" is a metaphor for a pattern. I read it twice. | Say "most of them take". |
| FASTQ operations, "There is no `--in` or `--out`." | Telling me what does not exist does not help me type anything, and this is the third such sentence in the appendix. | Cut. |
| FASTQ operations, "a deterministic two-pass selection, so the same input and count give the same reads every time" | The jargon clause is then explained in plain words by the second clause. The first clause is unnecessary work for me. | Keep only the plain clause. |
| FASTQ operations, "Its `meanQuality` field is the arithmetic mean of the Phred scores, which is a different average from the probability-averaged Mean Q the window's FASTQ viewport shows, so the two numbers disagree on the same reads and both are correct." | A 44-word sentence with three technical terms and a paradox at the end. I read it three times and still do not know which number to report. | Split into three sentences and say which one to use. |
| FASTQ operations, "removes human reads with Deacon, choosing the database by identifier rather than by path" | The command needs `--database-id` but no identifier is shown anywhere and no command is named that lists them. I could not run this. | Name the command that lists database ids. |
| FASTQ operations, "Both default the word length to 12 and the masking to `dust`." | Word length and dust masking are both unexplained, and I cannot judge 12. | Gloss both, or drop the numbers. |
| FASTQ operations, "the group carries five lanes worth knowing about by name" | "Lanes" means sequencing lanes elsewhere in this manual. Here it means categories. I stopped at the word. | Say "five groups". |
| FASTQ operations, the five-lane paragraph | About 34 subcommands named in running prose with no line breaks. Looking one up meant scanning a solid block. It is the hardest paragraph in the appendix to search. | Make it a list or a table. |
| FASTQ operations, "`fastq ont-barcode-genotype` is marked deprecated in its own help text" | This command appears nowhere in the 34 names just listed, so it arrives with no context, and "deprecated" is unexplained. | Say it still works but should not be used for new work. |
| Sequence utilities, "Nx is the length at which records sorted longest first cover at least x percent of all bases." | This is a general formula and I had to substitute 50 myself to understand N50. I read it twice. | Give N50 as the worked example. |
| Sequence utilities, "`stats` is the default subcommand, so `lungfish-cli analyze <input>` runs it." | The example directly below then writes `analyze stats` anyway, which made me doubt the rule. | Show the short form in the example. |
| Sequence utilities, the `analyze stats` output block | The example uses `NG_000007.3.gb`, a third fixture file the appendix never introduces, and `.gb` is not explained. The other examples use `NC_012920.1`. | Use one fixture consistently. |
| Sequence utilities, "GC content  : 39.5%" | I cannot judge 39.5%. Is that normal for a human sequence? Nothing gives me a range. | Say what a typical human value is. |
| Sequence utilities, "Codons are counted in frame +1 independently for each record, and incomplete terminal codons and windows holding ambiguous symbols are left out of the denominators." | Twenty-seven words, four technical ideas, and "denominators" with no numerator ever named. I read it three times. | Split, and name what the percentages are of. |
| Sequence utilities, "in-place conversions along with symlink or hard-link aliases of the input are rejected" | Symlink and hard link are unexplained. I do not know whether I have any. | Say the input and output must be different files, and stop. |
| Sequence utilities, "`--table` selects an NCBI genetic code and defaults to 1, the standard code" | Table 1 is glossed well, but table 2 is explained only after the example has already used it. | Name table 2 where the flag is introduced. |
| Sequence utilities, "gives a 318-residue protein for MT-ND1" | I have no way to check this, because `mt-nd1.fasta` comes from the earlier example whose input file I also could not obtain. | Chain the examples from one downloadable file. |
| Sequence utilities, "Searching the human mitochondrial genome for the EcoRI site `GAATTC` finds six matches." | The result is quoted but the command is not shown, unlike every other worked result in the appendix. | Show the command. |
| Sequence utilities, "The coordinates are 0-based with an exclusive end" | This is the opposite of the 1-based inclusive rule given for `extract sequence`, and the contrast is never flagged. I would have used the wrong numbers. | Flag the difference explicitly. |
| Sequence utilities, "Field tokens are `type:<kind>`, `format:<format>`, `sample:<value>`" | A small query language defined in about 70 words with no example until after the paragraph ends. I read it twice and still could not build a query. | Move the example up. |
| Alignment and phylogenetics, "`--strategy` accepts `auto`, `linsi`, `ginsi`, `einsi`, `fftns2`, and `parttree`" | Six values, none explained, with a default of `auto`. I cannot tell why I would ever change it. | Say what one or two are for. |
| Alignment and phylogenetics, "`--model` defaults to `MFP`" | MFP is never expanded. This looks like the most important setting in tree building and it is three unexplained letters. | Expand MFP. |
| Alignment and phylogenetics, "`--bootstrap` and `--alrt` set replicate counts." | Bootstrap and aLRT are never explained, no default is given, and I cannot judge a replicate count. | Give the defaults and one typical value. |
| Alignment and phylogenetics, "`--safe` enables IQ-TREE's safe numerical mode" | "Safe numerical mode" is unexplained and it is not clear why I would not always want safety. | Say when it is needed. |
| Alignment and phylogenetics, "There is no `--msa` or `--out`." | Again the text tells me what does not exist. This is the fourth time. | Cut. |
| Workflows, "a Nextflow file, a Snakefile, or the one supported nf-core pipeline" | nf-core is never explained and I cannot tell a Nextflow file from a Snakefile from a pipeline. Three formats in one sentence. | Gloss the three formats. |
| Workflows, "so LGE knows which artifact must receive focused provenance" | "Artifact", "focused provenance", and "receive" together make a sentence I could not picture. | Say which output file the record should describe. |
| Workflows, "`--repeat-from <previous.lungfishrun>` re-validates the retained configuration and source identity of a supported bound local run" | A 24-word clause with five abstract nouns and no concrete object. I read it three times and could not tell what the command does. | Say it reruns a previous run with the same settings. |
| Workflows, "an unbound or unsupported history cannot slip past validation" | "Slip past" is an idiom and the sentence describes a thing not happening. | Say the command refuses a run it cannot verify. |
| Workflows, "without it prints a two-line usage hint rather than an inventory of your project's workflows" | Two negatives and a contrast with something I never expected. I read it twice. | Say `--nf-core` is required for the flag to be useful. |
| Tool packs, "`--pack` is a switch that changes how the positional arguments are read" | The two example commands differ by one flag and mean completely different things. This is genuinely dangerous and the sentence does not warn me strongly enough. | Add a warning marker. |
| Tool packs, "That is a defect rather than a design choice." | I cannot tell whether this means it will be fixed or whether I should avoid the command. The judgement does not tell me what to do. | Say to use the Plugin Manager and stop there. |
| Tool packs, "`conda run [--env <name>] <tool> [args...]`" | `args...` uses a placeholder style found nowhere else in the appendix, and the three dots are never explained. | Use one placeholder style throughout. |
| Tool packs, "It is unresolved, meaning it is not a complete installed-artifact inventory and does not claim compatibility with conda-lock." | Unresolved, installed-artifact inventory, and conda-lock are three unknowns defining each other. I gave up on the sentence. | Say the file records what was asked for, not what was installed. |
| Tool packs, "not guaranteed to reproduce an earlier environment byte for byte" | "Byte for byte" is an idiom and this is the third paragraph in a row telling me reproduction is not supported. | Say once that exact reproduction is not available. |
| Tool packs, "Zero means there was nothing to do or the update was applied, 10 means work is pending" | Four exit statuses in one 50-word sentence. To look up what 10 means I had to read all four. | Make it a table. |
| Tool packs, "still exits 0, because the tools did install and were only not written down" | "Were only not written down" is a construction I read three times. | Say the record failed but the install succeeded. |
| Provenance and metadata, "you have both halves of what a manuscript needs" | "Both halves" is a metaphor and "methods export" has not been introduced at this point. | Name the two things. |
| Provenance and metadata, "carry runnable command material when the recorded provenance holds enough argument detail" | Conditional, abstract, and 24 words. I could not tell whether my export would be runnable or not. | Say the export may be incomplete and how to tell. |
| Provenance and metadata, "reports a missing signature artifact and exits with a failing status. That is the expected result" | A failure that is expected is confusing on first read. I checked twice to be sure I had not misread. | Say the command is only useful if you signed the record. |
| Provenance and metadata, "manages PHA4GE-aligned sample metadata" | PHA4GE is never expanded anywhere in the appendix. | Expand PHA4GE once. |
| ONT genotyping, the section heading | ONT is used in the command index, in the `map` presets, in the FASTQ section, and in this heading, and it is never expanded. | Expand Oxford Nanopore at first use. |
| ONT genotyping, "The eleven subcommands are `list`, `validate`, `import`, `save`, `export`, `duplicate`, `delete`, and four bundle-management subcommands" | Seven are named and four are counted, so I had to add before I could trust the eleven. | Name all eleven in one list. |
| ONT genotyping, "being a scientific write workflow that appends a versioned AI haplotype analysis revision with its own provenance and review metadata" | Twenty-two words of stacked nouns with no concrete action. I could not tell what this command produces. | Say what file it writes. |
| ONT genotyping, "`--percent-basis <viewed-locus\|sample-retained>` choosing the denominator" | Neither value is explained and the denominator of what is never said. The percentages in my export would be uninterpretable. | Say what each denominator counts. |
| Diagnostics, "This is the default `debug` subcommand." | The sentence sits at the end of the entry, after the full command has been shown twice. I looked back to check what it referred to. | Move it to the start of the entry. |
| Diagnostics, "which must have arm64 Linux support" | arm64 is never explained. I do not know whether my Mac has it. | Say Apple Silicon Macs need arm64 images. |
| What the command line cannot do, "the flat index above is the map of that overlap" | Two metaphors, "flat index" and "map of that overlap", in one clause. I read it twice. | Say the index lists what both can do. |
| What the command line cannot do, "this campaign's chapter reviews recorded four of them" | "This campaign" and "chapter reviews" name an editing process I am not part of. It made me wonder whether I was reading a draft. | Cut the process reference. |
| Known defects, "worth knowing before you script around them" | "Script around" is an idiom, and the section is one 200-word paragraph of six unrelated warnings. Finding the one that affects me meant reading all six. | Make it a list. |
| Known defects, "stops with `Empty Kraken2 report` and exit status 64 rather than reporting a fully unclassified result" | This says what should have happened instead of what I should do. I could not tell whether my run failed or my sample was simply clean. | Say how to tell the two apart. |
| Known defects, "because the path the sidecar records and the path the transaction checks do not match through the `/tmp` symlink" | Sidecar, transaction, and symlink in one 30-word explanation, all placed before the plain advice at the end. I read it twice to reach the useful part. | Put "run from an ordinary folder" first. |

The one thing I learned. A subcommand, a positional argument, and a flag are three different parts of a command, and the opening paragraph explains all three clearly.

The one thing I still could not do. Type a single command, because nothing tells me to open Terminal, whether to type the angle brackets, or how to handle the space in the installed path.

The sentence I liked most. "A positional argument is a value you type in a fixed place with no name in front of it, usually an input file."
