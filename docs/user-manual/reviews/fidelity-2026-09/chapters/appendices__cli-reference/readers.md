# Merged reader report - Appendices, CLI Reference

Four readers reviewed this chapter. Reader 1 is a genetics sophomore who has never opened a terminal. Reader 2 is a wet-lab senior with two years of pipetting experience and no data-analysis background. Reader 3 is a pre-med student reading in a second language. Reader 4 has used the Geneious graphical sequence viewer for a semester and has never opened a terminal. Reader 1 submitted 131 rows, reader 2 submitted 85 rows, reader 3 submitted 127 rows, and reader 4 submitted 108 rows, for 451 source rows in total.

| Location | Issue | Suggested fix | Readers |
|---|---|---|---|
| What it is, "ships a command-line program alongside its window", "The program is called `lungfish-cli`", Finding the binary, the `lungfish-cli version` code block, Whole chapter | All four readers never learned where to type a command. The chapter names the program and says it ships "alongside its window" without ever saying it is typed into the macOS Terminal app, and no worked first command appears anywhere in the chapter. | State once, at the top, that these commands are typed into the macOS Terminal app, say where to find it, and add a three-line "your first command" block using `version`. | 1, 2, 3, 4 |
| Scattered across the chapter, fetch syntax lines, `<command> --help`, `--format <text\|json\|tsv>`, `{--proportion <p> \| --count <n>}` | All four readers hit angle brackets, square brackets, pipes, curly braces, and trailing dots with no key explaining what any of them mean or whether to type them. | Add a short notation key near the top explaining `<value>`, `[optional]`, `a\|b`, `{a\|b}`, and `...`. | 1, 2, 3, 4 |
| What it is, "a flag with no value after it is a switch" | All four readers found "switch" introduced as a new term buried inside the definition of "flag," so they missed it and had to come back. | Give switch its own short sentence instead of tucking it into the flag definition. | 1, 2, 3, 4 |
| Finding the binary, "the list of folders your shell searches" / "your `PATH`" | All four readers met "shell" used to explain PATH while shell itself is never explained, so the gloss depends on a word they do not have. | Gloss shell before using it to explain PATH, or drop the word. | 1, 2, 3, 4 |
| Finding the binary, "add its folder to your `PATH` once", Diagnostics, "probes the common bioinformatics tools on `PATH`" | All four readers were offered adding the program to PATH as an option, but no command showing how to do it appears anywhere in the chapter, including when PATH resurfaces later at Diagnostics. | Show the one line that adds the folder to PATH, or say plainly that the full-path route is the only supported one. | 1, 2, 3, 4 |
| Finding the binary, "the SwiftPM product lands at `.build/debug/lungfish-cli`" / "If you build from source instead" | All four readers met SwiftPM, product, and checkout stacked in one sentence with no gloss, aimed at a reader who compiles the app themselves. | Mark the sentence as for people building from source, and drop or explain SwiftPM, product, and checkout. | 1, 2, 3, 4 |
| Finding the binary, "substitute whichever path you are using" | All four readers could not tell what "substitute" means to do in practice, whether to retype the long path in every example or set it up once. | Show one worked example with the full path so the substitution is visible once, and say the path can be copied and reused. | 1, 2, 3, 4 |
| Command index, "The program exposes 44 top-level commands" / `fastq` row "with 44 subcommands" | All four readers noticed the program's top-level command count and the `fastq` subcommand count are both 44 and assumed one was a copy error or typo. | Confirm both numbers are correct, or note explicitly that the match is a coincidence. | 1, 2, 3, 4 |
| Command index, `align` row and other rows naming `.lungfish*` extensions | All four readers met unglossed `.lungfish*` bundle extensions (`.lungfishmsa`, `.lungfishref`, and others) starting at the very first row of the command index, with MAFFT and "bundle" also unexplained there. | Add one lead-in sentence before the table saying every `.lungfish*` name is a folder format LGE makes, with a pointer to File Formats. | 1, 2, 3, 4 |
| Command index, `conda` row, Tool packs, "`conda run [--env <name>] <tool> [args...]`" | Three readers found conda used repeatedly as a command name and section heading without ever being explained, with one assuming it was a typo. | Gloss conda at its first appearance in the command index as the package installer LGE uses. | 1, 2, 4 |
| Global flags, "Repeat it as `-v`, `-vv`, or `-vvv`" | All four readers could not judge which verbosity level to pick or what each extra `-v` actually adds. | Say briefly what each verbosity level adds, or say to start with `-v`. | 1, 2, 3, 4 |
| Global flags, "`--threads <n>`, `-t` ... The default is automatic" | All four readers found thread unexplained and could not judge what number to use or whether more threads help. | Gloss thread as one parallel worker and give a sane starting number or say more threads run faster. | 1, 2, 3, 4 |
| Global flags, "whatever older copies of this appendix said" | Three readers were confused by the chapter arguing with a past version they had never seen, which made them distrust the current page. | Cut the reference to older copies and state the rule plainly. | 1, 3, 4 |
| Global flags, "Several of the wrapped tools are not bit-identical across thread counts" | All four readers met "wrapped tools" and "bit-identical" unexplained and could not tell whether this means their results would be wrong. | Say plainly that results can differ slightly between runs and that this is not an error. | 1, 2, 3, 4 |
| Downloading records, "Its four subcommands are `ncbi`, `search`, `sra`, and `genome`, plus `ena`" | All four readers counted four subcommands stated and then five listed in the same sentence, and stopped to recount. | Say five subcommands, or say four plus one, so the count is not self-contradicting. | 1, 2, 3, 4 |
| Downloading records, "retrying an HTTP 429 rate-limit response" | All four readers met HTTP 429 with no gloss and could not judge whether they want retries on or off. | Say plainly that HTTP 429 means the server asked LGE to slow down. | 1, 2, 3, 4 |
| Importing into a project, "A bare `lungfish-cli import <path>` fails" | All four readers were told the bare command fails but not what they would see when it does, unlike other sections that give the exact error text. | Give the error message or exit status, the way other sections do. | 1, 2, 3, 4 |
| Importing into a project, "`--quality-binning <illumina4\|eightLevel\|none>` defaults to `illumina4`" | All four readers found quality binning unexplained and could not evaluate the default, and two also noted the inconsistent camelCase value name. | Explain what binning discards in one sentence, and make the value names consistent in case. | 1, 2, 3, 4 |
| Reference bundles, "`--compress` bgzips the FASTA inside the bundle" | All four readers met "bgzip" used as a verb with no gloss. | Say it compresses the file in a way the tools can still index or read. | 1, 2, 3, 4 |
| Reference bundles, first code block, Sequence utilities, "`lungfish-cli analyze stats NG_000007.3.gb`", "gives a 318-residue protein for MT-ND1" | All four readers hit example commands that use fixture files, such as `NC_012920.1.fasta` and `NG_000007.3.gb`, that no earlier command in the chapter produces and that are never explained how to obtain. | Show the fetch command that produces each fixture before it is first used, and use the chapter's own named fixtures consistently. | 1, 2, 3, 4 |
| Reference bundles, "writes a deterministic OCI-layout tarball" | All four readers met "deterministic," "OCI-layout," and "tarball" stacked with no gloss, with several giving up on the sentence entirely. | Say what the file is for before naming its format, or mark the sentence as advanced. | 1, 2, 3, 4 |
| Mapping and alignment tracks, "`--min-mapq` sets a mapping-quality floor that defaults to 0" | All four readers found mapping quality unexplained and could not judge 0 against any scale or name a stricter value. | Say what mapping quality measures, give its range, and name one commonly used cutoff. | 1, 2, 3, 4 |
| Mapping and alignment tracks, "Five read-group flags are `--rg-id`, `--rg-sm`, `--rg-lb`, `--rg-pu` ... and `--rg-pl`" | All four readers met read group unexplained and none of the five two-letter suffixes expanded, so they would not know what to type after any of them. | Say what a read group is and expand each of the five abbreviations, ideally in a short table. | 1, 2, 3, 4 |
| Mapping and alignment tracks / Calling variants, "`--alignment-track <track-id>`" | All four readers hit a literal `<track-id>` placeholder in a worked example with no command shown that lists real track ids, so the example cannot be run as printed. | Name the command that lists track ids, for example `bundle list --tracks`, and use a real-looking id in the example. | 1, 2, 3, 4 |
| Calling variants, "`--min-af <float>` ... The flag has no default of its own, and when omitted the iVar pipeline applies its own 0.05 threshold" | All four readers read this as self-contradicting, since it says there is no default and then states one, and none could tell what the other four callers do when the flag is omitted. | State the effective default plainly as 0.05 for iVar, and say what the other callers do. | 1, 2, 3, 4 |
| Calling variants, "the ALT_QUAL below which a call fails the bq filter" | All four readers met ALT_QUAL and "bq filter" as raw, unglossed tool jargon and could not judge the default of 20 without a scale. | Say plainly that ALT_QUAL is the quality score of the alternate base, and give its scale. | 1, 2, 3, 4 |
| Classification, "`--preset <sensitive\|balanced\|precise>` defaults to `balanced`" | All four readers could not judge what sensitive and precise trade against each other. | Add one clause per preset saying what it favors and what it gives up. | 1, 2, 3, 4 |
| Classification, "`--bracken-read-length <n>` at 150" | All four readers met the number 150 with no unit and no guidance on whether it must match their own read length. | Say the number is in bases and that it should match the sequencing read length. | 1, 2, 3, 4 |
| Classification, "`--confidence` defaults to 0.2" | All four readers noticed the same flag name, `--confidence`, is 0.2 for TaxTriage but unspecified for Kraken 2 earlier, and could not tell whether the two settings mean the same thing. | State plainly whether the two confidence values are comparable, and give the Kraken 2 default too. | 1, 2, 3, 4 |
| Classification, "`--max-memory` takes a Nextflow size string and defaults to `16.GB`" | All four readers found "Nextflow size string" unglossed and the dot inside `16.GB` looked like a typo, so they would have typed `16GB` and failed. | Show the format explicitly, note the dot is required, and give one more example. | 1, 2, 3, 4 |
| Extracting reads and sequences, "1-based inclusive coordinates", Sequence utilities, "The coordinates are 0-based with an exclusive end" | All four readers hit two different coordinate conventions in the same chapter, 1-based inclusive for `extract sequence` and 0-based exclusive for ORFs, with the contrast never acknowledged, so several assumed one was a mistake. | State explicitly, at the second convention's first use, that the two commands use different conventions and why. | 1, 2, 3, 4 |
| Extracting reads and sequences, "and an exit status of 3", Tool packs, "the number a command hands back to say how it finished" | All four readers met exit status numbers used from the first extract command onward, with the actual gloss of what an exit status is arriving much later, in Tool packs. | Move the exit-status gloss to its first use in the chapter. | 1, 2, 3, 4 |
| Assembly, "MEGAHIT 1.2.9 fails most runs on Apple Silicon in this release" | All four readers could not judge how often "most runs" fails, could not tell whether their own Mac is Apple Silicon, and one could not tell if the dialog has the same problem. | Say roughly how often it fails, explain Apple Silicon means M-series Macs, how to check, and whether the window is affected too. | 1, 2, 3, 4 |
| FASTQ operations, "the arithmetic mean of the Phred scores ... probability-averaged Mean Q" | All four readers found this the subtlest point in the chapter, a 44-word sentence claiming two different averages of the same reads disagree and both are correct, with Phred score itself unglossed. | Explain Phred score, split the sentence, and say plainly which number to quote. | 1, 2, 3, 4 |
| FASTQ operations, "removes human reads with Deacon, choosing the database by identifier" | All four readers found Deacon unglossed and no command named that lists the database identifiers needed to run the command. | Say briefly what Deacon is and name the command that lists database identifiers. | 1, 2, 3, 4 |
| FASTQ operations, "five lanes worth knowing about by name" | All four readers met roughly thirty to forty subcommand names run together in one paragraph with no descriptions, making the exact command they came to look up impossible to scan for. | Put the subcommands in a table with one short phrase per command. | 1, 2, 3, 4 |
| Sequence utilities, "Nx is the length at which records sorted longest first cover at least x percent" | All four readers read the Nx definition multiple times and still could not apply it, partly because the worked example is a single-sequence file where every statistic equals the total length. | Add a two-sentence worked example on a small multi-contig set, and say which direction is better. | 1, 2, 3, 4 |
| Sequence utilities, "Codons are counted in frame +1" | All four readers met reading-frame notation used before the `translate` section explains what a frame is. | Move the frame explanation before its first use, or repeat it briefly here. | 1, 2, 3, 4 |
| Sequence utilities, "finds six matches" (EcoRI site search) | All four readers were given a result to check but no runnable command shown for it, unlike every other worked result in the appendix. | Show the command that produces the result. | 1, 2, 3, 4 |
| Alignment and phylogenetics, "`--strategy` accepts `auto`, `linsi`, `ginsi`, `einsi`, `fftns2`, and `parttree`" | All four readers met six MAFFT strategy names with no guidance on what any of them do or when to leave auto. | Say auto is right unless there is a specific reason, and name one or two reasons to change it. | 1, 2, 3, 4 |
| Alignment and phylogenetics, "`--model` defaults to `MFP`" | All four readers found MFP unexpanded, in what one called the most important setting in tree building. | Expand MFP as ModelFinder Plus and say it picks a model automatically. | 1, 2, 3, 4 |
| Alignment and phylogenetics, "`--bootstrap` and `--alrt` set replicate counts" | All four readers met bootstrap and aLRT unglossed with no default or typical replicate count given for either. | Gloss both terms and give a usual replicate count for each. | 1, 2, 3, 4 |
| Workflows, "runs a Nextflow file, a Snakefile, or the one supported nf-core pipeline" | All four readers met three unexplained pipeline ecosystems named in one clause and could not tell what a workflow means in this context. | Say plainly that a workflow is a saved multi-step recipe, and gloss the three formats once. | 1, 2, 3, 4 |
| Workflows, "so LGE knows which artifact must receive focused provenance" | All four readers could not picture "artifact" and "focused provenance," and could not work out what to actually pass to `--expected-output`. | Say plainly that the flag names the folder or file the run must produce. | 1, 2, 3, 4 |
| Workflows, "`--repeat-from <previous.lungfishrun>` re-validates the retained configuration and source identity of a supported bound local run" | All four readers called this the densest sentence in the chapter, reading it three or four times and still not being able to say what the flag does. | Rewrite as one short sentence saying it reruns a previous run with the same settings. | 1, 2, 3, 4 |
| Tool packs, "It is unresolved, meaning it is not a complete installed-artifact inventory and does not claim compatibility with conda-lock" | All four readers met three unknown terms defining each other in one dense, negative sentence, and could only conclude the file should not be used. | Lead with what the lock file is good for, and say it records what was asked for rather than what was installed. | 1, 2, 3, 4 |
| Tool packs, "Zero means there was nothing to do ... 10 means work is pending" | All four readers found four exit-status meanings packed into one dense, unordered sentence, hard to scan for the one code they needed. | Put the exit codes in a small ordered table, and say where the number appears. | 1, 2, 3, 4 |
| Provenance and metadata, "reports a missing signature artifact and exits with a failing status. That is the expected result" | All four readers found a command that is supposed to fail on an ordinary install confusing, needing to reread it to trust it was not a bug. | Merge into one sentence saying a failure here is normal when nothing has been signed. | 1, 2, 3, 4 |
| Known defects, "Running a command whose working directory sits under `/private/tmp`" | All four readers met working directory never explained, so none could tell whether their own runs were affected by this defect. | Say how to tell where you are, and that a folder in Documents is unaffected. | 1, 2, 3, 4 |
| Finding the binary, heading word "binary" | Three readers almost skipped the section because "binary" in the heading was never explained and reads like it might mean binary data. | Retitle the section "Finding the program," or gloss binary in its first sentence. | 2, 3, 4 |
| Finding the binary, "type that whole path" / the installed path containing a space | Three readers noted the installed path contains a space in "Lungfish Preview.app" with nothing warning that a space needs quoting, so their first attempt would have failed. | Show the path quoted or escaped exactly as it must be typed. | 1, 2, 4 |
| Downloading records, "`lungfish-cli fetch sra download <accession>`" | Three readers found that `sra` has its own nested subcommands not signalled anywhere the index lists the top-level subcommands. | Say explicitly that `sra` is a group with its own subcommands, or show the nesting in the index. | 2, 3, 4 |
| Importing into a project, "`<kind>` is one of `clc-workbench`, ... `igv-session-track-set`" | Three readers met nine values run together in one sentence with no line breaks and no explanation of which program produces each. | Break the nine values into a list or table, and say which program produces each. | 1, 2, 3 |
| Reference bundles, code block line endings ending in `\` | Three readers did not know what the trailing backslash does and would have typed the whole command on one line. | Note once that a trailing backslash continues a command onto the next line. | 1, 2, 4 |
| Mapping and alignment tracks, "`--preset <preset>`" / platform preset values | Three readers met preset values, such as `asm5` and `splice`, listed with some but not all explained, and could not match a preset to their own data without already knowing their sequencing platform. | Explain every preset value, or pair each with its instrument name in the list. | 1, 3, 4 |
| Mapping and alignment tracks, "`--secondary` keeps secondary alignments, `--no-supplementary` drops supplementary ones" | Three readers found secondary and supplementary alignments defined only in contrast to each other, leaving the actual difference unglossed. | Gloss both terms in one sentence each. | 1, 3, 4 |
| Mapping and alignment tracks, "the file is rewritten in place" | Three readers found this the only destructive operation in the appendix, with its warning buried mid-paragraph and no instruction to keep a backup. | Give the warning its own sentence and say to keep a copy of the original BAM. | 1, 3, 4 |
| Calling variants, "`--medaka-model <id>` The ONT basecaller model" | Three readers found this a required flag with ONT and basecaller model unexplained and no source given for where to find a model id, so they could not run the command at all. | Say where a model id comes from, and give one example. | 1, 2, 3 |
| Calling variants, "since it is off by default for amplicon data" | Three readers met amplicon unglossed and could not tell whether their own data counts as amplicon data. | Gloss amplicon as reads generated from PCR primers, and say when the default applies. | 1, 3, 4 |
| Calling variants, "printing it unless `--execute` is given" | Three readers were surprised that the command prints a plan instead of running by default, and had to reread the sentence to catch it. | Lead with the fact that nothing runs without `--execute`. | 1, 3, 4 |
| Classification, "which matters because the dialog always runs Bracken and this command does not" | Three readers met Bracken unglossed and "the dialog" undefined in the same clause that flags a real behavior mismatch between the command and the window. | Gloss Bracken and give the dialog-versus-command mismatch its own sentence. | 1, 3, 4 |
| Classification, "`--skip-assembly` is on by default and `--no-skip-assembly` turns assembly back on" | Three readers had to read this double negative several times to work out that assembly is off by default. | State the default plainly as "assembly is off by default." | 1, 3, 4 |
| Classification, "reports how many come back independently confirmed" | Three readers could not judge what confirmation count out of the default 20 reads would count as reassuring. | Say the count is out of `--reads`, and give one interpretation of a good result. | 1, 3, 4 |
| Extracting reads and sequences, "and an exit status of 64" versus 3 elsewhere | Three readers noticed two different exit numbers, 3 and 64, given for what looks like the same kind of usage mistake, with no explanation of the difference. | Say what each exit status means, once, or note the numbers are not meant to be diagnostic to a human reader. | 1, 2, 3 |
| Extracting reads and sequences, "so the two produce byte-identical output for the same selection" | Three readers met "byte-identical" unexplained, though one called it the only assurance in the appendix that the command matches the window and nearly missed it. | Say plainly that the command and the window give the same reads for the same selection. | 1, 3, 4 |
| Extracting reads and sequences, "`--bundle` here is a switch rather than a path" | Three readers had already learned `--bundle` as a path from other commands and had to backtrack to recover the earlier gloss of "switch." | Repeat the one-word gloss inline wherever `--bundle` behaves as a switch instead of a path. | 2, 3, 4 |
| Assembly, "Two spellings pass extra arguments, and they differ by one letter" | Three readers had to hunt character by character for the one-letter difference between the two flags, since no worked example shows both side by side. | Show both flags side by side in one example line. | 1, 3, 4 |
| FASTQ operations, "a deterministic two-pass selection" | Three readers met "deterministic two-pass selection" as opaque jargon that is then restated in plain words later in the same sentence, so the jargon clause did no useful work. | Keep only the plain clause, saying the same input and count give the same reads every time. | 1, 3, 4 |
| FASTQ operations, "Both default the word length to 12 and the masking to `dust`" | Three readers met word length and dust masking unexplained and could not judge whether 12 is a reasonable default. | Gloss both terms briefly, or say plainly that the defaults are fine to leave alone. | 1, 3, 4 |
| Sequence utilities, "gives a 318-residue protein for MT-ND1" | Three readers had no output shown to check the 318-residue claim against and could not tell whether a different number on their own run would mean an error. | Show the first line of expected output, and say what a wrong table looks like. | 1, 2, 4 |
| Sequence utilities, "Field tokens are `type:<kind>`, `format:<format>`" | Three readers met a small query grammar defined in about 70 words with no list of valid values and no example until after the explanation ends. | Move a worked example above the explanation, and list the valid token values. | 1, 2, 3 |
| Workflows, "prints a two-line usage hint rather than an inventory of your project's workflows" | Three readers were told what `workflow list` does not do, without being told what command actually lists a project's workflows. | Name the command that lists project workflows, or say plainly there is none. | 1, 3, 4 |
| Tool packs, "`--pack` is a switch that changes how the positional arguments are read" | Three readers found the same two words after the command mean two different things depending on this one flag, calling it a trap or the most confusing sentence in the appendix. | Show both commands side by side as two labelled example lines. | 1, 3, 4 |
| Tool packs, "That is a defect rather than a design choice" | Three readers found this editorial aside honest but unhelpful, since it does not say what to do next. | Cut the aside, or follow it immediately with the fix, naming the Plugin Manager button to click. | 1, 2, 3 |
| Provenance and metadata, "manages PHA4GE-aligned sample metadata" | Three readers met PHA4GE used without ever being expanded anywhere in the appendix. | Expand PHA4GE at its first use. | 1, 3, 4 |
| ONT genotyping, section heading and throughout | Three readers met ONT used repeatedly across the chapter, including in the section heading, and never expanded to Oxford Nanopore Technologies. | Expand ONT at its first use in the chapter. | 1, 2, 3 |
| ONT genotyping, "`--percent-basis <viewed-locus\|sample-retained>` choosing the denominator" | Three readers could not tell which of the two values to choose, since the denominator each one counts is never stated. | Say plainly what each denominator counts. | 1, 2, 3 |
| What the command line cannot do, "this campaign's chapter reviews recorded four of them" | Three readers found this internal editorial-process language meaningless as a reader, with one wondering if they were reading an unfinished draft. | Cut the reference to the campaign and its chapter reviews, and say plainly that four gaps are known. | 1, 3, 4 |
| Known defects, whole section | Three readers found six defects packed into one dense, unbroken paragraph, making it hard to scan for the one that might affect them. | Break the six defects into a list or a five-row table. | 1, 2, 3 |
| What it is, "the same provenance sidecar files" / "the small JSON records" | Two readers met JSON used with no gloss, assumed already known. | Gloss JSON as a plain-text file format at its first use. | 1, 2 |
| Command index, `bam` row, "bundle-owned BAM alignment tracks" | Two readers met BAM, bundle-owned, and track stacked unexplained in a row that sits well above the section that defines a bundle. | Gloss BAM and track at this first use, and move the bundle definition above the command index. | 1, 3 |
| Command index, `run-headless` row, "a CI-friendly alias" | Two readers met CI unexpanded anywhere in the appendix. | Expand CI at its first use. | 3, 4 |
| Global flags, "The default is to show it only when the output is a terminal" | Two readers read this three times and still could not picture what it means for output to "be" a terminal. | Say plainly that the progress bar is hidden when output goes to a file or script. | 1, 3 |
| Global flags, "`lungfish-cli variants phase --threads 4` records one thread rather than four" | Two readers found this bug notice buried in the flags section and could not tell whether it means the run itself is affected or only its record. | Move the notice to Known defects, cross-referenced from here, and say plainly whether the run itself is affected. | 1, 4 |
| Global flags, "it is consumed by the program before any subcommand sees it" | Two readers read this 38-word sentence three times and still could not tell whether `variants phase --threads` is silently ignored or produces an error. | Split into two sentences and say plainly that the flag is silently ignored there. | 2, 3 |
| Importing into a project, "matching the file's `CHROM` values" | Two readers met CHROM used as if already known, as a VCF column name. | Say plainly that it matches the chromosome names written in the file. | 1, 3 |
| Importing into a project, "`--recipe <vsp2\|wgs\|hifi\|none>` picks a processing recipe" | Two readers met VSP2, WGS, and HiFi as unexplained abbreviations they could not choose between. | Spell out each abbreviation once. | 1, 4 |
| Importing into a project, "`--clumping-tool <auto\|bbtools\|trim-galore\|none>`" | Two readers found "clumping" never defined anywhere in the appendix. | Say briefly that clumping groups similar reads so they compress better. | 1, 4 |
| Importing into a project, "`--compression <fast\|balanced\|maximum>`" | Two readers could not judge the tradeoff of choosing maximum compression. | Name the tradeoff, for example that maximum costs more time. | 1, 4 |
| Importing into a project, "`--samplesheet <csv>` holding `sample`, `r1`, and `r2` columns" | Two readers did not know whether the column names are case sensitive or whether there is a header row, since no example samplesheet is shown. | Show a three-line example samplesheet with a header row. | 2, 4 |
| Reference bundles, "a sequence, its indices, and any annotation" / "its indices" | Two readers met "index" or "indices" used for a sequence file with no gloss. | Gloss index as a helper file that lets tools jump to a position quickly. | 1, 4 |
| Reference bundles, the `bundle create` syntax line | Two readers lost their place inside a syntax line running to nine or more bracketed options with no line breaks. | Break long syntax lines across multiple lines, or move them into tables. | 2, 3 |
| Mapping and alignment tracks, "writes a coordinate-sorted, indexed BAM" | Two readers knew BAM only vaguely and found "coordinate-sorted" and "indexed" unglossed. | Gloss both terms once, or link to the alignment chapter. | 1, 2 |
| Classification, "override the numbers behind the preset" | Two readers could not override the preset's numeric values meaningfully, since those numbers are never printed anywhere. | Show the three preset value sets in a small table. | 1, 2 |
| Classification, "`--memory-mapping` runs the database from disk to save memory" | Two readers could not judge whether they need this flag, since no guidance is given for how much memory a database needs. | Say roughly when this flag is needed. | 1, 3 |
| Classification, "`--bracken-level <D\|P\|C\|O\|F\|G\|S>`" / "`--rank` to `S`" | Two readers met seven single-letter taxonomic rank codes with no key, appearing twice under different flag names. | Spell out the seven taxonomic ranks once, and cross-reference the second use. | 1, 4 |
| Classification, "an installed database such as `Viral` or `Standard-8`" | Two readers did not know what "installed" means here or how to see which databases they have, with the answer appearing only later in the chapter. | Point forward to the listing command, such as `conda db list`, in the same sentence. | 1, 2 |
| Classification, "passes BLAST URL API parameters such as `WORD_SIZE=11`" | Two readers met "URL API parameters" unglossed with no link to where the full list of parameters lives. | Link to the BLAST parameter list. | 1, 3 |
| Extracting reads and sequences, "takes one of four mutually exclusive mode switches" | Two readers had to read one dense paragraph covering four modes and a dozen flags just to find the one mode they needed. | Split the four modes into short separate entries or a table. | 1, 3 |
| Extracting reads and sequences, "`--taxon 2697049`" | Two readers met a bare taxon id number with no explanation of what it is or where to get one. | Say it is the NCBI taxonomy number, and name where the classifier result lists it. | 1, 2 |
| FASTQ operations, "more than this appendix walks through" | Two readers were told, in a lookup appendix, that their command might not even be listed, with no pointer to where the rest live. | Point directly to `fastq --help` for the full list, in the same sentence. | 1, 4 |
| FASTQ operations, "`fastq ont-barcode-genotype` is marked deprecated" | Two readers found this command appears from nowhere, missing from both the command index and the five-lane list just given. | List the command where the other subcommands are listed, and say what "deprecated" means for it. | 1, 3 |
| Sequence utilities, "GC and AT skew" | Two readers met "skew" unglossed and could not tell whether a skew value is good or bad. | Gloss skew in one sentence. | 1, 4 |
| Sequence utilities, "symlink or hard-link aliases of the input are rejected" | Two readers met symlink and hard link as unfamiliar filesystem terms. | Say plainly that the input and output must be different files. | 1, 3 |
| Sequence utilities, "selects an NCBI genetic code and defaults to 1" | Two readers found the second genetic code (table 2) explained only after an example had already used it. | Name and explain table 2 where the flag is first introduced. | 1, 3 |
| Alignment and phylogenetics, "and `--seed` to 1" | Two readers did not know what a seed does or why fixing it matters. | Gloss seed as what makes a run repeatable. | 1, 4 |
| Alignment and phylogenetics, "1-based aligned column ranges such as `10-40,55`" | Two readers could not tell whether a bare number like `55` in a range means a single column or an open range. | State the range syntax rule explicitly alongside the example. | 1, 4 |
| Workflows, "an unbound or unsupported history cannot slip past validation" | Two readers did not know what "bound" and "unbound" mean here, and the sentence describes a safety property rather than an action to take. | Say plainly which kinds of runs can be repeated with `--repeat-from`. | 3, 4 |
| Tool packs, "Use the Plugin Manager instead, at Tools > Plugin Manager..." | Two readers noted this is the only menu path in an otherwise pure command-line appendix, the one place they learned a command has a dialog equivalent, with no such note anywhere else. | State once near the top of the chapter whether each command has a dialog equivalent. | 1, 4 |
| Known defects, "stops with `Empty Kraken2 report` and exit status 64 rather than reporting a fully unclassified result" | Two readers could not tell whether this defect means their data is bad or their run failed outright. | Say plainly that the run may have completed and simply found nothing. | 1, 3 |
| Assembly, "`--profile` names a curated profile such as `meta-sensitive` or `nano-hq`" | Two readers were given two example profile names with no full list and no explanation of what "curated" means here, so neither could choose a profile. | Say where the full list of profiles is printed, and what each is for. | 2, 3 |
| What it is, "with two leading hyphens" | One reader could not tell from the text whether the two hyphens are two key presses or a single dash character to find. | Say plainly that the two hyphens are two presses of the ordinary hyphen key. | 2 |
| What it is, "This appendix is the map of that program." | One reader expected a picture from the word "map" and found tables instead. | Say plainly that the appendix lists every command. | 3 |
| What it is, "Three words appear on every page of this appendix" | One reader looked for pages in a file that has none, and the three words appear once, not repeatedly. | Say "three terms are used throughout" instead of "every page." | 3 |
| What it is, "the two are interchangeable as evidence" | One reader found "interchangeable as evidence" abstract, unsure what it means or for whom. | Say plainly that a scripted run is documented as fully as a clicked one. | 3 |
| What it is, "it runs the same operations the window runs, with no window at all" | One reader read this twice because "no window at all" sounded like a limitation rather than the whole point of the appendix. | Say plainly that anything clickable in the window can also be typed. | 4 |
| What it is, "as `convert` does in `lungfish-cli convert`" | One reader found the definitions of subcommand, positional argument, and flag good individually but never shown together labelled on one example line. | Add one annotated example line showing program, subcommand, positional argument, and flag together. | 4 |
| Finding the binary, the two code blocks | One reader could not tell which of two adjacent code blocks was the typed command and which was the output, and briefly assumed both had to be typed. | Label the output block "Output," or add a comment line. | 2 |
| Command index, "The help text is the authority whenever this appendix and a running copy disagree." | One reader understood only on a second reading that the running program wins over the printed page. | Say plainly that the program's own help text is correct if this page is out of date. | 3 |
| Command index, "The help text is the authority whenever this appendix and a running copy disagree" | One reader did not know how to get "a running copy" of help text on screen to compare against. | Show one `--help` invocation with its first lines of output. | 4 |
| Command index, `analyze` row, "format validity" | One reader did not know what makes a sequence file valid or invalid. | Say it checks that the file is not corrupt or malformed. | 1 |
| Command index, `build-db` row, "Build a SQLite index" | One reader found SQLite never explained anywhere in the chapter. | Say it builds a small searchable database file. | 1 |
| Command index, `assemble` row, "Assemble reads de novo" | One reader half-remembered "de novo" from lecture but could not define it. | Add a short clause: "assembling reads with no reference to guide them." | 1 |
| Command index, "`gatk` Build or run GATK4 germline-variant commands (10 subcommands)" | One reader found this row states a subcommand count without listing the subcommands, unlike other rows that do list them. | List subcommands consistently in every row, or in none. | 2 |
| Command index, `provision-tools` row, "the bundled micromamba bootstrap binary" | One reader met four unfamiliar words with no action they could recognize, and could not tell whether they would ever need this command. | Say plainly what the command does for the user. | 3 |
| Global flags, "`--no-color` strips the colour codes" | One reader was stopped by British spelling appearing once against American spelling everywhere else in the chapter. | Use one spelling consistently. | 1 |
| Downloading records, "Accessions are repeatable" | One reader could not tell whether this means repeating the flag or listing accessions separated by spaces, with no example given. | Show one command fetching two accessions. | 1 |
| Downloading records, "The limit defaults to 20" | One reader did not know whether 20 search results is generous or restrictive. | Say what a typical NCBI search returns. | 1 |
| Downloading records, "falls back to the NCBI SRA Toolkit when ENA refuses" | One reader met SRA, ENA, and Toolkit all unglossed at their first appearance in the body text. | Gloss SRA and ENA once at this first use. | 1 |
| Downloading records, "`GCF_` or `GCA_`" and "`MN908947.3`" | One reader knew accessions from ordering primers but not these prefixes, and had to reread a long sentence describing two different behaviors. | Break the sentence at the point it shifts to a second behavior. | 2 |
| Downloading records, "`--api-key <key>`" | One reader met this flag on four commands with no explanation of whether they have one or need one. | Say once that the key is optional and raises the NCBI rate limit. | 4 |
| Downloading records, "resolved through NCBI's assembly database ... comes back as the exact record you asked for" | One reader read three times to catch the implication that the `GCF_` route can substitute a different accession, which the text never states outright. | State plainly which fetch route can substitute a different accession than the one requested. | 4 |
| Downloading records, "It tries ENA first and falls back to the NCBI SRA Toolkit when ENA refuses" | One reader found "falls back" and "refuses" idiomatic, with the database described as if it were a person, and understood only on a second reading. | Say plainly that LGE uses the toolkit if ENA does not have the run. | 3 |
| Importing into a project, "`--compression <fast\|balanced\|maximum>`, the last defaulting to `balanced`" | One reader first misread "the last" as referring to the value `maximum` rather than to the `--compression` flag. | Name the flag again instead of using the pronoun "the last." | 2 |
| Importing into a project, the `import fastq` paragraph | One reader had to read fifteen lines of solid prose covering about fourteen flags, while nearby sections use tables instead. | Convert the paragraph into a table, matching nearby sections. | 3 |
| Importing into a project, "`--project <path>`" | One reader never saw a real project path in any example and did not know what one looks like on disk. | Show one real project path in a worked example. | 4 |
| Importing into a project, "`import cz-id` additionally requires `--project` and `--sample-name`" | One reader learned two of six classifier importers behave differently but saw no example command for any of the other five. | Show one worked example for a classifier import. | 4 |
| Reference bundles, "A reference bundle is a `.lungfishref` folder holding a sequence, its indices" | One reader found the actual definition of a reference bundle arriving in section six, after the term had already been used four times. | Move the bundle definition above the command index. | 3 |
| Reference bundles, "answers `Valid` on a healthy one" | One reader found this the one place output is described in prose rather than shown as a code block like everywhere else. | Show the output as a code block, matching the rest of the chapter. | 4 |
| Mapping and alignment tracks, "overrides the `@SQ` sequence name" | One reader met `@SQ` as unexplained BAM header notation. | Say it is the reference name recorded in the file's header. | 1 |
| Mapping and alignment tracks, "[--preset <preset>]" | One reader found this placeholder in the syntax line repeats the flag name and tells nothing, with the real values arriving two sentences later. | Show the actual preset values directly in the syntax line. | 3 |
| Mapping and alignment tracks, "Several inputs are treated as one sample's reads, and `--paired` binds exactly two files as that sample's mates, so run the command once per sample." | One reader read this 30-word sentence twice because its ending seems to contradict its opening clause. | Split into two shorter sentences. | 3 |
| Calling variants, "defaulting to 0.75 ... defaulting to 0.25 ... defaulting to 20" | One reader met three numeric defaults across three sentences with no indication of which direction, higher or lower, is more permissive. | Add the direction of each setting, for example "raising this is stricter." | 1 |
| Classification, TaxTriage paragraph | One reader had to scan roughly fifteen sentences covering three commands in one dense paragraph to find a single flag. | Break the flag list into a table, matching the variants section. | 2 |
| Classification, "`--nf-profile` defaults to `docker`" | One reader met Docker named without ever being told whether it must be installed separately. | Say once whether Docker must be installed separately. | 4 |
| Classification, "`--revision` defaults to the pinned TaxTriage commit" | One reader was assumed to already know what a commit is. | Say plainly it is the exact version LGE tested against. | 4 |
| Extracting reads and sequences, "The output line width defaults to 70" and later "`--line-width` defaults to 60" | One reader assumed one of two different line-width defaults for what reads like the same setting was a mistake. | Note explicitly that the two commands differ on purpose. | 1 |
| Extracting reads and sequences, "The source and output counts must match" | One reader could not tell whether "counts" means files or reads. | Say explicitly "the same number of files." | 3 |
| Extracting reads and sequences, code block "--source SRR36291587_1.fastq" | One reader found this one example uses a viral accession while every other example in the chapter is human, and could not adapt it to the mitochondrial fixture. | Give a parallel human example alongside the viral one. | 4 |
| Assembly, "a run that does complete is correct" | One reader read this twice, since it reads as reassurance phrased like a puzzle. | Say plainly that the output is trustworthy once the run finishes. | 1 |
| Assembly, "a run that does complete is correct, and rerunning is the only workaround" | One reader could not tell how many times to rerun or how to recognize a failed run on screen. | Say what a failed run actually looks like on screen. | 3 |
| Assembly, "`--memory-gb` sets a memory budget where the assembler supports one" | One reader had no typical value to reach for, despite assembly sounding like the most memory-hungry step in the manual. | Give one typical memory value. | 3 |
| FASTQ operations, "rebuilds a virtual subset, trim, or demux bundle" | One reader met "virtual bundle" as a concept never introduced in the chapter, alongside the abbreviation "demux." | Say plainly that a virtual bundle stores only a preview until it is rebuilt. | 1 |
| FASTQ operations, "`12s-match`" | One reader met "12S" used five times with no explanation of the 12S gene or why it matters. | Gloss 12S at its first use. | 1 |
| FASTQ operations, "The common shape is a positional input file and a required `-o` or `--output`" | One reader found this the clearest syntax-pattern sentence in the chapter, but it arrives two-thirds of the way through. | Move this pattern statement into the opening section. | 2 |
| FASTQ operations, "There is no `--in` or `--out`.", Alignment and phylogenetics, "There is no `--msa` or `--out`." | One reader found the chapter repeatedly tells the reader what flags do not exist, which does not help them type anything, at least four times across the chapter. | Cut sentences that only describe what does not exist. | 3 |
| FASTQ operations, "the group carries five lanes worth knowing about by name" | One reader noted "lanes" means sequencing lanes elsewhere in the manual but categories here, and stopped at the word. | Say "five groups" instead of reusing "lanes" for a different meaning. | 3 |
| Sequence utilities, "writes BED-format hits with chromosome, start, end, name, score, and strand" | One reader found BED listed only in the front-matter glossary, never glossed in the body text. | Gloss BED at this use in the body text. | 1 |
| Sequence utilities, "`--min-length` to 100 nucleotides" | One reader could not judge whether 100 nucleotides is a permissive or strict threshold for finding real genes. | Say briefly what the threshold lets through. | 1 |
| Sequence utilities, "numeric comparisons as `key>=n` and its four siblings" | One reader was left to guess the other four comparison operators rather than seeing them listed. | List all five comparison operators explicitly. | 1 |
| Sequence utilities, "GC content  : 39.5%" | One reader had no benchmark to judge whether 39.5% GC content is typical for a human sequence. | Say what a typical human GC-content value is. | 3 |
| Sequence utilities, "The pattern is an exact string unless `--regex` or `--iupac` reinterprets it" | One reader found regex unexplained and IUPAC never expanded. | Say IUPAC codes are the ambiguity letters, such as N and R. | 4 |
| Sequence utilities, "Date bounds are written `date>=YYYY-MM-DD`" | One reader found the query-syntax section the densest passage in the appendix, with no worked breakdown of the example that follows it. | Annotate the example query token by token. | 4 |
| Alignment and phylogenetics, "`--symbols` is `strict` or `any`" | One reader found this the only flag in the chapter with no default and no explanation at all. | Say what each value does and which one is the default. | 1 |
| Alignment and phylogenetics, "`--keep-identical` stops it collapsing identical sequences" | One reader could not tell whether collapsing identical sequences is good or bad for their result. | Say briefly why someone might want the duplicates kept. | 1 |
| Alignment and phylogenetics, "`--safe` enables IQ-TREE's safe numerical mode" | One reader found "safe numerical mode" unexplained and could not tell why it would ever be turned off. | Say when safe mode is needed. | 3 |
| Tool packs, "`lungfish-cli conda install --pack read-mapping variant-calling` installs two packs" | One reader still did not know what a pack contains, since `conda packs` is only mentioned two paragraphs later. | Point to `conda packs` at the first mention of a pack. | 2 |
| Tool packs, "`conda run [--env <name>] <tool> [args...]`" | One reader found `args...` a placeholder style used nowhere else in the appendix, with the three dots unexplained. | Use one consistent placeholder style throughout the chapter. | 3 |
| Tool packs, "not guaranteed to reproduce an earlier environment byte for byte" | One reader met "byte for byte" as an idiom, the third paragraph in a row saying reproduction is not supported. | Say once, plainly, that exact reproduction is not available. | 3 |
| Tool packs, "still exits 0, because the tools did install and were only not written down" | One reader read "were only not written down" three times before parsing it. | Say plainly that the record failed but the install succeeded. | 3 |
| Tool packs, "`conda setup` downloads micromamba into the managed conda root" | One reader found this one-time first step buried second-to-last in a seven-item paragraph. | Move the one-time setup step to the front of the section. | 4 |
| Provenance and metadata, "you have both halves of what a manuscript needs" | One reader found "both halves" a metaphor referring to a "methods export" not yet introduced at that point. | Name the two things plainly instead of using the metaphor. | 3 |
| Provenance and metadata, "carry runnable command material when the recorded provenance holds enough argument detail" | One reader could not tell whether their own export would be runnable from this 24-word conditional sentence. | Say plainly that the export may be incomplete, and how to tell. | 3 |
| ONT genotyping, "embedded in the project's `.lungfishmhcref` reference bundles" | One reader met MHC unexplained, and this bundle extension missing from the earlier extension list. | Gloss MHC, and add this format to the earlier extension list. | 1 |
| ONT genotyping, "carries its own provider, model, Azure-endpoint, chunking, and review-scope options" | One reader found this the only place in the chapter where a command's flags are named but never documented. | List the flags, or say plainly where they are documented. | 1 |
| ONT genotyping, "it writes a pivot-only workbook only when the bundle has no workbook of its own" | One reader read this conditional behavior twice before parsing it, since it is stated once inside a long sentence. | Split into two shorter sentences. | 1 |
| ONT genotyping, "The eleven subcommands are `list`, `validate`, `import`, `save`, `export`, `duplicate`, `delete`, and four bundle-management subcommands" | One reader had to add up the list before trusting the stated count of eleven, since seven are named and four are only counted. | Name all eleven subcommands explicitly in one list. | 3 |
| ONT genotyping, "being a scientific write workflow that appends a versioned AI haplotype analysis revision with its own provenance and review metadata" | One reader met twenty-two words of stacked nouns with no concrete action, and could not tell what the command actually produces. | Say plainly what file the command writes. | 3 |
| Diagnostics, "whether Apple Containerization is available, which needs macOS 26 or later" | One reader did not know what containerization is or whether they need it. | Say briefly, in one clause, what it enables. | 1 |
| Diagnostics, "probes the common bioinformatics tools on `PATH`" | One reader noted PATH was described once at the start of the chapter and never mentioned again since. | Repeat the one-line PATH gloss here. | 1 |
| Diagnostics, "This is the default `debug` subcommand." | One reader had to look back to see what this sentence referred to, since it sits at the end of the entry after the command was shown twice. | Move the sentence to the start of the entry. | 3 |
| Diagnostics, "which must have arm64 Linux support" | One reader met arm64 unexplained and did not know whether their Mac has it. | Say plainly that Apple Silicon Macs need arm64 images. | 3 |
| Diagnostics, "which needs macOS 26 or later" | One reader did not know their own macOS version and found no instructions for checking it. | Say where to check the installed macOS version. | 4 |
| What the command line cannot do, "the flat index above is the map of that overlap" | One reader read this clause twice, since it stacks two metaphors, "flat index" and "map of that overlap." | Say plainly that the index lists what both the window and the command line can do. | 3 |
| What the command line cannot do, "though `extract contigs --bundle` reaches the same result from the command line by a different path" | One reader wanted to know whether four listed gaps is the complete list, since the wording hedges it as incomplete. | State plainly whether four is the complete list of gaps. | 4 |
| Known defects, "so run from an ordinary folder instead" | One reader noted this is the only place in the whole appendix that says a command's working folder matters, though nothing earlier established that commands have a current folder at all. | Say early in the chapter that commands run in whatever folder the reader is currently in. | 4 |
| Next, "the canonical mpileup flags, indelqual handling" | One reader met three unexplained terms in the chapter's closing pointer, making the next chapter sound unreadable in advance. | Describe the linked pages in plain words instead of jargon. | 1 |
| Command index, "The program exposes 44 top-level commands." | One reader found "exposes" an odd word choice, familiar only from "exposed to a pathogen." | Say "has 44 top-level commands" instead of "exposes." | 3 |
| Global flags, "because they sit on the program itself rather than on any one operation" | One reader could not picture the structure implied by the metaphor "sit on." | Say plainly that global flags go before the subcommand. | 3 |
| FASTQ operations, "The common shape is a positional input file" | One reader read "shape" twice, since it is a metaphor for a pattern rather than a concrete description. | Say "most of them take" instead of "the common shape is." | 3 |
| Sequence utilities, "`stats` is the default subcommand, so `lungfish-cli analyze <input>` runs it." | One reader doubted the stated rule because the very next example writes `analyze stats` explicitly instead of using the short form. | Show the short form actually being used in the example. | 3 |
| Command index, "The program exposes 44 top-level commands" | One reader could not judge whether 44 top-level commands represents everything the window can do or only a fraction of it. | Say roughly what share of the window's operations the command line covers. | 4 |

## Consensus

Forty-seven merged rows were hit by all four readers, and thirty-two more were hit by three readers, for seventy-nine consensus rows in total.

- All four readers never found where to type a command, since the chapter never names the Terminal app or shows a first worked command.
- All four readers hit angle brackets, pipes, brackets, and braces with no notation key explaining what any of them mean.
- All four readers found "switch" introduced as a buried new term inside the definition of "flag."
- All four readers met "shell" used to explain PATH while shell itself is never explained.
- All four readers were offered adding the program to PATH with no command ever shown for how to do it.
- All four readers met SwiftPM, product, and checkout stacked with no gloss in the build-from-source aside.
- All four readers could not tell what "substitute whichever path you are using" means to actually do.
- All four readers noticed the 44 top-level commands and 44 fastq subcommands look like a copy error.
- All four readers met unglossed `.lungfish*` bundle extensions starting at the very first command-index row.
- Three readers found conda used repeatedly as a command name and heading with no gloss anywhere.
- All four readers could not judge which verbosity level, `-v` through `-vvv`, to pick.
- All four readers found thread unexplained with no sane number to choose.
- Three readers were confused by the chapter arguing with an unseen older version of itself.
- All four readers met "wrapped tools" and "bit-identical" unglossed with no sense of whether results are wrong or merely different.
- All four readers counted four subcommands stated and five listed for fetch, in the same sentence.
- All four readers met HTTP 429 with no gloss and no way to judge the retry setting.
- All four readers were told a bare import command fails with no error message or exit status shown.
- All four readers found quality binning unexplained, unable to evaluate the default.
- All four readers met bgzip used as an unglossed verb.
- All four readers hit example fixture files, such as NC_012920.1.fasta and NG_000007.3.gb, with no command shown to obtain them.
- All four readers gave up on the deterministic OCI-layout tarball sentence as unexplained jargon.
- All four readers found mapping quality unexplained, unable to judge the default floor of 0.
- All four readers met five read-group flags unexplained and unexpanded.
- All four readers hit a literal track-id placeholder in a worked example with no command shown to find a real one.
- All four readers read the minimum-allele-frequency flag as self-contradicting about its own default.
- All four readers met ALT_QUAL and the bq filter as unglossed raw tool jargon.
- All four readers could not judge what the sensitive, balanced, and precise classification presets trade against each other.
- All four readers met the bracken-read-length default of 150 with no unit and no guidance.
- All four readers could not tell whether the confidence flag means the same thing for Kraken 2 and TaxTriage.
- All four readers found the Nextflow size string and its dot notation, as in 16.GB, unexplained.
- All four readers hit two contradictory, unacknowledged coordinate conventions for the same kind of position.
- All four readers met exit-status numbers used well before the gloss of what an exit status even is.
- All four readers could not judge how often MEGAHIT fails on Apple Silicon or whether their own Mac is affected.
- All four readers found the Phred-score, two-averages sentence in FASTQ operations the subtlest and least explained point in the chapter.
- All four readers met Deacon unexplained with no command shown to find a database identifier.
- All four readers hit thirty to forty FASTQ subcommand names run together with no way to scan for the one they wanted.
- All four readers read the Nx definition multiple times and still could not apply it to the single-sequence worked example.
- All four readers met reading-frame notation used before the section that explains what a frame is.
- All four readers were given a result to check with no runnable command shown to reproduce it.
- All four readers met six MAFFT strategy names with no guidance on when to change from auto.
- All four readers found MFP never expanded despite it being central to tree building.
- All four readers met bootstrap and aLRT unglossed with no typical replicate count given.
- All four readers met three unexplained pipeline ecosystem names, Nextflow, Snakefile, and nf-core, in one clause.
- All four readers could not picture "artifact" and "focused provenance" or work out what to pass to expected-output.
- All four readers called the repeat-from sentence the densest in the chapter, unable to say what the flag does after several readings.
- All four readers gave up on the dense, negative conda-lock explanation, unsure what the file is even good for.
- All four readers found four exit-status meanings packed into one dense, unordered sentence.
- All four readers found a command that is supposed to fail on an ordinary install confusing rather than reassuring.
- All four readers met working directory unexplained in the known defect about the private tmp symlink.
- Three readers almost skipped the Finding the Binary section because "binary" in its heading is never explained.
- Three readers found the installed path's space in "Lungfish Preview.app" unflagged as needing quotes.
- Three readers found the nested sra subcommands never signalled in the top-level subcommand list.
- Three readers met nine import-kind values run together in one sentence with none explained.
- Three readers did not know what the trailing backslash in multi-line commands does.
- Three readers could not match a mapping preset to their own data without already knowing their sequencing platform.
- Three readers found secondary and supplementary alignments defined only in contrast to each other.
- Three readers found the destructive in-place BAM rewrite warned about only in a buried mid-paragraph clause.
- Three readers found the medaka-model flag required with no source ever given for a real model id.
- Three readers could not tell whether their own data counts as amplicon data for the strand-bias default.
- Three readers were surprised that variant-calling commands print a plan instead of running unless given --execute.
- Three readers met Bracken unglossed in the same sentence that flags a real command-versus-dialog mismatch.
- Three readers had to parse a double negative to learn that assembly is off by default.
- Three readers could not judge what confirmation count out of twenty reads would count as reassuring.
- Three readers found two different exit statuses, 3 and 64, given for the same kind of mistake with no explanation.
- Three readers met "byte-identical" unexplained despite it being the chapter's one assurance that command and window match.
- Three readers had already learned bundle as a path elsewhere and had to backtrack when it is used as a switch.
- Three readers had to hunt letter by letter for the one-character difference between two extra-argument flags.
- Three readers found "deterministic two-pass selection" restated in plain words later in the same sentence, making the jargon redundant.
- Three readers met word length and dust masking unexplained with no way to judge the default of 12.
- Three readers had no output shown to check the 318-residue protein claim against.
- Three readers met a small query grammar with no example until after its explanation and no list of valid values.
- Three readers were told what the workflow list command does not do without being told what does list a project's workflows.
- Three readers found the pack switch changes the same two words' meaning depending on a flag, calling it a trap.
- Three readers found the "defect rather than a design choice" aside honest but unhelpful, since it names no fix.
- Three readers met PHA4GE used without ever being expanded.
- Three readers met ONT used repeatedly, including in a section heading, without ever being expanded.
- Three readers could not tell which percent-basis denominator to choose, since neither value says what it counts.
- Three readers found the campaign's internal editorial language meaningless as a reader, with one suspecting an unfinished draft.
- Three readers found six known defects packed into one dense, unscannable paragraph.
