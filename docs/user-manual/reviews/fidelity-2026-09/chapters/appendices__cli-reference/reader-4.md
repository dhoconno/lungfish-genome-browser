# Reader report: CLI Reference

Persona: a student who used Geneious in one class for a semester. I am
comfortable with a graphical sequence viewer and I expect every operation to
have a dialog with fields I fill in. I have never opened a terminal. My TA
told me to look one command up in this appendix, so I did not read it cover
to cover. I jumped to the section I needed and then had to back up.

| Location | What stopped me | Suggested fix |
| --- | --- | --- |
| What it is, "ships a command-line program alongside its window" | I do not know what a command-line program is, or where I would put one. The appendix opens as if I already have a place to type. | Add one sentence naming the Terminal app and saying that is where these lines go. |
| What it is, "The program is called `lungfish-cli`" | I could not tell whether this is a file I download, something already inside the app, or something I install. | Say in the same sentence that it is already inside the app you installed. |
| What it is, "it runs the same operations the window runs, with no window at all" | This is the sentence that would decide whether I need this appendix, and I read it twice because "no window at all" sounded like a limitation rather than the point. | Say that anything you can click you can also type. |
| What it is, "as `convert` does in `lungfish-cli convert`" | The definitions of subcommand, positional argument, and flag are good but they never show one whole line with all three parts labelled. | Add one annotated example line showing program, subcommand, positional, and flag. |
| What it is, "a flag with no value after it is a switch" | I had to read this twice. "Switch" is a fourth new word introduced inside the definition of the third one. | Call it a flag that takes no value. |
| Finding the binary, heading "Finding the binary" | "Binary" is in the heading and is never explained. I did not know it meant the program file. | Title the section Finding the program. |
| Finding the binary, "Installed releases do not put it on your `PATH`" | The gloss of PATH arrives after the word, and "your shell" is a new term inside the gloss. I do not know what my shell is. | Gloss shell in the same sentence, or drop it. |
| Finding the binary, "add its folder to your `PATH` once" | This is offered as an option but there is no instruction anywhere for how to do it, so it is the one route I cannot take. | Either give the command or say it is outside this manual's scope. |
| Finding the binary, "/Applications/Lungfish Preview.app/Contents/MacOS/lungfish-cli" | There is a space in "Lungfish Preview.app". Nothing tells me a space needs quoting, and my first attempt would have failed. | Show the path quoted and say why. |
| Finding the binary, "the SwiftPM product lands at `.build/debug/lungfish-cli`" | SwiftPM, product, and checkout are three terms I do not know, in one sentence I cannot use anyway. | Mark the sentence as for people building from source. |
| Finding the binary, "substitute whichever path you are using" | I did not know I was allowed to make an alias or shorthand, so I pictured retyping that whole long path on every one of the fifty examples below. | Say the long path can be copied once and pasted. |
| Command index, "The program exposes 44 top-level commands" | I could not judge 44. Is that everything the window does, or a fraction? | Say roughly what share of the window's operations this covers. |
| Command index, "Run `lungfish-cli <command> --help`" | The angle brackets are never explained. I did not know whether to type them. | Say once that angle brackets mark something you replace and are not typed. |
| Command index, "The help text is the authority whenever this appendix and a running copy disagree" | I do not know how to get "a running copy" of help text on screen, or what to do when they disagree. | Show one `--help` invocation with its first lines of output. |
| Command index, table row "`align`" | Every row names a file type I have not met, such as `.lungfishmsa`, `.lungfishref`, `.lungfishfastq`, `.lungfishprimers`, `.lungfishgenotype`, `.lungfishtree`, and `.lungfishrun`. None is glossed here. | Add one line before the table saying every `.lungfish*` name is a folder LGE makes, with a pointer to File Formats. |
| Command index, table row "`conda`" | Conda appears as a command name, a section heading, and a tool pack manager, and it is never explained. I assumed at first it was a typo. | Gloss conda at its first appearance as the package installer LGE uses. |
| Command index, table row "`fastq` ... with 44 subcommands" | The same number 44 is used for the top-level command count and for the `fastq` subcommand count. I stopped and reread both, sure one was a copy error. | Confirm or vary the number so the coincidence does not read as an error. |
| Command index, table row "`run-headless`" | "Headless" and "CI-friendly" are both unexplained. CI is never expanded anywhere in the appendix. | Expand CI once at first use. |
| Global flags, "`--format <text\|json\|tsv>`" | The pipe character inside the angle brackets is never explained. I did not know it meant pick one. | Say once that a pipe between values means choose one of them. |
| Global flags, "Repeat it as `-v`, `-vv`, or `-vvv`" | I could not judge which level to pick or what more I would see at each. | Say what each level adds in one clause. |
| Global flags, "`--threads <n>`, `-t` ... The default is automatic" | I do not know what a thread is, and "automatic" does not tell me what number I would get. | Gloss threads as how many parts of the work run at once. |
| Global flags, "`--project` is not global, whatever older copies of this appendix said" | This sentence argues with a document I have never seen, and it took a reread to work out it was not telling me to do anything. | Drop the reference to older copies. |
| Global flags, "records one thread rather than four" | I could not tell whether this means my run is slow, or only that the record of it is wrong. | Say whether the run itself is affected. |
| Global flags, "Several of the wrapped tools are not bit-identical across thread counts" | "Wrapped tools" and "bit-identical" are both new. I could not tell whether this means my answers change. | Say the results can differ slightly. |
| Downloading records, "Its four subcommands are ... plus `ena`" | Four subcommands are named and then a fifth is added in the same sentence, so I counted five and stopped. | Say five subcommands. |
| Downloading records, "`--no-retry` stops LGE from retrying an HTTP 429 rate-limit response" | HTTP 429 means nothing to me and I could not decide whether I want retrying on or off. | Say it means NCBI asked you to slow down. |
| Downloading records, "`--api-key <key>`" | This flag appears on four commands and is never explained. I do not know whether I have one or need one. | Say once that the key is optional and raises your NCBI rate limit. |
| Downloading records, "It tries ENA first and falls back to the NCBI SRA Toolkit" | ENA is expanded once here, but the SRA Toolkit is a separate program I do not know I have. | Say whether LGE installs the toolkit for you. |
| Downloading records, "resolved through NCBI's assembly database ... comes back as the exact record you asked for" | The contrast implies the `GCF_` route may not give the exact record, but the text never says that outright. I read it three times. | State plainly which route can substitute a different accession. |
| Importing into a project, "A bare `lungfish-cli import <path>` fails" | It says it fails but not what I would see, so I could not tell a wrong command from a broken install. | Give the message or exit status. |
| Importing into a project, "`--project <path>`" | Every import example needs a project path but no example ever shows one. I do not know what a project path looks like on disk. | Show one real project path in an example. |
| Importing into a project, "`--samplesheet <csv>` holding `sample`, `r1`, and `r2` columns" | In Geneious I never made a samplesheet. I could not build this file from the text because I do not know if there is a header row. | Show a three-line samplesheet. |
| Importing into a project, "`--recipe <vsp2\|wgs\|hifi\|none>`" | These four values are bare abbreviations with no explanation. I could not choose one. | Gloss each in three words. |
| Importing into a project, "`--quality-binning <illumina4\|eightLevel\|none>` defaults to `illumina4`" | I do not know what quality binning is or why I would change it, and one value is camel case while the others are not, which made me doubt my typing. | Say what binning does in one clause. |
| Importing into a project, "`--compression <fast\|balanced\|maximum>`" | I could not judge the trade-off. Does maximum cost me time or quality? | Say what maximum costs. |
| Importing into a project, "`--clumping-tool <auto\|bbtools\|trim-galore\|none>`" | Clumping is never defined anywhere in the appendix. | Gloss clumping in one clause. |
| Importing into a project, "`import cz-id` additionally requires `--project` and `--sample-name`" | This tells me two of the six classifier importers behave differently but not which command I would run for the other five. | Show one classifier import example. |
| Reference bundles, "a `.lungfishref` folder holding a sequence, its indices" | "Indices" is used without explanation. In Geneious I never saw an index. | Gloss index as a lookup file that makes reading fast. |
| Reference bundles, code block "lungfish-cli bundle create \\" | The backslash at the end of each line is never explained. I did not know whether it was part of the command. | Say once that a trailing backslash continues one command onto the next line. |
| Reference bundles, "This example builds the human mitochondrial reference from the manual's fixture" | The example names `NC_012920.1.fasta` as if that file is already sitting next to me. Nothing says where to get it. | Point to the practice data or to the `fetch` command that makes it. |
| Reference bundles, "`--compress` bgzips the FASTA" | Bgzip is used as a verb and never explained. | Say it compresses the file in a form the tools can still index. |
| Reference bundles, "answers `Valid` on a healthy one" | This is the one place output is described but the exact text is not shown in a block like the others. | Show the output block. |
| Reference bundles, "a deterministic OCI-layout tarball" | Deterministic, OCI-layout, and tarball are three unknown terms in five words, and I could not tell what this is for. | Say what the file is used for before naming its format. |
| Mapping and alignment tracks, "`--preset <preset>`" | The preset values are listed but I cannot tell which one goes with the data I have unless I already know my platform. | Pair each preset with the instrument name in the list. |
| Mapping and alignment tracks, "`--min-mapq` sets a mapping-quality floor that defaults to 0" | I do not know what mapping quality measures or what a typical good value is, so 0 tells me nothing. | Say what the number means before saying the default. |
| Mapping and alignment tracks, "Five read-group flags are `--rg-id`, `--rg-sm`, `--rg-lb`, `--rg-pu` ... and `--rg-pl`" | Read groups are never explained and the five abbreviations are never expanded. I could not tell whether I must set them. | Say read groups are optional labels and expand the five once. |
| Mapping and alignment tracks, "`--secondary` keeps secondary alignments, `--no-supplementary` drops supplementary ones" | Secondary and supplementary are distinguished from each other without either being defined, so the contrast was empty. | Gloss both in one sentence. |
| Mapping and alignment tracks, "`--alignment-track <id>`" | Track ids appear in six commands and the one code example writes `<track-id>` as a literal placeholder. I never learn how to find a real one. | Say which command prints the track ids. |
| Mapping and alignment tracks, "the file is rewritten in place" | This is the only destructive operation in the appendix and the warning is one clause in the middle of a paragraph. | Give it its own sentence and say to keep a copy. |
| Calling variants, "`--min-af <float>` ... the iVar pipeline applies its own 0.05 threshold" | I do not know what allele frequency is, what "float" means as a value type, or how to judge 0.05. | Gloss allele frequency and say what 0.05 means as a share of reads. |
| Calling variants, "`--ivar-bad-quality-threshold` is the ALT_QUAL below which a call fails the bq filter" | ALT_QUAL and "bq filter" are both raw internal names I cannot look up from here. | Say it is the quality of the alternate base. |
| Calling variants, "since it is off by default for amplicon data" | I could not tell whether my data is amplicon data or whether this default applies to me. | Say it applies when the reads came from PCR primers. |
| Calling variants, code block "--alignment-track <track-id>" | The example cannot be typed as printed. It has a placeholder in the middle of an otherwise runnable command. | Show a real-looking id and say where it came from. |
| Calling variants, "builds a GATK HaplotypeCaller plus WhatsHap phasing plan, printing it unless `--execute` is given" | I had to read this twice to see that the command does nothing by default. That is surprising behaviour buried in a subclause. | Lead with the fact that it only prints unless you ask it to run. |
| Classification, "`--preset <sensitive\|balanced\|precise>` defaults to `balanced`" | Sensitive and precise are opposites here but I do not know what I gain or lose by moving either way. | Say what sensitive finds more of and what precise avoids. |
| Classification, "`--profile` runs Bracken afterwards, which matters because the dialog always runs Bracken and this command does not" | Bracken is never explained and this is exactly the dialog-versus-command mismatch I need flagged, but it is a subclause. | Gloss Bracken and give the mismatch its own sentence. |
| Classification, "`--bracken-read-length <n>` at 150" | The number 150 has no unit and no guidance. I do not know if it must match my reads. | Say it is in bases and should match your read length. |
| Classification, "`--bracken-level <D\|P\|C\|O\|F\|G\|S>`" | Seven single letters with no key. I guessed at kingdom and species and was not sure. | Expand the seven letters once. |
| Classification, "`--rank <D\|P\|C\|O\|F\|G\|S>` to `S`" | The same seven letters appear again under TaxTriage, still with no key. | Expand once and cross-reference the second use. |
| Classification, "`--confidence` defaults to 0.2" | Two different commands set a confidence number, 0.2 here and none stated for Kraken, and I cannot judge either. | Say what the number ranges over and which direction is stricter. |
| Classification, "`--max-memory` takes a Nextflow size string and defaults to `16.GB`" | "Nextflow size string" and the dot inside `16.GB` both stopped me. I would have typed `16GB` and failed. | Say the dot is required and show one more example. |
| Classification, "`--skip-assembly` is on by default and `--no-skip-assembly` turns assembly back on" | A double negative describing a default. I read it three times. | Say assembly is off by default. |
| Classification, "`--nf-profile` defaults to `docker`" | Docker is named here and again under workflows and is never explained or listed as something I must install. | Say once whether Docker must be installed separately. |
| Classification, "`--revision` defaults to the pinned TaxTriage commit" | "Pinned commit" assumes I know what a commit is. | Say it is the exact version LGE tested. |
| Classification, "reports how many come back independently confirmed" | I could not tell what number of confirmations out of twenty would count as good. | Give one interpretation sentence. |
| Extracting reads and sequences, "`name:start-end` with 1-based inclusive coordinates" | 1-based inclusive is contrasted three sections later with 0-based exclusive under `annotate-orfs`, and neither is explained. In Geneious I never chose. | Gloss both conventions once and say which commands use which. |
| Extracting reads and sequences, "an exit status of 3" | Exit statuses of 3, 64, 10, 2, 1, and 0 appear across the appendix. The gloss of exit status arrives near the end under `tools update`, long after the first use here. | Move the exit-status gloss to its first use. |
| Extracting reads and sequences, "so the two produce byte-identical output for the same selection" | This is the sentence that tells me the command matches the dialog, and it is the only such assurance in the appendix. I nearly missed it. | Say plainly that this command and the window give the same reads. |
| Extracting reads and sequences, code block "--source SRR36291587_1.fastq" | The example uses a viral taxon and a viral accession where the rest of the examples are human. I could not reuse it with the mitochondrial fixture. | Give a human example alongside. |
| Extracting reads and sequences, "`--bundle` here is a switch rather than a path" | This correction is needed because `--bundle` is a path in eight other commands in this same appendix. I had already learned the wrong habit. | Repeat the warning wherever `--bundle` is a switch. |
| Assembly, "MEGAHIT 1.2.9 fails most runs on Apple Silicon in this release" | I do not know whether my Mac is Apple Silicon, and I do not know whether the dialog has this same problem. | Say how to check, and whether the window is affected too. |
| Assembly, "Two spellings pass extra arguments, and they differ by one letter" | This is a trap that depends on noticing a final `s`, and the sentence draws attention to it without showing both spellings side by side in an example. | Show both in one example line. |
| FASTQ operations, "which is more than any other group and more than this appendix walks through" | For a lookup appendix, being told my command may not be listed is the worst possible outcome, and the sentence does not tell me how to find it. | Point directly at `fastq --help` in the same sentence. |
| FASTQ operations, "the `--count` path draws its reads with a deterministic two-pass selection" | I asked whether my results are repeatable and got an implementation detail. | Say the same input gives the same reads, and stop. |
| FASTQ operations, "a different average from the probability-averaged Mean Q the window's FASTQ viewport shows, so the two numbers disagree" | This is the single most useful warning for me, since I would otherwise think one was a bug, and it is buried mid-paragraph. | Give it its own sentence with the word warning. |
| FASTQ operations, "`--database-id <id>` ... choosing the database by identifier rather than by path" | I have no way to find an identifier from this text. | Name the command that lists the identifiers. |
| FASTQ operations, "Both default the word length to 12 and the masking to `dust`" | Word length and dust masking are both unexplained, and 12 is unjudgeable. | Say to leave both alone unless told otherwise. |
| FASTQ operations, "the group carries five lanes worth knowing about by name" | The paragraph then lists roughly thirty-five bare command names with no descriptions. I could not tell what any of them do. | Split into a table with a phrase per command. |
| Sequence utilities, "N50, N90 ... Nx is the length at which records sorted longest first cover at least x percent" | I read this definition three times and still could not apply it to the sample output, where N50 and N90 are both the file length. | Add a clause saying a single-sequence file makes them equal. |
| Sequence utilities, "`lungfish-cli analyze stats NG_000007.3.gb`" | The example output uses a file the chapter never introduces, and the front matter names `hbb-gene` and `human-mito` as its fixtures without linking those to filenames. | Name the file in one clause. |
| Sequence utilities, "GC and AT skew" | Skew is never defined, and I could not tell whether a skew value is good or bad. | Gloss skew in one clause. |
| Sequence utilities, "Codons are counted in frame +1 independently for each record" | I know what a codon is but not what "frame +1" means as a setting, and frames are only explained one command later under `translate`. | Move the frame explanation before its first use. |
| Sequence utilities, "gives a 318-residue protein for MT-ND1" | I could not check this without running the command, and I could not tell whether a different number means I did something wrong. | Say the number is what you should see. |
| Sequence utilities, "finds six matches" | Same problem. A number I cannot judge and cannot verify from the text. | Show the first line of the BED output. |
| Sequence utilities, "The pattern is an exact string unless `--regex` or `--iupac` reinterprets it" | Regex is never explained and IUPAC is never expanded. | Say IUPAC codes are the ambiguity letters like N and R. |
| Sequence utilities, "Date bounds are written `date>=YYYY-MM-DD`" | The query syntax section is the densest passage in the appendix and has no worked breakdown of the one example that follows it. | Annotate the example query token by token. |
| Alignment and phylogenetics, "`--strategy` accepts `auto`, `linsi`, `ginsi`, `einsi`, `fftns2`, and `parttree`" | Six MAFFT strategy names with no guidance at all. In the Geneious dialog I picked from a menu with descriptions. | Say auto is right unless you have a reason. |
| Alignment and phylogenetics, "`--model` defaults to `MFP`" | MFP is never expanded and I could not tell whether it is a real model or a mode. | Expand MFP once. |
| Alignment and phylogenetics, "`--bootstrap` and `--alrt` set replicate counts" | Bootstrap and aLRT are the two numbers a student is most likely to be asked for, and neither is explained nor given a typical value. | Give the usual replicate count for each. |
| Alignment and phylogenetics, "`--seed` to 1" | Seed is not explained here even though the FASTQ section made a point of there being no seed flag. | Gloss seed as what makes a random step repeatable. |
| Alignment and phylogenetics, "1-based aligned column ranges such as `10-40,55`" | I could not tell whether `55` means a single column or an open range. | Say a bare number is one column. |
| Workflows, "a Nextflow file, a Snakefile, or the one supported nf-core pipeline" | Nextflow, Snakefile, and nf-core are three ecosystems named in one clause, none explained, and none of them is something I have. | Say these are pipeline description files written by other groups. |
| Workflows, "so LGE knows which artifact must receive focused provenance" | "Focused provenance" and "artifact" are both jargon, and I could not work out what to pass to `--expected-output`. | Say to name the folder the results land in. |
| Workflows, "`--repeat-from <previous.lungfishrun>` re-validates the retained configuration and source identity of a supported bound local run" | This sentence has six modifiers and no verb I could hold onto. I read it four times and still cannot say what the flag does. | Rewrite as one short sentence saying it reruns an earlier run. |
| Workflows, "an unbound or unsupported history cannot slip past validation" | I do not know what bound and unbound mean here, and the sentence describes a safety property rather than telling me what to do. | Say which runs can be repeated. |
| Workflows, "prints a two-line usage hint rather than an inventory of your project's workflows" | This warns me that `workflow list` does not do what its name says, which is useful, but it does not say what does list my workflows. | Name the command that does, or say there is none. |
| Tool packs, "`--pack` is a switch that changes how the positional arguments are read" | This is the most confusing sentence in the appendix. The same two words after the command mean two different things depending on a flag. I read the example three times. | Show both commands as two labelled lines. |
| Tool packs, "Use the Plugin Manager instead, at **Tools > Plugin Manager...**" | This is the only menu path in the whole appendix, and it is the moment I learn a command has a dialog equivalent. Everywhere else I could not tell. | State once near the top whether each command has a dialog. |
| Tool packs, "`conda setup` downloads micromamba into the managed conda root" | Micromamba and "managed conda root" both appear as if known. This is described as the one-time first step but it is the second-to-last item in a seven-item paragraph. | Move the one-time setup step to the front of the section. |
| Tool packs, "It is unresolved, meaning it is not a complete installed-artifact inventory and does not claim compatibility with conda-lock" | The gloss after "meaning" is harder than the word it glosses. | Say the file records what you asked for, not what got installed. |
| Tool packs, "10 means work is pending" | Exit status 10 is unusual and I would have no idea where to see it. The appendix never says how to view an exit status. | Say once where the number appears. |
| Provenance and metadata, "on an ordinary installation this command reports a missing signature artifact and exits with a failing status. That is the expected result" | A command that is expected to fail is confusing, and I had to read the two sentences together twice to accept it. | Merge into one sentence saying a failure here is normal when nothing is signed. |
| Provenance and metadata, "PHA4GE-aligned sample metadata" | PHA4GE is never expanded. | Expand it once. |
| Diagnostics, "which needs macOS 26 or later" | I do not know my macOS version and the appendix does not say how to check. | Say where to look. |
| What the command line cannot do, "this campaign's chapter reviews recorded four of them" | This sentence refers to the process that made the manual, which means nothing to me as a reader. | Drop the reference to the campaign. |
| What the command line cannot do, "though `extract contigs --bundle` reaches the same result from the command line by a different path" | This is the section I most needed, since I want to know when I must go back to the window, but only four gaps are listed and the list is hedged as incomplete. | Say whether four is the complete list. |
| Known defects, "Running a command whose working directory sits under `/private/tmp`" | Working directory is never explained, and I do not know whether I am in that folder or how to leave it. | Say most people are not affected and how to tell. |
| Known defects, "so run from an ordinary folder instead" | This is the only place the appendix tells me my location matters when I type a command. Nothing earlier said I had a location. | Say early on that commands run in whatever folder you are in. |

The one thing I learned: a command and the dialog write the same provenance
records, so a typed run is worth the same as a clicked one as evidence.

The one thing I still could not do: run a single command, because nothing
tells me where to type it or how to open the place I would type it.

The sentence I liked most: "The help text is the authority whenever this
appendix and a running copy disagree."
