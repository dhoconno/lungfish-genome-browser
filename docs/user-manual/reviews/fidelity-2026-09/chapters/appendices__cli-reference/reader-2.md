# Reader 2 report: CLI Reference

Reader: senior undergraduate, two years of wet-lab work, strong on library prep
and PCR, no data analysis, has never opened a terminal. Reading this the way I
was told to, which is to look up one command rather than read it through.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "ships a command-line program alongside its window" | I do not know what a command-line program is or where it lives. "Alongside its window" makes it sound like a second window I could find in the app. I looked for it in the app and there is nothing. | Say in one sentence that this program is typed into the macOS Terminal application, not clicked. |
| What it is, "The program is called `lungfish-cli`" | Nothing tells me what program I type it into. I do not know that Terminal exists as an app on my Mac. | Name the Terminal app and where to find it. |
| What it is, "with two leading hyphens" | The glossary link is offsite and I am mid-sentence. I could not tell from the text whether the two hyphens are two separate keys or one long dash character I have to find. | Say the two hyphens are two presses of the ordinary hyphen key. |
| What it is, "a flag with no value after it is a switch" | Two new words in one clause, flag and switch, and I had to read it three times to see that a switch is a kind of flag. | Split into two sentences so switch gets its own. |
| What it is, "the small JSON records" | JSON is used as if I know it. I have never seen one. | Gloss JSON as a plain-text file format at first use. |
| Finding the binary, heading word "binary" | The heading uses a word the section never defines, and I do not know that binary means the program file. I almost skipped the section because I thought it was about binary data. | Retitle to "Finding the program" or gloss binary in the first sentence. |
| Finding the binary, "do not put it on your `PATH`" | The gloss says PATH is a list of folders your shell searches. I do not know what a shell is either, so the gloss uses an undefined word to explain an undefined word. | Gloss shell before PATH, or drop shell from the gloss. |
| Finding the binary, "add its folder to your `PATH` once" | This is offered as one of two options but there is no instruction anywhere for how to do it. I cannot perform it from the text. | Either show the one line that does it or say the whole path is the supported route. |
| Finding the binary, "/Applications/Lungfish Preview.app/Contents/MacOS/lungfish-cli" | There is a space in "Lungfish Preview.app" and nothing tells me a space matters when typing a path. My first attempt would have failed for a reason I could never diagnose. | Show the quoted or escaped form of the path exactly as it must be typed. |
| Finding the binary, "If you build from source instead" | I do not know what building from source means and cannot tell whether this sentence applies to me. | Add three words saying this is for people compiling the app themselves. |
| Finding the binary, "so substitute whichever path you are using" | I do not know what substitute means operationally here. Do I paste the long path in place of the two words `lungfish-cli` in every example. | Show one worked example with the full path so the substitution is visible once. |
| Finding the binary, the two code blocks | The first block is what I type and the second is what comes back, but nothing labels which is which. I assumed for a moment I had to type both. | Label the output block "Output" or add a comment line. |
| Command index, "The program exposes 44 top-level commands" | I do not know what top-level means, and the `fastq` row later says it has 44 subcommands too. The identical number made me think I had misread one of them. | Gloss top-level once and consider noting the coincidence is a coincidence. |
| Command index, "Run `lungfish-cli <command> --help`" | The angle brackets are never explained. I did not know whether I type the brackets. This convention is used in almost every command line in the chapter, so it blocks everything. | State once that angle brackets mark a value you replace and are not typed. |
| Command index, "`align` Align unaligned FASTA sequences with MAFFT into a native `.lungfishmsa` bundle" | MAFFT, bundle, and the `.lungfishmsa` extension all arrive with no gloss, in the very first row of the table I came here to scan. | Add a lead-in sentence saying tool names and bundle formats are defined in the File Formats and Tool Versions appendices. |
| Command index, "`conda` Manage tool packs" | conda, tool pack, and metagenomics database are three unglossed terms in one row. I have no idea what a tool pack is even after the later section. | Gloss tool pack once where it is first used. |
| Command index, "`gatk` Build or run GATK4 germline-variant commands (10 subcommands)" | The row names a count but not the subcommands, so scanning the index does not answer whether the thing I want exists. Other rows do list them. | Be consistent, either list subcommands in every row or none. |
| Global flags, "`--format <text\|json\|tsv>`" | The vertical bar is never explained. I read it as a character I type. | Say once that the bar means choose one. |
| Global flags, "Repeat it as `-v`, `-vv`, or `-vvv`" | I cannot judge which one to use. Nothing says what more output gets me or when I would want it. | Say what each level adds, or say start with `-v`. |
| Global flags, "`--threads <n>`, `-t` Sets the thread count. The default is automatic." | I do not know what a thread is or what number would be reasonable on my laptop. This is a number I cannot judge at all. | Gloss thread as a parallel worker and give a sane starting number. |
| Global flags, "it is consumed by the program before any subcommand sees it" | I read this three times. "Consumed" is doing work I do not follow, and I cannot tell whether the workaround is to move the flag or to give up. | Say plainly that the flag is silently ignored there and there is no workaround. |
| Global flags, "Several of the wrapped tools are not bit-identical across thread counts." | Wrapped and bit-identical are both unexplained, and I cannot tell whether this means my results would be wrong or merely different. | Say the results differ in small ways that are not errors. |
| Downloading records, "Its four subcommands are `ncbi`, `search`, `sra`, and `genome`, plus `ena`" | Four is stated and then five are listed. I stopped and counted twice. | Say five subcommands, or say four plus one. |
| Downloading records, "`--no-retry` stops LGE from retrying an HTTP 429 rate-limit response" | HTTP 429 means nothing to me and I cannot judge whether I want retries on or off. | Say it means the archive asked you to slow down. |
| Downloading records, "`lungfish-cli fetch sra download <accession>`" | The index row said the four subcommands are `ncbi`, `search`, `sra`, `genome`, but here `sra` has its own three subcommands underneath. The nesting was not signalled. | Say `sra` is a group with its own subcommands. |
| Downloading records, "`GCF_` or `GCA_`" and "`MN908947.3`" | I know accessions from ordering primers but not these prefixes, and the sentence is very long. I had to read it twice to see it describes two different behaviours. | Break the sentence at the comma after "assembly database". |
| Importing into a project, "A bare `lungfish-cli import <path>` fails." | It does not say what the failure looks like, so I would not recognise it. Elsewhere the chapter does give the exact error text. | Give the error message as the other sections do. |
| Importing into a project, "`--samplesheet <csv>` holding `sample`, `r1`, and `r2` columns" | I know what a CSV is from Excel but not whether these column names must be lowercase or in that order. This is a file I would have to make. | Say whether the header names are case sensitive and show two example rows. |
| Importing into a project, "`--quality-binning <illumina4|eightLevel|none>` defaults to `illumina4`" | I have no idea what quality binning is or what changing it would do to my data. It is a default I cannot evaluate. | One sentence on what binning discards and why the default is safe. |
| Importing into a project, "`--compression <fast|balanced|maximum>`, the last defaulting to `balanced`" | "The last" points at `--compression` but sits after a list of three flags and a list of three values, so I first read it as the value `maximum`. | Name the flag again instead of saying "the last". |
| Importing into a project, "`<kind>` is one of `clc-workbench`, ... `igv-session-track-set`" | Nine values with no explanation, and I cannot tell which one matches an export I was handed. | Point to a section that says which program produces each. |
| Reference bundles, the `bundle create` syntax line | The line runs to about fifteen bracketed pieces on one line and I lost my place inside it. This is the pattern for every section and it is the single hardest thing to read. | Break long syntax lines across lines the way the worked examples do. |
| Reference bundles, the example with `\` at line ends | The backslash is never explained. I did not know whether to type it or whether it is a line-wrap artifact of the page. | Say once that the backslash continues one command across lines. |
| Reference bundles, "`--compress` bgzips the FASTA" | bgzip is used as a verb with no gloss. | Gloss as a compression format that keeps random access. |
| Reference bundles, "`--fasta NC_012920.1.fasta`" | The example uses a file I do not have and the chapter never says how to get it. The fixture is named in the front matter but not in the text. | Add the one `fetch` command that produces the file. |
| Reference bundles, "a deterministic OCI-layout tarball" | Three unknown terms in four words. I skipped the sentence entirely. | Say what it is for before saying what it is. |
| Mapping and alignment tracks, "writes a coordinate-sorted, indexed BAM" | I know BAM vaguely from lab meeting slides but not coordinate-sorted or indexed. | Gloss both once or link to the alignment chapter. |
| Mapping and alignment tracks, "`--min-mapq` sets a mapping-quality floor that defaults to 0" | I do not know the scale mapping quality runs on, so 0 tells me nothing and I could not pick a better value. | Give the range and one commonly used value. |
| Mapping and alignment tracks, "Five read-group flags are `--rg-id`, `--rg-sm`, `--rg-lb`, `--rg-pu`, which all default to the sample name, and `--rg-pl`" | Read group is not glossed, the five abbreviations are never expanded, and the sentence lists four then one. I read it twice to find the fifth. | Expand each abbreviation in a short table. |
| Calling variants, "`--min-af <float>` ... The flag has no default of its own, and when omitted the iVar pipeline applies its own 0.05 threshold." | float is unglossed, and the two-part default rule took two readings. I still cannot tell what happens with the other four callers. | Say what the other callers do when the flag is omitted. |
| Calling variants, "`--ivar-bad-quality-threshold` is the ALT_QUAL below which a call fails the bq filter, defaulting to 20" | ALT_QUAL and bq filter are both unexplained. I cannot judge 20. | Gloss ALT_QUAL as the base quality of the alternate allele. |
| Calling variants, "`--alignment-track <track-id>`" in the example | The example leaves a placeholder inside a block I am meant to copy, and nothing says how to find a track id. | Say the id comes from `bundle list --tracks`. |
| Calling variants, "`--medaka-model <id>` The ONT basecaller model for Medaka" | ONT and basecaller model are unexplained and this is a required flag, so I cannot run the command at all. | Say where a model id comes from. |
| Classification, "`--db` names an installed database such as `Viral` or `Standard-8`" | I do not know what installed means here or how to find out what I have. The answer is two paragraphs later under `conda db list`. | Point forward to `conda db list` in the same sentence. |
| Classification, "`--preset <sensitive|balanced|precise>` defaults to `balanced`" | Three words that sound like a quality dial, but nothing says what each trades away. | One clause per preset saying what it favours. |
| Classification, "`--confidence <float>` and `--min-hit-groups <n>` override the numbers behind the preset" | The numbers behind the preset are never shown, so I cannot override them meaningfully. | Show the three preset value sets in a small table. |
| Classification, "`--bracken-read-length <n>` at 150" | Bracken is not glossed, and I do not know whether 150 must match my actual read length. My reads are 250. | Say whether this must match the sequencing read length. |
| Classification, "`--confidence` defaults to 0.2" (TaxTriage) | The Kraken 2 section also has `--confidence` with no default given, and this one is 0.2. I could not tell whether they are the same setting. | Note they are separate settings of the two pipelines. |
| Classification, "`--max-memory` takes a Nextflow size string and defaults to `16.GB`" | Nextflow size string is unexplained and `16.GB` has an unusual dot I would not have guessed to type. | Show the format explicitly, for example `32.GB`. |
| Classification, TaxTriage paragraph | This is one paragraph of about fifteen sentences covering three commands. Scanning it for one flag took me longer than any other section. | Break the flag list into a table like the variants section has. |
| Extracting reads and sequences, "1-based inclusive coordinates, matching the samtools faidx convention" | 1-based inclusive is a real concept I half know from genetics, but samtools faidx is not explained, and later the ORF section says 0-based exclusive. The two conventions in one chapter with no warning is a trap. | Add a sentence contrasting the two conventions where the second one appears. |
| Extracting reads and sequences, "an exit status of 3" | Exit status is glossed by link, but nothing says where I would see this number. In a terminal I do not know how to read one. | Say the number is not printed and how to see it, or drop the numbers. |
| Extracting reads and sequences, "exit status of 64" for `extract contigs` versus 3 for `extract reads` | Two different numbers for what looks like the same kind of mistake. I could not work out the rule. | Say the numbers are not meaningful to a person reading output. |
| Extracting reads and sequences, "`--taxon 2697049`" | A bare number with no explanation of what a taxid is or where to find one. | Gloss taxid and say the classifier result lists them. |
| Extracting reads and sequences, "`--bundle` here is a switch rather than a path" | I had to go back to the opening paragraph to recover what switch meant. | Repeat the two-word gloss inline. |
| Assembly, "MEGAHIT 1.2.9 fails most runs on Apple Silicon in this release." | I do not know whether my Mac is Apple Silicon, and this is a paragraph that decides whether a command works for me. | Say how to check, or say all current Macs are affected. |
| Assembly, "`--profile` names a curated profile such as `meta-sensitive` or `nano-hq`" | Two examples and no list, so I cannot tell what the other profiles are or which matches my library. | Say where the full list is printed. |
| FASTQ operations, "The common shape is a positional input file and a required `-o` or `--output`" | This is the clearest sentence about syntax in the chapter and it appears two thirds of the way in. Before this I did not know inputs generally come first. | Move this pattern statement into the opening section. |
| FASTQ operations, "`--proportion <p>` | `--count <n>`" | The brace and bar notation appears here for the first time with no explanation. | Say braces mean pick exactly one. |
| FASTQ operations, "a different average from the probability-averaged Mean Q the window's FASTQ viewport shows, so the two numbers disagree on the same reads and both are correct" | This matters and I had to read it three times. "Probability-averaged" is not explained. | Split into two sentences and say which number to quote in a methods section. |
| FASTQ operations, "`--database-id <id>`" for `scrub-human` | Required flag, no gloss, and no route to find an id. I could not run this. | Say which command lists the ids. |
| FASTQ operations, "the group carries five lanes worth knowing about by name" | Then about forty command names arrive in one paragraph with no descriptions. I could not tell `trim` from `quality-trim` from `fixed-trim`, which are exactly the ones I would want. | Give the trimming lane a small table with one line each. |
| Sequence utilities, "N50, and N90 ... Nx is the length at which records sorted longest first cover at least x percent of all bases." | The definition is correct but arrives after the numbers, and I still cannot tell whether a high or low N50 is good. | Say what direction is better. |
| Sequence utilities, "`lungfish-cli analyze stats NG_000007.3.gb`" | The example uses a `.gb` file that appears nowhere else in the chapter and is not one of the two named fixtures. | Use one of the chapter's own fixtures. |
| Sequence utilities, "Codons are counted in frame +1" | Frame notation is used before the translate section explains frames. | Move the frame gloss earlier or repeat it. |
| Sequence utilities, "gives a 318-residue protein for MT-ND1" | I have no way to check this and no idea whether a wrong table would give an obviously wrong answer or a subtly wrong one. | Say what a wrong table looks like, for example early stop codons. |
| Sequence utilities, "Searching the human mitochondrial genome for the EcoRI site `GAATTC` finds six matches." | This is the one example I could verify from my own knowledge, and there is no command shown for it. | Show the command. |
| Sequence utilities, "Field tokens are `type:<kind>`, `format:<format>`" | The token grammar has five forms plus date and numeric rules in one paragraph, and no list of valid `<kind>` values. | Table the token forms and list the kinds. |
| Alignment and phylogenetics, "`--strategy` accepts `auto`, `linsi`, `ginsi`, `einsi`, `fftns2`, and `parttree`" | Six opaque strings with no guidance. I would always leave it at auto because nothing lets me choose. | One clause saying which to use for many sequences versus few. |
| Alignment and phylogenetics, "`--model` defaults to `MFP`" | MFP is not expanded and I cannot judge it. | Expand as ModelFinder Plus and say it picks a model for you. |
| Alignment and phylogenetics, "`--bootstrap` and `--alrt` set replicate counts" | I know bootstrap loosely from a stats course but not alrt, and no default or sane value is given for either. | Give the usual values. |
| Workflows, "runs a Nextflow file, a Snakefile, or the one supported nf-core pipeline" | Three ecosystem names in one clause, none glossed. I had no idea what a workflow was in this context. | One sentence saying a workflow is a saved multi-step recipe. |
| Workflows, "LGE knows which artifact must receive focused provenance" | Artifact and focused provenance are both unexplained, and this sentence explains a required flag. | Say plainly that you must name the folder the results land in. |
| Workflows, "`--repeat-from <previous.lungfishrun>` re-validates the retained configuration and source identity of a supported bound local run" | I read this three times and still cannot say what it does. Bound, retained, and source identity are all doing unexplained work. | Rewrite as one plain sentence saying it reruns a previous run. |
| Tool packs, "`lungfish-cli conda install --pack read-mapping variant-calling` installs two packs" | I still do not know what a pack contains or how to see the list. `conda packs` is mentioned two paragraphs later. | Point to `conda packs` at first mention. |
| Tool packs, "That is a defect rather than a design choice." | Refreshingly honest, but it does not tell me what to do next in the Plugin Manager. | Name the button to click. |
| Tool packs, "`conda lock` ... It is unresolved, meaning it is not a complete installed-artifact inventory and does not claim compatibility with conda-lock." | Dense and negative throughout. After three readings I could only conclude I should not use it. | Lead with what it is good for. |
| Tool packs, "Zero means there was nothing to do or the update was applied, 10 means work is pending" | Four exit statuses in one sentence, one of which mixes two meanings. Same problem as before, I do not know how to see the number. | Table the four codes. |
| Provenance and metadata, "on an ordinary installation this command reports a missing signature artifact and exits with a failing status. That is the expected result" | A command that is supposed to fail is genuinely confusing, and I had to read it twice to trust it. | Show the exact message so I recognise it. |
| ONT genotyping, whole section | ONT is used in the heading and never expanded anywhere in the chapter. Haplotype definition set, lens, and percent basis are all unexplained. | Expand ONT at first use and gloss the three terms. |
| ONT genotyping, "`--percent-basis <viewed-locus|sample-retained>`" | Two values I cannot choose between, and the denominator language does not help me. | Say which one matches a typical report. |
| Diagnostics, "probes the common bioinformatics tools on `PATH`" | PATH returns here and I still do not know how to change or inspect it. | Say what to do when a tool is reported missing. |
| Known defects, "Running a command whose working directory sits under `/private/tmp`" | Working directory is never explained and I have no idea whether mine is under `/private/tmp`. This is a defect that would silently eat my output. | Say how to tell where you are and that a folder in Documents is fine. |
| Known defects, the whole section | Six defects in one dense paragraph with no line breaks, and I came to this chapter to look one thing up. | Use a five-row table. |
| Whole chapter | There is no first command anywhere. Every example assumes I already have a terminal open, a project made, and files in place. As the student told to look one command up, I could not get to the point of typing anything. | Add a three-line "your first command" block near the top using `version`. |

One thing I learned. A command line run and a clicked run record the same
provenance, so a script is not a shortcut that loses the paperwork.

One thing I still could not do. Open a terminal and run a single command,
because the chapter never says which application to type into.

The sentence I liked most. "That is a defect rather than a design choice."
