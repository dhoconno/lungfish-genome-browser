# Reader 2 report

Persona. A senior who has spent two years pipetting in a wet lab. I know library prep and PCR well. I have never done data analysis and I have never opened a terminal.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "runs from the command line in a terminal" | I have never opened a terminal and the chapter does not say what one is or how to get one on my Mac. The pointer to the CLI Reference does not arrive until "Before you start", two sections later. | Gloss "terminal" here and move the CLI Reference pointer to its first mention. |
| What it is, "tab-separated table" | I do not know what tab-separated means. I have only ever opened files in Excel. | Say a tab-separated table is a plain text file whose columns are separated by tab characters. |
| What it is, "carrying a confidence figure everywhere" | I cannot tell what the confidence is confidence in. Confidence that the sample matches the reference, or confidence that the reads were good? | Say in one clause what the figure is confidence about. |
| What it is, "written diploid-style as `0/1`" | "Diploid-style" was not explained and I could not work out why the numbers are 0 and 1 rather than base letters. | Say 0 means the reference allele and 1 the alternate. |
| What it is, "a single combined store" | "Store" is used as a noun for a thing on disk and I did not know whether it is a file, a folder, or a database. The answer only arrives in the Settings section. | Say here that the store is either one file or one folder depending on the strategy. |
| Why you would do this, "a tumour and its matched normal sample" | "Matched normal" was not glossed. I have heard it in seminars but could not define it. | Gloss it as tissue from the same patient without the tumour. |
| Why you would do this, "the reads simply ran out" | I had to read this twice. I know what a read is from library prep, but "ran out" sounded like a reagent running out rather than no reads covering that position. | Say no reads covered that position. |
| Why you would do this, "HG002 chromosome 20 slice" | HG002 is never explained. I do not know if it is a person, a cell line, or a file name, and "slice" is not glossed either. | Gloss HG002 at first use and say a slice is a chosen stretch of one chromosome. |
| Before you start, "the CLI writes wherever you point it" | CLI is used here as a bare acronym. It is expanded nowhere in the chapter body that I could find, only in the link text "CLI Reference". | Expand CLI at first use. |
| Before you start, "Download the file `GRCh38.chr20.10.0-10.5Mb.fasta`" | I download one FASTA, but the chapter later says GATK needs a `.fai` index "which the fixture ships". I do not know whether the `.fai` is a second file I also have to download from that page. | Say explicitly which files to download and where the `.fai` sits. |
| Before you start, "and remember where you saved it" | This is the only guidance about where the file goes, but every command later uses a bare file name with no folder. I could not perform the step because I do not know how to make the terminal look in my Downloads folder. | Say to run the commands from the folder holding the files, and how to get there. |
| Before you start, "You also need at least one GVCF" | I would have to go back and do the whole previous chapter first. Nothing tells me how long that takes or that the GVCF must sit next to the FASTA for the bare file names below to work. | Say the GVCF must be in the same folder as the reference for the commands as written. |
| Before you start, "which on the command line means `--emit-ref-confidence GVCF`" | I cannot tell whether that is a flag for `lungfish-cli` or for GATK, and there is no example command showing where it goes. | Name which command takes the flag. |
| Before you start, "This feature is experimental." | I do not know what consequences experimental has for me. Will the results be wrong? Will it stop working? | Say what experimental means for the reader. |
| Before you start, "The pack pins GATK 4.6.2.0" | "Pins" is used as a verb and I did not know it. I guessed it means fixes the version. | Say it installs that exact version and no other. |
| Before you start, "around 600 MB, so allow time" | I cannot judge this. 600 MB is a number I understand, but "allow time" gives me no idea whether that is two minutes or an hour on a normal connection. | Give a rough range, or drop the time hint. |
| Before you start, "answers `Unknown tool pack: gatk-core`" | I had to read the whole paragraph twice to work out it is telling me not to do something. It opens by describing a route that does not work. | Lead with the instruction, then the reason. |
| Before you start, "`~/.lungfish/conda/envs/gatk-core/bin/gatk`" | The tilde is never explained. I do not know what `~` means or how to type it in a way the terminal understands. | Gloss `~` as your home folder. |
| Before you start, code block, "CreateSequenceDictionary" | I could not perform this step. The block is the only instruction, and nothing says how to run a code block, whether to type the backslash, or how to tell whether it worked. It prints nothing I am told to expect. | Say what a successful run prints or leaves behind. |
| Before you start, "a sequence dictionary named `GRCh38.chr20.10.0-10.5Mb.dict`" | "Sequence dictionary" is not glossed, and it has no glossary link while `.fai` has none either. I do not know what either file contains. | Gloss both companion files in one sentence each. |
| Before you start, "LGE allows a single GATK step 24 hours before it gives up" | I could not judge whether 24 hours is generous or tight, because the only other number given is under five seconds. | Say whether a typical whole-genome cohort fits inside 24 hours. |
| Procedure, "a backslash at the end of a line continues one command" | I do not know what a backslash looks like versus a forward slash, and the paths in the same chapter use forward slashes. This is the exact kind of character I would get wrong. | Show the character once and name it. |
| Procedure, step 1, "This is the default behaviour, so leaving `--execute` off" | I had to read this twice because it explains the flag by naming the flag I am being told not to type. | Say the command previews unless you add `--execute`. |
| Procedure, step 1, "`--intermediate cohort.combined.g.vcf.gz`" | I could not tell whether this file must already exist or whether the run creates it. Nothing in step 1 says. | Say the run creates it. |
| Procedure, step 1, "with the directory part of each path shortened to three dots" | I had to read this twice to understand that my own output will look different from the printed example. I first thought the dots were something I type. | Say your own output shows full paths in place of the dots. |
| Procedure, step 1, "which of the two combining tools LGE picked" | Neither tool has been named at this point. CombineGVCFs and GenomicsDB are only defined in Settings, several pages later. | Name the two options here in one clause. |
| Procedure, step 2, "exit code 0" | I do not know what an exit code is or that 0 means success. The chapter later shows exit code 2 for a failure, which made me guess. | Gloss exit code and say 0 means success. |
| Procedure, step 2, "the exit code of the last GATK step rather than a figure for the run as a whole" | I read this three times. I could not see how a run can succeed at the second step but not the first, so the warning felt like it was about a situation I cannot picture. | Give the one-line case where the two differ. |
| Procedure, step 2, "`Provenance: .../.lungfish-provenance.json`" | Provenance is used before the glossary link, which appears much later in Reading the results. | Link and gloss provenance at first use. |
| Procedure, step 2, "3.3 seconds of wall time, split as 1.61 and 1.62" | 1.61 plus 1.62 is 3.23, not 3.3, and I could not tell whether the gap is rounding or a missing third step. | Say the remainder is LGE's own overhead, or drop the split. |
| Procedure, step 3, "Read the cohort VCF." | I could not perform this step. Nothing says what to open it with. The file ends in `.gz`, which I know means compressed, so double-clicking seems wrong. | Name one way to open a gzipped VCF. |
| Settings, lead-in, "a word beginning with two dashes" | This gloss of "flag" arrives after the Procedure section already had me typing five flags. | Move the gloss to first use in Procedure. |
| Settings, lead-in, "The GATK toolchain shares three flags across all ten of its subcommands" | I could not work out which three flags are shared, and the sentence has no bearing on anything I do here. | Name the three, or cut the sentence. |
| Settings, GVCF, "unlike most flags it is repeated once per sample rather than given a list" | I had to read this twice. It tells me what it is not before telling me what it is. | State the repetition rule first. |
| Settings, Output, "a tabix index named after it with `.tbi` appended" | Tabix is never glossed anywhere and has no glossary link, though it appears three times. | Gloss tabix at first use. |
| Settings, Intermediate, "Match its shape to your strategy" | "Shape" for a file path is not a word I have met, and this is a step I could get silently wrong. | Say to give a file path for one strategy and a folder name for the other. |
| Settings, Combine strategy, "50 samples or fewer and GenomicsDB above that" | I cannot judge why 50 is the line, or what goes wrong if I force the wrong tool for my cohort size. | Say in one clause what each tool is better at. |
| Settings, Extra args, "put the whole set inside one pair of quotes" | I do not know which kind of quote mark the terminal wants, and my word processor makes curly ones. | Name the straight double quote. |
| Settings, Dry run, "on its own the flag changes nothing" | I read this twice. A setting that does nothing, described in three sentences, made me suspect I had misread the Execute entry above it. | Say it exists only to cancel a fixed `--execute`. |
| Settings, closing, "a candidate whose quality score falls below 30 is not written out" | I do not know what a quality score of 30 means. The chapter tells me the cutoff before telling me the scale, so I cannot tell whether 30 is strict or loose. | Explain the scale before the cutoff. |
| Settings, closing, "the only way to change the confidence threshold is to pass a second copy of it through `--extra-args`" | I could not perform this. Passing a duplicate of an option sounds like it should be an error, and no example is given. | Show the exact `--extra-args` string. |
| Settings, strategy warning, "An unrecognised value is not rejected." | I had to read the paragraph twice because the failure is described before the thing that fails. | Lead with the typo example. |
| Reading the results, "A finished run leaves four files" | The table lists four rows, but one row mentions "its own `.tbi` beside it", which makes five files. I counted twice. | Make the count match the table. |
| Reading the results, "48,057 rows ... 1,026 rows, 843 of them substitutions and 184 of them indels" | 843 plus 184 is 1,027, not 1,026. I checked this three times and assumed I was wrong. | Fix the arithmetic or explain the extra row. |
| Reading the results, VCF row block, "`AC=2;AF=1;AN=2;AS_QD=25.36;DP=62;...;QD=28.73`" | Only some of these keys are explained. AC, AF, AN, and the row-level DP of 62 are never mentioned, and the DP of 62 here conflicts with the DP of 60 explained two paragraphs later, which I could not reconcile. | Explain the difference between the two DP figures. |
| Reading the results, "`2175.06`" and "`.`" in the row | The columns of the VCF row are never named, so I could not tell which field 2175.06 sits in or what the bare dots are. | Label the columns of the example row once. |
| Reading the results, "on a Phred scale" | Phred is not glossed and has no glossary link. I have heard it in a sequencing core talk and cannot define it. | Gloss Phred in one sentence. |
| Reading the results, "99 is the maximum GATK writes and anything under 20 is weak" | I could not judge the space between 20 and 99. Is 40 good? The chapter gives a ceiling and a floor and nothing in between. | Say what a routine good value looks like. |
| Reading the results, "quality-by-depth figure" | Quality-by-depth is used without explanation and the sentence assumes I already know what QD divides by what. | Say it is the quality divided by the depth. |
| Reading the results, "Mean depth across the 1,026 rows was 39.4 reads." | I could not judge this. The chapter gives me the number with no sense of whether 39.4 is thin, normal, or deep for germline work. | Say what depth range is expected for this kind of run. |
| Reading the results, "The lowest quality score in the recorded cohort VCF was 31.6" | This is the row quality in the sixth column, but the same paragraph uses "quality score" for something I earlier met as GQ, at 99. I could not tell whether they are the same measure. | Distinguish row quality from genotype quality by name. |
| Reading the results, "SHA-256 checksum" | SHA-256 is a term I do not know, and the gloss that follows explains checksum but not SHA-256. | Say SHA-256 is the recipe used to compute it. |
| Reading the results, "Cite the sidecar when you write up a method" | I could not perform this. I do not know what citing a JSON file in a methods section looks like. | Show what to quote from it. |
| Reading the results, "left no partial output behind, because LGE removes the files it created" | I read this twice. It says the run left nothing behind, then says a provenance record was written, which is a file left behind. | Say the provenance file is the exception. |
| Reading the results, "`callset.json`, a `vidmap.json`, a `vcfheader.vcf`" | None of these are explained and I cannot tell whether I ever need to look inside them. | Say the reader never opens them. |
| Reading the results, "258 rows ending at position 99,174. Both numbers confirm" | I had to read this twice. 258 rows on its own confirms nothing to me without knowing what the unrestricted run gave over the same 100 kb. | Give the comparison figure the confirmation rests on. |
| What good looks like, "Read the header line beginning `#CHROM` and count the names after `FORMAT`" | I could not perform this. The file is compressed and the chapter has not shown me how to display its header. | Point to the command that shows the header. |
| What good looks like, "A cohort VCF nearly as long as its input GVCF" | "Nearly as long" is not a number I can judge. My run's counts will differ from 48,057 and 1,026 and I have no ratio to compare against. | Give the ratio to expect. |
| On the command line, "`--intervals first100kb.bed`" | A BED file called `first100kb.bed` appears in the final example and nowhere else. I do not know where it came from or how to make one. | Say where the BED file comes from or drop it from the example. |

Three lines.

I learned that a GVCF differs from a VCF because it records the sample's state at every position rather than only where it differs, so silence in it is not ambiguous.

I still could not run a single command in this chapter, because nothing tells me how to open a terminal, how to get it to look in the folder where I saved my files, or how to open a compressed VCF once the run is done.

The sentence I liked most is "It could mean the sample matched the reference, or it could mean nobody looked."
