# Reader 3 report, 06-human-germline-variants/02-joint-genotyping

Persona: pre-med student, English is my second language, strong biology
vocabulary from textbooks, slowed by idioms and long sentences, never
opened a terminal.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "runs from the command line in a terminal" | "Terminal" is never defined. I have never opened one. I do not know if it is an app on my Mac, a window inside LGE, or a website. The chapter starts with a thing I cannot find. | Say in one sentence that Terminal is the macOS app in Applications > Utilities. |
| What it is, "turns a stack of those files" | "A stack of files" is a figure of speech. I read it twice to be sure it does not mean a special file format called a stack. | Write "several of those files". |
| What it is, "so silence in a VCF is ambiguous" | "Silence" as a metaphor for a missing row. I understood it only after the next sentence explained it. | Write "a missing row in a VCF is ambiguous". |
| What it is, "carrying a confidence figure everywhere" | "Confidence figure" is not defined. Confidence in what, and measured how? Later the chapter uses "reference confidence", "quality score", "GQ", and "Phred scale" and I could not tell whether these are the same thing. | Gloss reference confidence at first use and say it is the same quantity later called GQ. |
| What it is, "written diploid-style as `0/1`" | "Diploid-style" is used as an adjective for a notation. I know diploid from genetics, but I do not know what makes a notation diploid-style, and nothing says what `0` and `1` themselves mean. | Say `0` means the reference base and `1` the alternate base. |
| What it is, "gives you a set of tables you then have to line up by hand" | "Line up by hand" is an idiom. I pictured printing them on paper. | Write "compare position by position yourself". |
| What it is, "So the rule for this chapter is short." | Read twice. "Short" made me expect a rule stated in few words, but the following sentence is long and is really a condition, not a rule. | Write "Use this chapter when..." and drop the lead-in. |
| Why you would do this, "a tumour and its matched normal sample" | "Matched normal" is a term of art I do not know. I guessed it means healthy tissue from the same patient but the chapter never says. | Gloss matched normal at first use. |
| Why you would do this, "whether the reads simply ran out" | "Ran out" is idiomatic. I first read it as the reads escaping or being deleted. | Write "whether too few reads covered the position". |
| Why you would do this, "the HG002 chromosome 20 slice" | "Slice" is not defined and HG002 is not explained. I do not know if HG002 is a person, a cell line, or a file name. Later the file is 500 kb and the chapter says "the 500 kb fixture slice", so I think slice means a region, but I had to infer it. | Say HG002 is a public reference human sample and that the slice is a 500 kb region of chromosome 20. |
| Why you would do this, "That is a genuine limitation of the fixture" | "Fixture" appears here for the first time with no explanation. It is also in the front matter as `fixtures_refs`. In my English, a fixture is a lamp or a sports match. | Gloss fixture as the small practice dataset the manual ships. |
| Why you would do this, "the commands are identical for two samples or two hundred" | I could not judge this. Earlier the chapter says the tool changes above 50 samples, so "nothing else changes" and the 50-sample switch seem to contradict each other. | Add that the tool choice changes automatically but the command you type does not. |
| Before you start, "read [CLI Reference] first, which covers where the command line tool lives" | I cannot perform this chapter from the text alone. Every command starts with `lungfish-cli` but this chapter never shows how to make that word work, and it sends me to another document to find out. | State in one line here that `lungfish-cli` must be on your PATH or run by full path, and keep the cross-reference. |
| Before you start, "choose **File > New Project** (Cmd-N)" | The project is required but the chapter then says the files do not need to be inside it and no later step uses the project. I could not tell why I opened it. | Say in one sentence what the open project is needed for. |
| Before you start, "use the Download raw file button" | I could follow this, but the chapter never says where I should put the downloaded FASTA so the commands find it. All commands use a bare file name with no folder. | Say to put the FASTA and the GVCF in the same folder and to run the commands from that folder. |
| Before you start, "which on the command line means `--emit-ref-confidence GVCF`" | I do not know where to type this. It is a flag for a different tool in a different chapter, dropped into a prerequisite sentence with no command shown. | Point to the exact step in chapter 01 rather than quoting the flag alone. |
| Before you start, "Turn on **Show Experimental Features**" | Two settings paths and a menu path in one paragraph, plus the Packs tab. I read this three times to get the order right. | Split into a numbered list of the three clicks. |
| Before you start, "The pack pins GATK 4.6.2.0 and is around 600 MB" | "Pins" is jargon here. I read it as physically attaching something. | Write "installs exactly GATK version 4.6.2.0". |
| Before you start, "which is a defect rather than a design choice" | I could not judge what to do with this. It reads like a note to the developers, not to me, and it made me worry the whole chapter is broken. | Keep the warning but cut the judgement about defect versus design. |
| Before you start, "`~/.lungfish/conda/envs/gatk-core/bin/gatk`" | The `~` symbol is never explained. I do not know if I type it literally. | Say `~` means your home folder and can be typed as is. |
| Before you start, "You make once with GATK's own tool" | I typed the CreateSequenceDictionary command in my head and did not know what success looks like. Nothing says a `.dict` file appears, or where. | Say the command writes `GRCh38.chr20.10.0-10.5Mb.dict` next to the FASTA and prints nothing else. |
| Before you start, "LGE allows a single GATK step 24 hours before it gives up on it" | I could not judge this number. Is 24 hours a limit I can change, and what happens to my files if it stops? | Say whether the limit is adjustable and that a timed-out run is cleaned up. |
| Procedure, "a backslash at the end of a line continues one command" | I do not know what a backslash looks like as distinct from a forward slash, and the paths in the same block use forward slashes. | Show the character once, as `\`, in the sentence. |
| Procedure, Step 1 "This is the default behaviour, so leaving `--execute` off is all it takes." | Read twice. The sentence explains what not to type, which is harder to act on than being told what to type. | Write "Type the command without `--execute`." |
| Procedure, "with the directory part of each path shortened to three dots" | The three dots appear as `...` inside the sample output, so I could not tell whether my own screen will show dots or a real path. | Say your output shows full paths where the example shows dots. |
| Procedure, "Read the first word after `gatk` on the first line" | I could do this, but the chapter does not say what the two possible words are until the Settings section far below, so at this point I did not know what I was looking for. | Name both possible words, CombineGVCFs and GenomicsDBImport, right here. |
| Procedure, Step 1 output, "`GenotypeGVCFs ... -G AS_StandardAnnotation`" | The printed command shows options I never typed and cannot explain at this point. I could not tell if my run is wrong because it shows extra text. | Add one sentence saying LGE adds three options for you, explained in Settings. |
| Procedure, Step 2, "exit code 0" | "Exit code" is not defined at first use. I do not know if 0 is good or bad until I reason that the failure example later shows 2. | Say exit code 0 means success and any other number means failure. |
| Procedure, "the failure path is described at the end of the next section" | "Failure path" reads like a file path to me. | Write "what happens when a run fails". |
| Procedure, "3.3 seconds of wall time ... split as 1.61 seconds ... and 1.62" | 1.61 plus 1.62 is 3.23, not 3.3, and I could not tell if the missing time is a rounding or something I should worry about. | Say the two step times do not sum to the total because of startup overhead. |
| Procedure, Step 3, "Read the cohort VCF." | This is not a step I can perform. Nothing says what to open the file with. It is compressed (`.gz`) so I cannot double-click it. | Name a way to open it, such as a terminal command or an LGE viewport. |
| Settings, "meaning a word beginning with two dashes that you add to the command" | Good gloss, but it arrives in the Settings section long after the Procedure already used many flags. | Move this gloss to the first flag in the Procedure. |
| Settings, "shares three flags across all ten of its subcommands" | I could not judge which three flags these are, and the sentence does not say. | Name the three flags or drop the count. |
| Settings, Intermediate, "which is easy to miss because it has no equivalent in single-sample calling" | Read twice. I could not tell whether the warning is about forgetting the flag or about choosing the wrong shape of path. | Split into two sentences, one for required and one for shape. |
| Settings, Combine strategy, "50 itself falling on the CombineGVCFs side" | I read this three times. The participle clause after a comma is hard for me. | Write "A cohort of exactly 50 uses CombineGVCFs." |
| Settings, Combine strategy, "which matters in a pipeline that must not change behaviour as a cohort grows past 50" | "Pipeline" is not glossed anywhere in this chapter. | Gloss pipeline as an automated series of steps, or drop the clause. |
| Settings, Intervals, "a Picard-style interval list, or a bare contig name" | Three formats named, none explained. I do not know what Picard is or what "bare contig name" would look like. | Show one example of each, for example `chr20`. |
| Settings, Extra args, "a real difference from `lungfish-cli gatk bqsr` where the same flag reaches both commands" | BQSR is never expanded or explained. The comparison assumes I have used a command this chapter never covers. | Drop the comparison or expand BQSR at first mention. |
| Settings, Dry run, "on its own the flag changes nothing" | Read twice. A setting whose description says it does nothing confused me until the third sentence. | Lead with what it does, which is override `--execute`. |
| Settings, "a candidate whose quality score falls below 30 is not written out at all" | "Quality score" appears here with no scale given. Is 30 out of 100? Later the chapter says Phred and 99 is the maximum. I could not judge whether 30 is strict or lenient. | Gloss the Phred scale before quoting 30. |
| Settings, "each step runs with its working directory set to the folder holding that step's own output" | "Working directory" is not defined and I do not know why this matters to me. | Say what breaks if it were otherwise, or drop it. |
| Settings, "the only way to change the confidence threshold is to pass a second copy of it through `--extra-args`" | I could not perform this. It does not show the text I would pass, and I do not know whether GATK accepts the option twice. | Show the exact `--extra-args` string. |
| Settings, "An unrecognised value is not rejected." | Passive and negative. I read twice to work out that LGE ignores my typo. | Write "LGE ignores a value it does not recognise." |
| Reading the results, "leaves four files beside the paths you named" | The table lists four rows but one row mentions a fifth file, the combined `.tbi`, and the lead-in also says "plus the provenance record" while the provenance record is row four. I counted five or six and could not reconcile it. | Fix the count or list every file as its own row. |
| Reading the results, "48,057 rows ... 1,026 rows ... 843 ... 184" | 843 plus 184 is 1,027, one more than the stated 1,026. I could not judge which number is wrong. | Correct the arithmetic or say one row is counted in both classes. |
| Reading the results, first VCF row, "`AC=2;AF=1;AN=2;AS_QD=25.36;DP=62;...;QD=28.73;SOR=0.76`" | Six keys shown, only `AS_QD` explained. I do not know AC, AF, AN, QD, or SOR, and the row's `DP=62` differs from the `DP:60` in the last column with no explanation. | Explain the INFO `DP` versus the sample `DP` difference, or cut the INFO keys you do not use. |
| Reading the results, "2175.06" and "." columns | The eight VCF columns are never named. I cannot tell which number is the quality score the chapter later says must be above 30. | Label the columns once, at least CHROM, POS, REF, ALT, QUAL, FILTER. |
| Reading the results, "`GQ` ... where 99 is the maximum GATK writes and anything under 20 is weak" | Good, but this is the only place a threshold is given for GQ, and it is different from the 30 used for QUAL. I confused the two on first reading. | Say explicitly that GQ 20 and QUAL 30 are different numbers on different scales. |
| Reading the results, "without the weaker one being masked by the stronger" | "Masked" is a metaphor. In genetics I know masking as an epistasis term, so I misapplied it. | Write "without the weaker one being hidden in a single averaged figure". |
| Reading the results, "Mean depth across the 1,026 rows was 39.4 reads." | I could not judge this. No sentence says what depth is good enough for germline calling. | Say what depth range is usual for this kind of data. |
| Reading the results, "meaning no filter was applied rather than that every row passed one" | Read twice. Two "rather than" contrasts in one paragraph. | Write "The dot means no filter was applied. It does not mean the row passed a filter." |
| Reading the results, "a SHA-256 checksum" | SHA-256 is not explained, though checksum is. I did not know if the number matters to me. | Say SHA-256 is one common way of computing a checksum. |
| Reading the results, "Cite the sidecar when you write up a method" | I could not perform this. Citing a JSON file in a methods section is not something I have seen and nothing shows what to write. | Show a one-line example of what to put in a methods section. |
| Reading the results, "a `callset.json`, a `vidmap.json`, a `vcfheader.vcf`, and one subdirectory per interval" | Four names, none explained, and I cannot judge whether I should check them. | Say I never need to open these files. |
| Reading the results, "since a restriction applied only at genotyping would still have imported the whole slice" | Read three times. A counterfactual about a thing that did not happen. | Write "If only the second step were restricted, the combined store would still hold the whole slice." |
| What good looks like, "count the names after `FORMAT`" | I could not perform this. The header line beginning `#CHROM` is never shown anywhere in the chapter, so I do not know what it looks like or where FORMAT sits in it. | Show the `#CHROM` header line once. |
| What good looks like, "The row count should collapse sharply from the input." | "Collapse sharply" is a figure of speech and no threshold is given. 48,057 to 1,026 is about 47 times. Would 5 times be acceptable? | Say roughly what ratio to expect. |
| What good looks like, "A sample missing from that line was not genotyped, whatever the exit code said." | I could not act on this. The chapter tells me the failure but not what to do about it. | Add one sentence naming the likely cause. |
| Next, "the GATK steps that flag the calls not worth trusting" | "Flag" as a verb collides with "flag" meaning a command-line option, which this chapter uses everywhere. | Write "mark the calls not worth trusting". |

The one thing I learned. A GVCF records the sample's state at every
position, so an absent row in an ordinary VCF cannot tell you whether the
sample matched the reference or whether nobody looked.

The one thing I still could not do. Run any command at all, because the
chapter never says how to open a terminal or how to make `lungfish-cli`
work once I am in one.

The sentence I liked most. "It could mean the sample matched the
reference, or it could mean nobody looked."
