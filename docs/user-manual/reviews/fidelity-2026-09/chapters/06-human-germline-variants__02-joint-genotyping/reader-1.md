# Reader 1 report, Joint Genotyping

Persona: sophomore, one genetics course finished, never opened a terminal, never used sequencing software.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "runs from the command line in a terminal" | I have never opened a terminal. The chapter tells me this in the first sentence but does not tell me where a terminal is or how to get one until the Before you start section, and even then it sends me to another chapter. I stalled here wondering whether I could do this chapter at all. | Say in the first sentence which appendix to read first, so the pointer arrives with the warning. |
| What it is, "a confidence figure everywhere" | I do not know what a confidence figure is or what units it is in. Later the chapter uses Phred, quality score, and GQ, and I could not tell whether these are all the same thing as this confidence figure. | Gloss the confidence figure once and say it is the same quantity the later Phred scores measure. |
| What it is, "written diploid-style as `0/1`" | Diploid-style is not explained. I know diploid from genetics but I do not know why the notation is a slash, what the 0 means, or what a `0/0` or a `./.` would mean. | Say the 0 means the reference copy and the 1 the alternate, and give the `0/0` case too. |
| Why you would do this, "a tumour and its matched normal sample" | Matched normal is not defined. I guessed it means healthy tissue from the same patient but I had to stop and guess. | Gloss matched normal at first use. |
| Why you would do this, "the HG002 chromosome 20 slice" | HG002 is used from here to the end of the chapter and never explained. I do not know if it is a person, a cell line, or a file name. Slice is also unexplained. | Say in one clause what HG002 is and that a slice is a small region cut out of a genome. |
| Why you would do this, "the reads simply ran out" | I could not tell whether this means the sequencing machine produced no reads there or the analysis discarded them. The distinction seemed important to the argument. | Say plainly that no reads covered the position. |
| Before you start, "You need a project open" | This contradicts the two sentences above it, which say no step happens inside the app. I read the paragraph twice trying to work out why I must open a window in an app that is not used. | State what the open project is actually needed for, or drop the requirement. |
| Before you start, "the manual's fixtures on GitHub" | Fixture is not explained. It appears in the front matter, here, and twice more later. I do not know if it is a test file, a sample dataset, or something the software needs. | Gloss fixture as a small example dataset the first time it appears in the body. |
| Before you start, download instructions | The instructions download only the FASTA. The commands then use `HG002.g.vcf.gz`, which I was never told how to obtain from this page, and the `.fai` index is said to ship with the fixture but is not in the download steps. I could not perform step 1 from this text. | List every file to download, including the GVCF and the `.fai`. |
| Before you start, "`--emit-ref-confidence GVCF`" | I do not know where this flag goes. It looks like a command fragment with no command around it, and the chapter has not yet shown me any command. | Show the flag inside a full example command or point to the exact place in the previous chapter. |
| Before you start, "Turn on **Show Experimental Features**" | Experimental is not explained. I could not judge whether experimental means unfinished, unsafe for real data, or merely new, and that changes whether I would trust the result for coursework. | Say in one clause what experimental means for the reliability of the output. |
| Before you start, "The pack pins GATK 4.6.2.0" | Pins is jargon here. I read it as physically attaching something. | Say the pack installs exactly that version. |
| Before you start, "which is a defect rather than a design choice" | I was told a documented command does not work and to use the app instead, but not whether this affects anything else I am about to run. It reads like a note to the developers rather than to me. | Say plainly that this does not affect the joint-genotype command itself. |
| Before you start, "`.fai` index" and "sequence dictionary" | Neither is explained. I do not know what either file does or why GATK needs both, and neither is in the glossary list at the top. | Gloss both in one sentence each at first use. |
| Before you start, "`~/.lungfish/conda/envs/gatk-core/bin/gatk`" | I do not know what the tilde means, what conda is, or how to type this. As someone who has never used a terminal this line is opaque and I could not verify I ran it correctly, because the chapter does not say what a successful run prints. | Say the tilde means your home folder, and say what the command prints on success. |
| Before you start, "LGE allows a single GATK step 24 hours" | I could not judge this. Twenty-four hours sounds enormous next to five seconds and I did not know whether it is a warning or reassurance. | Say what happens to my files if the limit is hit. |
| Procedure, "The command line tool is `lungfish-cli`" | The chapter never says how the shell finds this command. Every earlier tool was given as a long path starting with a tilde but this one is a bare word, and I could not tell whether that difference matters. | Say one sentence on why this one is a bare name and the GATK one is a full path. |
| Procedure step 1, "`--intermediate cohort.combined.g.vcf.gz`" | I am asked to name a file that does not exist yet, and at this point I have not been told what an intermediate is. The Settings entry explaining it comes two pages later. | Say in step 1 that this names a working file the first step creates. |
| Procedure step 1, recorded output | The commands shown are `gatk CombineGVCFs ...` but I was told to call GATK by a full tilde path. I could not tell whether the preview is showing me something I should be able to paste and run. | Note that the preview abbreviates the GATK path. |
| Procedure step 1, "`--standard-min-confidence-threshold-for-calling 30.0`" | Thirty is a bare number here and I had no idea what scale it is on. The scale is only explained in Reading the results, several sections later. | Say here that this is a Phred score and what 30 means on it. |
| Procedure step 2, "exit code 0" | Exit code is not explained. I inferred zero means success only because the sentence sounded satisfied. | Gloss exit code and say zero means success. |
| Procedure step 3, "Read the cohort VCF" | This is not a step I can perform. It does not say how to open a gzipped file, whether to use LGE or a terminal command, or what tool to use. | Name the command or the app view that opens the file. |
| Settings, "the other seven subcommands" | Four required flags are promised but nine entries follow, and I counted no marker showing which four are the required ones. I read the section twice to work this out. | Mark the four required entries. |
| Settings, Output, "a tabix index named after it with `.tbi`" | Tabix is not explained and is not in the glossary list. | Gloss tabix in the clause. |
| Settings, Combine strategy, "50 samples or fewer" | I could not judge why 50 is the boundary or what goes wrong on either side of it. The entry tells me the rule but not the consequence of getting it wrong. | Say in one clause what CombineGVCFs does badly above 50. |
| Settings, Intervals, "a Picard-style interval list" | Picard is a tool name I have never met. It is used as though I know it. | Say Picard is another genomics tool and its list is a plain text format. |
| Settings, Dry run, "it overrides `--execute` when both are given" | I had to read this entry twice. The flag is described as changing nothing and then as having a real use, and the two halves seemed to contradict each other. | Lead the entry with the override behaviour. |
| Settings, "`-G AS_StandardAnnotation`" | I do not know what an annotation is in this context, and allele-specific is glossed only much later in Reading the results. | Gloss allele-specific annotation at this first use. |
| Settings, "the only way to change the confidence threshold is to pass a second copy of it through `--extra-args`" | I could not work out what passing a second copy of a flag does. It sounds like an error, not a technique, and no example is given. | Show the exact `--extra-args` string that does this. |
| Reading the results, "48,057 rows" and "1,026 rows" | I could not judge these. The chapter says the collapse should be sharp but gives no ratio or range, so with my own data I would not know whether ten to one or a hundred to one is right. | Give the fold reduction and a rough band to expect. |
| Reading the results, the VCF row block | Nine INFO keys appear, four are explained, and one is elided as three dots. I could not tell whether the unexplained ones matter. The row is also said to be wrapped across two lines and I could not see where the wrap is. | Explain or explicitly set aside the unexplained keys. |
| Reading the results, "`AD` is the pair of read counts" | AD is `0,60` while DP is 60, and the reference count is zero. In genetics I learned diploid samples have two copies, so zero reference reads with a `1/1` call took me a second reading to accept. | Add a clause saying `1/1` with zero reference reads is the expected pattern. |
| Reading the results, "`GQ` ... where 99 is the maximum GATK writes and anything under 20 is weak" | The example value is 2189,180,0 for PL and 99 for GQ, but three thresholds appear nearby on what look like different scales, 30, 99, 20, and 31.6. I could not keep straight which number belongs to which scale. | Name the scale each threshold sits on where it appears. |
| Reading the results, "`PL`" | PL appears in the format string and the example value and is never explained, while the other four keys are. | Gloss PL or say it is not needed here. |
| Reading the results, "Mean depth across the 1,026 rows was 39.4 reads" | A bare number with nothing to judge it against. I do not know whether 39.4 is good, and this is presented as one of two figures showing the run behaved as expected. | Say what depth range is adequate for germline calling. |
| Reading the results, "SHA-256 checksum" | SHA-256 is not explained, though checksum is. I did not know if the number matters. | Say SHA-256 names the recipe used and the reader need not know more. |
| Reading the results, "exit code 2" | A second exit code appears with no scale. I know zero is success but I could not tell whether 2 has a specific meaning or is just non-zero. | Say any non-zero code means failure. |
| Reading the results, "`gdb-workspace` directory holding a `callset.json` ..." | Four file names are listed and none is explained. I could not tell whether I am supposed to look inside any of them. | Say the reader never opens these files. |
| Reading the results, "ending at position 99,174" | The reasoning is that a restriction applied only at genotyping would still have imported the whole slice, but I could not follow how the row count and the end position prove that. I read the sentence three times. | Split the inference into two short sentences. |
| What good looks like, "count the names after `FORMAT`" | I do not know how to see the header line of a compressed file. This is the first check in the section and I could not perform it. | Name the command that prints the header. |
| On the command line, `--intervals first100kb.bed` | A BED file is used in the final example and I was never told how to make one. BED is named in the Intervals setting but never explained or shown. | Show the two lines of a BED file or point to where one is made. |

The one thing I learned. Silence in an ordinary VCF is ambiguous, and a GVCF removes the ambiguity by recording confidence at every position rather than only where the sample differs.

The one thing I still could not do. Step 3, read the cohort VCF, because nothing in the chapter tells me how to open a gzipped file.

The sentence I liked most. "It could mean the sample matched the reference, or it could mean nobody looked."
