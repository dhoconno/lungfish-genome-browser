# Reader report: The AI Assistant Panel

Reader 1. Sophomore, one genetics course, no lab time, never opened a terminal, never used sequencing software.

| Location | What stopped me | What would have helped |
|---|---|---|
| What it is, "answers questions about the dataset you have open" | "Dataset" is used from the first sentence, but later the chapter says "bundle", "project", "reference bundle", and "read bundle" for what seem to be related things. I never worked out whether a dataset is a bundle or a project. | Say once which word means which, or pick one word for the thing that is open. |
| What it is, "the state of the active viewer" | I do not know what a viewer is or what makes one active. It sounds like a window, but I have not opened the app. | One clause saying the viewer is the panel showing your loaded data, and the active one is the one in front. |
| What it is, "the loaded bundle, the organism, the region on screen" | "Bundle" is never glossed anywhere in this appendix, and it is the word used most often. | Gloss bundle at first use as the folder of files LGE treats as one loaded item. |
| What it is, "the rows you have selected in the variant or sample tables" | I have not seen these tables and do not know where they are. The chapter later says "the Variants tab of the table drawer" without saying what a table drawer is. | Gloss the table drawer once, or point to the chapter that covers it. |
| What it is, "LGE ships no model of its own" | "Ships" as a verb for software was not obvious to me on first read. I read the sentence twice. | Say LGE does not include a model. |
| What it is, "your project's provenance, the record of which tool with which version" | The gloss is there but it comes after two commas inside a long sentence, and I lost the main clause. I had to reread. | Break the gloss into its own sentence. |
| What it is, "confirm anything you intend to publish against the viewport" | "Viewport" appears here for the first time, right after "viewer" was used for what I think is the same thing. | Use one of the two words, and gloss it. |
| What it is, "the Operations panel" | First mention, no explanation of what it is or where it is. | One clause saying the Operations panel lists the jobs LGE has run. |
| Why you would do this, "a human chromosome 20 slice and an HBB gene record" | I know HBB from genetics but I do not know what "a slice" is here. A slice of what, a region of the chromosome or a piece of a file? | Say it is a stretch of chromosome 20, and give the size. |
| Why you would do this, "three read bundles under `Imports/`" | These backticked folder names look like something I would type. I have never opened a terminal and could not tell if these are folders I click in the app. | Say these are folders visible in the app's sidebar. |
| Why you would do this, "a SPAdes assembly of HG002 mitochondrial reads and a Kraken 2 classification" | Four unfamiliar things in one clause. SPAdes, HG002, assembly, Kraken 2. None is explained and none has a link. | Gloss each in three words, or say only that the project holds outputs from several tools. |
| Why you would do this, "how many variants sit in its variant track" | "Variant track" is new. I know what a variant is from genetics but not a track. | Gloss track as the row of data drawn along the sequence. |
| Before you start, "any project with a reference bundle loaded will do" | I do not know what makes a bundle a reference bundle rather than another kind. | One clause distinguishing a reference bundle from a read bundle. |
| Before you start, "every question you ask spends your own credit" | I have no idea how much a question costs. Cents? Dollars? I could not judge whether twenty questions is trivial or expensive. | Give a rough order of magnitude for one question. |
| Before you start, "Set aside about twenty minutes" | The twenty minutes is mostly account creation, but the appendix never says what creating a provider account involves or that it needs a credit card. | Say the account setup happens on the provider's website and needs payment details. |
| Step 1, "Choose **Settings...** (Cmd-,) from the application menu" | The gloss for application menu is good, but I did not know whether Settings is in that menu on every Mac app or only this one. Minor, but I paused. | No change needed if the screenshot shows it. |
| Step 3, "`sk-ant-...` for Anthropic, `sk-...` for OpenAI" | I could not tell whether I type those prefixes myself or whether they are already part of the key I paste. | Say the prefix is part of the key the provider gives you. |
| Step 3, "A small indicator sits to the left of each key field and reports what LGE knows about that key" | The chapter tells me to watch it but never says what the indicator shows or what a good state looks like. This is the one place I would actually be stuck at the screen. | Name the states the indicator can show and which one means the key works. |
| Step 4, "It is a floating panel" | The gloss follows, but I read "floating" and pictured something detached and easy to lose before I got to the explanation. | Fine as is, only slowed me briefly. |
| Step 4, "a data overview, a look at the current view, a gene search, a variant statistics breakdown, a PubMed literature search, and a chromosome guide" | Six suggestions listed as bare noun phrases. I could not tell what buttons would actually say on screen. | Quote one button's label so I can match it to the screenshot. |
| Step 5, "warns that a failed request may already have reached one provider before another receives the fallback" | I read this three times. I think it means my data can go to two companies if the first one fails, but the sentence buries that. | State it plainly as one short sentence. |
| Step 5, "only providers with validated credentials are used" | "Validated credentials" is new phrasing for what Step 3 called a working key. | Use the same words as Step 3. |
| Step 6, "A spinning indicator appears while the provider works" | I do not know how long to wait before something is wrong. Seconds? A minute? | Give a rough expected wait. |
| Reading the results, "whether it was grounded in a lookup or written from the model's own recollection" | "Grounded" is doing a lot of work here and is not defined until the next paragraph implies it. | Gloss grounded at first use as based on your actual data. |
| Reading the results, "Eleven lookups are available to the assistant" | The paragraph then describes what sounds like about ten things in a run-on list, and I could not count eleven. Then it says two move the view and the other nine only read, which is eleven, but I could not map the nine. | Put the lookups in a short list so the count can be checked. |
| Reading the results, "list the chromosomes or contigs in the loaded reference" | "Contig" is not glossed. | Gloss contig as a stretch of assembled sequence. |
| Reading the results, "its budget of eight lookup rounds" | I do not know what a lookup round is or why eight. I could not judge whether my question was near that limit. | Say one round is one time the assistant checks your data. |
| Reading the results, "it will name macaque-compatible antibody clones" | "Antibody clone" and "epitope compatibility" are immunology terms I have not had. | Gloss clone and epitope, or cut the example. |
| What good looks like, "open the Variants tab of the table drawer and compare" | I still do not know how to open the table drawer. The step is not performable from this chapter. | Say how to open it, or link to the chapter that does. |
| What good looks like, "The app's own count is the arbiter" | "Arbiter" made me stop. | Say the app's count is the correct one. |
| On the command line, "an IUPAC ambiguity motif, or a regular expression" | Both terms are unglossed. I have never seen a regular expression. | Gloss both, or say these are two ways of describing a pattern to search for. |
| On the command line, "writes the hits as BED" | BED is not explained. | Gloss BED as a plain text file listing positions. |
| On the command line, "a field syntax such as `type:fastq_dataset`" | I could not tell what this does or why it is being shown to me in a section that says nothing here is needed. | Cut the example or say what it returns. |
| On the command line, "a `.lungfishgenotype` bundle" | New file type, new word, in the last paragraph, with no explanation. | Say it is the file the genotyping chapters produce. |

Three lines.

The one thing I learned. Anything I ask the assistant leaves my computer and goes to an outside company under my own paid account, and the answer is written text rather than something LGE calculated.

The one thing I still could not do. Check the assistant's variant count against the app, because the chapter tells me to open the Variants tab of the table drawer and never says how.

The sentence I liked most. "Use the panel to interpret and to orient yourself, and confirm anything you intend to publish against the viewport, the Operations panel, or the command line."
