# Reader 4 report, Reference Files for GATK

Persona. Undergraduate, one semester of Geneious in a course, comfortable with a
graphical sequence viewer, expects every operation to have a dialog, has never
opened a terminal.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| Preview note, "The `gatk` commands print the command" | I do not know what "the `gatk` commands" are. Nothing so far has told me these are things I type somewhere. In Geneious every tool is a menu item. | Say in one sentence that this chapter is done by typing commands into the macOS Terminal app, not in the LGE window. |
| Preview note, "only run it when you add `--execute`" | "Add" it where. I have nothing to add it to yet. The backtick styling is not explained anywhere I have read. | Move this sentence after the first shown command so there is something visible to add the flag to. |
| What it is, "It is not an LGE object" | I do not know what an "LGE object" is. I have no mental model of what LGE's objects are. | Replace with a plain phrase such as "it is not something LGE stores or shows in the sidebar". |
| What it is, "No `lungfish-cli` command installs" | First use of `lungfish-cli` and it is never explained. Later the chapter shows it typed without a path while `gatk` is typed with a long path, which I cannot reconcile. | Gloss `lungfish-cli` at first use and say why it needs no path when `gatk` does. |
| What it is, "GATK will not read a bare FASTA" | GATK itself is never defined. The chapter assumes I know it is a variant-calling toolkit from the Broad Institute. | Expand GATK at first use with a half-sentence on what it does. |
| What it is, "a first GATK run against a bare FASTA fails" | "Bare" is doing technical work here and is not defined until I infer it from context two paragraphs later. | Say "a FASTA with no companion files beside it". |
| Why you would do this, "20 means a one in a hundred chance" | Good gloss, but I still cannot judge the scores in my own data because no typical value is given. Is 30 good. Is 37 normal. | Add the typical Illumina range so the number can be judged. |
| Why you would do this, "a human genome carries several million of them" | I cannot tell whether several million out of three billion is a lot or a little, and that judgement is the whole point of the paragraph. | Give the fraction, roughly one position in a thousand. |
| Before you start, "the command line writes wherever you point it" | "The command line" appears here as a known thing. I have never opened one and the chapter has still not told me to. | State plainly that you open Terminal from Applications > Utilities and type these lines there. |
| Before you start, "Download the files ... from the manual's fixtures on GitHub" | I followed this, but the third required file, the BAM, is described in a way I could not act on. It says the fixture "ships" it under `expected/mapping/HG002.sorted.bam` yet the GitHub link points at a different folder. | Give the direct path or link for the BAM the same way the two other files are given. |
| Before you start, "Turn on **Show Experimental Features** in **Settings > Advanced**" | In Geneious, Preferences is under the app menu. I looked for "Settings" and did not know whether this is a menu item, a window, or a tab. | Say Lungfish Genome Explorer > Settings, then the Advanced tab. |
| Before you start, "The card shows an estimate of about 600 MB ... measured 887.6 MB" | I cannot judge which number to plan around, and I do not know whether the difference means something is wrong. | Say directly to budget about 900 MB and treat the card estimate as low. |
| Before you start, "which understates what the pack now does" | Had to read twice. The sentence criticises the app's own description mid-clause and I lost the thread of what the pack is for. | Split into two sentences, one describing the pack and one noting the description is out of date. |
| Before you start, "the `lungfish-cli variants phase` route" | "Route" is used as jargon for a command path. Nothing has defined it. | Say "the phasing command". |
| Before you start, "Both exit with status 3" | I do not know what an exit status is or where I would see one. | Gloss exit status once, or drop the number for this audience. |
| Before you start, "`~/.lungfish/conda/envs/gatk-core/bin/gatk`" | The tilde is never explained. I would not know this means my home folder, and I cannot find this folder in Finder because it is hidden. | Gloss `~` at first use and note the folder is hidden in Finder. |
| Before you start, "which ships in the Required Setup pack every project already has" | I was never told my project has packs, and this contradicts my Geneious expectation that tools are just present. | Add a clause saying Required Setup installs itself the first time you make a project. |
| Reference FASTA, "You create it with `samtools faidx`" | This is the first command block in the chapter and I still have no instruction telling me where to type it or what folder to be in. The command has no path to my FASTA. | Add a sentence before the block saying to open Terminal and change to the folder holding the downloaded files. |
| Reference FASTA, "`chr20_10.0-10.5Mb	500001	19	50	51`" | The tabs are invisible on the page, so I could not tell the five fields apart before reading the sentence after it twice. | Render the five fields as a short labelled list instead of a raw line. |
| Reference FASTA, "prints nothing when it succeeds" | Helpful, but it left me unsure how to confirm it worked, since I have no Finder view of a hidden folder or a results panel. | Add the one command that lists the new file, or say the file appears next to the FASTA in Finder. |
| Reference FASTA, "A USER ERROR has occurred: Fasta index file file:///.../" | The `...` in the middle of the path made me think it was something I would literally see. | Say the ellipsis stands in for your own folder path. |
| Sequence dictionary, "a checksum of its bases" | Checksum is not defined and is not in the glossary list at the top. | Gloss it in half a sentence as a fingerprint that changes if the sequence changes. |
| Sequence dictionary, "`CreateSequenceDictionary`, which ships inside GATK itself" | The command block calls `gatk CreateSequenceDictionary` with no `-O`, so I did not know where the output goes or what it will be named. | Say the file is written beside the FASTA with the `.dict` name given above. |
| Sequence dictionary, "a `@HD` version line and one `@SQ` line" | These header codes are shown as if familiar. I have never seen a SAM-style header. | Say they are header lines and name what each one records. |
| Sequence dictionary, "the Call Variants dialog, which takes its reference from the bundle" | "Bundle" appears here for the first time with no gloss, and it is exactly the Geneious-shaped thing I would have reached for. | Gloss bundle at first use and link the chapter that creates one. |
| Known-sites files, "ordinary bgzip-compressed VCF files" | Bgzip is new and is not glossed. I do not know if a normal `.gz` file counts. | Gloss bgzip and say a plain gzip file will not work. |
| Known-sites files, "the curated Mills and 1000 Genomes indel set" | Named as if known. I have no idea what Mills is or how to obtain it. | Add a clause saying what it contains and where it lives in the Broad bundle. |
| Known-sites files, "so plan the dbSNP download rather than starting it casually" | No number for how long or how much disk, so I cannot plan anything from this. | Give a rough time or a total disk figure. |
| Known-sites files, "holding 961 records on the sliced contig" | "Contig" is used throughout and never glossed, though it is central to every error in this chapter. | Gloss contig at first use, which is in the What it is section. |
| Known-sites files, "`bcftools reheader --fai`" | The bcftools path is given as `~/.lungfish/conda/envs/bcftools/bin/bcftools`, but the chapter never said bcftools was installed or which pack provides it. | Say which pack supplies bcftools alongside the samtools sentence. |
| Known-sites files, "Splitting them into two files with `bcftools view -v indels`" | This is a fragment of a command, not a command. I could not run it, and I could not tell whether I was supposed to. | Either give the two full commands or say this step is optional and not used later. |
| Known-sites files, "since the option repeats" | Had to read twice. I did not know that "the option repeats" means you write `--known-sites` more than once on one line. | Show one example line with two `--known-sites` flags. |
| Interval list, "the start counts from zero and the end is not included" | I understood the words but not the consequence. I could not tell whether `0 100000` gives 100000 or 100001 bases. | State the base count the example line covers. |
| Interval list, "Write it as a BED file" | I do not know how to make a plain text file with real tab characters. TextEdit would give me a rich-text file and spaces. | Name a way to create it, or say to use the one that ships with the fixture. |
| What good looks like, "which `bcftools view -h` will show you" | The command is missing its file argument and its long path, so I could not run it as written. | Show the full line the way the other examples are shown. |
| What good looks like, "the BAM you plan to recalibrate carries a read group" | I am told to check this but never told how to check it. | Add the one command that prints the `@RG` line. |
| What good looks like, "a 1,191,386 byte recalibration table" | These three exact byte counts cannot be judged. I do not know how far off mine may be before something is wrong. | Say roughly what sizes to expect and that exact bytes will differ. |
| What good looks like, "a transition to transversion ratio of 2.25" | Transition and transversion are never defined, and the paragraph's whole conclusion rests on them. | Gloss both in one sentence before the ratio. |
| What good looks like, "a novel set whose ratio drops well below" | "Well below" is not a threshold I can apply. Two point two five versus one point three four, is that well below. | Give the rough cutoff that would worry you. |
| On the command line, "`GATK=~/.lungfish/conda/envs/gatk-core/bin/gatk`" | Shell variable assignment is completely new. I do not know that these three lines must be typed in the same window as everything after them. | Add one sentence saying these lines create shortcuts used by the commands below and must be in the same Terminal window. |
| On the command line, "Step 4, read the two commands before running anything" | Step 4 and Step 5 are the same command with one word different, and I nearly ran the wrong one. Nothing tells me what the printed preview looks like. | Say explicitly that Step 4 only prints and changes nothing. |
| On the command line, "`--create-output-bam-index true` on the second is the default" | Had to read twice. The sentence names a flag, says it is a default, and then names the opposite flag, all in one breath. | Split into two sentences. |
| On the command line, "`--extra-args` appends whatever you quote" | No example of what quoting looks like, and I have never quoted anything on a command line. | Show one short `--extra-args` example. |
| On the command line, "`conda export-pack` ... Move it to the target machine" | This paragraph is written for someone administering machines. As a student it stopped me because I could not tell whether it applied to me. | Open the paragraph by saying most readers can skip it. |
| Whole chapter, "The Plugin Manager is the only route that works" | The chapter has a GUI entry point for installing the pack and then nothing in the app afterwards. As a Geneious user I expected a dialog for each of these four files and there is none, which is only stated obliquely. | State once near the top that no dialog exists for any step in this chapter. |
| Whole chapter, front matter "audience: power-user" | Reading as an undergraduate, the label warns me off but the prose still starts by explaining FASTA from scratch, so I could not tell whether I was the reader. | Add one line at the top saying who this chapter is for. |

The one thing I learned. Known-sites files exist because BQSR would otherwise
count a person's real variants as sequencing errors.

The one thing I still could not do. Open a Terminal and get to the folder with
my downloaded files, because the chapter never tells me to.

The sentence I liked most. "Picture a folder you assemble yourself and then
point commands at, not something you fetch from a server."
