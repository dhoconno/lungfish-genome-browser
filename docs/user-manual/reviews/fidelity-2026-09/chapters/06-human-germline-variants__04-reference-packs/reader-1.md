# Reader 1 report, undergraduate reader

Chapter: `docs/user-manual/chapters/06-human-germline-variants/04-reference-packs.md`

Persona: sophomore who has just finished introductory genetics, has never opened a terminal, has never used sequencing software.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| Front matter, `audience: power-user` | The chapter is labelled for power users, but nothing at the top tells me whether I should be here or turn back. I read the whole thing before realising it may not be for me. | Add one line under the preview note saying who this chapter is for and what to read first if you are not that person. |
| Preview note, "The `gatk` commands print the command" | "GATK" is used four times before anything says what GATK is. I know what a gene is, not what a toolkit called GATK does. | Gloss GATK at first use as a widely used software package for calling variants from sequencing data. |
| Preview note, "only run it when you add `--execute`" | I have never typed a command. I do not know what "add `--execute`" means or where I would add it. | Say that `--execute` is a word you type at the end of the command line, and point at the terminal primer chapter. |
| What it is, "It is not an LGE object." | I do not know what an "LGE object" is, so a sentence defining a term by saying what it is not tells me nothing. | Replace with a plain statement that LGE does not track or manage this folder for you. |
| What it is, "No `lungfish-cli` command installs" | `lungfish-cli` appears with no explanation. I do not know it is a program you type into a terminal rather than a menu in the app. | Gloss `lungfish-cli` at first use as the command line version of LGE. |
| What it is, "the lines under it hold the bases" | Good so far, but "contig" arrives two paragraphs later with no gloss, and I only half know it from lecture. | Gloss contig at first use as one continuous stretch of assembled sequence. |
| What it is, "pass absolute paths at them" | "Absolute path" is never defined and the phrase "pass paths at them" is not English I recognise. I read it three times. | Gloss absolute path as the full address of a file starting at the top of the disk, and give one example. |
| Why you would do this, "where 20 means a one in a hundred chance" | The numbers 20 and 30 are explained, which is good, but I am never told what score is normal or what would worry me. I cannot judge a Phred score I see. | Add one sentence saying what range modern instruments typically report. |
| Why you would do this, "a human genome carries several million of them" | Several million sounds huge and I have no sense of whether that is a lot relative to 3 billion bases. | Give the fraction as well, for example roughly one difference every thousand bases. |
| Why you would do this, "and one of them fails on precisely that check" | I could not tell whether this failure is something I am supposed to cause on purpose or a warning to avoid. It reads as a teaser. | Say plainly that the chapter shows the failure deliberately so you can recognise it. |
| Before you start, "the fixture ships under `expected/mapping/HG002.sorted.bam`" | I downloaded two files from GitHub by clicking, but this third file is described as "shipping" somewhere with a relative path and no download instructions. I could not get it. | Give the same click-by-click GitHub instructions for the BAM as for the other two files. |
| Before you start, "You also need a BAM alignment of that sample" | BAM is used repeatedly and never glossed, even though FASTA and VCF are. | Gloss BAM at first use as the compressed file holding reads lined up against a reference. |
| Before you start, "Cmd-Shift-B" | The menu path and the shortcut are given, which helps, but I do not know if Plugin Manager needs the project window focused or works from anywhere. | State that the project window must be frontmost. |
| Before you start, "measured 887.6 MB once installed, so budget more disk" | I do not know how much free disk this needs in total once you add the phasing pack and the downloads, so I cannot check before starting. | Give one total figure for everything this chapter installs and downloads. |
| Before you start, "Both exit with status 3 and install nothing." | "Exit with status 3" means nothing to me. I have never seen an exit status. | Say the command stops with an error and installs nothing, and drop the number or gloss it. |
| Before you start, "not on your shell's search path" | "Shell" and "search path" are both new. I do not know what a shell is. | Gloss this as the reason you must type the whole location of the program each time. |
| Before you start, "which ships in the Required Setup pack every project already has" | I could not confirm this. Nothing tells me how to check that samtools is actually installed before I try the command. | Add one sentence on where in Plugin Manager to confirm Required Setup is installed. |
| The reference FASTA and its index, "```bash ~/.lungfish/conda/envs/samtools/..." | This is the first command block in the chapter and nothing tells me where to type it. I do not have a terminal open and the chapter never says to open one. | Add a short lead-in saying to open Terminal and change to the folder holding the FASTA. |
| The reference FASTA and its index, "`~/.lungfish/conda/envs/...`" | The `~` character is unexplained. I do not know it stands for my home folder. | Gloss `~` at first use. |
| The reference FASTA and its index, "prints nothing when it succeeds" | Helpful, but then I cannot tell success from the command silently failing to start. | Add the one thing to look at afterwards, such as the new `.fai` file appearing beside the FASTA. |
| The reference FASTA and its index, "34 bytes holding one line" | I cannot judge 34 bytes. If mine were 41 bytes I would not know whether that is fine. | Say that the size varies with the contig name length and that only the contents matter. |
| The reference FASTA and its index, "the byte offset where its bases begin" | "Byte offset" assumes I know files are counted in bytes from a starting point. I did not follow the five fields. | Say this field is only used internally and does not need checking. |
| The reference FASTA and its index, "```text A USER ERROR has occurred" | The error block appears with no instruction on where I would see it. In the app, or in the terminal? | Say the message appears in the terminal output, or in the failed operation's log. |
| The sequence dictionary, "a checksum of its bases" | "Checksum" is not glossed. I guessed it was some kind of fingerprint but was not sure. | Gloss checksum as a short fingerprint computed from the sequence. |
| The sequence dictionary, "a `@HD` version line and one `@SQ` line" | These header codes are shown with no explanation of what `@HD` and `@SQ` mean or whether I need to read them. | Say these are header tags you do not need to interpret. |
| The sequence dictionary, "a `.dict` of 249 or 252 bytes ... is the same dictionary" | Good explanation, but I still do not know how to open a `.dict` file to check it at all. | Name the command or the app that shows a `.dict` file's contents. |
| The sequence dictionary, "the Call Variants dialog, which takes its reference from the bundle" | "Bundle" is used with no gloss and I do not know what a bundle is in LGE. | Gloss bundle at first use, or link to the chapter that defines it. |
| The known-sites files, "ordinary bgzip-compressed VCF files" | "bgzip" is new and is never glossed, though tabix is linked. I do not know how it differs from a normal zip. | Gloss bgzip as a compression format that still allows jumping to a position. |
| The known-sites files, "roughly 1.5 GB compressed ... so plan the dbSNP download" | I am told to plan but not told how long that download takes or whether the chapter ever needs it. | Say plainly that this chapter never downloads dbSNP and the fixture stands in for it. |
| The known-sites files, "the failure is the most useful thing in this chapter" | I read this twice. I could not tell whether I am meant to run something that fails, and if so, which command produces this error. | Show the command that produces the error, or say the error is shown only for recognition. |
| The known-sites files, "`bcftools reheader --fai` does" | The bcftools binary path is used in the block but bcftools is never introduced the way samtools and GATK are. | Introduce bcftools at first use and say which pack provides it. |
| The known-sites files, "809 are SNVs ... and 152 are indels" | These counts are given but I am not told whether the ratio is normal or whether I should compare mine to it. | Say whether this split is expected, or state that the counts are informational only. |
| The known-sites files, "with `bcftools view -v indels` mirrors how dbSNP and Mills are used" | A splitting step is suggested but no complete command is given, so I could not perform it. The chapter's own worked runs never split. | Either give the full two commands or say the split is optional and not used here. |
| The interval list, "Write it as a BED file" | I am told to write a file but not how. I have never made a text file with a tab in it, and the text says "tab-separated" while I would type spaces. | Add the exact way to create the file, and warn that the separators must be real tabs. |
| The interval list, "the start counts from zero and the end is not included" | I read this twice. I did not work out that `0 100000` therefore covers bases 1 through 100000. | Add the worked equivalent in one-based coordinates. |
| The interval list, "and you pass it with `--intervals`" | Nothing shows `--intervals` used in a full command anywhere in the chapter, so I could not use it. | Add one complete example line with `--intervals`. |
| What good looks like, "check four things" | The paragraph then lists five checks joined by "and". I counted and could not reconcile it with "four". | Fix the count, or split the checks into a list. |
| What good looks like, "which `bcftools view -h` will show you" | The command is given without the full path, unlike everywhere else, so if I copy it my terminal will not find it. | Use the same full path form used in the other blocks. |
| What good looks like, "the `@RG` header block naming the sample" | I am told the BAM must have a read group but not how to check whether mine does. | Give the one command that shows the `@RG` line. |
| What good looks like, "a 1,191,386 byte recalibration table, a 16,929,652 byte" | These exact byte counts cannot be judged. If mine differ slightly I do not know whether something is wrong. | Say these are the fixture's exact figures and that small differences are expected. |
| What good looks like, "873 SNVs of which 805 were already catalogued" | This whole paragraph uses `lungfish-cli gatk collect-metrics` and "the fixture's bcftools calls", neither of which the chapter ever produced, so I could not reproduce it. | Say this run is illustrative, or point at the chapter that produces the calls. |
| What good looks like, "a transition to transversion ratio of 2.25" | Transition and transversion are never glossed. I met them in lecture but could not say which is which, and the ratio's meaning was hard to follow. | Gloss transition and transversion in one clause at first use. |
| On the command line, "GATK=~/.lungfish/conda/envs/gatk-core/bin/gatk" | These three lines assign shortcuts, which I have never seen. I did not know they must be typed in the same terminal window as the commands below. | Add one sentence saying these three lines define shortcuts used by the commands that follow. |
| On the command line, "# Step 4, read the two commands before running anything." | Step 4 and Step 5 are the same command apart from one word, and I nearly ran Step 5 first. The `\` line continuations are also unexplained. | Say Step 4 only previews and Step 5 runs, and gloss the trailing backslash. |
| On the command line, "lungfish-cli gatk bqsr" | Everything else in the chapter uses a full path because tools are not on the search path, but `lungfish-cli` is called bare. I do not know whether that will work for me. | State where `lungfish-cli` lives or how to make it runnable. |
| On the command line, "`--create-output-bam-index true` on the second is the default" | I could not tell whether this is something I type or something LGE adds for me. | Say LGE adds it automatically and name the flag that turns it off. |
| On the command line, "That differs from `gatk joint-genotype`, where `--extra-args` lands only on the final step" | A behaviour of a command this chapter never runs is compared to one it does. I lost the thread. | Move this comparison into the joint genotyping chapter, or drop it. |
| On the command line, "install it with `conda offline-install`" | The command is named without its options, so I could not run it. | Give the full command with its required flags. |
| Whole chapter | Every worked number, file, and error comes from a chromosome 20 slice, and I never learn what changes when I use a whole genome instead. Times and sizes would all be different. | Add one sentence saying how the runtimes and file sizes scale for a whole human genome. |

The one thing I learned is why known sites exist, that BQSR would otherwise treat a person's real variants as sequencing errors.

The one thing I still could not do is run any command at all, because the chapter never says to open a terminal or how to move into the folder holding my files.

The sentence I liked most is "silently mixing two versions of a genome produces results that look fine and are wrong."
