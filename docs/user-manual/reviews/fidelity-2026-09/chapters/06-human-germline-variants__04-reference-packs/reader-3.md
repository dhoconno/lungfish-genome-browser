# Reader 3 report, Reference Files for GATK

Reader persona. Pre-med student, English is my second language, comfortable
with biology words from textbooks, slow with idioms and long sentences, never
opened a terminal.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| Front matter, `audience: power-user` | I do not know if "power-user" means I should stop reading. Nothing in the chapter tells me what a power user is or what I need to already know. | Say in the first paragraph who this chapter is for and what the reader is expected to have done already. |
| Preview note, "print the command they would run and only run it when you add `--execute`" | I do not understand what "print the command they would run" means. Print on paper? Where does it appear? I have never opened a terminal so I do not know where text goes. | Say the command text appears on screen in the Terminal window and nothing is changed on disk. |
| Preview note, "check results against something you trust" | "Something you trust" is vague. I do not know what a trustworthy alternative would be here. | Name one concrete comparison, for example the benchmark VCF used later in the chapter. |
| What it is, "Picture a folder you assemble yourself" | "Picture" as a verb and "assemble" as a metaphor made me read the sentence twice. I first thought there was a picture or an image file. | Replace with a plain statement that you make the folder yourself. |
| What it is, "not something you fetch from a server" | "Fetch from a server" is not explained. I do not know what a server is in this context or how fetching would happen. | Say "not something LGE downloads for you". |
| What it is, "the lines under it hold the bases" | "Bases" is used before it is glossed. I know it from genetics class, but it is not linked like FASTA and reference genome are. | Add a one clause gloss, the letters A, C, G, and T. |
| What it is, "GATK will not read a bare FASTA" | "Bare" is an idiom here. I thought it meant an empty file. | Say "a FASTA with no companion files beside it". |
| What it is, "GATK" itself | GATK is never expanded or glossed anywhere in the chapter. It is in the title and used dozens of times. It is not in `glossary_refs`. | Expand it at first use and add a glossary entry. |
| What it is, "check that every other file it is handed describes the same genome" | "Handed" is a figure of speech for a program. I read it twice trying to work out who hands what. | Say "every other file you give it". |
| What it is, "a first GATK run against a bare FASTA fails on the missing file rather than building it" | Long sentence with two negatives in effect. I had to read it twice to learn the simple point, which is that GATK does not create the file for you. | Split into a short sentence saying GATK does not create the file, it stops. |
| What it is, "the only honest way to do that without knowing the answer in advance" | "Honest way" and "knowing the answer in advance" are figures of speech. I could not tell whether "the answer" means the true genotype or the correct file. | State plainly that you cannot use your own results to judge your own results. |
| What it is, "pass absolute paths at them" | I do not know what an absolute path is, and "pass paths at" is unfamiliar grammar. This is the first place a terminal concept appears with no explanation. | Gloss absolute path with one example, and use "give the commands the full path". |
| Why you would do this, "20 means a one in a hundred chance of being wrong and 30 means one in a thousand" | Good that two values are explained, but I cannot judge which value is good for my data. Is 30 good? Is 20 bad? | Add one sentence saying what range is typical for Illumina data. |
| Why you would do this, "Instruments are systematically optimistic or pessimistic" | Machines described as optimistic and pessimistic is a metaphor. I understood after rereading, but it slowed me. | Say the reported score is systematically too high or too low. |
| Why you would do this, "Feeding those in as errors would poison the model" | "Feeding in" and "poison the model" are both figures of speech. I do not know what "the model" refers to, because no model was named before this. | Say the calculation would be wrong, and name what the model is. |
| Why you would do this, "a human genome carries several million of them" | I cannot judge this number against anything. Several million out of how many positions? | Give the denominator, roughly 3 billion bases. |
| Why you would do this, "the mismatches left over really are dominated by errors" | "Dominated by" is imprecise for me. Mostly? Almost all? | Say "are mostly sequencing errors". |
| Why you would do this, "silently mixing two versions of a genome produces results that look fine and are wrong" | "Two versions of a genome" is not explained. I do not know what a genome version is, for example GRCh37 against GRCh38. | Name the two builds as the example. |
| Why you would do this, "BAM" | BAM is used here and throughout with no gloss and no glossary link, unlike VCF which is linked. | Gloss BAM at first use and link it. |
| Before you start, "Cmd-N" | I do not know if Cmd is the Command key on Mac. Nothing says this manual is macOS only. | Spell out the key once. |
| Before you start, "the command line writes wherever you point it" | "Point it" is an idiom and "the command line" has not been introduced. This is the first mention and I have never used one. | Introduce the command line with one sentence, or link to the chapter that does. |
| Before you start, "Download the files ... and remember where you saved them" | I can follow this, but nothing tells me where I should save them so the later commands work. Later commands use only bare file names, which implies a specific folder. | Say to put all files in one folder and to open the Terminal in that folder. |
| Before you start, "which Mapping Reads to a Reference produces and the fixture ships under `expected/mapping/HG002.sorted.bam`" | Two different sources for one file in one sentence. I could not tell whether I must run the other chapter or just download this path. The GitHub instructions above do not cover this path. | Give one route with the same click-by-click detail used for the other two files. |
| Before you start, "The card shows an estimate of about 600 MB. The environment ... measured 887.6 MB" | Three numbers, 600, 887.6, and later 865, for what feels like the same thing. I could not tell how much disk I actually need. | State one number to plan for and say the card underestimates. |
| Before you start, "which understates what the pack now does" | "Understates" plus a quoted description made me read twice. The point is that the description is out of date. | Say the description is out of date. |
| Before you start, "The Plugin Manager is the only route that works for either pack, which is a defect rather than a design choice." | I do not know what to do with this. It reads like a note to the developers, not to me. | Move the defect explanation to a note and keep the instruction in the body. |
| Before you start, "Both exit with status 3" | I do not know what an exit status is or where I would see a 3. | Gloss exit status once, or drop the number here. |
| Before you start, "it is not on your shell's search path" | "Shell", "search path", and "full path" all appear in one sentence and none is explained. As a first terminal user I stopped completely here. | Gloss shell and search path, or simply say you must type the whole path shown. |
| The reference FASTA and its index, the `samtools faidx` code block | I cannot perform this step. The chapter never says where to type this, how to open a Terminal, or that I must first move into the folder holding the FASTA. | Add one preliminary step showing how to open Terminal and change to the folder. |
| The reference FASTA and its index, "prints nothing when it succeeds" | I would think the command failed. Nothing tells me how to confirm it worked. | Say to check that a new `.fai` file appears beside the FASTA. |
| The reference FASTA and its index, "34 bytes holding one line, `chr20_10.0-10.5Mb 500001 19 50 51`" | I cannot judge 34 bytes. The five fields are explained after the numbers, so I had to read the line twice with the explanation. | Explain the five fields before showing the line. |
| The reference FASTA and its index, "the figure every later count in this part is measured against" | "This part" is unclear. Part of the chapter or Part of the manual? | Name what "this part" is. |
| The reference FASTA and its index, "GATK stops before it reads a single read" | "Reads a single read" uses read twice with two meanings in four words. I had to parse it twice. | Reword so read appears once. |
| The reference FASTA and its index, the error block with `file:///.../` | The `...` in the path is not explained. I do not know if I should expect to see three dots on my screen. | Say the dots stand in for your own folder path. |
| The sequence dictionary, "a checksum of its bases" | Checksum is not explained and not in the glossary list. | Gloss it in one clause. |
| The sequence dictionary, "The index appends to the whole file name and the dictionary replaces the `.fasta` extension" | Correct but abstract. I had to map it onto the two example names below it, twice. | Show the two file names first, then the rule. |
| The sequence dictionary, "GATK uses the dictionary as the authority on what genome you mean" | "The authority on what genome you mean" is a metaphor. I understood only after the next sentence. | Say GATK compares each file against the dictionary. |
| The sequence dictionary, "an `M5` checksum of `0bffe5f36c15cdb7069b96a1d4e4a0ef`, and a `UR` field" | `@HD`, `@SQ`, `SN`, `LN`, `M5`, and `UR` are six codes in one sentence, none explained. | Name only the fields the reader must check and drop the rest. |
| The sequence dictionary, "a `.dict` of 249 or 252 bytes for this same reference is the same dictionary written somewhere else" | I had to read this twice. The point is that the size may differ and that is fine. | Say the exact size varies and does not matter. |
| The sequence dictionary, "LGE never creates this file for you, on either route" | "Either route" assumes I know the two routes are the app and the command line. Not stated before. | Name the two routes. |
| The sequence dictionary, "the Call Variants dialog, which takes its reference from the bundle, and a bundle carries no `.dict` either" | "Bundle" is used three times with no explanation and no glossary link. | Gloss bundle at first use. |
| The known-sites files, "bgzip-compressed VCF files" | Bgzip is not explained. I know gzip from downloading files, not bgzip. | Gloss bgzip in one clause. |
| The known-sites files, "the curated Mills and 1000 Genomes indel set" | I do not know what Mills is or why it is curated. It reads like a name I should already know. | Say it is a hand-checked list of known insertions and deletions. |
| The known-sites files, "roughly 1.5 GB compressed and the Mills indel file to around 20 MB, so plan the dbSNP download rather than starting it casually" | "Rather than starting it casually" is an idiom. Also I cannot judge 1.5 GB without knowing how long it takes. | Say it can take a long time on a slow connection. |
| The known-sites files, "the failure is the most useful thing in this chapter" | I read this twice. It sounds like the chapter is not useful. I understand now it means the error teaches something. | State plainly that this error is common and worth learning. |
| The known-sites files, "961 records on the sliced contig" | "Sliced contig" and "slice" are used throughout with no explanation. I inferred it means a piece of chromosome 20, but it is never said. | Explain at first use that the fixture is a small cut-out region. |
| The known-sites files, "The fix rewrites the VCF header from the reference index" | I do not know what a VCF header is or that it can be rewritten separately from the data. | Say the header is the description block at the top of the file. |
| The known-sites files, the `bcftools reheader` code block | The command uses `~/.lungfish/conda/envs/bcftools/bin/bcftools` but the chapter never said the bcftools pack is installed. Only samtools and gatk-core were mentioned. I could not perform this step. | Say which pack provides bcftools and whether I already have it. |
| The known-sites files, "38,323 bytes with a single contig line of the right length, all 961 records intact, and a 392 byte `.tbi` index" | Four numbers in one sentence. I cannot check any of them from the app, and the chapter does not say how to check them. | Say which one to check and how. |
| The known-sites files, "Splitting them into two files with `bcftools view -v indels` mirrors how dbSNP and Mills are used" | This is described but not shown as a step I can run. The command is incomplete, with no input or output file. | Either give the full command or say this is optional and not needed here. |
| The known-sites files, "since the option repeats" | I do not know what a repeating option is. Do I type `--known-sites` twice? | Show the flag written twice in an example. |
| The known-sites files, "Support for unindexed block-compressed files has been temporarily disabled" | "Block-compressed" appears only in the error, never in the prose, so I could not connect it to bgzip. | Say in the prose that bgzip is block compression. |
| The interval list, "where the start counts from zero and the end is not included" | This is the hardest sentence in the chapter for me. I do not know what it means for an end to be "not included" and no example shows the difference. | Add one example, 0 to 100000 covers bases 1 through 100000. |
| The interval list, "the first 100 kilobases" | Kilobases is not glossed. I know it, but it is the only unit in the chapter not spelled out. | Spell out once. |
| The interval list, "Exome and targeted-panel experiments" | Exome and targeted panel are both unexplained. I know exome from class, not targeted panel. | Gloss targeted panel in one clause. |
| The interval list, "turns a long job into a quick one while you are still working out whether the command is right" | Long sentence, and "working out" is an idiom. | Split it. |
| The interval list, "LGE applies `--intervals` to both commands in a BQSR run, not just the first" | "Both commands" is only explained much later, in the On the command line section. Here I did not know BQSR was two commands. | Say earlier that BQSR runs two GATK tools. |
| What good looks like, "check four things" then five items in the paragraph | I counted the `.fai`, the `.dict`, the matching length, the `.tbi`, the VCF header length, and the read group, which is more than four. I had to reread to try to group them. | Make the list a list, or fix the count. |
| What good looks like, "which `bcftools view -h` will show you" | Another command with no input file and no explanation of where to type it. I could not perform this check. | Give the full command with a file name. |
| What good looks like, "the `@RG` header block naming the sample, since GATK groups reads by it" | I do not know how to check whether my BAM has a read group, and no command is given. | Give the command that shows it. |
| What good looks like, "a 1,191,386 byte recalibration table, a 16,929,652 byte recalibrated BAM" | Byte counts to the digit that I cannot possibly match or judge. They stopped me because I assumed I must match them. | Say approximate sizes and that exact bytes will differ. |
| What good looks like, "two steps of 3.78 and 2.58 seconds" | I cannot judge these. Is my run broken if it takes 30 seconds? | Say roughly how long to expect and that slower is normal. |
| What good looks like, "its first block lists every argument the run used, which is the fastest way to confirm the known-sites files were actually read" | I do not know how to open this file or what "block" means here. | Say to open it in a text editor and what line to look at. |
| What good looks like, "873 SNVs of which 805 were already catalogued, a 92.2 percent overlap" | I cannot judge 92.2 percent. Is high good or is high suspicious? | Say what a good overlap looks like for a real human sample. |
| What good looks like, "transition to transversion ratio of 2.25 ... and 1.34 among the 68 novel ones" | Transition and transversion are not explained and are not in `glossary_refs`. I know them from class but the manual glosses easier words. | Gloss both in one clause. |
| What good looks like, "sequencing errors have no such preference and sit near 0.5" | "Have no such preference" and "sit near" are both figures of speech, and the jump from 2.0 to 0.5 has no explanation of why random errors give 0.5. | Explain in one clause why random errors land at 0.5. |
| On the command line, "GATK=~/.lungfish/conda/envs/gatk-core/bin/gatk" | I do not know what this line does. It looks like an equation, not a command. Nothing explains variables. | Add one comment line saying these three lines just save typing. |
| On the command line, "Step 4, read the two commands before running anything." | The step numbers 4 and 5 run the identical command twice with one flag difference. I first thought this was a mistake in the manual. | Say explicitly that step 5 is step 4 plus `--execute`. |
| On the command line, "HG002.sorted.bam" | This file name appears only here. Earlier it was `expected/mapping/HG002.sorted.bam`. I do not know if I must move or rename it. | Use the same path in both places. |
| On the command line, "`--create-output-bam-index true` on the second is the default, which you turn off with `--create-output-bam-index false` when you plan to index the result yourself" | Very long sentence with two opposite flag values. I read it three times. | Split into two sentences. |
| On the command line, "That differs from `gatk joint-genotype`, where `--extra-args` lands only on the final step, so check the preview rather than assuming." | "The preview" was never named. I think it means the printed commands from step 4, but that word was not used for it. | Use the same word for the step 4 output throughout. |
| On the command line, "For scripted installs on a machine with no display" | "Scripted installs" and "no display" are unexplained. I do not know if this applies to me. | Say this is for servers you connect to remotely and can be skipped otherwise. |
| On the command line, "Move it to the target machine and install it with `conda offline-install`" | No full command is given and no explanation of how to move it. I could not perform this. | Give the full command or link to a chapter that does. |
| Next, "Both need the same `.fai` and `.dict` this chapter built" | The chapter built four things by my count, and the known-sites file was called the most important. Ending on only two of them confused me about what to keep. | Name all the files the next chapters need. |

One thing I learned. Known sites exist because BQSR must call every mismatch an
error, and real human variants would be counted as errors unless you exclude
them first.

One thing I still could not do. Run any of the commands, because the chapter
never shows how to open a terminal, where to put the downloaded files, or how
to move into that folder.

The sentence I liked most. "That assumption is false at exactly the positions
where the person genuinely carries a different base, and a human genome carries
several million of them."
