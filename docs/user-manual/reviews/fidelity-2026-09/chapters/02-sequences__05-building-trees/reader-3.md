# Reader report: Building Trees

Reader 3. Pre-med student, English is my second language, never opened a terminal.

| Location | What stopped me | What would have helped |
| --- | --- | --- |
| What it is / "Every place two branches meet" | "stands for an ancestor that no longer exists and was never sequenced" made me read twice. I thought a node was a real animal I could look up. | Say plainly that the node is a calculated guess, not a sample. |
| What it is / "The pattern of who joins whom" | "the tree's main claim" is a figure of speech I did not follow. A tree does not claim. | Say the topology is the main result. |
| What it is / "On the default drawing, the horizontal" | "substitutions per site" is used as if I know it. I know a substitution is one base changing, but "per site" as a unit I could not picture. | One sentence saying 0.06 means about 6 changes per 100 bases. |
| What it is / "Maximum likelihood asks, of all" | "which one makes the observed columns most probable" was the hardest sentence in the chapter. I read it three times and I am still not sure what is being compared to what. | Break it into two shorter sentences. |
| What it is / "That assumed model is the substitution" | "a set of rates for turning one base into another" is clear, but why a wrong set of rates would matter is never said. | Say what goes wrong if the model is wrong. |
| Why you would do this / "The known primate relationships make this" | "you can grade the answer" is idiom. I guessed it means check the answer. | Use "check" instead of "grade". |
| Why you would do this / "The topology comes out looking equally crisp" | "crisp" is not a word I associate with diagrams. | Say the picture looks equally confident either way. |
| Before you start / "IQ-TREE arrives in the phylogenetics" | I do not know what an "environment" is when the software installs into "its own environment". | Say it installs privately and does not touch other software. |
| Before you start / "Install it from Tools > Plugin Manager..." | I could not perform this. The Plugin Manager opens but the text does not say what to click there, or how I know the pack finished. | Name the pack row and the button, and say what shows when it is done. |
| Procedure step 1 / "Open the .lungfishmsa bundle so" | Open from where. The sidebar, a menu, or double-click. Not stated. | Say double-click it in the sidebar. |
| Procedure step 2 / "Click the first sequence name in" | "name gutter" is a term I have never seen and it is not glossed. | Gloss "gutter" as the strip of names on the left. |
| Procedure step 3 / "Its Inputs section carries a Scope" | "the rows and columns your selection carried in" is confusing word order and I could not tell what number I should expect to see. | Say it should read 5 rows, and give the column count. |
| Procedure step 4 / "Leave Model on MFP, which asks" | I do not know how long the model testing takes, so I did not know if the app had frozen. | Say model testing is included in the under-two-minute estimate. |
| Procedure step 5 / "leave its Replicates field on 1000" | A number I cannot judge. Is 1000 a lot. What would 100 cost me. | Say 1000 is the standard and lower values are less steady. |
| Procedure / "The new bundle appears in the sidebar" | I could not tell how long to wait before worrying. No progress indication is described. | Say where progress shows while it runs. |
| Import a tree built elsewhere / "It reads Newick and Nexus, which" | RAxML-NG and FastTree are named with no explanation. I do not know these. | Say they are other tree programs. |
| Settings / "The Phylogenetic Tree Operations dialog holds thirteen" | I counted the bold entries and got thirteen, but the Re-root and Extract subtree settings that follow sit under the same "Settings" heading, so at first I thought they were in the dialog too. | Say the later groups belong to different menu items. |
| Settings / Sequence Type / "or pick NT to AA when" | "NT to AA" and the list auto, DNA, AA, CODON, BIN, MORPH, NT2AA are given with no glossing. I know AA is amino acid only because of biochemistry class. | Gloss NT2AA once. |
| Settings / Sequence Type / "Set it by hand when the alignment" | "codon alignment" is used but not glossed and is not in the glossary list at the top. | Gloss codon alignment. |
| Settings / Ultrafast Bootstrap / "a procedure that rebuilds the tree many" | "shuffled versions of your columns" made me think the order is scrambled, which sounded wrong to me. | Say columns are drawn again with replacement, in plain words. |
| Settings / SH-aLRT / "compares each branch against the alternatives immediately" | I could not tell when I would want this instead of the bootstrap, only that it is different. | Say which one to use if choosing only one. |
| Settings / Seed / "The default is 1, which means" | "rather than a near miss" is idiom I had to reread. | Say the result is identical, not just similar. |
| Settings / Threads / "Sets how many processor cores" | I do not know how many cores my Mac has or how to find out. | Say leaving it blank is right for almost everyone. |
| Settings / Safe numerical mode / "Turn it on after a run stops" | "a likelihood error" is not a message I would recognise. | Quote the error text I would see. |
| Settings / IQ-TREE Executable / "Points the run at a specific" | I do not know what a binary is or where one would be. | Say most readers never touch this. |
| Settings / IQ-TREE Parameters / "such as a partition file or a topology" | Both terms are new and neither is glossed. | Drop the example or gloss it. |
| Settings / Re-root / "Re-rooting shows no dialog. It runs" | I did not know where to right-click to get this, because the procedure never had me re-root anything. | Add one numbered step showing the right-click. |
| Settings / Node to root on / "or a normalized node ID" | "normalized node ID" appears here for the first time with no explanation. | Gloss it here, not forty lines later. |
| Settings / Output bundle name (re-root) / "so the number after -rerooted grows" | No number was mentioned before, so "the number after" refers to something I never saw. | Show an example name. |
| Settings / Extract subtree / "Neither of the two subtree items shows" | "the two subtree items" is the first mention of there being two. | Name both items in this sentence. |
| Reading the results / "It gives the bundle name, then the" | I could not tell whether the wide spaces in the example summary line are meaningful or a mistake. | Say the app separates the parts with spacing. |
| Reading the results / "A second segmented control switches branch colouring" | "segmented control" is a Mac interface term I do not know. | Say it is a small two-part or three-part button. |
| Reading the results / "and it stays disabled when the bundle" | I did not know a bundle could contain a metadata.tsv until much later in the chapter, so this sentence was meaningless where it sits. | Point forward to the relabelling section. |
| Reading the results / "A node with a branch adds Branch Length" | "A node with a branch" sounds like every node has a branch. I could not tell which node does not. | Say the root has no branch. |
| The numbers on this tree / "A tree of five tips can hold" | I could not verify this rule and I do not know where three comes from. | Give the rule, tips minus two. |
| The numbers on this tree / "Fewer than three would mean the alignment" | "collapsed" is used in a new sense here, different from Collapse Clade later. | Use a different word for one of them. |
| The numbers on this tree / "(Human_NC_012920.1:0.0601,Chimp_NC_001643.1:0.0589,(Gorilla" | I cannot read this string at all, and the text does not walk me through even one bracket. | One sentence saying brackets group the sequences that join. |
| The numbers on this tree / "The two macaques join each other first" | This contradicts the Why section, which said the human and the chimpanzee should be closer to each other than either is to the gorilla and all three apes apart from the monkeys. Here the gorilla joins the macaques. I could not tell if the fixture is wrong or if I misread. | Say directly whether this fixture tree matches the expected relationships. |
| The numbers on this tree / "which is what tens of millions of" | The number 0.8982 is far larger than every other and I have no scale to judge whether that is normal. | Say what range is typical. |
| The numbers on this tree / "Select the cynomolgus macaque tip and the" | Cumulative divergence of 0.958 is called far, human 0.060 is called close, but far from what point is never fixed since the tree is unrooted. | Say the reference point is the drawing's left edge. |
| Support values and rooting / "A support value at or above 95" | Two thresholds are given, 95 and 70, and nothing about the gap between them. | Say what 70 to 95 means. |
| Support values and rooting / "The apparent root at the left edge" | This is the only place that told me the left edge is not real, and it comes after the whole numbers section used that edge implicitly. | Move this warning earlier. |
| Acting on a node / "Right-click a tip or an internal node" | Ten items are promised. I counted nine named in bold across the paragraphs. | Number the items or match the count. |
| Acting on a node / "Reveal Provenance work on any selected node" | Provenance is linked but never explained in this chapter, and I did not know what revealing it shows me. | One clause saying it shows the record of how the file was made. |
| Relabelling tips from metadata / "add a tab-separated metadata.tsv file at" | I could not perform this. I do not know how to see inside a bundle in Finder, and a tab-separated file is hard to make by hand. | Say how to open the bundle folder. |
| Relabelling tips from metadata / "use tree relabel on the command line" | I have never opened a terminal, so this is not available to me and no other route is offered. | Say plainly there is no in-app way to make it permanent. |
| Relabelling tips from metadata / "the relabelled Newick reads (Human:0.0601" | The dots inside the string made me unsure whether the real output contains them. | Say the dots stand for omitted text. |
| What good looks like / "A missing tip usually means IQ-TREE dropped" | The chapter never told me a drop would be reported anywhere, so I would only find out by counting. | Say whether a message appears. |
| What good looks like / "meaning under a few hundred informative columns" | "informative columns" is new here and is not the same as alignment columns. | Gloss informative column. |
| What good looks like / "If IQ-TREE reports that ModelFinder settled on" | ModelFinder appears for the first time here. Earlier MFP was described but never named ModelFinder. | Connect MFP to ModelFinder at first use. |
| On the command line / "The block below reproduces the whole chapter" | I cannot use any of this and it is a large part of the page, but nothing tells me I may skip it. | One line saying this section is optional. |
| On the command line / "--node node-a4609f19185ceddd" | Where this ID comes from is only explained two paragraphs later. | Point to the explanation at the code block. |

I learned that a tree drawn without support values looks exactly as confident as one with them, so the bootstrap tick box is the part that decides whether the picture can be believed.

I still could not relabel the tips, because the only permanent route is the command line and I have never used one.

The sentence I liked most is "A tree that says otherwise is reporting a problem with your run rather than news about primates."
