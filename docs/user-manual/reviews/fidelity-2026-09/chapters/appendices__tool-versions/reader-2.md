# Reader 2 report, appendices/tool-versions

Persona. Senior undergraduate, two years of wet lab, library prep and PCR
fluent, no data analysis, never opened a terminal. Reading this to put one
tool version into a methods section.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "A mapping run is minimap2, a variant call is GATK or LoFreq" | Four program names arrive in one sentence with nothing said about what any of them does. I know what mapping and variant calling mean from lecture, but I cannot tell which of these four names I would ever need. | Add one clause saying these are the names you will see in the tables below. |
| What it is, "Every number on this page is read out of one file" | The claim that nothing is typed by hand is reassuring for the writer, not for me. I did not doubt it, and I do not know what to do with it. | Cut the sentence "Nothing in the tables below is typed by hand." |
| What it is, "The file lives inside the app at `Contents/Resources/...`" | Two file paths given and no way for me to reach either one. I do not know how to see inside an app, and the second path is a "source tree" I have never heard of. | Say plainly that you do not need to open this file, the tables are its contents. |
| What it is, "This appendix reflects dependency set `2026.2`, cut on 2026-08-18, as shipped by release 2026.9.13." | Three numbers in one sentence and I could not tell which is my app version. I read it twice and still had to go to the command section to learn that 2026.9.13 is the app. | Label them, for example "app release 2026.9.13" on first use. |
| What it is, "Name the dependency set beside the app version in a methods section" | This is the one instruction I actually need and it does not show me the result. I do not know whether to write "2026.2" or "dependency set 2026.2" or both numbers. | Give one example sentence of what to paste into a methods section. |
| What it is, "read the **provenance sidecar** of that analysis instead" | A sidecar is named as the thing I should actually use, and I am never told where it is or how to open it. This is the second paragraph telling me this page is not what I want. | Add "see [chapter]" or one sentence saying where the sidecar file appears. |
| Reading the tables, "Four ideas separate the four tables" | The four ideas are conda, plugin pack, license, executables. Those are four columns, not four tables. I looked for four ideas that matched four tables and could not line them up. | Say "Four columns need a word of explanation." |
| Reading the tables, "These folders live under `~/.lungfish/conda`." | The tilde. I have never used a terminal and do not know that `~` means my home folder. | Gloss `~` as your home folder at first use. |
| Reading the tables, "Installing the `assembly` pack brings in five assemblers at once." | Assembler is used without a gloss, and this is a term I have only half heard. My persona has never analyzed data. | Gloss assembler in one clause. |
| Reading the tables, "so read the tool's own license text before you redistribute anything" | I have no idea whether writing a paper using a tool counts as redistributing it. This sentence made me worry about something I probably do not have to worry about. | Say that using a tool for your own analysis and citing it is not redistribution. |
| Reading the tables, "IQ-TREE's executable is `iqtree3`, and Clair3's is `run_clair3.sh`" | Two tool names I do not know, used as examples of a thing I do not yet know I need. The point about executables differing from display names is fine, the examples are opaque. | Use one example whose display name I would recognize, such as SAMtools. |
| Tools installed with every copy, "Seventeen of them are conda environments LGE provisions on first use." | "Provisions" is a word I had to reread. I could not tell if something happens automatically or if I have to do it. | Replace with "sets up automatically the first time a tool is needed". |
| Tools installed with every copy, "the lock records it under a separate `bootstrap` key that carries no license field" | Key, field, bootstrap. Three file-format words in one clause, none glossed, and the sentence is already long. This is the longest sentence to get through in the chapter. | Split it and drop `bootstrap` key, or gloss key as a labeled entry in the file. |
| Tools installed with every copy, table row "pysam ... `python`" | The Executables column says `python` for two different rows, which contradicts the earlier promise that this column names the program the environment provides. I stopped to check whether it was a mistake. | Add a note that library rows list the interpreter that runs them. |
| Tools installed with every copy, "bedGraphToBigWig counts by build number rather than by a dotted release" | Build number and dotted release are both unexplained. I understood the practical point, copy `482` whole, but only after two passes. | Say "its version is a plain count, not a 1.2.3 style number". |
| Tools installed with every copy, "the packaging revision of that build" | Same problem, and this one is the number I am most likely to be typing into a methods section, so a wrong guess costs me. | Say "part of the version, copy it including the `-0`". |
| Tools installed by a plugin pack, "a fresh copy of LGE cannot map a read until `read-mapping` is installed" | This is real news to me and it lands halfway down a paragraph as a subordinate clause. I would have wanted this before I read the first table. | Lead the section with it as its own sentence. |
| Tools installed by a plugin pack, "or on the command line with `lungfish-cli conda install --pack read-mapping`" | The command line arrives 90 lines before the section that explains that `lungfish-cli` is not something I can just type. I tried to picture doing this and could not. | Point to the command line section, or drop the command here. |
| Tools installed by a plugin pack, "Cmd-Shift-B" | I do not know what Plugin Manager is or whether installing a pack downloads gigabytes, takes minutes, or needs the internet. Nothing here tells me the cost. | Add one sentence on what installing a pack does and how long it takes. |
| Tools installed by a plugin pack, "The ten pack IDs are ..." | The list of ten IDs repeats the Pack column of the table immediately below it. I read the list, then read the same words again in the table. | Cut the sentence, the table already carries them. |
| Tools installed by a plugin pack, "a provenance record naming either environment is naming the pack" | I still do not know what a provenance record looks like, so I cannot use this. It is the third mention of provenance with no example. | Show two lines of a provenance record once, anywhere in the chapter. |
| Pinned external pipelines, "It fetches each from its public repository at one exact revision and runs it through Nextflow." | Repository, revision, and Nextflow all in one sentence, none glossed. Nextflow appears in the first table as a tool, so I could not tell if it is a program I have or a service somewhere. | Gloss repository and revision, and say Nextflow is the workflow runner from the first table. |
| Pinned external pipelines, "`e10bfebda32a62711f38a4e23ab03b61725a9675`" | I do not know what this string is until the paragraph after the table calls it a commit identifier. Seeing it first was alarming and I assumed it was an error code. | Move the explanation before the table. |
| Pinned external pipelines, "Quote the commit for TaxTriage and the tag for Viral Recon" | Quote where. I assume in my methods section but the sentence does not say, and "quote" could also mean put in quotation marks. | Say "write in your methods section". |
| Pinned external pipelines, "Override the TaxTriage revision by adding `--revision <ref>`" | Overriding a pinned version sounds like the opposite of everything the chapter told me to do, and no reason is given for why I would. The angle brackets are also unexplained. | Say who would need this and why, or move it out of a reference table chapter. |
| Reference databases, "The lock pins sixteen." | I counted the rows to check, because the sentence gives me a number I cannot use for anything. | Cut, or say why sixteen matters. |
| Reference databases, "Source policy" values `unpinnedArchive`, `liveSnapshot`, `localBuild`, `bundledPayload` | These four run-together words appear in the table before the paragraph that explains three of them, and `liveSnapshot` is never explained in that paragraph at all. I had to jump ahead and then back. | Explain all four before the table, including `liveSnapshot`. |
| Reference databases, "so the archive is trusted to keep its contents" | I could not tell whether this is a warning or a reassurance. "Trusted to" reads as an admission that nobody checks. | Say directly whether I should worry about it. |
| Reference databases, "`kraken2-standard-8` ... `PlusPF-16`" | Nine Kraken 2 rows with names I cannot tell apart, and nothing says what 8 and 16 mean or how I would know which one my run used. | Add one sentence saying the number is the memory size in gigabytes. |
| Reference databases, "the same read can be assigned a different species name in a run made a month later" | This is the single most important sentence for me and it is buried three quarters of the way down a reference chapter. It changes what I have to write in a methods section. | Promote it, or repeat it in "What good looks like". |
| Reference databases, "the lock also lists under a separate `managedData` key" | Another internal file key. I cannot see the file and this tells me nothing I can act on. | Cut, or move to a note for developers. |
| What good looks like, "Run `lungfish-cli tools update --plan`" | This is the second command given before I am told I cannot type `lungfish-cli`. I would have tried it, failed, and assumed the app was broken. | Move the command line section before this one. |
| What good looks like, "It exits with status 10 when work is pending, so a script can test for that." | Exit status and script are both outside anything I have done. I do not know if this sentence is for me. | Mark it as being for people writing scripts. |
| What good looks like, "An exit status of 0 means the machine matches the lock." | Same. Also, I would not know where to look to see an exit status. | Say what I see on screen when everything matches. |
| What good looks like, "a `retiredEnvironments` list ... In dependency set 2026.2 that list is empty" | A whole paragraph about a list that is empty. Nothing for me to do. | Cut for this release. |
| On the command line, "`lungfish-cli` is the command-line copy of LGE" | This is the first sentence in the chapter that acknowledges the terminal exists, and it comes after three commands have already been given. | Move this section earlier, or forward-reference it at the first command. |
| On the command line, "Installed releases do not put it on your `PATH`, the list of places your shell searches for programs" | Shell is not glossed, PATH is glossed inside the same clause, and the sentence then offers two alternatives and a cross reference. I read it three times and still do not know how to open a Terminal in the first place. | Split into short sentences and say where to find Terminal. |
| On the command line, "run first the one-line `export PATH` command" | Word order made me stumble, and "the one-line export PATH command" names a command whose text is not shown here. | Reorder to "or first run the one-line command that the CLI Reference gives". |
| On the command line, "The example below writes the bare name." | After a paragraph telling me the bare name will not work, the example uses the bare name anyway. That felt like a trap. | Show the full quoted path in the example instead. |
| On the command line, code block "BBTools 40.02 managed bbtools clumpify.sh, bbduk.sh, ..." | The `...` is in the real output? Or is it the manual trimming the line? I could not tell. | Say the line is truncated here. |
| On the command line, "checked row by row against the lock on 2026-09-07" | A date stamp about the writing process. It made me wonder if the numbers go stale, which is a worry the chapter did not otherwise raise. | Cut, or move to the front matter. |
| Which one governs, "A machine can hold an older installed version than the lock names" | This finally tells me the tables might not match my machine, four sections after I was told to copy numbers out of them. | Move this warning to "What it is". |
| Which one governs, "Use your run's provenance sidecar for a methods section describing a finished analysis" | The whole chapter ends by telling me that for my actual use case, this page is the wrong page, and it still never tells me where the sidecar is. | Give the sidecar's location or a link, once. |
| Whole chapter | I never learn how to look up one tool version without reading the tables by eye. There is no "find your tool here" entry point and no search or index. | Add one sentence at the top saying which table to look in for which kind of tool. |

The one thing I learned. The database matters as much as the classifier,
and the NCBI taxonomy is not pinned at all, so the date I ran something is
part of my methods.

The one thing I still could not do. Write a version into a methods section
with confidence, because the chapter tells me four times that the
provenance sidecar is the right source and never once tells me where it is.

The sentence I liked most. "Almost none of the analysis in Lungfish Genome
Explorer (LGE) is done by LGE itself."
