# Reader 1 report, appendices/tool-versions

Persona: sophomore who just finished introductory genetics, has never opened a terminal, has never used sequencing software. Reading to find the version of one tool for a lab report.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "a variant call is GATK or LoFreq" | Four program names arrive in one sentence and none is explained. I know what a mutation is from genetics but not what "a mapping run" or "a variant call" is as a thing a program does. | Add one clause per name saying what job it does, such as "minimap2, which lines reads up against a reference". |
| What it is, "Almost none of the analysis" | Had to read twice. The first half says LGE does not do the analysis and the second half says LGE decides and hands and reads, so I could not tell what LGE actually is until the third sentence. | Lead with what LGE does, then say it calls outside programs to do the computing. |
| What it is, "The file lives inside the app at `Contents/Resources/...`" | I cannot perform this. I do not know how to look inside an app, and nothing tells me to right-click and choose Show Package Contents. | Say the reader does not need to open this file, or give the exact Finder step. |
| What it is, "and in the source tree at `Sources/LungfishWorkflow/...`" | "Source tree" is never explained and I have no source tree. I could not tell whether this path was for me. | Mark the second path as for developers only. |
| What it is, "as shipped by release 2026.9.13" | I do not know which release I have or how to find out. The chapter tells me to check this only much later. | Point forward to the version command here at first mention. |
| What it is, "Name the dependency set beside the app version in a methods section" | This is the sentence I most needed and I could not act on it. It does not show me what to write. I do not know if I type "2026.2" or "dependency set 2026.2" or both numbers. | Give one example line of methods text with both values filled in. |
| What it is, "read the **provenance sidecar** of that analysis" | Glossary link given but no gloss in the sentence. I do not know what a sidecar is, where it lives, or how to open one, and this is presented as the thing I should do instead. | Gloss it in one clause and say which chapter shows how to open one. |
| Reading the tables, "Four ideas separate the four tables" | Read twice. The four ideas are conda, plugin pack, License, Executables, but those are columns, not the thing that separates the tables. The plugin pack paragraph is the only one that actually separates two tables. | Say "four columns need a note each" instead. |
| Reading the tables, "These folders live under `~/.lungfish/conda`" | The tilde is never explained. I have never seen a path written this way and would not find it in Finder. | Say the tilde means the home folder, and that the folder is hidden. |
| Reading the tables, "Installing the `assembly` pack brings in five assemblers at once" | "Assembler" is used here for the first time with no gloss. I know genome assembly loosely from lecture but not what a program called an assembler produces. | Gloss assembler in the clause. |
| Reading the tables, "IQ-TREE's executable is `iqtree3`" | "Executable" is used as the column name and in the paragraph, but is only described as "the program files the environment provides", which does not tell me what a program file is or why it has a different name. | Say an executable is the name you type to run the tool. |
| Every-copy table, "These eighteen are present from the first launch" | The next sentence says seventeen are provisioned on first use, which contradicts "present from the first launch" as I read it. I could not tell whether the tools are on my machine now or not. | Pick one framing, such as "these eighteen come with every copy and install themselves the first time you use them". |
| Every-copy table, "`bootstrap` key that carries no license field" | "Key" is jargon from the file format that I have no way to interpret. This is the only place a lock file key is named in this section. | Drop the key name or say it is a section of the file. |
| Every-copy table, header row "Environment" | The column tells me a folder name but never why I would want it. I only learn its use much later under provenance records. | Say in the Reading the tables note what a reader would ever do with an environment name. |
| Every-copy table, "pysam | 0.24.0 | ... | `python`" | Two rows list the executable as `python`, which contradicts the earlier rule that the Executables column names the program the environment provides. I read the table twice thinking I had misread a row. | Note that library rows run through Python rather than their own command. |
| Every-copy table, "Two of those version numbers look wrong at a glance" | I could not tell whether I write `482` or `v482` or `bedGraphToBigWig 482` in a lab report, which is exactly my task. | Show one finished citation string. |
| Plugin pack table, "Install a pack from the Plugin Manager at **Tools > Plugin Manager...** (Cmd-Shift-B)" | This is the only GUI step in the chapter and it stops at opening the window. I do not know what I see next or how to pick a pack. | Add the one step that follows, or link to the plugin packs chapter. |
| Plugin pack table, "`lungfish-cli conda install --pack read-mapping`" | A command line appears roughly 90 lines before the section that explains I cannot type `lungfish-cli` as written. I would have typed it and had it fail. | Move the PATH caveat forward, or footnote this first command. |
| Plugin pack table, "so a fresh copy of LGE cannot map a read until `read-mapping` is installed" | I do not know how to tell which packs I have installed. The chapter never says. | Name the command or screen that lists installed packs. |
| Plugin pack table, License column values | Values such as `GPL-3.0-or-later OR BSD-2-Clause` and `Varies, see https://...` are unexplained strings. The text says a few are more restrictive but does not say which, so I cannot judge any of them. | Say plainly that a reader writing a paper does not need this column. |
| Pinned external pipelines, "It fetches each from its public repository at one exact revision and runs it through Nextflow" | Three unglossed ideas in one sentence, repository, revision, and Nextflow. Nextflow appears in the first table as a tool but is never explained. | Gloss Nextflow once as the program that runs multi-step pipelines. |
| Pinned external pipelines, "a forty-character commit identifier" | "Commit" is never explained. I cannot tell why a forty-character string is safer than `3.0.0`, even though the paragraph says the difference matters. | Gloss a commit as an unchangeable snapshot label. |
| Pinned external pipelines, "Override the TaxTriage revision by adding `--revision <ref>`" | I do not know what a `<ref>` is or where I would get one, and I would not know when overriding is appropriate. | Say most readers should not override this. |
| Reference databases, "The lock pins sixteen" | I counted the rows and got sixteen, but two of them are later described as reference data rather than databases, so I was unsure whether sixteen was the right number to quote. | Say sixteen rows, two of which are reference data. |
| Reference databases, "Source policy" values | `unpinnedArchive`, `liveSnapshot`, `localBuild`, and `bundledPayload` are run-together words I have never seen. They are explained a paragraph later, but `liveSnapshot` is explained nowhere. | Explain all four, in the order the table lists them. |
| Reference databases, "Standard-8" and "PlusPF-16" | Nine Kraken 2 database rows differ only by suffix and nothing says what the numbers or the letters mean. If my lab used one of these I would not know which row is mine. | One sentence saying the number is a memory size in gigabytes. |
| Reference databases, "the archive is trusted to keep its contents" | Had to read twice. I could not tell whether this is reassurance or a warning. | Say plainly that nothing verifies the archive did not change. |
| Reference databases, "Deacon uses them to recognise and discard host reads" | "Host reads" is unglossed, and this is the only place in the chapter where a biological concept appears. | Gloss host reads as reads from the person or animal rather than the target. |
| What good looks like, "Run `lungfish-cli tools update --plan`" | Again given before the section explaining that this name does not work as typed. I also do not know where to type it, since nothing has told me to open Terminal. | Say to open Terminal, and cross-reference the PATH note. |
| What good looks like, "It exits with status 10 when work is pending, so a script can test for that" | "Exit status" and "script" are developer ideas that mean nothing to me, and I could not tell whether I am supposed to do something with the 10. | Say a reader can ignore the status number and read the printed list. |
| What good looks like, "the `retiredEnvironments` list" | This paragraph tells me a list is empty and this release retires nothing. I could not work out why I was told this or what I would do if it were not empty. | Cut, or say why an upgrading reader would care. |
| On the command line, "Installed releases do not put it on your `PATH`, the list of places your shell searches" | One sentence carries PATH, shell, quoted full path, `export PATH`, and Terminal session. I read it three times and still could not perform it. I do not know how to open Terminal. | Split into numbered steps starting with opening Terminal. |
| On the command line, "so either type the quoted full path ... or run first the one-line `export PATH` command" | Two alternatives are offered and then the example uses neither, since it shows the bare name. I could not tell which one I am meant to do. | Show the example with the full quoted path, which needs no setup. |
| On the command line, "Its first lines on release 2026.9.13 are these" | The sample output stops after three tools with no ellipsis, so I could not tell whether my output should have more. | Add a trailing line showing the output continues. |
| On the command line, "checked row by row against the lock on 2026-09-07" | This paragraph is about how the manual was verified, not about anything I do. It interrupted my search for a version number. | Move the verification note to a footnote. |
| On the command line, "The command sorts alphabetically by display name while this appendix keeps the lock's own order" | Directly relevant to my task and buried in the longest paragraph in the chapter. The consequence, that the same tool sits in different places in the two lists, is left for me to infer. | State the consequence first in its own sentence. |
| On the command line, "it writes display names such as `SAMtools` ... where the lock stores identifiers such as `samtools`" | I do not know which spelling belongs in a lab report. This is my actual question and the chapter never answers it. | Say which form to cite. |
| Which one governs, "A machine can hold an older installed version than the lock names" | I do not know how to find the installed version of one tool, only the pinned one. My whole task was the installed version. | Name the command or file that shows what is installed. |
| Which one governs, "the sidecar is right about your analysis" | The chapter has now told me three times to use the sidecar and never once shown me one or told me where it is. | Link the provenance chapter on first mention. |

Three closing lines.

The one thing I learned is that the numbers in a paper have to come from the run that happened, not from the release notes.

The one thing I still could not do is find the version of samtools that actually ran on my own laptop, which is the only reason I opened this page.

The sentence I liked most is "Nothing in the tables below is typed by hand."
