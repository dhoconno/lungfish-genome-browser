# Reader report: Tool Versions

Persona: a student who used Geneious in one class. I expect every operation to have a dialog. I have never opened a terminal.

| Location | What stopped me | What would have helped |
|---|---|---|
| Front matter, "CLI: lungfish-cli version --tools" | The only entry point listed is a command line. I own a mouse, not a terminal, and I could not tell from this whether the app has any window that shows me these numbers. | Say up front that the tables here are the window-free route and no dialog shows them. |
| What it is, "a variant call is GATK or LoFreq" | GATK and LoFreq are dropped in with no gloss. I only need one version number for my methods section and I already cannot tell which of these two I used. | Say that the chapter you ran tells you which caller was used. |
| What it is, "Every number on this page is read out of one file" | I was reading to copy one version into a methods section, and the first thing I got was a description of a file I will never open. | Put the "look up a number and move on" sentence first. |
| What it is, "The file lives inside the app at `Contents/Resources/...`" | I do not know how to look inside an app on a Mac, and nothing says I do not have to. | Add that you never need to open this file. |
| What it is, "This appendix reflects dependency set `2026.2`, cut on 2026-08-18" | "Cut on" is not a phrase I know in this sense. I read it twice before deciding it meant the date the list was fixed. | Use "fixed on" or "frozen on". |
| What it is, "Name the dependency set beside the app version in a methods section" | This is the sentence I actually needed and it does not show me what to write. I do not know the format. | Give one example sentence I can copy. |
| What it is, "read the provenance sidecar of that analysis instead" | The chapter tells me to use the sidecar instead of this page, but never says where the sidecar is or how to open it. This is the whole reason I am here. | Name the menu or the panel that shows a run's sidecar, or link the chapter that does. |
| Reading the tables, "Four ideas separate the four tables" | I could not tell which of the four tables my tool would be in, so I did not know where to start looking. | Say plainly that if a tool is not in the first table, look in the second. |
| Reading the tables, "kept apart from every other program" | The conda paragraph explains what an environment is but never says why I, looking up a version, need to care about the Environment column. | One clause saying you can ignore this column unless a provenance record names it. |
| Reading the tables, "These folders live under `~/.lungfish/conda`" | The tilde means nothing to me and I do not know how to reach a folder whose name starts with a dot. | Say you never need to open this folder. |
| Reading the tables, "read the tool's own license text before you redistribute anything" | I do not know what redistributing would mean for me as a student, so I could not tell if this warning applies to me. | Say this only matters if you ship the software to someone else. |
| Tools installed with every copy, "Seventeen of them are conda environments LGE provisions on first use" | "Provisions on first use" was read twice. I could not tell whether they are already there or arrive later, which matters because I want to know if I actually ran that version. | Say LGE installs each one the first time you use it. |
| Tools installed with every copy, "the lock records it under a separate `bootstrap` key that carries no license field" | This sentence is about the structure of a file I will never see, and it stopped me mid-table. | Move to a footnote, or cut. |
| First table, "| bcftools | 1.24 | `bcftools` | GPL |" | Every other license is a precise code like `GPL-3.0-only` and this one is a bare `GPL`. I could not tell if the table was wrong or if that is really all that is known. | Say the lock records some licenses loosely and this is copied as found. |
| First table, "| pysam | 0.24.0 | ... | `python` |" | The Executables column says `python` for two different rows, which contradicts the earlier claim that the column "names the program files the environment provides". | Say a library's environment provides only the interpreter. |
| Tools installed with every copy, "so copy it whole" | I was told to copy the version whole, but nothing on this page tells me where in my methods section any of this goes or in what form. | One worked example of a methods sentence. |
| Plugin pack section, "so a fresh copy of LGE cannot map a read until `read-mapping` is installed" | This warns me a tool may not be installed, but nothing here tells me how to check which packs I actually have. The table lists what could be installed, not what is. | Say the Plugin Manager window shows which packs are installed. |
| Plugin pack section, "Install a pack from the Plugin Manager at **Tools > Plugin Manager...** (Cmd-Shift-B)" | This is the only window route in the whole chapter and it is for installing, not for reading versions. I still cannot find out from a window what version I have. | State outright that no window lists tool versions and this page is the only source. |
| Plugin pack section, "The ten pack IDs are `read-mapping`, ..." | Ten ids are listed as bare text and then all ten reappear in the table's first column. I read the list, then found it duplicated, and wondered if I had missed a difference. | Cut the list since the table already carries it. |
| Second table, "| `metagenomics` | bracken | 1.0.0 |" | Bracken 1.0.0 next to Kraken 2.17.1 looked like a typo or a very old tool, and I have no way to judge whether that is right. | A note that a low number here is the real upstream version. |
| Pinned external pipelines, "runs it through Nextflow" | Nextflow appears in the first table with a version but is never explained, and here it is doing something to a pipeline I do not follow. | Gloss Nextflow in four words at first use. |
| Pinned external pipelines, "`e10bfebda32a62711f38a4e23ab03b61725a9675`" | I do not know what a commit identifier is, and putting forty characters into a methods section felt wrong. I could not tell if I am really supposed to type all of that. | Say a commit is a snapshot id and yes, quote it in full. |
| Pinned external pipelines, "Override the TaxTriage revision by adding `--revision <ref>`" | The GUI half of this sentence says "the version field of the Viral Recon wizard" but the TaxTriage half offers only a command. I have no dialog route for TaxTriage and the text does not say so. | Say the TaxTriage revision cannot be changed from a window. |
| Reference databases, "The Source policy column is the lock's own word" | "Source policy" is a term I have never met, and the explanation of the three values comes two tables later. | Move the three-way explanation above the table. |
| Reference databases, "| `kraken2-standard-8` | ... | Standard-8 |" | Nine Kraken 2 database rows differ only by a suffix, and nothing says how I know which one I used. | Say the run's results panel names the database. |
| Reference databases, "the archive is trusted to keep its contents" | I read this twice. It sounds like the archive is trustworthy and also like nobody has checked. | Say the server could in principle change the file without changing the version. |
| Reference databases, "which your provenance sidecar records for you" | Second time the sidecar is my answer and still nothing tells me how to open one. | Link the sidecar chapter here. |
| Reference databases, "the lock also lists under a separate `managedData` key" | Another sentence about the shape of a file I cannot open. | Cut. |
| What good looks like, "confirm your own copy reports this dependency set" | I am told to confirm my copy matches, but the confirmation is a terminal command in the next section. There is no window route offered and no note that one is missing. | Say whether the About window shows the dependency set. |
| What good looks like, "Run `lungfish-cli tools update --plan`" | I cannot run this. It is presented as one of two checks "worth running before you quote a number", so I now believe I cannot safely quote a number at all. | Say what a mouse-only user should do instead. |
| What good looks like, "It exits with status 10 when work is pending, so a script can test for that" | Exit status and scripts mean nothing to me and I could not tell if I was supposed to understand this. | Mark the sentence as for scripting only. |
| On the command line, "Installed releases do not put it on your `PATH`" | Every noun in this sentence is new, and the sentence is 70 words long with two branching options. I gave up. | Split into two sentences and gloss PATH separately. |
| On the command line, "run first the one-line `export PATH` command" | Word order made me stop. "Run first the command" is not how I would read it. | "First run the one-line command". |
| On the command line, "```bash lungfish-cli version --tools```" | This block is the chapter's only stated entry point and I cannot use it. Nothing says a GUI equivalent does not exist until much later. | Put the "no window equivalent" note beside the command, not two paragraphs down. |
| On the command line, "checked row by row against the lock on 2026-09-07" | A verification date for the authors, not for me. It made me wonder if numbers go stale between checks. | Move to a comment or the front matter. |
| On the command line, "so the other three tables here have no command-line equivalent" | This says three tables have no command-line route, but I needed the reverse fact, which is that no table has a window route. | State both directions in one sentence. |
| Which one governs, "A machine can hold an older installed version than the lock names" | This tells me the version I quote may be wrong, and the only way to find out is a terminal. I finished the chapter unable to do the one task it promised. | Say plainly that a mouse-only user must ask someone to run the check. |

I learned that the version I should quote is the one my run actually used, not the one in this table.

I still could not find any window in the app that tells me which version of one tool I ran.

The sentence I liked most was "What you should do with this page is look up a number and move on."
