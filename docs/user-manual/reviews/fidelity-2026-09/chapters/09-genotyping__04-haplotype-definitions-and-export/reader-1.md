# Reader report: Exporting Genotypes

Reader 1, undergraduate persona. Sophomore, one genetics course, no lab time, never used sequencing software, never opened a terminal.

| Location | What stopped me | What would have helped |
|---|---|---|
| Title vs filename, "Exporting Genotypes" | The file is called haplotype-definitions-and-export and the tags promise haplotypes, but haplotype definitions turn out to be a placeholder at the end. I kept reading for a section that never comes. | Say in the first paragraph that haplotype definitions are not covered yet. |
| What it is, "a folder carrying the `.lungfishgenotype` extension" | I did not know a folder could have an extension. I looked for a file and expected double-clicking it to open Excel. | One clause saying macOS shows it as a single item even though it is a folder. |
| What it is, "the read counts, an Excel workbook, the run statistics" | Four things are listed as being inside the bundle, and one of them is an Excel workbook, so I could not tell why I need to export at all. | Say here that the internal workbook is not the one you send out, and why. |
| What it is, "lays samples down the rows and loci across the columns" | Locus is used here but only glossed much later, in Step 2. I did not know what a locus was at this point. | Gloss locus at this first use instead. |
| What it is, "[allele targets] run down the rows" | Allele target is linked but not explained in the sentence. My class taught allele as a version of a gene, and I could not tell what a target adds. | One clause saying an allele target is one reference sequence the run tried to match reads against. |
| What it is, "which is the shape most downstream spreadsheets in a genotyping lab already expect" | I have never seen a genotyping lab spreadsheet, so this reason did not help me choose. | Say what the person receiving it does with it. |
| What it is, "which any script can read without a spreadsheet library" | I do not know what a spreadsheet library is or why a script would need one. | Cut, or say plainly that CSV is just text. |
| What it is, "one row per single fact rather than one row per sample" | I read this three times. I could not picture what one fact is. | Show two tiny tables, wide and long, side by side. |
| What it is, "So what should you do with this?" | The question is asked of me but the chapter has not yet told me who my reader is, so I had nothing to answer with. | Cut the question and keep the advice sentence. |
| Why you would do this, "The immunologist wants a spreadsheet" | Three job titles are used as shorthand for three formats, but I do not know which of those I am. | Add a clause saying which one applies if you are exporting for yourself. |
| Why you would do this, "filtering the pivot at a modest threshold removed 192 of 305 allele rows" | Modest is not a number, and I had no way to judge whether removing 192 of 305 is modest or drastic. | Give the two numbers here, the same 50 and 5 used later. |
| Why you would do this, "without touching a single strongly supported call" | Strongly supported is not defined anywhere, and it is the whole basis of the reassurance. | Say what read count counts as strongly supported. |
| Before you start, "the Williams MiSeq genotyping project, a rhesus macaque study of 30 animals" | MiSeq is never explained. I guessed it is a machine but the chapter never says. | One clause saying MiSeq is the sequencing instrument. |
| Before you start, "matched against an IPD-MHC Mamu allele library" | Three unfamiliar terms in one phrase, IPD-MHC, Mamu, and allele library. I could not tell whether any of them mattered for exporting. | Gloss Mamu as the macaque MHC naming prefix, or drop the phrase. |
| Before you start, "read this chapter against a genotype result of your own" | I do not have a genotype result of my own and cannot make one from this chapter. I could not perform any step. | Point to where a practice bundle can be obtained, or say the chapter is read-only without one. |
| Before you start, "A run that produced only allele calls, with no haplotype analysis on top of them" | I do not know what haplotype analysis on top of allele calls means, or how to tell which kind of run I have before I look. | Say what to look for in the window to tell the two apart. |
| Before you start, "the viewport's **Actions** menu is hidden" | Viewport is used with no explanation. I do not know which part of the window that is. | Gloss viewport at first use, the way Inspector is glossed. |
| Procedure Step 1, "Open the Inspector's **Genotype Display** section by clicking its disclosure triangle" | I do not know what a disclosure triangle looks like or where in the panel to find it. The screenshot is only a caption to me. | Say it is the small triangle to the left of the section name. |
| Procedure Step 1, "past the search fields, the numeric filters, and the colour controls" | I could not tell how far down to scroll or how many controls I am passing. | Say roughly how many controls sit above the Export block. |
| Procedure Step 1, "**Min reads** hides any allele value supported by fewer reads" | The step tells me to set two filters but gives me no number to set them to, and only says both default to 0. | Name the starting values used later, 50 and 5. |
| Procedure Step 1, "Both sit at 0 by default, which means both are off" | If both are off by default and the step is to set them, I could not tell whether I was supposed to change them on a first run. | Say plainly whether a first-time reader should leave them at 0. |
| Procedure Step 1, "Choose a location outside the project folder" | I do not know where the project folder is, so I could not tell what is outside it. | Name a safe choice such as Desktop or Documents. |
| Procedure Step 1, "The copy is one-way, so edits you make in Excel never flow back" | This is important and I only understood it on a second read, because the sentence before it is about formatting. | Give this its own short sentence at the start of the paragraph. |
| Procedure Step 2, "The first two carry `Animal ID` and `GS ID`" | GS ID is never explained. I could not tell whether it is something I supply or something LGE makes. | One clause saying what GS ID stands for. |
| Procedure Step 2, "that total is 682,927 reads across 30 samples, an average of 22,764 each" | I have no idea whether 22,764 reads per sample is a lot or a little, and the range 713 to forty thousand made me more unsure, not less. | Say what read count is enough to call a sample, so the range means something. |
| Procedure Step 2, "the individual samples range from 713 to well over forty thousand" | Two numbers are exact and the third is vague. I could not tell whether the vagueness was hiding something. | Give the actual maximum. |
| Procedure Step 2, "a much stricter test than ordinary mapping" | Mapping has not been explained in this chapter, and I do not know how strict ordinary mapping is. | Compare in one clause, or link to the mapping chapter. |
| Procedure Step 2, "labelled `MHC-A Haplotype 1`, `MHC-A Haplotype 2`, and so on through eight loci" | Haplotype is linked but never explained in the running text, and this is the first place it does real work. | Gloss haplotype in the sentence, a set of alleles inherited together. |
| Procedure Step 2, "so on through eight [loci]" | Eight loci here, but Step 3 says the run reported 13 loci. I could not reconcile the two counts. | Say why the pivot shows eight and the matrix thirteen. |
| Procedure Step 2, "blanked 1,478 individual allele values and removed 192 rows" | I could not tell the difference between a blanked value and a removed row on a first read. | One clause saying a row is removed only when every value in it was blanked. |
| Procedure Step 2, "a Total of 929 across 5 observations before filtering, and after filtering the same row reads 927 across 3 observations" | The arithmetic did not work for me. Two single-read observations removed should drop 5 to 3 and 929 to 927, which I only checked by hand. It is called plain but it was not. | Show the subtraction, 929 minus 1 minus 1. |
| Procedure Step 2, "A long summary sheet holds the same data one row per sample-and-allele pair, 2,109 rows" | I could not tell why 30 samples and 305 alleles give 2,109 rows rather than 9,150. | Say the long sheet holds only the pairs that were actually observed. |
| Procedure Step 3, "The three remaining formats have no button. They come from `lungfish-cli genotype export`" | I have never opened a terminal, and this is Step 3 of the main procedure, not the optional section. I stopped being able to follow along. | Move the command-line steps into the optional section at the end. |
| Procedure Step 3, "whose `--export-format` chooses among `xlsx`, `csv`, and `tsv`" | I do not know what typing a flag looks like or where I would type it. Nothing here shows a whole command. | Show one full command here rather than in a later section. |
| Procedure Step 3, "headed `H1` and `H2` for the two report slots each locus carries" | Report slot is a new term. I guessed it means the two copies you inherit but the chapter never says. | Say the two slots are the two alleles at that locus. |
| Procedure Step 3, "listing the tokens `M1` through `M7` with their hex colours" | I do not know what M1 through M7 mean or what a hex colour is. | Say what the M tokens stand for. |
| Procedure Step 3, "27 columns, a `Sample` column followed by an `H1` and `H2` pair for each of the 13 loci" | 13 times 2 plus 1 is 27, which I had to work out, and it contradicts the eight loci from Step 2. | Say the arithmetic, and reconcile the locus counts. |
| Procedure Step 4, "use `lungfish-cli genotype export-labkey`" | Another terminal-only step in the main procedure. LabKey is linked but I do not know what it is. | Gloss LabKey in one clause as a shared research database. |
| Procedure Step 4, "the header `animal_id,gs_id,allele,locus_group,unique_reads,passed_unique_reads,passed_alignments`" | Seven column names, four of which I cannot guess. The difference between unique_reads and passed_unique_reads is the one I most wanted. | Define the three read-count columns. |
| Procedure Step 4, "reflect your overrides merged over the pipeline result" | Pipeline result is a new term and merged over is doing a lot of work. | Say the exported call is your corrected call where you made one. |
| Settings, "Everything in this section lives in the Inspector's **Genotype Display** section, except the two export buttons and the workbook button" | Three exceptions listed at the top of a long section made me unsure what I was reading. | Split the buttons into their own short list. |
| Settings, "Every control here is display state" | Display state is a software term I do not know. | Say it changes only what you see. |
| Settings, "**Min reads.** ... it accepts a whole number from 0 to 100,000" | I do not know how to choose inside a range that wide. 50 appears elsewhere but not here. | Name 50 as a starting point in this entry. |
| Settings, "**Min percent.** ... accepts a value from 0 to 100 in steps of 0.5" | Same problem. I could not judge whether 5 percent is strict or loose. | Say what 5 percent removes on a typical run. |
| Settings, "**Percent Basis.** ... offering Viewed Locus ... and Sample Retained" | Two options whose names I could not map onto the descriptions without rereading both twice. | Rename in the description, per-locus and per-sample. |
| Settings, "which is the usual case for an amplicon panel" | Amplicon panel is never explained in this chapter. | Gloss or link amplicon panel. |
| Settings, "typing `DRB` narrows a 970-row matrix to the DRB targets alone" | 970 rows here but 305 allele rows earlier. I could not tell which number describes the Williams result. | Use the same result's numbers throughout, or say this one is a different result. |
| Settings, "**Show All Rows.** ... remembering that there is no automatic row limit for it to undo" | I read this twice and still do not know what it warns me about. | Cut, or say what the risk actually is. |
| Settings, "**Cell Color.** ... Support, which shades each cell by its read count" | I could not tell whether the colours carry into the exported file, which is the only thing I would care about here. | Say whether colours survive the export. |
| Settings, "**Update and View Current Excel Version.** ... which is the one thing in this chapter that writes back into the result" | This contradicts the opening claim that nothing in this chapter changes a call. I did not know which to trust. | Reconcile the two sentences, or note the exception up front. |
| Settings, "Eight further options exist only on the command line" | I counted the entries that follow and there are eight, but after the terminal steps in the Procedure I no longer knew whether this section was optional for me. | Say plainly that a reader who does not use the terminal can skip these. |
| Settings, "**--view-projection.** Points at a JSON file describing a rendered viewport" | I do not know what JSON is or how I would obtain such a file. | Say the app writes this file and you rarely name one. |
| Settings, "**--keep-empty-rows.** ... would break if 192 rows vanished" | The 192 is from one specific result but reads here like a general number. | Say this is the Williams count, not a rule. |
| Reading the results, "prints a JSON summary naming `matchedAlleleRows`, `filteredAlleleValueCount`, and `removedAlleleRowCount`" | The section says an export tells me nothing new, then tells me to read three numbers I can only get from the terminal. | Say where a window user sees the equivalent counts. |
| Reading the results, "The matched-row count does not change, because it counts what the result holds rather than what survived" | I read this three times. Matched sounds like it should mean what survived matching. | Rename it in the description, total rows examined. |
| Reading the results, "it should match the number of samples you submitted rather than the number that worked" | I could not tell how I would know how many I submitted if the run were not mine. | Say where the submitted count is recorded. |
| Reading the results, "even though 7 of those samples were too thin to call much" | Too thin is not defined and 7 of 30 sounds bad. I could not tell whether that run was a success. | Give the read count below which a sample is too thin. |
| What good looks like, "Removing 192 of 305 rows sounds severe until you notice" | This reassurance is the fourth appearance of the same two numbers, and I still do not know what threshold would be too aggressive. | Say what fraction removed would be a warning sign. |
| What good looks like, "the threshold is wrong for your sequencing depth" | Sequencing depth has not been explained in this chapter, and it is the diagnosis being offered. | Gloss depth as how many reads each sample got. |
| Haplotype analysis (placeholder), "A worked example with an MCM dataset will be added" | MCM is never expanded. The chapter title promised haplotype definitions and this is all there is. | Expand MCM, and note the gap in the front matter. |
| On the command line, "The whole procedure runs headless, meaning with no window at all" | The section says everything above is complete without it, then the next paragraph says two of four formats exist only here. The two claims cancel out. | Fix the first claim, since CSV, TSV, and LabKey are terminal-only. |
| On the command line, "lungfish-cli genotype export-xlsx \\" | The backslashes at line ends are unexplained and I would not know whether to type them. | One clause saying the backslash continues one command onto the next line. |
| On the command line, "`--percent-basis` on the command line defaults to `sample-retained`" | A default that differs between the window and the terminal sounds like a bug, and I could not tell whether I should trust either. | Say which default is intended. |
| On the command line, "reporting that a provenance publication artifact no longer matches the transaction generation" | I could not parse this error message at all, and I do not know what a symbolic link is. | Say only that the export fails, and give the plain-path fix. |

One thing I learned: exporting is about choosing the file for whoever opens it next, not about getting the numbers right.

One thing I still could not do: perform any of the four exports, because the chapter needs a genotype bundle I do not have, and three of the four formats need a terminal.

The sentence I liked most: "A good export is boring."
