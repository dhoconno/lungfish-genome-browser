# Merged reader report - Exporting Genotypes

Four readers reviewed this chapter. Reader 1 is a genetics sophomore fresh from an introductory course, with no lab time and no terminal experience. Reader 2 is a wet-lab senior with two years of pipetting and PCR experience but no data analysis background and no terminal experience. Reader 3 is a pre-med student reading English as a second language, comfortable with biology vocabulary but slowed by idioms and long sentences, with no terminal experience. Reader 4 has used Geneious for one semester and expects every operation to have a dialog, with no terminal experience. Reader 1 filed 64 rows, reader 2 filed 50 rows, reader 3 filed 60 rows, and reader 4 filed 36 rows, for 210 source rows in total.

| Location | Issue | Suggested fix | Readers |
|---|---|---|---|
| Title and front matter, "Exporting Genotypes" vs the file name | The chapter is titled Exporting Genotypes but files under haplotype-definitions-and-export, and haplotype definitions turn out to be only a placeholder paragraph at the end. All four readers kept reading for a section that never arrives. | Rename the file to match the title, or say in the first paragraph that haplotype definitions are not covered yet. | 1, 2, 3, 4 |
| What it is, "a folder carrying the `.lungfishgenotype` extension" | All four readers did not know a folder could carry a file extension, and could not tell whether double-clicking it in Finder opens it as a single item or opens a folder of loose files. | Add one sentence saying Finder shows this folder as a single item that opens directly in LGE. | 1, 2, 3, 4 |
| Before you start, "read this chapter against a genotype result of your own" | All four readers do not have a genotype result of their own, the manual does not ship one, and every number in the chapter comes from a dataset they cannot open or check their own data against. | Say plainly that a reader without a result should read for orientation only, or point to a sample bundle. | 1, 2, 3, 4 |
| Procedure Step 1, "clicking its disclosure triangle" | All four readers could not picture a disclosure triangle or find the Inspector's Genotype Display section from this instruction alone, since there is no screenshot at this point and one reader could not tell how to open the Inspector at all. | Say it is the small arrow at the left of the section heading, and name the menu item or shortcut that shows the Inspector if it is not already open. | 1, 2, 3, 4 |
| Procedure Step 1, "past the search fields, the numeric filters, and the colour controls" | All four readers could not confirm they had scrolled to the right place, since these controls are named here before they are ever shown or explained. | Name the last control immediately above the Export block so the reader knows they have arrived. | 1, 2, 3, 4 |
| Procedure Step 1, "Both sit at 0 by default, which means both are off" | All four readers were told Min reads and Min percent default to 0 but were given no number to type for a first export, only learning the worked values much later. | State a suggested starting pair here and say it is a starting point rather than a rule. | 1, 2, 3, 4 |
| Procedure Step 1, "Choose a location outside the project folder" | All four readers were given the instruction with no explanation of where the project folder is or what goes wrong if the export lands inside it. | Name a safe concrete location such as the Desktop, and say what happens if the export is placed inside the project folder. | 1, 2, 3, 4 |
| Procedure Step 1, the one-way and permanent-filter warning | All four readers found the chapter's most important warning, that the export freezes whatever the filters currently show, buried mid-paragraph rather than stated up front. One reader read the word permanent as contradicting the caption that filters do not change calls. | Give this warning its own short sentence at the start of the paragraph, and say plainly it means permanent in the exported copy only. | 1, 2, 3, 4 |
| Procedure Step 3, "headed `H1` and `H2` for the two report slots each locus carries" | All four readers met report slot as a new, unglossed term and had to guess it means the two alleles a diploid animal carries at a locus. | Gloss report slot at first use as the two alleles called at that locus. | 1, 2, 3, 4 |
| Procedure Step 3, "listing the tokens `M1` through `M7` with their hex colours" | All four readers found token and the M1 through M7 labels unexplained and could not tell what they stand for. | Say what the M tokens mean, or point to the section that defines them. | 1, 2, 3, 4 |
| Procedure Step 2, "a Total of 929 across 5 observations" before filtering and "927 across 3 observations" after | All four readers had to work out the arithmetic by hand to trust that two blanked single-read observations explain both the drop from 929 to 927 and from 5 to 3. | Spell out the subtraction directly, 929 minus two single-read values equals 927. | 1, 2, 3, 4 |
| Procedure Step 3, the step not marked as command-line only | All four readers hit terminal-only export formats presented as ordinary numbered Procedure steps rather than flagged as optional or command-line only, and stopped being able to follow along since none had opened a terminal before. | Mark Steps 3 and 4 as command-line only at the top of the Procedure, or move them into the optional section at the end. | 1, 2, 3, 4 |
| Settings, "typing `DRB` narrows a 970-row matrix to the DRB targets alone" | All four readers met a 970-row figure that contradicts the 305 allele rows quoted throughout the worked example and could not tell whether it describes the same result. | Use the worked example's own numbers, or say plainly this figure comes from a different result. | 1, 2, 3, 4 |
| Settings, "Show All Rows... remembering that there is no automatic row limit for it to undo" | All four readers read this clause multiple times and still could not tell what risk it was warning them about. | Cut the clause, or state the risk directly. | 1, 2, 3, 4 |
| Settings, "Eight further options exist only on the command line" | All four readers found this line's framing confusing. One miscounted the entries as nine, one found the sentence's ending clause unfinished, and two lost track of whether the section applied to them after already hitting terminal-only steps in the Procedure. | Fix the count, finish the sentence, and say plainly that a reader who does not use the terminal can skip these entries. | 1, 2, 3, 4 |
| Settings, "**--view-projection.** Points at a JSON file describing a rendered viewport" | All four readers met JSON, rendered, and viewport together with none glossed, and could not tell who produces such a file or whether they would ever need to supply one. | Gloss JSON as a plain text data file, say the app writes it, and note that a non-terminal reader can ignore this option. | 1, 2, 3, 4 |
| Reading the results, "even though 7 of those samples were too thin to call much" | All four readers met too thin with no defined cutoff and could not tell whether seven failing samples out of 30 means the run succeeded or not. | Give the read count below which a sample counts as too thin, and say whether this failure rate is expected. | 1, 2, 3, 4 |
| On the command line, the section's opening claim of being optional or headless | All four readers found the section's opening, that everything above is complete without the terminal or that the whole procedure runs headless, contradicted by Steps 3 and 4 already requiring the terminal. None was told plainly where Terminal is or how to start it before that point. | Replace the boilerplate opening with the qualification that follows it, and move a one-line pointer to Terminal up to Step 3. | 1, 2, 3, 4 |
| On the command line, "`--percent-basis` on the command line defaults to `sample-retained`" | All four readers found this default disagrees with the window's Percent Basis default and could not tell whether the mismatch is intentional or a bug. | Say whether the difference is intended, and repeat the warning in the Percent Basis setting entry before the example that relies on it. | 1, 2, 3, 4 |
| On the command line, "reporting that a provenance publication artifact no longer matches the transaction generation" | All four readers could not parse this error message at all and did not know what a symbolic link is or how to tell whether a chosen folder is reached through one. | Say only what the reader sees and what to do, name a common safe folder, and drop or gloss the internal wording. | 1, 2, 3, 4 |
| Why you would do this, "filtering the pivot at a modest threshold removed 192 of 305 allele rows" | Three readers found modest gives no number, and all four could not judge whether removing nearly two thirds of the rows is expected or a disaster, since the actual thresholds are not named in this sentence. | Name the thresholds in this sentence, Min reads 50 and Min percent 5, so modest is anchored. | 1, 2, 3, 4 |
| Procedure Step 2, "the individual samples range from 713 to well over forty thousand" | Three readers could not judge whether 713 reads is a failing sample or an acceptable one, since no line is drawn between good and bad and the vague upper bound reads as digits everywhere else. | Give the pass or fail meaning of a low read count, and write the maximum as a digit. | 1, 2, 3 |
| Procedure Step 2, "so on through eight loci" vs "13 loci" in Step 3 | Three readers hit two different locus counts for what they took to be the same run and could not reconcile eight against 13. | Say explicitly why the pivot shows eight loci and the run reports 13. | 1, 2, 3 |
| Before you start, "an IPD-MHC Mamu allele library" | Three readers met three unfamiliar terms in one short phrase, IPD-MHC, Mamu, and allele library, and could not tell whether any of them mattered for exporting. | Gloss all three in one sentence, or drop to a plain phrase such as a rhesus macaque MHC allele reference set. | 1, 2, 3 |
| What it is, "one row per single fact rather than one row per sample" | Three readers read this sentence multiple times and could not picture what one fact is without an example. | Give the concrete example, one row per sample-and-allele pair, or show two tiny tables, wide and long, side by side. | 1, 2, 3 |
| Before you start, "A run that produced only allele calls, with no haplotype analysis on top of them" | Three readers could not tell from this sentence whether their own run has haplotype analysis, since the only test offered, a missing menu, is exactly the kind of thing a new user would miss. | Name a visible marker in the window or the Inspector that says which kind of result is open. | 1, 3, 4 |
| Settings, Min reads, "it accepts a whole number from 0 to 100,000" | Three readers were given the input range with no sensible starting value, even though 50 is used elsewhere in the chapter without being named here. | Name 50 as a reasonable starting point in this entry. | 1, 2, 3 |
| Procedure Step 2, "a much stricter test than ordinary mapping" | Three readers met mapping used as a comparison with no gloss and, never having done mapping, got nothing from the comparison. | Gloss mapping at first use as aligning reads to a reference, or say instead that a single mismatching base disqualifies a read. | 1, 2, 3 |
| Procedure Step 2, "blanked 1,478 individual allele values and removed 192 rows" | Three readers could not tell the difference between a blanked value and a removed row, since both sound like deletion on a first read. | Add one clause saying a row is removed only when every one of its values was blanked. | 1, 2, 3 |
| Procedure Step 4, "reflect your overrides merged over the pipeline result" | Three readers found pipeline unglossed and merged over unclear, unable to tell whether an override replaces the original call or sits beside it. | Say plainly that a reader's correction replaces the original pipeline call. | 1, 2, 3 |
| Procedure Step 2, "The first two carry `Animal ID` and `GS ID`" | Three readers found GS ID never explained, unable to tell whether it is supplied by the user or generated by LGE. | Gloss GS ID at first use. | 1, 3, 4 |
| Settings, "Everything in this section lives in the Inspector's Genotype Display section, except..." | Three readers found this long exception-listing sentence confusing, and one noted it contradicts Step 1, which places the Filtered Pivot button inside that same section. | Split the sentence, and say plainly which control sits in which panel. | 1, 3, 4 |
| Settings, "Every control here is display state" | Three readers met display state as unglossed jargon and only worked out the meaning from the next sentence. | Say plainly that these controls change only what the reader sees. | 1, 2, 3 |
| Settings, Min percent, "in steps of 0.5" | Three readers could not tell whether this means a typed value off the 0.5 step is rejected, and none was given a typical percent to judge against. | Say whether typed values off the step are allowed, and give a typical value. | 1, 2, 3 |
| Settings, "which is the usual case for an amplicon panel" | Three readers met amplicon panel with no gloss anywhere in the chapter. | Gloss amplicon panel as a set of PCR products sequenced together. | 1, 2, 3 |
| Reading the results, "the threshold is wrong for your sequencing depth, not the export" | Three readers were offered sequencing depth as the diagnosis without depth ever being explained or tied to a Min reads number. | Gloss depth as how many reads each sample got, and give a rule of thumb tying depth to a threshold. | 1, 2, 3 |
| On the command line, "`lungfish-cli genotype export-xlsx \`" | Three readers met the backslash at the end of each command line with no explanation and could not tell whether to type it. | Add one line saying the backslash continues the command onto the next line. | 1, 2, 3 |
| Settings, "Update and View Current Excel Version" write-back | Three readers found that this is the one control that writes back into the result, and that fact is not warned about clearly enough before the entry itself, coming late in a long settings list. | Warn about the write-back at the top of the Settings section, not only inside this entry. | 1, 2, 4 |
| Reading the results, "prints a JSON summary naming `matchedAlleleRows`..." | Three readers found this section presented as proof an export succeeded, yet the summary only prints from a terminal command that a window-only reader never runs. | Say what a window-only reader can check instead, such as opening the file and counting rows. | 1, 2, 4 |
| Why you would do this and Before you start, MiSeq never explained | Two readers met MiSeq with no gloss and had to guess it names the sequencing machine. | Add one clause saying MiSeq is the sequencing instrument, an Illumina short-read sequencer. | 1, 3 |
| What it is, "lays samples down the rows and loci across the columns" | Two readers hit locus here, several paragraphs before it is defined in Step 2, and read the first use as unexplained or as a typo. | Move the one-line locus gloss to this first use. | 1, 2 |
| What it is, "[allele targets] run down the rows" | Two readers found allele target linked but not glossed in the sentence itself. | Add one clause saying an allele target is one reference sequence the reads are matched against. | 1, 2 |
| What it is, "which any script can read without a spreadsheet library" | Two readers did not know what a spreadsheet library is and could not use it as a reason to choose CSV. | Cut the phrase, or say plainly that a CSV file opens in any program, including Excel. | 1, 2 |
| What it is, "So what should you do with this?" | Two readers found this question addressed to them before the chapter had told them who the intended reader is, leaving nothing to answer with. | Cut the question and keep the advice sentence that follows it. | 1, 3 |
| Settings, "**Cell Color.** Support, which shades each cell by its read count" | Two readers could not tell which direction the shading runs or whether the colours survive into the exported file. | Say that darker shading means more reads, and whether colours carry into the export. | 1, 2 |
| Procedure Step 4, "use `lungfish-cli genotype export-labkey`" | Two readers hit this as another terminal-only step inside the numbered Procedure, with LabKey itself unglossed and no dialog available. | Gloss LabKey as a shared research database, and say at the top of the Procedure which steps need the terminal. | 1, 4 |
| Before you start, "the viewport's Actions menu is hidden" | Two readers met viewport used as if already known, with no gloss at first use. | Gloss viewport at first use as the main display area in the middle of the window. | 1, 3 |
| Settings, "disabled while the workbook is already current or the bundle is read-only" | Two readers did not know what makes a bundle read-only or how to tell which of the two disabled reasons applies. | Say the common cause, such as a locked folder, and how the window shows a read-only bundle. | 3, 4 |
| Settings, "--annotations, an annotation sidecar" | Two readers found sidecar an unexplained metaphor. | Say it is a separate notes file stored next to the bundle. | 2, 3 |
| Why you would do this, "removed 192 of 305 allele rows sounds severe" | Two readers could not tell whether losing nearly two thirds of the rows is normal, since the reassurance sits far away in a later section and even there gives no threshold for what would be a warning sign. | State plainly that the removal is expected here, and give a fraction that would count as a warning sign. | 1, 3 |
| Haplotype analysis placeholder, "A worked example with an MCM dataset will be added" | Two readers could not expand MCM and found it the only content in the section the chapter's own title promises. | Expand MCM at first use, and note the gap in the front matter. | 1, 3 |
| What it is, "the read counts, an Excel workbook, the run statistics" | One reader could not tell why an export is needed at all when the bundle already lists an Excel workbook among its contents. | Say here that the internal workbook is not the one sent out, and why a separate export exists. | 1 |
| What it is, "which is the shape most downstream spreadsheets in a genotyping lab already expect" | One reader had never seen a genotyping lab spreadsheet, so this reason did not help in choosing a format. | Say what the person receiving the file actually does with it. | 1 |
| Why you would do this, "The immunologist wants a spreadsheet" | One reader could not tell which of three job titles, used as shorthand for three export formats, applied to them. | Add a clause saying which format applies when exporting for yourself. | 1 |
| Why you would do this, "without touching a single strongly supported call" | One reader found strongly supported undefined even though it is the whole basis of the chapter's reassurance. | Say what read count counts as strongly supported. | 1 |
| Procedure Step 2, "an average of 22,764 each" | One reader had no way to judge whether 22,764 reads per sample is a lot or a little. | Say what read count is enough to reliably call a sample. | 1 |
| Procedure Step 2, "the individual samples range from 713 to well over forty thousand" (vague maximum) | One reader found two exact numbers and one vague upper bound in the same sentence and could not tell whether the vagueness hid something. | Give the actual maximum as a number. | 1 |
| Procedure Step 2, "so on through eight loci" (haplotype term itself) | One reader found haplotype linked but never explained in the running text at the point it first does real work. | Gloss haplotype in the sentence as a set of alleles inherited together. | 1 |
| Procedure Step 3, "27 columns, a `Sample` column followed by an `H1` and `H2` pair for each of the 13 loci" | One reader had to work out the 13-times-2-plus-1 arithmetic by hand, and it further contradicts the eight loci named in Step 2. | Show the arithmetic, and reconcile the locus counts. | 1 |
| Procedure Step 2, "A long summary sheet holds the same data one row per sample-and-allele pair, 2,109 rows" | One reader could not tell why 30 samples and 305 alleles produce 2,109 rows rather than 9,150. | Say the long sheet holds only the pairs that were actually observed, not every possible combination. | 1 |
| Step 2, "retained percentages" | One reader found this term never defined and could not tell what it is a percentage of. | Gloss it as the share of a sample's reads that matched an allele exactly. | 2 |
| Procedure Step 3, "whose `--export-format` chooses among `xlsx`, `csv`, and `tsv`" | One reader was told what a flag chooses among without ever seeing a whole command typed out. | Show one full example command here rather than only in a later section. | 1 |
| Procedure Step 4, "the header `animal_id,gs_id,allele,locus_group,unique_reads,passed_unique_reads,passed_alignments`" | One reader could not tell the difference between unique_reads and passed_unique_reads among the seven column names. | Define the three read-count columns explicitly. | 1 |
| Settings, "**Percent Basis.** offering Viewed Locus... and Sample Retained" | One reader could not map the two option names onto their descriptions without rereading both twice. | Rename in the description using plain terms, per-locus and per-sample. | 1 |
| Settings, "which is the usual case for an amplicon panel" (spread across samples) | One reader could not tell whether a sixty-fold spread between samples is normal for an amplicon panel or a warning sign. | Say whether that spread is expected for an amplicon panel. | 4 |
| Procedure Step 2, four different counts in one result | One reader could not reconcile eight loci, 13 loci, 305 allele rows, and a 970-row matrix as counts belonging to the same result. | Say explicitly that the haplotype band, the reported loci, and the allele rows count different things. | 4 |
| Settings, "**--keep-empty-rows.** ... would break if 192 rows vanished" | One reader found the 192 figure specific to the Williams result but presented here as though it were a general rule. | Say this is the Williams count, not a rule. | 1 |
| Reading the results, "The matched-row count does not change, because it counts what the result holds rather than what survived" | One reader read this three times, since matched sounds like it should mean what survived matching. | Rename it in the description, total rows examined. | 1 |
| Reading the results, "it should match the number of samples you submitted rather than the number that worked" | One reader could not tell how they would know how many samples were submitted if the run were not their own. | Say where the submitted count is recorded. | 1 |
| Settings, "typing `DRB`" | One reader found DRB used as if already familiar, with no gloss for the acronym itself. | Say DRB is one MHC class II gene family. | 2 |
| Settings, "--lens, Records which viewport view an export came from" | One reader found lens used as a flag name with no explanation as a concept. | Gloss lens once at first use. | 3 |
| Settings, "--force, which is a guard worth keeping until a script needs to rerun cleanly" | One reader does not write scripts and could not judge when this flag would be needed. | Say plainly that most readers never need this flag. | 3 |
| Settings, "Filtered Pivot... it appears only when the result carries a workbook the pivot can be built from" | One reader could not tell in advance whether their own result has a usable workbook, or what to do if the button does not appear. | Say what to check if the Filtered Pivot button is missing. | 3 |
| Settings, --active-haplotype-definition | One reader noted this is the only place the chapter's own title subject appears, and the guidance is to leave it unset. | Say haplotype definitions are covered in a later release. | 2 |
| What it is, three workbook words arriving close together | One reader lost track of which button or command produces the matrix workbook, the pivot workbook, and the bundle's own current.xlsx by the time Step 3 used all three. | Add a small table naming the three workbooks, what each looks like, and which control writes it. | 4 |
| Procedure Step 2, "how many retained reads it contributed, alongside a Total and an Average" | One reader had to read out of order because the definition of a retained read arrives two sentences after the term is first used. | Move the retained-read definition to the term's first mention. | 4 |
| Settings, "**Samples.** On the command line this is `--sample`, which is repeatable" | One reader did not know what repeatable means for a command-line flag, encountered inside a setting otherwise used from the window. | Drop the repeatable note here, or show it only in the command-line section. | 4 |
| What good looks like, "Clear the Min reads and Min percent fields and export again" | One reader could not tell whether clear means leave the fields empty or set them to 0, which the Settings describe as the default that turns the filter off. | Say explicitly to set both fields to 0. | 4 |
| On the command line, "Every command takes a `--bundle` pointing at the `.lungfishgenotype` folder" | One reader did not know how to write a folder path and the example uses a relative path they could not reproduce. | Say how to get a folder path, for example by dragging the folder into the Terminal window. | 3 |
| Step 4, "It writes five CSV files into a directory you name" | One reader found directory used here where the rest of the chapter says folder, unsure whether they are the same thing. | Use folder throughout the chapter. | 2 |
| Step 4, "with a stable header and standard quoting" | One reader found standard quoting meaningless. | Delete the phrase, or say that commas inside a value are handled safely. | 2 |
| Settings, Export Excel View, "with the format fixed rather than chosen" | One reader had to reread this to conclude it means the file is always an Excel workbook with no format choice. | Say directly that the file is always an Excel workbook. | 2 |
| Next, "Return to Reading the Genotype Comparison to set the filters an export will capture" | One reader set filters blind in Step 1 because the chapter's dependency on the previous chapter is only mentioned at the very end. | Point to that chapter in Step 1, where the filters are first used. | 4 |
| What it is, "which turns that layout on its side" | One non-native reader stopped on this figure of speech, unsure whether rows became columns or the whole table was mirrored. | Say plainly that rows become columns and columns become rows. | 3 |
| Why you would do this, "which is exactly the step that introduces errors nobody catches" | One non-native reader had to reread this long sentence to see that retyping is the dangerous step, since the relative clause sits far from what it refers to. | Split into two shorter sentences. | 3 |
| Step 2, "it is laid out in three bands" | One non-native reader pictured a gel electrophoresis band rather than a group of rows. | Say plainly three groups of rows. | 3 |
| Why you would do this, "a long tail of very low read counts" | One reader had not met long tail as a statistics phrase. | Replace with a plain phrase, such as many alleles seen only once or twice. | 2 |
| Before you start, "because every export described here reads the same manifest" | One reader found manifest used once and never explained. | Gloss manifest at first use, or say the same index file inside the bundle. | 3 |
| What it is, "their checksums" | One reader found checksum unexplained inside the sentence that explains provenance. | Gloss checksum as a fingerprint that shows a file has not changed. | 2 |
| Step 2, "where a haplotyped run would write its family calls" | One non-native reader found family calls undefined and haplotyped used unfamiliarly as a verb. | Gloss, or say where a haplotype analysis would write its results. | 3 |
| Step 2, "named by its reference record" | One reader could not decode the parts of an example allele name such as 01_Mamu-A1_002g. | Break down one allele name once, saying what each part means. | 3 |
| Step 3, "an `ERR` token for an error or no call" | One non-native reader did not know what no call means or whether it signals a problem. | Gloss no call as meaning the software could not decide. | 3 |
| Step 4, "it takes no tuning options at all" | One non-native reader read tuning options as idiomatic and was unsure whether it meant filters do not apply to this export. | Say directly that filters do not apply to this export. | 3 |
| Step 2, "on an unreviewed result both hold only their header row" | One non-native reader understood the visible terms but not the assumption that a person, the analyst, adds notes to the result. | Say in one clause what an analyst edit is. | 3 |
| Before you start, "One fact about the window decides which in-app export you will find, and it is worth settling before you go looking" | One non-native reader needed three readings to parse the idioms settling and go looking. | Replace with a plain instruction such as check one thing about your result before you start. | 3 |
| Reading the results, "those three numbers are the record of how hard your filters bit" | One non-native reader understood the words but not the idiom how hard your filters bit on first pass. | Say plainly how much your filters removed. | 3 |
| Reading the results, "read it as a check that the exporter saw the whole result" | One non-native reader found this a single sentence over 60 words covering three separate numbers. | Split into three short sentences, one per number. | 3 |
| What good looks like, "A good export is boring" | One non-native reader liked the line but paused to check whether boring was a technical judgement rather than a plain compliment. | Keep the line, but follow it immediately with the plain meaning. | 3 |
| Procedure Step 1, "Both sit at 0 by default, which means both are off" (change on a first run) | One reader could not tell whether a first-time exporter should change the defaults before running, since the step both says they default to off and asks the reader to set them. | Say plainly whether a first-time reader should leave them at 0 or change them. | 1 |

## Consensus

Twenty-one rows were hit by all four readers.

- The chapter is titled Exporting Genotypes but files under a name promising haplotype definitions, and that section is only a placeholder.
- All four readers did not know a folder can carry the `.lungfishgenotype` extension and show as a single clickable item.
- All four readers have no genotype result of their own and no sample bundle is offered to practice with.
- All four readers could not find or picture the disclosure triangle that opens the Inspector's Genotype Display section.
- All four readers could not tell whether they had scrolled past the right controls to reach the Export block.
- All four readers were given no starting values for Min reads and Min percent beyond the 0 default.
- All four readers were told to choose an export location outside the project folder with no guidance on where that folder is or what happens otherwise.
- All four readers found the warning that filters become permanent in the export buried or unclear.
- All four readers had to guess that a report slot means the two alleles at a locus.
- All four readers found the M1 through M7 tokens and their hex colours unexplained.
- All four readers had to work out by hand that two blanked single-read observations explain the drop from 929 to 927 reads and from 5 to 3 observations.
- All four readers hit command-line-only export steps presented as ordinary numbered Procedure steps with no warning.
- All four readers found the 970-row DRB matrix figure contradicts the 305 allele rows used throughout the worked example.
- All four readers could not tell what risk the Show All Rows warning was describing.
- All four readers found the Eight further options line miscounted, unfinished, or unclear about who can skip it.
- All four readers met JSON, rendered, and viewport together with none glossed in the --view-projection entry.
- All four readers could not tell whether seven thin samples out of 30 means the run succeeded.
- All four readers found the command-line section's opening claim of being optional or headless contradicted by the terminal-only steps they had already read.
- All four readers could not tell whether the percent-basis default mismatch between window and terminal is a bug.
- All four readers could not parse the symbolic-link error message at all.
- All four readers found the modest threshold that removed 192 of 305 allele rows given with no actual numbers to anchor it.
