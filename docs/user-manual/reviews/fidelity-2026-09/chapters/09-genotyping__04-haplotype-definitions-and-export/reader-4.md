# Reader 4 report

Chapter: `docs/user-manual/chapters/09-genotyping/04-haplotype-definitions-and-export.md`
Reader: undergraduate, one semester of Geneious in a course, comfortable with a graphical sequence viewer, expects every operation to have a dialog, has never opened a terminal.
Date: 2026-09-07

| Location | Issue | Suggested fix |
| --- | --- | --- |
| Title and front matter, "Exporting Genotypes" | The filename says "haplotype definitions and export" and the chapter is filed under that name, but haplotype definitions get one placeholder paragraph. I opened this chapter to learn how to define a haplotype and found only an export chapter. | Rename the file to match the title, or say in the first line that haplotype definitions are not covered yet. |
| What it is, "leaves behind a genotype result bundle, a folder carrying the `.lungfishgenotype` extension" | I did not know a folder could have an extension. In Geneious everything lives in the program's own database and I never see a folder. I do not know whether double-clicking this in the Finder opens LGE or opens a folder of files. | Add one sentence saying the Finder shows it as a single item and double-clicking opens it in LGE. |
| What it is, "One is a matrix workbook that lays samples down the rows and loci across the columns" | Three workbook words arrive close together, matrix workbook, pivot workbook, and later the bundle's `current.xlsx`. By Step 3 I had lost track of which button makes which. | Add a small table naming the three workbooks, what each looks like, and which button or command writes it. |
| Why you would do this, "filtering the pivot at a modest threshold removed 192 of 305 allele rows" | "Modest" is doing a lot of work. Removing nearly two thirds of the rows did not sound modest to me, and I could not judge whether that is what I should expect on my own data. | Give the threshold in the same sentence, Min reads 50 and Min percent 5, so the word modest is anchored. |
| Before you start, "It is the project owner's own macaque dataset and is not distributed with the manual" | Every number in the chapter comes from a dataset I cannot open. I have no way to check my own run against anything, and I do not know which numbers should look similar on my data and which are specific to those 30 animals. | Say which quoted numbers are dataset-specific and which are structural, for example the six sheets and the 27 columns. |
| Before you start, "A run that produced only allele calls, with no haplotype analysis on top of them, is what LGE calls a genotype-only result" | I could not tell whether my own result is genotype-only. The only test offered is that a menu is missing, which is exactly the thing a new user would be confused by. | Name a visible marker in the window or the Inspector that says which kind of result is open. |
| Procedure Step 1, "Open the Inspector's **Genotype Display** section by clicking its disclosure triangle" | I do not know how to show the Inspector if the panel is not already there. The chapter says it is down the right side but never says how to open it. | Add the menu item or keyboard shortcut that shows the Inspector. |
| Procedure Step 1, "Scroll to the foot of the section, past the search fields, the numeric filters, and the colour controls" | The Settings section later names Alleles, Samples, Cell Color, Rows..., and Columns..., but the Procedure walks past them unnamed, so I could not tell whether I had scrolled far enough or was in the wrong section. | Name the last control above the Export block so the reader knows they have arrived. |
| Procedure Step 1, "Both sit at 0 by default, which means both are off" | I do not know what values to type. No starting number is suggested anywhere except buried inside a worked-example sentence. | State a suggested starting pair for a first export and say it is a starting point rather than a rule. |
| Procedure Step 1, "the export is where those visual filters finally become permanent in a file" | I read this twice. "Permanent" sitting next to a caption saying filters do not change calls felt contradictory until I worked out that permanent means permanent in the copy only. | Say the filters become fixed in the exported copy while the result is unchanged. |
| Procedure Step 1, "Choose a location outside the project folder" | This is the opposite of what Geneious taught me, where exports went back into the same document folder. The chapter gives a reason but not a consequence. | Say in one clause what goes wrong if the export lands inside the project folder. |
| Procedure Step 2, "The first two carry `Animal ID` and `GS ID`, the sample identifiers" | `GS ID` is never explained. I do not know what GS stands for or whether it is something I supply or something LGE generates. | Gloss `GS ID` at first use. |
| Procedure Step 2, "how many retained reads it contributed, alongside a Total and an Average" | The definition of a retained read arrives two sentences after the term, so I read the paragraph out of order hunting for the meaning. | Move the retained-read definition to the first mention. |
| Procedure Step 2, "the individual samples range from 713 to well over forty thousand" | I could not judge this. A sixty-fold spread between samples would have alarmed me in class, and the text does not say whether it is normal or a warning sign. | Say whether that spread is expected for an amplicon panel. |
| Procedure Step 2, "labelled `MHC-A Haplotype 1`, `MHC-A Haplotype 2`, and so on through eight loci" | Eight loci here, 13 loci in Step 3, 305 allele rows in Step 2, and a 970-row matrix in the Settings. Four counts for what felt like one result, and I could not reconcile them. | Say explicitly that the haplotype band, the reported loci, and the allele rows count different things. |
| Procedure Step 2, "The allele target `01_Mamu-A1_002g` carried a Total of 929 across 5 observations" | I could not follow the arithmetic on one pass. Two blanked single-read observations, a Total falling by 2, and an observation count falling by 2 look like the same number doing two jobs. | Spell out the subtraction, 929 minus two single-read values equals 927. |
| Procedure Step 3, heading "Export the matrix workbook, CSV, or TSV from the command line" | This is where the chapter stopped being usable for me. I have never opened a terminal, and this is a numbered Procedure step rather than the optional section at the end. Three of the four formats are simply unavailable to me. | Mark the step as command-line only in the heading, and say plainly that no dialog exists for these formats. |
| Procedure Step 3, "`Legend` decodes the fill colours, listing the tokens `M1` through `M7` with their hex colours" | `M1` through `M7` are never explained. I do not know whether M means a match tier, a mismatch count, or something MHC-specific. | Say what the M tokens mean, or point to the chapter that defines them. |
| Procedure Step 3, "two columns per locus, headed `H1` and `H2` for the two report slots each locus carries" | "Report slot" is a new term. I guessed it means the two alleles a diploid animal can carry at a locus, and if that guess is wrong every row I read is wrong. | Gloss "report slot" at first use. |
| Procedure Step 4, "use `lungfish-cli genotype export-labkey`" | The same wall as Step 3. There is no dialog for the LabKey export, and I only discovered that after reading three sections about why a data manager needs it. | Say at the top of the Procedure which steps need the terminal. |
| Settings, "Everything in this section lives in the Inspector's **Genotype Display** section, except the two export buttons and the workbook button" | I read this three times. Step 1 puts the Filtered Pivot button inside the Genotype Display section, so the exception contradicts the Procedure. | Split the exceptions out, naming which control sits in which panel. |
| Settings, "**Min percent.** ... it accepts a value from 0 to 100 in steps of 0.5" | Whether this is a percent of one locus or of the whole sample is decided by Percent Basis, which is a separate entry I might not read. | Give the default Percent Basis inside the Min percent entry too. |
| Settings, "**Alleles.** ... so typing `DRB` narrows a 970-row matrix to the DRB targets alone" | 970 rows appears nowhere else. Step 2 said 305 allele rows on the same Williams result, so I could not tell whether this is a different dataset or a different count. | Use the worked example's own numbers, or say this figure comes from a different result. |
| Settings, "**Samples.** ... On the command line this is `--sample`, which is repeatable" | I do not know what repeatable means for a command, and it is the only place the idea appears, inside a setting I would otherwise use from the window. | Drop the repeatable note here, or show it in the command-line section. |
| Settings, "**Show All Rows.** ... remembering that there is no automatic row limit for it to undo" | I did not understand this clause at all. It answers a question I did not have, about a limit the chapter never mentions. | Cut the clause, or say what limit a reader might have expected. |
| Settings, "**Update and View Current Excel Version.** Rebuilds the bundle's own `current.xlsx` workbook" | This is the one control that writes back into the result, and it appears near the end of a settings list rather than in the Procedure. Coming from Geneious, where exporting never alters the document, I would have pressed it without realising. | Warn about the write-back at the top of the Settings section, not only inside the entry. |
| Settings, "**Update and View Current Excel Version.** ... disabled while the workbook is already current or the bundle is read-only" | I do not know what makes a bundle read-only or how to tell. If the button is greyed out I would not know which of the two reasons applies. | Say how the window shows a read-only bundle. |
| Settings, "Eight further options exist only on the command line" | Eight entries follow that I cannot use, formatted exactly like the settings I can. Reading them cost me several minutes before I understood none had a dialog. | Move these eight into the command-line section at the end. |
| Settings, "**--view-projection.** Points at a JSON file describing a rendered viewport" | JSON is never glossed, and "rendered viewport" is not a phrase I met earlier. I could not tell whether this file is something LGE writes for me or something I write. | Gloss JSON, and say who produces the file. |
| Reading the results, "prints a JSON summary naming `matchedAlleleRows`, `filteredAlleleValueCount`, and `removedAlleleRowCount`" | These three checks are presented as the way to verify an export, but the summary only comes from a terminal command. From the Inspector button I get no summary at all, so I cannot run the check the chapter recommends. | Say what a window-only reader can check instead, for example opening the file and counting rows. |
| Reading the results, "The Williams exports all report 30, which is correct even though 7 of those samples were too thin to call much" | Seven of thirty samples failing is new information arriving in a verification section. I do not know whether that is a normal failure rate or a sign the run was bad. | Say whether 7 of 30 thin samples is expected, or drop the figure. |
| What good looks like, "Clear the Min reads and Min percent fields and export again" | Clearing suggests leaving the fields empty, but the Settings say 0 is the default that turns the filter off. I did not know which the app accepts. | Say set both to 0. |
| On the command line, "This section is optional. If you do your work in the LGE window, everything above is complete without it" | This is not true for this chapter and the very next paragraph admits it. Having read Steps 3 and 4, I already knew three formats need the terminal, so the reassurance read as an error. | Replace the boilerplate opening with the qualification that follows it. |
| On the command line, "The Inspector's Percent Basis defaults to Viewed Locus, while `--percent-basis` on the command line defaults to `sample-retained`" | A trap that appears only here. A reader who works in the window and hands a command to a colleague would never see it. | Repeat this mismatch in the Percent Basis setting entry. |
| On the command line, "`genotype export` fails when the output path sits under `/private/tmp` or any other directory reached through a symbolic link" | Symbolic link is not explained, and I would have no idea whether a folder I picked is one. | Gloss symbolic link, and name one common folder that is safe. |
| Next, "Return to Reading the Genotype Comparison to set the filters an export will capture" | The whole chapter depends on filters set in the previous chapter, but the dependency is only mentioned at the very end. I set my filters blind in Step 1. | Point at that chapter in Step 1, where the filters are first used. |

One thing I learned. The exported file is a frozen copy and the result bundle keeps changing underneath it, which is why the file you sent is the file your collaborator still holds.

One thing I still could not do. Export a CSV, a TSV, or the LabKey set, because all three exist only as terminal commands and the chapter puts them inside the numbered Procedure as though I could follow along.

The sentence I liked most. "A good export is boring."
