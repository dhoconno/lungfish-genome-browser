# Reader report, chapter 02-sequences/01-importing-and-viewing

Reader 3. Pre-med student, English is my second language, no terminal experience.

| Location | What stopped me | What would have helped |
| --- | --- | --- |
| What it is, "Lungfish Genome Explorer (LGE) keeps every..." | "the Finder shows as one icon" assumes I know the Finder is the Mac file browser and that a folder can pretend to be a file. I read this twice. | Say the folder opens like a file in the Mac file browser. |
| What it is, "The bundle holds a copy of..." | "the indexes that let LGE jump" uses "index" with no gloss. In my classes an index is a number in a list. | Gloss index at first use. |
| What it is, "A feature is a labelled stretch..." | "a start, an end, a strand, and a type" then "such as `gene` or `CDS`". CDS is never spelled out in the body even though it is in the glossary list. | Spell out coding sequence at first use of CDS. |
| What it is, "Strand means which of the two..." | I know DNA has two strands, but I do not know how a feature "is read from" one. Which direction is forward? | One sentence saying which strand counts as forward. |
| What it is, "A GFF3, GTF, or BED file is the opposite case." | Three format names appear at once and only GFF3 is linked. I do not know what GTF and BED are or why there are three. | Say all three are feature-only text formats and the differences do not matter here. |
| Why you would do this, "Chapter 1 imported the HBB gene..." | "works out the rest of the window around it" is unclear English to me. Works out means calculates in my usage. | Say the chapter covers the rest of the window. |
| Why you would do this, "The HBB gene record is `NG_000007.3`..." | "the human beta-globin cluster on chromosome 11" and RefSeqGene arrive together. I do not know whether a cluster is a group of genes or a region. | Say a cluster is a group of related genes sitting near each other. |
| Why you would do this, "It is 81,706 bases long and carries..." | Eight genes, five mRNAs, five coding sequences, thirteen exons is 31, and the text says 102 features. I could not judge whether the leftover 71 is normal. | Say the remaining features are descriptive and are not broken out here. |
| Why you would do this, "The rest are other GenBank feature types..." | I do not know what `misc_feature` or `regulatory` mean and cannot look them up inside the chapter. | Gloss the two types in half a sentence. |
| Before you start, "Download the file `NG_000007.3.gb`..." | The link goes to a GitHub folder page, not to a file. I have not used GitHub and did not know how to save a single file from that page. | Add the click path on GitHub for downloading one file. |
| Before you start, "Nothing here needs an external tool..." | "no plugin pack has to be installed and Docker is not involved" mentions Docker, which I have never heard of, only to say it is absent. | Drop Docker or gloss it. |
| Procedure, "Choose **File > Import Center...** (Cmd-Shift-I)." | Six tab names arrive in one sentence with no word about the five I will not use. | Say the other five belong to later chapters. |
| Procedure, "Drag `NG_000007.3.gb` from the folder..." | "compresses and indexes the sequence" repeats index without ever defining it, and compress worried me that something is thrown away. | Say compression only makes the file smaller and changes no bases. |
| Procedure, "Click that card's **Import...** button instead..." | Step 3 is an alternative to step 2 but is numbered as if it follows it. I started to do both. | Mark step 3 as an alternative rather than a next step. |
| Procedure, "Find the new bundle under `Reference Sequences/`..." | The track name Imported Annotations is stated but I am not told where in the window that name is shown, so I could not check it. | Say where the track name appears. |
| Attach a standalone annotation file, "Do this only when you have a GFF3..." | The whole section is optional but sits inside Procedure with numbered steps. I could not tell whether skipping it breaks the walkthrough. | State plainly that the HBB walkthrough skips this section. |
| Attach a standalone annotation file, "Fill in the Import Annotation Track alert..." | Step 3 points forward to Settings and Settings points back here. I had to jump twice to finish one step. | Describe the three fields inline. |
| Settings, "The Reference Sequences card has no settings." | "Two options exist on the command line only" left me unsure whether staying in the window costs me something important. | Say the two options only rename and relocate, so the window loses nothing. |
| Settings, "**Track ID.** Sets the stable identifier..." | "what other files inside the bundle refer to" is abstract. I could not tell what a collision looks like or how I would notice one. | Say LGE warns you when the identifier collides. |
| Reading the results, "The bundle is a folder that behaves..." | Control-click is Mac vocabulary. I use a mouse and did not know this is the same as right-click. | Say control-click is the same as right-click. |
| Reading the results, "A `manifest.json` sits at the root..." | Five folder and file names arrive as a list with no explanation of what a manifest is. | Say the manifest is a small text file listing what the bundle holds. |
| Reading the results, "The Inspector is a summary rather..." | "read what is there rather than looking for a row this manual promised" reads like an apology and left me unsure which rows I should expect. | Name the two or three rows that always appear. |
| The viewport, "The top lane is the position ruler..." | I do not know what a density rendering looks like. I could not tell whether an unreadable smear means I am zoomed out or the file is broken. | One sentence describing the zoomed-out drawing. |
| The viewport, "The colour comes from a per-type table..." | `mat_peptide` appears once with no gloss and is not in the glossary list at the top of the chapter. | Gloss mat_peptide or use a type already introduced. |
| The viewport, "On the HBB record that lane fills..." | "Around 102 features" is strange because 102 was given as an exact count earlier. I could not tell whether my count should match exactly. | Give a range that counts as normal. |
| Moving around the record, "**Sequence > Go to Location...** (Cmd-L) takes a coordinate." | The file is NG_000007.3.gb but the coordinate drops the `.3`. The explanation comes after the example, so I typed the wrong thing first. | Give the warning before the example. |
| Moving around the record, "the viewport frames the three bases..." | I know from chapter 1 this is the sickle cell codon, but nothing here says whether GAG is the healthy version or the disease version. | Say GAG is the normal codon. |
| Moving around the record, "The editable position field at the left end..." | The placeholder `chr:start-end` uses chr, which does not match NG_000007, so I thought I had to type chr literally. | Say chr stands for whichever contig name your bundle uses. |
| Right-click actions, "A **Copy** submenu holds **Copy Name**..." | Complement and reverse complement are only distinguished much later in the translation section. Here they are three near-identical menu items. | Gloss complement where the menu items are listed. |
| Right-click actions, "**Run FASTQ/FASTA Operation...**, which sends..." | The operations dialog is never described in this chapter, so I did not know what clicking would do. | Point to the chapter that covers the operations dialog. |
| Right-click actions, "Those items keep acting on the visible region..." | I read this three times. It warns about a surprising behaviour but is written as a statement about menu items. | Say the selection is ignored by those three items. |
| Translating a sequence to protein, "The standard code, table 1, covers..." | Four table numbers arrive in one sentence, and I do not know what a nuclear gene is as opposed to a mitochondrial one. | Keep table 1 and table 2 and say the app lists the rest. |
| In the app, "**Sequence > Translate...** (Cmd-Shift-T) is a different thing..." | Two commands share the word Translate and behave differently. "runs the translate operation on the active sequence" did not tell me what I get out. | Say the menu command writes a file and the toolbar button draws on screen. |
| In the app, "The `Color Scheme` picker sets how..." | Residue is used without gloss, and four colour scheme names arrive with only one of them explained. | Gloss residue as amino acid and say the default is fine. |
| Adding one annotation by hand, "The type menu offers `gene`, `CDS`..." | Nine types in one sentence, several of them never glossed. I could not choose one for my own selection. | Say which type to pick when you are unsure. |
| Auto-detecting open reading frames, "Translation holds `Codon table`..." | I do not know whether 100 nucleotides is short or long for a real gene, so I could not judge the default. | Say a typical human coding sequence is far longer than 100. |
| Auto-detecting open reading frames, "Options holds `Include partial ORFs`..." | Alternative start codons are never introduced. I only know ATG. | Say some genetic codes allow starts other than ATG. |
| Transferring best-match CDS annotations, "Once you have mapped one reference's..." | Mapped and assembly are both unglossed here, and the section describes something I cannot do yet. | Mark the section as forward-looking and name the chapter. |
| What good looks like, "Four checks are worth running..." | The GAG check sentence has two clauses and I had to reread it to see that failure means different bases, not an error message. | Split it into two sentences. |
| On the command line, "This section is optional. The window does everything..." | I have never opened a terminal, so I cannot delete a track at all, and the chapter does not tell me what to do instead. | Say to ask a colleague with terminal access. |
| On the command line, "lungfish-cli import fasta ~/Downloads/NG_000007.3.gb" | The `~` character and the trailing backslashes are unexplained. I would not know how to type this correctly. | One line saying `~` means your home folder. |
| On the command line, "`--start` and `--end` bound the search..." | 0-based, inclusive, and exclusive all in one clause. This is the hardest sentence in the chapter for me. | Give the 70544 against 70545 example first, then the rule. |
| On the command line, "`--min-query-cover` sets how much of a CDS query..." | Query, cover, and model transfer are three unglossed terms in one sentence about a command I cannot run. | Move this paragraph to the alignment chapter. |

The one thing I learned is that the bundle, not my original file, is what every later step in LGE points at, so I import a genome once and point everything afterwards at the bundle.

The one thing I still could not do is delete an annotation track, because the only route is the command line and I have never opened a terminal.

The sentence I liked most is "An ORF track is a set of candidates and not a set of genes, because ORF length is only a weak proxy for a real gene."
