# Reader report, undergraduate reader 2

Persona: a senior who has pipetted for two years and has never analyzed data. I have never opened a terminal.

| Location | What stopped me | What would have helped |
|---|---|---|
| What it is, "compares each one against a reference" | I do not know what a reference database physically is here. Is it a file I download, a website the app contacts, or something already inside the app? The chapter tells me much later that I install one, but at first read I pictured the internet. | Say in this sentence that the database is a file you install on your own machine. |
| What it is, "Every taxon sits at a taxonomic" | I had genetics but we never drilled the ranks. I can recite domain and species and I lose the middle. I could not tell whether family is above or below genus without rereading. | A one-line ladder with a worked example, human down to species, next to the list of rank names. |
| What it is, "When a read's sequence is shared" | I had to read this twice. "Steps back up the hierarchy" and "most specific taxon that all the matching organisms belong to" are two descriptions of the same move and I could not tell if they were one step or two. | Keep one of the two phrasings. |
| What it is, "A run reports a percentage of reads" | Percentage of what denominator. All reads in the file, or only the reads that got classified? This decides whether the percentages add to 100. | Name the denominator in that sentence. |
| Why you would do this, "a clinical amplicon dataset of 86,281" | I do not know what amplicon means here or why it matters that the example dataset is amplicon rather than something else. It is never glossed. | Gloss amplicon at this first use. |
| Why you would do this, "86,281 read pairs" | Read pairs, not reads. Nobody has told me what a pair is or whether 86,281 pairs is a big run or a tiny one. I cannot judge the number. | Say what counts as a typical run size so the figure means something. |
| What LGE runs, "reports how well each one is covered" | Covered is doing a lot of work and it is not explained until two sections later. On first read I thought it meant how many reads. | Move the coverage explanation to this first use or gloss it in half a sentence. |
| What LGE runs, "scores its calls for confidence" | I do not know what a call is. I guessed it means a reported organism but I was not sure. | Gloss call at first use. |
| What LGE runs, "assembles reads into longer sequences" | Assembly is new to me here and I do not know if that is something I would have to do or something NVD does on its own. | Say plainly that NVD does this for you outside LGE. |
| What LGE runs, "bring the output file into LGE" | Which output file. These pipelines produce folders of files in my experience of watching others work, and I would not know which one to pick. | Name the file type each import expects, or point to the chapter that does. |
| What LGE runs, "keep provenance for alongside" | Provenance is used three times in the chapter and never explained. I can guess it means a record of what was run but I am guessing. | Gloss provenance at first use. |
| Picking a classifier, "its Standard database covers archaea" | Standard is capitalised like a proper name and I could not tell whether it is a specific named download or just an adjective. Same for PlusPF. | Say these are the names of specific downloads in the Plugin Manager. |
| Picking a classifier, "the PlusPF builds, which the Plugin" | Build is a new word for a database version and I did not connect build, collection, and database as the same thing. The chapter uses all three. | Use one word for this throughout. |
| Picking a classifier, "matching short fixed-length words of" | I do not know what fixed length means in practice. Twenty bases, thirty? And I do not understand why a fingerprint of a k-mer is faster than the k-mer. | Give the actual k-mer length used and drop or expand the minimizer aside. |
| Picking a classifier, "it needs both Nextflow and a" | I have never installed a container runtime and I would not know if my Mac already has one. The chapter says its own chapter covers it, which leaves me unable to judge whether TaxTriage is realistic for me. | One sentence saying whether this is a self-service install or something IT does. |
| Where the classifiers live, "Select your reads in the project" | This is the only step that tells me to do something before opening the menu and it arrives after the menu instructions. I would have already opened the menu and found the dialog reporting nothing. | Put this instruction before the menu step. |
| Where the classifiers live, "the dialog reports the current selection" | I do not know what happens if I selected nothing or selected the wrong thing. Does Run grey out, or does it run on something unintended? | Say what the dialog shows when nothing is selected. |
| Where the classifiers live, "a multi-sample run appears as" | I do not know how I would start a multi-sample run. Nothing earlier said I could select more than one bundle. | Say whether selecting several bundles is how a batch starts. |
| Where the classifiers live, "kraken2-20260907-143005" | I worked out the date but not the second half. Is 143005 a time, and in what zone? | Say the pattern is date then time. |
| Where the classifiers live, "writes a .lungfishtax bundle into a" | The file extension appears once with no explanation and I do not know if I ever handle this file myself. | Say whether this is something the user ever opens directly. |
| What you will see, "columns for Sample, Taxon Name" | Six columns are named and only Reads, Direct, and the percentage are explained. I do not know what Sample holds when I ran one sample. | Explain the Sample column or say it repeats the input name. |
| What you will see, "A Bracken column joins them when" | Bracken appears here with no explanation and is never defined anywhere in the chapter, though it is listed as installed later. I do not know what abundance estimation is or whether I want it. | Gloss Bracken and abundance estimation at this first use. |
| What you will see, "A very large one is a prompt" | I cannot judge large. Ten percent unclassified, fifty, ninety? This is exactly the number I would be staring at after my first run. | Give a rough range for a typical clinical sample against a standard database. |
| Databases, "about half a gigabyte for the viral-only" | I can read the sizes but not the download times, and I do not know if a 72 GB download is an afternoon or a week on a normal connection. | Note that the largest ones can take hours. |
| Databases, "The Plugin Manager's Databases tab lists" | Thirteen databases, nine Kraken2. I do not know what the other four are for or whether one of them is EsViritu's, which I need if I run EsViritu. | Say which database EsViritu uses and whether it comes with the pack. |
| Databases, "(Cmd-Shift-B)" | I could not guess what B stands for and I mistrusted that I had read the shortcut correctly. Minor, but it made me stop. | No change needed beyond confirming it is correct. |
| What good looks like, "which the run's provenance record names" | Second use of provenance and I still do not know where to find this record. The chapter never says where to look. | Say where the provenance record appears in the interface. |
| What good looks like, "The run finished rather than stopping" | I do not know what a stopped run looks like in the Operations panel or what I would do about it. | Point to the chapter that covers a failed run. |
| Next, "Importing NAO-MGS Results (05-running-nao-mgs.md)" | The link text says Importing but the file name says running, and the chapter told me LGE never runs NAO-MGS. I could not tell which is right. | Make the link text and the target agree. |

Learned: classification asks which organism each read came from, and mapping asks where a read sits on a genome I have already named. That distinction was new and it is now clear.

Could not do: choose and install a database. I know I need one before anything runs, I know sizes range from half a gigabyte to 72 GB, and I still could not tell you which one I should pick for a swab or how long it would take to get it.

Liked most: "A hundred reads spread across a whole genome is a very different observation from a hundred reads stacked on one conserved gene, and only the first supports saying the virus is present."
