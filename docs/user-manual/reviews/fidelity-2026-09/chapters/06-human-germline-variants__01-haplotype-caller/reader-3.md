# Reader 3 report

Chapter: `docs/user-manual/chapters/06-human-germline-variants/01-haplotype-caller.md`

Reader: pre-med student, English is my second language. I know biology words
from my textbooks. I have never opened a terminal. Idioms and long sentences
slow me down.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| What it is, "the Broad Institute's variant caller" | I do not know what the Broad Institute is. It is not glossed and the sentence assumes I will recognize the name. | Say "an institute at MIT and Harvard" once in the same sentence. |
| What it is, "where the reads look unsettled" | "Unsettled" is a feeling word for people. I could not tell what property of the reads it describes. | Say "where the reads disagree with each other". |
| What it is, "This costs more time and it repays the cost around indels" | "Repays the cost" is a money metaphor. I read the sentence three times before I understood the extra time is worth it near indels. | Say "This is slower, and the extra time is worth it near indels". |
| What it is, "an aligner asked to place one read at a time" | "Aligner" is used here for the first time and is not glossed or linked, though "alignment track" is linked later. | Gloss it at first use as "the program that decides where each read belongs on the reference". |
| What it is, "reassembly resolves the disagreement" | I could follow the words but not the mechanism. Why does rebuilding from reads fix the disagreement that aligning one read at a time caused? | Add one sentence saying reassembly uses all the reads in the window together instead of one at a time. |
| What it is, "runs GATK inside a managed software environment" | "Managed software environment" is not glossed. I do not know if this is a folder, a virtual machine, or something I must set up. | Gloss it once as "a separate folder of programs LGE installs and keeps apart from the rest of your Mac". |
| What it is, "gives you the ten-subcommand GATK toolchain around it" | "Toolchain" is unglossed and "ten-subcommand" reads as jargon. I did not know whether this is a benefit or a warning. | Say "and gives you nine more related GATK commands". |
| What it is, "So what should you do with this?" | The question is addressed to me but it arrives without warning after a paragraph about routes, and I had to look back to see what "this" points at. | Replace with a plain lead-in such as "Choosing between the two routes." |
| What it is, "Turn on Show Experimental Features" | I cannot perform this step. The text names the setting and the pane but does not say what "experimental" means for me, or whether anything else in the app changes when I turn it on. | Add one sentence saying experimental features are unfinished and may change or fail. |
| Why you would do this, "a half-megabase window" | I know mega- and I know base, but "window" for a genome region is not glossed here, and 500,000 bases means nothing to me until I can compare it to something. | Add "about one six-thousandth of the human genome" or similar. |
| Why you would do this, "961 records over this window" | I cannot judge 961. Is that many variants for half a megabase, or few? The text tells me the number before it tells me how to read it. | Say what a normal per-megabase variant count is before giving 961. |
| Why you would do this, "whether the output looks plausible" | "Plausible" is a word I know from writing class, not from lab work. Here I could not tell what would make a variant list implausible. | Say "whether the output looks reasonable" and give one example of an unreasonable result. |
| Why you would do this, "compare counts against the benchmark rather than against intuition" | "Against intuition" is a figure of speech. I did not know what comparing against intuition would even look like. | Say "rather than against a guess". |
| Before you start, "Download raw file button" | I could not perform this from the text alone. On the GitHub page I did not know whether the button is at the top of the file preview or elsewhere, and there is no screenshot of it. | Say where on the page the button sits, or add a screenshot. |
| Before you start, "map them with Tools > Mapping > minimap2..." | This is one clause pointing at a whole other chapter, but the chapter says the alignment is what I actually need. I could not tell how long that detour is or which settings to choose there. | Say "this takes about N minutes with the default settings" and name the one setting that matters. |
| Before you start, "A loose BAM in a folder cannot be called from in the window." | I had to read this twice. "Loose" for a file is an idiom I do not know, and "called from in the window" has two prepositions together that broke my parsing. | Say "The dialog cannot use a BAM file that sits on its own in a folder." |
| Before you start, "Three things about the alignment matter to GATK specifically, and two of them will stop a run cold." | "Stop a run cold" is an idiom. Also the paragraph never labels which two of the three are the stopping ones, so I had to work it out myself. | Say "two of them will make the run fail" and number the three items. |
| Before you start, "reading `SM:HG002` on this fixture" | "Fixture" is used many times in this chapter and never glossed. I guessed it means the practice dataset but I was not sure. | Gloss it at first use as "the practice dataset this chapter uses". |
| Before you start, "a first `--execute` run against a bare FASTA fails" | `--execute` appears here for the first time and is only explained much later in the command-line section. As a reader who never uses a terminal I did not know if this sentence applies to me. | Say "if you use the command line" at the start of the sentence. |
| Before you start, "the alignment should also be shotgun rather than amplicon data" | Shotgun and amplicon are both unglossed. My textbook mentioned shotgun sequencing but I could not tell whether the reads I made in Step 1 are shotgun or amplicon. | Gloss both in one clause and say which one the fixture is. |
| Before you start, "Both packs are marked experimental, and that has a consequence on the command line worth knowing before you hit it." | Long sentence, and "before you hit it" is an idiom for meeting a problem. I read it twice. | Split into two short sentences and drop the idiom. |
| Before you start, whole paragraph on packs | This paragraph mixes a failing command with a working command and a GUI route, and I could not tell which single thing I should do. It is also the last paragraph before Step 1, but Step 2 calls it "Step 0". | Give one recommended route for GUI readers and put the command-line note after it. |
| Step 1 heading, "on an analysis-ready track" | "Analysis-ready" is defined one paragraph later, after it appears in the heading and in the first sentence. | Gloss it in the heading paragraph or move the definition up. |
| Step 1, "If another operation currently holds the bundle" | "Holds the bundle" is a metaphor. I did not know whether this means another window is open or a background job is running. | Say "if another operation is still running on that bundle". |
| Step 2, "If the row is greyed out and badged 'Requires GATK Core Pack', the pack is not installed and Step 0's Plugin Manager route is the fix." | There is no Step 0 in this chapter. The steps start at Step 1. I scrolled up and down looking for it. | Point to the "Before you start" section by name. |
| Step 2, "which fills itself in as the track name, a bullet, and the tool name" | "A bullet" as a character name is not obvious to me. I thought it meant a list bullet in the UI until I read the Settings section. | Show the actual generated name once, for example `hg002-minimap2 - GATK HaplotypeCaller`. |
| Step 2, "The Thresholds fields the GATK tools ignore" and the paragraph after | I read this three times. The chapter says the fields are drawn, do not reach GATK, are recorded with the run, and are ignored. I could not tell whether typing in them is harmless or harmful. | State plainly in one sentence that typing there changes nothing about the result. |
| Step 2, "Typing 0.20 into Minimum Allele Frequency ... does not make GATK apply a 20 percent floor." | "Floor" for a threshold is a metaphor I had to guess at, and it is used again in the Settings section without ever being explained. | Say "minimum" instead of "floor". |
| Step 2, "GATK decides which alleles to emit from its own genotype likelihoods" | "Genotype likelihoods" is a statistics term I have not met. It is not glossed and it is the reason the fields are ignored, so I could not follow the reason. | Gloss it as "GATK's own probability that each genotype is correct". |
| Step 3, "writes the VCF to `variants/gatk/<track-id>.vcf.gz` inside the bundle, loads the rows into a small SQLite database beside it" | One sentence carries five actions plus a file path plus SQLite, which is unglossed. I had to read it twice and I still do not know if SQLite matters to me. | Split into two sentences and say the database is internal and I never open it. |
| Step 3, "One default separates this run from its command-line twin, and it is the default most likely to surprise you later." | "Command-line twin" is a metaphor and the sentence promises a surprise without saying what to do about it. I read it twice. | Say "The dialog and the command line use different defaults for one setting." |
| Step 4, "The table drawer slides up from the bottom of the viewport" | "Viewport" is not glossed in this chapter and there is no screenshot of the drawer here, so I could not confirm I was looking at the right thing. | Gloss viewport at first use, or add a screenshot marker. |
| Step 4, "the Presets button above the table reveals the filter chips" | "Filter chips" is unglossed UI vocabulary. I do not know what a chip looks like. | Gloss as "small buttons that switch a filter on and off". |
| Step 5, "The dialog builds the two-step plan correctly and then no part of the app reads it, so the launcher finds nothing to run and falls through to the alert." | Very long sentence with three clauses about internal behavior. "Falls through" is an idiom. I read it twice and I do not think I needed the internal detail. | Say "this entry does not work in the window, and the command line is the working route". |
| Settings, "draw the same five controls" | "Draw" for showing a control is programmer usage. I read it as drawing a picture first. | Say "show the same five controls". |
| Settings, Alignment Track, "prefer a whole-genome or exome alignment over an amplicon one" | Exome and amplicon are unglossed here and in the "Before you start" section. I could not tell what my fixture is. | Gloss both and say the fixture is whole-genome. |
| Settings, Alignment Track, "so the dialog does not preselect whichever track you arrived from" | I read this twice. The "so" makes it look like a consequence of the default, but it reads more like a warning. | Split into a separate sentence starting "Note that". |
| Settings, Minimum Allele Frequency, "a decimal between 0 and 1 or a blank field is allowed" | I can read the rule but the paragraph also says the value is ignored, so I could not judge what a good value is or why a rule exists at all. | Say that the allowed range only affects what is recorded, not the result. |
| Settings, Minimum Depth, "The default is 10" | I cannot judge 10. The chapter later says under 10 is too thin, but at this point I do not yet know what depth values are normal. | Give the typical depth of this fixture here, not only in "Reading the results". |
| Settings, Extra arguments, "or to change ploidy on a sample that is not diploid" | Ploidy and diploid appear before the `--ploidy` entry that explains them. I know diploid from genetics but not what changing it in software does. | Move the one-line gloss up, or point forward to `--ploidy`. |
| Command-line only settings, opening line "Nine more settings exist on `lungfish-cli gatk haplotype-caller`" | I have never opened a terminal and the chapter does not say until much later that this section is optional. I spent effort here believing I needed it. | Put the "This section is optional" line at the top of the first command-line section. |
| `--pcr-indel-model`, "how much insertion and deletion noise to expect from the library's PCR step" | "Library" in the sequencing sense is not glossed anywhere in this chapter, and "noise" is a metaphor. | Gloss library once, and say "false insertions and deletions" instead of noise. |
| `--pcr-indel-model`, "where the conservative model would otherwise discard real indels as amplification artifacts" | I could not judge whether my fixture is PCR-free. Nothing earlier says how the HG002 reads were made. | Say in "Before you start" whether the fixture library is PCR-free. |
| `--stand-call-conf`, "The default is 30.0" | I cannot judge 30.0. The units are never named and the later QUAL discussion gives a mean of 903, which made 30 look tiny without explanation. | Say the score is the same QUAL scale and that 30 is deliberately permissive. |
| `--max-alternate-alleles`, "high enough that ordinary human sites never reach it" | "Sites" is used here for genome positions without gloss, and "reach it" left me unsure whether reaching the cap is bad. | Say "positions" and state what happens if the cap is reached. |
| `--pair-hmm-threads`, "Sets how many threads the read-to-haplotype scoring step uses" | "Threads" and "pair HMM" are both unexplained. I have never opened a terminal and do not know what a thread is. | Gloss threads once as "how many calculations run at the same time". |
| `--pair-hmm-threads`, "Raise it on a machine with cores to spare." | "Cores to spare" is an idiom and I do not know how to find out how many cores my Mac has or how many are spare. | Say how to check, or give a safe rule such as "half the cores your Mac reports". |
| `--dry-run`, "since `--dry-run` wins whenever both are present" | "Wins" is a metaphor for precedence. I understood it only on the second read. | Say "`--dry-run` takes priority". |
| `--threads` on the phased route, "It does not work in this release." | I had to read the whole paragraph twice because it first documents the setting and then says it is broken. The order surprised me. | Lead with "This setting is broken in this release." |
| Reading the results, "1,026 rows, of which 843 are SNVs ... and 184 are indels" | 843 plus 184 is 1,027, not 1,026. I checked twice because I thought I had misread. | Fix the arithmetic or say what the extra category is. |
| Reading the results, "589 rows read `0/1` ... 418 read `1/1` ... 19 read `1/2`" | 589 plus 418 plus 19 is 1,026, which I could check, but then "Roughly three heterozygous sites for every two homozygous ones" does not match 589 to 418, which is closer to seven to five. | Recheck the ratio or state it as "somewhat more heterozygous than homozygous". |
| Reading the results, "The mean QUAL across this fixture is 903" | I cannot judge 903 because the scale has no top and no units. The chapter says higher is more confident but never says what counts as high. | Give the scale's meaning, for example that 30 means a one in a thousand chance of error. |
| Reading the results, "A call in the low tens is one to look at before trusting." | The sentence is compressed and the object of "look at" is missing. I read it twice. | Say "Check a call whose QUAL is in the low tens before you trust it." |
| Reading the results, "38, against a mean of 44.7 across the whole aligned window" | I could not judge why the variant positions have lower depth than the window average, and the chapter does not say whether that gap is normal or a warning. | Add one clause saying the small gap is expected. |
| Reading the results, "A GVCF looks nothing like this and the difference catches people out." | "Catches people out" is an idiom I did not know. | Say "and the difference often confuses people". |
| Reading the results, "48,057 rows, because 46,858 of them are reference blocks" | 46,858 plus 1,199 is 48,057, which checks, but the chapter says only 1,199 rows "carry a real alternate allele" while the plain VCF had 1,026. I could not explain the difference between 1,199 and 1,026. | Say why the GVCF has more variant rows than the plain VCF. |
| Reading the results, "the upright bar says WhatsHap worked out which copy of the chromosome each allele sits on" | "Worked out" is a phrasal verb that slowed me, and "upright bar" is described but never named as the pipe character. | Say "the vertical bar, `|`, means WhatsHap determined which copy". |
| Reading the results, "The 216 heterozygous calls that stayed `0/1` are the ones no read spanned alongside a neighbour." | Long inverted clause. I read it three times and I am still not certain what "spanned alongside a neighbour" means. | Say "no single read covered these variants together with a nearby one". |
| What good looks like, "a HaplotypeCaller run returning 1,026 is the right order of magnitude" | "Order of magnitude" is a physics phrase. Since 1,026 and 961 are close, using it made me doubt my reading. | Say "close enough to the benchmark". |
| What good looks like, "A run returning a few dozen rows, or tens of thousands, has gone wrong somewhere upstream." | "Upstream" is pipeline jargon and is not glossed. | Say "in an earlier step". |
| What good looks like, "should sit somewhere near half of all calls" | Earlier the chapter said roughly three to two, which is 60 percent, and here it says near half. The two guidelines disagree and I did not know which to use. | Give one number and use it in both places. |
| What good looks like, "a run where nearly everything is heterozygous usually means two samples are mixed in one BAM" | I cannot act on this. The chapter tells me the cause but not how to check it or what to do next. | Point to the chapter or step that would confirm sample mixing. |
| What good looks like, "so the stderr GATK produced is recorded there" | "stderr" is never glossed and appears twice. I have never opened a terminal and do not know what it is. | Gloss it as "the error messages the tool printed". |
| What good looks like, "Read the provenance record before you rely on a number." | I could not perform this. The chapter never says where the provenance record is or how to open it from the window. | Say how to open it, for example by right-clicking the Operations panel row. |
| On the command line, "Every `lungfish-cli gatk` command builds and prints a GATK command without running anything." | I had already read the whole Settings section believing I needed the command line. The optional note comes only here. | Move "This section is optional" to the first mention of the command line. |
| On the command line, "That is the habit worth keeping, because reading the command back before it runs is the point of the wrapper." | "Wrapper" is unglossed programmer vocabulary and "reading the command back" is unusual phrasing. I read it twice. | Gloss wrapper once, or say "the point of running it through LGE". |
| On the command line, first code block | I could not perform any of this. The block assumes I know how to open a terminal, where my files are, and how the working directory relates to the filenames used. | Add one line saying which chapter or appendix explains opening a terminal. |
| On the command line, "gatk CreateSequenceDictionary -R ..." | This command starts with `gatk`, not `lungfish-cli`, so it must come from somewhere else. Nothing says how `gatk` becomes available after installing the pack. | Say how to run a pack tool directly, or give the `lungfish-cli` equivalent. |
| On the command line, "bcftools view -H HG002.chr20.vcf.gz | wc -l" | `bcftools` and `wc` appear with no explanation and are not in the packs the chapter told me to install. | Say where bcftools comes from, or mark these lines as optional extras. |
| On the command line, "Those are GATK's own argument names, and LGE promotes them under its own." | "Promotes them under its own" is unusual and I read it three times. I still had to guess the meaning. | Say "LGE gives them its own names". |
| On the command line, "a string it cannot split is rejected before anything runs" | I could not tell what kind of string cannot be split, so I could not avoid the error. | Give one example of a string that fails. |
| On the command line, "the phased VCF is written without an index, so a tool that needs one wants `tabix -p vcf` run over it first" | "Wants" here means "requires", which read strangely to me, and `tabix` is a new tool with no source given. | Say "you must run `tabix -p vcf` on it first" and say where tabix comes from. |
| Next, "which is the step that makes the GVCF default worth having" | Long trailing clause on an already long sentence. I read it twice. | Split into two sentences. |

Three closing lines.

The one thing I learned. HaplotypeCaller rebuilds the sequence in a small
region from the reads themselves instead of reading the reference one position
at a time, and that is why it handles insertions and deletions better.

The one thing I still could not do. I could not judge any of the quality
numbers. QUAL 903, depth 38, and the 30.0 default all arrived without a scale I
could place them on.

The sentence I liked most. "Here you can ask whether it is right, because 961
rows of truth sit next to it."
