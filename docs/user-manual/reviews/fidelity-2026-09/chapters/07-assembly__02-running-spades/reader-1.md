# Reader 1 report — Running SPAdes

Persona: sophomore, one genetics course, no lab time, never opened a terminal, never used sequencing software.

| Location | What stopped me | What would have helped |
|---|---|---|
| What it is — "SPAdes is a program that" | The chapter never says what SPAdes stands for or how to say it. I read it as the card suit and was not sure that was right. | Expand the name once and say how it is pronounced. |
| What it is — "two points are joined whenever" | "one k-mer's tail overlaps the next one's head" was the sentence I read three times. With k=3 I could not work out how much of the tail has to overlap. | Show the overlap on the ACGTA example already given. |
| What it is — "SPAdes prunes the forks that" | "prunes" is used as a technical action and never glossed. I guessed it means deletes, but deleting real data sounded alarming. | Say it discards those paths. |
| What it is — "actually runs the whole procedure several times at several k values" | This told me k can be changed but never says what value is used, whether I pick it, or what a bigger k does. | Name the default k behaviour and say you do not set it. |
| What it is — "LGE writes a `.lungfishref` assembly bundle" | The extension reads like "reference" but the thing is an assembly. I could not tell whether that was a typo. | One clause saying assemblies and references share the bundle format. |
| What it is — "shows each contig's length, GC percent" | GC percent is used here, several sections before it is explained under What good looks like. | Gloss GC content at this first use. |
| Why you would do this — "and completely blind to anything the reference does not contain" | I had to read this twice to see that "blind" describes the mapping approach and not my sample. | Split it into two sentences. |
| Why you would do this — "shows up plainly as an unexpected contig and only obliquely as a coverage anomaly on a mapped alignment" | Structural change, insertion, coverage anomaly and mapped alignment all arrive unglossed in one sentence, and coverage is not defined until the results section. | Gloss coverage here, or drop the alignment half of the comparison. |
| Why you would do this — "The fixture holds 9,958 such pairs." | I have no way to judge 9,958. Later the chapter warns that a count "in the low thousands" is too thin, which made me think 9,958 was borderline. | State the resulting coverage depth and say it is ample. |
| Why you would do this — "The fixture holds 9,958 such" | "fixture" is used here for the first time and never explained. I thought it meant lab equipment. | Say a fixture is the sample data set the manual supplies. |
| Why you would do this — "This chapter assembles the HG002 mitochondrial reads" | HG002 is never explained. I could not tell whether it is a person, a cell line, or a file naming convention. | One clause saying HG002 is a widely used reference human sample. |
| Before you start — "click the green Code button, choose Download ZIP" | The ZIP is the whole repository. I did not know how big it would be or how long it would take, and I was not told I can delete the rest afterwards. | Say roughly how large the download is. |
| Before you start — "Import them into your project following" | I could not tell whether to import the two `.gz` files as they are or unzip them first. | Say gzipped files import as they are. |
| Before you start — "The pack pins SPAdes 4.3.0, MEGAHIT" | Installing five tools when I only need one made me hesitate, and nothing says how large the install is or how long it takes. | Say roughly how long Install All takes and how much disk it uses. |
| Before you start — "took 110.7 seconds on a fourteen-core Mac" | I do not know how many cores my laptop has or how to find out, so I could not turn this into an expectation for my own machine. | Say where the core count is shown, or give a range. |
| Procedure step 1 — "Click the bundle holding the HG002" | I did not know which sidebar folder the imported bundle lands in, so I did not know where to look for it. | Name the folder the import creates. |
| Procedure step 4 — "LGE sniffs the first FASTQ header" | "sniffs" is jargon dressed up as a plain word, and "FASTQ header" has not been explained in this chapter. | Say LGE reads the first line of the file. |
| Procedure step 4 — "when the answer is clear the row" | I could not tell how I would know the answer was clear, other than by seeing the caption, and nothing says what the picker looks like instead. | Say the row shows a dropdown when detection fails. |
| Procedure step 5 — "Leave the Profile popup on Isolate" | Isolate is used as a noun here and in Why you would do this ("A new isolate") without ever being defined. | Gloss isolate as a single organism grown from one source. |
| Procedure — "Run stays disabled until the pack" | I did not know how long the smoke test takes or what to do if it never passes. | Say roughly how long the check takes. |
| Procedure step 2 — "LGE first materialises the FASTQ files" | One sentence with materialises, derived bundle and preview all new to me. I could not tell whether my imported bundle is a derived one. | Say a freshly imported bundle is not derived, so this step is instant. |
| Procedure — the `Assembly Results` code block | The block reports five numbers and at that point I could judge none of them. The explanations arrive two sections later. | Point forward to Reading the results right under the block. |
| Procedure — "such as `spades-2026-09-07T05-03-41`" | The `T05-03-41` part is not explained. I read it as part of the date and could not parse it. | Say the letter T separates the date from the time. |
| Procedure — "There is no `Assemblies` folder." | I never expected one, so being told it does not exist made me think I had missed a step somewhere. | Drop it, or mark it plainly as a note for returning users. |
| Settings — "The sheet carries eleven controls for" | I counted thirteen bold entries in the section. I stopped to recount twice before giving up. | Make the count match the entries, or say which entries are not SPAdes controls. |
| Settings — "those closing sentences belong to the optional" | This sentence is doing three things at once and I read it twice. | Split it, or move the flag names into a small table. |
| Settings, Read Type — "one of Illumina short reads, ONT" | ONT, PacBio, HiFi and CCS are four unglossed names in one clause. | Gloss ONT and PacBio once as long-read instruments. |
| Settings, Profile — "Pick Meta for a metagenome" | Metagenome and enrichment are both new. I could not tell whether my sample is one. | Gloss metagenome as a mixture of organisms sequenced together. |
| Settings, Profile — "Plasmid when the small circular molecules" | Plasmid and prep both go unglossed, and I had to read the sentence twice to find its subject. | Gloss plasmid and simplify the sentence. |
| Settings, Threads — "as a slider running from 1" | I still do not know my core count or where LGE shows it. | Say the slider's top end is your machine's core count, shown on the slider. |
| Settings, Threads — "On Apple Silicon LGE lowers whatever" | I do not know whether my Mac is Apple Silicon, and `--no-hw-accel` is a command-line flag dropped into the window section I was told I could skip. | Say how to check the chip, or say this applies to all recent Macs. |
| Settings, Memory Limit — "It defaults to three quarters of" | I do not know how much memory my Mac has, so I could not predict the default or judge whether to change it. | Say the sheet shows the resolved number on the slider. |
| Settings, Memory Limit — "SPAdes stops with an error rather" | "swapping to disk" is unexplained, and I could not tell whether stopping with an error is the good outcome or the bad one. | Say stopping is the safe behaviour and name the fix. |
| Settings, Min Contig — "That gap between what the control" | I read this three times. I could not tell whether the control is broken, whether I should avoid it, or whether a fix is coming. | Say plainly that the stepper does nothing for SPAdes in this version. |
| Settings, Careful mode — "adds a mismatch and short-indel correction" | Indel is not glossed anywhere in the chapter, and "pass" as a noun was new to me. | Gloss indel as an insertion or deletion. |
| Settings, Careful mode — "Turn it on for a small" | This says the chapter's own example would benefit from Careful mode, but step 5 told me to leave every setting alone. I did not know which advice to follow. | Say the first run leaves it off deliberately, and why. |
| Settings, Extra arguments — "such as SPAdes' own `--viral` pipeline" | Both examples are things I have no way to evaluate, and flag syntax means nothing to me in the window section. | Drop the examples, or move them to the command-line section. |
| Settings, Extra arguments — "LGE pins `--min_count 2` on every" | I do not know what min_count counts or why 2, and this is the longest sentence in the chapter. I lost the thread halfway. | Say what the number is a minimum of, and split the sentence. |
| Settings, Project Name — "with the mate suffixes `_R1`, `_R2`" | I had to read this twice to work out what the resulting name would actually be. | Show the resulting name for the fixture. |
| Settings, Run Mode — "It offers Run separately per bundle" | "which is the setting" stopped me completely. I could not parse it. | Say the first option is the only one available. |
| Reading the results — "a stored layout preference offering three" | Three names with no explanation of what leading means, and no way to tell which one my window is showing. | Say leading means that pane sits first. |
| Reading the results — "For the SPAdes run on the" | Nine values in one sentence with no punctuation to hold onto. I read it twice and still lost my place. | Use the same list layout as the code block above. |
| Reading the results — "a bacterial isolate coming back as" | The 5 Mb is introduced only in the second half, so on first reading I did not know the two examples were the same genome. | State the genome size before the first example. |
| Reading the results — "a filter field above them narrows" | The table has a `Contig` column but no header column, so I could not tell what "header" refers to. | Say the header is the contig's name line from the FASTA file. |
| Reading the results — "There is no coverage column anywhere" | Being told what is missing, for a number I had not yet learned to use, made me think I had lost something. | Move the coverage gloss ahead of the statement of absence. |
| Reading the results — "where the `cov_121.957333` field says SPAdes" | I could not tell whether 122-fold is good, and the chapter has just said coverage is useful without saying how to judge it. | Say what depth range is healthy for a small genome. |
| Reading the results — "Right-clicking a row adds Extract Sequence..." | MAFFT is a name I have never seen and it is not glossed. | Gloss MAFFT as an alignment program, or point to its chapter. |
| Reading the results — "There is no double-click shortcut for" | Same problem as the Assemblies folder line. It answers a question I had not asked. | Fold it into the Create Bundle sentence. |
| What the three assemblers gave — table header "Global GC" | The summary strip called it "whole-assembly GC percent" and the code block called it "GC content". Three names for one number. | Use one name throughout. |
| What the three assemblers gave — "labelled its contig `Contig_1_257.173_Circ [topology=circular]`" | The number 257.173 is never explained, though the equivalent number in the SPAdes name was. I assumed coverage but could not confirm. | Say the number is SKESA's coverage estimate. |
| What the three assemblers gave — "SPAdes and MEGAHIT both overshot by" | I understood circular and linear separately but had to read this twice to see why the ends repeat. | Add a clause saying the closing overlap is written out twice. |
| What good looks like — "the fixture's 16,697 bases against a" | The margin is never stated as a number or a percentage, so I could not apply the rule to a genome of my own. | State the acceptable margin. |
| What good looks like — "Many short contigs and no dominant" | "Many" and "dominant" are not quantified, so I could not tell whether MEGAHIT's three contigs count as a problem. | Give a rough threshold, or say three contigs is fine. |
| What good looks like — "A contig whose GC percent sits" | "far" is never quantified, and I do not know the expected GC of anything except this fixture. | Say how many percentage points count as far. |
| What good looks like — "if the count is in the" | This seems to contradict the fixture, which succeeded on 9,958 pairs for a small genome. I could not reconcile the two. | Tie the threshold to genome size rather than to a flat count. |
| What good looks like — "Open the FASTQ bundle and read" | The Inspector has not been mentioned in this chapter and I did not know how to open it. | Say where the Inspector is, or point to its chapter. |
| On the command line — "The whole procedure runs headless, meaning" | I have never opened a terminal. I could not tell whether `lungfish-cli` already exists on my machine or whether I must install it. | Say the section assumes the CLI is installed and point to where that is covered. |
| On the command line — the trailing `\` in the code block | I did not know what the backslashes at the ends of lines do, or whether I should type them. | One clause saying the backslash continues one command across lines. |
| On the command line — "with `--paired` binding exactly two of" | Forward and reverse mates appear here under those names for the first time. Earlier the chapter said "two mate files". | Use one term for the pair throughout. |
| On the command line — "Aliases are `-o` and `--output-dir`." | Alias is not glossed. I could not tell whether all three must be typed or any one of them. | Say the three spellings are interchangeable. |
| On the command line — "`--profile` is accepted for SKESA and" | A flag that is accepted and then ignored sounded like a bug. I could not tell whether my command had failed. | Say the run still succeeds and the flag simply has no effect. |
| Next — "Continue to Running Flye or Hifiasm" | Long-read is used as a category here and in Settings, but the chapter never says what makes a read long or why it needs different tools. | Gloss long reads once, early. |

One thing I learned: N50 is a length, not a count, and on a one-contig assembly it tells you nothing.

One thing I still could not do: judge whether my own library has enough reads, because the chapter's only threshold is "low thousands" and the fixture succeeds on 9,958 pairs.

The sentence I liked most: "Each walk it completes becomes one contig."
