# Reader 2 report, Running SPAdes

Persona. Senior, two years of wet lab, library prep and PCR are second nature, never analyzed data, never opened a terminal.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "SPAdes is a program that takes sequencing reads" | The chapter never says what SPAdes stands for or how to say it. I said "spades" like the card suit for the whole chapter and still do not know if that is right. | Give the expansion and the pronunciation once at first mention. |
| What it is, "reconstructs the longer stretches of DNA" | I could not tell whether "longer" means longer than a read, or as long as the whole genome. That gap made the rest of the paragraph vague. | Say longer than a single read and usually far shorter than a whole chromosome. |
| What it is, "the read `ACGTA` yields the 3-mers" | The k-mer example is the clearest thing here, but it never says what k SPAdes actually uses, so I could not connect the toy example to my run. | Name a typical k value used on Illumina reads. |
| What it is, "two points are joined whenever one k-mer's tail overlaps the next one's head" | Read three times. "Tail" and "head" are not defined for a k-mer, so I did not know how many bases overlap. | Say the last k minus 1 bases match the next k-mer's first k minus 1 bases. |
| What it is, "runs the whole procedure several times at several k values and merges the answers" | If it runs at several k values, and I never choose k, I could not tell whether k is something I am ever responsible for. | Add a clause saying LGE never asks you to set k. |
| What it is, "writes a `.lungfishref` assembly bundle" | The extension reads "ref" but the thing is an assembly, and I could not tell whether an assembly bundle and a reference bundle are the same object. The end of the chapter also talks about deriving a reference bundle from a contig, which made it worse. | State once that assembly bundles and reference bundles share the `.lungfishref` extension. |
| Why you would do this, "only obliquely as a coverage anomaly on a mapped alignment" | Coverage is used here and only defined much later under Reading the results. I did not know what a coverage anomaly would look like. | Move the coverage gloss to its first use. |
| Why you would do this, "The fixture holds 9,958 such pairs" | The word "fixture" is used without explanation and I read it as a lab fixture at first. I also could not judge whether 9,958 pairs is a lot or a little for a 16.5 kb genome. | Gloss fixture as the practice dataset, and say what depth 9,958 pairs gives here. |
| Before you start, "choose **File > New Project** (Cmd-N), or click Create Project" | It does not say what to name the project or whether the folder should be empty. I stalled deciding where to put it. | Add one sentence naming a folder and saying it should be a new empty one. |
| Before you start, "GitHub offers no download for a single folder" | I have never used GitHub. The ZIP is the whole repository, which I expected to be enormous, and nothing says how big it is or that most of it is safe to ignore. | State the download size and say to keep only the two FASTQ files. |
| Before you start, "The two files you need are `HG002.chrM_R1.fastq.gz`" | It never says whether to unzip the `.gz` files. In lab we always thaw things before use, so I hovered here. | Say to import them still compressed. |
| Before you start, "which groups the two mate files into one bundle" | "Mate files" and "bundle" both arrive without a gloss. I know mate pairs from library prep, but not that R1 and R2 files are called mate files. | Gloss mate files as the two files from a paired-end run. |
| Before you start, "look at the **Packs** tab, and click **Install All**" | It does not say how long installing the pack takes or that it needs internet. The Genome Assembly card sounds like it downloads five tools, which felt like a long unattended wait I was not warned about. | Give an approximate install time and note it needs a network connection. |
| Before you start, "The SPAdes run in this chapter took 110.7 seconds on a fourteen-core Mac." | I do not know how many cores my laptop has or how to find out, so I could not translate this into an expectation for me. | Say where to read the core count, or give a range for a typical laptop. |
| Procedure, "Your figures will land close to these rather than exactly on them" | "Close" is not quantified. Later the chapter is very precise about 16,697 versus 16,569, so I had no idea what size of difference should worry me. | Say what magnitude of difference is normal, for instance a few bases. |
| Procedure step 1, "Click the bundle holding the HG002 mitochondrial reads in the project sidebar" | After importing I did not know what the bundle row would be called, so I could not confirm I had clicked the right one. | Name the row text the import produces. |
| Procedure step 4, "LGE sniffs the first FASTQ header" | "Sniffs" and "FASTQ header" are both unexplained. I have never opened a FASTQ file and did not know it had headers. | Gloss the FASTQ header as the identifier line above each read. |
| Procedure step 4, "when the headers give no clear answer the row becomes a picker you must set" | If that happens to me, nothing tells me how to find out which chemistry my reads came from. | Point to where the read chemistry can be confirmed. |
| Procedure step 5, "Leave the **Profile** popup on **Isolate**" | Isolate is not glossed here. In micro lab an isolate is a purified colony, and I could not tell if that is the same meaning. | Gloss Isolate at this first use, not only in Settings. |
| Procedure, "Run stays disabled until the pack is installed and its smoke test" | Smoke test is glossed inline, which helped, but nothing says what to do if the smoke test fails. | Add one sentence naming the recovery step. |
| Procedure step 2, "LGE first materialises the FASTQ files, meaning it reconstructs the full read set on disk when the bundle you picked is a derived one" | Read twice. "Derived one" and "only stores a preview" arrive with no prior explanation and I could not tell whether my imported bundle is derived. | Say that a freshly imported bundle is not derived, so this step is instant. |
| Procedure step 2, "GC content    : 44.4%" | The sample output block appears before GC content is explained, so at that point the number meant nothing to me. | Add a pointer to Reading the results here. |
| Procedure step 2, "such as `spades-2026-09-07T05-03-41`" | The timestamp format with the `T` in the middle is unfamiliar and I misread it as part of the tool name at first. | Say the part after the tool name is the date and time. |
| Settings, "The sheet carries eleven controls for SPAdes" | I counted twelve bold entries in the section and could not reconcile that with eleven. That made me distrust the whole list. | Make the count match the entries, or say which entries are not counted. |
| Settings, "those closing sentences belong to the optional command-line section" | Read twice. The sentence is long and I could not tell whether I was being told to skip the last sentence of each entry or the whole section. | Split into two sentences. |
| Settings, Threads, "a slider running from 1 to the number of cores your Mac reports" | I still do not know my core count, so I cannot predict where the slider starts. | Say the default is already set for you and needs no action. |
| Settings, Threads, "On Apple Silicon LGE lowers whatever you pick to 2 and adds `--no-hw-accel`" | I do not know whether my Mac is Apple Silicon, and `--no-hw-accel` is raw command-line text in the middle of the window section I was told I could stay in. | Say how to check the chip, and move the flag text to the closing sentence. |
| Settings, Memory Limit, "defaults to three quarters of installed memory capped at 32 GB" | I do not know my installed memory and cannot judge whether the default is enough for my library. | Say the default is fine for a small genome like this fixture. |
| Settings, Memory Limit, "SPAdes stops with an error rather than swapping to disk" | "Swapping to disk" is unexplained. I could not tell whether stopping is better or worse than the alternative. | Gloss swapping in half a sentence. |
| Settings, Min Contig, "the stepper is shown and editable but the value never reaches the run" | This is the most alarming sentence in the chapter and it stopped me completely. A control that does nothing made me wonder which other controls also do nothing. | Say plainly that every other control on the sheet does take effect. |
| Settings, Min Contig, "it is recorded as one" | Recorded where? I had no way to look it up or check whether it had been fixed by the time I read this. | Name where the defect is tracked, or drop the clause. |
| Settings, Careful mode, "adds a mismatch and short-indel correction pass" | "Indel" is never expanded. I have heard it but was not certain it meant insertion or deletion. | Expand indel at first use. |
| Settings, Careful mode, "Turn it on for a small genome where per-base accuracy matters more than speed, which a mitochondrial or plasmid assembly usually is." | This directly contradicts step 5, which told me to leave everything alone, and the reference run that produced all the numbers did not use it. I could not decide what to do. | Say the reference run left it off and that is correct for a first pass. |
| Settings, Extra arguments, "an unclosed quotation mark for instance, blocks Run" | I would not know what a parser or an unclosed quotation mark looks like in this field, and the sentence is long enough that I lost the subject. | Shorten, and say the footer names the problem. |
| Settings, Extra arguments, "LGE pins `--min_count 2` on every SKESA run" | This is buried at the end of an entry about a field I was told to leave empty. I nearly missed a real behavior. | Move the SKESA note to its own line. |
| Settings, Project Name, "the mate suffixes `_R1`, `_R2`, `_1`, `_2`, and the `.lungfishfastq` extension stripped" | Read twice. I could not work out what the resulting name would actually be for my files. | Show the resulting name for the fixture. |
| Settings, Run Mode, "It offers **Run separately per bundle**, which is the setting, and a locked **Combine all inputs, run once**" | "Which is the setting" reads as a typo or a missing word. I read the sentence three times. | Rephrase to say only the first option is selectable. |
| Reading the results, "a stored layout preference offering three arrangements, detail-leading, which is the default" | I do not know what leading means for a pane, and nothing says where the preference is stored or how to change it. | Say which side detail-leading puts the pane on, and where to change it. |
| Reading the results, "N50 is the length of the contig you are holding when the running total first reaches half" | The definition itself is clear, but "you are holding" made me hunt for a physical list. I read it twice. | Replace holding with the contig you are on. |
| Reading the results, "a bacterial isolate coming back as 50 contigs with an N50 of 200 kb is tidy" | I cannot judge these numbers, because nothing says what the bacterial genome size was for the tidy case. The 5 Mb is only given for the bad case. | State the genome size for both examples. |
| Reading the results, "SPAdes encodes its own coverage estimate in the contig name" | Coverage is glossed one paragraph earlier as reads stacked over a position, but nothing says whether 122-fold is good, adequate, or excessive for this fixture. | Say what coverage range is healthy for a small genome. |
| Reading the results, "**BLAST Contigs**, **Copy FASTA**, **Export FASTA**, and **Create Bundle**" | BLAST and FASTA are both unglossed here. I have heard of BLAST but have never run it and did not know what pressing it would do or whether it sends my data somewhere. | Gloss BLAST and say where the search runs. |
| Reading the results, "Right-clicking a row adds **Extract Sequence...**, **Align with MAFFT...**" | MAFFT arrives with no explanation at all, and there is no pointer to a chapter that covers it. | Gloss MAFFT in half a sentence or link a chapter. |
| Three assemblers table, "| SKESA | 2.5.1 | 1.6s |" | SPAdes took 110.7 seconds and SKESA 1.6 seconds for nearly the same answer. Nothing tells me why I would ever pick the slow one, and the chapter is named after it. | Add one sentence saying what SPAdes buys for the extra time. |
| Three assemblers, "labelled its contig `Contig_1_257.173_Circ [topology=circular]`" | I could not tell what 257.173 is. Given the SPAdes example I guessed coverage, but the chapter never says. | Name the 257.173 field. |
| Three assemblers, "MEGAHIT additionally emitted two short contigs of 332 and 362 bases" | Nothing says what those two contigs are or whether I should be worried about similar extras in my own run. | Say what such short extras usually are. |
| What good looks like, "the fix is more reads rather than different settings" | I do not know how to get more reads without resequencing, which in my lab is weeks and money. The advice felt unactionable. | Say whether pooling existing runs counts, or that resequencing is meant. |
| What good looks like, "read its read count in the Inspector" | The Inspector has not been mentioned anywhere in this chapter and I did not know how to open it. | Say how to open the Inspector. |
| What good looks like, "if the count is in the low thousands for a genome of any size" | The fixture has 9,958 pairs, which sounds like low thousands to me, yet it assembled perfectly. I could not reconcile the rule with the example. | Tie the threshold to genome size rather than an absolute count. |
| On the command line, "The whole procedure runs headless, meaning with no window at all, by typing commands into the Terminal application." | I have never opened a terminal. The section says it is optional, which helped, but the code block still uses a backslash at the end of lines and I have no idea what that does. | Say the backslash continues one command across lines, or print it on one line. |
| On the command line, "`lungfish-cli assemble HG002.chrM_R1.fastq.gz`" | The command uses bare file names, so it only works if I am already in the right folder, and nothing says how to get there. | Say the command must run from the folder holding the two files. |
| On the command line, "`--extra-arg` | Adds one extra assembler argument and may be repeated" | `--extra-arg` and `--extra-args` differ by one letter and I read the row twice before seeing the difference. | Note the singular and plural difference explicitly. |
| Next, "[Extracting Contigs](04-extracting-contigs.md)" | This chapter is 07-assembly/02 and the next link is 04, so I looked for 03 and only found it in the first half of the sentence. Minor, but I stopped. | No change needed if 03 is genuinely long-read only, but consider naming both numbers. |

One thing I learned. N50 is a length, not a count, and on a one-contig assembly it tells you nothing.

One thing I still could not do. Decide, for my own library, whether the run had enough reads, because the read-count rule and the fixture's own read count point opposite ways.

The sentence I liked most. "Sort the contigs longest first, walk down the list adding up their lengths, and N50 is the length of the contig you are holding when the running total first reaches half the assembly's total bases."
