# Reader 3 report — Running SPAdes

Persona: pre-med student, English is my second language. Strong biology vocabulary from textbooks. Idioms, figures of speech, and long sentences slow me down. I have never opened a terminal.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is — "using nothing but the reads themselves" | I read this twice. "nothing but" is an idiom I had to translate. I first understood it as "the reads are nothing", which is the opposite. | Write "using only the reads". |
| What it is — "The way SPAdes gets there is worth understanding" | "gets there" is a figure of speech. There is no place in this paragraph, so I looked back for a location. | Write "How SPAdes does this is worth understanding". |
| What it is — "one k-mer's tail overlaps the next one's head" | Tail and head are anatomy words to me. The chapter never says tail means the last bases and head means the first bases. | Say "the last bases of one k-mer are the same as the first bases of the next". |
| What it is — "SPAdes prunes the forks" | Prune is a gardening word I know only as a dried fruit. Fork I know only as a utensil, and the earlier sentence says "the path forks" as a verb, so I met two different grammars of the same unknown word. | Write "SPAdes removes the branches". |
| What it is — "then walks the surviving unambiguous paths" | Three difficulties in one clause. "Walks" as a transitive verb, "surviving" as a metaphor for graph parts, and "unambiguous" undefined for a path. I could not picture the action. | Write "then follows each remaining path that has only one route through it". |
| What it is — "but the shape of it is right" | An idiom. I understood every word and not the sentence. Is the shape a property of the graph or of the explanation? | Write "but the description is accurate". |
| What it is — "the other two assemblers LGE ships for short reads" | "Ships" is a business idiom. In my textbooks ships carry cargo. I stopped to decide whether LGE sends the tools over the internet. | Write "the other two assemblers LGE includes". |
| What it is — "They differ in their presets and in a few defaults" | Preset and default are used as if I know them, and they are used as if they are two different things. I could not say what the difference is. | Gloss both at first use, or use one word for both. |
| What it is — "So what should you do with this?" | A rhetorical question addressed to me. I stopped and tried to answer it before reading on. | Write a statement instead. |
| What it is — "leave every setting alone the first time" | "Leave alone" is an idiom. Alone from what? I checked whether it meant delete or empty the settings. | Write "do not change any setting". |
| Why you would do this — "completely blind to anything the reference does not contain" | Blind is a metaphor about a method, not a person. I read the sentence twice to attach the blindness to mapping and not to me. | Write "and it cannot detect anything the reference does not contain". |
| Why you would do this — "An assembly has no such blind spot" | Second metaphor built on the first. "Blind spot" is a fixed English phrase I know only from driving. | Write "an assembly does not have this limitation". |
| Why you would do this — "shows up plainly as an unexpected contig and only obliquely as a coverage anomaly" | "Obliquely" is a rare word and it is contrasted with "plainly". Also this is the first mention of coverage in the chapter and it is not glossed here, only much later in Reading the results. | Replace "obliquely" with "indirectly" and gloss coverage here. |
| Why you would do this — "you believe the tube holds one organism" | I could not tell whether "the tube" is a specific tube already mentioned or any tube. Nothing before this sentence mentions a tube. | Write "your sample contains one organism". |
| Why you would do this — "The fixture holds 9,958 such pairs." | Fixture is used for the first time here with no gloss. I know the word only as a light attached to a ceiling. | Gloss fixture at first use as the practice dataset supplied with the manual. |
| Why you would do this — "9,958 such pairs" | I cannot judge this number. Is 9,958 pairs a lot or a little for a 16,569-base genome? The chapter later says thin data gives many small contigs, so the number clearly matters, but I have no scale. | State the approximate coverage this gives, for example about 120-fold. |
| Why you would do this — "large enough to be a real genome rather than a toy" | "A toy" as a metaphor for a dataset. I paused because a genome cannot be a toy. | Write "rather than an artificial example". |
| Before you start — "click the green Code button, choose Download ZIP, double-click the downloaded file to unpack it" | I could follow this, but the step before it gives a direct folder URL and then says it does not work. I read both paragraphs twice to learn that I should ignore the first link. | Give only the working instruction, and put the folder URL after it as a reference. |
| Before you start — "which groups the two mate files into one bundle" | Mate file is used here for the first time. Paired-end was glossed one section earlier as two reads travelling together, but nobody said those two files are called mates. | Say "the two files, called mates". |
| Before you start — "installs on demand into private conda environments" | "On demand" is an idiom, and I could not tell whose demand. Mine, or the tool's? | Write "installs when you first ask for a tool". |
| Before you start — "so one tool cannot break another" | "Break" is used loosely. In my language this means physical damage. I could not tell what one tool breaking another would look like. | Write "so one tool cannot interfere with another tool's software". |
| Before you start — "The pack pins SPAdes 4.3.0, MEGAHIT 1.2.9, SKESA 2.5.1, Flye 2.9.6, and hifiasm 0.25.0" | "Pins" is a specialised verb I do not know in this sense. The rest of the sentence explains the effect, so I guessed, but I was not sure. | Write "fixes the versions at". |
| Before you start — "took 110.7 seconds on a fourteen-core Mac" | I do not know how many cores my Mac has, or how to find out, so I cannot use this number to predict my own wait. | Say where the core count is shown, or give a range instead. |
| Procedure — "Your figures will land close to these rather than exactly on them" | "Land close to" and "land on" are idioms about numbers. I read this twice. | Write "Your numbers will be close to these but not identical". |
| Procedure step 1 — "Click the bundle holding the HG002 mitochondrial reads in the project sidebar" | I do not know what the bundle row looks like or what it is named after import. The next sentence says a paired bundle is one row, which helped, but I still could not identify the row by name. | Say what name the row carries, for example HG002.chrM. |
| Procedure step 4 — "LGE sniffs the first FASTQ header" | "Sniffs" is a metaphor from smell. I understood it only after reading the rest of the sentence. | Write "reads". |
| Procedure step 4 — "when the answer is clear the row is a locked label" | Locked label is app vocabulary I do not know. Is it text I cannot edit, or a control that is disabled? | Say it is text you cannot change. |
| Procedure step 4 — "When the headers give no clear answer the row becomes a picker you must set before Run turns on." | I could not perform this from the text. It does not say which value to choose, or how to find out which chemistry my reads came from. | Add one sentence saying to choose the platform your sequencing provider named. |
| Procedure step 5 — "The Settings section below documents all of them." | I understood, but the Settings section then describes eleven controls I was told to leave alone. I could not tell whether I was expected to read it before running or after. | Say the Settings section is a reference for later runs. |
| Procedure — "Run stays disabled until the pack is installed and its smoke test ... has passed" | Smoke test is glossed inline, which helped, but the sentence is 30 words long with the gloss inserted in the middle, so I lost the main clause. | Split into two sentences. |
| Procedure step 2 (Run it) — "LGE first materialises the FASTQ files, meaning it reconstructs the full read set on disk when the bundle you picked is a derived one that only stores a preview, then launches SPAdes" | This is the longest sentence in the chapter, about 60 words, with a gloss and a condition inside it. I read it three times and I still cannot say whether my own bundle is "a derived one". | Split into three sentences and say plainly that a freshly imported bundle is not derived. |
| Procedure step 2 — "so nothing scrolls away permanently" | "Scrolls away" is an idiom. I understood the reassurance but not the mechanism. | Write "so you can read the whole log later". |
| Procedure step 2 — "Total length : 16697 bp" | The published genome length appears much later as 16,569. Here I saw 16697 with no comma and no comparison, and I could not judge whether it was correct. | State the expected length beside the result. |
| Procedure step 2 — "There is no Assemblies folder." | This denies something I was never told existed. I went back to look for where the chapter had promised one. | Remove it, or say "unlike earlier versions". |
| Settings — "each entry ends with a short sentence naming the command-line flag ... so skip them if you are staying in the window" | I have never opened a terminal, so I must skip a sentence in every one of eleven entries. I could not tell reliably where each skippable sentence began. | Put the command-line sentences in a separate column or table. |
| Settings, Assembler — "as a row of segmented buttons" | Segmented buttons is Mac interface vocabulary I do not have. I did not know what to look for on screen. | Write "a row of connected buttons where only one is selected". |
| Settings, Profile — "assumes one organism sequenced at reasonably even depth" | Depth is used here with no gloss. Coverage is glossed much later and I did not know the two words describe the same thing. | Gloss depth here, and say it is the same as coverage. |
| Settings, Profile — "when the small circular molecules in a bacterial prep rather than the chromosome are the point" | "Prep" is an abbreviation and "are the point" is an idiom. The sentence also never says the word plasmid, although the option is called Plasmid. | Write "when you want the plasmids in a bacterial sample rather than the chromosome". |
| Settings, Profile (MEGAHIT) — "Move off Default when rare organisms in a mixture are what you are after." | "Move off" and "what you are after" are two idioms in one short sentence. | Write "Change this setting when you want to find rare organisms in a mixture". |
| Settings, Profile (MEGAHIT) — "Default is not a value you pass, because its internal id is empty and an empty id makes LGE omit the flag entirely" | This explains an internal implementation to a reader who was just told to skip command-line sentences. I could not tell whether it affects the window at all. | Move it to the command-line section. |
| Settings, Threads — "the number of cores your Mac reports" and "the smaller of that core count and 8" | I still do not know my core count, and now the default depends on it. I cannot predict what my slider will say. | Say the slider shows the chosen value so you can read it. |
| Settings, Threads — "One caveat for MEGAHIT, which is a genuine app behaviour rather than a typo." | Caveat is a rare word. Also this is not a complete sentence with a main verb, so I read it twice looking for what the caveat was. | Write "MEGAHIT behaves differently, and this is not an error in the manual". |
| Settings, Threads — "On Apple Silicon LGE lowers whatever you pick to 2 and adds --no-hw-accel" | I do not know whether my Mac is Apple Silicon, and the flag name means nothing to me in a section I was told is for the window. | Say how to check the chip in About This Mac, and drop the flag name here. |
| Settings, Memory Limit — "defaults to three quarters of installed memory capped at 32 GB" | Two conditions combined. I had to work out on paper what my own default would be, and I do not know my installed memory. | Say the slider shows the resulting number. |
| Settings, Memory Limit — "The number is a ceiling and not a reservation" | Ceiling and reservation are both metaphors, one from buildings and one from restaurants. I read this twice. | Write "This is the maximum allowed, and the memory is not set aside in advance". |
| Settings, Min Contig — "Is meant to drop contigs shorter than this length" | The entry begins with "Is", which has no subject, and "is meant to" signals immediately that it does not work. I read the whole entry twice trying to find out whether I should use it. | Start with "Min Contig drops contigs", and state the SPAdes exception in the second sentence. |
| Settings, Min Contig — "keeps thousands of unidentifiable fragments out of the contig table" | I cannot judge 500 as a threshold. Why 500 and not 200 or 1000? Nothing tells me how to choose. | Say the value depends on read length, and give the rule. |
| Settings, Min Contig — "the stepper is shown and editable but the value never reaches the run ... it is recorded as one" | I could follow that the control does nothing, but "it is recorded as one" gave me no way to check or to know when it will be fixed. | Say where the defect is recorded, or remove the clause. |
| Settings, Careful mode — "adds a mismatch and short-indel correction pass" | Indel is not glossed anywhere in the chapter and is not in the glossary list at the top. "Pass" as a noun meaning a stage is also new to me. | Gloss indel and replace "pass" with "stage". |
| Settings, Careful mode — "the pass costs noticeably more wall time" | Wall time appears here before it is used in the results section. I know clock time, not wall time. | Gloss wall time as the real elapsed time you wait. |
| Settings, Skip error correction — "off is right for a routine run, because correcting sequencing errors before graph building is most of what keeps spurious branches out of the graph" | A 35-word sentence with a double negative structure. Off is right, because correcting keeps errors out. I read it three times to be sure that leaving the switch off means correction still happens. | Split it, and say plainly that off means correction runs. |
| Settings, Extra arguments — "text the parser cannot read, an unclosed quotation mark for instance, blocks Run and prints the reason in the footer" | Parser and footer are both new words, and the example is inserted between the subject and the verb. | Gloss footer as the bottom strip of the sheet, and move the example to the end. |
| Settings, Extra arguments — "LGE pins --min_count 2 on every SKESA run unless you supply your own, because SKESA's high-coverage auto-escalation can otherwise return an empty assembly" | "Auto-escalation" is a compound I have never seen and it is not explained. I could not tell what escalates. | Explain in plain words that SKESA raises its own threshold when coverage is high. |
| Settings, Project Name — "with the mate suffixes _R1, _R2, _1, _2, and the .lungfishfastq extension stripped and _assembly appended" | A 40-word sentence describing a string transformation. I could not work out what my own default name would be from it. | Give the fixture's actual resulting name as an example. |
| Settings, Run Mode — "It offers Run separately per bundle, which is the setting, and a locked Combine all inputs, run once" | "Which is the setting" stopped me completely. The setting of what? I read the sentence four times. | Write "Only Run separately per bundle can be chosen". |
| Reading the results — "a stored layout preference offering three arrangements, detail-leading, which is the default, list-leading, and stacked" | Three option names with a parenthetical inserted in the middle, and none of the three names is explained. I could not picture any arrangement. | Say what each one puts where, or drop the names. |
| Reading the results — "N50 is the one you will meet most" | "Meet" for a number is a metaphor. Small, but I paused. | Write "N50 is the one you will use most often". |
| Reading the results — "the length of the contig you are holding when the running total first reaches half" | "Holding" is a metaphor and the definition depends on it. I read the N50 paragraph twice, then a third time. It is the clearest definition I have read, but the metaphor is what made me slow down. | Write "the length of the contig at which the running total first reaches half". |
| Reading the results — "which is what a one-contig assembly always gives you and which consequently tells you nothing you did not already know" | A 45-word sentence with two "which" clauses and a double negative at the end. | Split after "16,697 bases". |
| Reading the results — "50 contigs with an N50 of 200 kb is tidy and the same 5 Mb arriving as 5,000 contigs with an N50 of 1 kb is a stressed assembly" | I can read the numbers but "tidy" and "stressed" are informal judgements, and "stressed" describes a feeling. Also 5 Mb appears without having been introduced as the bacterial genome size. | Say "good" and "poor", and introduce 5 Mb as the genome size in the first half. |
| Reading the results — "N50 earns its keep on a fragmented assembly" | An idiom about wages. I did not know it. | Write "N50 is useful on a fragmented assembly". |
| Reading the results — "L50 is the same measurement counted the other way" | "Counted the other way" did not tell me the direction. I read the rest of the sentence to reconstruct the definition. | Write "L50 counts contigs instead of bases". |
| Reading the results — "There is no coverage column anywhere in this viewport." | Again a denial of something never promised. Coverage is then glossed in the next sentence, after being used three times earlier in the chapter. | Gloss coverage at its first use in Why you would do this. |
| Reading the results — "SPAdes encodes its own coverage estimate in the contig name, so you can read the depth off the header text" | "Read off" is an idiom and depth appears again without a gloss. Also I could not tell whether 122-fold is good. | Write "the number after cov_ is the coverage", and say what a good value is. |
| Reading the results — "all disabled until you select at least one contig" | I could not perform this. It does not say how to select a contig. Clicking a row is described in the previous paragraph for the detail pane, but I did not know it is the same action. | Say clicking a row also selects it. |
| Reading the results — "There is no double-click shortcut for this." | Third denial of a feature never promised. Each one makes me wonder what I missed. | Remove, or gather these three into one note. |
| What the three assemblers gave — "assemblers disagree at the edges and agree about the substance" | "At the edges" and "about the substance" are both idioms, and here "edges" also has a literal meaning because the paragraph is about the ends of a circular genome. I could not tell which meaning was intended. | Write "assemblers differ in the exact ends and agree on the sequence content". |
| What the three assemblers gave — "SPAdes and MEGAHIT both overshot by a little over a hundred bases" | "Overshot" is an idiom from shooting. The explanation that follows is clear, but the verb slowed me. | Write "produced about a hundred extra bases". |
| What the three assemblers gave — "which is the normal state of affairs" | An idiom. | Write "which is expected". |
| What good looks like (heading) | The heading is an idiom. I expected a picture or a screenshot of something good. | Use "How to judge your assembly". |
| What good looks like — "the fixture's 16,697 bases against a published 16,569 is well within the margin" | "Within the margin" is used with no number. 128 bases of difference is fine, but I do not know the limit. | Say roughly how many extra bases are acceptable. |
| What good looks like — "is the fingerprint of thin or uneven coverage" | Fingerprint as a metaphor for a diagnostic pattern, and "thin" as a metaphor for low. Two figures of speech in eight words. | Write "is the sign of low or uneven coverage". |
| What good looks like — "the fix is more reads rather than different settings" | I could not perform this. Nothing tells me how to get more reads or how many more. | Point to the chapter on sequencing depth, or say to resequence. |
| What good looks like — "it is close to constant within a genome and different between genomes, which makes it a cheap identity check" | "Cheap" as a metaphor for computationally easy. I first read it as low quality. | Write "which makes it a quick identity check". |
| What good looks like — "if the count is in the low thousands for a genome of any size, map the reads against a known reference instead" | The fixture has 9,958 pairs, which I would call low thousands, and it assembled perfectly. I could not reconcile the rule with the example. | Say the threshold depends on genome size, and give the fixture as the contrast. |
| What good looks like — "Open the FASTQ bundle and read its read count in the Inspector" | Inspector is used for the first time here with no explanation of where it is or how to open it. | Say how to show the Inspector. |
| What good looks like — "LGE reports a run that exited cleanly but wrote nothing as a distinct outcome" | "Exited cleanly" is programmer vocabulary. I do not know what a clean exit is. | Write "a run that finished without an error message but produced no contigs". |
| On the command line — "This section is optional." | I appreciated this, but the Settings section above already contained a command-line sentence in every entry, which I had to skip one at a time. The optional label arrives too late to help. | Say at the start of Settings that the flag sentences belong to this optional section. |

Three lines.

The one thing I learned. N50 is a length and not a count, and half the assembly sits in contigs at least that long.

The one thing I still could not do. Set the Read Type picker when the headers give no clear answer, because the chapter never says how to find out which chemistry produced my reads.

The sentence I liked most. "Sort the contigs longest first, walk down the list adding up their lengths, and N50 is the length of the contig you are holding when the running total first reaches half the assembly's total bases."
