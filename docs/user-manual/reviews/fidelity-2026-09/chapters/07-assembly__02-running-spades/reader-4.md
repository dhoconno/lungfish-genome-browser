# Reader 4 report, Running SPAdes

Persona. An undergraduate who has used Geneious in a course for one semester. Comfortable in a graphical sequence viewer. Expects every operation to have a dialog. Has never opened a terminal.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "SPAdes is a program that takes" | The name SPAdes is never expanded or explained. In Geneious the assembler was just called "De Novo Assemble" so I do not know if SPAdes is a brand, an acronym, or a setting. | Say once what SPAdes is short for and that it is a separate program LGE runs for you. |
| What it is, "using nothing but the reads themselves" | Fine, but two paragraphs later I am told SPAdes "actually runs the whole procedure several times at several k values". I could not tell whether I have to choose those k values. Nothing in the Settings section mentions k. | Add one sentence saying LGE does not expose k and SPAdes picks the values itself. |
| What it is, "two points are joined whenever one k-mer's tail overlaps the next one's head" | I had to read this three times. With `ACG` and `CGT` I could not see which part is the tail and which is the head, or how many bases must match. | Show the join on the same `ACG`/`CGT` example, naming the two overlapping bases. |
| What it is, "LGE writes a `.lungfishref` assembly bundle" | The extension reads like "reference" but the thing is an assembly. I assumed I had picked the wrong menu item. | Say in one clause that assemblies and reference sequences share the `.lungfishref` extension. |
| What it is, "an assembly sheet reached from Tools > Assembly" | "Sheet" is used from here on for the dialog. Geneious calls this an options window. First use is unglossed. | Gloss "sheet" once as the panel that slides down from the window title bar. |
| Why you would do this, "a plasmid you engineered yourself" | Plasmid is used here and again in the Profile setting without a gloss, and it is not in the glossary_refs list at the top. | Gloss plasmid at this first use. |
| Why you would do this, "only obliquely as a coverage anomaly on a mapped alignment" | Coverage is not glossed until the Reading the results section, far below. I could not judge what an anomaly would look like. | Move the coverage gloss to its first use here. |
| Before you start, "The fixture holds 9,958 such pairs" (Why you would do this) | I have no way to judge whether 9,958 pairs is a lot or a little for a 16,569-base genome. Later the chapter warns that "low thousands" of reads is too thin, which made me worry 9,958 is marginal. | State the approximate coverage the fixture gives, so the number can be judged. |
| Before you start, "Download the fixture folder from the manual's fixtures on GitHub" | The instruction says download a folder, then immediately says GitHub offers no folder download and I must fetch the whole repository as a ZIP. I read the paragraph twice before realising the first link is not a step. | Drop the first link, or label it as the location rather than an action. |
| Before you start, "double-click the downloaded file to unpack it" | The unpacked folder name is not given, so I did not know what to look for in Downloads. | Name the unpacked folder. |
| Before you start, "Install All on the Genome Assembly card" | No indication of how long the install takes or how big it is. I stopped here for several minutes not knowing whether the app had hung. | Give a rough download size and time. |
| Before you start, "its smoke test, a quick check" (Procedure step 1) | Smoke test is defined only in the Readiness paragraph, after the Before you start section already told me to install the pack. The order of first use and gloss is reversed. | Keep the gloss but mention the smoke test in Before you start too. |
| Before you start, "took 110.7 seconds on a fourteen-core Mac" | I do not know how many cores my laptop has, and the chapter never says how to find out, so I could not scale this. | Say where the core count appears, or give a slower-machine figure as well. |
| Procedure step 1, "Click the bundle holding the HG002 mitochondrial reads" | Bundle is used throughout and is in glossary_refs but is never glossed in the body. In Geneious I select documents in a folder, so I did not know if a bundle is a file, a folder, or a database record. | Gloss bundle at its first body use. |
| Procedure step 1, "SPAdes, MEGAHIT, SKESA, Flye, and Hifiasm are five separate menu items into the same sheet" | This is the opposite of Geneious, where one assemble dialog has an algorithm dropdown, and the text does not flag the difference. It also seemed to contradict the next paragraph, where the Assembler control switches tools anyway. | Say plainly that the five menu items are shortcuts into one sheet and that the Assembler control does the same job. |
| Procedure step 1, "its read layout, and the detected read class" | Read layout and read class are two new terms in one sentence, neither glossed. I could not tell them apart from Read Type further down. | Gloss both, and say whether read class and Read Type are the same thing. |
| Procedure step 1, "LGE sniffs the first FASTQ header" | "Sniffs" is informal and I was not sure whether it means it reads the file or guesses. | Replace with "reads". |
| Procedure step 1, "Leave the Profile popup on Isolate" | Step 1 is titled "Open the sheet" but this step and the Readiness paragraph are about checking and running. I lost track of where the numbered list ends. | Move the Readiness paragraph into step 2, or renumber. |
| Procedure step 2, "LGE first materialises the FASTQ files, meaning it reconstructs the full read set on disk when the bundle you picked is a derived one" | Derived bundle and preview are both new here. Since I imported the fixture directly, I could not tell whether this step applies to me at all or whether I should wait for it. | Say this step is skipped for a directly imported bundle. |
| Procedure step 2, "N50 : 16697 bp" in the results block | N50 appears in the output block with no explanation and is not defined until the Reading the results section pages later. On first pass I did not know if 16697 was good. | Add a forward pointer beside the block, or one clause defining N50 in place. |
| Procedure step 2, "There is no `Assemblies` folder." | I never expected one, so this sentence stopped me looking for something I had not thought about. | Drop it, or attach it to the sentence about the timestamped folder. |
| Procedure step 2, "double-clicking it opens the assembly result viewport" | Later, in Reading the results, I am told "There is no double-click shortcut for this" about opening a contig. Two double-click statements about different objects, and I confused them. | Name the object explicitly in the later sentence. |
| Settings, "The sheet carries eleven controls" | I counted thirteen bold entries in the section. I could not reconcile the number with the list. | Make the count match the entries, or say the count is for SPAdes only and mark which entries are other assemblers. |
| Settings, "those closing sentences belong to the optional command-line section" | I was told to skip the last sentence of every entry, which meant reading each entry and then discarding part of it. It broke my reading rhythm for eleven entries. | Move the flag names into the command-line table instead. |
| Settings, Assembler, "the row lists only the assemblers that accept the read class LGE detected" | So the Assembler picker's contents depend on a value I cannot edit. If detection is wrong, I could not work out from the text how to reach the assembler I want. | Say what to do when the assembler you want is missing from the row. |
| Settings, Read Type, "one of Illumina short reads, ONT reads, or PacBio HiFi/CCS" | ONT, HiFi, and CCS are three abbreviations in one clause, none expanded. | Expand ONT at least, since it recurs in the Next section. |
| Settings, Profile, "assumes one organism sequenced at reasonably even depth" | Depth has not been introduced. Coverage is glossed much later and I did not know the two words meant the same thing. | Use coverage, or say the two words are interchangeable. |
| Settings, Profile (MEGAHIT), "Default is not a value you pass, because its internal id is empty and an empty id makes LGE omit the flag entirely" | This is implementation detail about a program I will never type a command into, placed in the middle of a window-based settings list. I read it twice looking for something I had to do. | Move it to the command-line section. |
| Settings, Threads, "It defaults to the smaller of that core count and 8" | I do not know my core count, so I cannot predict my default. | Say the sheet shows the resolved number. |
| Settings, Threads, "One caveat for MEGAHIT, which is a genuine app behaviour rather than a typo." | The reassurance that this is not a typo made me distrust the rest of the numbers. Apple Silicon and arm64 are also both used for the same thing without saying so. | Drop the reassurance, and use one name for the chip. |
| Settings, Memory Limit, "It defaults to three quarters of installed memory capped at 32 GB" | Again a default I cannot compute without a number I do not have. | Say the slider shows the resolved value on open. |
| Settings, Min Contig, "the stepper is shown and editable but the value never reaches the run" | A control that does nothing, described as a defect, in the middle of the instructions. I could not tell whether the whole chapter is describing a broken feature or just this one control. | Move this to a short known-issue note and keep the entry to what the control does. |
| Settings, Min Contig, "screen short contigs afterwards by sorting the viewport's Length (bp) column" | Sorting does not remove short contigs, it only orders them, so the suggested workaround does not do what the control promised. | Say the workaround is visual only and the short contigs stay in the export. |
| Settings, Careful mode, "inside the Curated extra arguments disclosure under Advanced Settings" | Disclosure is UI jargon. I looked for a control labelled "disclosure". | Call it a collapsible group, or say click the triangle. |
| Settings, Careful mode, "the pass costs noticeably more wall time" | Wall time is used here and in the results strip without a gloss. | Gloss wall time once as the real elapsed time. |
| Settings, Careful mode, "Turn it on for a small genome where per-base accuracy matters" | The chapter's own example is a mitochondrial genome, so by this advice I should have turned Careful mode on, but step 1 told me to leave every control alone. The two instructions conflict. | Say which one wins for the fixture run. |
| Settings, Extra arguments, "text the parser cannot read, an unclosed quotation mark for instance, blocks Run" | This is the only place a user types raw tool syntax, and I was told to read "the assembler's own documentation" with no link to it. | Link the three assemblers' documentation. |
| Settings, Extra arguments, "LGE pins `--min_count 2` on every SKESA run" | A hidden default I cannot see anywhere in the window. I would never have known unless I read this paragraph. | Note that the sheet does not display this pinned value. |
| Settings, Project Name, "with the mate suffixes `_R1`, `_R2`, `_1`, `_2`, and the `.lungfishfastq` extension stripped" | `.lungfishfastq` is a fourth Lungfish extension, alongside `.lungfishref` and `.lungfishprimers` mentioned nowhere here. I could not tell which file on disk this is. | Say once that imported reads become a `.lungfishfastq` bundle. |
| Settings, Output Folder, "though `--output` on the command line writes the run somewhere else" | I cannot change where my assembly lands from the window at all, which is unlike Geneious where I choose the destination folder. The text does not acknowledge that this may surprise anyone. | Say plainly that the destination is fixed in the window. |
| Settings, Run Mode, "It offers Run separately per bundle, which is the setting, and a locked Combine all inputs, run once" | "which is the setting" reads as a typo or a missing word. I read the sentence three times. | Rewrite as one clear sentence. |
| Reading the results, "Where the detail pane sits depends on a stored layout preference offering three arrangements" | Three layout names are given but not where the preference is set, so I could not make my window match the screenshot. | Say where the layout control lives. |
| Reading the results, "it adds the tool version and the wall time when the run recorded them" | "When the run recorded them" implies sometimes they are missing, with no way to know which case I am in. | Say when they are absent. |
| Reading the results, "a bacterial isolate coming back as 50 contigs with an N50 of 200 kb is tidy and the same 5 Mb arriving as 5,000 contigs" | Four numbers in one sentence about a genome that is not the chapter's example, and kb and Mb are used without expansion. I lost the thread. | Split into two sentences and expand kb and Mb at first use. |
| Reading the results, "There is no coverage column anywhere in this viewport." | Then two paragraphs later I am told SPAdes puts coverage in the contig name and I can read it there. The first statement made me stop looking. | Join the two facts in one place. |
| Reading the results, "`NODE_1_length_16697_cov_121.957333`" | I could read 122-fold coverage off this, but nothing tells me what coverage value is good or bad for an assembly. | Give a rough adequate range. |
| Reading the results, "all disabled until you select at least one contig" | Selecting a row is described as "Click a row" for the detail pane, but I could not tell whether clicking also counts as selecting for the buttons, or whether there is a checkbox. | Say clicking a row selects it. |
| Reading the results, "Right-clicking a row adds Extract Sequence..., Align with MAFFT..., and Run Operation..." | Three operations named with no explanation of any of them and no links, unlike every other cross-reference in the chapter. | Link each to its chapter, or say they are covered elsewhere. |
| Reading the results, "use Create Bundle to derive a reference bundle from it" | In Geneious I double-click a contig and it opens. Here I must create a new object first. The text says there is no double-click shortcut but does not say why the extra step exists. | One clause saying the viewport needs a bundle before it can show a sequence. |
| What the three assemblers gave, table row MEGAHIT | To fill this table I would have to run MEGAHIT and SKESA, but the Procedure only ever walks through SPAdes. I did not know whether I was expected to run all three. | Say whether running the other two is optional. |
| What the three assemblers gave, "labelled its contig `Contig_1_257.173_Circ [topology=circular]`" | The `257.173` is unexplained. By analogy with the SPAdes name I guessed coverage, but SPAdes reported 122 for the same reads and the mismatch worried me. | Say what the number is and why the two differ. |
| What good looks like, "the fixture's 16,697 bases against a published 16,569 is well within the margin" | The margin is never given as a number, so I could not judge my own result if it came back at, say, 17,200. | State the acceptable overshoot. |
| What good looks like, "if the count is in the low thousands for a genome of any size, map the reads against a known reference instead" | The fixture has 9,958 pairs, which is close to "low thousands", and the sentence says "a genome of any size" which contradicts the earlier point that a mitochondrial genome is small enough to work. | Give the threshold as coverage rather than read count. |
| What good looks like, "Open the FASTQ bundle and read its read count in the Inspector" | The Inspector is named once, here, with no explanation of how to open it. | Say how to show the Inspector. |
| On the command line, "The whole procedure runs headless, meaning with no window at all, by typing commands into the Terminal application." | I have never opened a terminal. This is the only line telling me the section is not for me and it comes after two sentences of setup. | Put the "skip this if you work in the window" sentence first. |
| On the command line, "This is the exact command that produced every SPAdes figure in this chapter." | This implies the numbers I am checking my run against were produced by a route I cannot use, which made me doubt the window figures would match. | Say the window produces the same result. |
| Next, "Continue to Running Flye or Hifiasm for long-read assembly" | Long reads are mentioned repeatedly through the chapter (Flye, hifiasm, ONT, PacBio) but never contrasted with short reads, so I do not know if that chapter applies to me. | One clause saying short reads and long reads come from different instruments. |

Three lines.

The one thing I learned. N50 is a length, not a count, and on a one-contig assembly it tells you nothing.

The one thing I still could not do. Decide whether my own run went well, because the two judgement numbers I was handed, the read count and the acceptable length overshoot, are never given as thresholds I could compare against.

The sentence I liked most. "The three assemblers disagreed about the genome's exact ends and agreed about its content, which is the normal state of affairs and the reason the contig count matters more than the last hundred bases of length."
