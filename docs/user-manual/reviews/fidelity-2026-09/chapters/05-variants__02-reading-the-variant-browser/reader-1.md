# Reader report: Reading the Variants Table

Reader 1. Sophomore, one genetics course, no lab time, never opened a terminal.

| Location | What stopped me | What would have helped |
|---|---|---|
| What it is, "A variant caller writes its answer" | I know what a mutation is but not what a "caller" is as a program. The glossary link is there but the sentence assumes I already picture a tool that reads reads and emits a file. | One clause saying a variant caller is a program that compares aligned reads to the reference and lists the differences. |
| What it is, "The drawer starts 250 points tall" | "Points" is not a unit I know. Is that pixels, or something else? | Say roughly a quarter of the window height instead of a number in points. |
| What it is, "**Annotations** lists gene features" | "Gene features" could mean almost anything to me. Exons? Names? | Name one example of a feature, like an exon or a coding region. |
| What it is, "A second two-segment control decides" | Two sentences in a row start with "A two-segment control" and "A second two-segment control" and I lost track of which one did what. I had to reread the paragraph. | Name each control the way the button is labelled, so I can match text to screen. |
| What it is, "reach for `lungfish-cli variants query`" | Backticked text this early, with no explanation that this is something you type into a terminal. I have never opened one. | A half sentence saying this is a command typed in Terminal, covered at the end. |
| Why you would do this, "the HG002 chromosome 20 slice" | I do not know what HG002 is or why a cell line is well studied. It sounds like it matters. | One clause saying HG002 is a reference human sample whose true variants are already known. |
| Why you would do this, "mapped to a 500 kb stretch" | "Mapped" is used before it is explained. I think it means the reads were matched to positions but I was guessing. | Gloss "mapped" the same way the other terms get glossed. |
| Why you would do this, "the two call sets share 852 positions" | I could not work out how 852 shared plus 201 bcftools-only equals 1,056, and 852 plus 9 does not equal 862 either. I stared at this and could not make the arithmetic close. | Say what the leftover rows are, or say the counts are positions and not rows. |
| Before you start, "which is a fixture, the sample data set" | The word fixture is defined mid-sentence in a way I read twice. It is a software word being used for a data file. | Call it a practice data set and drop the jargon word. |
| Before you start, "No plugin pack is needed" | I do not know what a plugin pack is or what the "Required Setup pack" contains. It is mentioned as if I already installed one. | One clause saying plugin packs are the optional tool bundles LGE downloads, and Required Setup came with the app. |
| Step 1, "Check the Operations panel with **Operations > Show Operations Panel**" | I did not know there was an Operations panel or that runs happen in the background. This is the first mention. | One clause saying the Operations panel is where long jobs report progress. |
| Step 2, "Seven of them are the VCF's own standard columns" | I counted the table below and it lists six rows, one of which covers two columns (Ref and Alt). Seven, six rows, twelve total, six remaining, and I could not make the counting line up. | Either list all twelve in the table or drop the running counts. |
| Step 2, "`Filter`, which is the caller's own verdict" | "Verdict" told me nothing concrete. Verdict about what, passing what test? | Say the caller's own pass or fail label for the row. |
| Step 2, "LGE's own iVar output" | iVar appears once with no gloss and no glossary link, unlike bcftools and LoFreq which both get links. | Gloss iVar or drop it, since the fixture does not use it. |
| Step 2, "each `INFO` key the loaded track declares" | I do not know what an INFO key is even after the glossary link, and "declares" sounds like programming. I could not picture what appears on screen. | Say INFO keys are the extra per-variant measurements the caller recorded, such as depth. |
| Step 2, "sixteen `INFO` keys including `DP`, `DP4`, `MQ`, and `AC`" | Four abbreviations with no expansion. I know DP is mentioned later as depth but MQ, AC, DP4, and SB are never explained. | Expand each abbreviation once, or say only DP and AF matter for this chapter. |
| Step 4, "You will not see all fourteen" | I counted the chips listed in the paragraph above and got fourteen, but then the text says several are hidden, and I could not tell which ones I should actually expect to see on my screen. | State plainly which chips appear on this fixture. |
| Step 4, "`Moderate+`" | I do not know what impact levels are or what Moderate is moderate about. | One clause saying impact is an annotation program's guess at how damaging a change is. |
| Step 4, "The three frequency chips appear only for a haploid organism" | I know haploid from genetics but I could not see why a frequency filter would only work for haploids. It felt backwards. | One clause explaining why allele fraction is only a clean filter with one genome copy. |
| Step 4, "`Bookmarked`" | Bookmarked is listed as a chip but nothing anywhere says how a row gets bookmarked. | Say where bookmarking happens, or note it is covered elsewhere. |
| Step 5, "the three population-database frequency keys" | Never named and never explained. I do not know what a population database is. | Name them once, or say they come from public human frequency catalogues. |
| Step 5, "a coordinate rule takes a range such as `1-250000`" | The Chrom column earlier said the sequence is `chr20_10.0-10.5Mb`, so I expected coordinates around ten million, not starting at 1. I could not tell which numbering to use. | Say the slice is renumbered from 1, if that is what is happening. |
| Step 5, "that leaves 512 rows out of the 574 in that window" | I did not know how to get the table to show me 574 so I could check. The scope control is only explained later in Settings. | Point forward to the Genome and Region setting here, where I first need it. |
| Step 5, "the query engine has no OR" | Appears in Settings, but I met the Match All limit here first and did not understand why I could not ask for one condition or another. | Say here that you cannot ask for either-or conditions. |
| Step 6, "The tick colors on the genome track above" | This is the first mention of a genome track with ticks above the table. I do not know what I am looking at. | One clause describing the track of tick marks above the drawer. |
| Step 6, "reports `AF=0.571429` instead" | I could work out that this is 33 divided by 63 roughly, but the text never says the two numbers are the same fact. I had to check by hand. | Say the fraction is the alternate reads over the depth. |
| Step 6, "LoFreq skips indels unless you ask for them" | The text says to ask, but never says where or how. I would not know how to turn it on. | Say whether this is a setting in LGE or something you cannot change here. |
| Settings, "at narrow window widths the second segment shortens its label to GT" | GT is used as an abbreviation here and as a genotype field name in step 5, and I could not tell if they were the same thing. | Say GT is short for Genotype. |
| Settings, "Auto / Haploid / Diploid" | This control is never mentioned in the Procedure, so I met it cold in Settings after being told there were six toolbar controls, and the opening section only described five. | Mention it once in step 4 where the frequency chips are discussed. |
| Reading the results, "a logarithmic scale where 30 means one time in a thousand" | I understand the sentence but not how to get from 30 to one in a thousand, so I could not judge what 73 or 2,478 mean. | Give one more anchor, such as what 20 means, so I can see the pattern. |
| Reading the results, "The two callers put their scores on scales that cannot be compared" | Then a paragraph earlier a chip called `Qual >= 30` applies one threshold to both. I could not reconcile those. | Say whether the 30 chip is meaningful on both tracks. |
| Reading the results, "623 rows reading `0/1` and 415 reading `1/1`" | 623 plus 415 is 1,038, not 1,056. Eighteen rows are unaccounted for and I did not know whether that was a typo or a real category. | Say what the remaining rows hold. |
| Reading the results, "339 at or above 0.8, 517 between 0.2 and 0.8, and 6 below 0.2" | Those add to 862 so that one checked out, but the phrase "two-humped shape" describes a graph I cannot see anywhere in the table. | Say the humps are visible only by sorting, or show the distribution. |
| What good looks like, "roughly one difference every 470 bases" | I could not tell whether 1,056 rows over 500 kb is per-caller or combined, since the table holds 1,918 rows. | Say the rate is computed from one track only. |
| On the command line, "The `--filter` flag accepts per-sample clauses only" | The whole paragraph tells me what does not work before showing me anything that does, and it contradicts step 5 where I learned to write `filter=PASS`. I read it three times. | Lead with the one form that works, then note the Search Builder grammar does not carry over. |
| On the command line, "BUNDLE=\"MyProject.lungfish/Reference Sequences" | I have never used a terminal. I do not know where to type this, whether the path is relative to something, or what the quotes do. | One sentence saying to open Terminal and that the path is the folder you picked. |
| On the command line, "bcftools view -H hom-alt.vcf" | Two programs and two flags I have never seen, in a chapter that said bcftools comes with the app but never said I would run it myself. | Say this line counts the rows in the file. |
| Next, "Continue to Nanopore Variant Calling" | The link jumps from chapter 02 to 04, and I wondered whether I had skipped chapter 03. | Say what 03 covers or why it is skipped. |

The one thing I learned. An empty table after clicking a filter can mean the rows were never judged rather than that they failed, so the bare dot in the Filter column is not a fail mark.

The one thing I still could not do. Run any of the command-line examples, because I do not know where to type them or what the bundle path should be on my own computer.

The sentence I liked most. "Read a bare `.` as unjudged rather than as failed, look at the column before you filter on it, and use **Clear** to bring the rows back."
