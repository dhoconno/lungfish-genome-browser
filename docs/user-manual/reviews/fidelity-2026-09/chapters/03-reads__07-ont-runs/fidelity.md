# Fidelity review, 03-reads/07-ont-runs.md

Reviewed 2026-09-07 against the Swift source in this worktree, the CLI help
dumps under `reviews/fidelity-2026-09/cli-help/`, `parameters.yaml`, the
fixture README, `GLOSSARY.md`, and six live reruns of the chapter's own
commands with `.build/debug/lungfish-cli` against copies of the fixture
under the session scratchpad. Nothing was written to `~/Desktop/lge-docs`
and no build was run.

Rows 18 to 24 of the chapter's DRIFT section concern Orient Reads, which the
rewrite correctly moved to 08-read-processing.md. They are checked here only
as the forward pointer in `## Next`, which is row 66 below.

## Claims

| # | Claim (quoted) | Verdict | Evidence | Corrected wording |
|---|---|---|---|---|
| 1 | `entry_points`: "File > Import Center... (Cmd-Shift-I) > Sequencing Reads > ONT Run Folder" | true | `MainMenu.swift:208-212` sets title "Import Center…" with keyEquivalent "i" and mask `[.command, .shift]`. `ImportCenterViewModel.swift:175` gives the tab title "Sequencing Reads", line 293 the card title "ONT Run Folder". | no change |
| 2 | `entry_points`: "Tools > Demultiplexing > Demultiplex Barcodes..." | true | `ToolsMenuModel.swift:64` `case .demultiplexing: return "Demultiplexing"`. `FASTQOperationDialogState.swift:1956` returns "Demultiplex Barcodes"; line 1267 puts it in that category. Corrects DRIFT rows 3 and 26. | no change |
| 3 | `entry_points`: "Tools > Demultiplexing > ONT Fluidigm Sample Split..." | true | `FASTQOperationDialogState.swift:1957` returns "ONT Fluidigm Sample Split"; line 1267 returns `[.demultiplexBarcodes, .ontFluidigmSampleSplit]` for the category. | no change |
| 4 | `entry_points`: "CLI: lungfish-cli fastq import-ont" and "CLI: lungfish-cli fastq demultiplex, lungfish-cli fastq scout" | true | `fastq.txt` banners at lines 610, 400, 553. | no change |
| 5 | `parameters_refs: [import.ont-run, fastq.demultiplex-barcodes, fastq.ont-fluidigm-sample-split]` | true | Matches roster row 20 at `DRIFT.md:3746` exactly, in that order. All three ids resolve in `parameters.yaml` at lines 1479, 60, and 150. | no change |
| 6 | `tools: [cutadapt]` | true | Cutadapt is the default demultiplexing engine (`FASTQOperationDialogState.swift:280`) and the only third-party tool the chapter's operations invoke. Import-ont shells nothing. | no change |
| 7 | `shots` lists four ids, each with a caption | true | Four `<!-- SHOT -->` markers in the body at lines 74, 78, 82, 92 (`ont-import-configuration-sheet`, `ont-barcode-sheet-controls`, `sidebar-after-ont-import`, `demultiplex-barcodes-pane`) match the four `shots` ids one for one, each carrying a caption. | no change |
| 8 | `glossary_refs`, fourteen anchors | true | All fourteen resolve in `GLOSSARY.md`. The five new ones are at lines 45 (`barcode-scout`), 111 (`cutadapt`), 153 (`fluidigm-sample-barcode`), 261 (`minknow`), 443 (`unclassified-reads`), each in the existing `**Term**{#anchor}. …  See also: …` shape. | no change |
| 9 | "MinKNOW writes those reads into a folder it names `fastq_pass`" | true | `ONTDirectoryImporter.swift:24` documents the layout; the fixture README's run-folder section shows the same tree. Domain claim otherwise, consistent with the importer's expectations. | no change |
| 10 | "MinKNOW puts each barcode's reads in its own subfolder named `barcode01`, `barcode02`" and drops uncalled reads into `unclassified` | true | `ONTDirectoryImporter.swift:59` and `:92` accept exactly `barcode*` prefixes and the literal name `unclassified`. | no change |
| 11 | "LGE walks the tree, joins every chunk under a given barcode folder into one read bundle, and gives you one row in the sidebar per barcode" | true | `ONTDirectoryImporter.swift:79-135` scans subdirectories; `:323` names each output `"<barcodeName>.lungfishfastq"`; the concatenation loop at `:355-366` joins the chunk files. Verified live: 1 barcode directory, 1 chunk, one bundle out. | no change |
| 12 | "The HG002 long reads run from 263 bases to 39,647 bases" | true | Fixture `README.md`: "min 263 bp, max 39,647 bp". | no change |
| 13 | "The HG002 long reads average Phred quality 7.9" | true | Fixture `README.md`: "average quality 7.9". | no change |
| 14 | "meaning the basecaller expects to be wrong at a rate near one base in six" | true | Q7.9 gives an error probability of 10^(-0.79) = 0.162, one base in 6.2. Arithmetic from the fixture's own measured value. | no change |
| 15 | "a 16,569-base circular chromosome" | true | Fixture `README.md`: "`NC_012920.1`, Homo sapiens mitochondrion, complete genome, 16,569 bp". | no change |
| 16 | "a slice this small still holds 950 reads covering the genome about 262 times over" | true | Fixture `README.md` stats table: 950 reads, 262x. | no change |
| 17 | "The fixture's run folder holds a single `barcode01` directory" | true | `find` over `docs/user-manual/fixtures/hg002-long-reads` returns exactly one run-folder chunk, `ont-run/fastq_pass/barcode01/HG002_chrM_pass_barcode01_0.fastq.gz`. | no change |
| 18 | "The folder holds one file, `ont-run/fastq_pass/barcode01/HG002_chrM_pass_barcode01_0.fastq.gz`" | true | Same evidence as row 17. | no change |
| 19 | "the importer reads the barcode name from the folder rather than from the filename" | true | `ONTDirectoryImporter.swift:323` builds the bundle name from `barcodeDir.barcodeName`, which `:92-101` takes from the directory name. The filename is never parsed. | no change |
| 20 | "Importing a run folder needs no optional plugin pack and no Docker Desktop." | true | `import.ont-run` has `gating: []` in `parameters.yaml:1483`. `ONTDirectoryImporter` is pure Swift file I/O and shells no managed tool. | no change |
| 21 | "Demultiplexing later in the chapter calls cutadapt, which arrives with the application." | true | `fastq.txt` `==== fastq demultiplex ====` describes "embedded cutadapt". `fastq.demultiplex-barcodes` has `gating: []` (`parameters.yaml:65`). The live demultiplex ran with no plugin install. | no change |
| 22 | Step 1, "choose **File > Import Center...** (Cmd-Shift-I). The Import Center opens as a tabbed grid of cards" | true | `MainMenu.swift:208-212` for the item and shortcut. `ImportCenterViewModel` builds `allCards` grouped by an `ImportCenterTab` enum whose `title` cases are at lines 170-180. | no change |
| 23 | Step 2, "Click the Sequencing Reads tab, then click the ONT Run Folder card. A panel opens that accepts only folders, not files" | true | `ImportCenterViewModel.swift:291-305`: `tab: .sequencingReads`, `canChooseFiles: false, canChooseDirectories: true`. | no change |
| 24 | Step 2, "You can also select a single `barcode01` folder here when you want just one barcode" | true | `ONTDirectoryImporter.swift:59` handles a directory whose own name starts with `barcode`. `fastq.txt` `==== fastq import-ont ====` says the same for the CLI. | no change |
| 25 | Step 3, "Platform already reads Oxford Nanopore, because the ONT Run Folder card sets it." | true | `MainSplitViewController+FASTQImport.swift:722` passes `detectedPlatform: .oxfordNanopore` to the sheet; `FASTQImportConfigSheet.swift:277-279` selects the matching popup item. Unlike ground-truth row 14 (the BAM path, unverifiable), this path passes the platform literally. | no change |
| 26 | Step 3, "The Pairing control is absent, since nanopore reads are single-end" | true | `FASTQImportConfigSheet.swift:714-723` hides the Pairing label and popup for Oxford Nanopore and selects index 0. Matches ground-truth row 29. | no change |
| 27 | Step 4, "Leave 'Apply processing recipe after import' off for this chapter." | true | `FASTQImportConfigSheet.swift:126` gives the checkbox that exact title; `:362` sets `recipeCheckbox.state = .off`, so off is already the state. | no change |
| 28 | Step 4, "It is the checkbox that offers the two nanopore demultiplexing recipes" | true | `MainSplitViewController+FASTQImport.swift:726` passes `recipeOptions: [.ontFluidigmSampleSplit, .ontPacBioBarcodeDemux]`; `FASTQImportConfigSheet.swift:825` reveals the picker only when the checkbox is on. | no change |
| 29 | Step 5, "a bundle named `barcode01` appears under `Imports/` in the sidebar" | **false** | The GUI ONT run-folder import writes its bundles to the **project root**, not `Imports/`. `ONTImportOperationCoordinator.swift:108-137` returns `projectURL` itself as the output directory unless a previous ONT output already sits there, in which case it makes a sibling folder named after the run (for example `Run42`), still at the project root. `ONTImportOperationCoordinatorTests.swift:46-47` asserts the CLI command is `--output <projectURL>`, and `:84-89` asserts the conflict case lands in `<project>/Run42/`. Nothing on this path appends "Imports". The generic `Imports/` destination at `AppDelegate+ToolsMenu.swift:636` belongs to the plain file-import route, not to this one. Ground-truth row 10 asserted `Imports/` from that same generic line and is wrong for the same reason. | "Click Import. When the import finishes, a bundle named `barcode01` appears at the top level of your project in the sidebar. A 24-barcode run would land 24 bundles there, one per barcode folder, with names taken from the folder names. Import a second run into the same project and its bundles are grouped into a folder named after the run, so the two runs stay apart." |
| 30 | "Reads whose barcode MinKNOW could not call are skipped by default. The importer walks past the `unclassified` folder" | true | `ONTDirectoryImporter.swift:161` filters on `config.includeUnclassified || !dir.isUnclassified`. `fastq.txt`: "--include-unclassified  Include unclassified reads (default: skip)". | no change |
| 31 | "on the command line that is the `--include-unclassified` option" | true | `fastq.txt` `==== fastq import-ont ====` OPTIONS. Verified live in the reruns. | no change |
| 32 | "For the first two, use **Tools > Demultiplexing > Demultiplex Barcodes...**, which sorts a bundle's reads into per-barcode bins by matching the barcode kit you name." | true | Menu path as row 2. `FASTQOperationDialogState.swift:2003` describes it as "Split pooled reads into barcode-defined samples." | no change |
| 33 | "Twenty barcode kits ship with LGE, including the nanopore native and rapid kits, and you can supply your own barcode list instead." | true | `fastq.txt` `==== fastq demultiplex ====` "Built-in kits:" lists exactly 20 ids (counted). The nanopore native (`ont-nbd*`) and rapid (`ont-rbk*`) families are among them. Custom definitions are accepted via `--kit <path>` and via the Custom Definition source (`FASTQOperationToolPanes.swift:647-651`). | no change |
| 34 | "For the third, use **Tools > Demultiplexing > ONT Fluidigm Sample Split...**." | true | As row 3. | no change |
| 35 | "each read carries a Fluidigm sample barcode between two fixed primer sequences called CS1 and CS2" | true | `fastq.txt` `==== fastq ont-fluidigm-samples ====`: "extracts the CS1-CS2 amplicon insert". The live manifest records `forwardPrimer` `ACACTGACGACATGGTTCTACA` and `reversePrimer` `TACGGTAGCAGAGACTTGGTCT`, the standard CS1 and CS2. | no change |
| 36 | "writes one bundle per sample holding the distinct inserts with their duplicate counts recorded in the read headers" | true | `fastq.txt`: "writes one physical .lungfishfastq bundle per sample containing unique insert exemplars … duplicate support is encoded in FASTQ headers as size=N". The live manifest carries `"duplicateCountEncoding": "size=N"`. | no change |
| 37 | The barcode scout "is a command-line step with no window of its own" | true | `fastq scout` exists (`fastq.txt:553`). No scout entry appears in `FASTQOperationToolID` (`FASTQOperationDialogState.swift:1911-1957`) or in any Tools category (`:1267`). | no change |
| 38 | The scout "reads a subset of the reads, counts hits for every barcode in the kit, and writes a `scout-result.json` marking each barcode accept, reject, or undecided" | true | `fastq.txt` `==== fastq scout ====`: "Scans a subset of reads against a barcode kit and writes a scout-result.json file with per-barcode hit counts and suggested accept/reject dispositions." Reproduced live. | no change |
| 39 | Settings lead-in, "The sheet also carries Platform, Quality Binning, Optimize storage, Compression Tool, and Compression Level" | true | All five are fields of `FASTQImportConfigSheet` (Platform popup, binning popup, `clumpifyCheckbox` at `:702`, `clumpingToolPopup` at `:703-704`, `compressionPopup` at `:124`). `parameters.yaml:1526-1530` says the same and points to `import.fastq`. | no change |
| 40 | Settings lead-in, "ONT Fluidigm Sample Split has no settings of its own." | true | `parameters.yaml:155` `settings: []` for `fastq.ont-fluidigm-sample-split`. | no change |
| 41 | Settings lead-in quotes the Fluidigm pane as "Uses the selected Fluidigm sample barcode definition, extracts CS1-CS2 inserts, and writes one counted FASTQ bundle per sample" | true | `FASTQOperationToolPanes.swift:695`, verbatim including the final period. | no change |
| 42 | **Apply processing recipe after import.** off by default, no CLI flag | true | `FASTQImportConfigSheet.swift:362` `recipeCheckbox.state = .off`. `parameters.yaml:1487-1492` gives default "off" and `cli_flag: null`. Label matches the registry and `FASTQImportConfigSheet.swift:126` verbatim. | no change |
| 43 | **(recipe picker).** "carries no label of its own, appearing as the unlabelled popup under the checkbox", starts on "Split by Fluidigm sample barcodes", other choice "Demultiplex full-length MHC ONT amplicons with PacBio barcodes", no CLI flag | true | `FASTQImportConfigSheet.swift:127` declares `recipePopup` with no label field beside it. The two option names are at `:68` and `:80`. `MainSplitViewController+FASTQImport.swift:726` passes them in that order so the Fluidigm one is index 0. `parameters.yaml:1493-1499` matches, including `label: (recipe picker)` and `cli_flag: null`. | no change |
| 44 | **Barcode Sheet:.** label, chosen "from a popup of sheets already in the project or through the Choose... button beside it", nothing selected by default, blocks Import, Fluidigm needs sample and barcode columns, PacBio needs `sample_id`, `barcode_1`, `barcode_2`, no CLI flag | true | Label verbatim at `FASTQImportConfigSheet.swift:131` and `parameters.yaml:1500`. Popup at `:133`, "Choose..." button at `:134`. `:855` sets `importButton.isEnabled = selectedBarcodeDefinitionURL != nil` when a recipe requires a definition, so it does block Import. Candidates come from project CSV/TSV/TXT files (`MainSplitViewController+FASTQImport.swift:809-812`). The PacBio column names are at `fastq.txt` `==== fastq ont-pacbio-barcode-demux ====`; the Fluidigm ones at `==== fastq ont-fluidigm-samples ====` ("CSV/TSV file with sample and Fluidigm barcode sequence columns"). The trailing colon in the bold label is quoted verbatim as the campaign rule requires. | no change |
| 45 | **Demux Folder:.** "starts as a name derived from the run folder, falling back to 'ONT Demultiplexed FASTQs' when no name can be derived, and characters outside letters, digits, space, period, underscore, and hyphen are replaced", no CLI flag | true | `FASTQImportConfiguration.swift:72` `static let fallback = "ONT Demultiplexed FASTQs"`; `:74-86` derives the name from the source URL, stepping up a level for `fastq_pass`, `barcode*`, or `unclassified`; `:88-99` replaces `[^A-Za-z0-9 ._-]+` and falls back when the result is empty. Label verbatim at `FASTQImportConfigSheet.swift:136`. `parameters.yaml:1507-1513` matches. | no change |
| 46 | **Barcode Source.** Built-In Kit and Custom Definition, defaults to Built-In Kit, Custom Definition asks for "a CSV, TSV, or whitespace-delimited file with the columns `id,sequence[,secondary_sequence][,sample_name]`", CLI `--kit` | true | `FASTQOperationToolPanes.swift:647-651` gives the two segment titles. `FASTQOperationDialogState.swift:273` sets `.builtinKit`. The Custom Definition caption at `FASTQOperationToolPanes.swift:661` names exactly those three shapes and that column form. `fastq.txt` `--kit` help agrees. | no change |
| 47 | **Built-In Kit.** "defaults to the first kit in the list", names the ten ONT kits, "Twenty kits are offered", CLI `--kit` | true, with one omission | `FASTQOperationDialogState.swift:274` sets the id to `BarcodeKitRegistry.builtinKits().first?.id`, so "first in the list" is right. The ten quoted ONT ids match the `fastq.txt` list exactly, in order. Twenty kits total, confirmed by count. The omission is that the menu is filtered to bare-barcode kits when Engine is Exact Bare Barcode (`FASTQOperationDialogState.swift:1097-1101`, `:1633-1638`), so twenty are offered only on the Cutadapt path. Not counted false because the chapter's own Engine paragraph tells the reader that engine behaves differently, and the registry (`parameters.yaml:1501-1508` allowed list) makes the same unconditional statement. | Optional tightening: "Twenty kits are offered on the Cutadapt path, and the nanopore ones are …". |
| 48 | **Engine.** Cutadapt and Exact Bare Barcode, defaults to Cutadapt, "that engine ignores Location and Trim Barcodes and always keeps every read", CLI `--engine` | true | Segment titles from `DemultiplexEngine.displayName` via `FASTQOperationToolPanes.swift:665-670`. Default at `FASTQOperationDialogState.swift:280`. `:468` forces `trimBarcodes: false` for exact-bare; `FASTQOperationToolPanes.swift:674-691` shows Location, both distances, Error Rate, and Trim only while the engine is Cutadapt. `fastq.txt` `--no-trim`: "exact-bare always preserves reads". | no change |
| 49 | **Location.** Both Ends, 5' End, 3' End, defaults to Both Ends, CLI `--location` | true | `FASTQOperationToolPanes.swift:675-680` gives those three titles in that order. `FASTQOperationDialogState.swift:276` sets `"bothends"`. `fastq.txt`: "(default: bothends)". | no change |
| 50 | **5' Distance.** defaults to 0, "the barcode must start at the very first base", CLI `--max-distance-5prime` | true | `FASTQOperationDialogState.swift:277` sets 0. `fastq.txt`: "Cutadapt max bases from 5' terminus where barcodes may start (default: 0)". Label at `FASTQOperationToolPanes.swift:684`. | no change |
| 51 | **3' Distance.** defaults to 0, CLI `--max-distance-3prime` | true | `FASTQOperationDialogState.swift:278`; `fastq.txt` same block; label at `FASTQOperationToolPanes.swift:685`. | no change |
| 52 | **Error Rate.** "at 0.15 a 20-base barcode tolerates three mismatched bases", defaults to 0.15, CLI `--error-rate` | true | `FASTQOperationDialogState.swift:279` sets 0.15; `fastq.txt` "(default: 0.15)". The arithmetic is right, 0.15 x 20 = 3. Label at `FASTQOperationToolPanes.swift:688`. | no change |
| 53 | **Trim Barcodes.** "starts on", "On the command line the sense is reversed, and the flag is `--no-trim`" | true | `FASTQOperationDialogState.swift:281` sets `true`. `fastq.txt`: "--no-trim  Cutadapt only: keep barcode sequences in output reads". Label at `FASTQOperationToolPanes.swift:689`. Fills DRIFT's missing row on the inverted sense. | no change |
| 54 | **Output Strategy.** Per Input and Grouped Result, defaults to Per Input, no CLI flag | true | `FASTQOperationDialogState.swift:2198` puts `.demultiplexBarcodes` in the configurable-output group; `:2112` returns `.perInput` for every non-classification category. `parameters.yaml:1524-1530` gives the same default and `cli_flag: null`. | no change |
| 55 | Settings coverage: every setting in all three registry entries has a paragraph | true | `import.ont-run` has four settings, all four documented (rows 42-45). `fastq.demultiplex-barcodes` has nine, all nine documented (rows 46-54). `fastq.ont-fluidigm-sample-split` has none, and the chapter says so. Labels match the registry verbatim, including both trailing colons. | no change |
| 56 | "the FASTQ viewport, with the same nine summary cards and three sparkline charts" | true | `FASTQChartViews.swift:30-40` returns exactly nine cards (Reads, Bases, Mean Length, Median Length, N50, Mean Q, Q20, Q30, GC). `FASTQSparklineStrip.swift:386-403` updates three charts, the read-length histogram, the quality-score histogram, and the per-position quality plot. | no change |
| 57 | The quoted ONT Import Summary block (Barcodes 1, Total reads 950, Time 0.2s, "barcode01: 950 reads") | true | Reproduced live on 2026-09-07 into a fresh directory. Output identical line for line, with `Output: verify-imported` in place of the chapter's `Output: imported`, which is just the `-o` argument the chapter shows. | no change |
| 58 | "Alongside the bundles the importer writes `demux-manifest.json` … Demultiplexing writes a file of the same name." | true | `DemultiplexManifest.swift:28` `public static let filename = "demux-manifest.json"`. Both live runs wrote a file of that name at their output root. Settles ground-truth row 32. | no change |
| 59 | The quoted Demultiplexing Summary block (Kit "ONT Native Barcoding (NBD114, 12)", Input reads 927, Assigned 0 (0.0%), Unassigned 927, Barcodes with reads 0, Time 2.5s) | true | Reproduced live. Every line identical except the wall time, 2.6s on my run against the chapter's 2.5s, which is timing noise rather than a claim about the app. | no change |
| 60 | "These reads are a slice taken from an aligned whole-genome BAM, so their barcodes were removed long before the fixture was built." | true | Fixture `README.md` Sources section: both read sets are `chrM` slices of GIAB whole-genome alignment BAMs, converted to FASTQ. A run-folder layout was synthesised afterwards, so no barcode was ever present. | no change |
| 61 | The quoted Barcode Scout Summary block (Reads scanned 950, Assigned 0 (0.0%), Accepted barcodes 0) | true | Reproduced live, identical. The chapter elides only the trailing absolute `Output:` path line, which is a local path and not a claim. | no change |
| 62 | "Reads matching no barcode go to a bin named `unassigned`, written as `unassigned.lungfishfastq` beside the per-barcode bundles. LGE keeps that bin by default … Discarding it is a command-line choice." | true | The live demultiplex wrote `demux-verify/unassigned.lungfishfastq/unassigned.fastq.gz`, and the manifest records `"bundleRelativePath": "unassigned.lungfishfastq"` with `"disposition": "keep"`. `fastq.txt`: "--discard-unassigned  Discard reads that do not match any barcode". Settles ground-truth row 38. | no change |
| 63 | "ONT Fluidigm Sample Split … scanning all 950 reads and writing 0 sample bundles … Its manifest recorded an input read count of 950 against an extracted read count of 0" | true | Reproduced live twice. Progress log: "Scanned chunk 1/1: 950 reads." then "Writing 0 sample bundles." The manifest carries `inputReadCount` 950, `extractedReadCount` 0, `unassignedReadCount` 950, `samples` empty. | no change |
| 64 | "Do not read a base count out of `demux-manifest.json` after a run-folder import. That field is filled with an estimate rather than a measurement" | true | `ONTDirectoryImporter.swift:371-372`, comment "Estimate base count (compressed bytes × ~1.5 …)" and `let baseCount = Int64(Double(totalBytesWritten) * 1.5)`. See row 68. | no change |
| 65 | "on this fixture it reported 7,495,398 bases where the reads actually hold 4,348,051, an overstatement of about 72 percent" | true | The manifest from the author's run records `"baseCount" : 7495398`. The fixture chunk is 4,996,932 bytes and 4,996,932 x 1.5 = 7,495,398 exactly. The fixture README gives 4,348,051 total bp. 7,495,398 / 4,348,051 = 1.724, so 72 percent over. | no change |
| 66 | `## Next` pointer, "Continue to Read Processing … including Orient Reads. … a nanopore molecule threads into the pore from whichever end reaches it first" | true | `08-read-processing.md` exists and DRIFT row 27 for that chapter says the "last chapter" sentence belongs there, not here, which this chapter honours. `orient-reads` resolves in `GLOSSARY.md`. The chapter makes no claim about Orient Reads controls, so DRIFT rows 18 to 24 no longer apply to it. | no change |
| 67 | "POD5 and FAST5 … Neither format has an Import Center card." | true | No POD5 or FAST5 card appears among `ImportCenterViewModel`'s `allCards`; the ONT card takes directories only (`:291-305`). Matches ground-truth row 40. | no change |

## Command-line section

| # | Claim (quoted) | Verdict | Evidence | Corrected wording |
|---|---|---|---|---|
| 68 | "The scout and demultiplex commands need to be run from inside a `.lungfish` project folder, because both resolve their paths against a project." | true, and understated | Verified live: running `fastq scout` on a copy outside a project fails with "Error: Project context required but no .lungfish root found above …". The same holds for `fastq ont-fluidigm-samples`, which the chapter's own third example invokes. `fastq import-ont` does not, confirmed by a successful run outside any project. | Optional: name the third command too, since the code block includes it. "The scout, demultiplex, and Fluidigm commands need to be run from inside a `.lungfish` project folder." |
| 69 | `lungfish-cli fastq import-ont ont-run/fastq_pass -o imported` and the `--include-unclassified` variant | true | `fastq.txt` USAGE `<input> --output <output> [--include-unclassified] …`. Both run live. | no change |
| 70 | `fastq scout <input> --kit ont-nbd114 -o scout-result.json` | true | `fastq.txt` `==== fastq scout ====` USAGE. `-o` is the documented short form of `--output`. Run live. | no change |
| 71 | `fastq demultiplex <input> --kit ont-nbd114 -o demux-out` | true | `fastq.txt` `==== fastq demultiplex ====` USAGE. Run live. | no change |
| 72 | `fastq ont-fluidigm-samples <input> --barcodes fluidigm-barcodes.csv -o fluidigm-out` | true | `fastq.txt` `==== fastq ont-fluidigm-samples ====` USAGE `<input> --barcodes <barcodes> --output <output>`. Run live. | no change |
| 73 | "`--concurrency` sets how many barcode folders are imported at once, and it defaults to 4" | true | `fastq.txt`: "Max concurrent barcode imports (default: 4)". `ONTImportOperationCoordinator.swift:29` `concurrency: Int = 4` on the GUI path too. | no change |
| 74 | "The window's Quality Binning and Optimize storage settings reach the command line as `--quality-binning` and `--optimize-storage`, and both apply only on the `--optimize-storage` path" | true | `fastq.txt`: "--optimize-storage  Run clumpify on flattened per-barcode FASTQ payloads" and "--quality-binning  Quality binning for --optimize-storage: none, illumina4, or eightLevel (default: none)". Settles ground-truth row 13, which asked for exactly this dependency. | no change |
| 75 | "`--storage-mode`, which chooses whether a barcode's chunks stay separate inside the bundle (`chunked`, the default) or are joined into one payload (`flattened`)" | true | `fastq.txt`: "How to store ONT chunks inside each bundle: chunked or flattened (default: chunked)". | no change |
| 76 | "`--overlap` sets the minimum number of barcode bases that must overlap the read for a match and defaults to 3" | true | `fastq.txt`: "Cutadapt minimum overlap length (default: 3)". | no change |
| 77 | "`--discard-unassigned` throws away the reads that matched nothing, rather than writing them to `unassigned.lungfishfastq`" | true | `fastq.txt` flag help plus the observed bundle name from row 62. | no change |
| 78 | "`--threads` sets how many cutadapt workers run at once and defaults to 4" (demultiplex) | true | `fastq.txt` `==== fastq demultiplex ====`: "Cutadapt thread count (default: 4)". | no change |
| 79 | "Demultiplexing has three options the pane does not show" | true | `--overlap`, `--discard-unassigned`, `--threads` are absent from `FASTQOperationToolPanes.swift:646-692` and are listed under `cli_only` in `parameters.yaml:128-137`. | no change |
| 80 | "`--primer-mismatches` … defaults to 2" | true | `fastq.txt`: "(default: 2)". `parameters.yaml:157-159`. | no change |
| 81 | "`--minimum-insert-length` sets the shortest insert kept, in bases, and defaults to 20" | true | `fastq.txt`: "Minimum CS1-CS2 insert length to retain (default: 20)". Confirmed in the live manifest, `"minimumInsertLength": 20`. | no change |
| 82 | "`--canonicalize-reverse-complements` folds an insert and its exact reverse complement into one counted exemplar and is disabled by default" | true | `fastq.txt`: "(default: disabled) (default: --no-canonicalize-reverse-complements)". Live manifest: `"canonicalizeReverseComplements": false`. | no change |
| 83 | "`--threads` is recorded for provenance only, because the split does not yet run in parallel" (Fluidigm) | true | `fastq.txt`: "Worker count reserved for future parallel materialization; currently recorded for provenance (default: 1)". | no change |
| 84 | "`--force` replaces an existing output directory instead of stopping" | true | `fastq.txt` `==== fastq ont-fluidigm-samples ====`: "--force  Replace an existing output directory". | no change |
| 85 | "`lungfish-cli fastq ont-pacbio-barcode-demux` splits full-length MHC nanopore amplicons on PacBio barcode pairs from a sheet carrying `sample_id`, `barcode_1`, and `barcode_2` columns" | true | `fastq.txt` `==== fastq ont-pacbio-barcode-demux ====` OVERVIEW and `--barcodes` help name exactly those three columns. | no change |
| 86 | Scout options: `--read-limit` default 10,000, `--accept-threshold` default 10, `--reject-threshold` default 3, plus `--error-rate`, `--overlap`, `--source-platform`, `--no-indels`, `--format` writing `text`, `json`, or `tsv` | true | `fastq.txt` `==== fastq scout ====` OPTIONS block, every default and value list as quoted, including "(values: text, json, tsv; default: text)". | no change |
| 87 | "Two nanopore commands have no window equivalent at all." | true | Neither `ont-pacbio-barcode-demux` nor `scout` appears in `FASTQOperationToolID` (`FASTQOperationDialogState.swift:1911-1957`). The PacBio path is reachable in the window only as an import recipe, which is a different surface from an operations-dialog equivalent, and the chapter documents that recipe separately. | no change |

## Author's defect claims

| # | Claim | Verdict | Evidence |
|---|---|---|---|
| 88 | Defect 1: `ONTDirectoryImporter.swift:372` estimates baseCount as compressed bytes x 1.5, giving 7,495,398 against an actual 4,348,051 | true | Line 371-372 reads exactly as quoted. The author's manifest records `"baseCount" : 7495398`; the chunk is 4,996,932 bytes; 4,996,932 x 1.5 = 7,495,398 to the byte. The fixture's true total is 4,348,051 bp. The read count in the same manifest (950) is exact, so the defect is confined to the base field. Worth reporting to the Lead as a source defect, not a documentation one. |
| 89 | Defect 2: demultiplexing reported Input reads 927 for a 950-read fixture and silently dropped 23 reads | true | Reproduced independently in a fresh output directory. Summary reported `Input reads: 927`. Comparing read ids between the input FASTQ and `unassigned.fastq.gz`: 950 input ids, all distinct; 927 output ids, all distinct; 23 present in the input and absent from the output; 0 extra. Neither the summary, the progress log, nor `demux-manifest.json` (`inputReadCount` 927) accounts for the 23. Two separate faults, a read-conservation break and a reported input count that is the post-loss figure. |
| 90 | Defect 3: `fastq ont-fluidigm-samples` prints a provenance error after completion **yet exits 0** | **half false** | The provenance error is real and reproduces: after "ONT Fluidigm sample split complete." at progress 1.0 and after the manifest is written, the command prints "Error: Local provenance file descriptor must be added from a URL so checksum and size are computed from disk: <output directory>". But the exit status is **1**, not 0. Measured three times in fresh output directories, each `echo $?` returning 1. The author's own report shows the reading that produced the wrong conclusion: their transcript captured `EXIT=` from `${PIPESTATUS}` after a pipe to `tail`, which does not report the command's own status. The substantive defect stands, that a command reports completion and writes its manifest and then fails, but the "a script checking the exit status would treat a failed run as a success" consequence is backwards. The real consequence is the opposite, a script sees a failure after the work has been done and the manifest written. The chapter does not quote this error, so no chapter wording changes. |

## Notes for the Lead

Row 29 is the only false claim and the only chapter edit required. It also
falsifies ground-truth row 10 of `ground-truth/03-reads.md`, which asserted
`Imports/` for this path from `AppDelegate+ToolsMenu.swift:636`. That line
governs the plain file-import route. The ONT run-folder route goes through
`ONTImportOperationCoordinator.resolvedOutputDirectory`, which returns the
project root, or a run-named sibling folder at the project root when a
previous ONT import already occupies it. `CONSISTENCY.md`'s Folders section
covers imports and FASTQ-operation outputs but does not yet describe this
third shape, so it is worth a line there once the chapter is corrected.

Row 90 corrects a defect report rather than the chapter. The Fluidigm
provenance failure should still be filed, with the exit status recorded as 1.

The lint-rule collision the author raised in their report has since been
settled campaign-wide. `sentence-colon.js` now subtracts the colons in a
settings paragraph's leading bold label (commit `fc365eaf5`, rule lines
40-47), and this chapter lints clean under
`LUNGFISH_MANUAL_STRICT=1`, with no warnings.

Row 47 is true but slightly incomplete, and row 68 is true but names two of
the three commands it describes. Both are optional tightenings, not errors.

## Counts

**86 true, 1 false, 0 unverifiable** across the chapter's 87 claims (rows 1
to 87). Two of the 86 are true with a noted incompleteness, row 47 (twenty
kits are offered only on the Cutadapt path) and row 68 (the project-root
requirement covers three commands, not two), and neither is an error.

The three defect claims are adjudicated separately as **2 true and 1 half
false** (row 90, where the provenance failure is real but the exit status is
1 rather than the reported 0, which reverses the stated consequence).

Combined, **88 true, 1 false, 0 unverifiable**, with one defect claim
partly wrong on a detail that does not touch the chapter text.
