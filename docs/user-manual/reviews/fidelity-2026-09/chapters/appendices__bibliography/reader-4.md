# Reader 4 report: Tool Bibliography

Persona: undergraduate who used Geneious for one semester, expects a dialog for
every operation, has never opened a terminal.

| Location | Issue | Suggested fix |
|---|---|---|
| Front matter, `audience: power-user` | The chapter is labelled for a power user, but I am the person who has to write a methods section. I almost stopped here because I assumed it was not for me. | Change the audience label, or add one sentence saying a student writing a methods section is an intended reader. |
| What it is, "A mapping run is minimap2, a variant call is GATK or LoFreq" | Four tool names arrive before any of them is explained. I know Geneious does mapping and variant calling, but I cannot tell which of these my own run used. | Add a clause saying these are examples and that the tables below say which one a given run used. |
| What it is, "the string beginning `10.` that a journal prints" | The backticks and the bare `10.` read as code to me. I did not know whether the dot is part of what I type. | Show one real DOI inline as the example instead of the fragment. |
| What it is, "A **provenance sidecar** is the JSON file LGE writes" | JSON is never glossed. I have never opened one and cannot tell whether it is something I can read or something only the program reads. | Gloss JSON in half a sentence and say whether a person can open it. |
| What it is, "whose **dependency set** is `2026.2`" | I could not judge this number. It is not a date and not a version I recognise, and nothing says where I find mine. | Say in one sentence where a reader finds their own dependency set. |
| What it is, "Name it in a methods section alongside the app version" | Two things to name and no example of the finished sentence. The worked example only appears seven sections later. | Point forward to the Citing LGE section here. |
| What the bibliography command prints, "The command lives only on the command line and has no menu item." | This is the wall for me. There is no window route to a bibliography anywhere in the chapter, and the fact is delivered in one clause before the text moves straight into typing. | State plainly near the top that this appendix has no window route, and say whether one is planned. |
| Same section, "Terminal is in the Utilities folder inside Applications" | Four terminal concepts land in one paragraph: the prompt, the Return key, `cd`, and dragging a folder in. I have done none of them and could not tell what order to do them in. | Break the first-time steps into a short numbered list. |
| Same section, "`lungfish-cli` is the command-line copy of LGE and it arrives with the app." | It arrives with the app, but I cannot tell whether typing `lungfish-cli` will simply work or whether I must install something or point at a path first. | Add one line giving a command that confirms it is present, and what to do when it is not. |
| Same section, "`./Analyses/mapping-HG002`" | I do not know what `./` means, and I could not tell whether I type this literally or replace it with my own folder. | Say explicitly that the path is replaced by the reader's own folder, dragged in. |
| Same section, "HG002" | Never explained. I guessed it is a sample, but not what kind or why it is the example. | Gloss HG002 as the standard human reference sample at first use. |
| Same section, "looks first for a root sidecar named `.lungfish-provenance.json`" | Two paragraphs of search order that I cannot act on and cannot check. I stopped because I thought I was supposed to go find these files myself. | Say the reader need do nothing with this, or move it into a note. |
| Same section, "a fixed list built into LGE that maps the many spellings" | The alias table is glossed, but I could not find out what is in it. Later the chapter tells me things are missing from it without ever letting me look. | Say whether the alias table can be listed, and how. |
| Same section, "`bwa`, `bwa-mem`, and `bwa-mem2`" | Three spellings of a tool that never appears in the first table and appears as BWA-MEM2 in the second. I could not connect them. | Use a tool the reader will actually meet in the tables as the example. |
| Same section, "the whole output is four lines" | I counted three lines in the block plus a blank one. I read it twice thinking I had missed something. | Fix the count, or stop counting lines. |
| Same section, "This is the same pair the workflow export chapter reports." | I have not read that chapter and this sentence gave me nothing to act on or check. | Drop it, or link it. |
| Same section, "exits with status 0" and "exits 64" | Exit status is never explained and I have no way to see one. Since the text tells me to read the output anyway, the numbers meant nothing. | Gloss exit status once, or cut the numbers and keep the plain advice. |
| Tools the command does not recognise, "Every assembler, every GATK entry, Clair3, WhatsHap, Freyja, BLAST, Bracken, EsViritu, RiboDetector, Savont, Trim Galore, and TaxTriage have no entry" | Twelve names in one sentence, several of them never introduced. I read it twice and still could not tell whether my own run is affected. | Split it, or say simply that most assembly and variant-calling tools are missing. |
| Same section, "prints `- spades 4.3.0` and nothing else" | I could not tell whether that is the entire output or only the unmatched line of a longer one. | Show the block as it appears, or say it is one line of a longer output. |
| Same section, "Two of these are worth reporting as defects" | I am told to report a defect and never told where or how. I do not know whether I am supposed to email someone. | Say where a defect is reported, or drop the instruction. |
| Tools installed with every copy of LGE, "These tools install without any plugin pack." | I cannot tell whether they are already on my machine or install the first time something needs them. | Say plainly that they are present in every copy. |
| Same section, "Eight of these tools have never published a paper." | I counted the rows carrying a URL instead of a DOI and got nine. I read the table twice. | Recount, or drop the number. |
| Same section table, "UCSC bedGraphToBigWig \| 482" | Every other version is dotted. A bare 482 made me think a digit was missing from the table. | Note that this tool versions by build number. |
| Same section, the code block of references | The table holds DOIs and the block holds author lists, and neither is a finished reference. Pasting into a reference manager, I did not know which half to copy or how to join them. | Show one complete finished reference as a worked example. |
| Same section, "in the wording LGE's own alias table carries where it has one" | I had to read this three times. I still cannot tell which entries are LGE's wording and which are the manual's. | Split it into two sentences. |
| Same section, "Article titles are transcribed exactly as published, punctuation included." | Reassuring, but I have nothing to check it against, because none of the entries in the block carry their DOI. | Say the DOI in the table above completes each entry. |
| Tools installed by a plugin pack, "either from the Plugin Manager at **Tools > Plugin Manager...**" | This is the only window route in the chapter, and it installs tools rather than producing citations. I briefly hoped it would give me a bibliography. | Say the Plugin Manager installs but does not cite. |
| Same section, "A run only used the ones its provenance sidecar names" | This is the key instruction for using the table, and it is buried in a lead-in sentence. Without a terminal I cannot read a sidecar, so I cannot follow it at all. | Say how a reader without the terminal finds which tools ran. |
| Same section table, "Bracken \| 1.0.0" | The reference below it is a 2017 paper. A version 1.0.0 for a tool published in 2017 made me doubt the whole table. | Confirm the version, or add a note explaining it. |
| Same section table, "Savont \| 0.6.3" and "EsViritu" | Neither tool is explained anywhere, and their pack names, such as full-length-mhc-genotyping, mean nothing to me. | Gloss the pack names, or link the plugin pack chapter from this table. |
| Same section, "keep Li and Durbin 2009 (`10.1093/bioinformatics/btp324`) as a secondary citation" | Secondary citation is not a term I was taught. I cannot tell whether a reviewer expects one or whether it is optional. | Gloss secondary citation in one clause. |
| Same section, "IQ-TREE 3 had not published its own paper when this release was built" | I am told to cite a different version's paper and to recheck before submitting, but not how to recheck. | Say to check the IQ-TREE project page for a version 3 paper. |
| Pinned external pipelines, "LGE pins one release of each and launches it through Nextflow" | Pin is used several times in the chapter and never glossed. I guessed it means fixed at one version. | Gloss pin at first use. |
| Same section, "cite the pipeline, the Nextflow paper above, and the pipeline's own component tools as its documentation directs" | Three citations out of one run, the third pointing at documentation I would have to find myself. This is the hardest instruction in the chapter to act on. | Say where the pipeline documentation lives. |
| Same section, "they belong in a methods section only if the pipeline that used them says so" | I cannot tell how to find out whether the pipeline says so. This is exactly the judgement call I do not have the background to make. | Give the default answer for a viralrecon run. |
| Reference databases, "Kraken 2 Standard, Standard-8, Standard-16, PlusPF, PlusPF-8, PlusPF-16, Viral, and MinusB" | Eight database names in one cell, none explained, and no way for me to tell which one my run searched. | Say where a reader finds the database name their own run used. |
| Same section, "20260626" | I worked out that it is a date, but only on the second read, and I cannot tell whether I write it as a date or as this exact string in a methods section. | Show the date form once. |
| Same section, "NCBI Taxonomy \| live" | The warning is good, but the fix, recording the run date, is something I would have had to know to do beforehand. Nothing says where the run date is kept. | Say the provenance sidecar records the run date. |
| Citing LGE itself, "LGE has no published paper and no DOI of its own" | I did not know whether a journal will accept a citation with no DOI, and nothing here reassures me. | Add half a sentence saying software citations without a DOI are normal. |
| Same section, "`lungfish-cli --version` prints the app version alone" | Another terminal-only route to a number I need. The app itself must show its version somewhere, and the chapter never says where. | Give the About window as the window route to the version. |
| Using this with a methods section, "run `provenance export --format methods`" | A second command introduced with no example output and no description of what a methods export contains. | Show one line of its output, or link the chapter that does. |
| Same section, "a reviewer reading a citation for a tool that never ran will wonder what else is approximate" | I read this twice before seeing that it argues for trimming citations rather than warning me about my data. | Split it into two sentences. |
| Whole chapter | Every action here is a terminal command. There is no window route to a bibliography, and the chapter never says whether that is deliberate or temporary. | Add one sentence near the top setting that expectation. |

Learned: the provenance sidecar records the exact version and command of every
tool that touched my data, so the citation and the methods detail come out of
the same file.

Could not do: produce a bibliography at all, because every route in this chapter
runs through a terminal I have never opened.

Liked most: "A citation says which tool you used and a provenance sidecar says
which build of it ran with which arguments, and a methods section needs both."
