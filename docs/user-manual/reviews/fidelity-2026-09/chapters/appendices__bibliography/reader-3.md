# Reader 3 report, Tool Bibliography

Reader: pre-med student, English is my second language, strong biology
vocabulary, no terminal experience. I read this as somebody who has to write
a methods section and cite the software.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "the journal expects you to credit" | "the journal expects" is a figure of speech. Which journal? I have not chosen one yet. I read it twice before I understood it means journals in general. | Say "journals require you to credit". |
| What it is, "Almost none of the analysis in LGE is LGE's own code." | I had to read this twice. The double negative plus the possessive "LGE's own" is hard. I could not tell if this was a warning or a fact. | Say "Most of the analysis in LGE is done by other people's tools." |
| What it is, "A mapping run is minimap2, a variant call is GATK or LoFreq, a classification is Kraken 2" | Four tool names with no gloss. I know what mapping and variant calling mean from class, but I do not know what "a classification" is here or what Kraken 2 does. | Add three words after each, for example "Kraken 2, which assigns reads to species". |
| What it is, "This appendix holds two things." | I expected two numbered items. The next sentences say "The first is" and "The second is" but they are buried in a paragraph. | Keep the sentence but make the two items a short list. |
| What it is, "the string beginning `10.`" | The backtick formatting made me think `10.` was something I type. It is only the start of a number. | Say "a number that always starts with 10." |
| What it is, "recording which executable ran" | "executable" is not glossed. I do not know what an executable is. | Say "which program file ran". |
| What it is, "a later release will pin different versions" | "pin" is used as a verb here and again later ("LGE pins one release", "pinned to `live`"). I have never seen "pin" used this way. It sounds like a physical pin. | Gloss at first use, for example "pin, meaning lock to one exact version". |
| What it is, "What you should do with this is straightforward." | "straightforward" plus a long sentence that then has three steps in it. The word promised easy but the sentence was not. | Split into three short sentences. |
| What the bibliography command prints, "The command lives only on the command line" | This is the first time I learn there is no button for this. I am a person who has never opened Terminal. This one paragraph is the whole instruction and it is dense. | Give the terminal steps as a short numbered list. |
| What the bibliography command prints, "which you reach by typing `cd` and then dragging the folder onto the Terminal window" | I could not perform this. Do I type `cd`, then space, then drag? Then do I press Return? The sentence does not say. | Show the exact three keystrokes, including the space and the Return. |
| What the bibliography command prints, "`lungfish-cli` is the command-line copy of LGE and it arrives with the app." | "arrives with the app" is idiomatic. I did not know if it is already installed or if I must install it. And if the app is in Applications, does typing `lungfish-cli` just work? | Say whether it works immediately after installing LGE, and what to do if the terminal says command not found. |
| Code block, `lungfish-cli provenance bibliography ./Analyses/mapping-HG002` | I could not run this. The `./Analyses/mapping-HG002` part is not explained, and the earlier text told me to drag a folder, so I do not know whether to type this path or drag something. | Say in one sentence that the path is where you drag your own folder. |
| What the bibliography command prints, "The single argument is a bundle or an output directory." | "argument" and "bundle" both unexplained. In my biology classes an argument is a debate. I guessed it means the thing after the command. | Gloss argument at first use. Bundle needs a gloss too, or a pointer. |
| What the bibliography command prints, "looks first for a root sidecar named `.lungfish-provenance.json`" | This whole paragraph, with four file paths and "in path order", is written for somebody who understands folders and hidden files. The leading dot means the file is hidden and nobody says so. I could not check whether my folder has it. | Say this is internal behaviour I do not need to follow, or say how to check. |
| What the bibliography command prints, "an **alias table**, a fixed list built into LGE that maps the many spellings a tool answers to onto one citation" | "a tool answers to" is a metaphor, like a person answering to a nickname. I read the sentence three times. It is also very long. | Say "a list of the different names one tool can be recorded under". |
| What the bibliography command prints, "Matching ignores case and punctuation and accepts a partial overlap" | I do not know what "partial overlap" means for text, and the example `samtools sort` matches SAMtools did not make it clear whether more matching or less matching is the risk. | Say plainly that a wrong tool could match, or that it cannot. |
| What the bibliography command prints, "the whole output is four lines" | I counted the code block. I see a title line, a blank line, and two citations. That is four only if the blank line counts. I stopped to count and doubted myself. | Say "two citations" instead of counting lines. |
| What the bibliography command prints, "This is the same pair the workflow export chapter reports." | No link here, though the Next section links that chapter. I did not know if I was supposed to go check. | Link it, or drop the sentence. |
| What the bibliography command prints, "The command exits with status 0 in all three of those cases" | "exits with status 0" and later "exits 64" are numbers I cannot judge. Is 0 good? Is 64 bad? I never opened a terminal so these mean nothing. | Say "reports success" and "reports an error", and keep the numbers in parentheses. |
| What the bibliography command prints, "so a script that calls it should read the output rather than the exit status" | This sentence is for a programmer. I do not write scripts and I could not tell whether I should worry. | Mark this sentence as advice for people writing scripts. |
| Tools the command does not recognise, "the unmatched list is common rather than exceptional" | I read this twice. It means "you will usually see one", but the phrasing is formal and indirect, and it worried me before the next sentence reassured me. | Say "you will usually see this list, and it does not mean anything is wrong." |
| Tools the command does not recognise, "Clair3, WhatsHap, Freyja, BLAST, Bracken, EsViritu, RiboDetector, Savont, Trim Galore, and TaxTriage" | Ten names in one sentence with no glosses. I only recognise BLAST. I could not tell which of these I might have used. | Break the sentence, or point to the tables where each tool has a pack name. |
| Tools the command does not recognise, "prints `- spades 4.3.0` and nothing else" | "nothing else" made me think the whole command output would be that one line, which contradicts the earlier example that had a title line. | Say "prints only its name and version in the unmatched list". |
| Tools the command does not recognise, "a recorded step name that does not resemble the tool it wrapped" | "wrapped" is a metaphor I do not know in software. I pictured wrapping paper. | Say "the tool it ran". |
| Tools the command does not recognise, "the unmatched line is only noise" | "noise" is an idiom. In my statistics class noise means random error, which is a different meaning. I paused. | Say "you can ignore that line". |
| Tools the command does not recognise, "Two of these are worth reporting as defects rather than working around quietly." | I do not know who I would report to, or how. "Working around quietly" is an idiom I had to think about. | Say where to report, or drop the sentence, since a student cannot act on it. |
| Tools installed with every copy, "These tools install without any plugin pack." | Passive and reversed. It sounds like the tools do the installing. I read it twice. | Say "LGE installs these tools for you". |
| Tools installed with every copy, "Eight of these tools have never published a paper." | Eight is a number I cannot check against the table without counting rows myself, and I counted nine URLs in the last column. I stopped and recounted twice and still was not sure. | Drop the count, or make sure it matches the URL rows. |
| Tools installed with every copy, table, "DOI or project page" column showing bare `10.1038/nbt.3820` | The DOIs are not links but the project pages are. As a student pasting into a reference manager I did not know whether to type `https://doi.org/` in front. The intro said DOIs resolve there, but the table does not repeat it. | Make the DOIs links, or add one line above the table. |
| Tools installed with every copy, "in the wording LGE's own alias table carries where it has one" | Very long noun phrase. I read it three times and I am still not certain what "carries" means here. | Say "as LGE writes it, where LGE has a version of it". |
| Tools installed with every copy, "Article titles are transcribed exactly as published, punctuation included." | I could not judge this. I noticed "Moelder" and "Mahe" look like names that should have accents, so "exactly as published" seemed contradicted. | Say the accents were removed, since a citation manager may need them. |
| Tools installed with every copy, code block of references | The references have no year for BBTools, Deacon, Trim Galore, SRA Tools, pysam, openpyxl, micromamba. My reference manager requires a year field. I did not know what to put. | Say what to use when there is no year, for example "n.d." or the access date. |
| Tools installed with every copy, "Three rows are easy to leave out of a methods section by accident." | Idiomatic and indirect. It took me a second read to see this is a warning to include them. | Say "Remember to cite these three, which are easy to miss." |
| Tools installed by a plugin pack, "Each of these arrives with the pack named beside it" | "arrives with" again, and "the pack named beside it" made me look for a column called something else before I found "Pack". | Say "the Pack column names the pack that installs each tool." |
| Tools installed by a plugin pack, "`lungfish-cli conda install --pack <name>`" | I could not perform this. `<name>` is a placeholder but nothing says to replace it, and "conda" is never explained. | Show one real example with a real pack name. |
| Tools installed by a plugin pack, "cite from this table selectively rather than wholesale" | "wholesale" is a shopping word. I had to reread. I also did not know how to tell which ones my run used. | Say "cite only the tools your sidecar names". |
| Tools installed by a plugin pack, table, versions like `2.31`, `0.6.3`, `482` | I cannot judge these. `482` for bedGraphToBigWig in the earlier table looked like a typo next to `1.24` and `2.8`. I doubted the whole table for a moment. | One sentence saying version numbering differs between projects. |
| Tools installed by a plugin pack, "Three notes belong with this table." | The paragraph after has three notes packed into three long sentences with parenthetical DOIs inside. I read it twice and lost track of which DOI belonged to which. | Make the three notes three separate short paragraphs or a list. |
| Tools installed by a plugin pack, "keep Li and Durbin 2009 ... as a secondary citation for the underlying Burrows-Wheeler algorithm when a reviewer asks" | I do not know what a Burrows-Wheeler algorithm is, and "when a reviewer asks" made me unsure whether to include it now or later. | Say whether to include it in the first submission. |
| Tools installed by a plugin pack, "is worth rechecking before you submit" | I could not perform this. Recheck where? I do not know where IQ-TREE would announce a paper. | Name the site to check. |
| Pinned external pipelines, "LGE pins one release of each and launches it through Nextflow" | "release" and "pipeline" are not glossed, and this is the first place I learn a pipeline contains other tools. | Gloss pipeline here, since it changes what I must cite. |
| Pinned external pipelines, "cite the pipeline, the Nextflow paper above, and the pipeline's own component tools as its documentation directs" | Three things to cite and the third is conditional on documentation I have not seen. I could not perform this step. | Link the documentation for each of the two pipelines. |
| Pinned external pipelines, "inside containers that the pipeline manages" | "containers" is unexplained. In biology a container is a tube. | Gloss or remove, since I do not need it to cite. |
| Pinned external pipelines, "they belong in a methods section only if the pipeline that used them says so" | "says so" is vague. Where does a pipeline say so? I could not act on this. | Point to where in the pipeline output or docs this appears. |
| Pinned external pipelines, "nf-core/viralrecon" reference with no year | Same reference-manager problem, and this one is the only entry in its code block without a year. | Add the year or say none exists. |
| Reference databases, "a reviewer cannot reproduce a Kraken 2 report without knowing which build you searched" | "build" is a new word for a database version. I know "build" from construction. | Say "which version of the database". |
| Reference databases, table, `20260626` and `kraken2-special-v1` and `panhuman-1` | I cannot judge any of these. `20260626` looks like a date but nobody says so, and the three styles side by side made me think one was wrong. | Say the numeric ones are dates in year-month-day form. |
| Reference databases, "The last row is the one to read twice." | I liked the warning but the phrase is an idiom, and it points at a row by position, so if a row is added later I would look at the wrong one. | Name the row, for example "The NCBI Taxonomy row is the important one." |
| Reference databases, "two runs months apart can assign the same read to differently named taxa" | This is the most important sentence for me and it is the hardest one. "differently named taxa" is an unusual construction and I read it three times. | Say "the same read can be given a different species name". |
| Citing LGE itself, "Write it in the form your journal uses for software" | I could not perform this. I do not know what form my journal uses, and the block above gives one form already, so I did not know whether to change it. | Say the block is a usable default. |
| Citing LGE itself, "`lungfish-cli --version` prints the app version alone" | Another terminal command with no context. By this point I still do not know if my terminal will recognise the command. | Move all terminal commands together, or note once that they need the setup in the earlier section. |
| Using this with a methods section, "Do not paste a table from this appendix into a paper on its own." | "on its own" is ambiguous. Does it mean never paste the table, or paste it plus something else? I read it twice. | Say "Do not paste these tables into a paper." |
| Using this with a methods section, "run `provenance export --format methods`" | This command is written without `lungfish-cli` in front of it, unlike every earlier example. I did not know whether that was intentional. | Write the full command. |
| Using this with a methods section, "Three habits keep the result honest." | "keep the result honest" is a metaphor. A result cannot be dishonest. I understood eventually but paused. | Say "Three rules keep your citations accurate." |
| Using this with a methods section, "a reviewer reading a citation for a tool that never ran will wonder what else is approximate" | Long, and "what else is approximate" is indirect. It took a second read to see it means the reviewer will distrust the paper. | Say "will doubt the rest of your methods". |
| Next, "See [Tool Versions](tool-versions.md)" | Three links in two sentences, and I could not tell which one I should read next as a student writing a methods section. | Say which one matters most for citing. |

One thing I learned. The database version and the run date matter as much as
the tool version, because the NCBI taxonomy is not frozen.

One thing I still could not do. I could not run the bibliography command,
because the terminal instructions are one dense paragraph and I do not know
what to do if the command is not found.

The sentence I liked most. "A citation says which tool you used and a
provenance sidecar says which build of it ran with which arguments, and a
methods section needs both."
