# Reader 2 report, Tool Bibliography

Persona. A senior who has spent two years pipetting in a wet lab. I know
library prep and PCR. I have never analyzed data and have never opened a
terminal. I am reading this because I have to write a methods section and I
need to know what to cite.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "A mapping run is minimap2, a variant call is GATK or LoFreq" | The four tool names arrive with no gloss. I know what mapping and variant calling are from lecture, but I do not know that minimap2 is one program among several, so I cannot tell whether this sentence is a list of examples or a statement of what LGE always uses. | Say the tool names are examples of the programs LGE runs for each step. |
| What it is, "each of those has authors who published a paper" | I read this twice. The paragraph starts by telling me LGE is not its own code, then tells me to cite other people, and I did not see until the second read that the point is that I owe citations to the tool authors, not to LGE. | Put the instruction first, then the reason. |
| What it is, "A **provenance sidecar** is the JSON file LGE writes" | JSON is not glossed. Also I do not know where the sidecar lives or whether I ever see it, so I cannot tell if I already have one. | Gloss JSON as a plain text file format, and say the sidecar is written automatically so I already have one. |
| What it is, "the checksums of what went in and came out" | Checksum is not glossed and I do not know why a citation appendix mentions it. | Gloss checksum in half a sentence or drop it here. |
| What it is, "whose **dependency set** is `2026.2`" | The gloss is given, but I cannot judge the number. I do not know where to find my own dependency set if my copy of the app is not 2026.9.13, and the sentence assumes I know my own version. | Say how to check the app version and dependency set before naming this one. |
| What it is, "paste the citations it prints into your reference manager" | Reference manager is assumed. More importantly the citations here are plain sentences, not a file my manager can import, so I do not know what pasting a sentence into Zotero or EndNote does. | Say the output is plain text to be entered by hand, not an importable file. |
| What the bibliography command prints, "The command lives only on the command line and has no menu item" | This is the first place the chapter tells me I need a terminal. The opening promised something straightforward. I have never opened a terminal and I did not know until here that the whole appendix requires it. | Warn in the opening section that this appendix needs Terminal. |
| What the bibliography command prints, "which you reach by typing `cd` and then dragging the folder" | I cannot perform this. There is no example of what the line looks like after the drag, no statement that a space is needed after `cd`, and no way for me to tell whether it worked. | Show one full example `cd` line and say what the prompt looks like when it succeeds. |
| What the bibliography command prints, "`lungfish-cli` is the command-line copy of LGE and it arrives with the app" | It does not say `lungfish-cli` will be found automatically. In my experience with any installed thing, you have to be in the right place. I could not tell whether I need a path. | State that `lungfish-cli` works from any folder once the app is installed, or give the path. |
| What the bibliography command prints, "`lungfish-cli provenance bibliography ./Analyses/mapping-HG002`" | I cannot map this to my own data. `./Analyses/mapping-HG002` is not explained, `./` is not explained, and HG002 is not explained. I do not know which of my folders is the one to name. | Say `./` means the folder you are in, and name what kind of folder to substitute. |
| What the bibliography command prints, "a bundle or an output directory" | Bundle is used here for the first time as a thing I must recognize, and directory and folder are used interchangeably in the same sentence. I do not know if a bundle is something I made. | Gloss bundle at first use and use one word for folder throughout. |
| What the bibliography command prints, "trying `provenance/bundle.lungfish-provenance.json` and then" | A whole paragraph of file names and search order. I read it twice and still do not know what I am supposed to do with it. Nothing here is an action. | Say up front that the command finds the file on its own and this paragraph is only for troubleshooting. |
| What the bibliography command prints, "maps the many spellings a tool answers to onto one citation" | Alias table is glossed but the example `bwa`, `bwa-mem`, `bwa-mem2` reaching one entry conflicts with the later note that BWA-MEM2 should be cited as the 2019 paper and BWA as 2009. I could not tell which citation I would actually get. | Say which of the two papers the shared entry prints. |
| What the bibliography command prints, "Matching ignores case and punctuation and accepts a partial overlap" | Partial overlap is not explained. Since this decides whether I get a citation or an unmatched line, I would want to know when it goes wrong, and I cannot judge it. | Drop the mechanism or give one sentence on when partial matching mismatches. |
| What the bibliography command prints, "On a minimap2 mapping run of the HG002 chromosome 20 reads the whole output is four lines" | I counted two lines of citation plus a header and a blank. Saying four lines when I see two bullets made me stop and recount. | Say two citations rather than counting blank lines. |
| What the bibliography command prints, "This is the same pair the workflow export chapter reports" | I have not read that chapter and there is no link here, unlike the links in Next. I could not check the claim. | Link the chapter or drop the cross-reference. |
| What the bibliography command prints, "exits with status 0 in all three of those cases" | Exit status is never glossed and only two example outputs were shown, so "all three" has no antecedent I can find. Also "a script that calls it" assumes I write scripts. | Gloss exit status, name the three cases, and mark the script advice as not for hand use. |
| What the bibliography command prints, "prints `Error: ... No Lungfish provenance sidecar found in <path>` and exits 64" | `<path>` is not explained as a placeholder, and 64 means nothing to me. More useful would be what to do about it. | Say what to check when this error appears. |
| Tools the command does not recognise, "so the unmatched list is common rather than exceptional" | This tells me the tool I most likely used will not be cited by the command, which undercuts the opening promise that I can paste the output into a reference manager. I had to reread the opening to see that the table was the real answer. | State in the opening that the command covers only part of the tool set. |
| Tools the command does not recognise, "GATK steps are recorded as `gatk-haplotype-caller`, `gatk-joint-genotype`" | This paragraph explains why the software behaves as it does. As a student I cannot act on it, and it sits between two paragraphs that do tell me to act. | Move the mechanism into a note, or say plainly it needs no action from me. |
| Tools the command does not recognise, "`lungfish extract reads` or `lungfish genotype export-pivot-xlsx`" | Two internal step names I have never seen. I cannot tell whether these appear in my output or whether I would recognise an LGE step by some pattern. | Say LGE steps all begin with `lungfish` so I can spot them. |
| Tools the command does not recognise, "Two of these are worth reporting as defects" | I do not know where to report a defect and there is no link. Also the sentence tells me the manual thinks the software is wrong, which made me unsure whether to trust the rest. | Give the place to report, or drop the aside from a reader-facing appendix. |
| Tools installed with every copy of LGE, "install without any plugin pack" | Plugin pack is glossed only in the next sentence, after the heading uses it and this sentence uses it. | Move the gloss ahead of first use. |
| Tools installed with every copy of LGE, "Eight of these tools have never published a paper" | I counted the rows with URLs instead of DOIs and got eight, but only after counting. The number is unverifiable at a glance and adds nothing. | Say some rows show a project page instead. |
| Tools installed with every copy of LGE, table header "DOI or project page" | The DOI column shows bare strings like `10.1038/nbt.3820` with no `https://doi.org/` prefix, though the earlier gloss said a DOI resolves at that address. I did not know whether to paste the bare string into a paper. | Say how to turn the bare DOI into a link. |
| Tools installed with every copy of LGE, "in the wording LGE's own alias table carries where it has one" | I read this twice. It implies some entries are from the alias table and some are not, but the block does not say which, so I do not know which lines are authoritative. | Drop the qualification or mark the entries that differ. |
| Tools installed with every copy of LGE, code block "Deacon            Deacon host-depletion toolkit." | Several entries have no author, year, or journal, so I cannot form a citation from them. Host depletion is also not glossed. | Say what to write in a reference list when only a project name exists. |
| Tools installed with every copy of LGE, "pysam parses the BAM files behind several viewport statistics" | BAM and viewport are both unglossed here. I do not know which numbers in my figures came from a viewport, so I cannot apply the advice. | Gloss BAM and name the specific readouts. |
| Tools installed by a plugin pack, "from `lungfish-cli conda install --pack <name>`" | conda and `--pack` are unexplained, and `<name>` is a placeholder I would have to guess at. I could not run this. | Show one complete example with a real pack name. |
| Tools installed by a plugin pack, "A run only used the ones its provenance sidecar names" | This tells me to check the sidecar, but the chapter never showed me how to open or read one. The command output is the only view I was given. | Point to where the sidecar is read, or say the command output is the list. |
| Tools installed by a plugin pack, table Pack column values "full-length-mhc-genotyping" | Pack names are given with no explanation of what they contain, and MHC is not glossed. I could not tell which pack my own run used. | Gloss MHC and say the pack name appears in the Plugin Manager. |
| Tools installed by a plugin pack, "WhatsHap | 2.3 | phasing | 10.1101/085050" | This DOI is a bioRxiv preprint, as the reference block confirms with "bioRxiv. 2016." Nothing tells me whether a preprint is acceptable in a methods section. | Note that this reference is a preprint. |
| Tools installed by a plugin pack, "Bracken | 1.0.0" | The version reads 1.0.0 while my project notes and the classification chapters describe Bracken 3.1. I could not tell which number to write down. | Confirm the shipped Bracken version. |
| Tools installed by a plugin pack, "keep Li and Durbin 2009 (`10.1093/bioinformatics/btp324`) as a secondary citation" | Secondary citation is not a term I have been taught. I do not know whether that means I list both papers or only mention one if challenged. | Say plainly whether both go in the reference list. |
| Tools installed by a plugin pack, "the IQ-TREE 2 paper is the correct citation for now and is worth rechecking before you submit" | I am told to recheck but not where or against what. | Name the place to check for an IQ-TREE 3 paper. |
| Pinned external pipelines, "launches it through Nextflow, so cite the pipeline, the Nextflow paper above, and the pipeline's own component tools as its documentation directs" | Three things to cite in one sentence, and the third sends me to documentation I am not given. This is the sentence I most needed to act on and the one I could least act on. | Split into a short list and link the pipeline documentation. |
| Pinned external pipelines, "inside containers that the pipeline manages" | Container is not glossed, and the distinction between a pipeline-managed tool and an LGE-managed one is the whole basis for the advice that follows. | Gloss container in half a sentence. |
| Pinned external pipelines, "they belong in a methods section only if the pipeline that used them says so" | I cannot perform this. I have no way to find out what the pipeline says, and the next sentence gives me the four citations anyway, which reads as contradictory. | Say where the pipeline reports which tools it ran. |
| Reference databases, "Kraken 2 Standard, Standard-8, Standard-16, PlusPF, PlusPF-8, PlusPF-16, Viral, and MinusB | 20260626" | Eight database names crammed into one cell with no explanation of the differences. I would not know which of the eight my run used or how to find out. | Say where the run records which database was searched. |
| Reference databases, "Kraken 2 SILVA and Greengenes | kraken2-special-v1" | This version string is not a date and not a number I can interpret. I cannot judge whether it is current. | Say what the string means or give a date. |
| Reference databases, "Human Read Scrubber Database | 20260706v2" | Read scrubber is not glossed anywhere in the chapter. I only guessed it removes human reads because the next row says so. | Gloss the scrubber, or name the rows consistently. |
| Reference databases, "The NCBI taxonomy is pinned to `live`" | Pinned is used in two senses in this chapter, for fixed versions in the heading above and here for the opposite. I read the row twice before understanding that `live` means not pinned. | Say the taxonomy is not pinned. |
| Citing LGE itself, "Write it in the form your journal uses for software" | I do not know what form that is and no example is offered beyond the raw block. As a student this is the exact thing I needed spelled out. | Give one worked example in a common style. |
| Citing LGE itself, "`lungfish-cli --version` prints the app version alone, reporting `2026.9.13`" | This is the first time I am told how to check my version, four sections after the version mattered. Also it is another terminal command. | Move this to the opening where the version is first discussed. |
| Citing LGE itself, "the dependency set comes from the tool lock manifest that ships with it" | Tool lock manifest is a new unglossed term at the very end, and I am not told how to open it, so I still cannot find my own dependency set. | Say how to read the dependency set, or that the bibliography command prints it. |
| Using this with a methods section, "run `provenance export --format methods` against the same folder" | A second command appears with no full example, unlike the first one. I do not know whether `lungfish-cli` goes in front of it. | Show the full command line. |
| Using this with a methods section, "a reviewer reading a citation for a tool that never ran will wonder what else is approximate" | I read this twice. The clause about the reviewer wondering is doing the work of the instruction and I missed the instruction on the first pass. | Lead with the rule, then the reason. |
| Using this with a methods section, "name the database version and the run date for any classification result" | I am told to record the run date but never told where LGE records it. Nothing earlier in the chapter showed a date in any output. | Say where the run date is found. |
| Next, "See [Tool Versions](tool-versions.md) for the full version, license, and environment of every managed tool" | If that chapter has the full versions, I could not tell which of the two chapters is authoritative when they disagree, which matters because of the Bracken row above. | Say which chapter governs when versions differ. |

One thing I learned. Every tool that ran in my analysis leaves a record next
to my results, and that record is what tells me who to cite.

One thing I still could not do. Get a single citation out of this, because I
could not perform the very first step of opening a terminal and reaching my
project folder from the instructions given.

The sentence I liked most. "A classification result depends on the database
as much as on the classifier, and a reviewer cannot reproduce a Kraken 2
report without knowing which build you searched."
