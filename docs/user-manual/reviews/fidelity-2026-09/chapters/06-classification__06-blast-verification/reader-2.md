# Reader 2 report, BLAST Verification

Persona: senior, two years of wet-lab pipetting, strong on library prep and PCR, no data analysis, never opened a terminal.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| What it is, "by matching short chunks of its sequence" | I do not know what a "chunk" is here. In PCR a fragment has a defined length. Is this k-mers? The word is doing real work and it is vague. | Say the classifier matches short subsequences of a fixed length, and name the length or say it is set by the database. |
| What it is, "reference database installed on your own machine" | I never installed a database. I do not know if I have one, where it is, or how big. The chapter later says no database is needed here, which made me think I had skipped a step. | Add a half sentence saying this database came with the Kraken 2 setup in the earlier chapter. |
| What it is, "asks a narrower question than BLAST usually answers" | I have never used BLAST, so I do not know the wide question it usually answers. The comparison told me nothing. | Drop the comparison, or say plainly that LGE only asks whether the top match agrees with the classifier. |
| What it is, "supporting the classifier ... or contradicting it" | I could not tell what "a good match" is. The whole count depends on a threshold I am never given a number for. | State the identity, coverage, and e-value cutoffs that make a match good, in the same paragraph. |
| What it is, "**Supported** at 80 percent or above, **Mixed** from 40 up to 80" | I could not judge these. Why 80 and not 90? With 20 reads, how many contradicting reads move me out of Supported? | Give one worked count, for example 17 of 20 supporting is 85 percent and reads as Supported. |
| What it is, "organisms that share long stretches of sequence can satisfy the check for each other" | Read twice. I eventually got it, but "satisfy the check for each other" is a construction I had to unpack. | Say two closely related organisms can each look like a good match for the other's reads. |
| Why you would do this, "it does not scale" | Not explained. Scale in what sense, cost, time, number of samples? | Say the search takes minutes per taxon and NCBI limits how fast you may submit. |
| Why you would do this, "hand the reads to a relative" | Read twice. "Relative" is being used for an organism, but the sentence reads as if reads are handed to a person. | Say the classifier assigns the reads to a related organism it does have. |
| Why you would do this, "a threshold you set in advance" | I do not remember setting a threshold, and I do not know what number it would be. The cross-reference does not say. | Name the threshold and its typical value, or say which setting in the Kraken 2 chapter it refers to. |
| Why you would do this, "a low-abundance hit" | "Abundance" is not glossed here. In my head abundance is a concentration, but here it seems to be a read count. | Gloss it on first use as the number or share of reads assigned to a taxon. |
| Why you would do this, "the SRR36291587 SARS-CoV-2 reads" | An accession like this means nothing to me on sight. I did not know it was an SRA run number until two sections later. | Say on first use that SRR36291587 is a public sequencing run identifier at NCBI. |
| Before you start, "fetch them from the Sequence Read Archive" | The download size and time are not given, so I could not plan. My laptop has limited space. | State the approximate download size and read count. |
| Before you start, "the source and licence notes for the data" | I could not tell whether I need to read the licence before using the data or whether it is optional context. | Say plainly that the notes are background and no action is required. |
| Before you start, "a finished classification open in its viewport" | I do not know what "viewport" means. It is used throughout and never defined. | Gloss viewport at first use as the main results panel in the middle of the window. |
| Before you start, "the reads you verify leave your Mac" | This alarmed me and I could not act on it. Are the reads stored at NCBI, for how long, and can anyone else see them? | Add one sentence saying what NCBI does with submitted sequences and for how long. |
| Before you start, "any sample whose sequence you are not free to share" | I do not know how to tell whether my sample is one of those. Human samples? Patient samples? Unpublished data? | Give one concrete example, such as identifiable human sequence under an IRB restriction. |
| Before you start, "the job's request ID" | Term used before the "Reading the results" and CLI sections explain RID. First use is unexplained. | Gloss request ID here as the tracking number NCBI assigns the job. |
| Before you start, "Both outcomes happened while this chapter was written" | Read twice. I could not tell whether this was reassurance or a warning that the feature is unreliable. | Say directly that a timeout is common and is not a failure of your data. |
| Procedure, step 2, "in the action bar under the table" | I could not find this from the text. I do not know what an action bar looks like or whether "the table" is the taxon list. | Name the panel and say the action bar is the row of buttons along the bottom of the results table. |
| Procedure, step 2, "its tooltip says why" | I did not know a tooltip appears on hover here, and I could not act on this without knowing to hover. | Say to hover the pointer over the disabled button to see the reason. |
| Procedure, step 3, "Its title is `Verify \"<taxon>\" via NCBI BLAST`" | The angle brackets and quotes are code notation I had to decode. I first looked for a literal `<taxon>`. | Show the title as it appears for the worked example instead of the placeholder form. |
| Procedure, step 5, "shows the three phases of the job in turn" | I could not judge how long each phase should take, so I would not know when something is stuck. | Give a rough duration for the waiting phase, and repeat the ten-minute cutoff here. |
| Procedure, "The run also appears in the Operations panel" | I do not know what the Operations panel is or how to open it. | Say where the Operations panel lives and how to show it. |
| Settings, "**Reads to submit:.**" | Typographical error, a colon followed by a period. It made me stop and reread the line. | Remove the stray colon. |
| Settings, "checked against the whole nucleotide database" | "Nucleotide database" is not glossed until three paragraphs later when `nt` appears. | Move the gloss of the nt database up to first mention. |
| Settings, "the slider runs from 1 to 50 ... On the command line this is `--reads`, which accepts 1 to 100" | I could not judge why the two limits differ, and it made me wonder if the GUI is worse. | Say why the interface caps at 50, or note that the difference does not affect the result. |
| Settings, "takes some of the longest reads ... and fills the rest at random" | "Some" is not a number. I could not tell whether that is half or a handful, which matters for whether the sample is representative. | Give the proportion, for example the longest five reads and the rest at random. |
| Settings, "It spreads its picks across quarters of the reference genome" | I did not understand why NAO-MGS differs, or whether this affects how I read its result. | Add a clause saying this gives even genome coverage and does not change how the confidence word is computed. |
| Settings, "The program is `blastn`" | Unexplained code-formatted name. I did not know whether I would ever type this. | Say blastn is the nucleotide-to-nucleotide BLAST program and you never enter it yourself. |
| Settings, "The `--extra-args` flag described in the last section" | I have never opened a terminal, so a forward reference to a flag was a dead end for me. | Say this applies only to the command-line path described later. |
| Reading the results, "a ten-dot bar" | Read twice. I could not work out how 20 reads become 10 dots, or what a partly represented read looks like. | Say each dot stands for one tenth of the submitted reads, rounded. |
| Reading the results, "reading `<n> with conflicting organisms`" | I could not judge what counts as high. The text says "a high count" without a number. | Give a rough threshold, for example more than a quarter of the submitted reads. |
| Reading the results, "disagrees about the genus" | Genus I know from taxonomy, but I could not tell what "the read's own list of hits" is until I read the next paragraph about child rows. | Say the read's own five returned matches name organisms in different genera. |
| Reading the results, "one row per submitted read, each of which expands" | I could not perform this. There is no instruction for how to expand a row. | Say to click the disclosure triangle at the left of the row. |
| Reading the results, "A read at 99.6 percent over 250 aligned bases disagreed at one position" | I had to do the arithmetic to trust this, and it did not come out clean for me at 99.6. | Use a figure that divides evenly, or state the calculation. |
| Reading the results, "the scale is brutal" | Read twice. "Brutal" is vivid but told me nothing about the scale being logarithmic. | Say the value spans many orders of magnitude, so small differences in the exponent matter a lot. |
| Reading the results, "A value of `1e-30`" | Scientific notation in this form is not glossed. I know 10 to the minus 30 but not the `1e-30` spelling. | Write it once as 10 to the power minus 30 alongside the notation. |
| Reading the results, "Four more columns are hidden ... which are Accession, Coverage, Align Length, and Verdict, along with Tax ID" | Read twice. It says four, then lists five. I could not tell if Tax ID is one of the four. | Say five hidden columns, or explain why Tax ID is counted separately. |
| Reading the results, "Verified when the top hit clears identity, coverage, and e-value together" | The three cutoffs are never given anywhere in the chapter, so I cannot reproduce or report this call. | State the three thresholds. |
| Reading the results, "**Export** in the summary bar writes the table as CSV or TSV" | I do not know what TSV is, and I could not tell which one to pick for Excel. | Gloss both and say which opens cleanly in a spreadsheet. |
| What good looks like, "e-values small enough to be written in scientific notation" | Circular to me. Any small number can be written that way. It did not give me a cutoff. | Give a number, for example below 10 to the minus 20. |
| What good looks like, "with both submitted reads matching" | Two reads, when the default is 20. I could not tell why the worked example used two, and it undercut the earlier advice about firmer answers. | Say the example uses two reads to keep the run fast, and that a real check would use the default. |
| What good looks like, "low-complexity or adapter-contaminated" | Adapter contamination I know from library prep. "Low-complexity" I do not. | Gloss low-complexity as repetitive sequence such as long runs of one base. |
| What good looks like, "two supporting reads out of three is a 67 percent share" | I could not reconcile this with the worked example, which sent two reads and was called Supported on the same thin evidence. | Note that the worked example is equally thin and is only trustworthy because the answer was expected. |
| On the command line, section as a whole | I have never opened a terminal and there is no pointer telling me I may skip this. I read all of it looking for something I needed. | Open the section with a line saying interface users can skip it. |
| On the command line, "The source FASTQ must be uncompressed" | I could not perform this. My downloads arrive as .fastq.gz and the chapter does not say how to uncompress one. | Point to the chapter or command that uncompresses a FASTQ. |
| On the command line, "its number comes from the kreport's sixth column" | I have never opened a kreport and would not know how to count its columns. | Prefer the Copy Taxon ID route for interface users and mention the column second. |
| On the command line, "`--max-concurrent` caps how many submissions this process keeps in flight" | "In flight" is jargon I had to reread. | Say it caps how many searches run at the same time. |
| On the command line, "treating fifty sequences an hour as its ceiling" | I could not judge this against the slider. If I send 50 reads, have I used my whole hour? | Say explicitly how many verifications of the default size fit in an hour. |
| Where else verification starts, "EsViritu, TaxTriage, NAO-MGS, and Novel Virus Diagnostics" | Four tool names appear in the Procedure lead-in with no gloss, well before this section explains anything about them. | Add a clause at first mention saying these are the other classifiers covered in this part. |
| Where else verification starts, "verification submits the selected contig's own sequence" | "Contig" is not glossed. I have heard it but could not define it. | Gloss contig as a longer sequence assembled from overlapping reads. |
| Where else verification starts, "the 12S metabarcoding viewport" | Both "12S" and "metabarcoding" are unexplained, and this is their only appearance. | Gloss in one clause or drop the sentence. |

One thing I learned: BLAST verification only checks whether the reads match the named organism, and it says nothing about how many reads there were.

One thing I still could not do: decide whether my own sample is safe to send to NCBI, because the chapter warns me the reads leave my Mac but never tells me how to judge that.

The sentence I liked most: "Verification checks an identity, not a quantity."
