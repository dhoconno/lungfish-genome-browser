# Reader 3 — BLAST Verification

Persona: pre-med student, English is my second language. Biology words from
textbooks are fine. Idioms, figures of speech, and long sentences slow me
down. I have never opened a terminal.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| What it is, "matching short chunks of its sequence" | "Short chunks" is vague. A classifier textbook would say k-mer. I could not tell if a chunk is 10 bases or 1000, so I could not picture the matching at all. | Name the unit and give its size, for example "short fixed-length pieces of sequence called k-mers, typically 31 bases". |
| What it is, "it settled for second best" | Idiom. I read it twice. "Settled for" is not in my vocabulary as a phrase, and "second best" sounds like a competition. | Replace with a plain statement, for example "no warning that the reported organism is only a relative of the true one". |
| What it is, "asks a narrower question than BLAST usually answers" | I have never used BLAST, so I do not know what question BLAST usually answers. The comparison told me nothing because I have no baseline. | State the usual question first, for example "BLAST usually reports every good match, but LGE asks only whether the best match agrees with the classifier". |
| What it is, "a spot check rather than a re-analysis" | "Spot check" is an idiom I had to guess from context. | Say "a check on a small sample rather than a full repeat of the analysis". |
| What it is, "**Mixed** from 40 up to 80" | I could not tell if 80 belongs to Mixed or to Supported. "Up to 80" is ambiguous in my English, and the boundary matters because the chapter later says 67 percent lands in Mixed. | Write the bands as explicit ranges, for example "80 percent and above", "40 percent to under 80 percent", "under 40 percent". |
| What it is, "The share of supporting reads among those two groups" | I had to reread to work out the denominator. It is supporting divided by (supporting + contradicting), and reads with no good match are excluded, but the sentence hides that in "among those two groups". | Give the fraction directly, for example "supporting reads divided by supporting plus contradicting reads". |
| What it is, "a handful of reads is still a handful" | Figure of speech that repeats itself. I understood the words but not the point, and I read the sentence three times. | Say the point plainly, for example "twenty reads is a small sample and can be unlucky". |
| What it is, "organisms that share long stretches of sequence can satisfy the check for each other" | Long sentence, and "satisfy the check for each other" is not a phrase I could parse. Do both organisms pass, or does the wrong one pass? | Say "if two organisms share long identical regions, a read from one can match the other and still be counted as supporting". |
| Why you would do this, "it does not scale" | Jargon from software, not biology. I do not know what "scale" means here. Does it mean it gets slower, or that it stops working? | Say "you cannot run it on every taxon in a large result". |
| Why you would do this, "a network round trip" | I do not know this term. I guessed it means the data goes to NCBI and comes back, but I was not sure whether it costs money or only time. | Gloss it at first use, for example "a wait of a few minutes while your reads travel to NCBI and the answer comes back". |
| Why you would do this, "has to hand the reads to a relative" | Metaphor. Databases do not hand things to relatives. I understood only after the next clause. | Say "assigns the reads to a related organism that is in its database". |
| Why you would do this, "hold the long tail to a threshold you set in advance" | Three unexplained things in one clause. "Long tail" is a statistics idiom, "hold to a threshold" is unclear as an action, and I do not know what quantity the threshold applies to. | Gloss "long tail" as "the many taxa with very few reads" and name the quantity, for example "a minimum read count". |
| Why you would do this, "BLAST breaks the tie" | Idiom. I worked it out but had to pause. | Say "BLAST agrees with one classifier, or with neither". |
| Why you would do this, "before a result leaves your hands" | Idiom. Also unclear what "leaves your hands" means concretely, since I am a student and my results only go to my supervisor. | Say "before you report a result to someone else". |
| Why you would do this, "The fourth is a sanity check ... and the one people skip." | "Sanity check" is an idiom, and the sentence packs the fourth situation, its cost, and a comment about human behaviour into one line. I read it twice. | Split into two sentences and replace "sanity check" with "a final confirmation". |
| Why you would do this, "the SRR36291587 SARS-CoV-2 reads" | The chapter never says what this sample is or how many reads it has, so I could not judge whether 20 out of it is a lot or almost nothing. | Add the read count and one line saying what the sample is, for example "a clinical swab sequenced on Illumina, about N reads". |
| Why you would do this, "*Severe acute respiratory syndrome coronavirus 2*" | The italic Latin name is fine, but the chapter never links it to SARS-CoV-2, which it uses elsewhere in the same paragraph. For a moment I thought they were two organisms. | Say "SARS-CoV-2" in brackets after the Latin name at first use. |
| Why you would do this, "the classifier databases are pathogen databases, so a viral sample is what exercises the whole path" | "Exercises the whole path" is a metaphor from software. I could not tell what "path" means, so the justification did not land. | Say "a viral sample is the only kind that uses every step of this feature". |
| Before you start, "choose **File > New Project** (Cmd-N)" | This is the only step in the chapter I could actually perform without guessing, but the chapter does not say what to do after picking a folder, and I did not know whether the project needs a name. | Add one sentence saying what appears after you pick the folder. |
| Before you start, "fetch them from the Sequence Read Archive as accession `SRR36291587`" | This sends me to another chapter for a whole download, and the chapter does not warn me how long it takes or how large the file is. I did not know whether to start it before reading on. | Add the download size and rough time, and say to start it first. |
| Before you start, the bare GitHub URL on its own line | A raw URL in the middle of prose. I did not know if I need to visit it to follow the chapter, or only if I want the licence notes. | Add a lead-in saying the link is optional background. |
| Before you start, "You also need a finished classification open in its viewport" | "Viewport" is used here for the first time in this chapter with no gloss. I guessed it means the main window showing results, but I was not confident. | Gloss "viewport" at first use, for example "the results window inside LGE". |
| Before you start, "the reads you verify leave your Mac" | I could not judge the consequence. Does NCBI store them, can others see them, is this a problem for patient samples? The chapter says "the fact matters" but not why. | State the concrete risk in one sentence, for example "NCBI receives and may log the sequences, so do not send reads from samples with restricted consent". |
| Before you start, "LGE offers no local BLAST to fall back on" | "Local BLAST" is not explained, and "fall back on" is an idiom. I do not know that BLAST can also be installed on a computer. | Say "LGE cannot run BLAST on your own machine, so a sample you may not send out cannot be verified here". |
| Before you start, "your job queues at NCBI behind everyone else's" | I did not know a "job" was a thing until this sentence, and the chapter never says how long the wait usually is, only the ten minute cutoff. | Give a typical wait, for example "usually one to five minutes". |
| Before you start, "Both outcomes happened while this chapter was written, on the same reads, an hour apart." | I read this twice. "Both outcomes" points back two sentences to something not stated as two outcomes, and only after reading the CLI section did I understand it means success and timeout. | Name the two outcomes in the sentence. |
| Procedure, "the taxonomy viewport, which is where a Kraken 2 result opens" | The chapter lists five viewports by name (EsViritu, TaxTriage, NAO-MGS, Novel Virus Diagnostics) that I have never met, in the very first line of the procedure. I stopped, wondering whether I was in the right one. | Move the list of other viewports out of the procedure lead and keep only the taxonomy viewport here. |
| Procedure step 1, "select the taxon row you want to check" | I could not perform this. The chapter does not say where the table is or what a taxon row looks like, and the promised screenshot is only of the popover, not the table. | Add one sentence locating the table, and say the row shows the organism name and a read count. |
| Procedure step 2, "**BLAST Verify** in the action bar under the table" | "Action bar" is never explained. I do not know if this is a toolbar, a strip of buttons, or a menu. | Gloss "action bar" once, for example "the strip of buttons along the bottom of the table". |
| Procedure step 2, "The button stays disabled until exactly one row is selected, and its tooltip says why." | "Disabled" and "tooltip" are interface words I half know. More importantly, this is a troubleshooting note placed inside a step, and I read the step twice trying to see if it was an instruction. | Move it out of the numbered step, or start it with "If the button is greyed out". |
| Procedure step 3, "Its title is `Verify \"<taxon>\" via NCBI BLAST`" | The angle-bracket placeholder notation is a convention I have never seen. I first thought the title literally contains `<taxon>`. | Show the real example title with the worked example's organism name in it. |
| Procedure step 4, "the slider is replaced by a line stating the fixed count" | Very compressed. I read it twice to see whether this was something I do or something the app does. | Start with "If", for example "If the taxon has only one read, LGE shows the count instead of the slider." |
| Procedure step 5, "shows the three phases of the job in turn" | I could not tell how long each phase lasts or what I should do if one stops. The chapter tells me the total cutoff is ten minutes but not what a stuck phase looks like. | Say roughly how long the waiting phase takes and that no action is needed. |
| Procedure, "The drawer is one of two tabs ... sharing the space with Collections." | "Drawer" and "Collections" both appear with no explanation. I do not know what Collections is, and it is not part of this task. | Gloss "drawer" at first use and either gloss Collections or drop it. |
| Settings, "**Reads to submit:.**" | There is a colon followed by a full stop inside the bold label. It looks like a typing mistake and it stopped me. | Remove the stray colon. |
| Settings, "The default is 20, which is enough for the supporting share to mean something" | "Enough to mean something" is not a number I can judge. I wanted to know why 20 rather than 10 or 40. | Give the reason concretely, for example "with 20 reads a single unlucky read moves the share by 5 percent". |
| Settings, "the slider runs from 1 to 50, capped at the number of reads the taxon actually has" | The word "capped" plus a second limit made me read twice. I was not sure which limit wins if the taxon has 12 reads. | Say "if the taxon has fewer than 50 reads, the slider stops at that number". |
| Settings, "On the command line this is `--reads`, which accepts 1 to 100 there rather than stopping at 50." | I have never opened a terminal. I could not tell if this affects me, and the mismatch between 50 and 100 made me wonder if the app is limiting me for a reason. | Say why the two limits differ, or move this to the command line section. |
| Settings, "fills the rest of the sample at random" | I could not judge how many reads are "some of the longest" versus random. Half? Five? This matters for whether my result is repeatable. | Give the split, for example "the ten longest reads, then the rest chosen at random". |
| Settings, "It spreads its picks across quarters of the reference genome instead" | I do not know NAO-MGS and I did not know a reference genome was involved in read selection at all. Read twice. | Move this exception to the section on other viewports. |
| Settings, "Each read gets at most five hits back, which is why an expanded read row never shows more than five." | "Hits" is used here before the results section defines what a hit is. I understood only in retrospect. | Gloss "hit" at first use as "a matching database record". |
| Settings, "The `--extra-args` flag described in the last section is the only way to push a further parameter through to NCBI, and it is a pass-through rather than a supported setting." | Three unknown terms for me, "flag", "push through", "pass-through". Since I never use a terminal this whole sentence was noise. | Mark the sentence as command line only. |
| Reading the results, "the summary bar is the answer" | Read twice. It is a figure of speech, and it contradicts the later advice to read the per-read rows and not the summary alone. | Say "the summary bar gives the overall result". |
| Reading the results, "a ten-dot bar ... all scaled to ten dots however many reads you sent" | I could not work out what one dot means if I sent 20 reads. Does one dot equal two reads? What if the numbers do not divide evenly? | Say explicitly that the dots show proportions, not counts. |
| Reading the results, "a filled circle is a supporting read, a diamond is a contradicting one, and a hollow circle" | Three shapes described in words only. Without seeing them I could not tell a filled circle from a hollow one in a small bar, and the screenshot caption does not mention the shapes. | Show the three symbols inline next to their names. |
| Reading the results, "`<n> with conflicting organisms`" | "A high count there" tells me to worry, but not what high is. Is 3 out of 20 high? | Give a rough number, for example "more than a quarter of the reads". |
| Reading the results, "it usually points at a conserved region that many organisms share" | "Conserved region" I know from textbooks, but "points at" is idiomatic and the sentence is the third clause in a long chain. Read twice. | Split into a separate short sentence. |
| Reading the results, "each of which expands to show the hits ranked below its best one" | I could not perform this. The chapter does not say how to expand a row, whether I click a triangle or double click. Expand All is mentioned much later under right-click. | Say how to expand a row where the expansion is first mentioned. |
| Reading the results, "Status is the icon at the left" | The chapter never says what the status icons look like or what they mean. Later I learned about Verdict values but not whether the icon shows the same thing. | Either list the status icons or say they mirror the Verdict column. |
| Reading the results, "A read at 99.6 percent over 250 aligned bases disagreed at one position." | I checked this and it did not work for me. 0.4 percent of 250 is 1, so it is right, but the chapter makes me do the arithmetic to see the point. | Show the arithmetic, for example "0.4 percent of 250 bases is one base". |
| Reading the results, "the scale is brutal" | Metaphor. An e-value cannot be brutal. I understood the intent only from the next sentence. | Say "the scale is logarithmic, so small differences in the exponent are large differences in meaning". |
| Reading the results, "`1e-30`" | The scientific notation with `e` is a computing convention, not the one my textbooks use. I had to think to see this is ten to the minus thirty. | Add the exponent form in brackets at first use. |
| Reading the results, "a value near `0.1` means chance alone plausibly would" | Two anchors, 1e-30 and 0.1, but no threshold for my own decisions. I did not know what value counts as good. | Give a working cutoff, for example "treat anything above 1e-5 as weak". |
| Reading the results, "so it is the number to compare across two searches run months apart" | Long sentence with three clauses. I read it twice, and I still do not know what bit score value is good. | Split the sentence and give a rough good bit score for a typical read length. |
| Reading the results, "Four more columns are hidden ... which are Accession, Coverage, Align Length, and Verdict, along with Tax ID." | The sentence says four then lists five. I counted twice and thought I had misread. | Fix the count or explain why Tax ID is separate. |
| Reading the results, "Right-click the column header" | I could not perform this confidently. On my laptop trackpad right-click needs a two finger tap, and the chapter never mentions the Mac equivalent, which the earlier chapters may have. | Add "or Control-click" once. |
| Reading the results, "99 percent identity across a third of the read is much weaker evidence than 95 percent across all of it" | This is the most useful sentence in the chapter but it is buried in the middle of a long paragraph about hidden columns, and I nearly missed it. | Give it its own short paragraph. |
| Reading the results, "Verified when the top hit clears identity, coverage, and e-value together" | "Clears" is an idiom, and no thresholds are given. I cannot reproduce LGE's judgement or check it. | Say "meets all three thresholds" and give the three numbers. |
| Reading the results, "**Re-run BLAST** submits the taxon again with a fresh sample of reads." | I could not tell if re-running uses the same slider value or reopens the popover. | Say whether the popover appears again. |
| Reading the results, "**Export** in the summary bar writes the table as CSV or TSV through a save panel." | "Save panel" is Mac interface vocabulary I do not have, and CSV and TSV are not glossed. | Gloss the formats once and say "a Save dialog". |
| What good looks like, "e-values small enough to be written in scientific notation" | This is not a criterion I can apply, because whether a number is written in scientific notation is a display choice, not a property of the value. | Give a numeric cutoff instead. |
| What good looks like, "identities in the high nineties" | Vague for a checklist. "High nineties" could be 95 or 99, and the difference matters. | Give a number, for example "at least 97 percent". |
| What good looks like, "with both submitted reads matching" | Only two reads, but the Settings section says the default is 20 and warns that a small sample is noise. The worked example therefore contradicts the chapter's own advice, and I did not know whether to trust it. | Say why the example used two reads, or run the example at the default. |
| What good looks like, "the classifier probably reached for a relative it had in its database" | "Reached for" is a metaphor and repeats the earlier "hand the reads to a relative". | Use the plain wording. |
| What good looks like, "the reads are probably low-complexity or adapter-contaminated" | "Low-complexity" and "adapter-contaminated" are both unglossed. I know what an adapter is from library prep, but not what low-complexity means for a read. | Gloss both at first use. |
| What good looks like, "two supporting reads out of three is a 67 percent share and lands in the Mixed band" | This is the clearest number in the chapter, but it also told me the worked example's 2 out of 2 is exactly the sample size the chapter calls too thin. I read the two sections against each other twice. | Keep the example and the warning consistent. |
| What good looks like, "Verification checks an identity, not a quantity." | Short and clear, but "an identity" collides with the "Identity" column two sections earlier, which means percent identity. For a moment I thought this was about the column. | Use a different word, for example "checks what the reads are, not how many there are". |
| On the command line, the whole section | I have never opened a terminal, and nothing tells me I can skip this section. I read all of it and understood almost none of it, which cost me several minutes. | Add one line at the top saying GUI users can skip to the next section. |
| On the command line, "The same verification runs headless" | "Headless" is an unexplained term. I guessed it means without a window. | Gloss or replace it. |
| On the command line, "The source FASTQ must be uncompressed, though the Kraken 2 output may be gzipped." | I do not know how to tell whether a file is compressed or how to uncompress one, and the chapter does not say. | Say how to check and what to run, or link to a chapter that does. |
| On the command line, the code block with `\` at line ends | The backslashes are unexplained. I thought they were part of the file names. | Add one line saying the backslash continues the command onto the next line. |
| On the command line, "`--taxid` ... its number comes from the kreport's sixth column" | I do not know what a kreport looks like, so counting to the sixth column is not something I can do from this text. | Show one kreport line with the column marked. |
| On the command line, "`--max-concurrent` caps how many submissions this process keeps in flight" | "In flight" is an idiom, and "process" is unexplained. | Say "how many searches run at the same time". |
| On the command line, "Two habits keep you a good citizen of a shared service." | Metaphor. Also this paragraph mixes advice for terminal users with a fact about LGE that applies to everyone. | Say "Two rules keep you within NCBI's usage limits" and move the LGE pacing fact into the main text. |
| On the command line, "treating fifty sequences an hour as its ceiling" | I could not reconcile this with the slider maximum of 50 and the CLI maximum of 100. If the ceiling is 50 an hour, can I run two 50-read jobs? | Say what happens when you exceed the hourly limit. |
| Where else verification starts, "Every classifier viewport carries the same **BLAST Verify** button" | Four viewport names and four different menu labels in one paragraph, none of which I have seen. I read the paragraph twice and retained none of it. | Put the viewport-to-label mapping in a small table. |
| Where else verification starts, "**BLAST All \\<n\\> Reads**" | The escaped angle brackets show as literal backslashes when I read this in plain text, which looks like an error. | Use the real example text. |
| Where else verification starts, "That pipeline has already assembled the reads into contigs" | "Contig" is not glossed and is not in the glossary list at the top of the chapter. I know the word vaguely from a lecture but not well enough. | Gloss "contig" at first use. |
| Where else verification starts, "The same is true of the 12S metabarcoding viewport, which BLASTs the sequences it could not match locally." | "12S metabarcoding" and "match locally" both arrive with no preparation, in the last line of a section, and "BLASTs" as a verb was new to me. | Gloss 12S metabarcoding or drop the sentence. |

Three lines.

The one thing I learned. A classifier can only name organisms that are in
its own database, so a confident-looking name can simply be the closest
relative it happened to have.

The one thing I still could not do. Perform the procedure. Step 1 assumes I
can find the taxon table and a taxon row in the viewport, step 2 assumes I
know what an action bar is, and the only screenshot is of the popover that
opens after those two steps.

The sentence I liked most. "99 percent identity across a third of the read
is much weaker evidence than 95 percent across all of it."
