# Reader 4 report, BLAST Verification

Persona. Undergraduate. One semester of Geneious in a course. Comfortable
with a graphical sequence viewer. Expects a dialog for every operation.
Has never opened a terminal.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| What it is, "matching short chunks of its sequence" | "Short chunks" is doing the work of a real term I was never given. In Geneious the search unit is a whole query sequence, so I could not tell whether the classifier compares my read as a unit or in pieces, and that changes what a match means. | Name the unit once, as in "short fixed-length pieces called k-mers". |
| What it is, "a reference database installed on your own machine" | I had no idea I had a database on my machine. Nothing earlier in this chapter says one got installed, and Geneious searches a remote server by default. | Add a half-sentence pointing back to where the Kraken 2 database was installed. |
| What it is, "sends them over the internet to that service" | This is the sentence a Geneious user needs flagged loudly and it is buried mid-paragraph. Geneious BLAST also goes to NCBI, so I would not have registered this as new, but here it decides whether I may use the feature at all on a patient sample. | Move the data-leaves-your-Mac point to its own short sentence at the head of the paragraph. |
| What it is, "asks a narrower question than BLAST usually answers" | I could not tell what the usual question is, so "narrower" had no baseline. I read the paragraph twice. | State the contrast, as in "rather than listing every match, it asks one yes-or-no question per read". |
| What it is, "**Supported** at 80 percent or above, **Mixed** from 40 up to 80" | I could not judge these numbers. Nothing says why 80 and 40, or whether they are conventions from the field or choices LGE made. I do not know if 79 percent is nearly Supported or genuinely different. | Say in one clause that these are LGE's own bands, not a community standard. |
| What it is, "A read with no good match counts as neither" | "Good match" is used three times as the pivot of the whole scoring rule and never defined. Later the Verdict column says Verified means clearing identity, coverage, and e-value together, but I did not connect those on first read. | Point forward once, as in "good match is defined by the Verdict rule below". |
| What it is, "organisms that share long stretches of sequence can satisfy the check for each other" | I had to read this twice. I eventually understood it means two organisms can each look like a valid match for the other's reads, but "satisfy the check for each other" is not a phrase I could parse at speed. | Replace with a plain statement that reads from one organism can match the other just as well. |
| Why you would do this, "it does not scale" | I did not know what this means for me in practice. Does it mean slow, or that NCBI blocks me, or that results get worse? Only much later did the ten seconds and fifty sequences an hour appear. | Say the concrete limit here, or say it is stated in the last section. |
| Why you would do this, "the wrong name that comes out often belongs to the same genus or family" | "Genus" and "family" are used as if I will know the rank ladder. I have had genetics, so I know them, but the chapter's own glossary lists taxonomic-rank and never links it here. | Link taxonomic rank at this first use. |
| Why you would do this, "hold the long tail to a threshold you set in advance" | I have not read the Kraken 2 chapter closely and "long tail" was not explained here. I could not tell what a threshold on it is. | Gloss long tail in four words, as in "the many taxa with very few reads". |
| Why you would do this, "the SRR36291587 SARS-CoV-2 reads" | The accession appears with no statement of what it is until the next section. I did not know if SRR36291587 was a sample, a run, or a file. | Say "the public sequencing run SRR36291587" at first use. |
| Before you start, "fetch them from the Sequence Read Archive" | This is the step I could not perform from the text. It hands me off to another chapter with no indication of how long the download takes or how big the file is, and I needed to know before starting. | Add the file size and rough download time. |
| Before you start, "You also need a finished classification open in its viewport" | "Viewport" is used as though I know it. In Geneious I would say a document opened in a tab. This is the app's own word and it appears here without a gloss. | Gloss viewport at first use in this chapter. |
| Before you start, "any sample whose sequence you are not free to share" | This is a real decision point and I was given no way to make it. I do not know whose permission I would need or what category my data falls in. | Name the common case, such as human patient reads under an institutional agreement. |
| Before you start, "gives up after ten minutes, reporting a timeout that carries the job's request ID" | "Request ID" arrives here with no explanation and the glossary entry is only linked much later, in the command-line section. I did not know what I would do with one. | Gloss request ID at this first mention and link it here. |
| Before you start, "Both outcomes happened while this chapter was written" | I read this twice. I could not tell whether this was reassurance that timeouts are normal or a warning that the feature is unreliable. | State the point directly, as in "a timeout is common and is not a failure". |
| Procedure, "The steps below describe the taxonomy viewport, which is where a Kraken 2 result opens" | I did not know a Kraken 2 result opens anywhere in particular. Coming from Geneious I expected to pick the tool and get a dialog, not to have a window chosen for me. | Say the result opens on its own when the run finishes. |
| Procedure step 1, "select the taxon row you want to check" | I could not find the table from the text. Nothing describes what the taxonomy viewport looks like, so I do not know where the rows are relative to the action bar mentioned in step 2. | One sentence naming the table and the action bar under it. |
| Procedure step 2, "Click **BLAST Verify** in the action bar under the table" | "Action bar" is app vocabulary I do not have. Geneious calls this a toolbar and puts it at the top, so "under the table" surprised me. | Gloss action bar as the strip of buttons under the table. |
| Procedure step 2, "The button stays disabled until exactly one row is selected" | This differs from Geneious, where I routinely multi-select documents and run a search on all of them. The chapter states the rule but does not say the one-taxon-at-a-time limit is deliberate, so my first read was that I had done something wrong. | Add that verification is deliberately one taxon per run. |
| Procedure step 3, "Read the popover that opens" | "Popover" is not a word I use and it is not the dialog I expected. I could not tell whether it is modal, whether it blocks the rest of the window, or how to cancel out of it if I change my mind. | Say it is a small panel anchored to the button and that clicking away cancels. |
| Procedure step 5, "shows the three phases of the job in turn" | I could not judge how long any phase should take, and the only timing figure anywhere is the ten-minute give-up. The second phase is the one that hangs. | Give a typical total time for the default 20 reads. |
| Procedure step 5, "The run also appears in the Operations panel" | The Operations panel is named as if I have met it. I do not know where it is or how to open it. | Link the Operations panel chapter at this first mention. |
| Procedure, "The drawer is one of two tabs in the taxonomy viewport, sharing the space with Collections" | Collections is introduced here and never explained. I could not tell whether it is something I need. | Say in a clause that Collections is unrelated to verification. |
| Settings, "**Reads to submit:.**" | The stray colon before the period made me stop and reread, thinking a word had been dropped. | Remove the colon. |
| Settings, "the slider runs from 1 to 50, capped at the number of reads the taxon actually has" | I could not tell what the slider looks like when the cap bites. If my taxon has 12 reads, does the slider still show 50 and refuse, or does it end at 12? | Say the slider's maximum becomes the read count. |
| Settings, "On the command line this is `--reads`, which accepts 1 to 100 there rather than stopping at 50" | I have never opened a terminal, so this told me the app is deliberately giving me less than something I cannot use. That is discouraging and no reason is given. | Say why the interface caps lower, or drop the comparison from the Settings entry. |
| Settings, "LGE takes some of the longest reads assigned to the taxon and fills the rest of the sample at random" | "Some of" gave me no number, and this is the one thing I most wanted to be able to write in a methods section, which the section itself says is the point. | State the split, such as the longest five then random. |
| Settings, "The `--extra-args` flag described in the last section" | A terminal flag is cited as the only route to a setting, inside the section about the graphical settings. For me that route does not exist. | Say plainly that this is not reachable from the interface. |
| Reading the results, "a ten-dot bar" and "all scaled to ten dots however many reads you sent" | I read this twice and still could not work out what one dot means if I sent 20 reads. Does each dot stand for two reads, and what happens to an odd count? | Say each dot stands for a tenth of the reads, rounded. |
| Reading the results, "a diamond is a contradicting one" | Shape alone carries the meaning and I could not tell the shapes apart in my head, nor could I check, since the screenshot is only described. | Add the color of each shape alongside its shape. |
| Reading the results, "reads whose own list of hits disagrees about the genus" | I could not judge this number at all. The text says a high count matters but never says what high is out of 20. | Give a rough figure, such as more than a quarter of the reads. |
| Reading the results, "Six columns are shown by default" then "Four more columns are hidden ... which are Accession, Coverage, Align Length, and Verdict, along with Tax ID" | I counted five items in a list introduced as four and had to reread. Also, Accession appears as a hidden column when the visible Read / Accession column already showed accessions. | Fix the count and say how the hidden Accession column differs. |
| Reading the results, "A read at 99.6 percent over 250 aligned bases disagreed at one position" | I could not reproduce this. 0.4 percent of 250 is one, but the arithmetic is left to me and I checked it twice before trusting the sentence. | Show the subtraction, as in "one mismatch in 250". |
| Reading the results, "a value near `0.1` means chance alone plausibly would" | I do not know where the boundary sits between these two examples. Between 1e-30 and 0.1 there is a vast range and no guidance. | Give the threshold LGE itself uses for the Verified call. |
| Reading the results, "Right-click the column header and choose one to show it" | Right-clicking a header to add columns is not how Geneious does it, and there is no mention of a menu I could find another way. I would have hunted for a View menu first. | Say there is no menu-bar equivalent. |
| Reading the results, "**Export** in the summary bar writes the table as CSV or TSV" | Export is grouped with the two drawer buttons but lives somewhere else, and the sentence does not say whether it exports all rows or only expanded ones. | Say the export covers every row regardless of what is expanded. |
| What good looks like, "identities in the high nineties, coverage across most of each read" | "Most of each read" is the only guidance given for the number the chapter just called the one that stops identity from misleading me. I could not judge 70 percent coverage from this. | Give a figure for coverage the way the nineties figure is given for identity. |
| What good looks like, "the reads are probably low-complexity or adapter-contaminated" | Both terms are new here and neither is glossed or linked. I do not know what a low-complexity read is or what an adapter is doing in my data. | Gloss both in one clause each. |
| What good looks like, "two supporting reads out of three is a 67 percent share" | This contradicts the Settings default of 20 reads and I could not see how I would ever get a sample of three, unless the taxon had only three reads. I reread the paragraph looking for the missing condition. | Say this arises when the taxon itself has few reads. |
| On the command line, "The source FASTQ must be uncompressed" | Downloads from the SRA arrive compressed, per the chapter this one points me to. Nothing tells me how to uncompress one, and I have no terminal. | Say whether the app can do this, or that this constraint is command-line only. |
| On the command line, whole section | Every recovery path in the chapter runs through here. The timeout paragraph earlier says a request ID makes the job recoverable, and the only worked recovery is a terminal command. I cannot follow any of it. | Say in the graphical sections how to recover a timed-out job without a terminal. |
| Where else verification starts, "each right-click menu names the action differently" | Four different names for one action, with no reason given, made me doubt I had understood that they are the same feature. I reread the section. | Say once that the wording differs but the behavior is identical. |
| Where else verification starts, "the 12S metabarcoding viewport, which BLASTs the sequences it could not match locally" | "12S" and "metabarcoding" both arrive unexplained in the last line of a section, and "BLASTs" as a verb was a stumble. | Gloss 12S metabarcoding or cut the sentence. |

One thing I learned. An e-value depends on how big the database is, so a
bit score is the number to carry between searches.

One thing I still could not do. Recover a timed-out job, because every
route the chapter gives me runs through a terminal.

The sentence I liked most. "Verification checks an identity, not a
quantity."
