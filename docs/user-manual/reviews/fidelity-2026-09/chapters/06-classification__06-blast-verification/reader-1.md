# Reader 1 (undergraduate, sophomore fresh from genetics, never used a terminal)

Chapter: `docs/user-manual/chapters/06-classification/06-blast-verification.md`

| Location | Issue | Suggested fix |
| --- | --- | --- |
| What it is, "matching short chunks of its sequence" | I never learn what the short chunks are or how long. In my genetics class this was called a k-mer, and the word never appears, so I cannot connect this to anything I was taught. | Name the chunks k-mers at first use and give a rough length. |
| What it is, "That database is the whole of what the classifier knows" | I could not tell whether this database is something I downloaded, something bundled with the app, or something I have to build. The chapter tells me later there is no database to download for BLAST, which made me more confused, not less. | Add half a sentence saying the classifier database is the one installed in an earlier chapter. |
| What it is, "asks a narrower question than BLAST usually answers" | I have never used BLAST, so I have no idea what question it usually answers. The comparison is to a thing I do not know. | Say what BLAST normally returns before saying LGE asks something narrower. |
| What it is, "assigned to one taxon" | Taxon is linked but not glossed in the sentence. I guessed it means a species, but the chapter later treats a genus as a taxon too, so my guess was too narrow. | Gloss taxon inline as a named group at any level, species or genus or family. |
| What it is, "The share of supporting reads among those two groups" | I had to read this twice. The denominator leaves out reads with no good match, and that only becomes clear when I go back a sentence. It changes the percentage a lot. | State the fraction explicitly as supporting divided by supporting plus contradicting. |
| What it is, "**Mixed** from 40 up to 80" | I cannot tell whether exactly 80 is Mixed or Supported, or what exactly 40 is. The boundaries matter for judging my own result. | Write the bands with explicit inclusive or exclusive wording. |
| What it is, "reads were too short or too poor" | Too short compared to what? I have no number to judge against, and nothing tells me how to check my own read lengths. | Give a rough length below which BLAST usually settles nothing. |
| What it is, "can satisfy the check for each other" | Read twice. I think it means two relatives each look supported, but "satisfy the check for each other" is not a phrase I could restate. | Replace with a plain statement that a close relative's reads can look like a match. |
| Why you would do this, "it does not scale" | Scale is a software word. I do not know what actually fails. Is it slow, is it blocked, does NCBI refuse me? | Say plainly that you cannot do this for many taxa at once. |
| Why you would do this, "the wrong name that comes out often belongs to the same genus or family" | This assumed I know classifier databases are organized by taxonomy and that a miss falls to a neighbor. It was the sentence I most wanted an example for. | Add one concrete pair of organisms as an example. |
| Why you would do this, "A few dozen reads is fragile evidence" | I cannot judge this. A few dozen out of what total? Fragile compared to what number that would be solid? | Give the comparison, for example a few dozen out of millions. |
| Why you would do this, "hold the long tail to a threshold you set in advance" | Long tail and threshold both go unexplained here, and I have not read the Kraken 2 chapter yet even though it is listed as a prerequisite. | Gloss long tail as the many taxa with very few reads. |
| Why you would do this, "the dominant call is" | Dominant call is not defined. I assume it means the taxon with the most reads, but the chapter never says so. | Gloss dominant call as the taxon with the most reads. |
| Why you would do this, "the classifier databases are pathogen databases" | This is the first time I learn the databases hold only pathogens. That seems important much earlier, since it explains why a human sample would not work here. | Move this fact to the first section where the database is introduced. |
| Before you start, "fetch them from the Sequence Read Archive as accession `SRR36291587`" | I could not do this step from the text. It points at another chapter, which is fine, but I do not know how large the download is or how long to wait, so I could not tell whether it was working. | Add the file size and rough download time. |
| Before you start, "a finished classification open in its viewport" | Viewport is used throughout the chapter and never explained. I think it is the pane showing a result, but I was never told. | Gloss viewport at first use as the panel that displays a result. |
| Before you start, "the reads you verify leave your Mac" | I do not know what this means for me practically. Are they stored by NCBI, are they public, can someone find them later? The warning matters but I cannot act on it. | Say in one sentence what NCBI does with the submitted reads. |
| Before you start, "LGE offers no local BLAST to fall back on" | Local BLAST is a thing I have never heard of. The sentence assumes I know a local version exists somewhere. | Gloss local BLAST as a copy of the search running on your own machine. |
| Before you start, "It polls for the result" | Polls is jargon. I guessed it means checks repeatedly. | Replace polls with checks repeatedly. |
| Before you start, "a timeout that carries the job's request ID" | Request ID is linked but not said in plain words here, and it is the thing that rescues me after a failure, so I wanted it explained on the spot. | Gloss the request ID inline as the ticket number NCBI gives your job. |
| Before you start, "Both outcomes happened while this chapter was written" | I had to read this twice. I could not tell which two outcomes were meant until I went back up two sentences. | Name the two outcomes, a finished result and a timeout. |
| Procedure, "the EsViritu, TaxTriage, NAO-MGS, and Novel Virus Diagnostics viewports" | Four tool names arrive with no explanation of what any of them is. I do not know whether I have them or need them. | Add three words saying these are other classifiers covered in this part. |
| Procedure step 2, "in the action bar under the table" | I do not know what the action bar is or which table is meant. As a first-time user I would be hunting around the screen. | Gloss action bar as the strip of buttons under the result table. |
| Procedure step 2, "stays disabled until exactly one row is selected" | Useful, but I only learn here that selecting two rows breaks it. Step 1 told me to select a row without saying only one. | Move the one-row rule into step 1. |
| Procedure step 3, "Its title is `Verify \"<taxon>\" via NCBI BLAST`" | The angle brackets and quotation marks made me look for those characters on screen. I did not know they marked a placeholder. | Say the taxon name appears where the placeholder is. |
| Procedure step 5, "shows the three phases of the job in turn" | I do not know how long each phase should take, so I cannot tell a slow job from a stuck one. | Give a rough expected duration for the waiting phase. |
| Procedure step 5, "The run also appears in the Operations panel" | The Operations panel is named without saying where it is or how to open it. | Say where the Operations panel lives. |
| Settings, "**Reads to submit:.**" | There is a stray colon and full stop together. I stopped and re-read thinking I had missed something. | Remove the stray colon. |
| Settings, "checked against the whole nucleotide database" | Whole nucleotide database is not the same phrase as `nt` used two paragraphs later, and I did not realize they were the same thing. | Use one name for the database consistently. |
| Settings, "The default is 20, which is enough for the supporting share to mean something" | I cannot judge this. Why is 20 enough and 5 not? Nothing gives me a rule. | Say what happens below roughly ten reads. |
| Settings, "accepts 1 to 100 there rather than stopping at 50" | I could not tell why the two limits differ, or which number is the safer one to use. | Say why the command line allows more. |
| Settings, "LGE takes some of the longest reads" | Some is vague. I do not know whether it is half, a few, or most, and the sentence promises a mix without giving the proportion. | State the proportion of longest reads. |
| Settings, "spreads its picks across quarters of the reference genome" | I did not know NAO-MGS uses a reference genome at all, so this sentence had no ground to stand on. | Add three words identifying NAO-MGS as reference-based. |
| Settings, "The program is `blastn`" | The program name is given with no hint that other programs exist, so I could not tell whether this is a limitation or just a fact. | Say blastn is the right program for reads, which are nucleotides. |
| Settings, "Each read gets at most five hits back" | Hit is used here for the first time and never glossed. I guessed it means a matching database record. | Gloss hit at first use. |
| Settings, "The `--extra-args` flag described in the last section" | I have not read the last section yet, and I do not know what a flag is because I have never opened a terminal. | Say flags are command-line options and defer the detail. |
| Reading the results, "a ten-dot bar" and "all scaled to ten dots" | I had to read this twice. If I send 20 reads and see ten dots, I cannot map a dot back to a read, and the text does not say a dot stands for two reads. | Say each dot stands for a share, not a read. |
| Reading the results, "The bar is tinted to match the word" | I do not know which colour means what, and I am somewhat colourblind, so a colour-only cue worries me. | Name the colours, or say the word carries the meaning. |
| Reading the results, "hits disagrees about the genus" | Genus I know, but I do not know why genus is the level being checked rather than species. | Say why genus is the test level. |
| Reading the results, "a conserved region that many organisms share" | Conserved region came up in my class but never in a sequencing context. I could not tell how it makes a read ambiguous. | Gloss conserved region as sequence that changed little across species. |
| Reading the results, "expands to show the hits ranked below its best one" | I could not perform this. Nothing says how to expand a row, whether I click a triangle or double-click. | Say how to expand a row. |
| Reading the results, "on a parent row" and "on a child row" | Parent and child rows appear before I have been told the table is nested. | Say the table nests hits under each read. |
| Reading the results, "99.6 percent over 250 aligned bases disagreed at one position" | I had to check this with a pencil. The point is good but the text makes me do the arithmetic to see it. | Show the arithmetic in the sentence. |
| Reading the results, "A value of `1e-30`" | Scientific notation written this way was not something I met in genetics. I know what ten to the minus thirty is, but `1e-30` took a moment. | Write it once in the ten-to-the-power form. |
| Reading the results, "the scale is brutal" | Brutal tells me nothing measurable. I still do not know what value separates a good e-value from a bad one. | Give the value below which a hit is usually trustworthy. |
| Reading the results, "Four more columns ... Accession, Coverage, Align Length, and Verdict, along with Tax ID" | That lists five names for four columns. I counted twice and could not resolve it. | Fix the count. |
| Reading the results, "Right-click the column header and choose one" | I could not perform this with confidence. I do not know whether a submenu appears or whether the list uses checkboxes. | Say what the menu shows. |
| Reading the results, "clears identity, coverage, and e-value together" | Clears what values? The thresholds are never given, so I cannot reproduce LGE's own verdict or check it. | Give the three thresholds. |
| What good looks like, "identities in the high nineties" | High nineties is the only number-like guidance in the section, and it sits against the earlier warning that identity alone misleads. I could not tell what coverage number to pair with it. | Give a coverage number alongside the identity number. |
| What good looks like, "e-values small enough to be written in scientific notation" | This is not a test I can apply. Any small number can be written that way, and the app chooses the display format, not me. | Replace with a numeric e-value cutoff. |
| What good looks like, "low-complexity or adapter-contaminated" | Low-complexity and adapter are both new to me. Adapter came up once in class, briefly. | Gloss both terms at first use. |
| What good looks like, "two supporting reads out of three is a 67 percent share" | The worked example sent only two reads and is called reliable. Here three reads is called far too thin to act on. I could not reconcile the two. | Say why two reads sufficed in the example. |
| On the command line, "runs headless" | Headless is a word I have never seen. | Gloss headless as without the app window. |
| On the command line, "The source FASTQ must be uncompressed" | I do not know how to tell whether mine is compressed, or how to uncompress it, and I have never opened a terminal. | Say that a `.gz` ending means compressed and point at the chapter that unpacks it. |
| On the command line, the code block | I could not perform any of this. Nothing tells me where to type it, and the backslashes at the line ends looked like typos to me. | Point at the terminal chapter and say the backslash continues one command. |
| On the command line, "comes from the kreport's sixth column" | I have not seen a kreport and did not know it has columns. Counting to the sixth column of a file I cannot open is not something I can do. | Prefer the Copy Taxon ID route for readers who have not opened a terminal. |
| On the command line, "caps how many submissions this process keeps in flight" | In flight is jargon and I do not know what a process is here. | Replace in flight with sent at the same time. |
| On the command line, "for example `WORD_SIZE=11`" | I do not know what word size is or when I would change it, so the example taught me nothing. | Say word size is advanced and point elsewhere. |
| On the command line, the `Inconclusive` and `Unverified` lines of the output | The output shows both, but the first section called Inconclusive a summary word and the columns section called Unverified a per-read verdict. I could not tell the two apart. | Say how the two differ in one sentence. |
| On the command line, "fifty sequences an hour as its ceiling" | This limit is never mentioned in Settings, where I learned the slider goes to 50 and that re-running is normal. I could not tell whether two runs of 50 in one hour would be refused. | Say what happens when the ceiling is reached. |
| Where else verification starts, "**BLAST All \<n\> Reads**" | The escaped angle brackets read as stray backslashes to me, and I again took them for literal characters. | Use plain wording instead of a placeholder. |
| Where else verification starts, "already assembled the reads into contigs" | Contig is not glossed here and is not in the chapter's glossary list. I met the word once in class. | Gloss contig at first use. |
| Where else verification starts, "the 12S metabarcoding viewport" | Both 12S and metabarcoding arrive unexplained in the last line of the section. | Gloss 12S metabarcoding in three words. |
| Next, "for lineage abundance in a mixed sample" | Lineage abundance and mixed sample are both new phrases at the very end of the chapter. | Gloss the phrase or drop it. |

The one thing I learned: a classifier can only name organisms that are in its own database, so a confident wrong answer is possible and BLAST is how you catch it.

The one thing I still could not do: judge my own result, because the thresholds LGE uses for identity, coverage, and e-value are never given as numbers.

The sentence I liked most: "It reports the nearest thing it does hold, and it reports it with no visible sign that it settled for second best."
