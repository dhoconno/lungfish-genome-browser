# Reader 3: Running External Workflows

Persona: pre-med student, English is my second language. I know biology words
from my textbooks. I have never opened a terminal. Long sentences and English
idioms slow me down.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| What it is, "This chapter runs the arrow the other way." | This is a metaphor. There is no arrow drawn anywhere on the page, so I looked back up the page for a picture. I could not tell if the arrow was a real thing I missed. | Say the chapter goes in the opposite direction, from an outside pipeline into the app. |
| What it is, "Nextflow at 26.04.6 and Snakemake at 9.25.2" | I cannot judge these numbers. I do not know if a different version on my computer is a problem or if the app installs these for me. | Add one sentence saying the app supplies these versions and I do not install them. |
| What it is, "a pipeline" | Pipeline is used before it is explained, and it is not in the glossary list at the top. I guessed it means the analysis file, but I was not sure it was different from workflow. | Gloss pipeline at first use, or say it is the same thing as a workflow. |
| What it is, "There are two doors into this chapter's material" | Door is a metaphor for a way in. I read it twice because I first pictured a window in the app named Doors. | Say there are two ways to reach this feature. |
| What it is, "which is why a package that declares a reference bundle and a read bundle shows a reference picker and a reads picker without anyone writing that dialog." | Very long sentence with four nouns I do not know yet: reference bundle, read bundle, picker, dialog. I had to read it three times. | Split into two sentences and gloss reference bundle and read bundle. |
| What it is, "read bundle" then later "FASTQ Bundles" then later "`.lungfishfastq`" | Three names appear for what I think is one thing. I could not confirm they are the same. | Use one name, and say once that a read bundle is a `.lungfishfastq` bundle. |
| What it is, "skips the package layer entirely" | Layer is a metaphor. I do not know what is stacked on what. | Say the command line does not use a package at all. |
| Why you would do this, "Six months on, that folder answers the question the terminal run cannot." | Idiom "six months on" and an unnamed "the question". I had to read the paragraph again to work out which question. | Name the question, for example which files and which command produced this result. |
| Why you would do this, "You could run it in a terminal outside LGE" | I have never opened a terminal. Nothing tells me what a terminal is or that I do not need one for this chapter. | Add a short gloss for terminal, and say the window route needs none. |
| Before you start, "choose **File > New Project** (Cmd-N)" | I could follow this, but I could not tell whether the example packages need a project at all, since the hello-world packages ignore their input. | State plainly that a project is required even for the example packages. |
| Before you start, "open the repository's front page at https://github.com/..., click the green **Code** button, choose **Download ZIP**" | I could not perform this. I do not know what a repository is, and the word is used three times before this step. Also I do not know where a downloaded ZIP goes on my Mac. | Gloss repository once, and say the ZIP lands in the Downloads folder. |
| Before you start, "a linked package stays where it is rather than being copied into the project" | I did not understand the consequence until I reached step 1. On first read I did not know why "somewhere you will not lose them" mattered. | Add the consequence here, that moving the folder later breaks the workflow. |
| Before you start, "writes a four-base FASTA file" | Four-base is very small. I could not judge whether that is normal or a sign something failed. | Say it is deliberately tiny so the run finishes instantly. |
| Before you start, "wraps it as a `.lungfishref` reference bundle with a manifest and an index" | Manifest and index are not glossed. I know manifest from the earlier paragraph about `manifest.json`, but index I do not know at all. | Gloss index at first use, one clause. |
| Before you start, "Neither reads its input, and that is the point." | "That is the point" is an idiom. I read the paragraph twice before I understood the packages are only a test of the plumbing. | Say the packages ignore input so a failure can only come from the app, not the pipeline. |
| Before you start, "their manifests declare `\"runtime\": {\"kind\": \"none\"}\"`" | This is code inside a sentence. I do not know JSON. I could not tell if I must type this anywhere. | Say in words that the manifest asks for no container, and drop the code or move it to a code block. |
| Before you start, "conda environment" | Conda is never explained and is not in the glossary list. | Gloss conda, or drop the sentence, since the paragraph then says neither package needs it. |
| Procedure step 1, "The open panel that appears is titled Link Workflow Package" | Open panel is Mac vocabulary I do not have. I expected the word window or dialog. | Call it the file chooser window. |
| Procedure step 1, "Choose folders rather than files in this panel, because a `.lungfishflowpkg` is a folder." | I could not perform this. In Finder a `.lungfishflowpkg` might look like one file. The text does not say what I will actually see or click. | Say what the item looks like in the chooser and that a single click selects it. |
| Procedure step 1, "grouped by the category its manifest declares, which for these two is **Templates**" | Long sentence, and I did not know categories existed until this clause. Later the Tools menu category matters, so I had to come back. | Introduce category as its own short sentence. |
| Procedure step 1, "**Runnable** or **Catalog only**" | Catalog only is not explained until the next paragraph. On first read I did not know if it was a problem. | Gloss both terms in the same sentence where they first appear. |
| Procedure step 1, "In this build a package is Runnable only when its runner is Nextflow or Snakemake and its manifest declares a required `.lungfishref` input, a required `.lungfishfastq` input, and at least one output." | Very long, four conditions in one sentence. I read it three times to count the conditions. | Break the conditions into a short list. |
| Procedure step 1, "which includes every command-runner package" | Command-runner package is new here and never defined. | Define it, or remove the clause. |
| Procedure step 1, "Linking is by identity rather than by location." | Abstract sentence. I understood only after the two sentences that follow. | Lead with the concrete case, that relinking a package with the same id replaces the old entry. |
| Procedure step 2, "Until you do this the workflow still appears in that submenu, but greyed out" | This contradicts step 2's first sentence, which says enabling is what puts the workflow into the menu. I read both twice and still am unsure whether a disabled workflow is in the menu or not. | Say enabling makes the menu item usable, since the item is present but greyed out before that. |
| Procedure step 2, "the Required Setup pack every LGE installation already has" | I could not judge this. If it is always present, I do not know why the paragraph tells me to install a missing pack first. | Say plainly that for these two packages nothing needs installing. |
| Procedure step 3, "subtitled \"Run enabled specialized and user workflows.\"" | Specialized workflows are never explained anywhere in the chapter. | Add a clause saying specialized workflows are the ones built into LGE. |
| Procedure step 3, "a **FASTQ Bundles** list holding whatever was selected in the sidebar when the window opened" | I could not perform this. Nothing earlier told me to select anything in the sidebar, and the hello-world packages have no reads to select. | Say what to select before opening the window, and what to do if nothing is selected. |
| Procedure step 3, "One behaviour in the FASTQ Bundles list surprises people" | I do not know these people. I could not tell whether I should expect this or worry about it. | State the behaviour directly without the aside. |
| Procedure step 3, "Run bundles one at a time when you want a separate result per library." | Library is used here for the first time in a sequencing sense. In my textbooks library means something else. | Gloss library as one prepared sample's reads. |
| Procedure step 3, "The Advanced settings section for a linked package is read-only text rather than controls." | I did not know what "controls" means in a Mac window. I guessed buttons and boxes. | Say there is nothing to click or type there. |
| Procedure step 4, "Read the line rather than clicking hopefully at the button." | Idiom, and it sounds a little like a joke at me. I read it twice. | Say the button stays disabled until the line reports the run is ready. |
| Procedure step 4, "The run reports into the Operations panel" | "Reports into" is unusual English. I could not tell if I must open the panel or if it opens itself. | Say progress appears in the Operations panel and give the step to open it. |
| Procedure, whole section | I could not perform a run from this text. There are four steps and no sentence telling me what a successful run looks like on screen, or how long it takes. | Add one sentence at the end of step 4 saying the example run finishes in seconds. |
| Settings, "**Reference.** ... On the command line this is `--input`." and "**FASTQ Bundles.** ... On the command line this is `--input`." | Two different settings map to the same flag. I read both twice thinking I had misread. | Explain that `--input` is repeated for each input, or note both go to the same flag. |
| Settings, "**Cores.** ... The default is the app's own thread count, which is sized to the machine." | I cannot judge this number. I do not know my thread count or where to see it. | Say the app picks a sensible value and that most people leave it alone. |
| Settings, "a local Snakemake run receives this as its `--cores` value while a Nextflow run records it without enforcing a per-process limit" | Long, and "records it without enforcing" means the setting does nothing for Nextflow, which took me two reads to accept. | Say plainly that the setting has no effect on a Nextflow run. |
| Settings, "**Output Name.** ... the field is disabled during a Run Again" | Run Again is capitalised like a button but has not been introduced. I did not know where it is. | Introduce Run Again in the Procedure, or say it is a button in the Operations panel. |
| Command-line only settings, whole section | This section comes before the section that says the command line is optional. I read fifteen settings I could not use before being told I could skip them. | Move the optional note above this section, or move this section after it. |
| Command-line only settings, "**`--executor`.** ... every container runtime it knows about, Apple Containerization included, is mapped onto the same Docker profile string." | Docker, container runtime, and profile string are all new. I could not judge whether I need Docker installed. | Say whether a plain user needs Docker for the examples in this chapter. |
| Command-line only settings, "This flag is the setting." | This sentence repeats about twelve times and I never worked out what it means. | Remove it, or say once at the top of the section that these flags have no window control. |
| Command-line only settings, "**`--timeout`.** ... There is little reason to set it, because the local runner accepts it and does not enforce it" | If it does nothing, I do not know why it is documented as a setting I might use. | Say directly that the flag currently has no effect. |
| Reading the results, "Inside the bundle sit four things worth knowing about." | Unusual word order in English. I read it twice. | Say the bundle holds four things worth knowing about. |
| Reading the results, "`replayIdentity` inside the manifest is the block that makes Run Again possible." | Block is a metaphor. I did not know if it is a section of text or something else. | Say it is a section of the JSON file. |
| Reading the results, "It records a SHA-256 checksum" | SHA-256 appears without explanation. Checksum was glossed earlier, but I could not tell if SHA-256 is a different thing. | Say SHA-256 is one kind of checksum. |
| Reading the results, "which LGE signs when a signer is configured and leaves unsigned otherwise" | Signer and signing are never explained, and I have no idea how to configure one or whether I should. | Say signing is optional and covered elsewhere, or drop the clause. |
| Reading the results, "The command `lungfish-cli ops stats <directory>`" | This is a terminal command inside a section I thought applied to the window route. I have never opened a terminal, so I could not do it. | Move this paragraph into the command-line section. |
| Reading the results, "Peak RAM reads `unknown` for these runs" | I could not judge this. I did not know if unknown means something failed. | Say unknown here is expected and not a problem. |
| What good looks like, "Four checks separate a run that worked from one that merely finished." | I read this twice. The difference between worked and merely finished is the whole point and it is left implicit. | State that a run can exit without producing the files you wanted. |
| What good looks like, "The exit status is the first and the cheapest." | Cheapest is a metaphor for fastest to check. I thought about money. | Say it is the quickest to check. |
| What good looks like, "the command itself should have exited zero" | Exited zero is jargon. Exit codes are never explained, and I have no terminal to see one in. | Gloss exit code once, and say where the window route shows the same thing. |
| What good looks like, "The logs should be quiet in the way that engine is quiet when it succeeds." | This sentence seems to have a missing word before "engine", and quiet is a metaphor. I read it three times and still am not sure of the meaning. | Rewrite as a plain statement about what a successful log contains. |
| What good looks like, "Two defects found while testing this chapter against Preview 2026.9.13" | Defect and Preview 2026.9.13 are unexplained. I do not know if my copy of the app has these problems. | Say which app version the reader should check, and what to do if they see the behaviour. |
| What good looks like, "because it returns before the guard is ever reached" | Guard is programmer vocabulary. I could not picture it. | Say the check never runs in that case. |
| On the command line, "The whole procedure runs headless, meaning with no window at all, by typing commands into the Terminal application." | This is the first mention of the Terminal application, and it comes after two earlier sections already showed terminal commands. Nothing tells me how to open it. | Say how to open Terminal, or link to an appendix that does. |
| On the command line, "it works out the engine from the file name" | Works out is an idiom. I read it twice. | Say the app determines the engine from the file name. |
| On the command line, "falls back to whatever it finds on the effective `PATH`" | PATH is not explained and I have never used a terminal. Effective PATH is even less clear. | Gloss PATH in one clause, or say the app finds an installed engine automatically. |
| On the command line, "`lungfish-cli workflow validate hello-world-nextflow.lungfishflowpkg/main.nf`" | I could not perform this. Nothing says I must first move to the folder holding the package, and I do not know how. | Add a line showing how to reach the folder before running these. |
| On the command line, "`✓ Workflow syntax appears valid`" | Appears valid is hedged. I could not judge whether this is a pass or a maybe. | Say it means the file parsed and nothing more. |
| On the command line, "the command exits 64 with a message saying so" | The number 64 means nothing to me. | Say it fails with an error naming the missing option. |
| On the command line, "which is why the `--cpus 2` below is not optional" | I had to scroll down to see what "below" meant, then back up. | Name the flag and say it must match the recorded run. |
| On the command line, "Two smaller commands round this out." | Idiom. I read it twice. | Say two more commands complete this section. |
| On the command line, "`lungfish-cli workflow list` is narrower than its name suggests" | Narrower than its name suggests is indirect. I could not tell what the command does until the next clause. | Say what it does first, then note the name is misleading. |
| On the command line, "either because they refuse the file locks Nextflow takes on its cache or because they store extended attributes as `._` sidecar files" | Very long, and file locks, extended attributes, and sidecar files are three unexplained terms in one sentence. | Say some external disks cannot host Nextflow's scratch files, and leave the reasons to a note. |
| On the command line, "external exFAT disk" | exFAT is not explained. I do not know how to tell what my external disk is. | Say this covers most external drives formatted for Windows compatibility. |
| Whole chapter | The chapter never shows a picture. Both figures are marked planned. Because I could not perform several steps from the text, a picture of the Workflow Library and the run window would have carried me. | Prioritise the two planned screenshots. |

One thing I learned: a run bundle keeps the exact command and a fingerprint of
every file, so a result can be checked months later.

One thing I still could not do: get the example packages onto my Mac and link
one, because the download and folder steps assume I know what a repository is
and how to select a folder that looks like a file.

The sentence I liked most: "A checksum is a short fingerprint computed from a
file's exact bytes, so two people can confirm later that they are holding the
identical file."
