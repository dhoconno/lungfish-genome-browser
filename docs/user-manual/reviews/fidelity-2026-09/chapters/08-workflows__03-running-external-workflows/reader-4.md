# Reader 4 report: Running External Workflows

Reader: undergraduate who used Geneious for one semester, expects a dialog for every operation, has never opened a terminal.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| What it is, "runs the arrow the other way" | Had to read twice. I did not know what arrow was being referred to, since no arrow or diagram appeared in the previous paragraph. | Say plainly that the previous chapter exported a pipeline out of LGE and this one brings a pipeline in. |
| What it is, "Nextflow at 26.04.6 and Snakemake at 9.25.2" | Numbers I cannot judge. I do not know whether these are current, old, or whether my collaborator's pipeline needs a different version. | Add one sentence saying what happens if a collaborator's pipeline was written for a different version. |
| What it is, "processes connected by the files that flow between them" | "Process" is used here as a technical term and is never glossed. In Geneious an operation is just a menu item, so I read this as the operating-system meaning. | Gloss process at first use as one step of a pipeline that runs a single command. |
| What it is, "a folder ending in `.lungfishflowpkg`" | I could not perform the check. On my Mac a folder ending in an extension usually shows up as a single file, and I did not know I would have to treat it as a folder until step 1 told me. | Say here that the Finder shows the package as one item even though it is a folder. |
| What it is, "declares which engine runs it" | "Declares" is used about a dozen times before I understood it means a line written in `manifest.json` by the package author, not something I do. | On first use, say the manifest author writes these statements and you never edit them. |
| What it is, "reference bundle and a read bundle" | Neither term is glossed here, and both are the things I must supply in step 3. Geneious calls these a reference sequence and a read set. | Gloss reference bundle and read bundle at first use, or point to the chapter that does. |
| What it is, "`lungfish-cli workflow run`" | I have never opened a terminal. This appears in the third paragraph as if it were a normal alternative, before the chapter tells me the terminal section is optional. | Move the reassurance from the "On the command line" section up to this first mention. |
| What it is, "pinned at release 3.0.0" | Another version number I cannot judge, and "pinned" is not explained. | Gloss pinned once as fixed to one version so results do not change. |
| Before you start, "click the green **Code** button, choose **Download ZIP**" | I could follow this, but I then had a folder of the whole repository and no guidance on which of many folders to keep. The instruction to copy the packages "somewhere you will not lose them" gave me no place. | Name a concrete destination, for example the Documents folder. |
| Before you start, "wraps it as a `.lungfishref` reference bundle with a manifest and an index" | Three terms in one clause. "Index" is never explained, and I do not know what a `.fai` index does. | Gloss index once as a small file that lets the app jump to any position in the FASTA without reading it all. |
| Before you start, "`"runtime": {"kind": "none"}`" | This is raw JSON with no explanation of the punctuation, and it is the first code the chapter shows. | Say in words that the manifest sets the runtime to none, and drop the literal syntax. |
| Step 1, "Choose folders rather than files in this panel" | I could not perform this. The sentence tells me what to choose but not how, and I do not know whether the panel has a control that switches between the two. | State the actual click, for example that the package appears as a folder icon you select once and then click Link Workflow. |
| Step 1, "its manifest declares, which for these two is **Templates**" | I could not find Templates ahead of time. I did not know whether Templates is a heading inside the Workflow Library or a Tools submenu, and step 2 later uses it as both. | Say once that the category name is used both as a group heading in the Library and as the Tools submenu name. |
| Step 1, "a required `.lungfishref` input, a required `.lungfishfastq` input, and at least one output" | This is the rule that decides whether anything I link will work at all, and it is stated only as file extensions. A colleague's workflow that takes an assembly rather than reads would silently be Catalog only and I would not know why. | Add one sentence saying a workflow that does not take both a reference and reads cannot be run from the window in this build. |
| Step 1, "which includes every command-runner package" | "Command-runner package" appears once, is not glossed, and is not in the glossary list at the top. | Gloss it, or delete the clause. |
| Step 1, "Linking is by identity rather than by location" | Had to read twice. The paragraph then says the opposite-sounding thing, that moving the folder breaks the link, and I could not hold both ideas at once. | Split into two sentences, one about replacing a package of the same id and one about not moving the folder. |
| Step 2, "Until you do this the workflow still appears in that submenu" | Contradicts the sentence directly above it, which says enabling is what puts the workflow into the Tools menu. I read the pair three times. | Say the workflow always appears in the submenu and enabling is what makes it clickable. |
| Step 2, "the Required Setup pack every LGE installation already has" | I could not verify this. If mine does not have it, the text gives me no way to check or to install it. | Point to where Required Setup packs are listed and installed. |
| Step 3, "whatever was selected in the sidebar when the window opened" | Behaviour a Geneious user would not expect and cannot recover from. In Geneious I select documents and then choose the operation, but here if I selected nothing I apparently must close the window, select, and reopen. The text never says whether I can change the selection afterwards. | State whether the FASTQ Bundles list can be edited inside the window, and if not, say to select bundles before opening it. |
| Step 3, "The Advanced settings section for a linked package is read-only text" | Surprising and unexplained. Every other Advanced section I have met in software holds controls, and the chapter does not say why this one does not. | Say the package manifest fixes these values, so there is nothing to change. |
| Step 3, "Reference picker, which lists the reference bundles already in the open project" | I could not perform this. Nothing earlier in the chapter puts a reference bundle in my project, and the hello-world packages produce one only as output. | Say what to select for the hello-world run, given the packages ignore their inputs. |
| Step 4, "the **Run** button stays disabled until that line reports the configuration is ready" | I do not know what the ready line says, so I cannot tell a ready state from a nearly ready one. | Quote the exact wording of the ready line. |
| Step 4, "reports into the Operations panel" | The panel is named and a shortcut given, but nothing says what I should see there or how I know the run finished. | Add one sentence naming the row and the state it reaches on success. |
| Settings, Reference, "On the command line this is `--input`" | Both Reference and FASTQ Bundles say `--input`, which reads as an error. As a window user I also do not see why a command-line flag is listed under a window setting at all. | Say the flag is repeated, once for each input, or drop the flag from window settings. |
| Settings, Cores, "The default is the app's own thread count, which is sized to the machine" | Number I cannot judge. I do not know my machine's thread count or where to see it, so I cannot tell whether lowering it to 2 is a big change or a small one. | Say where the current value is shown, or give a typical figure. |
| Settings, Cores, "a Nextflow run records it without enforcing a per-process limit" | Had to read twice, and I still do not know whether setting Cores does anything at all for a Nextflow run. | Say plainly that Cores has no effect on a Nextflow run other than being recorded. |
| Settings, Include subfolders, "the checkbox appears at all only when the selected folder has subfolders" | I could not act on this. If I do not see the checkbox I cannot tell whether it is absent by design or whether I picked the wrong folder. | Say that a missing checkbox means the selected folder has no eligible subfolders. |
| Settings, Output Name, "disabled during a Run Again" | Run Again is used here and in Directory and in `--repeat-from`, but the Procedure never introduces it and no step shows where the button is. | Introduce Run Again in the Procedure, or gloss it at first mention. |
| Command-line only settings, whole section | Fifteen settings I cannot use appear before the section that tells me the command line is optional. I read all of them looking for the window equivalent. | Move the "this section is optional" reassurance to the head of this section too. |
| `--executor`, "read 'a working Docker runtime' wherever the app says container runtime" | Assumes I know what Docker is and that I have it. It is not glossed and is not in the glossary list. | Gloss Docker once, or say the setting does not apply to the workflows in this chapter. |
| `--expected-output`, "declaring an output does not make the workflow create it" | Had to read twice and is repeated twice more later. The first statement gave me no idea what the flag is for if it does not create anything. | State the purpose first, that it tells LGE which files to fingerprint, then give the caveat. |
| Reading the results, "`main.lungfishrun` and a second produces `main-2.lungfishrun`" | I do not know where "the current directory" is, since I never opened a terminal, so I cannot find the bundle at all. | Say the window route writes the bundle inside the project and point to it. |
| Reading the results, "`statusHistory` array with a timestamped entry" | Assumes I can read JSON. The whole section describes files by their internal field names with no mention of whether LGE shows any of this in a window. | Say whether the run bundle can be inspected in the app, and if not, say to open the files in a text editor. |
| Reading the results, "records a SHA-256 checksum" | SHA-256 is new here. Checksum was glossed earlier but the algorithm name was not, and I did not know whether it mattered. | Say SHA-256 is simply the kind of checksum used and no choice is involved. |
| Reading the results, "`lungfish-cli ops stats <directory>`" | A terminal command placed in a section I need as a window user, with angle brackets I do not know how to fill in. | Move it into the command-line section. |
| Reading the results, "Peak RAM reads `unknown`" | Number I cannot judge, and RAM is not explained in this context. It reads like something went wrong. | Say plainly that this is expected for these runs and is not a failure. |
| What good looks like, "the command itself should have exited zero" | Assumes a terminal. From the window I never see an exit code, so I cannot perform this check. | Say what the window shows in place of an exit code. |
| What good looks like, "quiet in the way that engine is quiet when it succeeds" | Had to read twice. The phrase seems to be missing a word and I could not parse it. | Reword as a plain statement about what a successful log contains. |
| What good looks like, "Two defects found while testing this chapter against Preview 2026.9.13" | Confusing placement. A section that tells me how to judge my own run ends with two bugs in the app, and I could not tell whether either affects my run. | Move known defects to their own short section, or say plainly that neither affects a window run. |
| What good looks like, "a manifest that is an empty JSON object" | I cannot judge this. The text says the difference is real rather than a mistake, but I do not know whether an empty manifest means my Snakemake output is usable. | Say whether the Snakemake output can be opened in LGE. |
| On the command line, "runs headless, meaning with no window at all, by typing commands into the Terminal application" | This is the first and only sentence telling me how to reach a terminal, and it comes after seventeen code blocks and fifteen flag entries. | Say at the head of the chapter that the terminal parts can be skipped entirely. |
| On the command line, "whatever it finds on the effective `PATH`" | PATH is not glossed and the word "effective" made me think there is more than one. | Gloss PATH once, or drop the sentence for this audience. |
| On the command line, "`✓ Workflow syntax appears valid`" | "Appears valid" is hedged and I cannot judge how much confidence to take from it. | Say what validate does and does not check. |
| On the command line, "which is why the `--cpus 2` below is not optional" | Had to read twice. I did not connect the `--cpus 2` in this example to the one in the earlier Snakemake example three code blocks up. | Say the repeat must match the original run's settings, and name which earlier command set them. |
| On the command line, "refuse the file locks Nextflow takes on its cache" and "`._` sidecar files" | Two unexplained storage concepts in one sentence, in a paragraph about external drives. I could not tell whether this affects me. | Say plainly that runs on an external drive still work and LGE moves the scratch files for you. |
| Whole chapter, no failure guidance | The chapter names the Workflow Operation Error alert and `stderr.log` but never shows one failure and what to do about it. A Geneious user expects an error to be recoverable in the dialog. | Add one short example of a failed run and the first thing to check. |

Row count: 41.

**One thing I learned.** A run bundle keeps the exact command, the exit status, and a fingerprint of every file, so I can prove months later which file came out of which run.

**One thing I still could not do.** Complete step 3 for the hello-world packages, because the Reference picker needs a reference bundle that nothing earlier in the chapter puts in my project.

**The sentence I liked most.** "They exist so you can confirm the whole path works, from linking a package to reading its provenance, before you trust the path with a pipeline that takes an hour."
