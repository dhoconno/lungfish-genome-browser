# Reader 1 report, undergraduate

Chapter: `docs/user-manual/chapters/08-workflows/03-running-external-workflows.md`

Persona: sophomore, one genetics course, never opened a terminal, never used sequencing software.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "Nextflow at 26.04.6 and Snakemake at 9.25.2" | Two version numbers I cannot judge. I do not know whether these are recent, whether mine will match, or what I should do if the app has a different one. | Say the numbers are fixed by LGE and the reader never has to check them. |
| What it is, "does not silently change behaviour when someone upgrades a tool" | I had to read this twice. I could not tell who "someone" is, whether it is me, and whether upgrading is something I might do by accident. | Name the actor, such as another user of the same Mac. |
| What it is, "a folder ending in `.lungfishflowpkg`" | A folder with a file-style extension is new to me. In Finder I expect a folder to have no dot suffix, and I could not picture what I would be looking at. | Say the folder looks like a single file in Finder until you open it. |
| What it is, "holds a pipeline file together with a `manifest.json`" | "Pipeline file" and "manifest" are both unexplained here, and "JSON" is never explained anywhere in the chapter. I do not know if I can open a JSON file or what is inside one. | Gloss JSON at first use as a plain-text format both people and programs read. |
| What it is, "declares what the pipeline needs as input and what it produces as output" | "Declares" is used many times in this chapter as a technical verb and I was never told what it means for a file to declare something. | Say once that declaring means the manifest lists it in writing. |
| What it is, "a package that declares a reference bundle and a read bundle" | "Reference bundle" and "read bundle" arrive with no gloss. I know from genetics what a reference genome is, but not what a bundle is or why reads come in one. | Gloss bundle at first use as a folder holding a data file plus its index and record. |
| What it is, "which is where a run is actually configured and started" | The paragraph gives me two doors, a submenu, and a window in one block, and I had to read it twice to keep straight which door does what. | Split the Library door and the Operations door into two sentences with a blank line. |
| What it is, "reaches the same engines through `lungfish-cli workflow run`" | The command line is introduced here as an equal option, but I have never opened a terminal and nothing here tells me I may skip it. The reassurance only arrives on line 185, far too late. | Say here that the command line is optional and the window does everything. |
| What it is, "one published nf-core pipeline, `nf-core/viralrecon`, pinned at release 3.0.0" | "nf-core", "published", and "pinned" all land in one sentence. I could not tell whether this paragraph is something I need or something I may skip. | Open the paragraph with "You can skip this unless you work on viruses." |
| What it is, "If you have a bare `.nf` file or a `Snakefile`" | I do not know what a `.nf` file is or that `Snakefile` is a file name with no extension. I would not recognise either on my Desktop. | Say a `.nf` file is a Nextflow pipeline and `Snakefile` is the Snakemake one, named exactly that. |
| Why you would do this, "it calls a tool that has not been packaged for the app" | "Packaged for the app" is unexplained and reads like jargon. I could not tell what a lab would have to do to package something. | Cut the clause or say the tool is not one LGE ships. |
| Why you would do this, "a `.lungfishrun` folder holding the exact command, the engine, the exit status, the timing" | "Exit status" is not explained until much later, at line 173, and I did not know a program had one. | Gloss exit status here as the number a program leaves behind saying whether it worked. |
| Before you start, "click the green **Code** button, choose **Download ZIP**" | This step sends me to a website I have never used to download source code. I could not tell whether the ZIP is large, whether this is safe, or why example files for a shipped app are not inside the app. | Say the download size and why the examples are not bundled. |
| Before you start, "a linked package stays where it is rather than being copied into the project" | I read this twice. The consequence, that I must not move the folder, is only given later at line 71, so at this point the sentence felt like a fact with no purpose. | Add the consequence right here, that moving the folder later breaks it. |
| Before you start, "wraps it as a `.lungfishref` reference bundle with a manifest and an index" | Third bundle extension in three pages, and "index" is unexplained. I do not know what a sequence index is or why a four-base file needs one. | Gloss index at first use as the small lookup file that lets a program jump inside a big one. |
| Before you start, "Neither reads its input, and that is the point." | I had to read this twice. It sounds like the example is broken, and the reason only becomes clear one sentence later. | Reorder so the reason comes before the claim. |
| Before you start, "their manifests declare `\"runtime\": {\"kind\": \"none\"}`" | Raw code punctuation in a sentence I am supposed to read as prose. I do not know this notation and it stopped me cold. | Say in words that the manifest asks for no container. |
| Procedure 1, "The open panel that appears is titled Link Workflow Package" | "Open panel" is a term I do not know. I would have called it a file chooser or a Finder window. | Call it a file chooser the first time. |
| Procedure 1, "linking an existing workflow identity replaces its prior source and version" | Read three times. "Workflow identity" means nothing to me at this point, and the sentence is describing on-screen text rather than telling me what to do. | Move this to the identity paragraph below and say plainly that relinking overwrites the old entry. |
| Procedure 1, "Choose folders rather than files in this panel" | I could not perform this step. The panel has no described control for switching between folders and files, so I do not know how to obey the instruction. | Say the reader clicks the `.lungfishflowpkg` folder once and presses Link Workflow without opening it. |
| Procedure 1, "naming the declared input bundle types, the declared output bundle type, the runtime kind, the plugin packs" | Four unexplained row labels in one sentence, and the screenshot that would show them is still a placeholder comment. I cannot check my screen against this. | Land the planned screenshot, or list the rows as a short list. |
| Procedure 1, "Runnable only when its runner is Nextflow or Snakemake and its manifest declares a required `.lungfishref` input, a required `.lungfishfastq` input, and at least one output" | This is a four-condition rule written as one sentence. I read it three times and still could not hold it. | Break the conditions into a short list. |
| Procedure 1, "which includes every command-runner package" | "Command-runner package" is introduced only to be excluded, and never explained. I do not know what one is or whether I have one. | Cut the clause or gloss it in three words. |
| Procedure 1, "If you link a package whose manifest declares an `id` that is already in the library" | I do not know where to see a package's `id`, so I cannot tell in advance whether a link will replace something. | Say where the id is visible, or that LGE warns you. |
| Procedure 2, "Until you do this the workflow still appears in that submenu, but greyed out" | Read twice. The step above says enabling is what puts the workflow into the menu, and this says it is already in the menu. The two statements contradict each other on first reading. | Reword the first sentence to say enabling is what makes the menu item work. |
| Procedure 2, "Both hello-world packages require the `lungfish-tools` pack, which is the Required Setup pack every LGE installation already has" | I could not tell whether I need to do anything here. The paragraph tells me to install a pack, then tells me I already have it. | State the outcome first, that no install is needed for these two examples. |
| Procedure 3, "a **FASTQ Bundles** list holding whatever was selected in the sidebar when the window opened" | I cannot perform this step. The chapter never told me to put any FASTQ into the project or to select one in the sidebar, and the hello-world packages supposedly ignore input, so I do not know what to select or whether an empty list blocks me. | Say explicitly what to select for the examples, or that any bundle will do. |
| Procedure 3, "The Advanced settings section for a linked package is read-only text rather than controls." | I had to read this twice to understand there is nothing for me to do there. | Say plainly that there is nothing to change in that section. |
| Procedure 4, "the **Run** button stays disabled until that line reports the configuration is ready" | I could not act on this. If the line says not ready, the chapter never tells me what the common causes are or how to fix them. | Add the one or two usual causes, such as no reference chosen. |
| Procedure 4, "an alert headed Workflow Operation Error names the reason" | The procedure ends without telling me what a successful run looks like on screen or how long these examples take. | Add one sentence saying the example finishes in seconds and where to see it. |
| Settings, "**Cores.** ... The default is the app's own thread count, which is sized to the machine." | I cannot judge this number. I do not know what a core or a thread is, what my Mac's count is, or what value would be too high. | Gloss a core as one of the parallel workers in the processor and say the default is right for most people. |
| Settings, "a local Snakemake run receives this as its `--cores` value while a Nextflow run records it without enforcing a per-process limit" | Read three times. It tells me the setting does different things in two engines, and I could not work out which applies to me or whether the number matters at all. | Say the setting is honoured by Snakemake and recorded only by Nextflow. |
| Settings, "**Output Name.** ... the field is disabled during a Run Again" | "Run Again" is used as the name of a feature that the chapter never introduces or shows me where to find. It appears here, at line 117, and at line 137. | Introduce Run Again in the Procedure, saying which button it is. |
| Command-line only settings, section opening | The chapter says on line 185 that the command line is optional, but this section of fourteen flags comes before that reassurance. I read all of it thinking it was required. | Move the "this section is optional" sentence to the head of the flags. |
| `--executor`, "every container runtime it knows about, Apple Containerization included, is mapped onto the same Docker profile string" | Read twice, and I do not know what Docker, a runtime, or a profile string is. The sentence also seems to warn me about something I would never do. | Say the default is correct and no reader should change it. |
| `--expected-output`, "declaring an output does not make the workflow create it" | I understood the words but not the situation. If declaring does not create it, I could not tell what declaring is for. | Say the declaration is only a promise LGE checks afterwards. |
| `--memory`, "written in the engine's own style such as `8.GB`" | The dot in `8.GB` looks like a typo to me and I would have retyped it as `8 GB`. | Say the dot is required and is the engine's own spelling. |
| `--workdir`, "the engine's scratch files" | "Scratch files" is not glossed. I guessed it means temporary, but I was not sure. | Gloss scratch as temporary files deleted after the run. |
| `--timeout`, "There is little reason to set it, because the local runner accepts it and does not enforce it" | A setting documented at length and then declared useless. I read it twice trying to find the case where it does work. | Say up front that this flag does nothing today. |
| Reading the results, "a `statusHistory` array with a timestamped entry" | "Array" is programming vocabulary I do not have, and this whole section asks me to read inside a JSON file without ever showing me how to open one. | Say how to open the file, for example with TextEdit, before naming its contents. |
| Reading the results, "`replayIdentity` inside the manifest is the block that makes Run Again possible. It records a SHA-256 checksum" | "SHA-256" appears with no explanation. Checksum was glossed earlier, but I do not know whether SHA-256 is a different thing or the same thing with a name. | Say SHA-256 is simply the kind of checksum used. |
| Reading the results, "`lungfish-cli ops stats <directory>`" | This is a terminal command placed in a section I thought was about reading files in Finder. I cannot run it, and no window equivalent is offered. | Say whether the Operations panel shows the same summary. |
| Reading the results, "Peak RAM reads `unknown` for these runs" | I do not know what peak RAM is here or whether `unknown` means something went wrong. | Say `unknown` is expected and not a fault. |
| What good looks like, "The logs should be quiet in the way that engine is quiet when it succeeds." | I read this sentence three times. It seems to be missing a word, and "quiet" as a property of logs is not something I could check. | Replace with a plain statement about what the log should end with. |
| What good looks like, "A nonzero exit with output files present is worse than no output at all, because the files may be partial." | This tells me to distrust files but not how to spot a partial one or what to do with it. | Say the fix, which is to delete the results and rerun. |
| What good looks like, "the Snakemake example writes ... a manifest that is an empty JSON object" | I could not judge whether an empty manifest is a pass or a fail until the next sentence told me. That is exactly the check I was told to make. | Put the reassurance before the observation. |
| What good looks like, "Two defects found while testing this chapter against Preview 2026.9.13" | Two paragraphs about bugs in the app placed inside the section that tells me whether my own run succeeded. I read it twice thinking these were things I had done wrong. | Move the defects to their own short section at the end. |
| On the command line, "The whole procedure runs headless, meaning with no window at all, by typing commands into the Terminal application." | I have never opened Terminal and this sentence assumes I can find it and know what to type into it. | Add where Terminal lives, or say the section is for readers who already use it. |
| On the command line, "falls back to whatever it finds on the effective `PATH`" | "PATH" is capitalised jargon with no gloss, and "effective" makes it worse. I cannot act on this sentence. | Cut it, or gloss PATH as the list of places the Mac looks for programs. |
| On the command line, "`lungfish-cli workflow validate hello-world-nextflow.lungfishflowpkg/main.nf`" | I could not run this. The chapter never says where `lungfish-cli` comes from, whether it is installed with the app, or which folder my Terminal has to be in for that relative path to work. | Say how to get `lungfish-cli` and to run these from the folder holding the packages. |
| On the command line, "Each prints the detected engine and then `✓ Workflow syntax appears valid`" | The backslashes at the ends of the code lines below are never explained. I did not know whether to type them. | Say a trailing backslash means the command continues on the next line. |
| On the command line, "without one the command exits 64 with a message saying so" | The number 64 means nothing to me, and I could not judge whether it is bad. | Say 64 is just an error code and name the message instead. |
| On the command line, "which is why the `--cpus 2` below is not optional" | I read this twice. It reads as a rule about that one flag, when the real rule is that every original setting must be repeated. | State the general rule, that a repeat run must pass the same settings. |
| On the command line, "some volumes cannot host them, either because they refuse the file locks Nextflow takes on its cache or because they store extended attributes as `._` sidecar files" | Three unfamiliar terms in one sentence, "file locks", "extended attributes", and "`._` sidecar files". I read it three times and took nothing from it. | Say some external drives cannot hold the engine's temporary files, and LGE moves them automatically. |
| On the command line, "a run on an external exFAT disk" | "exFAT" is not explained and I do not know how to tell whether my drive is one. | Say this covers most USB drives formatted for both Mac and Windows. |
| Whole chapter | No screenshots at all. Both are still placeholder comments, so at no point could I confirm that what I was seeing matched the text. | Land the two planned screenshots. |

The one thing I learned: a run bundle is a folder that keeps the exact command, the result, and a fingerprint of every file, so months later you can prove which files an analysis used.

The one thing I still could not do: run either example, because I never learned what to put in the FASTQ Bundles list or how to get `lungfish-cli` onto my Mac.

The sentence I liked most: "A checksum is a short fingerprint computed from a file's exact bytes, so two people can confirm later that they are holding the identical file."
