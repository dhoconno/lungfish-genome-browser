# Reader 2 report

Chapter: `docs/user-manual/chapters/08-workflows/03-running-external-workflows.md`

Persona: senior undergraduate, two years of wet-lab pipetting, strong on library prep and PCR, no data analysis experience, has never opened a terminal.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| Front matter, "audience: analyst" | The chapter labels itself for an analyst. I am not one, and nothing tells me whether I am supposed to be reading this or whether I should stop. | Say in the first paragraph who this chapter is for and what a reader is expected to already know. |
| What it is, "took a finished run inside Lungfish Genome Explorer (LGE) and wrote it out as a pipeline" | "Pipeline" is used before it is explained. I know a pipeline in the sense of a lab protocol, but I cannot tell whether that is the same thing here or something more formal. | Gloss pipeline at first use as the ordered set of analysis steps written in a file. |
| What it is, "This chapter runs the arrow the other way." | Had to read twice. There is no arrow drawn anywhere, so I was looking for a figure. | Replace the arrow image with a plain statement that the previous chapter exported and this one imports. |
| What it is, "LGE pins the versions it manages, Nextflow at 26.04.6 and Snakemake at 9.25.2" | Two numbers I cannot judge. I do not know if these are recent, old, or whether I need to have them installed already. | Say that the numbers are simply the fixed versions LGE ships and that the reader does not need to install anything. |
| What it is, "so a run does not silently change behaviour when someone upgrades a tool on the machine" | I do not know what "the machine" means here. My Mac? A server? Who would be upgrading it? | Name it as your own Mac, and say who could upgrade a tool there. |
| What it is, "a folder ending in `.lungfishflowpkg`" | On my Mac a folder does not usually have an extension, and the text later says to choose folders rather than files. I could not picture what I would actually see in Finder. | Add one sentence saying it looks like a single file in Finder but LGE treats it as a folder. |
| What it is, "holds a pipeline file together with a `manifest.json` describing it" | "manifest.json" is not glossed. I do not know what JSON is or whether I am ever meant to open this file. | Gloss JSON at first use as a plain-text format for structured data, and say the reader never has to edit it. |
| What it is, "declares what the pipeline needs as input and what it produces as output" | "input" and "output" here mean file types with LGE-specific names, but I did not learn that until much later in step 1. | Say at this point that inputs and outputs are declared as bundle types, and forward-point to where they are listed. |
| What it is, "shows a reference picker and a reads picker" | "reference bundle" and "read bundle" appear here with no definition. I know what a reference genome is from class, but not what a bundle is. | Gloss bundle at first use as a folder LGE manages that holds a data file plus its indexes and metadata. |
| What it is, "The command line reaches the same engines through `lungfish-cli workflow run`" | This is the first appearance of the command line and it lands in the middle of a paragraph about menus, with no warning that it is optional. | Add the note that the command line is optional here rather than only at the start of the last section. |
| What it is, "LGE supports exactly one published nf-core pipeline, `nf-core/viralrecon`, pinned at release 3.0.0" | "nf-core" and "published" are both unexplained. I could not tell whether this sentence affects what I am about to do. | Gloss nf-core in the sentence, and state plainly that this chapter is not about that pipeline. |
| What it is, "If you have a bare `.nf` file or a `Snakefile`" | I do not know what a `.nf` file is or what a Snakefile looks like, and "bare" adds a third unknown. | Say that these are the single pipeline files a collaborator might email you, without a package folder around them. |
| Why you would do this, "a result file with no record attached to it saying what produced it" | Took two reads. The point is good but the sentence stacks two negatives. | Split it into a statement of what you get and a statement of what is missing. |
| Why you would do this, "a `.lungfishrun` folder holding the exact command, the engine, the exit status, the timing" | "exit status" is not explained until much later. I did not know a program had a status when it stopped. | Gloss exit status at first use as the number a program reports when it finishes, with zero meaning success. |
| Why you would do this, "provenance sidecar, which is a JSON file recording the checksum" | Three unfamiliar terms in one clause. Checksum is glossed in the next sentence, but sidecar never is. | Gloss sidecar as a small companion file written next to a result file. |
| Before you start, "choose **File > New Project** (Cmd-N), or click Create Project on the Welcome window, and pick a folder" | I could follow this, but nothing tells me what to name the project or whether the choice matters later. | Add one sentence saying any name and any folder you can write to will work. |
| Before you start, "They live in the repository under `Examples/WorkflowPackages/`" | "the repository" is used before it is explained. I did not know LGE had a public code repository or that I was allowed to download from it. | Say that the repository is the project's public page on GitHub, where the source code lives. |
| Before you start, "click the green **Code** button, choose **Download ZIP**" | I could perform this, but the ZIP unpacks into a folder whose name I was not told, so I did not know what to look for. | Name the folder the ZIP produces, or say the folder name varies and to search inside it. |
| Before you start, "a linked package stays where it is rather than being copied into the project" | I did not understand the consequence until the warning in step 1. Reading it here, it sounded like a technical aside. | State the consequence right here, that moving the folder later breaks the workflow. |
| Before you start, "The Nextflow package writes a four-base FASTA file and wraps it as a `.lungfishref` reference bundle with a manifest and an index" | "four-base FASTA" stopped me. I know FASTA from class, but a four-base sequence is so small I assumed it was a typo or an abbreviation. | Say explicitly that the sequence is only four letters long because the example is deliberately trivial. |
| Before you start, "with a manifest and an index" | "index" is unexplained. I do not know what an index does for a FASTA file or whether I need to make one. | Gloss index as a small companion file that lets tools jump to a position without reading the whole file. |
| Before you start, "Both packages carry an `environment.yml` naming Python 3.10, which is the conda environment they would use" | "conda" and "environment.yml" are both new, and the sentence tells me about something that then turns out not to apply. I read it twice trying to work out whether I had to install Python. | Either gloss conda or cut the sentence, and say plainly that no Python install is needed. |
| Before you start, "their manifests declare `\"runtime\": {\"kind\": \"none\"}\"`" | This is raw JSON shown to a reader who has not been told what JSON is. I could not tell whether I was supposed to type it anywhere. | Describe the declaration in words instead of showing the raw text, or say the reader is only being shown what is inside the file. |
| Procedure 1, "its message states that the package stays at this location and that linking an existing workflow identity replaces its prior source and version" | "workflow identity" appears here with no definition, and the sentence is long enough that I lost the subject. | Gloss identity as the id string inside the manifest, and split the sentence. |
| Procedure 1, "Choose folders rather than files in this panel" | I could not perform this. In a macOS open panel I do not choose a mode, I just click something. I did not know whether there was a toggle I was missing. | Say that the package appears as a selectable item and that you click it once and press Link Workflow. |
| Procedure 1, "grouped by the category its manifest declares, which for these two is **Templates**" | "category" is a new concept introduced in passing, and it later determines which Tools submenu the workflow lands in. I did not connect the two until step 2. | Say here that the category is also the name of the Tools submenu the workflow will appear under. |
| Procedure 1, "naming the declared input bundle types, the declared output bundle type, the runtime kind, the plugin packs the manifest requires" | Four labelled rows named in one sentence, three of them terms I have not met. I had to read it twice and still could not picture the card. | Turn the row list into a short list, or point to the planned screenshot as the place to see it. |
| Procedure 1, "a required `.lungfishref` input, a required `.lungfishfastq` input, and at least one output" | I can guess that `.lungfishfastq` holds reads, but neither extension is spelled out at first use. | Say in words what each extension holds, a reference genome and sequencing reads. |
| Procedure 1, "which includes every command-runner package" | "command-runner package" appears once and is never explained. I do not know whether I would ever have one. | Either gloss it or cut the clause, since the reader has no way to act on it. |
| Procedure 2, "Until you do this the workflow still appears in that submenu, but greyed out" | The order confused me. The paragraph tells me to switch it on, then describes the state before switching it on. I read the paragraph twice to be sure I had not skipped a step. | Put the disabled-state description before the instruction, or mark it clearly as what you would have seen. |
| Procedure 2, "the `lungfish-tools` pack, which is the Required Setup pack every LGE installation already has" | I cannot judge this. It says every installation already has it, but the previous sentence tells me to install it if missing. I did not know which situation I was in or how to check. | Say where in the app to check whether the pack is present. |
| Procedure 3, "a **FASTQ Bundles** list holding whatever was selected in the sidebar when the window opened" | I could not perform this. I was never told to select anything in the sidebar before opening the window, and the example packages do not come with reads. | Add the step that selects a read bundle first, and say what to use when the project has none. |
| Procedure 3, "which lists the reference bundles already in the open project" | The example project I just created is empty, so this picker would be empty too. The chapter never tells me where to get a reference bundle for the walkthrough. | Name a reference bundle to import first, or say the packages ignore their inputs so any bundle will do. |
| Procedure 3, "It has six sections in a fixed order, Overview, Inputs, Primary settings, Advanced settings, Output, and Readiness." | Primary settings and Output are named here and then never described, while the other four are. I kept waiting for them. | Say one sentence about each of the six, or say which ones the example packages leave empty. |
| Procedure 3, "Selecting several read bundles does not queue several runs." | Good warning, but I did not know what "pooled into a single run" does to my results. Would I get one merged answer or a mess? | Say what pooling means for the result, one combined output rather than one per library. |
| Procedure 4, "the **Run** button stays disabled until that line reports the configuration is ready" | I cannot judge this. I was not told what the ready line actually says, so I would not recognise it. | Quote the exact wording of the ready line. |
| Procedure 4, "The run reports into the Operations panel" | This is the end of the procedure and I still do not know how I learn that the run finished, or how long these examples take. | Add the expected run time for the examples and say what a finished run looks like in the panel. |
| Settings, "**Reference.** ... On the command line this is `--input`." | Both Reference and FASTQ Bundles say they map to `--input`. I read both twice trying to work out which one wins. | Explain that `--input` is repeated once per input, or drop the mapping here. |
| Settings, "**Cores.** ... The default is the app's own thread count, which is sized to the machine." | I cannot judge this. "Thread count" is a new term, and I do not know what number my Mac would give or what a safe value is. | Gloss cores in one clause and give a concrete example number. |
| Settings, "a local Snakemake run receives this as its `--cores` value while a Nextflow run records it without enforcing a per-process limit" | Read twice. The sentence tells me the setting partly does not work, but not what to do about it. | Say plainly that the setting affects Snakemake runs and is only recorded for Nextflow. |
| Settings, "**Output Name.** ... the field is disabled during a Run Again" | "Run Again" is used as a named feature here but is only explained later, under `--repeat-from`. | Explain Run Again where the procedure first needs it, or at first mention here. |
| Command-line only settings, whole section | Sixteen flags described in a row, before the section that tells me the command line is optional and I may skip it. I read all of them thinking they were required. | Move this section after the optional-command-line note, or put the note at its head. |
| Command-line only settings, "**`--executor`.** ... `docker`, `conda`, and `local` accepted." | "Docker", "conda", and "profile" are all unexplained, and the entry then says the setting is effectively fixed. I could not tell whether I needed Docker installed. | Say that this applies only to the Viral Recon route covered in another chapter. |
| Command-line only settings, "read \"a working Docker runtime\" wherever the app says container runtime" | I had to read this twice. It asks me to mentally substitute one phrase for another while describing a setting I was just told to leave alone. | Cut the substitution instruction or move it to the Viral Recon chapter. |
| Command-line only settings, "**`--memory`.** ... written in the engine's own style such as `8.GB`" | I cannot judge this. I do not know how much memory these runs need or whether the dot in `8.GB` is a typo. | Confirm the dot is intentional and say the examples in this chapter need no memory setting. |
| Command-line only settings, "**`--workdir`.** ... it reaches Nextflow as `-work-dir`" | Single dash versus double dash appears here for the first time with no explanation. As someone who has never typed a command, this looked like an error. | Note once that Nextflow's own options take a single dash. |
| Command-line only settings, "This flag is the setting." | This sentence repeats eleven times and carries no information I could act on. Each time I stopped to check whether I had missed something. | Drop the repeated sentence or replace it with what the flag does when omitted. |
| Reading the results, "a first run of `main.nf` produces `main.lungfishrun` and a second produces `main-2.lungfishrun`" | I could not tell where these appear. "The current directory" means nothing to me because I have never used a terminal. | Say that this behaviour applies only to command-line runs, and where a window run puts its bundle. |
| Reading the results, "`manifest.json` is the record of the run itself." | I do not know how to open this file or with what. Double-clicking JSON on my Mac does something unpredictable. | Say to open it with TextEdit, or say the app shows the same information somewhere. |
| Reading the results, "A completed Snakemake run of the example package records three, `prepared`, `running`, and `completed`, seconds apart." | I cannot judge this. Three entries seconds apart is given as fact, but not as a thing to check, so I do not know whether four entries would be wrong. | Say whether the count matters or whether only the final state does. |
| Reading the results, "reads, in part, `[SUCCESS] completed=1 failed=0 cached=0`" | "cached" is unexplained. I could not tell whether a nonzero cached count is good or bad. | Gloss cached as a step skipped because its result already existed. |
| Reading the results, "It records a SHA-256 checksum and byte size for every file" | "SHA-256" appears with no gloss. Checksum was explained much earlier, but the name of the method was not. | Say SHA-256 is simply the recipe used to compute the checksum. |
| Reading the results, "which LGE signs when a signer is configured and leaves unsigned otherwise" | "signer" and "signs" are unexplained, and I do not know whether I have one or should want one. | Say that signing is optional, off by default, and covered elsewhere. |
| Reading the results, "The command `lungfish-cli ops stats <directory>`" | A command-line instruction placed in a section I would read after a window run. The angle brackets are also unexplained. | Move it to the command-line section, and say the brackets mean substitute your own path. |
| Reading the results, "Peak RAM reads `unknown` for these runs" | I cannot judge this. It sounds like a defect, and I would have gone looking for what I did wrong. | Say plainly that `unknown` is expected here and not an error. |
| What good looks like, "the command itself should have exited zero" | I do not know how a window user would see this, and the phrase "exited zero" assumes the terminal knowledge I do not have. | Say where in the Operations panel the same information appears. |
| What good looks like, "A nonzero exit with output files present is worse than no output at all, because the files may be partial." | Read twice. "Worse than no output at all" is a value judgement I had to unpack before the reason landed. | Lead with the reason, that partial files look usable and are not. |
| What good looks like, "The logs should be quiet in the way that engine is quiet when it succeeds." | This sentence is ungrammatical and I read it three times. "the way that engine" is missing a word. | Fix the sentence. |
| What good looks like, "a manifest that is an empty JSON object" | I cannot judge this. An empty file sounds broken, and although the next sentence says it is real, I would not trust my own run. | Say what the empty manifest looks like on screen, `{}`, so the reader recognises it. |
| What good looks like, "Two defects found while testing this chapter against Preview 2026.9.13 are worth knowing." | This paragraph is about defects in the app, placed under a heading that promised me a checklist for judging my own run. I read the whole thing looking for something to check. | Move the defect notes to their own short heading. |
| What good looks like, "only `--prepare-only` waives that requirement in the guard itself. In practice `--dry-run` still works ... because it returns before the guard is ever reached" | "the guard" is developer language. I do not know what a guard is, and the paragraph is about internal code behaviour. | Say only what the reader observes, that both flags work for planning. |
| On the command line, "The whole procedure runs headless, meaning with no window at all, by typing commands into the Terminal application." | This is the first mention that a Terminal application exists, and there is no pointer to how to open it. | Point to where the terminal is covered, or say to skip the section if you have not used one. |
| On the command line, "it works out the engine from the file name. A lower-case `.nf` extension means Nextflow, and a file name containing `snakefile` in any case means Snakemake." | Read twice. Lower-case matters for one and does not for the other, and the earlier defect note contradicts this for `workflow validate`. | State the rule once and cross-reference the defect note. |
| On the command line, "otherwise falls back to whatever it finds on the effective `PATH`" | "PATH" is unexplained and typed in code font as if I should recognise it. | Gloss PATH as the list of folders your Mac searches for programs, or cut the clause. |
| On the command line, "```bash lungfish-cli workflow validate hello-world-nextflow.lungfishflowpkg/main.nf```" | I could not perform this. The command assumes I am already inside the folder holding the package, and nothing tells me how to get there. | Add the step that moves into the folder, or use a full path in the example. |
| On the command line, "Each prints the detected engine and then `✓ Workflow syntax appears valid`." | I cannot judge what a failure looks like, so I would not recognise one. | Show the failure output too, or say what the first line of a failure says. |
| On the command line, "Passing `--format json` returns the same answer as a small object with `engine`, `valid`, an `errors` array, and the resolved `workflow` path." | "object" and "array" are programming terms used without gloss, in a section aimed at a first-time terminal user. | Say it returns the same answer in machine-readable form and leave the field list out. |
| On the command line, "without one the command exits 64 with a message saying so" | I cannot judge the number 64. I do not know whether it is an error code I should look up. | Say that any nonzero number means the command refused to run, and give the message text. |
| On the command line, "the output path one level deeper because its `Snakefile` writes into a `results/` subfolder of its own" | Read twice. The reason is buried in a subordinate clause, and getting this path wrong would silently produce a failing check. | Split into two sentences and flag that the path must match exactly. |
| On the command line, "so `--results-dir` reaches Snakemake twice, once as its working directory and once as a config value the pipeline may read" | "config value" is unexplained, and "may read" left me unsure whether anything depends on it. | Say the second use is only available to pipelines that ask for it, and does not affect these examples. |
| On the command line, "which is why the `--cpus 2` below is not optional" | I had to scroll back to the earlier block to see where `--cpus 2` came from. The connection between the original run and the repeat is not stated. | Say that the repeat must match the original run's settings exactly, and name which block it is repeating. |
| On the command line, "`lungfish-cli run-headless` is a thin alias for `workflow run --quiet`" | "alias" and "--quiet" are both new. I could not tell why I would use this instead of the command I just learned. | Say it is the same command with less printed output, useful inside scripts. |
| On the command line, "either because they refuse the file locks Nextflow takes on its cache or because they store extended attributes as `._` sidecar files" | Four unfamiliar terms in one sentence. I gave up on this paragraph. | Reduce it to the observable fact, that runs on external drives may put their scratch files on the internal disk. |
| On the command line, "a run on an external exFAT disk" | "exFAT" is unexplained. I do not know what format my external drive uses or how to find out. | Say this covers most drives formatted for use with Windows, and say how to check in Finder. |
| Next, "This is the last chapter in [Workflows](.)." | The link text is a single dot. I could not tell where it goes. | Give the link a readable name. |

The one thing I learned. A run bundle is the thing that lets somebody six months later prove which file produced which result, and that is a better reason to use the app than convenience.

The one thing I still could not do. Complete step 3, because the chapter never tells me where to get the reference bundle and the read bundle the pickers ask for, and my new project is empty.

The sentence I liked most. "A checksum is a short fingerprint computed from a file's exact bytes, so two people can confirm later that they are holding the identical file."
