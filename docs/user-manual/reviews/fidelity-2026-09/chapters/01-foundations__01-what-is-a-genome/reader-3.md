# Reader 3 report: What Is a Genome

Persona: pre-med student, English is a second language, never opened a terminal.

| Location | What stopped me | What would have helped |
| --- | --- | --- |
| What it is / "Every organism runs on an instruction" | "Runs on" is an idiom. I thought first of a machine running. I read the sentence twice before I saw it means "uses". | Say the genome is the full set of genetic instructions the organism uses. |
| What it is / "Sequencing does not hand you the" | "Hand you" is another idiom, and "the genome whole" puts "whole" in a place I did not expect. | Say sequencing does not give you the complete genome at once. |
| What it is / "That already-known version is the reference" | The sentence "It is one specific sequence, read from a well-characterised sample, filed in ..." runs over four lines with five comma clauses. I lost the subject before the end. | Split it after "stable accession". |
| What it is / "read from a well-characterised sample, filed" | I do not know what makes a sample "well-characterised", and the phrase is not glossed. | One clause saying it is a sample already studied in detail. |
| What it is / "adopted by a field as the" | "A field" means an area of science, but I first read it as a data field in a file. | Say "a research community". |
| What it is / "Take the map away and two" | This is a command shape used for a hypothetical. My grammar training says it is an instruction, so I stopped. | "If there were no shared map, two labs would ..." |
| What it is / "The second is how Lungfish Genome" | One sentence carries three separate rules about coordinates. After three readings I still am not sure whether "inclusive" applies to the start, the end, or both. | Give "inclusive" its own sentence saying both the first and last base are counted. |
| Why you would do this / "The HBB gene record is NG_000007.3" | "RefSeqGene" is used before it is explained, and it is not a term I can click. | Put the explaining sentence first, then the record name. |
| Why you would do this / "This one is 81,706 bases long" | I could not judge whether 81,706 bases is large or small. I have no sense of scale. | One comparison, such as how it relates to the size of chromosome 11. |
| Why you would do this / "so it carries eight genes and" | Later the chapter says "The record carries 8 genes". Here it is a word, there a numeral. I stopped to check they were the same fact. | Use one style for the same count. |
| Why you would do this / "Its protein-coding stretch, the CDS, is" | The string `join(70595..70686,70817..71039,71890..72018)` is shown but never read out. I do not know whether `..` means "to", and I cannot tell which range is which piece. | One sentence saying `..` means "through" and that the three ranges are the three coding pieces. |
| Why you would do this / "The sixth amino acid of the" | "The mature beta-globin protein" uses "mature" without explanation. I did not know a protein could be immature. | Say what "mature" means here, or drop the word. |
| Why you would do this / "Change the middle base of that" | The chapter never states that the middle base of `GAG` is the A. I worked it out but was not certain, and this is the central fact. | Say the middle base is the A at position 70614. |
| Before you start / "You need a project open in" | I have never made a project in this app. The chapter assumes one exists and does not say how to make one. | A link to the step or menu item that creates a project. |
| Before you start / "The fixture ships the record as" | I do not know the word "fixture" in this sense, and I do not know where the file is on my computer. | Say a fixture is a sample file supplied with the manual, and give its folder. |
| Before you start / "Fixture details and citation are recorded" | The text `{{ fixtures_refs[] \| cite }}` is printed on the page. It looks like broken code and I did not know whether to ignore it. | Nothing should show here. It reads as unfinished. |
| Procedure step 1 / "Choose File > Import Center... (Cmd-Shift-I)" | I could not tell whether the Reference Sequences tab is already open or I must click it. | Say to click the tab named Reference Sequences. |
| Procedure step 2 / "Drop NG_000007.3.gb onto the Reference Sequences" | "Drop" means drag from somewhere with the mouse. The chapter never says from where, and I do not know where the file sits. | Say to drag the file from the folder where the fixture lives. |
| Procedure step 2 / "LGE compresses and indexes the sequence" | I could not tell how I know when this has finished. Is there a progress bar? Do I wait? | One sentence saying what appears when the import is done. |
| Procedure step 3 / "Find the new bundle under Reference Sequences/" | I did not know what name the new bundle would carry, so I did not know what to look for. | Say the bundle appears under the name taken from the file. |
| Procedure step 4 / "type NG_000007:70613-70615. The viewport frames the" | Earlier the record is `NG_000007.3` with `.3`, but here I must type it without `.3`. In my head I typed the wrong thing. The reason appears two sections later. | Warn at the step itself that the version is dropped. |
| Procedure step 5 / "Type the same coordinate into the" | "The position field on the ruler" assumes I can find the ruler. I do not know which part of the window that is. | Name where the ruler sits, or point to the screenshot. |
| Reading the results / "a folder that macOS shows as" | I did not understand how a folder can look like one item, or whether I should try to open it. | Say macOS treats it as one package and that you should not open it by hand. |
| Reading the results / "holds the sequence as a compressed" | "With its two indexes beside it" does not say what the two are or why there are two. | Name the two index files, or say why two are needed. |
| Reading the results / "Hand LGE a coordinate whose contig" | A clause sits inside a clause ("even after its chromosome-name mapping runs") and I lost the main verb "refuses". | Split into two sentences, and say what the refusal looks like on screen. |
| Reading the results / "even after its chromosome-name mapping runs" | I do not know what chromosome-name mapping is, or that the app does it. It is not glossed. | One sentence saying the app tries to match names like chr11 and 11. |
| Reading the results / "and 102 annotation features in total" | I do not know whether 102 is the number I should see, or what a different number would mean. | Say what to do if the count differs. |
| Reading a variant / "Read the > aloud as \"to\"" | I could not tell whether this is only for speaking, or whether the meaning is "becomes" or "compared with". | Say the reference base is written first and the sample base second. |
| Reading a variant / "An insertion makes REF one base" | "A deletion does the reverse" made me rebuild the sentence backwards to check it. | Spell out that a deletion makes REF several bases and ALT one. |
| Sample data and reference data / "and stretches where almost no reads" | I did not know whether uneven coverage is normal, or how much is acceptable. | One clause saying this is expected and is measured in a later chapter. |
| Sample data and reference data / "and that stillness is the point" | Figurative language. I understood "stillness" as "not moving" only on the second reading. | Say the reference does not change between analyses. |
| Sample data and reference data / "so when you publish a position" | "Pin the version" is an idiom I did not know. | Say to write the full accession including the `.3`. |
| Sample data and reference data / "The layout teaches as it organises" | Figurative. I could not decide whether it was telling me to do something. | Remove it, or say the folder names show what each file is. |
| Linear, circular, and segmented genomes / "A circular genome is simply unrolled" | "Unrolled" is used as a technical action but never defined, and it appears twice in the paragraph. | Say the loop is cut at one chosen point and laid out flat. |
| Linear, circular, and segmented genomes / "The problem belongs to plasmids and" | I could not tell which problem is meant. The nearest was the read split in two, but the next sentence says LGE still unrolls, which sounds like the problem is not solved. | Name the problem again, and say whether I must do anything about it. |
| What good looks like / "Confirm that the sequence viewport shows" | I do not know where in the window the length is displayed. | Say where the length appears. |
| What good looks like / "since a bundle built from a" | "Bare FASTA" appears here and in Before you start. I understand FASTA from the glossary, but not "bare". | Say a FASTA holds only sequence and no feature table. |
| On the command line / "The same import runs from lungfish-cli" | I have never opened a terminal. The command has a `\` at the end of a line and I do not know what that does, or where I would type any of this. | One line saying this section is optional and the menu steps above do the same thing. |
| On the command line / "lungfish-cli import fasta docs/user-manual/fixtures/hbb-gene/NG_000007.3.gb" | The path starts with `docs/`, which is not a place I can find on my computer. | Say the path is relative to the manual folder, or show a full path. |

The one thing I learned is that a position number means nothing without the name and version of the reference it was counted on, which is why the same sickle cell change carries different numbers on different records.

The one thing I still could not do is get from "you need a project open in LGE" to a bundle in the sidebar, because I do not know how to create a project or where the fixture file lives on my disk.

The sentence I liked most is "REF always comes from the reference and never from a sample."
