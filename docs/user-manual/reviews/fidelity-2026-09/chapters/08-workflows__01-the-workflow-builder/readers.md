# Merged reader report - The Workflow Builder

Four readers reviewed this chapter. Personas were a genetics sophomore who has never opened a terminal, a wet-lab senior with two years of pipetting and no data analysis, a pre-med student reading in a second language, and an undergraduate who has used Geneious. None had opened a terminal before.

| Location | Issue | Readers who hit it | Suggested fix |
|---|---|---|---|
| What it is, "The node-type model in the source does define eleven further types" | All four readers found "the source" unexplained and could not tell whether it meant the app's code, a file on their Mac, or something else. | 1, 2, 3, 4 | Say "the app's code" or "a saved file" instead of "the source". |
| Before you start, "19,916 reads, which is 9,958 pairs, with a mean read length of 248.4 bases" | All four readers could do the arithmetic but could not judge whether these numbers are typical, since nothing says the dataset is a deliberately small practice sample. | 1, 2, 3, 4 | Add a clause saying this is a deliberately tiny practice dataset, given so readers can check their own import matches. |
| 1. Open the window, "It is a legacy path that asks you to pick a sample at run time." | All four readers found "legacy path" unexplained and could not tell whether the Sample input was broken, deprecated, or just a different style they should ignore. | 1, 2, 3, 4 | Say plainly it is an older way of choosing the input that this chapter does not use. |
| 2. Read the palette, "The Trimming node is not a bare shell, since it holds a minimum length defaulting to 20 and a qualified quality defaulting to 15" | All four readers read this paragraph about a node they were already told they cannot place, and could not tell whether they needed to act on it. "Qualified quality" and "bare shell" landed as unexplained metaphors. | 1, 2, 3, 4 | Say up front that the paragraph is background only, and gloss or cut "bare shell" and "qualified quality". |
| 3. Place the nodes, "so the cheap steps run first and the expensive human-read comparison sees fewer reads" | All four readers were unsure whether "cheap" and "expensive" meant money or computer time until they guessed from context. | 1, 2, 3, 4 | Say plainly the words mean how long each step takes to compute, for example "faster" and "slower". |
| 4. Connect them, "Two exceptions exist for node types outside the palette, where a reference output may feed an assembly input" | All four readers met an exception about node types they were told they can never place, and could not tell why it mattered to a chain built from the palette. | 1, 2, 3, 4 | Cut the exception, or say plainly it cannot arise in a chain built in this chapter. |
| 5. Set the parameters, "The Settings section below documents all thirteen controls." | All four readers tried to reconcile "thirteen" against the entries they could actually count and came up short or confused about what counted as a control. | 1, 2, 3, 4 | State the count once and make the Settings headings match it exactly, or say plainly that three of the thirteen are toolbar controls rather than node settings. |
| 6. Run it, "any node parameter it does not recognise or that is required and missing" | All four readers could not picture how this failure would happen in a chain built entirely from the palette with unchanged defaults. | 1, 2, 3, 4 | Say this happens with files written by another version or hand-edited outside the app, not from ordinary use. |
| 6. Run it, "A project opened read-only refuses the run" | All four readers did not know what makes a project read-only or how they would notice being in that state. | 1, 2, 3, 4 | Say in one clause what makes a project read-only and where that shows. |
| Settings, "Cut mode ... offers exactly four values, `right`, `front`, `tail`, and `both`" | All four readers could not tell "right" from "tail", since the explanation that follows describes them as trimming from the same end without saying how they differ. | 1, 2, 3, 4 | State in one clause how right and tail differ, or say plainly that they behave the same here. |
| Settings, "Maximum length ... Set it when a fixed-length protocol has produced over-long reads that point at joined-together fragments" | All four readers could not picture the situation this setting addresses, since "joined-together fragments" is never named as a known artefact and no example length is given. | 1, 2, 3, 4 | Name the phenomenon in a short clause and give an example number. |
| Reading the results, "Deacon removed 9,717 of 9,876 reads, leaving 159" / "The final bundle held 106 reads over 30,498 bases" | All four readers watched the run destroy nearly all of the input and could not tell whether 106 surviving reads out of 19,916 is a usable result or only a demonstration. | 1, 2, 3, 4 | State plainly whether the reference run's output is a scientific result or a demonstration only, and what a result that size would or would not support. |
| Comparing two versions, "Every saved chain carries a version such as `1.0.0`" | All four readers were never told how or where the version number changes, yet the diff example jumps from 1.0.0 to 1.1.0 with no explanation of who or what set it. | 1, 2, 3, 4 | Say whether the version increments automatically or is set by the reader, and where. |
| Comparing two versions, "a defect in 2026.9.13 makes all three print the text form" | All four readers could not tell whether this known bug would be fixed or whether it should change how much they trust the rest of the chapter. | 1, 2, 3, 4 | Say whether a fix is expected, and that the bug affects only scripted reading, not the values themselves. |
| What good looks like, "The reference run's fused fastp call read `-q 15 -W 5 --cut_right` and its seqkit call read `-m 50`" | All four readers were asked to check these command-line flags against their own settings but were not told where in the window to find the equivalent values. | 1, 2, 3, 4 | Say where the run's command line is displayed in the window, and label each flag with the setting it corresponds to. |
| What good looks like, "Open the `workflow.json` and confirm the input node's `bundle_path` begins `@/`" | All four readers were asked to open a JSON file with no instructions for how to open it, where it sits inside the bundle, or a window-only alternative. | 1, 2, 3, 4 | Give the steps to open the file, or say what the window shows in its place. |
| On the command line, "The whole procedure runs headless, meaning with no window at all, by typing commands into the Terminal application." | All four readers had never opened Terminal and were not told how to open it or where to type, even though later sections lean on command-line output for verification. | 1, 2, 3, 4 | Add a sentence saying how to open Terminal, and make the earlier verification steps performable from the window alone. |
| Why you would do this, "reads shorter than 50 bases are not worth keeping" | Three readers could not judge 50, since nothing yet says why short reads are a problem or how 50 compares with the roughly 250-base reads used in this chapter. | 1, 2, 3 | Give the one-line reason next to the first appearance of 50, and relate it to the chapter's own read length. |
| Before you start, "click the green Code button, choose Download ZIP" | Three readers followed this and downloaded the entire repository to get two small files, with no warning about the size or that most of it could be discarded. | 1, 2, 3 | Say the ZIP holds the whole repository and only two files inside it are needed. |
| Before you start, "index" | Three readers met "index" with no gloss and pictured something else, a subscript or a book index, since their own training gave the word a different meaning. | 1, 2, 3 | Gloss index as a prepared, searchable copy of the human genome. |
| Procedure lead-in, "using fastp 1.3.6, Deacon 0.16.0, and seqkit 2.13.0" | Three readers met three tool names with no statement of what each one actually does, though glossary links exist earlier in the chapter. | 1, 2, 3 | Add one short clause per tool saying what it does, for example fastp trims, Deacon removes host reads, seqkit filters. |
| 3. Place the nodes, "Nudge the selection one grid step at a time" | Three readers met "grid step" before the Grid toggle that would explain it was introduced. | 1, 2, 3 | Introduce the grid toggle before referring to a grid step. |
| 4. Connect them, "A node whose output fans out to two downstream nodes is legal in the editor and legal for export, and refused when you run it." | Three readers read this sentence multiple times, since it states a thing is allowed and disallowed in the same breath, with "fans out", "editor", and "export" all arriving together. | 1, 2, 3 | Split into two sentences: the editor allows drawing it, but the runner refuses it. |
| 5. Set the parameters, "the project-relative form `@/Imports/HG002.chrM.lungfishfastq`" | Three readers were never told what the `@/` symbol means at its first appearance, and had to infer it from later, indirect confirmation. | 1, 2, 3 | State plainly at first use that `@/` stands for the project folder. |
| Reading the results, "it carries a lineage of five entries ... keyed `deduplicate`, `fastpTrim`, `humanReadScrub`, `pairedEndMerge`, and `lengthFilter`" | Three readers found these five internal keys spelled differently from the node names they had actually placed, and had to map them by hand. | 1, 2, 3 | Pair each key with its node name in a short table. |
| When a run fails, "leaves every unfinished node in the `skipped` state" | Three readers were given the status words but not told where in the app they would actually see them. | 1, 2, 3 | Say where per-node status is visible in the window. |
| On the command line, "`lungfish-cli workflow validate` ... exits with status 5" | Three readers did not know what an exit status is or whether the number 5 carried meaning for them. | 1, 2, 3 | Say plainly that the command refuses and prints the message shown, without needing the exit code explained. |
| On the command line, "an argv beginning `lungfish-cli workflow builder-step run --operation ...`" | Three readers had never met "argv", which the chapter itself replaces one paragraph earlier with "argument list". | 1, 2, 3 | Use "argument list" throughout instead of "argv". |
| Why you would do this, "A chain binds its input by a path relative to the project" | Three readers found "path" and "relative to the project" assumed knowledge they did not have, since none had typed a file location before. | 2, 3, 4 | Gloss it as recording where the input file sits inside the project folder rather than on the Mac generally. |
| Before you start, "Deacon also needs its human index, which the Plugin Manager lists under Databases" | Three readers called this the one step in the chapter they could not perform, since it names where the database is listed but never says how to install it or confirm it is present. | 1, 2, 4 | Give the click path to install the database, and say what an already-installed entry looks like. |
| 1. Open the window, "the node inspector is on the right" | Three readers found "inspector" used from here on as if already known, with no gloss at first use. | 1, 2, 4 | Gloss it once as the panel showing the selected node's settings. |
| 2. Read the palette, "the data type of each input and output port" | Three readers found "data type" used before or instead of a concrete gloss, and could not connect it to what actually flows through a port. | 1, 2, 4 | Gloss data type with a concrete example, for example a Reads port carries a FASTQ bundle. |
| 2. Read the palette, "wiring single-end reads into it produces a step that finds no overlaps" | Three readers met "single-end" with no gloss, though "paired-end" had been glossed earlier and this term was only inferable by contrast. | 1, 2, 4 | Gloss single-end at this first use. |
| 5. Set the parameters, "Every other node's numbers are already at the values this chapter uses" | Three readers could not tell whether this meant the defaults were correct as-is or that they still needed to check something themselves. | 1, 3, 4 | State explicitly that the defaults are the chapter's values and nothing needs to be typed. |
| 5. Set the parameters, "the shared FASTQ/FASTA Operations dialog with an Apply button" | Three readers had never seen this dialog, and the chapter neither describes it nor links to where it is covered. | 1, 2, 4 | Link to the chapter that documents that dialog, or say whether it is interchangeable with the inspector. |
| Settings, five command-line-only options | Three readers found five command-line entries placed inside the Settings section they were told to read, with no marker saying these could be skipped by window-only readers. | 2, 3, 4 | Move the five options into the command-line section, or mark them skippable at the head of the block. |
| Step 3, "press `Delete` or forward delete" | Three readers did not know which key "forward delete" refers to on a laptop with a single Delete key. | 1, 3, 4 | Name the key combination, for example fn plus Delete. |
| Settings, `--threads`, "Sets how many processor cores the tools in the chain may use at once" | Two readers did not know their own Mac's core count and could not judge whether the default was right for their machine. | 1, 3 | Say how to check core count, or state plainly that the default is safe. |
| Reading the results, "Deacon removed 9,717 of 9,876 reads, leaving 159" | Two readers found the near-total data loss alarming, with the reassurance that this is correct behaviour arriving only after the shock. | 3, 4 | Flag the row itself, for example "expected, see below", rather than leaving the explanation several paragraphs later. |
| What it is, "a finished run made with them can still be turned into a shareable pipeline" | Two readers met "pipeline" with no gloss anywhere in the chapter, arriving as if already known. | 1, 3 | Gloss pipeline at its first use. |
| Why you would do this, "This chapter builds its chain over the HG002 mitochondrial reads" | Two readers met HG002 used repeatedly as if it should be recognised, with no explanation of what it is. | 1, 3 | Say once that HG002 is a widely used human reference sample. |
| 1. Open the window, "whatever an older version of this chapter said" | Two readers found this aside about a previous version of the manual confusing, since they had never seen an older version and could not tell which instruction was current. | 1, 3 | Delete the aside, or state plainly that there is no save command. |
| 2. Read the palette, "a saved graph containing an Alignment node holds no minimap2 preset and no allele-frequency cutoff to read" | Two readers met three unexplained terms, minimap2, preset, and allele-frequency cutoff, in one clause describing a node they cannot place. | 1, 3 | Cut the example, or replace it with a plain statement that those nodes carry no scientific settings. |
| Settings, "it accepts whole numbers from 0 to 93" | Two readers could not judge 93 as a maximum, since nothing says what the top of the scale means or whether it is ever approached in practice. | 1, 3 | Add a clause saying where 93 comes from or that values above 40 rarely occur on this kind of data. |
| Reading the results, "each run gets its own folder at `runs/<run-id>/` inside that bundle" | Two readers did not know how to look inside a bundle to reach this folder. | 1, 4 | Say to right-click and choose Show Package Contents, or give the window equivalent. |
| Reading the results, "A `workspace` folder holds the intermediate files each step handed to the next." | Two readers were not told whether these intermediate files were safe to delete. | 1, 3 | Gloss the term and say plainly whether the files are safe to delete. |
| What the reference run did, "a single command carrying both `--dedup` and `--cut_right`" | Two readers, who had never typed a command, met these flags with no explanation of what each represents. | 1, 3 | Say these are the two options fastp received and name what each does. |
| On the command line, code block with `\` at line ends | Two readers did not know that the trailing backslash continues one command across lines, or whether to type it. | 1, 3 | Say plainly that the backslashes let one command span several lines. |
| Why you would do this, "which is why provenance exists" | Two readers found provenance linked but not glossed inline, unlike every other hard word in the chapter. | 2, 4 | Add an inline gloss, for example a record of what was run and with what settings. |
| Before you start, "This feature is experimental" / "not intended for production scientific work" | Two readers were warned off the feature's reliability but then asked to use it for a full procedure, and could not tell whether the resulting output was safe to trust. | 2, 4 | State plainly whether a result from this window is safe to report or is practice only. |
| 2. Read the palette, palette table of port names | Two readers could not tell whether the different output-port names, Deduplicated, Trimmed, Filtered, and so on, meant the ports were different types that could not connect to each other. | 2, 3 | Add a line saying every one of these ports is a FASTQ bundle and the names are descriptive only. |
| Settings, "Quality threshold ... roughly a 3 in 100 chance that a base at the cutoff is wrong" | Two readers liked this explanation as the clearest number gloss in the chapter, but noted it made the unexplained numbers around it stand out by comparison. | 2, 4 | Extend the same treatment of stating the practical meaning of the number to the other settings. |
| Settings, "Window size ... a larger window smooths over a single bad base and a smaller one reacts to it" | Two readers understood both directions but could not tell which behaviour was actually desirable, or why 5 sits in the middle. | 2, 3 | Say which direction carries the greater risk, and why the default sits where it does. |
| Settings, "Minimum overlap ... chance matches rarely reach it" | Two readers did not know what a "chance match" was or why an accidental overlap would be a problem. | 2, 3 | Say plainly that a wrong merge fuses two reads that were never really overlapping. |
| Reading the results, "the run identifier is a long unique string" | Two readers did not know where to find a run identifier on screen or what one actually looks like. | 2, 3 | Point to where the identifier appears on screen and show its example form. |
| Reading the results, counts table, "Merge overlapping pairs 159 -> 225" | Two readers were stopped cold by seeing more reads out than in, with the explanation arriving only paragraphs later. | 2, 3 | Add a short parenthetical note directly in the table row. |
| Reading the results, "renames that into place only once the provenance has been written" | Two readers read a paragraph about staging folders and renaming that described a mechanism they did not need to picture, with no clear statement of what to actually do. | 2, 3 | Reduce to one sentence saying an interrupted run leaves no half-finished bundle. |
| What good looks like, "Error: FASTQ bundle input is outside the active project: /tmp/.../Outside.lungfishfastq" | Two readers read the `...` inside the path and thought part of the manual's text was missing or a typo. | 2, 3 | Say the middle of the path was shortened for print. |
| On the command line, "`lungfish-cli workflow list` lists the supported nf-core pipeline" | Two readers met nf-core with no gloss, appearing only this once. | 2, 3 | Gloss nf-core in one clause, or cut the mention. |
| Step 2, "They exist so a file written by an older version, or by another tool, still opens" / "Those types are FASTQ Input, FASTA Input, BAM Input ... and Export" | Two readers read a long list of node types they had already been told they could never place, and found nothing telling them they could skip it. | 3, 4 | Say at the start of the paragraph that a first-time reader can skip it, or cut the full list. |
| Step 4, "makes it play the system alert sound and drop the line" | Two readers noted that a sound is the only feedback for a rejected connection, with no on-screen message, which fails silently for a muted machine. | 3, 4 | Note plainly that there is no on-screen message for a refused connection. |
| What it is, "such as trimming adapters or dropping short reads" | One reader read this twice because the chapter defines "read" and "adapter" only after using both in the same sentence. A second reader knew what an adapter is at the bench but not why it needs trimming out of the data. | 1, 2 | Move the glosses ahead of first use, and add a clause saying the adapter is not part of the organism's DNA. |
| 4. Connect them, "A port typed `Any`" | One reader could not tell whether backticked `Any` was something to type or a name used only internally. | 1, 2 | Say plainly it means a port whose data type is Any. |
| What it is, "The Workflow Builder is a window" | One reader received a definition of the window before any reason to care about it. | 2 | Open with the one-sentence version of the "why you would do this" argument first. |
| What it is, "directed acyclic graph" | One reader had to read the sentence twice to attach the following gloss back to the term, since three unfamiliar words arrived at once. | 2 | Put the plain description first and the term second. |
| What it is, "and a saved file can carry them, but the palette never offers them, so you cannot place one" | One reader was warned about a mistake before doing anything at all, which felt like an unnecessary early caution. | 2 | Move this note to later in the chapter, together with its expansion in step 2. |
| What it is, "point the same chain at a different sample" | One reader read "point at" as a figure of speech and pictured a finger before understanding it. | 3 | Write "run the same chain on a different sample" instead. |
| What it is, "the same chain without clicking through five separate dialogs" | One reader met an idiom ("clicking through") and an ungloseed term ("dialog") together in the same sentence. | 3 | Say "without opening five separate windows one after another", and gloss dialog at first use. |
| What it is, "a chain of boxes" | One reader could not tell whether "chain", "workflow", and "graph" were three different things or all names for the same drawing. | 3 | Say once that these three words all mean the same thing in this chapter. |
| What it is, "no path ever leads back to where it started" | One reader found the definition of directed acyclic graph clear, but "upstream" arrived in the very next sentence with no gloss. | 3 | Gloss upstream as "earlier in the chain" at its first use. |
| What it is, "it is easy to over-read" | One reader did not know the word "over-read" and could not guess it from its parts. | 3 | Say "it is easy to assume more than it means" instead. |
| What it is, "The palette you drag from offers six node types" | One reader met "palette" here before the later section that actually explains the word. | 3 | Gloss palette at this first use as the list of node types you drag from. |
| What it is, "`lungfish-cli workflow builder-run` executes it" | One reader met a backticked command in the second paragraph, before knowing whether the window path required it at all. | 4 | Add "which you will not need if you work in the window" at first mention. |
| Why you would do this, "which box you ticked in March" | One reader was unsure whether "ticked" meant checked or crossed out, and did not follow the joke about forgetting a setting. | 3 | Say "which value you set months ago" instead. |
| Before you start, "Download the folder `human-mito`" | One reader clicked the direct folder link first, since it was given before the ZIP route, and reached a page they could not download from. | 4 | Lead with the ZIP route, and give the folder link only as a place to confirm file names. |
| Before you start, "That toggle is not a nicety." | One reader did not know the word "nicety" and could not guess its meaning. | 3 | Say "That toggle is required, not optional" instead. |
| Before you start, "ship in the Required Setup pack that LGE installs as a unit" | One reader met "ship" as an idiom for "are included", and did not know where to find the named pack. | 3 | Say "are included in the Required Setup pack" instead. |
| Before you start, "Turn on **Show Experimental Features** in **Settings > Advanced**" | One reader, used to Geneious's Preferences menu, was not told how to open Settings, by menu path or keystroke. | 4 | Give the keystroke or menu path that opens Settings. |
| Procedure lead-in, "using fastp 1.3.6, Deacon 0.16.0, and seqkit 2.13.0" | One reader had no way to check which versions their own copy had installed, and the chapter does not say how much difference in counts is acceptable. | 4 | Say the version numbers are for the record, and that some difference in counts is expected. |
| Step 1, "There is no **File > Save Workflow** item and no Cmd-S binding" | One reader, expecting every operation to have a save command as in Geneious, found this delivered as a correction to an older manual rather than a warning to them directly. | 4 | State it as a plain behaviour difference: edits save on Run, and there is no manual save. |
| Step 1, "clicking **Run** saves the current drawing into the library before it starts" | One reader realized an unrun chain is never saved, but was not told what happens to it if the window closes first. | 4 | Say plainly what happens to an unrun drawing when the window closes. |
| Step 2, "Remove PCR duplicates" | One reader knew PCR from class but not what a duplicate read is or why removing one helps. | 1 | Gloss PCR duplicate as copies of the same original fragment made during library prep. |
| Step 2, "The sidebar and the inspector both collapse, and the canvas does not." | One reader read "collapse" without an object and thought something was breaking. | 3 | Say "can be hidden" instead of "collapse". |
| Step 2, "whose tooltips read New workflow" | One reader guessed at "tooltip" from context without being sure. | 3 | Gloss it as the label that appears when the pointer rests on a button. |
| Step 2, "which matters little at six nodes but is there" | One reader read this aside twice, since the tone shifted suddenly away from instruction. | 3 | Cut the aside or rewrite it as a plain statement. |
| Step 2, "Hovering a node shows its name and the data type" | One reader, unfamiliar with the term from birdwatching rather than software, would have preferred a physical instruction. | 3 | Say "Rest the pointer on a node" instead of "hover". |
| Step 2, "with no paired-only typing to stop you" | One reader read "typing" as keyboard typing rather than the data-type system, causing real confusion. | 3 | Say "the port does not check whether the reads are paired" instead. |
| Step 2, "produces a step that finds no overlaps rather than a connection LGE refuses" | One reader found the comparison between a step and a connection did not land, since they are not the same kind of thing. | 3 | Say "the step runs and finds nothing to merge, instead of being blocked". |
| Step 3, "note that you drag the whole node rather than a header strip" | One reader could not picture "header strip" and found a warning about what the app does not do harder to follow than a plain statement. | 3 | Say "drag anywhere on the node" instead. |
| Step 3, "Drag on empty canvas to rubber-band a group of nodes" | One reader had never met "rubber-band" used as a verb. | 3 | Say "drag a selection box around several nodes" instead. |
| Step 4, "release over the target to commit" | One reader only knew "commit" from making a promise. | 3 | Say "release to finish the connection" instead. |
| Step 4, "A node whose output fans out to two downstream nodes is legal in the editor ... and refused when you run it" | One reader, expecting a graphical tool to block an illegal action immediately, found there was no way to check for this before pressing Run. | 4 | State plainly that there is no pre-run check for branching, and Run is where it is caught. |
| Step 4, "Ports carry data types and the check is a plain match" | One reader found "plain match" vague and "carry" a confusing metaphor. | 3 | Say "a port only accepts another port of the same type" instead. |
| Step 4, "Under the popup a path control shows where that resolves to on disk." | One reader met "path control", "resolves to", and "on disk" together in one sentence, the hardest sentence in the chapter for them. | 3 | Say "Below the popup, LGE shows the file's full location on your Mac" instead. |
| Step 6, "If several sidebar rows were selected but only one of them seeded that anchor" | One reader could not decode "seeded" as a farming metaphor applied to software, in a sentence describing a case they had already been told to avoid. | 3 | Say "supplied the sample for that anchor", or cut the sentence. |
| Settings lead-in, "Ten belong to individual nodes ... and three are window controls" | One reader counted nine node entries above the Remove PCR duplicates note and could not make the arithmetic close to thirteen. | 3 | Recount the entries and correct the stated total. |
| Settings, "detection costs almost nothing" | One reader read "costs" as a money metaphor rather than computer time. | 3 | Say "takes almost no extra time" instead. |
| Settings, "the 3-prime end" | One reader knew the term from class but found it skipped while easier terms were glossed. | 1 | Gloss 3-prime as one specific end of a DNA strand. |
| Settings, "which happens on some chemistries" | One reader had not met "chemistries" used as a countable plural. | 3 | Say "on some sequencing kits" instead. |
| Settings, "Minimum overlap ... The default is 15" | One reader could not judge 15 against the 248.4-base read length given earlier in the chapter. | 1 | Relate the default explicitly to the read length used in this chapter. |
| Settings, "Minimum length ... long enough that a read usually matches one place on a genome rather than many" | One reader found this a good explanation, but one that assumed knowledge of genome mapping the chapter says has no node here. | 3 | Add a clause noting this applies when the read is later compared against a genome. |
| Settings, "None of the window's thirteen controls has a command-line flag" | One reader found the same closing sentence repeated after each of thirteen entries, and stopped reading it after the third repetition. | 2 | State it once at the top of the section and drop it from each entry. |
| Reading the results, "What the reference run did, table row 'Remove PCR duplicates + Adapter + quality trim ... 19,916 ... 9,876'" | One reader saw half the reads vanish at the very first step with no comment, though later rows are discussed at length. | 1 | Say in one sentence whether a drop of this size at the first step is expected. |
| Reading the results, "Adjacent steps that both run fastp are fused into one fastp invocation" | One reader only knew "invocation" from prayer and "fused" from cell biology. | 3 | Say "are combined into one fastp command" instead. |
| Reading the results, "The second row is the one to look at hardest" | One reader found "look at hardest" an unfamiliar idiom. | 3 | Say "deserves the most attention" instead. |
| Reading the results, "the lesson is that a step is only correct relative to what you are looking for" | One reader understood this only on a second reading, since the abstract phrasing hid a useful idea. | 3 | State the point plainly: a step that is right for one experiment can be wrong for another. |
| When a run fails, "Treat that as a real difference rather than a documentation gap" | One reader was addressed as though worried the manual itself was wrong, a concern they did not actually have. | 3 | Say "this difference is deliberate" instead. |
| When a run fails, "A run made from the command line writes neither of those two files" | One reader found this command-line-only note placed in the main results section, and worried their own window run was missing records. | 4 | Move the note into the command-line section. |
| Comparing two versions, "the history counts saves rather than edits" | One reader had to read this twice before the distinction made sense, and only once they recalled that Run itself saves. | 3 | Say plainly that a run adds a history entry even when nothing was changed. |
| What good looks like, "A bundle with no parent recorded is a bundle whose history stops at itself." | One reader found this sentence circular on first reading and was not told what action to take if they found such a bundle. | 3 | Add a sentence saying what to do when a bundle has no parent recorded. |
| What good looks like, "treat a first run as a rehearsal" | One reader found the rehearsal metaphor clear, but the two options offered, a terminal command or a full run, left only one actually available to them. | 3 | Give the window-only version of this advice first. |
| On the command line, "Both are audit records of what happened" | One reader only understood "audit record" from context, as a business term unfamiliar to them. | 3 | Say "records of what happened, kept for reference" instead. |
| On the command line, "--workflow \"Mito read cleanup.lungfishflow\"" | One reader did not know where they would be standing in the filesystem for these relative paths to resolve correctly. | 1 | State the assumption that Terminal is opened in the project's parent folder. |
| What good looks like, "Run it with `--dry-run` and read the plan" | One reader found this step placed in the non-optional part of the chapter, though it requires the terminal that the next section calls optional. | 1 | Give the window equivalent, or move the step into the optional section. |
| What it is, "no menu item that generates one from a template" | One reader, used to Geneious's copy-and-edit workflow templates, looked for a starting template and found none, with no statement of what to use instead. | 4 | Say there are no starter templates and that this chapter's chain is the intended starting example. |
| What it is, "the plain-text format that holds sequencing reads along with a quality score for every base" | One reader met "quality score" with no gloss here, only to see it return later as a number they had to set. | 1 | Gloss quality score at first use as the instrument's own estimate of the chance a base is wrong. |
| Before you start, "grouped into a single bundle named `HG002.chrM.lungfishfastq`" | One reader could not tell whether LGE derived the bundle name automatically or whether they had to type it themselves. | 1 | Say the name is derived automatically and needs no typing. |
| 4. Connect them, "Error: Workflow Builder runner requires a single linear FASTQ chain: Node 'FASTQ bundle input' must have exactly one outgoing connection." | One reader was shown this error with no statement of what to actually do about it, such as deleting the extra connection. | 1 | Add a sentence saying to delete the extra connection. |
| 6. Run it, "both carrying the same run identifier" | One reader met "run identifier" here, before it is explained in Reading the Results as a long unique string. | 1 | Gloss it at its first use here instead. |
| Settings, "The default is 15, which means the machine estimates roughly a 3 in 100 chance that a base at the cutoff is wrong, where 20 would mean roughly 1 in 100." | One reader found this the first place quality scores were explained, four sections after first use, and could not check the arithmetic, since 15 and 20 differ by 5 but 3 in 100 and 1 in 100 differ by less. | 1 | Move the explanation to the first mention, and state the scale rather than two sampled points. |
| Reading the results, "The final bundle held 106 reads over 30,498 bases, a mean read length of 287.7 against the input's 248.4" | One reader found four numbers packed into one sentence and only got the point on a second reading. | 2 | Split the sentence, and state the point first. |

Total rows: 121.

## Consensus

Thirty-seven rows were hit by three or more readers.

- "The source" is never explained as meaning the app's code or a saved file.
- The 19,916-reads and 248.4-base figures are never flagged as a small practice dataset.
- "Legacy path" for the Sample input never says plainly it is an older, unused way of working.
- The Trimming node paragraph describes a node readers cannot place, using unglossed terms like "bare shell" and "qualified quality".
- "Cheap" and "expensive" steps are never confirmed to mean compute time rather than money.
- The reference-and-assembly exception describes node types readers were already told they cannot place.
- The "thirteen controls" count never squares with what readers can actually find and tally.
- The unrecognised-parameter failure condition cannot be pictured happening in a hand-built chain.
- Read-only projects are never explained, so readers cannot tell how they would end up in that state.
- Cut mode's "right" and "tail" values are never distinguished from each other.
- "Joined-together fragments" behind Maximum length is never named or given an example length.
- Readers cannot tell whether the reference run's 106 surviving reads is a usable result or only a demonstration.
- The chain version number's origin, 1.0.0 becoming 1.1.0, is never explained.
- The known defect in the comparison feature never says whether a fix is expected.
- Readers are asked to check command-line flags against their settings with no pointer to where those flags appear in the window.
- Opening workflow.json is asked of readers with no instructions and no window-only alternative.
- Terminal is named with no instructions for opening it, even though later checks depend on it.
- The reason for a 50-base cutoff arrives long after the number is first used.
- The ZIP download's size and disposable contents are never flagged.
- The word "index" is never glossed at first use.
- The three named tools, fastp, Deacon, and seqkit, are never explained in prose.
- "Grid step" is used before the Grid toggle that would explain it.
- The fan-out sentence in Connect Them states a thing is both legal and refused without separating the two claims.
- The `@/` project-folder symbol is never explained at its first use.
- The five lineage keys never map back to the node names readers actually placed.
- Per-node run status is described without saying where in the app it appears.
- Exit status codes are given with no explanation of what they mean.
- "Argv" is used instead of the chapter's own term, "argument list".
- "Path relative to the project" is used before readers have any concept of a file path.
- The Deacon install step names where the database is listed but never how to install it.
- "Inspector" is used from its first appearance with no gloss.
- "Data type" is used or only partly glossed before the table that depends on it.
- "Single-end" is never glossed, unlike "paired-end" earlier in the chapter.
- Readers cannot tell whether the chapter's default values are already correct or still need checking.
- The FASTQ/FASTA Operations dialog is mentioned with no description or link.
- Five command-line-only settings sit inside the Settings section with no skip marker.
- "Forward delete" never names the actual key combination on a laptop keyboard.
