# Reader 1: The Workflow Builder

Persona: sophomore, one intro genetics course, never opened a terminal, never used sequencing software.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "such as trimming adapters or dropping short reads" | Read twice. The chapter tells me what a read and an adapter are only *after* using both words in the same sentence, so the first clause meant nothing on first pass. | Move the read and adapter glosses ahead of the sentence that uses them. |
| What it is, "the plain-text format that holds sequencing reads along with a quality score for every base" | "Quality score" is never explained here, and it comes back later as the number I am supposed to set. I did not know what is being scored or by what. | Gloss quality score at first use as the instrument's own estimate of the chance a base is wrong. |
| What it is, "The node-type model in the source does define eleven further types" | "The source" is unexplained. I do not know whether that means the app's code, a file on my Mac, or a document I should look at. | Say "the app's code" instead of "the source". |
| What it is, "a finished run made with them can still be turned into a shareable pipeline" | "Pipeline" arrives with no gloss and is not in the glossary links for this chapter. I could not tell whether a pipeline and a chain are the same thing. | Gloss pipeline at first use, and say whether it differs from a chain. |
| Why you would do this, "reads shorter than 50 bases are not worth keeping" | I could not judge 50. Nothing yet says why short reads are a problem, so the number was arbitrary until the Settings section explained it much later. | Give the one-line reason next to the first appearance of 50, not only in Settings. |
| Why you would do this, "This chapter builds its chain over the HG002 mitochondrial reads" | HG002 is used as if I should recognise it. I do not. I also do not know what makes mitochondrial reads a special case here. | Say in one clause what HG002 is, for example a widely used reference human sample. |
| Before you start, "click the green Code button, choose Download ZIP" | I could follow this, but nothing told me how big the download is or that I am downloading the entire repository to get two files. I worried I had done the wrong thing. | Say the ZIP is the whole repository and only two files inside it are needed. |
| Before you start, "grouped into a single bundle named `HG002.chrM.lungfishfastq`" | The two files are named `_R1` and `_R2` and the bundle is named neither. I could not tell whether LGE renames anything or whether I must type this name. | Say the name is derived automatically and needs no typing. |
| Before you start, "that bundle held 19,916 reads, which is 9,958 pairs, with a mean read length of 248.4 bases" | Three numbers with no way to judge them. I do not know whether 19,916 reads is a lot, or whether 248.4 bases is normal. | Add one clause saying this is a deliberately tiny sample so the chapter runs in seconds. |
| Before you start, "Deacon also needs its human index, which the Plugin Manager lists under Databases as the Deacon panhuman database" | Step could not be performed. It says Deacon needs the index and where it is listed, but never tells me to install it or how. I did not know whether it was already there. | Give the click path to install it, or say it installs with the Required Setup pack. |
| Before you start, "index" | "Index" is used with no gloss. In genetics class an index was a barcode on a sample, so I first read this wrong. | Gloss index here as a prebuilt searchable copy of the human genome. |
| Procedure lead-in, "using fastp 1.3.6, Deacon 0.16.0, and seqkit 2.13.0" | I do not know what any of these three programs are at this point. The glossary links exist earlier but the chapter never says in prose what each one does. | Add one clause each, for example fastp trims, Deacon removes host reads, seqkit filters. |
| 1. Open the window, "the node inspector is on the right" | "Inspector" is used before it is explained. I guessed from context but had to read on to confirm. | Say once that the inspector shows the settings of whatever node is selected. |
| 1. Open the window, "whatever an older version of this chapter said" | Read twice. I do not have an older version of the chapter, so this aside confused me about which instruction was current. | Delete the aside, or say plainly that there is no save command. |
| 1. Open the window, "It is a legacy path that asks you to pick a sample at run time." | "Legacy path" is jargon. I could not tell whether Sample input is broken, deprecated, or simply a different style. | Say it is an older way of doing the same thing that this chapter does not use. |
| 2. Read the palette, "the data type of each input and output port" | "Data type" is used before the gloss that follows it in the next sentence, and the gloss itself, "what kind of thing may flow through it", left me no clearer on what the kinds are. | Give one concrete example, for example a Reads port carries a FASTQ bundle. |
| 2. Read the palette, "Remove PCR duplicates" | PCR duplicate is never explained in prose. I know what PCR is from class but not what a duplicate read is or why removing one helps. | Gloss PCR duplicate in prose as copies of the same original fragment made during library prep. |
| 2. Read the palette, "wiring single-end reads into it produces a step that finds no overlaps" | "Single-end" appears here for the first time with no gloss, though paired-end was glossed earlier. | Gloss single-end as reading each fragment from one end only. |
| 2. Read the palette, "The Trimming node is not a bare shell, since it holds a minimum length defaulting to 20 and a qualified quality defaulting to 15" | This whole paragraph is about nodes I am told I cannot place. I read it twice trying to work out whether I needed to act on it, and concluded I did not. | Say up front that this paragraph is background only and needs no action. |
| 2. Read the palette, "a saved graph containing an Alignment node holds no minimap2 preset and no allele-frequency cutoff to read" | Three unexplained terms in one clause, minimap2, preset, allele-frequency cutoff. None mean anything to me. | Cut the example, or replace it with a plain statement that those nodes carry no scientific settings. |
| 3. Place the nodes, "press `Delete` or forward delete" | I did not know what forward delete is on a Mac laptop keyboard, which has one Delete key. | Name the key combination, for example fn plus Delete. |
| 3. Place the nodes, "so the cheap steps run first and the expensive human-read comparison sees fewer reads" | "Cheap" and "expensive" are used about steps without saying they mean computer time. I first read it as money. | Say the words mean how long each step takes to compute. |
| 3. Place the nodes, "Nudge the selection one grid step at a time" | "Grid step" is used before the Grid toggle is introduced two sentences later. | Introduce the grid before referring to a grid step. |
| 4. Connect them, "A node whose output fans out to two downstream nodes is legal in the editor and legal for export, and refused when you run it." | Read three times. The sentence says a thing is allowed and not allowed at once, and "fans out", "editor", and "export" all arrive together. | Split into two sentences and say the editor lets you draw it but the runner rejects it. |
| 4. Connect them, "Error: Workflow Builder runner requires a single linear FASTQ chain: Node 'FASTQ bundle input' must have exactly one outgoing connection." | The error is shown but the chapter never says what to do about it. I would not know that I must delete one of the two lines. | Add one sentence saying to delete the extra connection. |
| 4. Connect them, "a reference output may feed an assembly input and an assembly output may feed a reference input, because those two are interchangeable further downstream" | I do not know what a reference or an assembly is in this app, and these are node types I was told I cannot place. The sentence did not land at all. | Cut it, or move it to a note for readers opening files from other tools. |
| 4. Connect them, "A port typed `Any`" | I could not tell whether `Any` is something I type, something I see on screen, or a name used only in the code. | Say whether the word appears anywhere in the window. |
| 5. Set the parameters, "the project-relative form `@/Imports/HG002.chrM.lungfishfastq`" | The `@/` symbol is never explained. I guessed it stands for the project folder but the chapter only says so indirectly, later, in Settings. | Say at first use that `@/` means the project folder. |
| 5. Set the parameters, "Every other node's numbers are already at the values this chapter uses" | Reassuring, but it contradicts my reading of Settings, where Maximum length "arrives unset". I could not tell whether unset counts as a value this chapter uses. | Say explicitly that unset is the value used for Maximum length. |
| 5. Set the parameters, "The Settings section below documents all thirteen controls." | The chapter has told me there are six nodes with ten node settings, plus three window controls. I counted the Settings entries and found twelve headings plus a note on Remove PCR duplicates, which has no settings. The arithmetic did not resolve for me. | State the count once, and make the Settings headings match it exactly. |
| 5. Set the parameters, "the shared FASTQ/FASTA Operations dialog with an Apply button" | I have never seen this dialog and the chapter does not say what it looks like or link to where it is described. | Link to the chapter that covers that dialog. |
| 6. Run it, "any node parameter it does not recognise or that is required and missing" | Read twice. I could not work out how a parameter I never typed could be unrecognised. | Say this happens with files written by another version, not by your own editing. |
| 6. Run it, "A project opened read-only refuses the run" | I did not know a project could be opened read-only, or how I would end up in that state. | Say in one clause what makes a project read-only. |
| 6. Run it, "both carrying the same run identifier" | "Run identifier" appears here before it is explained in Reading the results as "a long unique string". | Gloss it at first use. |
| Settings, "The default is 15, which means the machine estimates roughly a 3 in 100 chance that a base at the cutoff is wrong, where 20 would mean roughly 1 in 100." | This was the first place quality scores were explained, four sections after they were first used. I also could not check the arithmetic, since 15 and 20 differ by 5 but 3 in 100 and 1 in 100 differ by 3. | Move the explanation to the first mention, and state the scale rather than two sampled points. |
| Settings, "it accepts whole numbers from 0 to 93" | I cannot judge 93. Nothing says what the top of the scale means or that nobody ever goes near it. | Add a clause saying values above 40 do not occur in practice on this data. |
| Settings, "Cut mode ... offers exactly four values, `right`, `front`, `tail`, and `both`" | I could not tell `right` from `tail`. The next sentence says both trim from the same end, which made me think one of them is redundant, and I could not tell which to pick. | Say in one clause what distinguishes right from tail. |
| Settings, "the 3-prime end" | Prime notation was in my genetics course but the chapter has glossed much easier terms and skipped this one. | Gloss 3-prime as one specific end of a DNA strand. |
| Settings, "Minimum overlap ... The default is 15" | I could not judge 15 against the 248.4 base read length given earlier. I did not know whether 15 shared bases is a lot or a little. | Relate the default to the read length used in this chapter. |
| Settings, "Maximum length ... Set it when a fixed-length protocol has produced over-long reads that point at joined-together fragments" | "Fixed-length protocol" and "joined-together fragments" are both new. I could not tell when this situation would apply to me. | Say plainly that two fragments stuck together during library prep read as one long read. |
| Settings, `--threads`, "Sets how many processor cores the tools in the chain may use at once" | This is the only Settings entry that assumes I know what a processor core is, and it is in the command-line group I was told is optional. | Gloss core, or move the note about "leaves most of a modern Mac free" up front. |
| Reading the results, "each run gets its own folder at `runs/<run-id>/` inside that bundle" | I did not know how to look inside a `.lungfishflow` bundle. In Finder it presumably opens rather than reveals contents. | Say to right-click and choose Show Package Contents. |
| Reading the results, "A `workspace` folder holds the intermediate files each step handed to the next." | "Intermediate files" was not explained and I could not tell whether I should keep or delete them. | Say whether they are safe to delete. |
| Reading the results, "it carries a lineage of five entries ... keyed `deduplicate`, `fastpTrim`, `humanReadScrub`, `pairedEndMerge`, and `lengthFilter`" | The five keys are spelled differently from the five node names I placed. I had to map them by hand and was unsure about `humanReadScrub`. | Pair each key with its node name in a short table. |
| What the reference run did, table row "Remove PCR duplicates + Adapter + quality trim ... 19,916 ... 9,876" | Half my reads vanished in the first row and the chapter never comments on it, though it comments at length on rows two and three. I could not tell whether losing half is expected. | Say in one sentence whether a drop of this size at the first step is normal. |
| What the reference run did, "a single command carrying both `--dedup` and `--cut_right`" | These flags appear with no explanation. I have never typed a command and did not know these were parts of one. | Say they are the two options fastp received, one per logical step. |
| What the reference run did, "The final bundle held 106 reads over 30,498 bases, a mean read length of 287.7 against the input's 248.4" | I started with 19,916 reads and finished with 106. The chapter explains each step but never says whether 106 reads is a usable result or whether I should now change the chain. | Say plainly whether the reference run's output is usable or is a demonstration only. |
| When a run fails, "leaves every unfinished node in the `skipped` state" | I could not tell where I would see these states. The chapter says the run record stores them as text but not whether the window shows them. | Say where per-node status is visible. |
| Comparing two versions, "Every saved chain carries a version such as `1.0.0`" | The chapter never says how I change the version, yet the diff example shows it changing from `1.0.0` to `1.1.0`. Step could not be performed. | Say where the version is set, or that it is bumped automatically. |
| Comparing two versions, "a defect in 2026.9.13 makes all three print the text form, so a script cannot yet parse the comparison" | "Parse" and "script" assume terminal knowledge in a section that is not marked optional. The optional label only arrives in the next section. | Move this note into the On the command line section. |
| What good looks like, "The reference run's fused fastp call read `-q 15 -W 5 --cut_right` and its seqkit call read `-m 50`" | Step could not be performed. I am told to check that these match the inspector, but not where in the window or the files I would read the command back. | Say which file the command line is recorded in. |
| What good looks like, "Open the `workflow.json` and confirm the input node's `bundle_path` begins `@/`" | Step could not be performed. Nothing tells me how to open a JSON file or where inside the bundle it sits. | Give the path and say to open it in TextEdit. |
| What good looks like, "Run it with `--dry-run` and read the plan" | This is in the non-optional part of the chapter but requires the terminal, which the next section says is optional. The two claims conflict. | Give the window equivalent, or point to the optional section. |
| On the command line, "The whole procedure runs headless, meaning with no window at all, by typing commands into the Terminal application." | I have never opened Terminal. The section does not say how to open it or where to type, so I stopped here. | Add one sentence saying how to open Terminal and that the chapter's commands are typed at its prompt. |
| On the command line, "`lungfish-cli workflow validate` ... exits with status 5" | "Exit status" means nothing to me and the number 5 could not be judged. | Say the command simply refuses and prints the message shown. |
| On the command line, code block with `\` at line ends | I did not know the backslash continues one command across lines, and I could not tell whether to type it. | Say the backslashes let one command span several lines. |
| On the command line, "--workflow \"Mito read cleanup.lungfishflow\"" | I could not tell where I would be standing in the filesystem for these relative paths to work. | Say the commands assume the Terminal is in the project's parent folder. |
| On the command line, "an argv beginning `lungfish-cli workflow builder-step run --operation ...`" | "argv" is unexplained and is the most opaque word in the chapter. | Replace argv with "argument list", which the chapter already uses one paragraph earlier. |
