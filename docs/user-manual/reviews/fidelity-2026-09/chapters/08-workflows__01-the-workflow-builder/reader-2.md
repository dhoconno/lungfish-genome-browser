# Reader 2 report, The Workflow Builder

Persona. A senior who has spent two years pipetting in a wet lab. I know
library prep and PCR. I have never analyzed data and have never opened a
terminal.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "The Workflow Builder is a window" | The chapter never says what problem this window solves before it starts naming its parts. I got a definition of the thing before I got a reason to care. | Open with the one-sentence version of the "Why you would do this" paragraph about not repeating five dialogs by hand. |
| What it is, "such as trimming adapters or dropping short reads" | I know what an adapter is at the bench, a sequence we ligate on. I do not know what "trimming" it means once the data exist, or why it needs removing if we put it there on purpose. | Add half a sentence saying the adapter is not part of the organism's DNA, so it has to be cut off the sequence before analysis. |
| What it is, "directed acyclic graph" | Three unfamiliar words at once. The gloss that follows is clear, but I had to read the sentence twice to attach the gloss to the term. | Put the plain description first and the term second, as "a set of boxes joined by one-way arrows, which is called a directed acyclic graph". |
| What it is, "The node-type model in the source does define eleven further types" | "The source" means nothing to me. I do not know what source is being referred to, and it sounds like something I should already have read. | Say "LGE's internal file format" instead of "the source". |
| What it is, "and a saved file can carry them, but the palette never offers them, so you cannot place one" | This is a paragraph about things I cannot do, placed before I have done anything. It felt like a warning about a mistake I was not in danger of making. | Move this sentence and its expansion in step 2 into one place later in the chapter. |
| Why you would do this, "which is why provenance exists" | Provenance is linked but not glossed in the sentence. Every other hard word in this chapter gets a short definition inline. This one does not. | Add the inline gloss, "a record of what was run and with what settings". |
| Why you would do this, "reads shorter than 50 bases are not worth keeping" | I cannot judge 50. Is that a lot of a 250-base read? The Settings section explains it much later, but at this point 50 is a bare number. | Add "which is short compared with the roughly 250-base reads in this chapter". |
| Why you would do this, "A chain binds its input by a path relative to the project" | "Path relative to the project" assumes I know what a path is. I have never typed a file location. | Gloss as "it records where the file sits inside the project folder rather than where it sits on your Mac". |
| Before you start, "click the green Code button, choose Download ZIP" | I followed this and it downloads the whole repository, which is large, to get two small files. Nothing warns me that the download is big or that I can throw the rest away. | Add "the download holds the whole project, and you only need the two files below". |
| Before you start, "19,916 reads, which is 9,958 pairs, with a mean read length of 248.4 bases" | I can divide, but I do not know whether these numbers are big, small, or typical. Nothing tells me why I am being given them. | Say they are given so I can check my import matches the reference run. |
| Before you start, "This feature is experimental" | The chapter tells me the warnings exist but never tells me whether I should be worried. I am about to spend an hour on something the app says is not for production scientific work. | Add one sentence saying what is safe to do with it, such as practise and small runs, and what is not. |
| Before you start, "Deacon also needs its human index, which the Plugin Manager lists under Databases" | This is the only step in the chapter I could not perform. I am told the index is needed and where it is listed, but not how to install it or how to tell whether it already is. | Give the click path, and say what the Plugin Manager shows when the database is already present. |
| Before you start, "index" | Never explained. I picture a book index. I do not know what a human index is or why removing human reads needs one. | Gloss as "a prepared copy of the human genome the tool searches against". |
| Procedure, "using fastp 1.3.6, Deacon 0.16.0, and seqkit 2.13.0" | Three tool names, none introduced before this line as tools I will be running. The glossary links exist in the earlier paragraph, but nothing said "these are the three programs that do the actual work". | Add one sentence naming the three and saying which node uses each. |
| 1. Open the window, "the node inspector is on the right" | "Inspector" is used from here to the end of the chapter as if I knew it. I do not. | Gloss at first use as "a panel showing the settings of whichever node you clicked". |
| 1. Open the window, "It is a legacy path that asks you to pick a sample at run time" | "Legacy path" stopped me. I do not know whether legacy means broken, old but fine, or about to be removed, and the chapter tells me to leave it alone without saying why. | Replace with "an older way of choosing the input that this chapter does not use". |
| 2. Read the palette, "the data type of each input and output port" | "Data type" is not glossed, only "port" is. I read the sentence twice trying to work out whether data type meant FASTQ versus FASTA or something else. | Gloss data type as "the kind of file that may flow through, such as a FASTQ bundle". |
| 2. Read the palette, palette table | The table lists a different output-port name per row, Deduplicated, Trimmed, Filtered, Scrubbed, Merged. I could not tell whether those names matter for wiring or are just labels. | One line saying every one of these is a FASTQ bundle and the names are descriptive only. |
| 2. Read the palette, "wiring single-end reads into it produces a step that finds no overlaps" | "Single-end" is used without gloss, although paired-end is glossed earlier. I inferred it, but only from the contrast. | Gloss single-end at this use. |
| 2. Read the palette, "The Trimming node is not a bare shell, since it holds a minimum length defaulting to 20 and a qualified quality defaulting to 15" | This is about a node I was just told I can never place. I read it twice trying to find why it mattered to me. "Qualified quality" is also two words I do not know. | Cut or move to an appendix, and gloss qualified quality if it stays. |
| 3. Place the nodes, "the cheap steps run first and the expensive human-read comparison sees fewer reads" | Cheap and expensive are not defined. I assume computer time, not money, but I had to guess. | Say "faster steps run first so the slow human comparison has less to do". |
| 3. Place the nodes, "Nudge the selection one grid step at a time with the arrow keys" | Grid step is not defined and the Grid toggle has not yet been introduced at this point in the chapter. | Move the grid toggle sentence ahead of the nudge sentence. |
| 4. Connect them, "A node whose output fans out to two downstream nodes is legal in the editor and legal for export, and refused when you run it" | I read this three times. It says the app lets me draw something it will then refuse. I could not tell whether I was being warned off a thing I might do by accident or told about a bug. | Say plainly that the editor allows branching but the runner does not, so keep it a single line. |
| 4. Connect them, "Two exceptions exist for node types outside the palette, where a reference output may feed an assembly input" | Reference, assembly, and downstream are all unexplained here, and again this describes nodes I cannot place. | Cut. It cannot affect anything I do in this chapter. |
| 4. Connect them, "A port typed `Any`" | Backticked `Any` looks like something I should type. It is a data type name. | Say "a port whose data type is Any". |
| 5. Set the parameters, "the project-relative form `@/Imports/HG002.chrM.lungfishfastq`" | The `@/` symbol is never explained. It reappears four more times and in the checks at the end, where I am asked to confirm a path "begins `@/`" without knowing what it stands for. | Say once that `@/` stands for the project folder. |
| 5. Set the parameters, "The Settings section below documents all thirteen controls." | I was told the defaults are already right, then pointed at thirteen entries. I could not tell whether I was expected to read them now or later. | Say to run first and read the Settings section only when changing something. |
| 5. Set the parameters, "opens the shared FASTQ/FASTA Operations dialog with an Apply button" | FASTA appears here for the first time and is never explained. I only know FASTQ from this chapter. | Gloss FASTA, or drop it from the dialog name. |
| 6. Run it, "any node parameter it does not recognise or that is required and missing" | Since I changed no parameters, I could not tell how this failure could ever happen to me, so I could not tell whether to worry. | Say it happens with files written by an older version. |
| 6. Run it, "A project opened read-only refuses the run" | Read-only is not explained and I do not know how a project becomes read-only or how I would notice. | Say what makes a project read-only. |
| Settings, "Quality threshold ... roughly a 3 in 100 chance that a base at the cutoff is wrong, where 20 would mean roughly 1 in 100" | This is the clearest number explanation in the chapter, and it arrives only in the Settings section. Every earlier number lacked one. | Keep it, and add the same kind of gloss to the earlier bare numbers. |
| Settings, "Window size ... a larger window smooths over a single bad base and a smaller one reacts to it" | I could not work out which behaviour is desirable. Both are stated neutrally, then I am told to change it rarely. | Say which direction is a risk, and why 5 sits in the middle. |
| Settings, "Cut mode ... quality on an Illumina run usually falls off at the 3-prime end and that is the end right and tail trim from" | Long sentence, read twice. I know 3-prime from class, but "the end right and tail trim from" is hard to parse. | Split into two sentences. |
| Settings, "Minimum overlap ... chance matches rarely reach it" | I do not know what a chance match is here, or why merging on one would be bad. | Say a wrong merge fuses two reads that were never really overlapping. |
| Settings, "Maximum length ... over-long reads that point at joined-together fragments rather than at real molecules" | Chimeras, presumably, but the phrase is vague and I could not tell how to spot the situation that calls for setting this. | Name the artefact and say what makes you suspect it. |
| Settings, "None of the window's thirteen controls has a command-line flag" | The same sentence, "This setting has no command-line flag", then closes all thirteen entries. After the third one I stopped reading the closing sentence entirely. | State it once at the top of the section and drop it from the entries. |
| Settings, "`--threads`. Sets how many processor cores" | This section is command-line only, which I was told at the top I could skip, but it is inside Settings, which I was told to read. I could not tell whether it applied to me. | Move the five command-line options into the On the command line section. |
| Reading the results, "the run identifier is a long unique string" | I do not know where to find a run identifier or how to tell two apart. | Point to where it appears on screen. |
| Reading the results, "it carries a lineage of five entries ... keyed `deduplicate`, `fastpTrim`, `humanReadScrub`, `pairedEndMerge`, and `lengthFilter`" | The keys do not match any node name I clicked. I could not map them back to my chain without guessing. | Show the node name beside each key. |
| Reading the results, counts table, "Merge overlapping pairs 159 -> 225" | More out than in stopped me completely. The explanation comes two paragraphs later, after I have already worried. | Put a short parenthetical in the table so the reader is not left alone with it. |
| Reading the results, "Deacon removed 9,717 of 9,876 reads, leaving 159" | This is the most useful passage in the chapter and it made me want to check my own chain. But it also means the example run destroys almost all the data, and I could not tell whether the final 106 reads are a usable result or a demonstration of a mistake. | Say outright that the reference chain is a demonstration and the 106 reads are not a scientific result. |
| Reading the results, "The final bundle held 106 reads over 30,498 bases, a mean read length of 287.7 against the input's 248.4" | Four numbers in one sentence, read twice. I got the point on the second pass. | Split, and say the point first. |
| Reading the results, "renames that into place only once the provenance has been written" | Staging folder, provenance file, and renaming are three mechanisms I do not need to picture. I read the paragraph twice to find what I should do about it, and the answer is nothing. | Reduce to one sentence saying an interrupted run leaves no half-finished bundle. |
| When a run fails, "leaves every unfinished node in the `skipped` state" | I am told the statuses but not where I read them, and the next paragraph then says a command-line run writes no such file. I could not tell what I would actually see. | Say where in the window the per-node status appears. |
| Comparing two versions, "Every saved chain carries a version such as `1.0.0`" | I was never told how the version changes or whether I control it. The example jumps from 1.0.0 to 1.1.0 with no explanation of who typed that. | Say whether the version increments on its own or you set it. |
| Comparing two versions, "a defect in 2026.9.13 makes all three print the text form" | I do not know what a defect means for me here, or whether it will be fixed. The advice that follows is to read JSON files directly, which I cannot do. | Note that this only affects scripted use. |
| What good looks like, "Then check that four numbers agree with what you set ... `-q 15 -W 5 --cut_right` ... `-m 50`" | This asks me to read a command line, which I have never done. I cannot tell which part of `-q 15 -W 5 --cut_right` is the quality threshold without guessing. | Label each flag with the setting it corresponds to. |
| What good looks like, "Open the `workflow.json` and confirm the input node's `bundle_path` begins `@/`" | I do not know how to open a workflow.json. It is inside a bundle, which the chapter earlier said LGE treats as one object. | Give the steps, or say the window shows the same thing. |
| What good looks like, "Error: FASTQ bundle input is outside the active project: /tmp/.../Outside.lungfishfastq" | The `/tmp/.../` with dots looks like a typo or a path I should complete. | Say the middle of the path was shortened for print. |
| On the command line, "The whole procedure runs headless, meaning with no window at all, by typing commands into the Terminal application" | Terminal is named as if I would know where to find it or what to do once it is open. The section says it is optional, which I took gratefully, but the chapter then relies on command-line output in Reading the results and What good looks like. | Either make the earlier sections window-only or say that the checks need the command line. |
| On the command line, "`lungfish-cli workflow list` lists the supported nf-core pipeline" | nf-core is never explained and appears once. | Gloss or cut. |
| On the command line, "pointing it at a builder graph exits with status 5" | Exit status means nothing to me. | Cut the number, or say it is a code scripts read. |
| On the command line, "an argv beginning `lungfish-cli workflow builder-step run`" | "argv" is not a word I have met. The paragraph then tells me it is not a command I can type, which left me unsure why it was shown. | Say "argument list", which the chapter itself uses earlier. |

The one thing I learned. A host-depletion step is only correct relative to
what you are looking for, and pointed at a human sample it will throw away
almost everything.

The one thing I still could not do. Install the Deacon panhuman database,
because the chapter names it and where it is listed but never gives the
steps.

The sentence I liked most. "A bundle with no parent recorded is a bundle
whose history stops at itself."
