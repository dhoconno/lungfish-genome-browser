# Fidelity review: 03-reads/01-importing-fastq.md

Reviewed 2026-09-06 against the Swift source in this worktree, `.build/debug/lungfish-cli` (read-only plus one `import fastq --dry-run`), the CLI help dumps under `reviews/fidelity-2026-09/cli-help/`, `parameters.yaml`, `GLOSSARY.md`, and the author's scratch import project at `/private/tmp/claude-501/.../scratchpad/fastq-import/`.

Every number the chapter quotes was rechecked against the scratch bundle's metadata sidecar and its provenance record rather than against the drift report. The pairing suffix table was rechecked twice, once by reading `groupFASTQByPairs` and once by running a real dry-run over a purpose-built folder of six files (`_R1_001`/`_R2_001`, `_r1`/`_r2`, `.R1`/`.R2`) that the author had already staged in the scratch directory.

## Claims

| # | Claim (quoted) | Verdict | Evidence | Corrected wording |
|---|---|---|---|---|
| 1 | Front matter `parameters_refs: [import.fastq, import.fastq-sample-sheet]` | true | Both ids exist in `docs/user-manual/parameters.yaml:1361` and `:1456`, and both are the operations this chapter documents. No third id is reachable from this chapter's procedures. | no change |
| 2 | "File > Import Center... (Cmd-Shift-I)" (front matter and step 1) | true | `MainMenu.swift:207-212` adds "Import Center…" to the File menu with `keyEquivalent: "i"` and `keyEquivalentModifierMask = [.command, .shift]`. | no change |
| 3 | "Sequencing Reads > Sequencing Read Files" | true | `ImportCenterViewModel.swift:175` names the tab "Sequencing Reads"; `:253` names the card "Sequencing Read Files" with `tab: .sequencingReads`. | no change |
| 4 | "Sequencing Reads > FASTQ Sample Sheet" | true | `ImportCenterViewModel.swift:276` gives the card the title "FASTQ Sample Sheet" on the same tab, accepting `.csv` only, `canChooseDirectories: false`. | no change |
| 5 | "Drag reads onto the sidebar" as an entry point | true | `MainSplitViewController+FASTQImport.swift:343` `presentFASTQImportSheet(pairs:projectDirectory:requestID:)` is the sidebar drop path and opens the same sheet as the Import Center path at `:385`. | no change |
| 6 | "a read bundle carries the extension `.lungfishfastq`" | true | `FASTQBatchImporter.swift:439` builds the bundle as `"\(pair.sampleName).\(FASTQBundle.directoryExtension)"`, and every bundle in the scratch project is a `.lungfishfastq` directory. | no change |
| 7 | "Inside it sit the read data, a metadata file holding statistics measured from the reads, and a `provenance/` folder" | true | The scratch bundle holds exactly `HG002.chr20.10.0-10.5Mb.fastq.gz`, `HG002.chr20.10.0-10.5Mb.fastq.gz.lungfish-meta.json`, and `provenance/` with three sidecars. | no change |
| 8 | "It computes a checksum, a short fingerprint calculated from a file's exact bytes" | true | The bundle provenance record carries `checksumSHA256` and `sha256` for both inputs and the output, for example the output's `b9ade271460f9f5262134ce348c7e813c85e0300bcc3acdefda314d1e7d9d41c`. | no change |
| 9 | "It also covers an unmapped Oxford Nanopore BAM, meaning a BAM file that holds raw reads rather than reads aligned to a reference." | true | `ImportCenterViewModel.swift:255` describes the card as accepting "an unmapped Oxford Nanopore BAM"; `:262` lists `bam` among the allowed types; `import.txt:188-189` argument help reads "FASTQ paths, or unmapped ONT BAM paths". | no change |
| 10 | "The two files hold 45,574 read pairs sliced from a 500 kb region of chromosome 20" | true | `docs/user-manual/fixtures/hg002-chr20/README.md` records "45,574 read pairs (91,148 reads, 182,296 lines per file)" over `chr20:10,000,000-10,500,000`, 500,001 bp. The scratch bundle's `readCount` of 91,148 is exactly twice 45,574. | no change |
| 11 | "small enough to import in about ten seconds" (and the same figure in Before you start and in the Procedure) | true | The scratch bundle provenance records `startTime` 2026-09-07T04:25:26Z and `endTime` 2026-09-07T04:25:35Z, so the real run took 9 seconds. | no change |
| 12 | "choose **File > New Project** (Cmd-N), or click Create Project on the Welcome window" | true | `MainMenu.swift:169-172` adds "New Project" with `keyEquivalent: "n"` and no extra modifier mask, so Cmd-N. The fixed sentence also matches CONSISTENCY.md's "Before you start, fixed sentences" block verbatim. | no change |
| 13 | "Download the files `HG002.chr20.10.0-10.5Mb_R1.fastq.gz` and `HG002.chr20.10.0-10.5Mb_R2.fastq.gz` from the manual's fixtures on GitHub at https://github.com/dhoconno/lungfish-genome-explorer/tree/main/docs/user-manual/fixtures/hg002-chr20" | true | Both filenames are the committed fixture files listed in `fixtures/hg002-chr20/README.md` under "Committed files" at 8.3 MB and 8.9 MB. The URL shape matches the fixed sentence in CONSISTENCY.md. | no change |
| 14 | Pairing table row "`_R1_001` and `_R2_001` / `Run01_R1_001.fastq.gz`, `Run01_R2_001.fastq.gz` / Illumina bcl2fastq and DRAGEN" | true | `FASTQImportConfiguration.swift:181-185` lists `("_R1_001", "_R2_001")` first in `suffixPairs`, and the doc comment above `groupFASTQByPairs` attributes it to "Illumina bcl2fastq/DRAGEN". The dry-run over the scratch `naming/` folder reported sample 3 as `Run01 [paired]` with `R1: Run01_R1_001.fastq.gz` and `R2: Run01_R2_001.fastq.gz`. | no change |
| 15 | Pairing table row "`_R1` and `_R2` / Common vendor style" | true | `FASTQImportConfiguration.swift:183` `("_R1", "_R2")`; the doc comment calls it "common shorthand". | no change |
| 16 | Pairing table row "`_1` and `_2` / `SRR123456_1.fastq.gz` / ENA and SRA downloads" | true | `FASTQImportConfiguration.swift:184` `("_1", "_2")`; the doc comment calls it the "SRA/ENA convention". | no change |
| 17 | "only three suffix pairs are recognised" | true | `suffixPairs` at `FASTQImportConfiguration.swift:181-185` holds exactly three tuples. The `.1`/`.2` pair named in the doc comment and stripped by `FASTQFilePair.sampleName:140` is not in `suffixPairs`, so it never pairs. | no change |
| 18 | "The match is case-sensitive, so use the uppercase `_R1` and `_R2` forms. A file named `Sample_r1.fastq.gz` does not pair with `Sample_r2.fastq.gz`." | true | `stem(of:)` at `FASTQImportConfiguration.swift:188-195` lowercases only the `.bam` test; the suffix comparisons at `:209` and `:219` call `hasSuffix` on the raw stem. Confirmed empirically: the dry-run reported `Lower_r1 [single-end]` and `Lower_r2 [single-end]` as two separate samples. | no change |
| 19 | "Neither does a dot before the suffix, so `Sample.R1.fastq.gz` and `Sample.R2.fastq.gz` import as two separate single-end bundles rather than one pair." | true | Confirmed empirically: the same dry-run reported `Sample.R1 [single-end]` and `Sample.R2 [single-end]`. `fixtures/README.md` independently records the same rule under "Read file naming". | no change |
| 20 | "A file whose mate is missing is imported as single-end ... and no warning appears." | true | `FASTQImportConfiguration.swift:232-235` appends the unmatched file as `FASTQFilePair(r1: url, r2: nil)` with no warning branch. `FASTQImportConfigSheet.swift:598-613` builds the summary from counts only. | no change |
| 21 | "The configuration sheet counts paired and single-end samples in its summary" | true | `FASTQImportConfigSheet.swift:604-612` emits "N samples detected", then "  N paired-end" and "  N single-end" when each count is above zero, then "Total size:". | no change |
| 22 | "a sample sheet ... overrides filename matching entirely" | true | `FASTQSampleSheet.swift:71-74` builds each `SamplePair` from the sheet's own `sample`, `r1`, and `r2` values, so `groupFASTQByPairs` is never consulted on that path. | no change |
| 23 | "Both `.fastq` and `.fastq.gz` are accepted, and a folder of reads may hold both." | true | `stem(of:)` at `FASTQImportConfiguration.swift:190-192` strips `.gz`, then `.fastq` or `.fq`, so the compression suffix is removed before matching. `ImportCenterViewModel.swift:259-264` allows `gz`, `fastq`, `fq`, `bam`, and folders. | no change |
| 24 | "The Import Center opens as a tabbed grid of cards, each card a drop target for one kind of data." | true | `ImportCenterViewModel.swift:165-208` defines the tab enum and `selectedTab`, and each `ImportCardInfo` carries an `importKind` open-panel configuration. The wording matches the fixed phrasing in CONSISTENCY.md's "Menu paths and surfaces". | no change |
| 25 | "Its cards cover the three ways reads arrive, as loose read files, as a sample sheet, or as an Oxford Nanopore run folder." | true | `ImportCenterViewModel.swift:253`, `:276`, and `:293` are the only three cards with `tab: .sequencingReads`. | no change |
| 26 | "The card accepts folders as well as files" | true | `ImportCenterViewModel.swift:264-270` sets `canChooseFiles: true`, `canChooseDirectories: true`, `allowsMultipleSelection: true`. | no change |
| 27 | "For these two files it reads `R1:` on one line, `R2:` on the next, and the total size below them." | true | `FASTQImportConfigSheet.swift:601` builds exactly `"R1: \(…)\nR2: \(…)\nTotal size: \(…)"` for a single paired sample. | no change |
| 28 | "A sample that did not pair shows one `R1:` line and no `R2:` line." | false | `FASTQImportConfigSheet.swift:603` takes the unpaired single-sample branch and prints the bare filename with no `R1:` prefix at all, followed by `Size: …` rather than `Total size: …`. There is no `R1:` label on that path. | "A sample that did not pair shows its filename on one line with the size beneath it, and no `R1:` or `R2:` labels at all." |
| 29 | "There is no sample-name field, because the name comes from the shared filename stem, here `HG002.chr20.10.0-10.5Mb`." | true | The sheet's controls at `FASTQImportConfigSheet.swift:112-137` include no text field for a name. `FASTQFilePair.sampleName:130-147` derives it by stripping the compression and mate suffixes. The scratch bundle is named `HG002.chr20.10.0-10.5Mb.lungfishfastq`. | no change |
| 30 | "Platform should read Illumina for this fixture, and Pairing should read Paired-end." | true | `FASTQImportConfigSheet.swift:277-279` selects the detected platform, and the scratch bundle's `sequencingPlatform` is `illumina`. `:289-290` selects index 1 (Paired-end) whenever any pair has a mate. | no change |
| 31 | "**Operations > Show Operations Panel** (Cmd-Shift-P)" | true | `MainMenu.swift:866-873` adds "Show Operations Panel" with `keyEquivalent: "p"` and `[.command, .shift]`. Matches CONSISTENCY.md. | no change |
| 32 | "The import runs two steps, one that repackages the reads and one that measures them" | true | The scratch bundle's provenance holds three step records, of which the two that invoke a tool are `clumpify.sh` (the repackage) and `seqkit stats -a -T` (the measure). The third is the `lungfish-cli import fastq` wrapper for the run itself, not a third piece of work. | no change |
| 33 | "a bundle named `HG002.chr20.10.0-10.5Mb` appears under `Imports/` in the sidebar" | true | `FASTQBatchImporter.swift:434-440` builds every bundle path as `<project>/Imports/[<relative>]/<sampleName>.lungfishfastq`. The scratch project holds it at exactly that path. Matches CONSISTENCY.md's folder rule. | no change |
| 34 | "Select the folder in step 3 rather than the individual files, and LGE matches mates across everything inside it and creates one bundle per sample." | true | `groupFASTQByPairs` matches across the whole URL list it is handed, and `FASTQBatchImporter.swift:434-440` creates one bundle per resolved pair. The dry-run over the scratch `naming/` folder produced five separate samples from six files. | no change |
| 35 | "A BAM input is always treated as single-end, because a BAM carries no `_R1` and `_R2` filenames to match on." | true | `stem(of:)` at `FASTQImportConfiguration.swift:193` strips `.bam` but leaves nothing for the suffix comparison to catch, so a `.bam` never enters a pair. | no change |
| 36 | "You can still change the Platform popup" for a BAM input | true | `FASTQImportConfigSheet.swift:265-273` adds all seven platform titles unconditionally, and nothing in the sheet keys the popup to the input's file extension. | no change |
| 37 | Settings, **Platform**, defaults to "the platform detected from the read headers" and offers seven values | true | `FASTQImportConfigSheet.swift:265-273` lists Illumina, Oxford Nanopore, PacBio, Element Biosciences, Ultima Genomics, MGI / DNBSEQ, Unknown / Other in that order; `:277-279` selects the item matching `detectedPlatform`. Label "Platform:" at `:114`. Matches the registry entry. | no change |
| 38 | "On the command line this is `--platform`, which accepts four values rather than seven, `illumina`, `ont`, `pacbio`, and `ultima`." | true | `import-fastq.txt`: "--platform <platform>   Sequencing platform: illumina, ont, pacbio, ultima (default: auto-detect)". | no change |
| 39 | Settings, **Pairing**, offers Single-end, Paired-end, Interleaved and defaults to Paired-end when a mate was matched | true | `FASTQImportConfigSheet.swift:288-290` adds those three titles and selects index 1 when `pairs.contains { $0.isPaired }`, else index 0. Label "Pairing:" at `:116`. | no change |
| 40 | "choosing Oxford Nanopore hides the control entirely and treats the reads as single-end" | true | `FASTQImportConfigSheet.swift:714-723` sets `pairingLabel.isHidden` and `pairingPopup.isHidden` when the selected platform is `.oxfordNanopore`, and forces `selectItem(at: 0)`. | no change |
| 41 | "This setting has no command-line flag." (Pairing) | true | No pairing flag appears in `import-fastq.txt` or `import.txt`'s `import fastq` banner. Matches the registry's `cli_flag: null`. Phrasing matches CONSISTENCY.md's fixed sentence. | no change |
| 42 | Settings, **Quality Binning**, offers Illumina 4-level, 8-level, None (preserve original) | true | `FASTQImportConfigSheet.swift:296` adds exactly those three titles. Label "Quality Binning:" at `:118`. | no change |
| 43 | "It defaults to Illumina 4-level for Illumina, Element Biosciences, and MGI / DNBSEQ and to None for the other four platforms" | true | `defaultBinningIndex(for:)` at `FASTQImportConfigSheet.swift:619-624` returns 0 for `.illumina`, `.element`, `.mgi` and 2 for `.oxfordNanopore`, `.pacbio`, `.ultima`, `.unknown`. Four platforms take None. | no change |
| 44 | "On the command line this is `--quality-binning`." | true | `import-fastq.txt`: "--quality-binning … illumina4, eightLevel, none (default: illumina4)". | no change |
| 45 | Settings, **Optimize storage (reorder reads for better compression)**, exact label | true | `FASTQImportConfigSheet.swift:121` gives the checkbox that exact title. | no change |
| 46 | "It starts on for Illumina, Element Biosciences, MGI / DNBSEQ, and Ultima Genomics and off for Oxford Nanopore, PacBio, and Unknown / Other." | true | `defaultOptimizeStorage(for:)` at `FASTQImportConfigSheet.swift:626-633` returns true for `.illumina`, `.element`, `.mgi`, `.ultima` and false for `.oxfordNanopore`, `.pacbio`, `.unknown`; `:305` applies it as the initial state. | no change |
| 47 | "On the command line this is `--no-optimize-storage`." | true | `import-fastq.txt`: "--no-optimize-storage   Skip read reordering for storage optimization". | no change |
| 48 | Settings, **Compression Tool**, offering "BBTools clumpify and Trim Galore --clumpify" | true | Label "Compression Tool:" at `FASTQImportConfigSheet.swift:122`; `clumpingToolChoices` at `:635-637` is `[.bbtools, .trimGalore]`; `ClumpingTool.displayName` at `ClumpingTool.swift:20-23` returns "BBTools clumpify" and "Trim Galore --clumpify". | no change |
| 49 | "It is resolved from the total input size against the machine's memory, so the sheet chooses for you unless you say otherwise." | true | `defaultClumpingTool()` at `FASTQImportConfigSheet.swift:639-641` calls `ClumpingTool.auto.resolve(estimatedInputBytes: totalInputSizeBytes())`, and `totalInputSizeBytes()` at `:643-645` sums every pair's bytes. | no change |
| 50 | "Pick Trim Galore only when you want its adapter and quality trimming as part of the import, because it changes the reads themselves and not only their order." | true | `ClumpingTool.importSheetDisclosure` at `ClumpingTool.swift:30-34` is the on-screen line "Trim Galore also performs adapter detection/removal, quality trimming, and short-read filtering." for that tool alone. | no change |
| 51 | "On the command line this is `--clumping-tool`." | true | `import-fastq.txt`: "--clumping-tool … Storage optimization tool: auto, bbtools, trim-galore, none (default: platform-specific)". | no change |
| 52 | Settings, **Compression Level**, offering Fast, Balanced, Maximum and defaulting to Balanced | true | Label "Compression Level:" at `FASTQImportConfigSheet.swift:125`; `:349-350` adds the three titles and calls `selectItem(at: 1)`. `import-fastq.txt` confirms `--compression` defaults to `balanced`. | no change |
| 53 | Settings, **Apply processing recipe after import**, exact label, off by default | true | `FASTQImportConfigSheet.swift:127` gives the checkbox that title; `:366` sets `recipeCheckbox.state = .off`. | no change |
| 54 | Settings, **(recipe picker)**, "It appears only once the checkbox above is on, and it starts on the first recipe in the list." | true | `updateRecipeControls()` hides the picker and its description while the checkbox is off, and `:369-372` adds the options in `recipeOptions` order with no explicit selection, so index 0 stands. Matches the registry default "the first recipe in the list". | no change |
| 55 | "the description beneath it names the steps that workflow performs" | true | `recipeDescLabel` at `FASTQImportConfigSheet.swift:129` is a wrapping label under the picker, refreshed by `updateRecipeControls()` alongside the selection. | no change |
| 56 | "A plain FASTQ import offers three bundled recipes, VSP2 Target Enrichment, Wastewater metagenomics, and Illumina Amplicon Merge" | true | `Sources/LungfishWorkflow/Resources/Recipes/` holds exactly three files whose `name` fields are "VSP2 Target Enrichment", "Wastewater metagenomics", and "Illumina Amplicon Merge". The plain path loads `RecipeRegistryV2.allRecipes()` at `FASTQImportConfigSheet.swift:160`. | no change |
| 57 | "All three are Illumina workflows." | true | All three recipe JSON files declare `"platforms": ["illumina"]`. | no change |
| 58 | "The two Oxford Nanopore demultiplexing recipes ... appear only when you import an Oxford Nanopore run folder, and each needs a Barcode Sheet and a Demux Folder name" | true | `MainSplitViewController+FASTQImport.swift:726` passes `recipeOptions: [.ontFluidigmSampleSplit, .ontPacBioBarcodeDemux]` only on the run-folder path. Both carry `requiresBarcodeDefinition: true` and `usesDemultiplexOutputFolder: true` at `FASTQImportConfigSheet.swift:65-87`, and the controls are labelled "Barcode Sheet:" at `:131` and "Demux Folder:" at `:136`. | no change |
| 59 | "The FASTQ Sample Sheet card itself has no settings beyond the file panel that accepts the CSV." | true | `ImportCenterViewModel.swift:276-289` gives that card an open-panel configuration and nothing else, and the registry's `import.fastq-sample-sheet` has `settings: []`. | no change |
| 60 | "The top pane holds one summary bar and one sparkline strip computed over the whole bundle, not a row per file." | true | `FASTQChartViews.swift:28-41` builds the card bar from one `FASTQDatasetStatistics`, and `FASTQSparklineStrip.swift:18-31` holds a fixed three-case strip. Neither is keyed to a file. | no change |
| 61 | "The summary bar carries nine cards. Reads and Bases ... Mean Length and Median Length and N50 ... Mean Q and Q20 and Q30 ... and GC" | true | `FASTQChartViews.swift:31-39` returns exactly those nine `Card` values in that order. | no change |
| 62 | "All nine are measured from the reads and none is editable." | true | Every card reads from `statistics`, a computed struct, and no card exposes a control. The scratch bundle's `computedStatistics` carries all nine values. | no change |
| 63 | Results table row "Reads, Bases / 91,148 and 22,662,846" | true | The scratch bundle's `computedStatistics.readCount` is 91,148 and `baseCount` is 22,662,846, matching `seqkitStats.numSeqs` and `sumLen`. | no change |
| 64 | Results table row "Mean Length, Median Length, N50 / 248.6, 250, and 250" | false | The measured values are right (`meanReadLength` 248.6, `medianReadLength` 250, `n50ReadLength` 250), but the table is introduced as "what those cards read", and the Mean Length card formats with `String(format: "%.0f bp", …)` at `FASTQChartViews.swift:34`, so the card reads "249 bp", not "248.6". Median Length and N50 render as "250 bp". | Either recast the table lead-in as the measured statistics rather than the card text, or change the row to "249 bp, 250 bp, and 250 bp" and move 248.6 into the prose below, where the chapter already uses it as the measured mean. |
| 65 | Results table row "Mean Q / 24.87" | false | `meanQuality` is 24.87, but the card formats with `String(format: "%.1f", …)` at `FASTQChartViews.swift:36`, so it reads "24.9". | "24.9" in the card table, with 24.87 kept only if the lead-in is recast as the measured value. |
| 66 | Results table row "Q20, Q30 / 95% and 91%" | true | `q20Percentage` is 95 and `q30Percentage` is 91, and the cards format with `%.1f%%` at `FASTQChartViews.swift:37-38`, giving "95.0%" and "91.0%". The chapter's "95% and 91%" is the same value without the trailing zero, which reads as the number rather than a misquote of the card. | no change |
| 67 | Results table row "GC / 39.4%" | true | `gcContent` is 0.394 and the card formats `gcContent * 100` with `%.1f%%` at `FASTQChartViews.swift:39`, giving "39.4%". `seqkitStats.gcPercentage` is 39.4. | no change |
| 68 | "91,148 is the two files' 45,574 pairs counted as individual reads" | true | 45,574 x 2 = 91,148, and the fixture README records both figures for the same downsampled read set. | no change |
| 69 | "Mean Length is the average read length in bases, and 248.6 against a 250-base run means most reads are full length with a small trimmed tail, which the minimum of 35 bases confirms." | true | `computedStatistics.meanReadLength` is 248.6, `maxReadLength` is 250, and `minReadLength` is 35. The fixture README confirms a 2x250bp run. | no change |
| 70 | "Q20 means a 1 in 100 chance the base is wrong and Q30 means 1 in 1,000" | true | Standard Phred definition, and `GLOSSARY.md` `{#phred-score}` carries the same statement. | no change |
| 71 | "A Q30 figure below about 70% on an Illumina run means the run had trouble and is worth raising with your sequencing provider." | unverifiable | This is a field heuristic, not a value the app computes or a threshold any source in `Sources/` encodes. A grep for a 70 percent Q30 gate finds nothing. | Settle it by citing an external Illumina run-quality specification in the chapter, or soften to a statement that does not name a numeric threshold. The number is plausible but the manual has no arbiter for it. |
| 72 | "three sparkline charts, labelled Length Dist., Q / Position, and Q Score Dist." | true | `FASTQSparklineStrip.swift:25-29` returns exactly those three titles for the three enum cases. | no change |
| 73 | "Click one to open it full size in a popover." | true | `FASTQSparklineStrip.swift:360-370` resolves the clicked kind to a chart view for presentation, and the strip's click handling routes into that path. | no change |
| 74 | "Under the charts sit two tabs, Operations and Reads." | true | `FASTQDatasetViewController.swift:492-496` sets `middleTabControl.segmentCount = 2` with labels "Operations" and "Reads". | no change |
| 75 | "The Reads tab lists individual records with columns for the row number, the read identifier, the length, the mean quality, and the sequence" | true | `FASTQDatasetViewController.swift:1438-1463` adds five columns titled "#", "Read ID", "Length", "Mean Q", and "Sequence". | no change |
| 76 | "it loads the first 1,000 records rather than the whole file" | true | `FASTQDatasetViewController.swift:290` sets the placeholder text "Select the Reads tab to preview the first 1,000 records." | no change |
| 77 | "Importing does not compute a quality report on its own." | true | The plain import path queues no quality-report operation, and the viewport exposes `computeQualityReport()` at `FASTQDatasetViewController.swift:1624` as a separate action reached from the operation list titled "Quality Report" at `:1635`. | no change |
| 78 | "Read counts, base counts, and the length figures appear as soon as the bundle lands." | true | The scratch bundle's sidecar was written by the import itself and already carries `readCount`, `baseCount`, `meanReadLength`, `medianReadLength`, `n50ReadLength`, `minReadLength`, and `maxReadLength`. | no change |
| 79 | "The quality charts stay empty until a report runs" | false | The import writes the chart data too. The scratch bundle's `computedStatistics` holds `perPositionQuality` with 250 entries, `qualityScoreHistogram` with 14 bins, and `readLengthHistogram` with 142 bins. `hasQualityData` at `FASTQDatasetViewController.swift:2672-2675` is true exactly when `perPositionQuality` and `qualityScoreHistogram` are both non-empty, and the strip draws from those same three collections at `FASTQSparklineStrip.swift:184-199`, `:282-283`, `:383-395`. All three charts render immediately after a plain import. | "All three sparkline charts are drawn from statistics the import already measured, so they are populated as soon as the bundle lands. A separate Quality Report operation produces the fuller per-file report, and [Quality Control](03-quality-control.md) covers how to run one." |
| 80 | "Technical fields such as read count, read length, and the quality percentages are measured from the file and cannot be edited." | true | All of those live in `computedStatistics`, written by the importer and surfaced as read-only cards. | no change |
| 81 | "Sample metadata such as collection date or host organism is editable ... use the Inspector, the right-hand pane of the project window, to edit those fields one bundle at a time." | unverifiable | The reality map left this open at its row 45 and this pass did not close it either. `FASTQBundleCSVMetadata.save` at `FASTQBatchImporter.swift:1022` proves per-bundle metadata is persisted, and `FASTQSampleSheet.swift:60-66` proves extra sheet columns become that metadata, but neither shows which fields the Inspector exposes for editing, nor that collection date and host organism are among them. | Settle it by reading the FASTQ Inspector pane and listing the fields it actually exposes as editable. Do not name specific fields until that pass runs. |
| 82 | "`lungfish-cli metadata import <folder> <csv>`. The CSV needs a `sample_name` column whose values match the bundle folder names." | true | `metadata.txt`, `==== metadata import ====`, USAGE `<folder-path> <csv-path>` and "The `sample_name` column is used to match rows to .lungfishfastq bundles in the folder." | no change |
| 83 | "Add `--sync-bundles` to also write a per-bundle `metadata.csv` into each matched bundle." | true | `metadata.txt`: "--sync-bundles   Also write per-bundle metadata.csv files". | no change |
| 84 | "a folder of ten paired samples should read ten paired and zero single-end. A count that is twice what you expected means the mate suffixes did not match" | true | Follows directly from the summary logic at `FASTQImportConfigSheet.swift:604-612` and the unmatched-file fallback at `FASTQImportConfiguration.swift:232-235`. The dry-run demonstrated it, turning six files into five samples because two pairs failed to match. | no change |
| 85 | "A bundle from loose files takes the shared filename stem, and a bundle from a sample sheet takes the value in the sheet's `sample` column. Both land under `Imports/`." | true | `FASTQFilePair.sampleName:130-147` derives the stem; `FASTQSampleSheet.swift:71` sets `sampleName: sample` from the sheet. Both scratch bundles prove it, `HG002.chr20.10.0-10.5Mb.lungfishfastq` from loose files and `HG002-chr20.lungfishfastq` from a sheet whose `sample` value was `HG002-chr20`, both under `Reads.lungfish/Imports/`. | no change |
| 86 | "An import skips a sample when a bundle of that name already sits in the project, and the summary reports it as skipped rather than completed. Pass `--force` on the command line when you mean to replace it." | false | This is the CLI's behaviour, not the app's. `FASTQBatchImporter.swift:726-733` skips and logs `sampleSkip` when `bundleExists` and `forceReimport` is off, and `ImportFastqCommand.swift:377` prints a "Skipped" summary row. But the app path at `MainSplitViewController+FASTQImport.swift:422-447` calls `showDuplicateFileDialog` and offers Replace, Keep Both, or Skip, so the GUI asks rather than skipping silently. The paragraph sits in "What good looks like", which every other check in that section frames around the app. | "A re-import of a sample that already has a bundle stops to ask. The app shows a duplicate dialog offering Replace, Keep Both, or Skip, and Keep Both lands a second bundle with a number appended to its name. On the command line there is no prompt. The import skips the sample and reports it under Skipped, unless you pass `--force` to replace it." |
| 87 | "An Illumina run should show a mean read length close to the length the kit was configured for. A read-length distribution that spreads over thousands of bases means the files came from a long-read instrument" | true | Reads as guidance rather than an app claim, and it is consistent with the fixture, whose 2x250bp run gives a 248.6 mean and a 35 to 250 range. | no change |
| 88 | CLI block, "lungfish-cli import fastq <r1> <r2> --project <path> --dry-run" | true | `import.txt:185` USAGE `lungfish-cli import fastq [<options>] [<input> ...] --project <project>`, and `--dry-run` is "List detected pairs without importing". The scratch project's provenance records this exact command shape being run for real. | no change |
| 89 | "`--recursive --log-dir ~/Downloads/run01-logs`" and the descriptions of both | true | `import-fastq.txt`: "--recursive   Recursively scan directories for FASTQ files" and "--log-dir <log-dir>   Directory for per-sample log files". | no change |
| 90 | "`--samplesheet ~/Downloads/samples.csv`" | true | `import-fastq.txt`: "--samplesheet <samplesheet>   CSV sample sheet with sample,r1,r2 columns and optional metadata columns". | no change |
| 91 | "For the two fixture files it prints one sample, `HG002.chr20.10.0-10.5Mb`, marked `[paired]`, with the `R1:` and `R2:` filenames listed beneath it." | true | The dry-run output format was confirmed against the scratch `naming/` folder, which printed each sample as "N. <name>  [paired]" or "[single-end]" with indented `R1:` and `R2:` lines beneath. The name and pairing for the fixture follow from claim 29 and claim 30. | no change |
| 92 | "That is the same information the configuration sheet's summary carries." | true | The sheet's single-paired-sample branch at `FASTQImportConfigSheet.swift:601` prints the same two filenames, differing only by adding the total size. | no change |
| 93 | "The sample sheet is a CSV with `sample`, `r1`, and `r2` columns." | true | `FASTQSampleSheet.swift:42-44` requires all three header names and throws `"Sample sheet must contain sample, r1, and r2 columns."` at `:172` otherwise. | no change |
| 94 | "Every row becomes one bundle named for its `sample` value, and relative paths resolve next to the CSV file." | true | `FASTQSampleSheet.swift:49` sets `baseDirectory = sourceURL.deletingLastPathComponent()` and `:72-73` resolve `r1` and `r2` against it. `:71` carries the sheet's `sample` through as the bundle name, as the scratch `HG002-chr20.lungfishfastq` shows. | no change |
| 95 | "Columns beyond those three are carried through as per-sample metadata" | true | `FASTQSampleSheet.swift:60-66` collects every column that is not `sample`, `r1`, or `r2` into the row's `metadata` dictionary, and `FASTQBatchImporter.swift:1020-1023` writes it into the bundle when it is non-empty. | no change |
| 96 | "`--threads` sets how many threads the import uses, and it picks a number from the machine by default." | true | `import-fastq.txt`: "-t, --threads <threads>   Number of threads to use (default: auto)". The scratch provenance records `"--threads", "14"` chosen automatically on this machine. | no change |
| 97 | "The global options `--format json`, `--verbose`, `--quiet`, and `--log-file` behave here as they do across `lungfish-cli`." | true | All four appear in the `import-fastq.txt` OPTIONS block, `--format` with "(values: text, json, tsv; default: text)". | no change |
| 98 | "The top-level alias `lungfish-cli import-fastq` takes an identical option set" | true | `import-fastq.txt` and the `import fastq` banner in `import.txt:191-226` list the same flags in the same order with the same defaults. | no change |
| 99 | "A bundle from a paired import holds one compressed FASTQ file with the mates interleaved" | true | `FASTQBatchImporter.swift:1013` names the single output `"\(pair.sampleName).fastq.gz"`, and `:1032` sets `pairingMeta = processingPair.r2 != nil ? .interleaved : .singleEnd`. Both scratch bundles and the demo project's `Imports/HG002.lungfishfastq` hold one `.fastq.gz` with `"pairingMode": "interleaved"` in the sidecar. | no change |
| 100 | "The metadata file beside it records the original filenames, the original total size, the size after storage optimisation, and the pairing mode." | true | `IngestionMetadata` at `FASTQBatchImporter.swift:1033-1043` carries `originalFilenames`, `originalSizeBytes`, `storageInputSizeBytes`, `storageOutputSizeBytes`, and `pairingMode`, all present in the scratch sidecar. | no change |
| 101 | "the two source files totalled 17,968,037 bytes and the stored file came to 5,778,889 bytes" | true | The scratch sidecar records `originalSizeBytes` 17,968,037 and `storageOutputSizeBytes` 5,778,889. The provenance input sizes 8,690,798 and 9,277,239 sum to exactly 17,968,037, and the output file's recorded `sizeBytes` is 5,778,889. | no change |
| 102 | "so storage optimisation and quality binning together cut the reads to under a third of their delivered size" | true | 5,778,889 divided by 17,968,037 is 0.3216, under one third. `isClumpified` is true and `qualityBinning` is `illumina4` in the same sidecar, so both were applied. | no change |
| 103 | "A bundle can also be virtual ... it stores a manifest naming a root file and the operation to apply to it, whether a read-identifier subset, a set of trim positions, or a full copy." | true | `fastq.txt`, `==== fastq materialize ====`: "Reads the derived bundle manifest, resolves the root FASTQ, and applies payload-specific materialization (subset read IDs, trim positions, or copy full payload)." | no change |
| 104 | "`lungfish-cli fastq materialize SampleA.lungfishfastq --output SampleA.fastq`" | true | `fastq.txt:663` USAGE `<input> --output <output> [--force] [--compress] [--temp-dir <temp-dir>]`. | no change |
| 105 | "Add `--temp-dir <dir>` ... `--force` to overwrite an existing output, and `--compress` to gzip the result." | true | `fastq.txt:669-672` OPTIONS block, all three with those exact descriptions. | no change |
| 106 | Front matter `shots:` lists all four `<!-- SHOT -->` markers with captions | true | The body carries markers `import-center-sequencing-reads-tab` (line 85), `import-fastq-configuration-sheet` (91), `sidebar-after-import` (99), and `fastq-viewport-summary-cards` (135). All four appear in `shots:` at lines 17 to 24, each with a caption. `fastq-viewport-summary-cards` is also declared in `01-foundations/02-sequencing-reads.md:13` and used at its line 235, which is the intended sharing. | no change |
| 107 | Shot caption "showing the summary reading R1 and R2 on separate lines above the Platform, Pairing, Quality Binning, and compression controls" | true | The sheet's layout order at `FASTQImportConfigSheet.swift:112-127` is header, summary, Platform, Pairing, Quality Binning, then the optimize checkbox, Compression Tool, and Compression Level. | no change |
| 108 | Shot caption "showing the nine summary cards above the three sparkline charts" | true | `FASTQDatasetViewController.swift:478-486` places the card bar above the strip, and claims 61 and 72 fix the counts at nine and three. | no change |
| 109 | Front matter `glossary_refs` anchors all resolve in GLOSSARY.md | false | Thirteen of the fourteen resolve as written. `FASTQ` does not. The glossary's anchor is lowercase, `**FASTQ**{#fastq}` at `GLOSSARY.md:119`, and every body link in this chapter already writes `#fastq`, so the front-matter slug is the only mismatch. Note that `phred-score` resolves in the glossary and is linked in the body but is absent from `glossary_refs`. | Change the first entry of `glossary_refs` from `FASTQ` to `fastq`, and add `phred-score` to the list so the body's link at line 149 is declared. |
| 110 | Front matter `fixtures_refs: [hg002-chr20]` | true | `docs/user-manual/fixtures/hg002-chr20/` exists with a README meeting the metadata requirements in `fixtures/README.md`, and it is the only fixture set this chapter uses. | no change |
| 111 | Settings coverage, every setting in `import.fastq` has a Settings paragraph with the label, default, and allowed values | true | The registry lists eight settings, Platform, Pairing, Quality Binning, Optimize storage, Compression Tool, Compression Level, Apply processing recipe after import, and the recipe picker. The chapter's Settings section carries eight paragraphs in the same order with matching labels and defaults, each in the three-sentence shape CONSISTENCY.md prescribes, each closing with the flag sentence or the fixed no-flag sentence. `import.fastq-sample-sheet` has `settings: []` and the chapter says so at line 111. | no change |
| 112 | "Nothing here needs a plugin pack or Docker Desktop." | unverifiable | The default import runs `clumpify.sh` from `~/.lungfish/conda/envs/bbtools/bin/`, per the scratch provenance, which is a managed tool environment rather than something the base app ships. Whether that environment counts as a plugin pack the reader must install, or is provisioned with the app, was not settled in this pass. Optimize storage starts on for Illumina, so the fixture import takes that path by default. | Settle it by checking whether the `bbtools` conda environment is provisioned at install time or on demand through the Plugin Manager. If it is on demand, the sentence needs a caveat, since the chapter's own fixture import invokes it. |

## Counts

True 103. False 6. Unverifiable 3.

## Notes for the rewrite

The six false verdicts fall into three groups.

Rows 64 and 65 are a mismatch between measured statistics and rendered card text. The chapter's numbers are all correct as measurements, but the table promises "what those cards read", and two cards round. The cheapest fix is to change the lead-in rather than the numbers.

Row 79 reverses a real behaviour. The chapter tells the reader the quality charts are empty until they run a report, and they are not. The import measures per-position quality, the quality-score histogram, and the read-length histogram, and the strip draws all three immediately. This one matters because it sends a reader looking for a step they do not need.

Rows 28 and 86 describe the CLI's behaviour where the chapter is describing the app. The unpaired summary line carries no `R1:` label, and a duplicate bundle in the app raises a Replace, Keep Both, or Skip dialog rather than skipping.

Row 109 is front-matter hygiene, one anchor case and one undeclared link.

The three unverifiable rows each name what would settle them. Row 81 is the same Inspector question the reality map left open at its row 45, and it is worth closing before the Inspector screenshot is captured.
