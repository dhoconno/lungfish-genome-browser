# Reader 3 report, Keyboard Shortcuts appendix

Reader persona. Pre-med student, English is my second language, comfortable
with biology words from textbooks, slowed by idioms and long sentences, never
opened a terminal. I read this the way I would read it in real life, which is
looking for one shortcut in a hurry.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "the letter or symbol at the end of that combination the key equivalent" | I had to read this sentence twice. It defines three things at once, keyboard shortcut, key equivalent, modifier keys, inside one long sentence with two links. In a hurry I skipped it and then did not know what "key equivalent" meant later. | Split into three short sentences, one term each. |
| What it is, "Ctrl is the Control key at the far left of the bottom row" | On my keyboard Control is at the far left of the bottom row but Fn is further left. I was not sure I had the right key. | Say the key is labelled Control or ctrl. |
| What it is, "Every row below was read out of the source code that builds LGE's menus and views, not typed into the running app" | "Read out of the source code" is a phrase for programmers. I do not know what source code is for this app and I do not know whether this makes the table more trustworthy or less. It stopped me because I could not judge it. | Say the table was checked against the app build, and delete the source code detail. |
| What it is, "so a row tells you what the build binds rather than what a particular window happened to accept on a particular day" | Read twice. "What the build binds" and "happened to accept on a particular day" are both figures of speech I had to decode. I still am not sure if it means the table might be wrong. | Say a shortcut may be inactive in some windows, and point to the greying rule below. |
| What it is, "The menu bar is the authority the reader can check in a second" | "The authority" is a metaphor and "the reader" addresses me in the third person, which was strange after "you" everywhere else. | Say the menu bar shows the current shortcut beside each command. |
| What it is, "its shortcut does nothing at all while it stays that way, so pressing it produces silence rather than an error" | "Produces silence" is a figure of speech. I read it as the app plays no sound, then realised it means nothing happens. | Say nothing happens and no message appears. |
| What it is, "Zoom In is dead until a sequence is on screen" and "Cancel All Operations is dead until something is running" | "Dead" used twice as a metaphor for disabled. In my first pass I thought "dead" meant broken or removed. | Use "disabled" or "greyed out", the words already defined in the same paragraph. |
| What it is, "a tool inside the Tools menu's Genotyping submenu is dead until its software pack is installed" | "Software pack" is not explained here or anywhere else in the chapter. I do not know what it is, where I get one, or how I check if it is installed. | Gloss it at first use and link to the chapter that installs it. |
| What it is, "The Workflow Builder (Experimental)... item is stronger still" | "Stronger still" is an idiom comparing two kinds of hiding. I read the sentence twice before I understood it means the item is not shown at all. | Start the sentence with "Workflow Builder (Experimental)... is different." |
| What it is, "turn on Show Experimental Features in Settings > Advanced" | This is the only place in the chapter that uses the arrow ">" for a path. Everywhere else the chapter writes paths as words, like "System Settings, click Keyboard, then Keyboard Shortcuts". I paused to check whether ">" meant something else. | Use one path style for the whole chapter. |
| How a shortcut is written here, "Order does not matter when you hold the modifiers, only that all of them are down before the last key goes down" | Read twice. The sentence drops the verb in the second half. | Split into two sentences. |
| How a shortcut is written here, "macOS treats the equals key and the plus key as the same physical key here" | The word "here" at the end is vague. I could not tell if "here" means in LGE, in this chapter, or on this keyboard. Also I did not know whether I press Shift or not, and the sentence says both "without reaching for Shift" and mentions plus, which needs Shift on my keyboard. | State plainly that pressing Cmd and the equals key zooms in, no Shift needed. |
| How a shortcut is written here, "Menu titles are quoted exactly as the app prints them, ellipsis included" | "Ellipsis" is a printing term, not a biology term. I guessed it means the three dots but I was not certain until the Customizing section. | Gloss it once, "the three dots at the end of a title". |
| Lungfish Genome Explorer menu, "Check for Updates... is greyed out on a build that has no update feed configured" | Three things I do not know appear in one clause, "a build", "update feed", "configured". I cannot tell whether my copy of LGE is such a build. | Say the item is greyed out when automatic updates are not set up for your copy. |
| File menu, "There is no Save command and no Cmd-S" | Good news but it stopped me, because I have been trained to save. I then looked for confirmation that my work is safe and had to read the rest of the paragraph to find it. | Lead with "Your work is saved automatically." |
| File menu, "the Provenance submenu nested inside Export" | "Provenance" is not explained. I know the ordinary English word but not what it means for a file in this app. | Gloss it at first use. |
| View menu, "the Document Inspector is a separate window of manifest-level metadata for the bundle you have open" | Three unexplained terms in one clause, "manifest-level", "metadata", "bundle". I could not picture what this window shows, so I do not know when I would press Cmd-Opt-D. | Gloss bundle and say the window lists file details such as source and date. |
| View menu, "Focus Viewer, Cmd-Opt-F" and "Restore Side Panes, Cmd-Ctrl-Opt-F" | The table gives no explanation of what either command does, and the paragraph above only explains the sidebar and the two inspectors. "Side panes" is also a new name for what was called sidebar and Inspector. | Add one sentence for each, and use one name for these panels. |
| View menu, "AI Assistant, Cmd-Shift-A" | No explanation of what the AI Assistant is or what pressing this does. This is the only row where I would be afraid to press the key. | Add one sentence, or link to the AI Assistant guide named in the Help menu section. |
| View menu, "Zoom Reset (10kb), Cmd-1" | "10kb" is not explained. I know kilobase from genetics class, but I do not know what the command does with it, whether it shows 10,000 bases centred on where I am or jumps somewhere else. | Say the view resets to a 10,000 base window at the current position. |
| View menu, "the app's own interface type is too small to read comfortably" | "Interface type" reads as a category of interface, not as letters on the screen. I read it twice before "type" meant typeface. | Say "text" instead of "type". |
| View menu, "The Option key is what separates these three from the three viewport zoom commands that sit a few rows below them in the same menu" | Long sentence, and "a few rows below them in the same menu" describes the app screen while I am reading a document, so I cannot check it. | Say adding Option changes viewport zoom into text size. |
| View menu, "Expand All is Cmd-Shift-Right Arrow" | I do not know how to type this. Is Right Arrow the arrow key on the keyboard, and do I hold all three at once? Every other shortcut in this chapter uses a letter, so this is the first one I could not press with confidence. | Say the arrow keys on the keyboard, held with Command and Shift. |
| Sequence menu, "The two Extract and Copy commands take the region the viewport is showing, or the range you have selected inside it if you have made one" | Read twice. "The two Extract and Copy commands" sounds like four commands. | Name the two commands. |
| Sequence menu, "Go to Location... accepts a plain coordinate or a chromosome:start-end range" | The code-styled `chromosome:start-end` is a format I am supposed to copy, but no example is given. I do not know if I type the word chromosome or a chromosome name, and whether spaces are allowed. | Give one worked example using a human chromosome. |
| Sequence menu, "matches gene names from the annotation attached to whatever is open, which is the GFF or GenBank record the bundle carries" | GFF, GenBank record and bundle all appear without a gloss. Long sentence with two relative clauses. I could not tell whether Cmd-Opt-G will work on my file. | Gloss the formats and say the command works only when the file has gene annotations. |
| Sequence menu, "It takes Cmd-Opt-G rather than Cmd-G so that it does not collide with Find Next" | "Collide" is a metaphor. Minor, but I stopped on it. | Say the two commands would otherwise share the same keys. |
| Tools menu, "Plugin Manager..., Cmd-Shift-B" | "Plugin" is never explained, and B does not relate to the name, which made it hard to remember. No sentence says what the Plugin Manager does. | Gloss plugin and add one sentence. |
| Operations menu, "The Operations panel is the window that lists every pipeline LGE is running or has finished" | "Pipeline" is not glossed. I have heard the word in a seminar but I could not say what one pipeline is in this app, one analysis or one whole project. | Gloss it as one analysis run. |
| Help menu, "Lungfish Genome Explorer Help, Cmd-?" | I cannot press this. On my keyboard the question mark needs Shift, so it is really Cmd-Shift-slash, and the chapter earlier promised that symbol shortcuts would be written so there is no doubt which key is meant. | Write it the way the plus and minus keys were explained. |
| Inside the sequence viewport, "Once you have clicked into the sequence display itself, the arrow keys move it" | I do not know how to tell whether I have clicked "into" the display, or what changes on screen when I have. This is the first section where the shortcut depends on focus and nothing tells me how focus looks. | Say what shows that the display has focus. |
| Inside the sequence viewport, "Left and right pan the view by a hundred bases at a time" | "Pan" is a camera word. I guessed from context but was not sure it means move sideways rather than zoom. | Say the view moves left or right by 100 bases. |
| Inside the sequence viewport, "The Escape key does two jobs in one, and which one it does depends on what the viewport is busy with" | Read twice. "Does two jobs in one" and "what the viewport is busy with" are both idioms, and the actual rule only arrives two sentences later. | State the rule first, then the two cases. |
| Inside the sequence viewport, "which is the way out of a very deep region that is taking a long time" | "Very deep region" is unexplained. In genetics "deep" means high coverage, but here it might mean many reads or a long region. I could not judge which. | Say a region with many reads. |
| Inside the alignment viewport, "The compact read viewer that appears inside a classifier result window" | "Classifier" is used here for the first time in the chapter and is not glossed until the section after next, where it is described as producing a tree of organism names. I was lost for two sections. | Gloss classifier at this first use. |
| Inside the alignment viewport, "The shared code behind these accepts the equals key for plus and the underscore key for minus" | "The shared code behind these" is a programmer's sentence and I do not know what shared code is. Also the underscore normally needs Shift on my keyboard, so this contradicts "you do not need to reach for Shift". | Say the equals key and the hyphen key work too, and drop the code detail. |
| Inside the genotype result window, "shows the alleles LGE called for each sample in a plate" | "Called" as a verb for alleles is field jargon. "Plate" is also not glossed for this context. I know a 96 well plate from lab class but I do not know what a plate means inside this software. | Gloss called and plate. |
| Inside the genotype result window, "This is the densest set in the app, because a person working through a plate is making one small judgement after another and reaching for the mouse each time is slow" | Long sentence explaining design reasoning that I do not need when I am looking up a key. It slowed my scan of the table below. | Cut, or move after the table. |
| Inside the genotype result window, table heading "carries six shortcuts of its own" | The table has five rows, not six. I counted twice thinking I had missed one. Escape is mentioned only after the table, so I could not tell if Escape was meant to be the sixth. | Make the count match the table. |
| Inside the genotype result window, "Mark the selected sample reviewed" and "Mark the selected sample confirmed" | The two rows are Cmd-R and Cmd-K. I do not know the difference between reviewed and confirmed, so I cannot choose between them. | Say what each state means. |
| Inside the genotype result window, "they work only when the window is on its Review lens or when a call is selected in a haplotyped MiSeq result" | "Lens", "call" and "haplotyped MiSeq result" all arrive unexplained in one sentence. I cannot tell whether my window qualifies, so I cannot use any of the four shortcuts. | Gloss lens, and say plainly when the shortcuts are active. |
| Inside the genotype result window, "Cmd-Opt-P marks the selected cell a false positive, Cmd-Opt-N marks it a false negative" | I know false positive and false negative from statistics class, but not what marking a cell does here, whether it changes the data or only adds a note. I would not press these without knowing. | Say the mark is a note and does not change the result. |
| Inside a classifier result window, "Cmd-right bracket moves to the next sample, Cmd-left bracket moves to the previous one" | I do not know which key the right bracket is. Every other symbol shortcut in the chapter got a sentence explaining the physical key, and these did not. | Show the bracket characters, as the plus and minus section does. |
| Inside a classifier result window, "The taxonomy sunburst, the circular chart of nested organism groups, takes Escape to step out one ring toward the root" | Read twice. "Step out one ring toward the root" mixes a picture of a circle with a picture of a tree. | Say Escape moves outward to the broader group. |
| Inside the sidebar, "These shortcuts belong to that context menu, so they work while it is open rather than as global bindings" | "Global bindings" is programmer language. I understood the first half but the second half added a word I do not know. | Say the shortcuts work only while the menu is open. |
| Inside the sidebar, "the delete command, whose title changes to name whatever you selected, is the Delete key on its own" | Long sentence with a clause in the middle. Also I do not know whether Delete removes the file from the disk or only from the list, which matters more to me than the key. | Split, and say what is deleted. |
| Inside the Workflow Builder, "the arrow keys to nudge a selected box one grid square at a time" | I do not know what the Workflow Builder is for. This is the second mention and the first section did not describe it either, only how to make it appear. | Add one sentence saying what a workflow is. |
| Inside the Workflow Builder, "Its toolbar tooltips advertise Cmd-plus and Cmd-minus ... but no key binding matches those tooltips in this release" | This is the only place where the manual tells me the app is wrong, and I did not know what to do with it. "Advertise" is a metaphor and "no key binding matches" is programmer language. | Say the tooltips are incorrect and the buttons must be clicked. |
| Memorizing chords, heading | "Chord" is used as a heading without ever being defined. The chapter has said "combination" everywhere else. In music a chord is notes played together, so I guessed, but I was not sure it was the same thing. | Use "combinations", or gloss chord at first use. |
| Memorizing chords, "knowing them is faster than memorizing forty-five separate rows" | I counted the rows and could not reach forty-five from the tables, because many shortcuts are only in prose. The number made me doubt whether I had missed a table. | Drop the count. |
| Memorizing chords, "On a Sequence menu verb, Cmd-Shift-letter performs the action on the visible region instead" | "On a Sequence menu verb" was the hardest phrase in the chapter. I read it three times. A verb is a part of speech, so I could not connect it to a menu. | Say "for the Sequence menu commands". |
| Memorizing chords, "The Sidebar is the exception to all of it" | "The exception to all of it" is loose. Exception to which of the three patterns? | Name the pattern it breaks. |
| Customizing shortcuts, "Press the new chord, click Add, and the override takes effect the next time LGE launches" | I could not perform this. It does not say where I press the new keys, whether there is a field to click first, and "the override" is a term used only here. | Say to click the shortcut field and press the keys. |
| Customizing shortcuts, "Type the menu title exactly, including any trailing ellipsis" | I cannot type an ellipsis. It does not say whether three separate periods work or whether I need a special character, and the previous paragraph calls it "an ellipsis character", which suggests three periods will fail. | Say how to type the character, or say to copy the title. |
| Accessibility, "Ctrl-Opt-M moves VoiceOver into the menu bar" | This only works if VoiceOver is already on, and the chapter does not say how to turn it on. I tried the keys mentally and got nowhere. | Say VoiceOver must be switched on first. |
| Accessibility, "Full Keyboard Access, under System Settings, Keyboard, "Keyboard Navigation", lets you reach every control without a pointer" | Two names for the same thing in one sentence, "Full Keyboard Access" and "Keyboard Navigation", with quotation marks on only one. I did not know which one to look for on my screen. | Use the name the screen shows. |
| Next, "See CLI Reference for the equivalent command-line surface" | "Command-line surface" means nothing to me and I have never opened a terminal. I could not tell whether that chapter is for me. | Say the chapter is for people who type commands instead of clicking. |

One thing I learned. Nothing in LGE needs saving, because every change is
written to the project folder as it happens.

One thing I still could not do. Press Cmd-? for Help, or the two bracket
shortcuts, because the chapter never says which physical keys those are even
though it promised to explain symbol keys.

The sentence I liked most. "LGE greys items out on purpose whenever the
command has nothing to act on."
