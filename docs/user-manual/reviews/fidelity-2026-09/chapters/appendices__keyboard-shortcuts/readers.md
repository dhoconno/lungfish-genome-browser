# Merged reader report - Keyboard Shortcuts appendix

Four readers reviewed this chapter. Reader 1 is a sophomore fresh from an introductory genetics course who has never opened a terminal. Reader 2 is a senior with two years of wet-lab pipetting, library prep and PCR experience but no data analysis background. Reader 3 is a pre-med student reading English as a second language, comfortable with biology vocabulary but slowed by idioms and long sentences. Reader 4 is an undergraduate who used Geneious for a semester and expects a graphical sequence viewer's conventions. Reader 1 filed 42 rows, reader 2 filed 39 rows, reader 3 filed 57 rows, and reader 4 filed 46 rows, for 184 source rows in total.

| Location | Issue | Suggested fix | Readers |
|---|---|---|---|
| What it is, "macOS calls the letter or symbol at the end of that combination the key equivalent" | All four readers learned the term "key equivalent" here and then never saw it used again anywhere in the chapter, so the definition did no work. | Drop the term, or use it consistently in the table headings so the definition earns its place. | 1, 2, 3, 4 |
| What it is, "Every row below was read out of the source code that builds LGE's menus and views, not typed into the running app" | All four readers found that being told the table was read out of source code, rather than tested in the app, undermined their trust in the rows rather than reassuring them. | Say the rows were checked against the shipping app build, and drop the "not typed into the running app" framing. | 1, 2, 3, 4 |
| What it is, "a tool inside the Tools menu's Genotyping submenu is dead until its software pack is installed" | All four readers hit "software pack" with no gloss, no explanation of where it comes from, and no way to tell whether one is already installed. | Gloss software pack at first use as an add-on set of analysis tools, and link to the chapter that installs them. | 1, 2, 3, 4 |
| How a shortcut is written here, "macOS treats the equals key and the plus key as the same physical key here" | All four readers found the equals-and-plus explanation contradictory or hard to place on their own keyboard, since plus is a shifted character for most of them. | State plainly to press Command and the equals key, with no Shift needed. | 1, 2, 3, 4 |
| File menu, "the Provenance submenu nested inside Export" | All four readers met "Provenance" with no gloss and could not say what it holds or exports. | Gloss provenance at first use as the record of how a result was produced. | 1, 2, 3, 4 |
| View menu, "the Document Inspector is a separate window of manifest-level metadata for the bundle you have open" | All four readers hit three unexplained terms at once, manifest-level, metadata, and bundle, and could not picture what the window shows. | Split the sentence and gloss bundle as one data item in the project. | 1, 2, 3, 4 |
| View menu, "Restore Side Panes, Cmd-Ctrl-Opt-F" (and, for readers 3 and 4, the neighboring Focus Viewer row) | All four readers found this command named with no sentence saying what it restores or when it is needed, and noted "side panes" is not the word used elsewhere for sidebar and Inspector. | Add one line saying it brings the sidebar and Inspector back after Focus Viewer hid them, and use one consistent name for these panels. | 1, 2, 3, 4 |
| View menu, "AI Assistant, Cmd-Shift-A" | All four readers found the AI Assistant listed with no description of what it does, leaving them unsure whether pressing it was safe. | Add one clause saying it opens a chat panel that answers questions about the open data. | 1, 2, 3, 4 |
| View menu, "Zoom Reset (10kb), Cmd-1" | All four readers could not tell what resetting to 10kb actually does or why that is the resting value rather than the whole sequence. | Say plainly that it sets the view to a 10,000 base window centered on the current position. | 1, 2, 3, 4 |
| Sequence menu, "Go to Location... accepts a plain coordinate or a `chromosome:start-end` range" | All four readers found no worked example of what to actually type, and could not tell whether "chromosome" is a literal word or a placeholder. | Give one worked example, such as chr6:32000000-32100000. | 1, 2, 3, 4 |
| Operations menu, "The Operations panel is the window that lists every pipeline LGE is running" | All four readers met "pipeline" used with no gloss and could not say whether one pipeline means one analysis they started or something smaller. | Gloss pipeline at first use as one analysis run as a chain of steps. | 1, 2, 3, 4 |
| Inside the alignment viewport, "The compact read viewer that appears inside a classifier result window" | All four readers hit "classifier" used here with no gloss and could not picture what a classifier result window is or how to open one. | Gloss classifier at first use as the tool that assigns reads to organisms. | 1, 2, 3, 4 |
| Inside the alignment viewport, "The shared code behind these accepts the equals key for plus and the underscore key for minus" | All four readers were stopped by "the shared code behind these" as a programmer's sentence that added nothing they could act on. | Drop the code detail and just say the equals and underscore or hyphen keys work here too. | 1, 2, 3, 4 |
| Inside the genotype result window, "carries six shortcuts of its own" then a table of five rows | All four readers counted the table twice because it has five rows, not six, and could not tell whether Escape, mentioned after the table, was meant to be the sixth. | Make the stated count match the table, or fold Escape into it as a row. | 1, 2, 3, 4 |
| Inside the genotype result window, "they work only when the window is on its Review lens or when a call is selected in a haplotyped MiSeq result" | All four readers met Review lens and haplotyped MiSeq result arriving unexplained in one condition, so none could tell whether their own window qualified for the four review shortcuts. | Gloss lens, and say plainly which tab or state must be showing for the shortcuts to be live. | 1, 2, 3, 4 |
| Inside the genotype result window, "Cmd-Opt-P marks the selected cell a false positive, Cmd-Opt-N marks it a false negative" | All four readers knew the statistical terms but could not tell whether marking a cell changes the underlying data or only adds a note, or whether it can be undone. | State that the mark is an annotation that does not change the result, and that Cmd-Opt-R removes it. | 1, 2, 3, 4 |
| Inside a classifier result window, "Cmd-right bracket moves to the next sample" | All four readers found the bracket keys spelled out in words rather than shown as characters, unlike the earlier care taken with plus and minus. | Show the characters, as in Cmd-] and Cmd-[. | 1, 2, 3, 4 |
| Memorizing chords, heading and first uses of "chord" | All four readers found "chord" used as a heading and throughout the section without ever being defined, when the rest of the chapter says "combination." | Gloss chord at first use as a combination of keys pressed together, or use combination throughout. | 1, 2, 3, 4 |
| Accessibility, "Ctrl-Opt-M moves VoiceOver into the menu bar" | All four readers found this is the only shortcut in the chapter that does nothing unless VoiceOver is already on, with no explanation of how to turn VoiceOver on. | Add that VoiceOver must be switched on first, and name the key that toggles it. | 1, 2, 3, 4 |
| What it is, "Ctrl is the Control key at the far left of the bottom row" | Three readers checked their own keyboard and found fn sits further left than Control, so the claim that Control is the leftmost key did not match what they saw. | Say Control sits near the left end of the bottom row, next to fn, without claiming it is the leftmost key. | 1, 2, 3 |
| What it is, "The Workflow Builder (Experimental)... item is stronger still" | Three readers had to reread "stronger still" to work out it means the menu item is hidden entirely rather than merely greyed out. | Say plainly the item is not greyed out but hidden entirely. | 1, 2, 3 |
| Inside the genotype result window, "shows the alleles LGE called for each sample in a plate" | Three readers knew allele but stumbled on "called" as a verb and on what a plate means inside this software, distinct from a lab plate. | Gloss called as the app's best-guess identification, and say what a plate means on screen. | 1, 2, 3 |
| Inside the sequence viewport, "which is the way out of a very deep region that is taking a long time" | Three readers met "deep region" with no gloss and were not sure whether it means many reads stacked up or something about elapsed time. | Say plainly a region with very many reads. | 1, 3, 4 |
| Inside the sidebar, "These shortcuts belong to that context menu, so they work while it is open rather than as global bindings" | Three readers were stopped by "global bindings" as unglossed jargon, and could not picture pressing a shortcut while a context menu stays open. | Say plainly the shortcuts work only while the right-click menu is showing. | 1, 3, 4 |
| Memorizing chords, "faster than memorizing forty-five separate rows" | Three readers counted the actual tables, could not reach forty-five, and doubted whether they had missed a table. | Drop the count, or make it match the tables including the prose-only rows. | 1, 3, 4 |
| Customizing shortcuts, "`Reverse Complement...` and `Translate...` both end in an ellipsis character" | Three readers did not know how to type an ellipsis character and could not tell whether three typed periods would work or silently fail. | Say to copy the title from the menu, or give the keystroke that produces the ellipsis character. | 1, 3, 4 |
| Inside the Workflow Builder, "Its toolbar tooltips advertise Cmd-plus and Cmd-minus ... but no key binding matches those tooltips in this release" | Three readers found the admission that a documented tooltip does not match any real shortcut undermined trust in the rest of the tables, arriving mid-chapter with no warning marker. | Move the known defect to a short, clearly marked note, and say the buttons must be clicked instead. | 1, 3, 4 |
| Accessibility, "Full Keyboard Access, under System Settings, Keyboard, "Keyboard Navigation"" | Three readers found the same setting given two different names in one sentence, with the path style also inconsistent with how paths are written elsewhere in the chapter. | Use the single name shown on screen, and one consistent path style throughout. | 1, 3, 4 |
| View menu, "**Expand All** is Cmd-Shift-Right Arrow" | Three readers were unsure how to type an arrow key as the last key of a shortcut, and whether the taxonomy tree needs focus first the way the sequence viewport does. | Note in the writing-style section that arrow keys can be the last key, and say the taxonomy tree must have focus. | 2, 3, 4 |
| Inside the sequence viewport, "Once you have clicked into the sequence display itself" | Three readers found this the only place explaining that a shortcut needs focus in a specific place first, arriving well after earlier sections quietly assumed it. | Move the focus explanation earlier, into the How a shortcut is written here section. | 2, 3, 4 |
| Inside the sidebar, "the delete command, whose title changes to name whatever you selected, is the Delete key on its own" | Three readers could not tell whether Delete removes the item from disk or only from the project or list, and whether it can be undone. | Say plainly what Delete removes and whether it asks for confirmation first. | 2, 3, 4 |
| What it is, "so a row tells you what the build binds rather than what a particular window happened to accept on a particular day" | Two readers had to decode this as figurative language and were still unsure whether it means the table itself could be wrong. | Say plainly that a shortcut may be inactive in some windows, and point to the greying rule below. | 3, 4 |
| What it is, "turn on Show Experimental Features in Settings > Advanced" | Two readers noticed this is the only place using the ">" arrow for a menu path, when the rest of the chapter writes paths as words. | Use one consistent path style for the whole chapter. | 3, 4 |
| How a shortcut is written here, "Order does not matter when you hold the modifiers, only that all of them are down before the last key goes down" | Two readers found this sentence contradicts itself, saying order does not matter and then immediately stating an order rule. | Split into two sentences, or drop the second clause. | 3, 4 |
| How a shortcut is written here, "Menu titles are quoted exactly as the app prints them" | Two readers noticed the tables print titles plain while the prose prints them bold, so neither could tell which form was the exact one the rule referred to. | Say plainly which typographic form, bold or plain, is the exact title. | 1, 4 |
| Lungfish Genome Explorer menu, "Check for Updates... is greyed out on a build that has no update feed configured" | Two readers met "build," "update feed," and "configured" together with no way to tell which build their own copy of LGE is. | Say plainly that the item greys out when automatic updates are not set up for that copy. | 1, 3 |
| File menu, "Import Center... Cmd-Shift-I" / "A project in LGE is a folder on disk" | Two readers could not tell whether a project needs to be open first, and one pressed the shortcut with nothing open and saw nothing happen. | State plainly that File and View shortcuts need a project window open first. | 1, 2 |
| Sequence menu, "matches gene names from the annotation attached to whatever is open, which is the GFF or GenBank record the bundle carries" | Two readers met GFF, GenBank record, and bundle together unglossed and could not tell whether the command would work on their own file. | Gloss the file formats, and say the command works only when the open file carries gene annotations. | 1, 3 |
| Sequence menu, "Copy Visible Region as FASTA" | Two readers found FASTA used in the chapter without ever being glossed. | Gloss FASTA at first use as the plain-text format holding a sequence and its name. | 1, 2 |
| View menu, "Larger is Cmd-Opt-plus" | Two readers found the three Content Text Size shortcuts given only in prose while every other group of shortcuts sits in a table, and missed them while scanning. | Put the three Content Text Size shortcuts into a table like the rest. | 1, 4 |
| Inside the sequence viewport, "Left and right pan the view by a hundred bases at a time" | Two readers could not tell whether the hundred-base step is fixed or scales with the current zoom level. | Say plainly whether the step is fixed or scales with zoom. | 1, 2 |
| Tools menu, "Plugin Manager..., Cmd-Shift-B" | Two readers found B has no mnemonic connection to Plugin Manager, which undercut the chapter's later claim that Cmd-Shift-letter panel shortcuts form a learnable pattern. | Note plainly that B is arbitrary here, as an exception to the pattern. | 2, 4 |
| Inside a classifier result window, "steps out one ring toward the root" | Two readers found the sunburst's "root" unexplained and the circle-and-tree metaphor mixed and hard to picture. | Say plainly that Escape moves outward to the broader group, or name the chart's centre. | 2, 3 |
| Customizing shortcuts, "Press the new chord, click Add" | Two readers found the steps skip which field the cursor must be in before the new key combination is pressed. | Name the Keyboard Shortcut field explicitly before saying to press the keys. | 2, 3 |
| Memorizing chords, "On a Sequence menu verb, Cmd-Shift-letter performs the action on the visible region" | Two readers were stopped by "verb" used to mean the Sequence menu's command names, a usage neither expected. | Say "for the Sequence menu commands" instead of "on a Sequence menu verb." | 3, 4 |
| File menu, "There is no Save command and no Cmd-S" | Two readers reacted to the absence of Save, one needing the reassurance that work is saved automatically stated more prominently, the other nearly missing it as the single most important fact in the chapter. | Lead with "Your work is saved automatically," and call the absence out rather than burying it in a paragraph. | 3, 4 |
| Help menu, "Lungfish Genome Explorer Help, Cmd-?" | Two readers noted the question mark needs Shift on their keyboard, so Cmd-? is really Cmd-Shift-slash, contradicting the earlier care taken to spell out shifted symbol keys. | Write it out as Command, Shift, and the slash key, the way the plus and minus keys were explained. | 3, 4 |
| Lungfish Genome Explorer menu, "Settings... Cmd-comma" | One reader could not tell how to press "comma" as a key, since every other row names a letter or symbol rather than a punctuation word. | Write it as Cmd-, with the character shown, or say to press the comma key. | 1 |
| File menu, "the Provenance submenu" precondition (Import Center specifically) | One reader could not tell whether the Import Center requires an open project, since the File menu section never states a precondition. | State that the Import Center is the window for bringing files into a project, and that a project must be open. | 1 |
| View menu, "In a taxonomy viewport, meaning the tree of organism names a classifier produces" | One reader met "classifier" used here with no gloss, before genetics-class vocabulary could carry them through. | Gloss classifier as software that decides which organism each read came from. | 1 |
| Sequence menu, "The two Extract and Copy commands take the region the viewport is showing" | One reader found "viewport" used from the start of the chapter and only explained later, in the View menu section. | Gloss viewport at its first use in the What it is section. | 1 |
| Tools menu, "Plugin Manager... Cmd-Shift-B" | One reader noticed the chapter calls the same thing a software pack earlier and a plugin here, without saying they are the same. | Use one consistent term for the add-on tools throughout the chapter. | 1 |
| Inside a classifier result window, "The TaxTriage result window" | One reader met TaxTriage with no gloss, and it is not listed among the Tools menu items described earlier. | Gloss TaxTriage as one of LGE's classifiers at first use. | 1 |
| Customizing shortcuts, "Click the plus button, choose Lungfish Genome Explorer, and type the menu title" | One reader could not perform this because the app may be listed under a different name, such as "Lungfish Preview," with no guidance for that case. | Say the app appears under the name shown in its own menu bar, and how to browse for it if it is missing. | 1 |
| Whole chapter, no Prerequisites or Before you start section | One reader noted every other chapter states up front what must be open, while here the precondition is scattered through the prose instead of stated once. | Add one short section saying which shortcuts are global and which need a specific window open. | 1 |
| File menu, "the **Provenance** submenu" project-open precondition (general framing) | One reader pressed Cmd-Shift-I with nothing open and nothing happened, with no hint anywhere in the appendix about which shortcuts need a project window open. | Add that File and View shortcuts need a project window open. | 2 |
| Sequence menu, "**Extract Visible Region...** opens a dialog and writes a new bundle into the project" | One reader found "bundle" unglossed again here, and could not tell where in the project the new item lands. | Say it saves a new item into the project's Extractions folder. | 2 |
| Sequence menu, "**Add Annotation...** and **Find ORFs...**" | One reader half-remembered ORF from genetics class but found it never expanded. | Expand ORF as open reading frames at first use. | 2 |
| Help menu, "Lungfish Genome Explorer Help, Cmd-?, Standard macOS" | One reader flagged that Cmd-? does not say whether Shift is needed, exactly the ambiguity the plus-and-minus paragraph had been careful about. | Say press Command, Shift, and the slash key. | 2 |
| Inside the genotype result window, "or when a call is selected in a haplotyped MiSeq result" | One reader found "haplotyped" itself unglossed as a separate stumbling block within the larger unclear condition. | Gloss haplotype and say plainly when the review shortcuts are live. | 2 |
| Inside the Workflow Builder, "no key binding matches those tooltips in this release" | One reader noted "key binding" is a new term introduced here for the same thing the chapter elsewhere calls a shortcut. | Use "shortcut," the term already established. | 2 |
| Customizing shortcuts, "the override takes effect the next time LGE launches" | One reader could not tell whether quitting and reopening LGE is required, or whether closing the window is enough. | Say plainly to quit LGE with Cmd-Q and reopen it. | 2 |
| Whole chapter, no shortcut index | One reader found no single alphabetical list of shortcuts, and had to scan ten sections to find one command. | Add a one-column list of every shortcut in key order at the end. | 2 |
| What it is, "The menu bar is the authority the reader can check in a second" | One reader was thrown by "the authority" as a metaphor and by the sudden third-person "the reader" after "you" everywhere else. | Say plainly the menu bar shows the current shortcut beside each command. | 3 |
| What it is, "its shortcut does nothing at all while it stays that way, so pressing it produces silence rather than an error" | One reader initially read "produces silence" literally, as the app playing no sound. | Say plainly that nothing happens and no message appears. | 3 |
| What it is, "Zoom In is dead until a sequence is on screen" and "Cancel All Operations is dead until something is running" | One reader's first pass read "dead" as broken or removed rather than as a metaphor for disabled. | Use "disabled" or "greyed out," the words already defined in the same paragraph. | 3 |
| View menu, "the app's own interface type is too small to read comfortably" | One reader read "interface type" as a category of interface before realizing "type" meant typeface. | Say "text" instead of "type." | 3 |
| View menu, "The Option key is what separates these three from the three viewport zoom commands that sit a few rows below them in the same menu" | One reader could not verify "a few rows below them" while reading a document rather than the app screen. | Say plainly that adding Option changes viewport zoom into text size. | 3 |
| Sequence menu, "The two Extract and Copy commands take the region the viewport is showing, or the range you have selected inside it" | One reader read "the two Extract and Copy commands" as naming four commands rather than two. | Name the two commands explicitly. | 3 |
| Sequence menu, "It takes Cmd-Opt-G rather than Cmd-G so that it does not collide with Find Next" | One reader stopped on "collide" as an unglossed metaphor. | Say plainly the two commands would otherwise share the same keys. | 3 |
| Tools menu, "Plugin Manager..., Cmd-Shift-B" | One reader found "plugin" itself never explained and no sentence saying what the Plugin Manager does. | Gloss plugin and add one sentence describing the Plugin Manager. | 3 |
| Inside the sequence viewport, "Left and right pan the view by a hundred bases at a time" | One reader read "pan" as a camera term and was not certain it meant sideways movement rather than zoom. | Say plainly the view moves left or right by 100 bases. | 3 |
| Inside the sequence viewport, "The Escape key does two jobs in one, and which one it does depends on what the viewport is busy with" | One reader had to decode two idioms before the actual rule arrived two sentences later. | State the rule first, then the two cases. | 3 |
| Inside the genotype result window, "This is the densest set in the app, because a person working through a plate is making one small judgement after another and reaching for the mouse each time is slow" | One reader found this design-reasoning sentence slowed their scan of the table just below it. | Cut the sentence, or move it after the table. | 3 |
| Inside the genotype result window, "Mark the selected sample reviewed" and "Mark the selected sample confirmed" | One reader could not tell the difference between reviewed and confirmed, and so could not choose between the two shortcuts. | Say plainly what each state means. | 3 |
| Inside the Workflow Builder, "the arrow keys to nudge a selected box one grid square at a time" | One reader still did not know what the Workflow Builder is for, since neither this nor its first mention describes its purpose. | Add one sentence saying what a workflow is. | 3 |
| Memorizing chords, "The Sidebar is the exception to all of it" | One reader could not tell which of the chapter's three patterns the Sidebar is an exception to. | Name the specific pattern the Sidebar breaks. | 3 |
| Next, "See CLI Reference for the equivalent command-line surface" | One reader, who had never opened a terminal, could not tell from "command-line surface" whether that chapter was meant for them. | Say plainly the chapter is for people who type commands instead of clicking. | 3 |
| What it is, "**Show Experimental Features** in **Settings > Advanced**" | One reader was unsure whether Advanced is a tab or a submenu, since this is the only path in the chapter written with an arrow. | Use one consistent style for menu paths. | 4 |
| View menu, "Show Sidebar, Cmd-Ctrl-S" | One reader could not tell from the text alone whether Cmd-Ctrl-S means pressing all three keys at once, since the explanation arrives only much later. | Say plainly it is Command, Control, and S pressed together. | 4 |
| Sequence menu, whole table (Geneious right-click habit) | One reader, used to reverse-complementing and translating from a right-click in Geneious, found no note saying whether LGE's bindings match or differ. | Note that these are LGE's own bindings and do not match other viewers. | 4 |
| Sequence menu, "It takes Cmd-Opt-G rather than Cmd-G so that it does not collide with **Find Next**" | One reader, having seen this one collision explained, assumed all bindings were checked, then lost confidence when the Workflow Builder section admitted a tooltip was simply wrong. | Say plainly whether every binding was checked for collisions. | 4 |
| Tools menu, "**Call Variants...**, **Workflow Library...**, **Haplotype Definitions...**" | One reader found three commands named with no gloss and no shortcut, leaving nothing actionable in the paragraph. | State plainly that none of these three has a shortcut, and gloss haplotype definitions. | 4 |
| Inside the sequence viewport, "Left and right pan the view by a hundred bases at a time, and up and down zoom in and out" | One reader, coming from viewers where up and down scroll rather than zoom, found this reversal stated but never flagged as unusual. | Flag explicitly that up and down zoom rather than scroll. | 4 |
| Inside the genotype result window, "Focus the quick filter search field, Cmd-F" | One reader expected Cmd-F to mean Find, as it does everywhere else in the chapter and in most Mac apps, with no warning of the collision. | State plainly that Cmd-F is taken over for the quick filter inside this window. | 4 |
| Inside a classifier result window, "The taxonomy sunburst... takes Escape to step out one ring" | One reader tallied four different jobs for the Escape key across four sections of the chapter, with nothing collecting them in one place. | Collect the Escape behaviours in one small table. | 4 |
| Whole chapter, missing Geneious expectations | One reader looked for four shortcuts they expected from Geneious, including add/annotate a feature, BLAST or database search, and zoom-to-selection, and found only the absence of Save explained. | Add a short list of common shortcuts LGE deliberately does not have. | 4 |

Eighty-seven merged rows total.

## Consensus

- All four readers learned the term "key equivalent" and never saw it used again anywhere in the chapter.
- All four readers found that the note about rows being read out of source code, not tested in the app, undermined their trust in the table.
- All four readers hit "software pack" with no gloss and no way to tell whether one is installed.
- All four readers found the equals-and-plus explanation contradictory or hard to place on their own keyboard.
- All four readers met "Provenance" in the File menu with no gloss.
- All four readers hit manifest-level, metadata, and bundle together in the Document Inspector sentence with no gloss.
- All four readers found Restore Side Panes and Focus Viewer named with no sentence saying what they do.
- All four readers found the AI Assistant listed with no description of what it does.
- All four readers could not tell what resetting to 10kb actually does.
- All four readers found no worked example for the Go to Location coordinate syntax.
- All four readers met "pipeline" with no gloss in the Operations menu description.
- All four readers hit "classifier" unglossed at the compact read viewer description.
- All four readers were stopped by "the shared code behind these" as unexplained programmer language.
- All four readers found the genotype result window's stated count of six shortcuts does not match its five-row table.
- All four readers could not tell when the four genotype review shortcuts are active, since Review lens and haplotyped MiSeq result arrive unglossed in one condition.
- All four readers could not tell whether marking a cell false positive or false negative changes the underlying data or can be undone.
- All four readers found the classifier result window's bracket-key shortcuts spelled out in words instead of shown as characters.
- All four readers found "chord" used throughout the Memorizing chords section without ever being defined.
- All four readers found the VoiceOver shortcut assumes VoiceOver is already on, with no explanation of how to turn it on.
- Three readers checked their own keyboard and found fn sits left of Control, contradicting the claim that Control is the leftmost key.
- Three readers had to reread "stronger still" to see it means the Workflow Builder item is hidden rather than greyed out.
- Three readers stumbled on "called" as a verb and on what a plate means inside the genotype window.
- Three readers met "deep region" with no gloss in the sequence viewport section.
- Three readers were stopped by "global bindings" as unglossed jargon in the sidebar context-menu description.
- Three readers counted the actual tables and could not reach the stated forty-five shortcuts.
- Three readers did not know how to type an ellipsis character for the Customizing shortcuts step.
- Three readers found the admission that a Workflow Builder tooltip does not match any real key binding undermined trust in the rest of the tables.
- Three readers found Full Keyboard Access and Keyboard Navigation given as two names for the same setting.
- Three readers were unsure how to type an arrow key as the last key of the Expand All shortcut, or whether the taxonomy tree needs focus first.
- Three readers found the focus requirement for sequence-viewport shortcuts explained only late in the chapter, well after earlier sections assumed it.
- Three readers could not tell whether the sidebar delete command removes an item from disk or only from the list, or whether it can be undone.

Thirty-one merged rows were hit by three or more readers.
