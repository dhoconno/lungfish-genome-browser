# Reader 3 — When to Assemble

Reader persona: pre-med student, English is my second language, strong biology
vocabulary from textbooks, slowed by idioms and long sentences, never opened a
terminal.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "An assembler looks for places where the end of one read matches the start of another" | One sentence carries four actions (look, build, walk, emit) across five clauses. I had to read it three times to see that "walks that graph" is a metaphor and not a real procedure I perform. | Split into three sentences and say the assembler traces paths through the graph. |
| What it is, "and it is the whole point" | Idiom. "The whole point" of what, the Latin phrase or the method? I could not tell which noun it refers back to. | Replace with "and that is what makes the method different". |
| What it is, "the fastest way to be disappointed by a good result" | Figure of speech that inverts twice (fastest way, disappointed, good). I understood it only after the next sentence. | Say plainly that a good assembly still usually has many contigs. |
| What it is, "Wherever the graph offered more than one way forward, the assembler stops rather than guessing" | I do not know what "more than one way forward" is in the data. Is it two reads that both match? The illustration does not show this branching. | Add one clause naming the cause, for example two different reads matching the same end. |
| What it is, "a repeated stretch longer than the reads can span" | "Repeat" is used here for the first time and never glossed, but it is central to the whole chapter and returns three more times. | Gloss repeat at first use as a sequence that occurs more than once in the genome. |
| What it is, "sequenced deeply" | "Deeply" is not explained here. Coverage is glossed two sections later, so at this point I have no way to know what deep means. | Move the coverage gloss forward or say "sequenced to high coverage, meaning many reads per base". |
| What it is, "as a `.lungfishref` assembly bundle inside a per-run folder" | I do not know what a file extension is or what a bundle looks like on disk. Is it a file or a folder? The word "bundle" suggests a folder but the dot-name suggests a file. | State in one clause that a bundle is a folder macOS shows as a single file. |
| What it is, "which is what makes an assembly usable as the input to everything downstream rather than as a dead end" | "Downstream" and "dead end" are both metaphors, in one sentence, and "everything downstream" names nothing concrete. | Name the two or three things, for example mapping and variant calling. |
| Why you would do this, "So is an organism that has drifted so far from its nearest deposited relative" | "Deposited" is jargon for submitted to a public database. Also the sentence begins with "So is", which I did not parse until I found the earlier "The first is". | Write "an organism whose closest relative in a public database is too distant". |
| Why you would do this, "mapping discards most of the reads as unalignable" | I cannot judge "most". Is that 51% or 90%? The later section gives "more than half", so the two numbers disagree in feel. | Use the same threshold in both places. |
| Why you would do this, "exactly what makes it fast and exactly what makes it lossy" | "Lossy" is a computing term I have never met in biology. The repeated "exactly what makes it" construction made me reread to find the contrast. | Gloss lossy as "some information is thrown away". |
| Why you would do this, "An insertion your sample carries and the reference does not has nowhere to go" | Three verbs in a row ("does not has nowhere to go") reads as a grammar error to me even though it is not. I stopped twice. | Recast as "If your sample carries an insertion that the reference lacks, mapping has no place to put it." |
| Why you would do this, "as pairs landing implausibly far apart" | Paired-end is not glossed until the HG002 section, much later. Here I do not know what a pair is, so this evidence means nothing. | Gloss paired-end at this first mention instead. |
| Why you would do this, "feeds variant calling directly" | Variant calling is never explained anywhere in this chapter and is not in the glossary list at the top. It appears twice. | Gloss it once as finding the positions where the sample differs from the reference. |
| Why you would do this, "Reach for it when the reference-based route genuinely cannot answer your question" | "Reach for it" is an idiom. Also "the reference-based route" is a new name for mapping introduced without warning. | Write "Use assembly only when mapping cannot answer your question." |
| What LGE ships, "at roughly 950 MB installed" | I cannot judge whether 950 MB is large. Is this a long download on a normal connection? Do I need free disk space checked first? | Add one clause on download time or disk requirement. |
| What LGE ships, "install it first from **Tools > Plugin Manager...** (Cmd-Shift-B)" | I cannot perform this step from the text. It does not say what I click inside the Plugin Manager, how I find Genome Assembly in it, or how I know when the install has finished. | Add the two or three clicks inside the Plugin Manager, or link to the plugin chapter. |
| What LGE ships, "You do not have to remember to check." | This sentence contradicts the previous one, which told me to install it first. I read the paragraph twice trying to decide whether installing first was required or optional. | Merge the two so the Readiness panel is presented as the check, not as a reason not to install. |
| What LGE ships, "The versions are pinned rather than tracking whatever is newest" | "Pinned" is software jargon. I guessed from the next clause but was not sure. | Gloss as "fixed to one version". |
| What LGE ships, "Select the reads you want to assemble in the project sidebar first" | I do not know how to select reads in the sidebar or what a reads item looks like there. This is the only instruction and it assumes I already know. | Say what to click, for example the FASTQ bundle under Imports. |
| What LGE ships, "Two assemblers people often ask after are absent." | "Ask after" is an idiom I did not know. I first read it as "ask, afterwards". | Write "Two assemblers are missing that people often expect." |
| What LGE ships, "having been largely overtaken by Flye for Nanopore work" | "Nanopore" is used here before the chapter explains what Oxford Nanopore is, which happens two sections later. | Introduce the three read classes before this paragraph. |
| Catalogue table, "around 10 Mb" and "no practical ceiling" | I do not know how many Mb my sample is, so the whole column cannot be applied. Nothing tells me where to find a genome size. | Add one line saying published genome sizes are on the NCBI record, with one example. |
| Catalogue table, "where a conservative assembly matters more than a contiguous one" | "Conservative" and "contiguous" are both used as technical opposites here, and neither is defined until the third question section. | Gloss both in the table caption or reorder the sections. |
| How the sheet decides, "narrows the Assembler picker to the tools that accept that class" | I do not know what a picker is, or what "segmented" means in the screenshot caption. This is my first LGE chapter word for a control. | Say it is a row of buttons where one is selected. |
| How the sheet decides, "reads whose headers were rewritten by an earlier processing step" | I do not know what a FASTQ header is or that anything rewrites it. This is offered as a common case, so I feel I should recognise it and I do not. | Gloss the header as the first line of each read record and name one tool that rewrites it. |
| How the sheet decides, "Hybrid assembly is not supported in v1." | "v1" is not explained. The chapter elsewhere says "this version" and "this release", so I could not tell whether v1 is LGE version 1 or something else. | Use one consistent phrase for the version. |
| How the sheet decides, "a run mode picker appears, and it is currently locked to running each bundle as its own separate assembly" | If it is locked to one choice, why is it shown? I could not tell whether I must do anything with it. | Say plainly that the picker is visible but not changeable yet. |
| Working out which, "somewhere above roughly 95% identity across most of the reference's length" | Two vague quantifiers stacked ("somewhere above roughly", "most of"). I also do not know how I would measure identity before I have mapped anything. | Say where this number comes from, for example a BLAST percent identity. |
| Working out which, "If the nearest database hit is a different genus" | "Hit" is jargon. Also I do not know how to find the nearest database hit at this stage. | Gloss hit as the best match returned by a search, and name the search. |
| Working out which, "with a per-base error rate of a few percent" | I cannot judge whether a few percent is bad. Compared with Illumina, which is not given a number here, I have nothing to compare against. | Give the Illumina error rate too, so the three classes can be compared. |
| Working out which, "The second question overrules it" | The three questions are numbered by words (first, second, third) and asked in bold, but the worked example refers to them out of order, so I had to scroll up and count. | Number the questions 1, 2, 3 and refer to them by number. |
| Two assemblers, "thinned to about 300-fold coverage" | "Thinned" and "300-fold" are both new. I know coverage now, but not what thinning is or why 300 was chosen. | Say reads were removed at random to reach 300 reads per base. |
| Two assemblers, "9,958 paired-end read pairs" | I could not tell whether this is 9,958 reads or 19,916 reads. The phrase "read pairs" after "paired-end" doubles the word and confused me. | Give both numbers once. |
| Two assemblers, "in 13.7 seconds" and "in 1.6 seconds" | I cannot judge these. On what computer? My laptop may be much slower and I would not know if something is wrong. | Name the machine class once. |
| Two assemblers, "The interesting part is the 127-base difference between them." | The arithmetic does not work for me. 16,697 minus 16,570 is 127, but the next sentences say SKESA is one base over and SPAdes 128 over, and I had to do three subtractions to see all three numbers are consistent. | State the reference length in the same sentence so the three differences line up. |
| Two assemblers, "SPAdes overshot by 128 bases, about 0.8%" | 128 of 16,569 is 0.77%, which rounds to 0.8, but I checked it because "about" plus a one-decimal figure felt imprecise. | Keep, but say 0.8% of the genome length so the denominator is explicit. |
| Two assemblers, "the sequence either side of that cut is easy to emit twice" | "Emit" appears three times in the chapter and is never glossed. I know it only from physics. | Use "write out", which the chapter already uses elsewhere. |
| Two assemblers, "labels its contig `[topology=circular]` in the FASTA header" | I do not know where to see a FASTA header in LGE. The viewport section later mentions a header field but does not connect back to this. | Say which pane shows it. |
| Two assemblers, "reading a 0.8% overshoot as a real insertion is a mistake worth not making" | Double negative in an idiom. I understood the warning only on second reading. | Write "do not read a 0.8% overshoot as a real insertion". |
| Two assemblers, "capping it to two threads and disabling its hardware acceleration" | "Threads" and "hardware acceleration" are computing terms with no gloss, and I am told LGE already does this, so I cannot act on it anyway. | Cut the detail or gloss threads in three words. |
| Two assemblers, "If a MEGAHIT run stops with a nonzero exit code" | "Nonzero exit code" is terminal vocabulary. I have never opened a terminal, so I do not know where I would see this. | Say what the failure looks like in the Operations panel. |
| What the numbers mean, "the length of the contig you are standing on" | The walking metaphor ("walk down the list", "standing on") carried the whole definition, and I had to read the paragraph twice to convert it into a rule. | Add one plain sentence stating the rule without the metaphor. |
| What the numbers mean, "a drift of short junk contigs barely moves it" | "A drift of" is an unusual collective noun and "junk" is informal. I did not know if junk contigs are a defined category. | Write "many short contigs". |
| What the numbers mean, "An N50 of 50 kb on a bacterial chromosome of 5 Mb is a perfectly ordinary short-read result." | kb and Mb are used without being defined anywhere in the chapter, and the table uses Mb too. | Define both units once at the first Mb. |
| Where the result lands, "for example `spades-2026-09-07T05-14-22`" | I could not read this timestamp. The T and the hyphens between the time parts are not a format I have seen. | Add "year, month, day, then the time". |
| Where the result lands, "There is no `Assemblies/` folder in an LGE project." | This tells me what does not exist, which only helps if I expected it. As a new reader I found it confusing rather than clarifying. | Cut, or say where people wrongly look. |
| Where the result lands, "total bp" | "bp" is not expanded here, though "bases" is used everywhere else in the chapter. | Use "bases" or expand once. |
| Where the result lands, "a preview of the sequence" | I do not know how much of the sequence a preview shows, so I cannot tell whether I need the detail pane. | Say roughly how many bases. |
| Where the result lands, "while SPAdes and MEGAHIT happen to write theirs longest first, Flye and hifiasm make no such promise" | The chapter writes Hifiasm capitalised in the menu and table but hifiasm lowercase here and in the frontmatter. I thought they were two tools. | Use one spelling. |
| Where the result lands, "with runs of `N` characters standing in for the gaps" | I do not know what N means in a sequence. The chapter never says it is an unknown base. | Gloss N as an unknown base. |
| Where the result lands, "LGE builds the bundle from the contigs rather than the scaffolds, so what you browse in the viewport is the unscaffolded set." | Then I cannot tell what the scaffold file is for or whether I should ever open it. The paragraph raises scaffolds and drops them. | Add one sentence saying whether the scaffold file is useful. |
| Where the result lands, "minimap2, BWA-MEM2, Bowtie2, or BBMap" | Four tool names appear with no gloss and no guidance on which to choose. | Say they are covered in the mapping chapter and link it. |
| Where the result lands, "it appears only on bundles that carry a record of the assembly that produced them" | I cannot tell which of my bundles carry such a record, so I cannot predict whether Reassemble will be there. | Say which bundles lack it, for example imported ones. |
| What good looks like, item 2, "in the right neighbourhood for the genome you expected" | Idiom, and no number. This is the check I most wanted to be able to perform and I cannot. | Give a tolerance, for example within 10% of the published length. |
| What good looks like, item 3, "landing in the tens to low hundreds of contigs is ordinary" | "Low hundreds" is vague after the chapter promised concrete comparisons. I do not know if 400 is still ordinary. | Give a single number as the boundary. |
| What good looks like, item 4, "after a run where the picker changed under you" | "Changed under you" is an idiom that suggests something went wrong, but the chapter earlier presented the narrowing as a helpful feature. The tone confused me. | Write "after the picker narrowed its choices". |
| What good looks like, "suspect coverage before the assembler" | Elliptical. "Suspect" needs an object twice and only gets it once, so I read it as suspecting a person. | Write "check coverage before blaming the assembler". |

Three closing lines.

One thing I learned. N50 and L50 are the same news said two ways, N50 going up
and L50 going down.

One thing I still could not do. Install the Genome Assembly plugin pack and
select my reads in the sidebar, because neither step is written out.

The sentence I liked most. "A circular genome has no beginning, so an assembler
walking a circular graph has to cut it somewhere to write out a linear contig."
