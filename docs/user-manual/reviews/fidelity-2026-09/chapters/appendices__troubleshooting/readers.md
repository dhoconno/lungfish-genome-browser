# Merged reader report, appendices/troubleshooting

Four readers reviewed this chapter. Reader 1 was a sophomore fresh from an intro genetics course with no lab time and no terminal experience. Reader 2 was a senior with two years of wet lab pipetting experience who had never analyzed data or opened a terminal. Reader 3 was a pre med student reading English as a second language who had never opened a terminal. Reader 4 was an undergraduate who had used Geneious for one semester and expected every operation to have a dialog. Reader 1 raised 78 rows, reader 2 raised 76 rows, reader 3 raised 63 rows, and reader 4 raised 56 rows, for 273 source rows in total.

| Location | Issue | Suggested fix | Readers |
|---|---|---|---|
| What it is, "pwd" and pressing Return, and Tools and databases, the check-tools diagnostic | The chapter never tells a reader they need to open Terminal before typing a command, and never names the app or where its typing area is, at either of the two places it first asks the reader to type something. | Add one sentence early on saying to open Terminal (in Applications > Utilities) and where to type, before the first command the chapter offers. | 1, 2, 3, 4 |
| What it is (Preview 2026.9.13), A run stopped (symlink/tmp), and Tools and databases (check-tools output, tool-row count, download size, CPU/memory) | Numbers and facts describing the machine that wrote the appendix (its macOS version, eighteen tool rows, a 157.3 MB download, 14 CPU cores, 48 GB of memory) read as targets the reader's own machine should match, with nothing saying these are one example machine's results rather than a number to match. | Say plainly, once, that these are one test machine's example output and that a reader's own numbers will differ without that meaning something is wrong. | 1, 2, 3, 4 |
| I cannot write to my project, "Confirm that session has genuinely ended before you clear the lock" / "remove the lock as Shared Projects describes" | Clearing a project lock is named as the needed action up to three times in this section and is never actually described here, only deferred to another chapter, leaving the reader stuck mid-emergency with a read-only project and no route forward. | Give the actual steps to clear a lock in this section, or at minimum name the exact menu item and chapter so the reader is not left to guess. | 1, 2, 3, 4 |
| Start here, "take the report the panel offers you", A command refused me (section heading), and Next (CLI Reference pointer) | Several sections are command-line only with no line at the top saying so, so a window-only reader reads through commands looking for something they can act on, and the only pointer to the CLI Reference for command syntax arrives at the very end of the chapter instead of the start. | Open each command-line-only section with one line saying so, and move a pointer to the CLI Reference into the opening section. | 1, 2, 3, 4 |
| What it is, "a window over a set of established command-line programs" | The opening sentence assumes the reader knows what a command-line program is, before anything explains that LGE runs other programs on the reader's behalf and shows the results, without the reader ever touching those programs directly. | Add one sentence saying the reader never has to run those programs themselves. LGE runs them and shows the results. | 1, 2, 3, 4 |
| What it is, "the symptoms that actually appear in Preview 2026.9.13" | Nothing near the opening tells the reader how to check their own app version, so they cannot judge whether this appendix even applies to them. The pointer to the About window arrives only in the Reporting section at the very end. | Name the About window here, at the opening, not only in the Reporting section. | 1, 2, 3, 4 |
| Start here, "take the report the panel offers you" | The instruction to take the report is vague about where to take it or what to do with it next, and the actual menu item, Copy Failure Report, is only named a paragraph or more later. | Name Copy Failure Report directly in this sentence. | 1, 2, 3, 4 |
| Start here, "LGE writes it under `~/Library/Logs/`" | The tilde is unexplained and Library is a hidden folder in Finder, so a reader following this path by hand finds nothing. The chapter offers Reveal Failure Report in Finder only a sentence away but still prints the unusable path. | Gloss the tilde as the home folder and note Library is hidden, or simply point to Reveal Failure Report in Finder and drop the raw path. | 1, 2, 3, 4 |
| Start here, "The store keeps the most recent reports and prunes older ones" | No number is given for how many reports are kept or how long one survives, so the reader cannot judge how much time they have before their own report is deleted. | Give the count of reports kept, or say roughly how long one survives. | 1, 2, 3, 4 |
| Nothing happens, "Select a reference bundle under `Reference Sequences/`" | The reader cannot perform this step: they do not know whether Reference Sequences is a Finder folder or a sidebar location, how a reference bundle gets there, or how it differs from the bundle kinds just listed. | Say where in the app window this folder appears, and point to the chapter that creates a reference bundle. | 1, 2, 3, 4 |
| I cannot write to my project, "an advisory lock, meaning it works because every cooperating program checks it" | The gloss explains the mechanism behind an advisory lock rather than its consequence, so the reader learns why it is advisory but not what changes for them or whether their data is safe. | Lead with the consequence: a program that ignores the lock can still write and overwrite files. | 1, 2, 3, 4 |
| I cannot write to my project, "names a user, a host, and a process id" | Host and process id are both unglossed, so the reader does not know what to do with either number once they read them, or how to use them to identify which session to close. | Gloss host as the computer name and process id as a number identifying the running program, and say what they tell the reader about which session to check. | 1, 2, 3, 4 |
| I cannot write to my project, "look for stray files whose names begin with `._`" | The reader cannot see files beginning with a dot in Finder and, even if they could, is never told what action to take once one is found. | Say how to reveal hidden files if needed, and state plainly what action to take on one, for example that it is safe to delete or safe to ignore. | 1, 2, 3, 4 |
| A run stopped, "`Empty Kraken2 report`, exit status 64" | Kraken2 is named with no gloss, and this entry's exit status 64 reads as contradicting a later paragraph that says 64 means the command stopped before doing any work, when here it is attached to a run that finished and matched nothing. | Gloss Kraken2 as the read classification program at first use, and reconcile the two readings of exit status 64 or explain why this case reuses it differently. | 1, 2, 3, 4 |
| A run stopped, "MEGAHIT 1.2.9 fails most runs on Apple Silicon in this release, with both shipped workarounds active" | The reader cannot judge most runs, does not know whether their own Mac is Apple Silicon, cannot check whether the mentioned workarounds are active, and is given no sense of how many retries are reasonable before giving up. | Say how to check the Mac's chip, give a rough failure rate or number of reasonable retries, and cut the workaround clause, which is aimed at a developer. | 1, 2, 3, 4 |
| A run stopped, "the two spellings differ across the symlink macOS keeps between `/tmp` and `/private/tmp`" / "Your working directory sits under `/private/tmp`" | Symlink stays unexplained even when linked to the glossary, the reader never chose a working directory so cannot tell whether this cause applies to them, and the one usable fix arrives only in the last sentence. | Lead with the fix, that the run must not live in a temporary folder, say what in the app causes a working directory to end up there, and state the rule before the cause. | 1, 2, 3, 4 |
| A run stopped, "reaches about 84 percent and stops" | The reader cannot judge whether their own run, stopping at a different percentage, is this same problem or a different one, since no margin or identifying detail is given. | Say the percentage is approximate and that the message text, not the percentage, is what identifies this failure. | 1, 2, 3, 4 |
| A run stopped, "Nextflow keeps a cache that needs file locks" | Nextflow, cache, and file locks all arrive unexplained in one or two sentences, and the reader cannot tell whether they are even using Nextflow. | Gloss Nextflow at first use as the pipeline runner LGE uses for some workflows, and drop or simplify the cache mechanics. | 1, 2, 3, 4 |
| A run stopped, "a drive formatted exFAT is the usual cause" | The reader does not know how their external drive is formatted or how to check. | Say how to check the drive format in Finder's Get Info, or give the one step for checking. | 1, 2, 3, 4 |
| A run stopped, "Three means a pack name was not recognised" / "Ten means `tools update --plan` found pending work" | Several exit-status numbers are given in prose with no anchor to where or whether a window-only reader would ever see one, and pack is used before plugin packs are explained two sections later. | Turn the list into a small table, say where a number would actually appear for a window user, and move any number that only a script would care about out of this list. | 1, 2, 3, 4 |
| A command refused me, "The subcommand's own `--format` flag collides with the global one, so this command cannot be run in this release." | The explanation of why the command fails arrives before the plain verdict that it is simply broken, so the reader reads the whole sentence before learning the one fact that matters. | Lead with the fact that this command cannot be used in this release, then give the explanation if needed. | 1, 2, 3, 4 |
| A command refused me, "Convert it to VCF 4.x with `bcftools convert` or with vcftools' `vcf-convert`" | Both programs are outside tools the reader does not have, and the text never says whether LGE ships them or where to get them. | Say whether these tools come from LGE or must be installed separately, and where to get them if not. | 1, 2, 3, 4 |
| A command refused me, "Build per-sample `.lungfishfastq` bundles with a FASTQ import recipe" | Recipe is a new term here, never explained anywhere else in the chapter, so the reader cannot start this step. | Gloss recipe, or link to the chapter that shows how to run an import recipe. | 1, 2, 3, 4 |
| The run finished but, "On a Genotype only run the value is recorded in the run statistics and never applied as a filter." | This is the most alarming row in the chapter: a setting the reader configured in a dialog looks like it worked and silently does nothing, and the sentence does not say up front which runs are Genotype only. | State the risk in the first clause, that Min Reads has no effect on this run type, and say what makes a run Genotype only. | 1, 2, 3, 4 |
| The run finished but, "Read the read-count column of the report's Long Summary sheet and set aside the thin rows yourself." | Thin is not a number, so the reader cannot judge which rows to remove, which is the whole task they came here for. | Give a starting threshold, even a rough one, and say what it depends on, for example the assay. | 1, 2, 3, 4 |
| The run finished but, "read the field you need out of the provenance sidecar with a JSON reader" | The reader has no JSON reader, does not know what one is, and is not offered a concrete way to open the file. | Name a concrete way to open it, for example that a text editor will do, or mark this option as for scripting users only. | 1, 2, 3, 4 |
| Tools and databases are missing, "which makes it usable as an assertion in a script" | Assertion and script are programming vocabulary aimed at someone who writes scripts, not the reader. | Move the scripting note to the CI chapter it already links, or mark the sentence as for scripting users. | 1, 2, 3, 4 |
| Tools and databases are missing, "Setting `HTTPS_PROXY` in the shell that launches LGE fixes it" | The reader cannot perform any part of this: proxy, shell, and setting an environment variable are all unfamiliar, no syntax is given, and the reader does not know whether their own network even uses a proxy. | Give the exact line to type, and say to ask IT whether a proxy is in use on the reader's network. | 1, 2, 3, 4 |
| Containers and pipelines, "containers, which are isolated environments holding a whole operating system's worth of software" | The gloss arrives after the heading has already used container and pipeline, and pipeline itself is never explained even though it carries the section heading. | Gloss both terms in the heading paragraph's first clause. Say a container packages a program with everything it needs, and a pipeline is a chain of tools run one after another. | 1, 2, 3, 4 |
| Containers and pipelines, "switch a Nextflow run to a local `.nf` file or to a conda executor" | Neither route named here is shown in the window, and executor is a new word, so the reader cannot actually take the action this row asks of them. | Point to the chapter and dialog where the executor or a local file is chosen, and gloss executor. | 1, 2, 3, 4 |
| Is this file or bundle intact, "`--strict` raises the bar" | The reader cannot judge when to use strict mode because the extra checks it adds are never named. | Say what strict mode additionally rejects. | 1, 2, 3, 4 |
| Is this file or bundle intact, "detects a record that changed after it was signed" | The longest sentence in the chapter buries its own upshot, that the command is useless on an ordinary unsigned sidecar since signing is off by default, arriving only in a final subordinate clause. | Split the sentence, and lead with the fact that most records are unsigned, so provenance verify does nothing unless signing was turned on. | 1, 2, 3, 4 |
| What it is, "A working directory is the folder your Terminal is sitting in" | The gloss defines working directory using Terminal, which has not been introduced and which a window-only reader has never opened, so the gloss explains one unfamiliar word with another. | Say in one sentence that Terminal is the macOS app for typing commands, and where to find it, before using it in a gloss. | 1, 2, 4 |
| What it is, "An exit status is the number a command hands back" | Exit status is defined immediately, but the reader is never told where they would actually see one. A failed run shows as a red row, not a number, so the definition cannot be used until much later, if ever. | Say here where an exit status appears, for example in the failure report, or that a window user may never see one directly. | 1, 2, 4 |
| Start here, "Click its disclosure triangle" | Disclosure triangle is a macOS interface term the reader has never met and cannot picture on the row. | Gloss it as the small arrow at the left edge of the row that opens it. | 1, 2, 3 |
| Nothing happens when I choose a menu item, "Three menu items in this release are inert" | Inert is an unfamiliar word for software behavior, more familiar from chemistry, and readers had to reread the line to confirm it meant nothing happens when clicked. | Say plainly that they do nothing when clicked. | 1, 2, 3 |
| Nothing happens, "Genotyping workflows are specialized workflows, off until you enable them" | The sentence defines specialized workflows by repeating the category name rather than explaining it, so the reader learns nothing about what genotyping is, why these are off by default, or what turning them on costs. | Add a half-sentence gloss of genotyping at first use, and say why these workflows are off by default, for example that they need a large install. | 1, 2, 3 |
| Nothing happens, "If the card says Needs install, click **Install Dependencies** first." | Dependencies is unexplained, and no size or duration is given, so a reader on limited wifi or before class cannot judge whether to start the install. | Gloss dependencies as the outside programs the workflow needs, and give a rough size or time, or say the panel shows progress. | 1, 2, 3 |
| Nothing happens, "The Assistant tab appears only when a genomics bundle is loaded" | Bundle is used here for the first time with no gloss, though it recurs throughout the chapter as reads bundle, result bundle, reference bundle, and FASTQ bundle, and the reader never learns the general term. | Gloss bundle at first use as a folder LGE manages that holds a dataset and its records, or link it to the glossary at this first use. | 1, 2, 3 |
| I cannot write to my project, "Lock failures surface against `.lungfish/project.lock`" | Paths beginning with a dot are invisible in Finder, and the reader is never told these are hidden files that they do not need to find themselves. | Say these are hidden files and that the reader does not need to open them directly. | 1, 2, 4 |
| I cannot write to my project, "network storage" and "how a share is mounted" | Network storage is never glossed, so the reader cannot tell whether their own shared lab drive or plugged-in USB drive counts, even though the whole paragraph only matters if it does. | Gloss network storage as a drive that lives on another computer reached over the network, and say a plugged-in USB drive is not one. | 1, 2, 3 |
| A run stopped, "Try a broader database" | The reader cannot judge broader without knowing what database they used, what a database means in this context, or which of the listed collections is actually broader. | Name the broader database directly, or say the recommend command prints options largest first, and gloss database as the reference collection the classifier compares reads against. | 1, 3, 4 |
| A command refused me, "Zip the bundle folder by hand to move it." | A bundle looks like a single file in Finder to the reader, so they cannot tell which folder to compress or find the bundle folder at all. | Say a bundle is a folder that Finder shows as one item, and say how to find or reveal it, for example via Reveal in Finder. | 1, 2, 3 |
| A command refused me, "prints a deprecation notice" | Deprecation is never glossed at first use, though its plain meaning appears in a nearby cell. | Gloss deprecated as still working but scheduled for removal, at its first use. | 1, 2, 3 |
| Tools and databases are missing, "Most LGE operations run out of per-tool environments" / "so that two tools needing different versions of the same library can coexist" | Environment is used before it is explained, and the engineering reason for per-tool environments is explained in a symptom lookup where the reader does not need it. | Gloss environment as a private folder of software for one tool, and cut or move the engineering explanation to a footnote. | 1, 2, 3 |
| Is this file or bundle intact, "read the sidecar's `exitStatus` field and check that the declared outputs exist instead" | The reader is not told where the sidecar file is or how to open it. | Say where the sidecar sits relative to the result, and how to open it. | 1, 2, 3 |
| Tools and databases, "`waiting for conda lock held by pid <n>`" | The angle-bracket placeholder confused the reader, who did not know whether the literal characters would appear on screen, and pid itself is not spelled out. | Say a number appears in place of n, and spell out process id. | 2, 3, 4 |
| Is this file or bundle intact, "the underlying tools are `samtools faidx`, `samtools index`, and `tabix`" | The reader cannot perform this manual route and does not know when they would ever need to, since the paragraph opens by saying the rebuild happens automatically. | Say this manual route is only needed if the automatic rebuild fails, and mark it as for command-line users. | 2, 3, 4 |
| Start here, "Cmd-Shift-P" | The chapter uses this keyboard-shortcut notation without ever explaining whether the keys are held together or pressed in order. | Write the shortcut once in full, for example hold Command and Shift and press P, or define the notation once earlier in the manual. | 1, 2 |
| Nothing happens, "The Workflow Builder is experimental and hidden by default" | Experimental is never defined, so the reader cannot tell whether it means unfinished, unpolished, or actually risky to their data. | Say in one clause what experimental means for a reader's results. | 1, 2 |
| Nothing happens, "A reads bundle, an assembly, a mapping result, a classifier result, or a genotype result" | Four or five unglossed kinds of result or bundle arrive in one sentence, whose whole point is which of them do not work, leaving the reader unable to tell which one they have. | Gloss or link each kind, or cut the list and gloss the category instead of the members. | 1, 4 |
| A command refused me, "These are refusals rather than crashes." | The distinction between a refusal and a crash does not help the reader, who cannot picture what either looks like in this app. | Say plainly that a refusal means nothing ran and nothing was changed. | 1, 3 |
| A command refused me, "at least two input FASTQ bundles are required" | FASTQ is never explained anywhere in the chapter even though it appears in several places, and bundle is unglossed here too. | Gloss FASTQ at first use as the file format holding sequencing reads and quality scores, and gloss bundle at its first use in this chapter. | 1, 2 |
| A command refused me, "Install both from the Plugin Manager" | This row has a clear, complete window route the reader could actually follow, naming the tab and the shortcut. | None needed. Keep as the model for other rows that lack a window route. | 1, 4 |
| A command refused me, "it also rejects a symlink or hard link that points back at the input" | Symlink and hard link are named again here though neither matters for the actual fix, which is simply to give the output a different name. | Cut the clause, since the simpler fix already covers it. | 1, 2 |
| The run finished but, "the cohort-level counts LGE builds are never shown" | Cohort is used throughout this section and never explained. | Gloss cohort at first use as the group of samples compared together. | 1, 3 |
| The run finished but, "You import a BigWig or BigBed file and nothing renders" | Both file formats are unexplained, and renders is app jargon the reader has to guess at. | Say briefly what the two formats are used for, and say nothing appears in the window rather than nothing renders. | 1, 2 |
| The run finished but, "Assembly accessions beginning `GCF_` or `GCA_` go through the assembly database" | The reader does not know what an accession is or the difference between an assembly database and a nucleotide database, and this row is entirely about that difference. | Gloss accession as the ID number a public database gives one sequence, and give one plain example of each kind. | 1, 2 |
| The run finished but, "Check the sequence name in the bundle that came back before you use it." / "`fetch genome` returns a record under a different sequence name" | The reader is not told where in the window a sequence name is shown, or whether a returned sequence under a different name is a wrong organism or just a renamed one, so the harm and the required check are both unclear. | Say where the name appears in the window, and say plainly whether the returned sequence is a different organism or the same one renamed. | 1, 3 |
| Tools and databases are missing, "`conda root is read-only; reinstall as the admin user`" | Admin user is unglossed, and a reader on a shared or lab computer does not know whether they are one or how to become one. | Say how to check, or say to ask whoever administers the computer. | 1, 2 |
| Containers and pipelines, "Add `--pull-test` to check that the machine can actually fetch an image" | Image is unglossed and is not the everyday meaning of the word. | Gloss image as the packaged copy of a tool that gets downloaded. | 1, 2 |
| Is this file or bundle intact, "LGE rebuilds a `.fai`, a `.bai`, or a `.tbi`" | Three file extensions arrive with no explanation of what an index file is or why it matters. | Gloss index file as a companion file that lets LGE jump into a large file quickly. | 1, 2 |
| Reporting something, "the app version, which the **Lungfish Genome Explorer > About Lungfish Genome Explorer** window shows" | This is the first place the chapter tells the reader how to find their own version, arriving at the very end, long after the opening paragraph told them which release the appendix documents. | Move a pointer to the version check into the opening section. | 1, 4 |
| Reporting something, "Leave your project's data files out of the report." / "Add the installed tool versions from `lungfish-cli version --tools`" | Good advice, but the reader cannot tell whether the automatically generated failure report is already safe to send, since it includes a log of unknown contents, and cannot tell whether the separately requested tool-versions addition is optional. | Say whether the generated report is already safe to send, and say whether adding tool versions is optional or give a window route for it. | 1, 2 |
| The run finished but, "Long Summary sheet" | Long Summary sheet is named as if the reader has already seen it, with no indication whether it is a tab in the app or an exported spreadsheet. | Say it is a sheet in the exported workbook. | 2, 4 |
| The run finished but, "Take the whole-run depth judgement from `lungfish-cli genotype list-samples`" | The only route offered for this judgement is a terminal command, and the text never says whether the window hides this permanently or whether any partial in-window route exists. | Say whether any in-window route exists, even a partial one, or state plainly there is no window equivalent in this release. | 2, 4 |
| The run finished but, "Take the whole-run depth judgement from" (depth) | Depth is used as a judgement term with no gloss, and the reader's other meanings for the word, from coursework or bench work, do not resolve which sense is meant. | Gloss sequencing depth, as reads per position, at its first use in this chapter. | 2, 3 |
| Tools and databases, "so that two tools needing different versions of the same library can coexist" | This clause explains an engineering reason the reader does not need in a symptom lookup, and library in the software sense collides with the reader's own sense of a sequencing library. | Cut or move this clause to a footnote, and gloss library in the software sense if it is kept. | 2, 3 |
| Start here, at the failed row / Whole chapter | No entry anywhere states plainly that a failed run shows as a red row, so a reader who arrives with a red row has to infer the match themselves. | Say in the opening that a failed run shows as a red row. | 2, 4 |
| Start here, at the failed row, "right-click its row" | A trackpad with no right button cannot perform this, the first action the chapter asks of the reader, and no alternative gesture is given. | Add the two-finger click or Control-click equivalent once, here. | 1 |
| Start here, "the operation title, the command, the error message, the error detail, and the log" | Error message and error detail read as the same thing, and the reader had to reread the sentence to guess at a difference. | Say in half a sentence how detail differs from message, or drop one of the two terms. | 1 |
| Start here, "Do not reconstruct the command by hand from what you remember choosing in a dialog" | The reader would never have thought to reconstruct a command by hand, so the warning implied it was a normal step they had missed. | Cut the warning, or move it to a note rather than the main instruction. | 4 |
| Nothing happens, "with `(not enabled)` after its name" | This row has a complete window route the reader could actually follow, the only one of its kind in this section. | None needed. Keep as the model for other rows in this section. | 4 |
| Nothing happens, "Turn on Show Experimental Features in Settings > Advanced" | Settings is not in the Tools menu the surrounding row is about, and the chapter never says Settings lives under the app menu or behind Cmd-comma, so the reader had to hunt for it. | Give the full menu path from the app menu to Settings. | 4 |
| Is this file or bundle intact, "Missing index files regenerate on their own" | This is the one place in the chapter that tells the reader plainly not to worry, and the reader believed it. | None needed. Keep as the model for reassurance elsewhere in the chapter. | 4 |
| Reporting, "Open GitHub Issue on the failed row's right-click menu" | This is a real, complete window route, and the promise that nothing is filed without the reader's own action was reassuring. | None needed. Keep as the model for this kind of route. | 4 |
| Start here, at the failed row, "in a folder named for the build" | The reader cannot find this folder by hand because its exact name, the build identifier, is never given. | Give the folder name, or say plainly the exact name does not matter because Reveal in Finder opens it directly. | 1 |
| Nothing happens, "find the workflow's card under **Specialized Workflows** in the **Genotyping** group" | Card is unglossed and three nested location words arrive in one sentence, leaving the reader unable to picture the window's layout. | Gloss card as the box for one workflow, and consider a screenshot of the Workflow Library. | 1 |
| I cannot write to my project, "LGE takes a lock on a project" | Project is never defined even though the whole section depends on the word. | Gloss project at first use as the folder LGE keeps one piece of work in. | 1 |
| I cannot write to my project, "or a command-line run and a window" | A reader who only clicks cannot tell whether this cause, a command-line run holding the lock, applies to them at all. | Say plainly that this cause only applies if the reader or someone else ran lungfish-cli. | 1 |
| A run stopped, "A message about a provenance publication artifact" | Provenance and artifact are both unglossed, and the symptom is described so vaguely the reader cannot match it against actual text on their screen, which is what the section promises. | Quote the actual message the way the other rows in this section do. | 1 |
| Tools and databases are missing, "which is the exact list of tool versions one LGE release was built and tested against" | The definition of dependency set is buried in the middle of a sentence about a different command. | Give dependency set its own short sentence. | 1 |
| Tools and databases are missing, "`lungfish-cli conda db recommend` to see what suits this machine" | This command is described differently than it was earlier in the chapter, and the reader cannot judge what its output is based on. | Say plainly and consistently what the recommendation is based on, for example free disk space. | 1 |
| Reporting something, "**Open GitHub Issue** on the failed row's right-click menu" | GitHub is unexplained, and a reader without an account does not know whether this route is open to them at all. | Say an account is needed, and name the alternative for a reader without one. | 1 |
| Start here, "the command LGE built and the running log" | The phrase surprised the reader because they never typed a command, only clicked through a dialog, and did not know LGE writes one for them behind the scenes. | Say that LGE writes a text command behind every dialog, on the reader's behalf. | 2 |
| I cannot write, "Reopening is what re-takes the lock" | The sentence reads backwards on first pass, sounding like reopening takes the lock away rather than restoring it to the reader. | Say the alert clears only after the reader closes and reopens the project. | 2 |
| A run stopped, "matched nothing your database recognises" | The reader thought a database meant something online, and did not know classification uses a separate, chosen reference collection. | Gloss database as the reference collection the classifier compares reads against, before this row. | 2 |
| A run stopped, "Run `lungfish-cli conda db recommend`" | This is the first command in the chapter the reader is told to run, and they still do not know where to type it. | Point once, early in the chapter, to the CLI Reference for how to run any command shown here. | 2 |
| A command refused me, "`conda install --pack gatk-core` exits 3" | Conda appears here unglossed, with its glossary entry only arriving later under Tools and databases. | Move the gloss of conda to this first use. | 2 |
| Reporting, "Add the installed tool versions from `lungfish-cli version --tools`" | The reader cannot perform this, so their own bug report will be missing a piece the chapter says to include. | Say this part is optional, or give a window route for it. | 2 |
| Whole chapter | Over twenty of the chapter's fixes are terminal commands, yet the appendix never states which sections need the command line, leaving a window-only reader with no route through much of the chapter. | Add one line early on saying which sections need the command line, and which do not. | 2 |
| What it is, "can read like a fragment of somebody else's conversation" | The metaphor is engaging but gives the reader nothing to act on, and reading it twice did not reveal an instruction inside it. | Say plainly that the message was written by another program, so it may use words LGE itself does not use. | 3 |
| What it is, "Look up the greyed item" | Greyed is used as a verb before the section that explains disabled menu items, and a non-native reader knows the color but not this verb form. | Gloss greyed at first use as a menu item shown in pale grey that cannot be clicked. | 3 |
| Start here, "that is how a real defect gets mistaken for a typing mistake and an hour disappears" | The idiom an hour disappears, and the abstract contrast between a real defect and a typing mistake, both needed a second reading to unpack. | Say plainly that typing the command by hand can introduce a new mistake that hides the real one. | 3 |
| A run stopped, "A run that completes is correct" | The reassurance is ambiguous: the reader could not tell whether it means the output is trustworthy or only that the run did not crash. | Say plainly that the contigs from a completed run are valid. | 3 |
| The run finished but, "a command exits zero and the answer is still wrong" | Exits zero is used as a compressed verb phrase. Even with exit status defined earlier, the compressed form made the reader pause. | Write it out as finishes with exit status zero. | 3 |
| Containers and pipelines, "which is a separate question from whether the framework is present" | The distinction is abstract, and the reader understood the words without learning what to do differently based on it. | Say plainly that the framework can be present and still fail to download images. | 3 |
| Reporting something, "Gather the failure report first, from the right-click menu on the failed row" | By the time the reader reaches this late section, they have forgotten the exact menu item name, Copy Failure Report, given many pages earlier. | Repeat the menu item name here rather than only referring back to it. | 3 |

## Consensus

- All four readers found that the chapter never tells a reader to open Terminal or where to type, at both the pwd example and the check-tools diagnostic.
- All four readers could not tell that the app version, tool row counts, download size, and CPU and memory figures throughout the chapter describe one example machine rather than a target their own machine should match.
- All four readers found that clearing a project lock is named as the needed action several times but never actually described, leaving a reader stuck with a read-only project.
- All four readers found that command-line-only sections never say so at the top, and that the pointer to the CLI Reference for command syntax arrives only at the very end of the chapter.
- All four readers found the opening sentence assumes the reader already knows what a command-line program is.
- All four readers could not check whether they were running Preview 2026.9.13, since the pointer to the About window arrives only at the very end.
- All four readers found take the report the panel offers you too vague, since the actual menu item, Copy Failure Report, is named only later.
- All four readers could not follow the path to LGE's log folder, since the tilde and the hidden Library folder are never explained.
- All four readers could not judge how long a failure report survives, since the store keeps the most recent reports gives no number.
- All four readers could not select a reference bundle, since the chapter never says where the Reference Sequences folder appears in the app window.
- All four readers found the advisory lock explanation describes its mechanism rather than telling the reader what it means for their own data.
- All four readers found host and process id unglossed, so neither number was usable for identifying which session to close.
- All four readers could not act on stray dot-underscore files, since they cannot see them in Finder and are never told what to do once one is found.
- All four readers met Kraken2 named without a gloss, in a row whose exit status 64 seems to contradict a later paragraph.
- All four readers could not judge MEGAHIT's fails most runs on Apple Silicon, since neither the failure rate nor a reasonable number of retries is given.
- All four readers found the symlink and working-directory explanation for the tmp versus private tmp failure too opaque to act on, with the actual fix buried at the end.
- All four readers could not judge whether their own run, stopping near but not at 84 percent, was the same failure described here.
- All four readers met Nextflow, cache, and file locks arriving unexplained in the same sentence.
- All four readers did not know how to check whether their drive was formatted exFAT.
- All four readers found the exit status numbers three and ten given in prose with no anchor to where a window user would ever see them.
- All four readers had to read past the explanation of the format flag collision before learning the plain fact that the command cannot be used in this release.
- All four readers could not convert a file with bcftools or vcftools, since neither program's source or installation is explained.
- All four readers met recipe as an unglossed new term with no link to where a recipe is run.
- All four readers flagged the Genotype-only row, where Min Reads is silently recorded but never applied as a filter, as the most alarming finding in the chapter.
- All four readers could not judge which rows were thin, since no read-count threshold is given.
- All four readers could not open the provenance sidecar with a JSON reader, since none is named and none is available to them.
- All four readers found assertion in a script written for a programmer rather than for them.
- All four readers could not set HTTPS_PROXY, since shell, proxy, and the exact syntax are all left unexplained.
- All four readers found container and pipeline both used in the section heading before either is glossed.
- All four readers could not switch a Nextflow run to a local file or a conda executor, since no window route or dialog is shown for either.
- All four readers could not judge when to use strict mode, since the extra checks it adds are never named.
- All four readers found the longest sentence in the chapter buries the fact that provenance verify fails on an ordinary unsigned sidecar until its final clause.
- Three readers found the working directory gloss defines itself using Terminal, a term never introduced.
- Three readers could not use the exit status definition, since they are never told where a number would actually appear for them.
- Three readers did not know what a disclosure triangle was or what to look for on the row.
- Three readers stopped on the word inert, unfamiliar as a description of a menu item.
- Three readers found the definition of specialized workflows circular, repeating the category name instead of explaining it.
- Three readers could not judge whether to start installing dependencies, since no size or duration is given.
- Three readers met bundle used at the Assistant tab with no gloss, though it recurs throughout the chapter.
- Three readers could not use the hidden project lock file path, since dot-prefixed files are invisible in Finder and never marked as such.
- Three readers could not tell whether their own drive counted as network storage, since the term is never glossed.
- Three readers could not judge Try a broader database, since neither database nor broader is explained here.
- Three readers could not find the bundle folder to zip by hand, since a bundle looks like one file in Finder.
- Three readers met deprecation unglossed at its first use.
- Three readers met environment used before it is explained, in a sentence carrying an engineering detail they did not need.
- Three readers could not find the provenance sidecar file to read its exitStatus field.
- Three readers found the angle-bracket placeholder in the conda lock message and the term pid both confusing.
- Three readers could not use the manual samtools and tabix route, since the paragraph already says the rebuild happens automatically.
