# Reader 1 report, appendices/troubleshooting

Persona. Sophomore, one intro genetics course, no lab time, has never opened a
terminal, has never used sequencing software. I came here because a row in the
app turned red and I wanted to know what to do about it.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "a window over a set of established command-line programs" | I do not know what a command-line program is, and this is the sentence that is supposed to orient me. The chapter later assumes I can type commands, so this is the moment to say what that means. | Gloss command-line program at first use as a program you run by typing its name into the Terminal app instead of clicking it. |
| What it is, "the symptoms that actually appear in Preview 2026.9.13" | I do not know whether I am running Preview 2026.9.13, and nothing here tells me how to check. If I am on a different one I do not know whether this appendix applies to me. | Point to the About window here, the way the Reporting section does at the end. |
| What it is, "A working directory is the folder your Terminal is sitting in" | The gloss uses Terminal, which has not been introduced. I have never opened Terminal and do not know where it is or what happens when I do. | Say in one sentence that Terminal is the macOS app for typing commands and where to find it. |
| What it is, "by typing `pwd` and pressing Return" | I cannot perform this. There is no instruction to open Terminal first, so I stared at the app window looking for somewhere to type. | Add the step of opening Terminal before the step of typing in it. |
| What it is, "An exit status is the number a command hands back" | The number is defined but I am not told where I would ever see it. I never saw a number, I saw a red row. | Say where an exit status appears, for example in the failure report or after a Terminal command. |
| Start here, at the failed row, "right-click its row" | I use a trackpad with no right button. This is the first action the chapter asks of me and I could not do it. | Add the two-finger click or Control-click equivalent once, here. |
| Start here, at the failed row, "take the report the panel offers you" | Vague. Take it where, and what do I do with it once I have it? The next paragraph names three menu items and I could not tell which one is "the report". | Name Copy Failure Report directly in this sentence. |
| Start here, at the failed row, "Cmd-Shift-P" | I could not tell whether this is three keys held together or pressed in order, and the chapter uses this notation without ever explaining it. | Write the shortcut once in full, for example hold Command and Shift and press P. |
| Start here, at the failed row, "Click its disclosure triangle" | I do not know what a disclosure triangle is or what it looks like. I looked for a triangle and was not sure I had found the right thing. | Gloss it as the small arrow at the left edge of the row that opens it. |
| Start here, at the failed row, "the operation title, the command, the error message, the error detail, and the log" | Error message and error detail sound like the same thing. I read this twice trying to work out the difference. | Say in half a sentence how the detail differs from the message, or drop one of them. |
| Start here, at the failed row, "LGE writes it under `~/Library/Logs/`" | I cannot follow this. I do not know what the tilde means, and Library does not appear in my Finder window, so I concluded the file was not there. | Gloss the tilde as your home folder and note that Library is hidden in Finder, or just tell me to use Reveal Failure Report in Finder. |
| Start here, at the failed row, "in a folder named for the build" | I do not know what a build is or what its folder would be called, so I could not have found this folder by hand. | Give the folder name, or say the exact name does not matter because Reveal in Finder opens it. |
| Start here, at the failed row, "The store keeps the most recent reports" | Most recent how many? I cannot judge whether I have days or minutes before mine is deleted. | Give the number of reports kept, or say roughly how long one survives. |
| Nothing happens when I choose a menu item, "Three menu items in this release are inert" | Inert is not a word I would use for software and I had to reread the line to be sure it meant nothing happens. | Say that they do nothing when clicked. |
| Nothing happens, "Genotyping workflows are specialized workflows, off until you enable them" | This says nothing beyond restating the name. I still do not know what genotyping is, and the chapter never says. | Add a half-sentence gloss of genotyping at first use, or link the term rather than the chapter title. |
| Nothing happens, "find the workflow's card under **Specialized Workflows** in the **Genotyping** group" | I do not know what a card is in this window. Three nested location words in one sentence and I could not picture the layout. | Gloss card as the box for one workflow, and consider a screenshot of the Workflow Library. |
| Nothing happens, "If the card says Needs install, click **Install Dependencies** first." | Dependencies is unexplained here. It is also not said how long this takes or how much it downloads, so I would not know whether to start it before class. | Gloss dependencies as the outside programs the workflow needs, and give a rough size or time. |
| Nothing happens, "The Workflow Builder is experimental and hidden by default" | I am told to turn on Show Experimental Features but not what the risk is. As a student I do not know whether experimental means unfinished or dangerous. | Say in one clause what experimental means for my data. |
| Nothing happens, "The Assistant tab appears only when a genomics bundle is loaded" | Bundle is used here for the first time and never explained. It then appears throughout the chapter, as reads bundle, result bundle, reference bundle, FASTQ bundle, and I never learned what a bundle is. | Gloss bundle at first use as a folder LGE manages that holds a dataset and its records. |
| Nothing happens, "A reads bundle, an assembly, a mapping result, a classifier result, or a genotype result" | Five unexplained things in one sentence. I could not tell which of them, if any, I had loaded. | Cut the list or gloss the category rather than the members. |
| Nothing happens, "Select a reference bundle under `Reference Sequences/`" | I could not perform this. I do not know where `Reference Sequences/` is, whether it is in Finder or in the app sidebar, or how a reference bundle differs from the other bundles just listed. | Say where in the app window this folder appears. |
| I cannot write to my project, "LGE takes a lock on a project" | I do not know what a project is in LGE. The word carries the whole section and is never defined. | Gloss project at first use as the folder LGE keeps one piece of work in. |
| I cannot write to my project, "it is an advisory lock, meaning it works because every cooperating program checks it" | I read this three times. The explanation of why it is advisory did not tell me what changes for me as a user. | Say what it means in practice, for example that a program that does not check can still overwrite your files. |
| I cannot write to my project, "or a command-line run and a window" | I do not know how I would have started a command-line run. If I only ever click, I cannot tell whether this cause applies to me. | Say that this only applies if you or someone else ran lungfish-cli. |
| I cannot write to my project, "names a user, a host, and a process id" | Host and process id are unexplained. I would not know what to do with either number even after reading them. | Gloss host as the computer name and process id as a number identifying the running program. |
| I cannot write to my project, "Confirm that session has genuinely ended before you clear the lock" | The step cannot be performed from this text. I am told to clear the lock but not how, and not how to confirm a session ended. | Point to the exact place in Shared Projects that gives the steps. |
| I cannot write to my project, "Lock failures surface against `.lungfish/project.lock`" | I cannot see files whose names start with a dot in Finder, so this path told me nothing I could act on. | Say these are hidden files and that you do not need to find them yourself. |
| I cannot write to my project, "look for stray files whose names begin with `._`" | Same problem, plus I am not told what to do if I find them. Delete? Ignore? | Say what action to take on the files once found. |
| I cannot write to my project, "network storage" and "how a share is mounted" | Three terms I do not know in one sentence. I could not tell whether my external drive counts as network storage. | Gloss network storage as a drive that lives on another computer, and say a plugged-in USB drive is not one. |
| A run stopped, "`Empty Kraken2 report`, exit status 64" | Kraken2 is named without explanation, and the row assumes I know what classification and a database are. This is the first entry in the section a lost student reads first. | Gloss Kraken 2 as the read classification program at first use. |
| A run stopped, "Try a broader database" | I cannot judge broader. I do not know which database I used or what the options are, and the recommend command output is not described. | Say what the command prints and how to read broader off it. |
| A run stopped, "MEGAHIT 1.2.9 fails most runs on Apple Silicon in this release, with both shipped workarounds active. A run that completes is correct." | I had to read this twice. Both shipped workarounds active is meaningless to me, and if it fails most runs I do not know how many times to retry before giving up. | Cut the workaround clause and say roughly how many retries are reasonable. |
| A run stopped, "A message about a provenance publication artifact" | Provenance and artifact are both unexplained, and the symptom is described so vaguely I could not match it to text on my screen, which is what the section promised. | Quote the actual message the way the other rows do. |
| A run stopped, "the two spellings differ across the symlink macOS keeps between `/tmp` and `/private/tmp`" | This is the hardest sentence in the chapter. I do not know what a symlink is, and the glossary link does not help me while I am panicking. | Say only that a folder inside Documents works and a temporary folder does not. |
| A run stopped, "Run from an ordinary folder such as one inside Documents." | I cannot perform this. If I never opened Terminal, I do not know what "run from a folder" means or how my working directory got set. | Add the click-path version of this fix for someone using the app window. |
| A run stopped, "reaches about 84 percent and stops" | I cannot judge whether my run stopping at 79 percent is this problem or a different one. | Say the percent is approximate and that the message text is what identifies it. |
| A run stopped, "Nextflow keeps a cache that needs file locks" | Nextflow, cache, file locks and scratch files all arrive unexplained in two sentences. | Gloss Nextflow as the pipeline runner LGE uses, and drop the cache mechanics. |
| A run stopped, "a drive formatted exFAT is the usual cause" | I do not know how my drive is formatted or how to check. | Add the one step for checking format, or say most portable drives ship this way. |
| A run stopped, "Three means a pack name was not recognised" | Pack is used before the Tools and databases section defines plugin packs. I did not know what a pack was here. | Move the plugin pack gloss earlier, or avoid pack in this list. |
| A run stopped, "Ten means `tools update --plan` found pending work, which is a report rather than a fault." | The list mixes numbers I might see after a failure with numbers only a script would care about. I could not tell which ones apply to my red row. | Split the numbers I might actually meet from the scripting ones. |
| A command refused me, "These are refusals rather than crashes." | The distinction did not help me, because I do not know what a crash looks like versus a refusal in this app. | Say a refusal means nothing ran and nothing was changed. |
| A command refused me, "`genotype-cohort` stops with a message" | Every row in this section is a Terminal command, but the section never says so. I spent a while looking for these in the menus. | Add one line saying this section covers commands you type, not menu items. |
| A command refused me, "at least two input FASTQ bundles are required" | FASTQ is not explained anywhere in the chapter, and it appears in four places. | Gloss FASTQ at first use as the file format holding sequencing reads and their quality scores. |
| A command refused me, "The subcommand's own `--format` flag collides with the global one, so this command cannot be run in this release." | Subcommand, flag and global are three unexplained terms, and I had to read the sentence twice to learn the simple fact that the command is broken. | Lead with the fact that the command does not work in this release. |
| A command refused me, "Zip the bundle folder by hand to move it." | I do not know which folder is the bundle folder or where it lives, so I could not do this. | Say how to find the bundle folder, for example via Reveal in Finder. |
| A command refused me, "Install both from the Plugin Manager" | The row says the pack exists but the installer does not know its name, which sounds like a bug I should not work around. I hesitated before trusting the fix. | Say plainly that installing from the Plugin Manager is the supported route. |
| A command refused me, "it also rejects a symlink or hard link that points back at the input" | Symlink and hard link again, and I have no idea how I would have created one accidentally. | Cut the clause, since the fix of giving a different name covers it. |
| A command refused me, "Convert it to VCF 4.x with `bcftools convert` or with vcftools' `vcf-convert`" | This step cannot be performed. Neither program has been introduced, neither ships with the app as far as I can tell, and no install instruction is given. | Say whether these come with LGE and where to get them if not. |
| A command refused me, "prints a deprecation notice" | Deprecation is unexplained. The What to do column then hands me a three-step migration with a recipe I do not know about. | Gloss deprecated as still working but scheduled for removal, and simplify the fix. |
| A command refused me, "Build per-sample `.lungfishfastq` bundles with a FASTQ import recipe" | Recipe is a new term here and appears nowhere else in the chapter. I could not start this step. | Gloss recipe or link the chapter that teaches importing. |
| The run finished but, "On a Genotype only run the value is recorded in the run statistics and never applied as a filter." | This says a setting in the app does nothing. That is alarming and I read it twice to be sure. It also does not tell me which runs are Genotype only. | Say what makes a run Genotype only, since the fix depends on it. |
| The run finished but, "Read the read-count column of the report's Long Summary sheet and set aside the thin rows yourself." | Thin is not a number. I cannot judge which rows to remove, which is the whole thing I came for. | Give a starting threshold, even a rough one, and say what it depends on. |
| The run finished but, "the cohort-level counts LGE builds are never shown" | Cohort is used throughout this section and never explained. | Gloss cohort at first use as the group of samples compared together. |
| The run finished but, "You import a BigWig or BigBed file and nothing renders" | Both formats are unexplained, though here I can at least tell whether I have one from the file name. Renders is also app jargon. | Say nothing appears in the window rather than nothing renders. |
| The run finished but, "Assembly accessions beginning `GCF_` or `GCA_` go through the assembly database" | I do not know what an accession is or what the difference between an assembly database and a nucleotide database is, and this row is entirely about that difference. | Gloss accession as the ID number a public database gives one sequence. |
| The run finished but, "Check the sequence name in the bundle that came back before you use it." | I could not perform this. I am not told where in the window a sequence name is displayed. | Say where the name appears. |
| The run finished but, "read the field you need out of the provenance sidecar with a JSON reader" | Three unfamiliar things and no tool named. I would not know what a JSON reader is or where to get one. | Name a way to open the file, or drop this option for a reader who does not script. |
| Tools and databases are missing, "Most LGE operations run out of per-tool environments" | Environment is used before it is explained, and the conda gloss that follows does not tell me what an environment is either. | Gloss environment as a private folder holding one tool and the exact versions it needs. |
| Tools and databases are missing, "The first diagnostic for any missing-tool error is `lungfish-cli debug env --check-tools`" | The chapter switches to Terminal commands with no warning and no instruction on how to run one. Six commands follow in this section alone and I could not run any of them. | Add one sentence before the first command telling me to open Terminal and type it there. |
| Tools and databases are missing, "which is the exact list of tool versions one LGE release was built and tested against" | I read this twice. The dependency set explanation is buried in the middle of a sentence about a different command. | Give dependency set its own short sentence. |
| Tools and databases are missing, "Run against the machine that wrote this appendix it exited 0 and printed" | This sentence reads as if a word is missing at the start, and I could not tell whether the numbers that follow are what I should expect or just an example. | Rewrite the opener and say plainly that these are one machine's results, not a target. |
| Tools and databases are missing, "above eighteen tool rows" and "an estimated download of 157.3 MB" | I cannot judge either number. If mine says twelve tools or 900 MB I do not know whether something is wrong. | Say the numbers vary by machine and are not something to match. |
| Tools and databases are missing, "which makes it usable as an assertion in a script" | Assertion and script are unexplained, and this is written for someone who writes scripts, not me. | Move the scripting note to the CI chapter it already links. |
| Tools and databases are missing, "`lungfish-cli conda db recommend` to see what suits this machine" | I cannot judge the output. Earlier the chapter told me to run this to find a broader database and here it is described differently. | Say what the recommendation is based on, for example free disk space. |
| Tools and databases are missing, "`conda root is read-only; reinstall as the admin user`" | Admin user is unexplained, and if I am on a lab computer I do not know whether I am one or how to become one. | Say to ask whoever administers the computer. |
| Tools and databases are missing, "Setting `HTTPS_PROXY` in the shell that launches LGE fixes it" | I cannot perform this at all. Proxy, shell and setting a variable are three things I have never done, and no syntax is given. | Give the exact line to type, or say to ask campus IT for the proxy address. |
| Containers and pipelines, "containers, which are isolated environments holding a whole operating system's worth of software" | The gloss is here, which I appreciated, but pipeline right beside it is not explained and carries the section heading. | Gloss pipeline as a chain of tools run one after another. |
| Containers and pipelines, "switch a Nextflow run to a local `.nf` file or to a conda executor" | This step cannot be performed from the text. I do not know where that switch is, what a `.nf` file is, or what an executor is. | Point to the chapter and the dialog where the executor is chosen. |
| Containers and pipelines, "Add `--pull-test` to check that the machine can actually fetch an image" | Image is unexplained and is not the everyday meaning of the word. | Gloss image as the packaged copy of a tool that gets downloaded. |
| Containers and pipelines, "reported macOS Version 26.6.2, 14 CPU cores, 48 GB of physical memory" | Another set of numbers from one machine with no guidance. My laptop has 8 GB and I cannot tell whether that is a problem. | State the minimum that matters, for example how much memory an assembly needs. |
| Is this file or bundle intact, "`--strict` raises the bar" | I cannot judge when to use it, because I am not told what extra checks it adds. | Say what strict additionally rejects. |
| Is this file or bundle intact, "detects a record that changed after it was signed" | Signed and signing are unexplained. The row then says signing is off by default, which made me wonder why the command is documented at all. | Gloss signing in half a sentence, or lead with the fact that this does not apply on a default install. |
| Is this file or bundle intact, "read the sidecar's `exitStatus` field and check that the declared outputs exist instead" | I could not perform this. I do not know where the sidecar file is or how to open it. | Say where the sidecar lives and how to view it. |
| Is this file or bundle intact, "LGE rebuilds a `.fai`, a `.bai`, or a `.tbi`" | Three file extensions with no explanation of what an index file is or why it matters. | Gloss index file as a companion file that lets LGE jump into a large file quickly. |
| Reporting something, "the app version, which the **Lungfish Genome Explorer > About Lungfish Genome Explorer** window shows" | This is the first place the chapter tells me how to find my version, and it is at the very end, long after the opening paragraph told me which release it documents. | Move a pointer to the version check into the opening section. |
| Reporting something, "**Open GitHub Issue** on the failed row's right-click menu" | GitHub is unexplained and I do not have an account. I did not know whether I could use this route at all. | Say an account is needed and name the alternative for someone without one. |
| Reporting something, "Leave your project's data files out of the report." | Good advice, but I could not tell whether the automatic failure report already contains data, since it includes the log and I do not know what a log holds. | Say whether the generated report is already safe to send. |
| Next, "See [CLI Reference](cli-reference.md) for the syntax of any command named here" | Syntax and CLI are unexplained. Since most fixes in this chapter are commands, sending me to an unexplained acronym at the end is where I would give up. | Expand CLI at first use and say the reference explains how to type commands. |

The one thing I learned. A failed run writes a report by itself, and the right
way to start is to right-click the red row and copy that report rather than
trying to remember what I clicked.

The one thing I still could not do. Run any of the roughly twenty
`lungfish-cli` commands this chapter offers as fixes, because it never tells me
to open Terminal or how to type a command into it.

The sentence I liked most. "Do not reconstruct the command by hand from what you
remember choosing in a dialog, because that is how a real defect gets mistaken
for a typing mistake and an hour disappears."
