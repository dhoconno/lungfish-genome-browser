# Troubleshooting appendix, reader 3

Pre-med student, English is my second language. I know biology words from my
textbooks. I have never opened a terminal. My run just failed with a red row
and I came here to find out what to do.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "is a window over a set of established command-line programs" | "A window over" is a figure of speech I had to read twice. I first pictured a real window. I also do not know what "command-line programs" are, since I have never used a terminal. | Say LGE runs other analysis programs for you and shows their results. |
| What it is, "can read like a fragment of somebody else's conversation" | A metaphor. It is charming but it does not tell me anything I can act on, and I read the whole sentence twice trying to find the instruction in it. | Say the message was written by another program, so it may use words LGE does not use. |
| What it is, "the symptoms that actually appear in Preview 2026.9.13" | "Preview" is not explained. I do not know if the app I have is Preview or something else, so I do not know if this appendix applies to me. | Say what Preview is and where to check my own version at first use. |
| What it is, "Look up the greyed item" | "Greyed" is used before the section that explains it. As a non-native reader I know "grey" the colour but not "greyed" as a verb for a disabled menu item. | Gloss greyed at first use as a menu item shown in pale grey that cannot be clicked. |
| What it is, "which you can see at any time by typing `pwd` and pressing Return" | I have never opened a terminal. Typing where? The text assumes I know how to open one and what a prompt looks like. I could not perform this step. | Add one sentence saying how to open Terminal, or mark this as for command-line users only. |
| Start here, "take the report the panel offers you" | "Take the report" is vague. Take it where? Only two paragraphs later do I learn there is a menu item called Copy Failure Report. | Say right-click the row and choose Copy Failure Report. |
| Start here, "that is how a real defect gets mistaken for a typing mistake and an hour disappears" | "An hour disappears" is an idiom. I understood it on the second reading, but "a real defect" versus "a typing mistake" is also an abstract contrast I had to unpack. | Say typing the command yourself can introduce a new mistake that hides the real one. |
| Start here, "Click its disclosure triangle" | "Disclosure triangle" is a macOS interface word I have never met. I did not know what to look for on the row. | Gloss it as the small arrow at the left of the row that opens it. |
| Start here, "LGE writes it under `~/Library/Logs/`" | The `~` symbol is not explained. I do not know what it stands for or how to reach that folder in Finder, and `~/Library` is hidden on my Mac. | Gloss `~` as your home folder and say how to open a hidden Library folder. |
| Start here, "The store keeps the most recent reports and prunes older ones" | I cannot judge this. How many is "the most recent"? Five? Fifty? I do not know how long I have before my report is deleted. | Give the number of reports kept. |
| Nothing happens, "Three menu items in this release are inert" | "Inert" is a chemistry word for me. Used about a menu item it made me stop. | Say the item does nothing when clicked. |
| Nothing happens, "Genotyping workflows are specialized workflows, off until you enable them" | This sentence defines a thing by repeating it. It told me nothing new and I read it twice looking for the missing part. | Say what makes a workflow specialized, or drop the repetition. |
| Nothing happens, "If the card says Needs install, click **Install Dependencies** first" | I do not know what dependencies are here, how long the install takes, or whether it needs internet. I would be afraid to click. | Gloss dependencies and say the install downloads software and needs a network. |
| Nothing happens, "The Assistant tab appears only when a genomics bundle is loaded" | "Genomics bundle" appears here for the first time and is not glossed, yet the next sentence lists five things that are NOT it. I could not tell what a genomics bundle actually is. | Gloss genomics bundle at first use, and say plainly that it means a reference bundle. |
| Nothing happens, "Select a reference bundle under `Reference Sequences/`" | The row says the tab needs a genomics bundle, then the fix says reference bundle. Two names for what I think is one thing. I had to read across three cells to guess they are the same. | Use one name for it. |
| I cannot write to my project, "it is an advisory lock, meaning it works because every cooperating program checks it rather than because the operating system refuses access" | Very long sentence with a definition inside a definition. I read it three times. I also do not know what follows from it for me as a user. | Split it, and add what it means in practice, such as that a program that ignores the lock can still write. |
| I cannot write to my project, "a host, and a process id" | "Host" and "process id" are not glossed. I do not know where to look for them or what I do with them once I read them. | Gloss both, and say what they tell me about which session to close. |
| I cannot write to my project, "Confirm that session has genuinely ended" | I could not perform this. The text does not say how to confirm a session on another computer has ended. | Say how to check, for example by asking the named user. |
| I cannot write to my project, "remove the lock as [Shared Projects] describes" | The one destructive-sounding action in this section is not described here at all, so I have to leave the page mid-emergency. | Give the actual step here, since this is where the reader is. |
| I cannot write to my project, "On network storage a reported locking failure is often not a real lock problem" | "Network storage" is not glossed and I do not know if my shared lab drive counts as network storage. | Gloss it and give an everyday example. |
| I cannot write to my project, "stray files whose names begin with `._`" | I could not perform this. Files beginning with a dot are invisible in Finder and the text does not say how to see them, or what to do once I find one. | Say how to show them and whether it is safe to delete them. |
| A run stopped, "`Empty Kraken2 report`, exit status 64" | Exit status 64 is defined nine rows later, at the end of the section. When I read the row I had no way to judge the number. | Move the exit status list above the table. |
| A run stopped, "matched nothing your database recognises" | I did not know a classification uses a separate database that I choose, since nothing above this row says so. | Say one line about what the database is before the table. |
| A run stopped, "Try a broader database" | I cannot judge "broader". I do not know which of the listed collections is broader, or how much disk it needs. | Name the broader database, or say the recommend command prints them largest first. |
| A run stopped, "MEGAHIT 1.2.9 fails most runs on Apple Silicon in this release, with both shipped workarounds active" | I do not know what Apple Silicon is on my own Mac, "shipped workarounds" is jargon, and "most runs" gives me no way to decide whether to keep retrying. | Say how to check my Mac's chip, and how many retries are reasonable. |
| A run stopped, "A run that completes is correct" | I read this twice. It sounds reassuring but I could not tell whether it means the output is trustworthy or only that it did not crash. | Say the contigs from a completed run are valid. |
| A run stopped, "the two spellings differ across the symlink macOS keeps between `/tmp` and `/private/tmp`" | Long, and full of things I do not know. Symlink is linked but I still cannot picture it, and I do not know whether my run is under `/private/tmp` because I never chose that. | Say plainly that this happens when the folder is a temporary one, and how to tell. |
| A run stopped, "reaches about 84 percent and stops" | I cannot judge "about 84 percent". If my run stops at 80, is it this problem or a different one? | Say the percentage varies and the message text is the real identifier. |
| A run stopped, "Nextflow keeps a cache that needs file locks and a volume free of macOS metadata sidecars" | Four terms I do not know in one sentence, Nextflow, cache, file locks, metadata sidecars. I gave up on understanding the cause. | Split into cause and effect, and gloss Nextflow at first use. |
| A run stopped, "a drive formatted exFAT is the usual cause" | exFAT is not glossed and I do not know how to check the format of my external drive. | Gloss exFAT and say how to check the format in Finder. |
| A run stopped, "Three means a pack name was not recognised" | "Pack" appears here but plugin packs are only glossed two sections later. I had no idea what a pack was at this point. | Gloss pack at first use, or move this list after the packs section. |
| A command refused me, "These are refusals rather than crashes" | I read this twice. The distinction matters to the author but I could not tell what it changes for me. | Say the command stopped before doing anything, so nothing was damaged. |
| A command refused me, "The subcommand's own `--format` flag collides with the global one, so this command cannot be run in this release" | "Subcommand", "flag", and "global one" are all terminal vocabulary I do not have. I understood only the last clause. | Gloss flag once early, and lead the row with the plain fact that the command is broken. |
| A command refused me, "Zip the bundle folder by hand" | I could not perform this. The bundle looks like one file to me in Finder, so I do not know which folder to compress or whether right-click Compress is what is meant. | Say to right-click the bundle in Finder and choose Compress. |
| A command refused me, "Convert it to VCF 4.x with `bcftools convert` or with vcftools' `vcf-convert`" | Both are terminal programs I have never installed, and the text does not say whether LGE provides them or whether I must install them myself. | Say where those tools come from, or give a route inside LGE. |
| A command refused me, "prints a deprecation notice" | "Deprecation" is not glossed. I guessed from "on its way out" in the next cell, but only after reading across. | Gloss it as a warning that the command will be removed in a future version. |
| A command refused me, "Build per-sample `.lungfishfastq` bundles with a FASTQ import recipe" | I could not perform this. "Recipe" is a new word here with no gloss and no link to where recipes live. | Gloss recipe and link the page that shows how to run one. |
| The run finished but, "a command exits zero and the answer is still wrong" | "Exits zero" is used as a verb phrase. Exit status was defined at the top but this compressed form still made me pause. | Write it as finishes with exit status zero. |
| The run finished but, "the value is recorded in the run statistics and never applied as a filter" | This is the most alarming row in the chapter for me, because it means a number I set does nothing. Buried in a table cell, I nearly missed it. | Raise this to a warning callout above the table. |
| The run finished but, "set aside the thin rows yourself" | "Thin rows" is a metaphor and I cannot judge it. Thin compared to what? I do not know what read count is too low. | Give a starting number or say the judgement depends on the assay and link where it is discussed. |
| The run finished but, "A genotype-only result hides both by design" | "By design" reads as an excuse to me rather than information. I still could not tell whether I did something wrong when I ran it. | Say a single-sample run has no cohort to summarise. |
| The run finished but, "Take the whole-run depth judgement from" | "Depth" is not glossed here. In my textbooks depth has several meanings and I could not tell which one. | Gloss sequencing depth at first use in this chapter. |
| The run finished but, "`fetch genome` returns a record under a different sequence name than the accession you asked for" | The row does not tell me what harm this causes. I did not know whether my downloaded sequence is wrong or just named differently. | Say whether the returned sequence is a different organism or the same one renamed. |
| The run finished but, "read the field you need out of the provenance sidecar with a JSON reader" | I could not perform this. I do not know what a JSON reader is or where the sidecar file sits. | Name a concrete way to open it, or say this step is for scripting users. |
| Tools and databases, "Most LGE operations run out of per-tool environments" | "Run out of" is an idiom that normally means to have none left. I read the sentence as an error message before I understood the intended meaning. | Say each operation runs from its own installed environment. |
| Tools and databases, "so that two tools needing different versions of the same library can coexist" | "Library" in a software sense is new to me and collides with the biology meaning of a sequencing library, which I do know. | Gloss library, and note it is not a sequencing library. |
| Tools and databases, "`lungfish-cli debug env --check-tools`" | Every command in this section assumes I can open a terminal and that `lungfish-cli` is installed. Neither is explained anywhere in the chapter. | Add one line near the top saying how to get a working terminal with lungfish-cli, or point to the CLI chapter. |
| Tools and databases, "Run against the machine that wrote this appendix it exited 0 and printed" | I read this sentence three times. "Run against the machine that wrote this appendix" is an odd construction and I could not tell whose machine or why it matters to me. | Say this is an example of what the output looks like on one test machine. |
| Tools and databases, "above eighteen tool rows" | I cannot judge eighteen. I do not know whether my machine should show the same number or whether fewer means something is missing. | Say the number depends on the dependency set, so a different count is not itself a problem. |
| Tools and databases, "It exits 10 when work is pending and 0 when there is none, which makes it usable as an assertion in a script" | "Assertion in a script" is programming vocabulary. The rest of the sentence I followed, this part I skipped. | Move the scripting note to the CI chapter. |
| Tools and databases, "an estimated download of 157.3 MB" | I cannot judge this. Is 157.3 MB the normal size or a sign my machine is far behind? | Say the size varies with how much is pending. |
| Tools and databases, "`waiting for conda lock held by pid <n>` and then continues on its own" | "pid" is not spelled out and the `<n>` placeholder confused me, since I expected to see that literal text. | Spell out process id and say the number varies. |
| Tools and databases, "Setting `HTTPS_PROXY` in the shell that launches LGE fixes it" | I could not perform this. "Setting a variable in the shell" is beyond me, and I do not know whether my university network uses a proxy. | Say how to find out whether a proxy is in use, and mark the fix as command-line only. |
| Containers and pipelines, "isolated environments holding a whole operating system's worth of software" | "A whole operating system's worth of" is an unusual measure phrase. I read it twice. | Say a container packages a program together with everything it needs to run. |
| Containers and pipelines, "switch a Nextflow run to a local `.nf` file or to a conda executor" | I could not perform this. Three unexplained things, a `.nf` file, a conda executor, and where the switch is made. | Say where in the interface this choice is made, and gloss executor. |
| Containers and pipelines, "which is a separate question from whether the framework is present" | Abstract. I understood the words but not what I should do differently based on the distinction. | Say the framework can be present and still fail to download images. |
| Containers and pipelines, "14 CPU cores, 48 GB of physical memory" | I cannot judge these. I do not know the minimum LGE needs, so seeing my own smaller numbers would tell me nothing. | State the minimum recommended cores and memory. |
| Is this file or bundle intact, "`--strict` raises the bar" | "Raises the bar" is an idiom. I guessed it means stricter checking but I was not sure, and I do not know what extra things it checks. | Say what strict mode additionally rejects. |
| Is this file or bundle intact, "detects a record that changed after it was signed, though it is worth knowing that it handles only signed records and that signing is off by default, so on an ordinary unsigned sidecar it exits 64" | The longest sentence in the chapter. Four clauses. I read it three times and still had to reread to find the action. | Split into three sentences, and lead with the fact that most records are unsigned. |
| Is this file or bundle intact, "read the sidecar's `exitStatus` field" | I could not perform this. I do not know where the sidecar file is or what opens it. | Say where the sidecar sits relative to the result and how to open it. |
| Is this file or bundle intact, "the underlying tools are `samtools faidx`, `samtools index`, and `tabix`" | Three terminal commands with no explanation of when I would ever need them, given the paragraph just said indexes rebuild automatically. | Say this manual route is only for command-line users. |
| Reporting something, "Gather the failure report first, from the right-click menu on the failed row" | By this point I have forgotten which menu item it was, since the name Copy Failure Report was given many pages earlier. | Repeat the menu item name here. |
| Next, "See [CLI Reference] for the syntax of any command named here" | The chapter uses commands throughout and only points me to the CLI chapter at the very end. I needed that pointer at the start. | Move a pointer to the CLI chapter into the opening section. |

One thing I learned. A run that stops is not always a run that did something
wrong, and the exit status number is the fastest way to tell the difference.

One thing I still could not do. I could not run a single command in this
chapter, because nothing tells a reader who has never opened a terminal how to
get one, or whether `lungfish-cli` is already on my computer.

The sentence I liked most. "The hardest failures are the quiet ones, where a
command exits zero and the answer is still wrong."
