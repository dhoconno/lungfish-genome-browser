# Reader 2 report, appendices/troubleshooting

Persona. A senior who has pipetted for two years. I know library prep and PCR. I have never analyzed data and I have never opened a terminal. My run just stopped with a red row and I came here to find out what to do.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "a window over a set of established command-line programs" | I do not know what a command-line program is. This is the second sentence of the appendix and it already assumes I know the thing I have never done. | Gloss it once, as a program with no window that you drive by typing. |
| What it is, "the symptoms that actually appear in Preview 2026.9.13" | I do not know whether I am running Preview 2026.9.13. Nothing here tells me how to check, and the check is only given eight sections later under Reporting. | Say here where to read your version, in the About window. |
| What it is, "A working directory is the folder your Terminal is sitting in" | Terminal is capitalized like an app name but never introduced. I have never opened it and I do not know if I am supposed to. | Say whether a bench user needs Terminal at all, or only for the command entries. |
| What it is, "by typing `pwd` and pressing Return" | I cannot perform this. I do not know where to type it. The backticks tell me it is something I type but not into what. | Name the app to open first and where its typing area is. |
| What it is, "An exit status is the number a command hands back" | The word is glossed but I cannot use it. I never saw a number when my run failed, only a red row, so I do not know where an exit status would appear for me. | Say where an exit status shows up in the window, or that it appears only in the failure report. |
| Start here, "take the report the panel offers you" | Vague. Take it where, and then what do I do with it? The next paragraph explains the menu, so I read this sentence twice before it made sense. | Say copy the failure report and keep it on the clipboard for the entries below. |
| Start here, "Cmd-Shift-P" | I do not know if this means press all three at once, and whether Shift means the arrow key or the letter. Minor, but I stalled. | Nothing needed if the manual defines shortcut notation once in an earlier chapter. |
| Start here, "Click its disclosure triangle" | I do not know what a disclosure triangle is or where on the row it sits. | Say the small arrow at the left edge of the row. |
| Start here, "the command LGE built and the running log" | I do not know what a log is here, and "the command LGE built" surprised me because I never typed a command. I only clicked through a dialog. | Say that LGE writes a text command for you behind every dialog. |
| Start here, "LGE writes it under `~/Library/Logs/`" | I cannot get there. The tilde means nothing to me and Library is hidden in Finder on my Mac. | Say to use Reveal Failure Report in Finder rather than navigating by hand. |
| Start here, "The store keeps the most recent reports and prunes older ones" | I cannot judge this. How many is the most recent, and how long do I actually have? "next month" in the same sentence suggests weeks, but the number is never given. | Give the count kept, or say the limit is not fixed. |
| Nothing happens, "Three menu items in this release are inert" | "Inert" made me stop. I know it from chemistry, not from menus. | Say do nothing when clicked. |
| Nothing happens, "Genotyping workflows are specialized workflows, off until you enable them" | This reads as circular. It tells me the category name twice and never tells me why they are off by default or whether turning them on costs me anything. | Say why they are off, for example that they need a large install. |
| Nothing happens, "If the card says Needs install, click **Install Dependencies** first" | I cannot judge this step. How long does it take and how much will it download? I am on lab wifi and I do not want to start something I cannot stop. | Give a rough size or duration, or say the panel shows progress. |
| Nothing happens, "The Workflow Builder is experimental and hidden by default" | "Experimental" is not defined. Does that mean it may lose my data, or just that it is unpolished? I do not know if I am allowed to use it on real samples. | State plainly what experimental means for your results. |
| Nothing happens, "a genomics bundle is loaded" then "A reads bundle, an assembly, a mapping result" | Five kinds of bundle arrive in one sentence and none is glossed here. I do not know which one my MiSeq run became when I imported it. | Link "bundle" to the glossary at this first use. |
| Nothing happens, "Select a reference bundle under `Reference Sequences/`" | I could not perform this. I do not know how a reference bundle gets into that folder in the first place, and I do not have one. | Point to the chapter that creates a reference bundle. |
| I cannot write, "an advisory lock, meaning it works because every cooperating program checks it" | I had to read this twice. The definition is correct but it explains a mechanism I do not need in order to act. | Lead with the consequence, that a lock stops a second writer. |
| I cannot write, "a user, a host, and a process id" | "Host" and "process id" are unexplained. If the banner names my own laptop I will not know whether that is me or somebody else. | Say host means the computer name and process id identifies the running copy. |
| I cannot write, "Confirm that session has genuinely ended before you clear the lock" | I cannot perform this. Nothing tells me how to confirm a session on another machine has ended, and "clear the lock" is an action that is never shown, only deferred to another chapter. | Say what to check, and that the clearing step lives in Shared Projects. |
| I cannot write, "Reopening is what re-takes the lock" | Read twice. It sounds like reopening takes a lock away from me when it actually gives one back. | Say the alert clears only after you close and reopen the project. |
| I cannot write, "Lock failures surface against `.lungfish/project.lock`" | I cannot use this. Both paths begin with a dot so they are invisible in Finder, and I am not told how to look at them. | Say these files are hidden and that you do not need to open them. |
| I cannot write, "files whose names begin with `._`" | Same problem. I am asked to look for stray files I cannot see. | Say how to reveal hidden files, or that IT should do this. |
| I cannot write, "network storage" | Never explained. I do not know whether my lab server drive counts, and this whole paragraph only matters if it does. | Say a shared drive mounted from a server. |
| A run stopped, "`Empty Kraken2 report`, exit status 64" | Contradiction I could not resolve. This section says 64 here, and the paragraph below says sixty-four means the command stopped before doing any work, but this entry says the classification finished. | Reconcile the two, or note that Kraken 2 reuses 64 differently. |
| A run stopped, "matched nothing your database recognises" | I do not know what a database is in this context. I thought a database was something online. | Gloss it as the reference collection the classifier compares reads against. |
| A run stopped, "Run `lungfish-cli conda db recommend`" | I cannot perform this. This is the first command in the chapter I am told to run and I still do not know where to type it. | Point once, early, to the CLI Reference for how to run any command shown here. |
| A run stopped, "MEGAHIT 1.2.9 fails most runs on Apple Silicon" | I cannot judge "most". If it is nine runs in ten, rerunning is useless advice, and the fix says rerunning is the only workaround. | Give a rough failure rate, or recommend SPAdes outright. |
| A run stopped, "with both shipped workarounds active" | Meaningless to me. I do not know what the workarounds are and I cannot check whether they are active. | Cut this clause or move it to a note for developers. |
| A run stopped, "Your working directory sits under `/private/tmp`" | I could not act. I never chose a working directory. I clicked buttons. I have no idea how mine would end up under /private/tmp. | Say what in the app puts you there, for example running from a downloaded file. |
| A run stopped, "the two spellings differ across the symlink macOS keeps" | Read three times. Symlink is linked to the glossary but the sentence stays opaque, and the payoff is only the last sentence of the fix. | Lead with the fix, that the run must not live in a temporary folder. |
| A run stopped, "reaches about 84 percent and stops" | I cannot judge the number. Is 84 percent exact enough to identify my failure, or would 80 also be this bug? | Say the run stops near the end, after the analysis is done. |
| A run stopped, "Nextflow keeps a cache that needs file locks" | Three unexplained things in one clause, Nextflow, cache, file locks. I do not know if I use Nextflow. | Say the pipeline engine behind some workflows, glossed once. |
| A run stopped, "a drive formatted exFAT" | I do not know how my external drive is formatted or how to find out. | Say how to check the format in Finder's Get Info. |
| A run stopped, "Three means a pack name was not recognised" | This paragraph gives five exit numbers in prose with no anchor to where I would see one. I read it twice and still cannot use it. | Turn it into a small table, or say where the number appears. |
| A run stopped, "Ten means `tools update --plan` found pending work" | I cannot follow this without already knowing what that command is, which is only introduced two sections later. | Move this line to the Tools section where the command is explained. |
| A command refused me, section heading and lead | The whole section assumes I am typing commands. As a window user I could not tell whether any of it applies to me. | Open the section by saying these apply only when you use the command line. |
| A command refused me, "at least two input FASTQ bundles are required" | I know what FASTQ is from the sequencing core. I do not know what makes a FASTQ into a bundle, and that difference decides whether I have two. | Gloss bundle at its first use in the chapter. |
| A command refused me, "The subcommand's own `--format` flag collides with the global one, so this command cannot be run" | I had to read this twice to learn that a documented feature is simply broken. The explanation comes before the verdict. | Lead with the fact that bundle export cannot be used in this release. |
| A command refused me, "Zip the bundle folder by hand" | I do not know which folder is the bundle, since a bundle looked like a single file to me in the window. | Say a bundle is a folder that Finder shows as one item. |
| A command refused me, "`conda install --pack gatk-core` exits 3" | Conda is linked at its glossary entry only later, in Tools and databases. Here it appears first, unglossed. | Move the gloss to this first use. |
| A command refused me, "it also rejects a symlink or hard link" | Two terms, one linked and one not, and neither matters for the action. I stalled on hard link. | Cut hard link or gloss it. |
| A command refused me, "Convert it to VCF 4.x with `bcftools convert`" | I cannot perform this. bcftools and vcftools are outside programs that I do not have and am not told to install. | Say whether LGE ships these, and where to run them. |
| A command refused me, "prints a deprecation notice" | Deprecation is not glossed at first use, though the plain meaning is given in the next column. | Say the command still works but will be removed. |
| A command refused me, "Build per-sample `.lungfishfastq` bundles with a FASTQ import recipe" | I could not perform this. Recipe is a new word here and is never explained. | Gloss recipe, or link to the chapter that has the import step. |
| The run finished, "On a Genotype only run the value is recorded in the run statistics and never applied as a filter" | This is the most alarming entry in the chapter and I read it three times. A setting I turned looks like it worked and did not. | State the risk first, that Min Reads has no effect on this run type. |
| The run finished, "set aside the thin rows yourself" | I cannot judge "thin". No threshold is offered, so I do not know what read count is too low for a real call. | Give a starting number, or say where the chapter on genotyping gives one. |
| The run finished, "Long Summary sheet" | Named as if I have seen it. I have not, and I do not know if this is inside the app or an exported spreadsheet. | Say it is a sheet in the exported workbook. |
| The run finished, "Take the whole-run depth judgement from `lungfish-cli genotype list-samples`" | I cannot perform this, and it is the only route offered for a judgement the window will not show me. | Say whether any in-window route exists, even a partial one. |
| The run finished, "depth" | Unexplained. I have heard coverage at the bench but not depth as a judgement I make. | Gloss depth as reads per position. |
| The run finished, "BigWig or BigBed" | Two formats named with no hint of what they hold or where I would have got one. | Say briefly what they are used for. |
| The run finished, "Assembly accessions beginning `GCF_` or `GCA_`" | I could not follow the mechanism. I do not know the difference between an assembly accession and a nucleotide accession, and the entry needs that difference to make sense. | Give one example of each, in human. |
| The run finished, "read the field you need out of the provenance sidecar with a JSON reader" | I cannot perform this. I do not have a JSON reader and do not know what one is. | Name a way to open it, or say a text editor will do. |
| Tools and databases, "run out of per-tool environments" | "Environment" is used before conda is glossed later in the same sentence, and the word is doing heavy lifting I could not follow. | Gloss environment as a private folder of software for one tool. |
| Tools and databases, "so that two tools needing different versions of the same library can coexist" | Read twice. This explains an engineering reason I do not need in a symptom lookup. | Cut or move to a footnote. |
| Tools and databases, "`lungfish-cli debug env --check-tools`" is "the first diagnostic" | I cannot perform the chapter's first diagnostic, since it is a command. That leaves this whole section closed to me. | Say whether the Plugin Manager window shows the same information. |
| Tools and databases, "Run against the machine that wrote this appendix it exited 0 and printed" | This sentence stopped me completely. I do not know what "the machine that wrote this appendix" is, and the phrase recurs four times. | Say on a test machine, and explain that these are example outputs. |
| Tools and databases, "eighteen tool rows" | I cannot judge this. Is eighteen what I should also see, or just what that machine had? | Say the count varies with what you have installed. |
| Tools and databases, "an estimated download of 157.3 MB" | Same. I do not know if my own number should be near this or whether a much larger one means a problem. | Say the size depends on what is missing. |
| Tools and databases, "makes it usable as an assertion in a script" | Assertion and script are both outside what I know, and this clause is aimed at someone else. | Cut it, or mark the sentence as for scripting users. |
| Tools and databases, "`waiting for conda lock held by pid <n>`" | The angle brackets confused me. I did not know whether I would literally see "<n>" on screen. | Say a number appears in place of n. |
| Tools and databases, "`conda root is read-only; reinstall as the admin user`" | I cannot perform this. I do not know whether I am the admin user on this Mac or how to become one. | Say how to check, or that IT must do it. |
| Tools and databases, "a proxy is the usual cause. Setting `HTTPS_PROXY` in the shell that launches LGE" | I cannot perform any of this. Proxy, shell, and setting a variable are all beyond me, and I do not know if my university network uses a proxy. | Say to ask IT whether a proxy is in use, then give the command for them. |
| Containers, "isolated environments holding a whole operating system's worth of software" | Read twice. The definition is fine but "whole operating system's worth" made me think I needed to install one. | Say the pipeline brings its own software with it. |
| Containers, "switch a Nextflow run to a local `.nf` file or to a conda executor" | I could not perform this. Two options are named and neither is shown, and executor is a new word. | Point to where in the wizard this choice is made. |
| Containers, "Add `--pull-test` to check that the machine can actually fetch an image" | Image is a new word and it is not the picture kind. I read the sentence twice. | Gloss image as the packaged software the container runs. |
| Containers, "reported macOS Version 26.6.2, 14 CPU cores, 48 GB of physical memory" | I cannot judge my own numbers against these. My laptop has 16 GB and I now do not know whether that is enough. | Say the minimum LGE needs, not just what one machine had. |
| Is this file intact, "`--strict` raises the bar" | I do not know what bar or which extra checks. I would not know when to use it. | Say what strict additionally rejects. |
| Is this file intact, "it handles only signed records and that signing is off by default, so on an ordinary unsigned sidecar it exits 64" | Longest sentence in the chapter and I read it three times. The upshot is that the command is useless to me by default, and that arrives last. | Lead with the fact that provenance verify does nothing unless signing was turned on. |
| Is this file intact, "read the sidecar's `exitStatus` field" | I cannot perform this. I do not know where the sidecar file is or how to open it. | Say where the sidecar sits next to the result. |
| Is this file intact, "LGE rebuilds a `.fai`, a `.bai`, or a `.tbi`" | Three extensions with no glosses. I know BAM from the core but not BAI. | Say these are index files that let LGE jump into a large file. |
| Is this file intact, "the underlying tools are `samtools faidx`, `samtools index`, and `tabix`" | I cannot perform this and I do not know whether I would ever need to, since the paragraph opens by saying it happens on its own. | Say this is only needed if the automatic rebuild fails. |
| Reporting, "Add the installed tool versions from `lungfish-cli version --tools`" | I cannot perform this, so my bug report will be missing a piece the chapter says to include. | Say this part is optional, or give a window route. |
| Reporting, "sequence data is rarely yours alone to share" | Good advice, but I could not tell what counts as data here. The failure report contains file paths with my sample names in them. | Say to check the report for sample names before you send it. |
| Whole chapter | No entry told me what a red row actually looks like or that red means failed. I arrived with a red row and the chapter never names that colour. | Say in the opening that a failed run shows as a red row. |
| Whole chapter | There is no route for me. Twenty-plus fixes are commands and I have never opened a terminal, yet the audience is listed as bench-scientist. | Add one line early saying which sections need the command line. |

One thing I learned. Every failed run writes a report file the moment it fails, and right-clicking the row copies the whole thing in one go.

One thing I still could not do. Run a single one of the diagnostic commands, because the chapter never says where a command gets typed.

The sentence I liked most. "Do not reconstruct the command by hand from what you remember choosing in a dialog, because that is how a real defect gets mistaken for a typing mistake and an hour disappears."
