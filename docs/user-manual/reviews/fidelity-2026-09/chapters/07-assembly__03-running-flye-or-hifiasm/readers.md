# Merged reader report - Running Flye or hifiasm

Four readers reviewed this chapter. Personas were a genetics sophomore who has never opened a terminal, a wet-lab senior with two years of pipetting and no data analysis, a pre-med student reading in a second language, and an undergraduate who has used Geneious. None had opened a terminal before.

| Location | Issue | Readers who hit it | Suggested fix |
|---|---|---|---|
| What it is, "walking the unambiguous paths through that graph" | All four readers could not picture what an unambiguous path is or what it means for the assembler to walk one. | 1, 2, 3, 4 | Say the assembler follows a stretch only while there is exactly one way forward, and stops where the path forks. |
| What it is, "LGE adds the flag that tells it so" | All four readers met "flag" with no gloss, and none had opened a terminal, so the word carried no meaning. | 1, 2, 3, 4 | Gloss flag at first use as a command-line option, or say plainly that LGE sets it for you. |
| Why you would do this, "where one plasmid ends and the chromosome begins" | All four readers met "plasmid" with no gloss, though the word is used again later in the chapter. | 1, 2, 3, 4 | Gloss plasmid at first use as a small separate circular DNA molecule alongside the chromosome. |
| Why you would do this, "downsamples each set to roughly 300-fold coverage" | All four readers understood the definition of coverage but could not judge whether 300-fold is a lot, since no typical figure is given for comparison. | 1, 2, 3, 4 | State a typical whole-genome coverage figure so 300-fold has a scale. |
| Step 1.3, "recognising the shape the instrument writes" | All four readers found "the shape the instrument writes" vague and could not picture what LGE actually matches to detect the read type. | 1, 2, 3, 4 | Say LGE matches the read header text against known patterns for each instrument. |
| Step 1.5, "reads called by a recent high-accuracy basecaller model" | All four readers stumbled on "called" used as a verb for basecalling, a sense none of them already had. | 1, 2, 3, 4 | Say the reads were translated from the raw signal by a basecaller, and use "basecalled" rather than "called." |
| Step 1, "The reference run finished in 35.2 seconds." | All four readers could not judge their own run against a figure given to one decimal place, especially since the chapter also warns that numbers will differ. | 1, 2, 3, 4 | Give a rough range, for example under two minutes on a recent laptop, instead of a precise figure. |
| Step 3, "named for the tool and the moment it started, such as `flye-2026-09-07T05-12-23`" | All four readers stopped on the timestamp format, unsure what the T and the surrounding digits meant. | 1, 2, 3, 4 | Say plainly that the name carries the date, then the letter T, then the time of day. |
| Step 3, "That item appears only on a bundle that carries assembly provenance" | All four readers could not tell which of their own sidebar rows would carry assembly provenance, even though the term itself is glossed inline. | 1, 2, 3, 4 | Say plainly that assembly results always carry it and imported reads never do. |
| Settings, "those closing sentences belong to the optional command-line section" | All four readers found the trailing command-line sentence on each Settings entry confusing to skip, since it sits in the middle of paragraphs meant for GUI readers. | 1, 2, 3, 4 | State once at the top of Settings that the last sentence of every entry is for the command line and can be skipped. |
| Settings, Read Type, "which is what happens with a PacBio CLR library, since CLR has no read class of its own in this version" | All four readers met "CLR" with no expansion, in the one case where the control actually unlocks. | 1, 2, 3, 4 | Expand CLR at first use and say plainly that it is an older PacBio mode. |
| Settings, Profile, "to skip both the duplicate-purging step and the memory-saving filter it normally builds first" | All four readers found duplicate purging and the memory-saving filter named but never explained, so could not judge what skipping them costs. | 1, 2, 3, 4 | Add a half sentence saying what duplicate purging removes and when skipping the filter is unsafe. |
| Settings, Threads, "defaults to the smaller of your Mac's core count and 8" | All four readers did not know their own Mac's core count or where to find it, so could not predict or judge the default. | 1, 2, 3, 4 | Say the sheet already shows the chosen number, so the reader does not need to know their core count. |
| Settings, "`--memory-gb` is accepted by the command and then ignored" | All four readers found this and the surrounding command-line-only options sitting in the middle of the GUI Settings section, meant for a reader who would never type them. | 1, 2, 3, 4 | Move the command-line-only options into the optional command-line section. |
| Reading the results, "so if you want Flye's own per-contig coverage figure you read it out of `assembly_info.txt`" | All four readers were told to open a file in the run folder but never told how to reach the run folder from inside the app. | 1, 2, 3, 4 | Say how to reveal the run folder in Finder from the sidebar. |
| Reading the results, "marked circular with a multiplicity of 4" | All four readers met "multiplicity" with no gloss and could not judge whether 4 was a good or bad number. | 1, 2, 3, 4 | Gloss multiplicity as how many times the assembler thinks the sequence repeats, or drop the figure. |
| What good looks like, "the fix is to collapse it by hand" | All four readers found this the only corrective action offered for the doubled contig, with no way to learn how to do it or which tool covers it. | 1, 2, 3, 4 | Point to the chapter or tool that trims a doubled contig, or say plainly that this is out of scope and the doubling is acceptable for teaching. |
| What good looks like, "roughly one per chromosome for a well-covered HiFi assembly of a small eukaryote" | All four readers found "well-covered" and "small eukaryote" unquantified, so could not judge their own assembly against the rule. | 1, 2, 3, 4 | Give a coverage figure and an example organism, or give the number for the worked example instead of a general rule. |
| What it is, "the few hundred an Illumina instrument produces" | Three readers met Illumina named as the short-read comparison with no gloss saying what it is. | 1, 2, 4 | Add a short clause naming Illumina as the common short-read sequencing instrument. |
| What it is, "keep the two parental copies of each chromosome apart rather than blending them" | Three readers met the idea of parental copies here, well before ploidy and haplotype are glossed in the procedure and in Settings. | 1, 2, 3 | Move the one-line ploidy gloss up to this first mention. |
| What it is, "Each run produces a `.lungfishref` assembly bundle" | Three readers stopped on the file extension "ref" attached to something the chapter calls an assembly, assuming a typo or confusing it with something they already had. | 1, 2, 3 | Add a clause saying assembly and reference results share one bundle format. |
| What it is, "There is no `Assemblies` folder." | Three readers had never been told an Assemblies folder might exist, so this denial confused rather than reassured. | 1, 2, 3 | Drop the sentence, or say plainly where assembly results do land. |
| Why you would do this, "the son in the Ashkenazim trio that the Genome in a Bottle consortium distributes" | Three readers met several unfamiliar proper nouns in one sentence, Ashkenazim, trio, Genome in a Bottle, and the sample codes, with none glossed. | 1, 2, 3 | Gloss trio as mother, father, and child sequenced together, and put the catalogue names in a parenthesis. |
| Before you start, "Download the fixture folder from the manual's fixtures on GitHub" | Three readers hit a folder URL that the text immediately says cannot be used, before giving the working route. | 1, 3, 4 | Lead with the working ZIP download route and drop or defer the unusable folder link. |
| Before you start, "Import both into your project as described in Importing FASTQ Files" | Three readers were sent to another chapter to complete a required step, unsure how long the detour was or whether both files import as one bundle or two. | 1, 3, 4 | Give the import clicks inline and keep the cross-reference as further reading, and state whether each file becomes its own bundle. |
| Before you start, "LGE pins the versions it was tested against" | Three readers met "pins" used as a verb and had to guess it meant "fixes" or "locks." | 1, 3, 4 | Say LGE always uses these exact tested versions. |
| Procedure, "Every figure quoted in this chapter came from real runs made on 2026-09-07 through `lungfish-cli`" | Three readers, none of whom had opened a terminal, worried that the chapter's numbers only apply if they use the command line. | 2, 3, 4 | State plainly that the app sheet produces the same results and the command line was used only for reproducibility. |
| Step 1, "Flye announces its stages by name ... configure, assembly, consensus, repeat, contigger, polishing, and finalize" | Three readers met seven stage names, including "contigger," with no gloss and no signal about whether a stalled stage was normal. | 1, 2, 3 | Say plainly that these are Flye's own internal stage names and need no action from the reader. |
| Step 2.3, "A picker with one option in it looks broken and is not." | Three readers had to read this sentence twice, either because the subject drops out or because the reassurance comes after the worry rather than before it. | 1, 2, 3 | Split the sentence, and move the reassurance ahead of the explanation. |
| Settings, "Two controls you may remember from SPAdes are absent for both of these tools" | Three readers had not read the SPAdes chapter, so the reminder named two controls, Memory Limit and Min Contig, they had never seen. | 2, 3, 4 | Briefly describe what each control does rather than assuming the earlier chapter was read. |
| Settings, Profile, "quietly also passes `--n-hap 1`, `-l0`, and `-f0`" | Three readers found three raw command-line flags dropped into a sentence meant for GUI readers, unable to judge what each one changes. | 2, 3, 4 | Describe the three effects in words and keep the raw flags out of the sentence. |
| Settings, Extra arguments, "a genome-size hint to Flye written as `--genome-size 16k`" | Three readers found 16k unexplained shorthand, not obviously tied to the 16,569 bp figure used elsewhere. | 1, 2, 3 | Say 16k means 16,000 bases, and tie it explicitly to the mitochondrial genome size. |
| Settings, Project Name, "with any mate suffix stripped" | Three readers met "mate suffix" with no gloss and could not tell whether it applied to their own single-end files. | 1, 3, 4 | Gloss mate suffix as the `_R1`/`_R2` marker on paired files, and say it applies only to paired files. |
| Settings, "prints four read-only descriptions ... ONT Read Mode, Genome Size, Overlap / Assembly Coverage, and Haplotype Controls" | Three readers found eight capitalised names listed with no explanation of any of them. | 1, 2, 3 | Say plainly that these are reference notes the reader can skip unless writing extra arguments. |
| Reading the results, "the whole-assembly GC content as a percentage" | Three readers found GC content linked but not explained in text, with no sense of what a normal value looks like. | 1, 2, 4 | Gloss GC content in one line and say what it is used for here, or say the values need no judgement. |
| Reading the results, "N50 is the length such that half of all the assembled sequence sits in contigs at least that long" | Three readers read the N50 definition multiple times, since the method sentence that follows restates rather than illustrates it. | 2, 3, 4 | Split the definition from the how-to-compute sentence, and add a short worked example. |
| Reading the results, "that contig is 210 bp, or 1.3%, short. That shortfall is expected" | Three readers understood the arithmetic but could not tell how much shortfall would count as a real problem. | 1, 2, 3 | Give a threshold above which a shortfall should worry the reader. |
| Reading the results, "Flye's own `assembly_info.txt` marks this contig `circ. Y`" (second mention) | Three readers were pointed a second time to a raw file value inside `assembly_info.txt`, still without being told how to reach it. | 2, 3, 4 | Say where the app itself shows circularity, or give the one line needed to open the run folder. |
| Reading the results, "Flye 2.9.6 is not deterministic here" | Three readers met "deterministic" with no gloss and had to work out from context that identical input can give different output. | 1, 2, 3 | Gloss deterministic in the sentence that introduces it, as meaning the same input can give a different answer each run. |
| Reading the results, "`<project name>.bp.p_ctg.gfa`, and beside it the two haplotype-resolved graphs" | Three readers could not decode p_ctg or bp, and found the first filename carrying a prefix placeholder while the other two did not. | 1, 2, 3 | Gloss p_ctg as primary contigs, and write all three filenames the same way. |
| What good looks like, "reported as completed with no contigs rather than as a failure" | Three readers could not tell where this wording actually appears, in the Operations Panel or the viewport. | 1, 2, 4 | Name where the completed-with-no-contigs message is shown. |
| What good looks like, "map the reads back to the assembly and look at whether coverage is even along it" | Three readers were pointed to this as the real check but found it a forward reference to another chapter, with no shortcut given here. | 2, 3, 4 | Say in one sentence what uneven coverage would look like, and note that this check is optional for the fixture. |
| On the command line, "which for gzipped FASTQ is just `cat a.fastq.gz b.fastq.gz > combined.fastq.gz`" | Three readers, none of whom had opened a terminal, found this the only stated way to combine multiple FASTQ files, with no way to perform it. | 1, 3, 4 | Say whether the app itself can merge FASTQ files, or state plainly that this step needs a terminal. |
| What it is, "the two long-read assemblers that Lungfish Genome Explorer (LGE) ships" | Two readers found "assembler" used as a noun for a program before anything says it is a program. | 1, 2 | Say "assembler, meaning a program that joins reads into longer sequences" at first use. |
| What it is, "each stretch of sequence the reads agree on is a point" | Two readers found "point" and "connection" used as informal stand-ins for "node" and "edge," with the formal terms arriving later unlinked. | 1, 2 | Use node and edge here as well, and say they are the formal names for the same ideas. |
| What it is, "polishes the result afterwards" | Two readers found "polishing" named as a stage with no gloss, and guessed it meant error correction without being sure. | 2, 4 | Gloss polishing as a pass that corrects remaining base errors using the reads again. |
| Why you would do this, "a specific way that long-read assembly of a small circular target goes wrong" | Two readers had the failure mode teased here and withheld until a section three headings later, and read on worrying about what it was. | 1, 2 | Name the failure in one clause at this first mention. |
| Before you start, "double-click the downloaded file to unpack it, and find the folder inside it" | Two readers were not told what name the unpacked folder would have, so did not know what to look for in Downloads. | 2, 4 | Name the folder the ZIP unpacks to. |
| Before you start, "double-click the downloaded file to unpack it" | Two readers were not warned that the ZIP download is the whole manual repository, with only two files actually needed. | 1, 2 | Say plainly that the download is the whole repository and only two files inside it are needed. |
| Procedure step 1, "Click the bundle holding `HG002.chrM.ont.fastq.gz`" | Two readers could not tell what name the sidebar row would actually show, since the filename and the bundle name may differ. | 1, 2 | State what name the imported bundle displays in the sidebar. |
| Step 1.3, "reports the read class LGE detected on the **Detected** row, which should read ONT reads" | Two readers met "ONT" used with no expansion, despite "Oxford Nanopore" being spelled out earlier without ever being tied to the abbreviation. | 1, 4 | Write "ONT, short for Oxford Nanopore Technologies" at first use of the letters. |
| Step 1, "Leave **Advanced Settings** collapsed, since the Settings section below explains everything inside it" | Two readers hunted forward and back because the sheet's Advanced Settings and the chapter's Settings section share one name. | 1, 4 | Call the chapter section "the Settings section of this chapter" to distinguish it from the sheet's own Advanced Settings. |
| Procedure step 3, "falling back to whatever the bundle recorded at import" | Two readers had not been told that bundles record anything at import, and met the idea here for the first time. | 2, 3 | Say plainly that the import step stores the detected read type for later use. |
| Procedure step 5, "The fixture's reads qualify." | Two readers could not tell what the reads were qualifying for, since the sentence gives no object. | 2, 3 | Say plainly that the fixture reads were produced by the high-accuracy basecaller model just described. |
| Procedure, "The panel streams Flye's own output as it works" | Two readers were unsure what to do with the streamed text or whether reading it mattered. | 2, 3 | Say plainly that the reader can ignore this output unless the run fails. |
| Step 1.1, "The sheet has no input picker of its own, so whatever is selected when you open the menu is what gets assembled" | Two readers were unsettled that a stray click before opening the menu could silently change the input with no on-screen check. | 3, 4 | Say the Inputs section names the selected bundle so the reader can verify it before pressing Run. |
| Step 2, "It writes its assembly as a GFA file, a text format for assembly graphs in which each node carries its sequence" | Two readers found "node" and "edge" reappearing here without being linked back to "point" and "connection" from earlier in the chapter. | 1, 3 | Use one vocabulary throughout, and say plainly that node and edge are the formal names for point and connection. |
| Settings, "The sheet carries seven controls for each assembler. Five sit in plain view ... and two sit inside the Advanced Settings section." | Two readers counted eight bold entries below and thought the arithmetic was wrong, since Metagenome mode and Primary contigs only each belong to only one tool. | 1, 2 | Say five plus two per assembler, and name which pair is tool-specific. |
| Settings, Extra arguments, "anything you pass here which the Haploid/Viral profile would also have added wins" | Two readers had to reread this sentence, since the subject and verb sit far apart and "wins" personifies the argument. | 2, 3 | Split the sentence, and say plainly that the reader's own value replaces the profile's. |
| Reading the results, "One is the ideal for a single circular replicon" | Two readers met "replicon" with no gloss, used again later in the chapter. | 1, 2 | Gloss replicon at first use as one separate DNA molecule. |
| Reading the results, "an N50 near the expected genome size means the assembly is mostly a few long pieces while a small N50 means it is mostly rubble" | Two readers found "rubble" vivid but could not turn "small" into a number to compare against their own result. | 2, 3 | Give a rough fraction of genome size below which N50 counts as small. |
| Reading the results, "Dozens or hundreds from a long-read run points upstream rather than at the assembler" | Two readers had to unpack "points upstream" as a figure of speech for "earlier in the workflow." | 3, 4 | Say plainly that the problem lies in the reads rather than in the assembler. |
| On the command line, "This section is optional." | Two readers found this reassuring, but noted that Settings and Reading the results had already carried command-line-only content, so the promise came late. | 2, 3 | Keep command-line-only content out of the earlier sections so the optional-section promise holds from the start. |
| Settings, Extra arguments, "which is the escape hatch for any option the sheet does not expose" | Two readers met "escape hatch" as an idiom, and could not act on the accompanying warning since they would not recognise a valid option from an invalid one. | 3, 4 | Say plainly that a typo here makes the run fail, and that most readers should leave the field empty. |
| What it is, "the two long-read assemblers that Lungfish Genome Explorer (LGE) ships" ("ships" as a verb) | One reader read "ships" as a boat and stopped to work out the software sense. | 3 | Use plainer wording such as "LGE includes these two assemblers." |
| What it is, "reads whose read length runs to tens of thousands of bases" (threshold placement) | One reader wanted the length threshold folded into the same sentence that first calls the reads "long-read." | 4 | Put the length figure in the sentence that introduces the term "long-read." |
| What it is, "each stretch of sequence the reads agree on is a point" ("agree on" undefined) | One reader could not tell how many reads must agree, or what disagreement looks like. | 3 | Say the assembler keeps a stretch when several reads read it the same way. |
| What it is, "so it first stitches reads into rough draft sequences that tolerate disagreement" | One reader could not tell whether "stitches" and "tolerate disagreement" pointed to a real intermediate file or were purely figurative. | 3 | Say plainly that Flye first builds draft sequences that allow mismatches. |
| What it is, "accurate enough to be trusted almost base for base" | One reader read "base for base" as an idiom modeled on "word for word" and was not confident of the meaning. | 3 | Say plainly that each individual base is almost always correct. |
| Why you would do this, "which is 16,569 base pairs long and circular" | One reader met the accession `NC_012920.1` with no explanation of what an accession is or where it comes from. | 2 | Say it is the standard catalogue identifier for the human mitochondrial genome. |
| Why you would do this, "so that everyone can check their own assembly against a known answer" | One reader found "a known answer" vague, unsure where such a comparison file would actually be. | 3 | Say plainly that a published reference sequence exists for this sample. |
| Why you would do this, "which makes it a good teaching case, and an honest one" | One reader was puzzled by the personification in "an honest one," since a dishonest example made no sense to them. | 3 | Drop the phrase, or say plainly that the example shows a real failure mode. |
| Why you would do this, "A real Nanopore assembly of a bacterial genome behaves the same way and takes hours instead of seconds" | One reader was told the timing but not whether they should actually attempt the larger example. | 3 | Say plainly that the chapter's fixture is all that is needed to follow along. |
| Why you would do this, "The fixture slices both read sets down to the mitochondrial genome" | One reader read "slices down to" as a physical operation and could not tell whether the fixture was a subset of a larger file or a separate sequencing run. | 4 | Say plainly that the fixture keeps only the reads that came from the mitochondrion. |
| Why you would do this, "because those are precisely the questions a fork in the graph leaves unanswered" | One reader had to scroll back to recover the fork metaphor from two sections earlier. | 3 | Restate in plain words that the assembler stops where a repeat makes the path ambiguous. |
| Before you start, "install Genome Assembly if it is not already there" | One reader could not perform this step, since neither the install button nor a rough download time is named. | 2 | Name the install button and give a rough download time or size. |
| Before you start, "The pack holds all five assemblers together" | One reader could not tell which five assemblers were meant, since they are named only later in the chapter. | 2 | Name the five assemblers at this first mention, or cut the count. |
| Before you start, "Flye 2.9.6 and hifiasm 0.25.0" | One reader had no way to check the tool versions actually installed on their own machine. | 2 | Say where in the app the installed tool version is shown. |
| Before you start, "**File > New Project** (Cmd-N)" | One reader had to work out unaided that the keyboard shortcut notation meant holding a modifier key. | 2 | Confirm this convention is explained once, early in the manual. |
| Before you start, "A plugin pack is a themed group of tools that downloads on demand" | One reader could not tell whether the download needed an internet connection. | 3 | State plainly that the download needs an internet connection once. |
| Before you start, "choose **File > New Project** (Cmd-N), or click ... pick an empty folder" | One reader found "pick a folder" and "pick an empty folder" arriving in that order to read as a contradiction. | 3 | Merge into one instruction to pick or create an empty folder. |
| Before you start, "holding 950 Nanopore reads" and "363 HiFi reads" | One reader could not judge whether 363 reads was enough, since the number is not tied back to the earlier 300-fold coverage figure. | 2 | Tie the read counts back to the 300-fold coverage figure so they make sense together. |
| Procedure, "Every figure quoted in this chapter came from real runs" ("figure" ambiguity) | One reader first read "figure" as a picture rather than a number, since the chapter also has picture-sense figures. | 1 | Use "number" in place of "figure" throughout this sentence. |
| Settings, "Advanced Settings" (expand/collapse state) | One reader could not tell whether Advanced Settings was a section to click open or one already closed by default. | 2 | Say plainly that Advanced Settings is closed by default. |
| Procedure step 3, "states whether the reads are single or paired on the Read Layout row" | One reader knew paired-end from library prep but the chapter never says which layout long reads use. | 2 | Say plainly that long reads are always single. |
| Step 1.4, "since there is nothing to decide once detection has run" | One reader could not tell what to do if the Detected row showed the wrong read type. | 3 | Say what to do when the Detected row is wrong. |
| Procedure, "defaults to the input file's name with `_assembly` appended" | One reader found this description in Procedure did not match the fuller wording given later in Settings, and mate suffix was never explained. | 2 | Make the two descriptions match, and gloss mate suffix where it first appears. |
| Procedure after 1.5, "which defaults to the input file's name with `_assembly` appended" (underscore formatting) | One reader was unsure whether the underscore was something they needed to type themselves. | 3 | Show the actual resulting name for the fixture file. |
| Procedure, "on a recent Apple silicon laptop" | One reader did not know whether their own Mac counted as Apple silicon. | 2 | Say which Mac models this means, or give a year the transition completed. |
| Procedure, "expect your numbers to sit near these rather than matching exactly" | One reader was given no tolerance, unsure whether a 1% or a 50% difference still counted as normal. | 3 | Say roughly how far apart a reader's own numbers can be while still being normal. |
| Procedure step 2 of hifiasm, "the Detected row reading PacBio HiFi/CCS" | One reader met "CCS" unexpanded, despite "circular consensus sequencing" being spelled out earlier without being tied to the letters. | 2 | Expand CCS at this first use of the abbreviation. |
| Procedure, "Hifiasm does not write a FASTA. It writes its assembly as a GFA file" | One reader did not know what a FASTA was beyond "a sequence file" and could not see why either format mattered to them. | 2 | Say plainly that the reader never has to open either file themselves. |
| Procedure, "LGE reads the primary contig graph out of that file" | One reader met "primary contig graph" before "primary" and "alternate" are explained later in Settings. | 2 | Gloss primary at this first use, or move the sentence after the terms are introduced. |
| Procedure step 3, "Double-click either folder in the sidebar and the assembly viewport opens." | One reader could not reconcile double-clicking a folder here with the earlier instruction to single-click a bundle, and was unsure the Analyses folder appears in the sidebar at all. | 2 | Say plainly that the run folder appears in the sidebar under Analyses. |
| What it is, "The length is the whole point, and the reason is mechanical rather than magical" | One reader stopped on the figure of speech and asked which of the two the assembler actually was. | 3 | Say plainly that the reason is a property of the graph, not a mystery. |
| Procedure after 1.5, "none of it needs changing for a single organism at even coverage" | One reader could not check "even coverage" anywhere in the app, so could not confirm the condition applied to their own data. | 3 | Say the fixture meets this condition, and where a reader could check it for their own data. |
| Step 2.1, "Opening a result moves the selection, so reselect the reads" | One reader did not recognise "moves the selection" as app-internal wording and had not realised a result had already been opened. | 3 | Say plainly that clicking anything else changes what is selected, so the reads need to be clicked again. |
| Step 2.4, "Ploidy is how many copies of each chromosome" (second occurrence) | One reader noted the ploidy gloss arrives here, long after the same idea was used earlier in What it is. | 3 | Move the ploidy gloss to its first use in the chapter. |
| Procedure after 2.5, "so the conversion happens without you asking and before the viewport can list anything" | One reader lost the subject partway through this three-clause sentence. | 3 | Split the sentence after "before the viewport can list anything." |
| Settings, "so a paragraph covering a shared control says so explicitly" | One reader reread this note about the document's own structure and gained nothing from it. | 3 | Delete the note, or shorten it to a single clause. |
| Settings, Metagenome mode, "a mixed community of organisms present at wildly different abundances" | One reader found "metagenome" described but never named as a term in its own right. | 2 | Name the term "metagenome" explicitly at this point. |
| Settings, Metagenome mode, "present at wildly different abundances rather than one even depth" | One reader found "wildly" informal and "even depth" repeated an "even coverage" idea they still could not check. | 3 | Say plainly that the organisms are present in very different amounts. |
| Settings, Metagenome mode, "leave it off for anything you would call one genome" | One reader could not judge the boundary case the instruction asked them to imagine. | 3 | Give one example on each side of the boundary. |
| Settings, Primary contigs only, "the alternate haplotype copies" | One reader could not judge which of primary or alternate they wanted, since the distinction is never explained in text. | 2 | Gloss primary and alternate in one sentence at this point. |
| Settings, Primary contigs only, "remembering that the viewport lists the primary contigs" | One reader reread this participle-led sentence twice to find what changes and what stays the same. | 3 | Split the sentence into two. |
| Settings, Project Name, "for hifiasm alone this name also becomes the output prefix hifiasm writes every one of its files under" | One reader could not tell whether the output prefix mattered to them at all. | 2 | Say plainly that it only affects file names inside the run folder. |
| Reading the results, "a preview of its opening sequence" | One reader could not tell how long the preview shown actually was. | 1 | Say how many bases the preview shows. |
| Reading the results, "an N50 near the expected genome size means the assembly is mostly a few long pieces" | One reader was told to compare N50 against the genome size only here, after an earlier paragraph gave no comparison target at all. | 1 | State up front, at the definition, that N50 is judged against the expected genome size. |
| Reading the results, "33,140 divided by 16,569 is 2.0002" | One reader understood the arithmetic but needed a second reading to see that a doubled contig counted as a bad result, since "artifact" carried the judgement unglossed. | 2 | Gloss artifact as a false result produced by the method, not a real biological feature. |
| Reading the results, "Hifiasm walks the overlap graph looking for a point at which to stop" | One reader counted four different graph words across the chapter, overlap, assembly, repeat, and unitig, and could not tell whether they meant the same thing. | 2 | Say plainly that these are all the same graph, seen at different stages of the process. |
| Reading the results, "It adds the resolved tool version and the wall time" | One reader guessed "wall time" meant clock time but was not certain. | 3 | Gloss wall time as how long the run actually took. |
| Reading the results, "because a few long contigs and a swarm of short ones can share an average with a uniform set" | One reader read this compressed, figurative sentence three times and remained unsure of the point. | 3 | Replace the metaphor with a short two-number example. |
| Reading the results, "an assembler emitting a circle as a straight sequence has to cut it somewhere" | One reader needed two readings to follow this four-step physical image of trimming a circular junction. | 3 | Split the sentence in two, separating the reason from the mechanism. |
| Reading the results, "hifiasm goes round the mitochondrial circle twice before calling a boundary" | One reader met "call" used in a third distinct sense in this chapter. | 3 | Say plainly that hifiasm stops after two passes and reports both. |
| Reading the results, "its choice of where to break the loop is the least constrained" | One reader found "least constrained" too abstract to turn into any concrete expectation. | 3 | Say plainly that the assembler has no clear place to cut, so the result varies. |
| Reading the results, "Hifiasm keeps more than it shows." | One reader did not know what "it" referred to until the following sentence. | 3 | Say plainly that hifiasm writes more files than the viewport lists. |
| Reading the results, "Running the identical command on the identical reads twice produced two different answers" | One reader was stopped completely by this and did not know whether to rerun the tool or which of two answers to trust. | 4 | Say what to do when two runs disagree, for example keeping the one closer to the expected genome size. |
| Reading the results, "plus the unitig graphs, which hold the unambiguous stretches before hifiasm joined any of them into contigs" | One reader could not see why they would ever want to open a unitig file. | 4 | Say plainly that unitig files are not needed for anything in this chapter. |
| On the command line, "lungfish-cli assemble HG002.chrM.ont.fastq.gz \\" | One reader, who had never opened a terminal, could not run this example because the backslash line continuation is never explained. | 1 | Add one sentence saying the backslash continues the command onto the next line. |
| Next, "turn one of these contigs into a reference bundle" | One reader, having just learned the doubled hifiasm contig is an artifact, did not know which of the two results the next chapter expects them to use. | 1 | Say plainly which run's contig the next chapter carries forward. |
| What good looks like, "or a read set that is not really long-read data at all" | One reader did not know how they would end up with the wrong data type, or how to check for it. | 2 | Say plainly that the Detected row from Step 1 is the check. |
| What good looks like, "which is the normal shape of a circular contig's shortfall" | One reader found "normal shape" applied to a percentage a mixed image, and expected a discussion of actual geometry. | 3 | Say plainly that this shortfall is expected, without the shape metaphor. |
| What good looks like, "most often a stretch where coverage dropped away to nothing" | One reader noted this was the third mention of low coverage as a cause, still with no number to check against. | 3 | Give a coverage figure below which assembly usually fails. |
| What good looks like, "is one good contig surrounded by debris, and worth rescuing" | One reader met a third building metaphor, after "rubble" earlier, layered with "worth rescuing." | 3 | Say plainly that only the second of the two cases is usually usable. |
| What good looks like, "An assembler that exits cleanly but emits nothing" | One reader, who had never opened a terminal, stumbled on "exits cleanly" as unexplained terminal language. | 3 | Say plainly that the run finishes without an error but produces no contigs. |
| What good looks like, "A contig is a hypothesis about what the genome looks like ... nothing in the assembly checked it against an external truth" | One reader liked this sentence but needed two readings to parse the double negative at its end. | 3 | Say plainly that the assembler never compared its result to a known genome. |
| On the command line, "The Run Mode picker that appears for the ... never appears" | One reader was told about Run Mode only to be told it never appears, the term's first and only mention. | 3 | Delete the mention, or explain Run Mode where it actually appears. |
| Whole chapter, entry_points lists "Tools > Assembly > Flye..." but no Reassemble path | One reader never saw the right-click Reassemble item mentioned outside of Step 3, and right-click menus are the one place they never look. | 4 | Mention the right-click Reassemble option once, in the procedure's lead-in. |
| Before you start, "choose **File > New Project** (Cmd-N)" | One reader could not tell whether this chapter needs a fresh project or the one carried over from the SPAdes chapter. | 1 | State plainly whether to reuse the previous project or start a new one. |
| What it is, "the same assembly sheet you used for SPAdes in the previous chapter" | One reader had not read the SPAdes chapter and found the sentence assumed they had. | 2 | Either add the SPAdes chapter to prerequisites or say the sheet is shared by all assemblers without assuming prior reading. |
| Settings, Metagenome mode, "This setting has no command-line flag of its own, so from the command line you pass `--extra-args`" | One reader noted the same two-sentence, GUI-then-terminal shape recurring for both Metagenome mode and Primary contigs only. | 2 | Consider moving both trailing command-line sentences to the command-line section together. |
| Settings, "Neither is a bug and neither has anything hidden behind it" | One reader found this reassurance addressed a worry they did not have, and the double negative made them reread. | 3 | Delete the reassurance, or state plainly what each control does instead. |
| Settings, Threads, "a higher value finishes sooner and leaves less of the machine free" | One reader had to reread this sentence since its two halves pull in opposite directions with no signal marking the trade-off. | 2 | Split the sentence into two, one for speed and one for the cost. |
| Reading the results, "because the number looks like a triumph until you notice it is doubled" | One reader found "looks like a triumph" a dramatic figure of speech that slowed them at an important sentence. | 3 | Say plainly that the number looks good but is in fact wrong. |
| Step 2 preamble, "Hifiasm does not write a FASTA. It writes its assembly as a GFA file" (placement) | One reader had already pressed Run before learning the output format differed from what they expected. | 4 | Move the conversion note ahead of the Run instruction, or say plainly that LGE handles the conversion automatically. |

## Consensus

Forty-three rows were hit by three or more readers.

- The unambiguous-path metaphor for how the assembler emits contigs is never made concrete.
- The command-line flag mentioned for hifiasm's Nanopore support is never glossed.
- Plasmid is never glossed at its first use.
- Three-hundred-fold coverage is never anchored against a typical figure.
- The description of how LGE detects the read type is too vague to picture.
- Basecalling is described with "called" and never glossed in that sense.
- The precise reference run time gives no tolerance for judging a reader's own slower run.
- The timestamp format in run folder names is never explained.
- Readers cannot tell which sidebar bundles carry assembly provenance.
- The trailing command-line sentence on every Settings entry is confusing to skip.
- CLR is never expanded, in the one case where the Read Type control actually unlocks.
- Duplicate purging and the memory-saving filter are named but never explained.
- The Threads default depends on a core count readers do not know and cannot easily find.
- Command-line-only options sit inside the GUI Settings section rather than the optional command-line section.
- Readers are told to open assembly_info.txt without being told how to reach the run folder.
- Multiplicity is never glossed, so a value of 4 cannot be judged.
- Collapsing a doubled contig by hand is the chapter's only fix with no procedure behind it.
- Well-covered and small eukaryote are never quantified, so readers cannot judge their own coverage against the rule.
- Illumina is named as the short-read comparison with no gloss.
- Parental copies and ploidy are introduced before the gloss that explains them.
- The .lungfishref extension on an assembly result is never explained.
- The "There is no Assemblies folder" sentence denies something readers were never told existed.
- Several unfamiliar proper nouns describing the HG002 sample arrive in one sentence.
- The fixture download leads with a dead GitHub folder link before the working ZIP route.
- The FASTQ import step sends readers to another chapter without saying how many bundles result.
- "Pins," used for fixed tool versions, is unexplained jargon.
- Mentioning the command-line tool early makes readers worry the command line is required.
- Seven Flye stage names, including "contigger," are listed with no gloss.
- The "picker with one option looks broken" sentence needs a second reading or better placement.
- The chapter assumes readers already completed the SPAdes chapter for two named controls.
- Three raw command-line flags are dropped into a Profile sentence meant for GUI readers.
- The 16k genome-size hint is unexplained shorthand not obviously tied to the mitochondrial genome size.
- Mate suffix is never glossed, though Project Name depends on it.
- Eight capitalised Advanced Settings descriptions are listed with no explanation of any of them.
- GC content is linked but never explained in text or given a normal range.
- The N50 definition is repeated rather than illustrated with a worked example.
- The shortfall on a trimmed circular contig has no threshold for when it becomes a real problem.
- A second pointer to assembly_info.txt still does not say how to reach the run folder.
- Deterministic is never glossed, though the chapter relies on the concept.
- Cryptic GFA filenames are written inconsistently and never decoded.
- The chapter cannot say where the completed-with-no-contigs message actually appears.
- The recommended coverage-evenness check is a forward reference with no shortcut given here.
- Combining multiple FASTQ files is only explained as a terminal-only command.
