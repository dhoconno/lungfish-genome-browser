# Reader 4 report: Running Flye or hifiasm

Persona: undergraduate who used Geneious for one semester, comfortable with a graphical sequence viewer, expects every operation to have a dialog, has never opened a terminal.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| What it is, "the two long-read assemblers that Lungfish Genome Explorer (LGE) ships" | "Long-read" is used before it is explained. I know what a read is from Geneious but not what makes one long, and the definition arrives a sentence and a half later. | Put the length threshold in the same sentence, so long-read means reads of tens of thousands of bases. |
| What it is, "the few hundred an Illumina instrument produces" | I have only ever imported finished sequences into Geneious. I do not know that Illumina is a short-read instrument or that it is the thing I would otherwise have used. | Name Illumina as the common short-read instrument at this first mention. |
| What it is, "Emitting contigs then means walking the unambiguous paths" | Had to read twice. "Unambiguous paths" is doing a lot of work and I could not tell whether the assembler walks all paths or picks one. | Say the assembler follows a path only while there is exactly one way forward. |
| What it is, "it first stitches reads into rough draft sequences that tolerate disagreement, then builds a repeat graph from those drafts and polishes the result afterwards" | Three unexplained terms in one sentence. "Rough draft sequences", "repeat graph", and "polishes" are all new, and "polishes" sounds cosmetic rather than error-correcting. | Gloss polishing as correcting the remaining base errors using the reads. |
| What it is, "Hifiasm also accepts Nanopore reads, and LGE adds the flag that tells it so" | "Flag" is terminal vocabulary and I have never opened a terminal. This is the first use in the chapter. | Gloss flag as a command-line option at first use, or say LGE handles this for you. |
| Why you would do this, "where one plasmid ends and the chromosome begins" | Plasmid is never explained. I met the word in genetics but could not say why it would be confused with a chromosome in an assembly. | Gloss plasmid as a small separate circular DNA molecule alongside the chromosome. |
| Why you would do this, "roughly 300-fold coverage, meaning each position is covered by about three hundred reads on average" | The gloss is good but I still cannot judge it. Is 300-fold a lot? The chapter never says what coverage is enough. | Add what coverage range is normal for a long-read assembly so 300-fold can be placed. |
| Why you would do this, "The fixture slices both read sets down to the mitochondrial genome" | "Slices down to" reads as a physical operation. I could not tell whether the fixture is a subset of a bigger file or a separate sequencing run. | Say the fixture keeps only the reads that came from the mitochondrion. |
| Before you start, "Download the fixture folder from the manual's fixtures on GitHub" | I have never used GitHub. The paragraph gives a folder URL, then immediately says that URL cannot be downloaded, which made me think I had misread. | Lead with the working Download ZIP route and drop the folder URL, or mark it as for reference only. |
| Before you start, "find the folder inside it under `docs/user-manual/fixtures/`" | The step could not be performed as written. The unzipped folder has a name I was not told, and I did not know whether to move the two files somewhere or leave them in Downloads. | Name the unpacked folder and say the files can stay where they are. |
| Before you start, "Import both into your project as described in Importing FASTQ Files" | This is a forward jump to another chapter for a step I cannot skip. In Geneious I would just drag a file into the folder tree and be done. | Give the one-line drag-or-menu import here and keep the cross-reference for detail. |
| Before you start, "LGE pins the versions it was tested against" | "Pins" is jargon in this sense. I read it as physically attaching something. | Say LGE uses these exact tested versions. |
| Procedure, "Every figure quoted in this chapter came from real runs made on 2026-09-07 through `lungfish-cli`" | The one place the whole procedure rests on is a terminal tool I was told is optional. It made me doubt whether the sheet gives the same numbers. | State that the sheet produces the same results and that the command line was used only for reproducibility. |
| Step 1.1, "The sheet has no input picker of its own, so whatever is selected when you open the menu is what gets assembled" | This is the opposite of Geneious, where the dialog shows and lets you change the selected documents. Nothing warns me that a mis-click before opening the menu silently changes the input. | Say the sheet cannot be corrected once open and the selection must be right first. |
| Step 1.3, "reports the read class LGE detected on the **Detected** row, which should read ONT reads" | "ONT" is never expanded. I know Nanopore from the earlier prose but not the abbreviation. | Expand ONT as Oxford Nanopore Technologies at first use. |
| Step 1.3, "recognising the shape the instrument writes" | Had to read twice. "The shape the instrument writes" is vague and I could not tell what is actually being matched. | Say LGE matches the pattern of the read name written by each instrument. |
| Step 1.5, "which suits reads called by a recent high-accuracy basecaller model" | "Called" here means basecalled, but I read it as named. I also cannot judge whether my fixture reads count as recent. | Use basecalled rather than called, and the text already says the fixture qualifies, so keep that adjacent. |
| Step 1 closing, "Leave **Advanced Settings** collapsed, since the Settings section below explains everything inside it" | The step tells me to skip a section and read about it later, which sent me hunting forward and back. I could not tell if anything in there was required. | State plainly that no advanced setting is needed for this run. |
| Step 1 closing, "The reference run finished in 35.2 seconds" | I do not know how to tell a finished run from a stuck one. The panel streams text but nothing says what the last line looks like when it is done. | Say what the Operations Panel row shows when the run completes. |
| Step 2 preamble, "Hifiasm does not write a FASTA. It writes its assembly as a GFA file" | Placed after the click-Run step, so I had already run it before learning the output format differs. Also I had to read twice to see that LGE fixes this for me. | Move the conversion note before Run, or open it by saying LGE handles the difference. |
| Step 3, "each in its own folder named for the tool and the moment it started" | Timestamps like `flye-2026-09-07T05-12-23` are not explained, and the `T` in the middle made me think it was a code. | Say the name carries the date and the time of day. |
| Step 3, "That item appears only on a bundle that carries assembly provenance" | "Provenance" is unexplained at first use, and the gloss that follows is thinner than the word. In Geneious the equivalent is just document history. | Gloss provenance as the stored record of how a result was made. |
| Settings preamble, "Two controls you may remember from SPAdes are absent for both of these tools" | I did not do the SPAdes chapter, so this assumes knowledge I do not have and names two controls I have never seen. | Describe what Memory Limit and Min Contig would have done rather than assuming the earlier chapter. |
| Settings, Read Type, "which is what happens with a PacBio CLR library, since CLR has no read class of its own in this version" | CLR is never expanded and never explained. I could not tell whether this is a problem I might hit or trivia. | Expand CLR and say plainly that CLR reads are not supported here. |
| Settings, Profile, "which quietly also passes `--n-hap 1`, `-l0`, and `-f0`" | Three raw command-line strings inside a sentence I am supposed to read as a GUI user. I could not judge what they change. | Describe the three effects in words and keep the flags out of the sentence. |
| Settings, Profile, "to skip both the duplicate-purging step and the memory-saving filter it normally builds first" | Had to read twice, and neither duplicate purging nor the memory-saving filter is explained anywhere else. | Add a half-sentence saying what duplicate purging removes. |
| Settings, Threads, "Sets how many processor cores the assembler may use at once" | I do not know how many cores my Mac has or how to find out, so I cannot judge the default or a change. | Say the sheet already shows the safe maximum, so any value it allows is fine. |
| Settings, Metagenome mode, "so from the command line you pass `--extra-args \"--meta\"` instead" | Every Settings entry ends with a command-line sentence I was told I could ignore, but they are not visually separable, so I read them all and had to keep discarding them. | Split the command-line sentences out of the entries into the optional section. |
| Settings, Extra arguments, "the escape hatch for any option the sheet does not expose" | "Escape hatch" is idiom, and "without LGE checking it" is a warning I could not act on since I would not know a valid option from an invalid one. | Say that a typo here makes the run fail and that most readers should leave it empty. |
| Settings, Project Name, "with any mate suffix stripped" | "Mate suffix" is unexplained. My files have no `_R1` in the name so I could not tell whether this applies to me. | Gloss mate suffix as the `_R1` or `_R2` marker on paired files. |
| Settings, closing paragraph, "`--memory-gb` is accepted by the command and then ignored" | This whole paragraph is command-line only and sits in the middle of the GUI Settings section, so I read it before realising it did not apply to me. | Move the five command-line-only options into the optional command-line section. |
| Reading the results, "the whole-assembly GC content as a percentage" | GC content is linked but the chapter never says what a GC value tells me. Flye reports 43.9% and hifiasm 44.4% and I cannot judge either. | Say what GC content is used for here, or that these values need no judgement. |
| Reading the results, "if you want Flye's own per-contig coverage figure you read it out of `assembly_info.txt` in the run folder" | I do not know how to reach the run folder from the app or how to open a `.txt` inside a bundle. In Geneious everything is in the window. | Say how to reveal the run folder in Finder from the sidebar. |
| Reading the results, "Dozens or hundreds from a long-read run points upstream rather than at the assembler" | "Points upstream" is idiom for earlier in the workflow, and I had to read the rest of the sentence to work that out. | Say the problem lies in the reads rather than the assembler. |
| Reading the results, "N50 is the length such that half of all the assembled sequence sits in contigs at least that long" | Read three times. The definition is correct but the walking-the-list explanation and the definition are the same idea told twice in one sentence. | Split into two sentences, the definition then the how-to-compute. |
| Reading the results, "Flye's own `assembly_info.txt` marks this contig `circ. Y`" | A second pointer to a file I still do not know how to open, and `circ. Y` is raw file content with no instruction on where to look inside it. | Either drop the file detail or give one line on opening the run folder. |
| Reading the results, "Running the identical command on the identical reads twice produced two different answers" | This stopped me completely. Nothing in Geneious behaves this way, and the chapter does not say whether I should rerun to check, or which answer to keep. | Say what to do when two runs disagree, which is to keep the one matching the expected size. |
| Reading the results, "marked circular with a multiplicity of 4" | "Multiplicity" is unexplained and appears only here. I could not tell whether 4 is bad. | Gloss multiplicity as how many times the assembler thinks the sequence repeats. |
| Reading the results, "plus the unitig graphs, which hold the unambiguous stretches before hifiasm joined any of them into contigs" | Unitig arrives in the last paragraph of a section, is glossed only in passing, and I could not see why I would ever want one. | Say plainly that unitig files are not needed for anything in this chapter. |
| What good looks like, "the fix is to collapse it by hand" | The one corrective action in the chapter has no procedure. I do not know what collapsing by hand means or which LGE tool would do it. | Point to the chapter or tool that trims a doubled contig, or say the doubling is cosmetic here. |
| What good looks like, "roughly one per chromosome for a well-covered HiFi assembly of a small eukaryote" | I cannot use this. My target is a mitochondrion and I do not know a small eukaryote's chromosome count. | Give the number for the worked example instead of a general rule. |
| What good looks like, "is reported as completed with no contigs rather than as a failure" | I could not tell where that wording appears, in the Operations Panel row or in the viewport. | Name where the completed-with-no-contigs wording is shown. |
| What good looks like, "map the reads back to the assembly and look at whether coverage is even along it" | This is presented as the real check but is a forward reference to a chapter I have not read, so the chapter ends on a task I cannot do. | Say this is optional and comes later, so the size check is enough for now. |
| On the command line, "concatenate them into one file first, which for gzipped FASTQ is just `cat a.fastq.gz b.fastq.gz > combined.fastq.gz`" | This is the only stated fix for multiple input files and it is terminal-only. As a GUI user with three ONT files I would be stuck. | Say whether the sheet or a menu item can merge FASTQ files. |
| Whole chapter, entry_points lists "Tools > Assembly > Flye..." but no Reassemble path | Reassemble is introduced in step 3 as a right-click item, and right-click menus are the one place I never look. Nothing else in the chapter mentions it. | Mention the right-click menu once in the procedure lead-in. |

Three closing lines.

The one thing I learned. A long read helps because it can span a repeat and reach unique sequence on both sides, which is why the same sample gives a handful of long contigs instead of hundreds of short ones.

The one thing I still could not do. Open the run folder to look at `assembly_info.txt`, which the chapter sends me to three times without ever saying how to get there from the app.

The sentence I liked most. "A contig is a hypothesis about what the genome looks like, assembled from reads that carry their own errors, and nothing in the assembly checked it against an external truth."
