# Fidelity review: 04-alignments/01-mapping-reads-to-a-reference

Reviewer: manual-fidelity-reviewer. Date: 2026-09-07.

Ground truth used, in campaign order. Swift source under `Sources/`, the
`lungfish-cli` help tree under `reviews/fidelity-2026-09/cli-help/`, the tool
lock manifest, `docs/user-manual/parameters.yaml`, and the author's own run
outputs in the session scratchpad, which I re-derived rather than trusted.
I reran `samtools flagstat` from `~/.lungfish/conda/envs/samtools/bin/samtools`
against all four result BAMs and read all four `mapping-result.json` files and
the minimap2 `mapping-provenance.json`. Nothing was written outside the
scratchpad.

## Claims

| Claim (quoted) | Verdict | Evidence | Corrected wording |
|---|---|---|---|
| "LGE ships four of them, minimap2, BWA-MEM2, Bowtie2, and BBMap, and minimap2 is the default." | true | `MappingTool.swift:9-13` defines exactly those four cases, display names at `:15-22`. `cli-help/map.txt` `--mapper` reads "minimap2, bwa-mem2, bowtie2, bbmap (default: minimap2)". | (none) |
| "The BAM comes out sorted by reference position ... It also comes out with a companion index file beside it, ending in `.bai`" | true | `ManagedMappingPipeline.swift:339-345` appends index then flagstat steps; `:271-272` names `<sample>.sorted.bam` and `<sample>.sorted.bam.bai`. `map.txt` overview: "Produces a coordinate-sorted, indexed BAM file." Confirmed on disk in every scratch run. | (none) |
| "Then you choose a mapper from **Tools > Mapping**" | true | `MainMenu.swift:703-706` builds one top-level Tools submenu per category; `ToolsMenuModel.swift:71` titles `.mapping` "Mapping"; `MainMenu.swift:804-817` adds one item per tool id. | (none) |
| "The reads come from your sidebar selection, and the mapper comes from the menu item you chose. Neither is a control inside the wizard." | true | `MappingWizardSheet.swift:110-138` takes `inputFiles` and `initialTool` at init; `MainMenu.swift:809-815` sets the tool id on `item.representedObject`. `MappingWizardSheet.swift:512-541` has neither a reads picker nor a mapper picker. | (none) |
| "The slice holds 45,574 read pairs, 91,148 reads in total, drawn from a 500 kb window of chromosome 20." | true | `fixtures/hg002-chr20/README.md` Downsampling section states "45,574 read pairs (91,148 reads, 182,296 lines per file)". `samtools flagstat` on my minimap2 run reports "91148 paired in sequencing / 45574 read1 / 45574 read2". | (none) |
| "**File > New Project** (Cmd-N)" | true | `MainMenu.swift:169-173` adds "New Project" with `keyEquivalent: "n"` and no extra modifier mask. | (none) |
| "Open **Tools > Plugin Manager...** (Cmd-Shift-B)" | true | `MainMenu.swift:774-781` adds "Plugin Manager…" with `keyEquivalent: "b"` and `[.command, .shift]`. | (none) |
| "Three of the four mappers arrive in the `read-mapping` plugin pack" | true | `third-party-tools-lock.json:31-33` lists minimap2, bwa-mem2, and bowtie2 all under `"packID": "read-mapping"`. No BBMap row carries that packID. | (none) |
| "BBMap is the exception. It comes from the BBTools environment in the Required Setup pack" | true | `third-party-tools-lock.json:10` lists `bbtools` in the top-level `tools` array (packageSpec `bioconda::bbmap=40.02`), and `PluginPack.swift:425-436` builds that lock's tools into a pack with `category: "Required Setup"`, `kind: .requiredSetup`. | (none) |
| "The wizard has five sections stacked top to bottom, **Reference**, **Preset** (titled **Mode** for the three mappers that are not minimap2), **Read Group**, **Input Compatibility**, and **Advanced Settings**." | true | `MappingWizardSheet.swift:512-541` renders exactly `referenceSection`, `modeSection`, (conditional multi-bundle picker), read-group, `compatibilitySection`, `advancedSection` in that order. Title switch at `:622` reads `initialTool == .minimap2 ? "Preset" : "Mode"`. | (none) |
| "Read Group and Advanced Settings sit below them as collapsed disclosures you can open" | true | `MappingWizardSheet.swift:89` `showReadGroup = false`, `:94` `showAdvanced = false`; both are `DisclosureGroup`s at `:701` and `:726`. | (none) |
| "Select more than one read bundle before you open the wizard and a sixth section appears between Preset and Read Group, asking whether to run each bundle separately or pool them into one." | true | `MappingWizardSheet.swift:516-524` inserts `MultiBundleRunModePicker` when `bundleCount > 1`, between `modeSection` and the read-group branch. Policy at `:159-162` allows `.perBundle` and `.combined`, defaulting to `.perBundle`. | (none) |
| "the editable Read Group fields are replaced by a note reading 'Each bundle gets its own read group, derived automatically from its sample name.'" | true | `MappingWizardSheet.swift:526-537` branches to `perBundleReadGroupNotice`, whose `Text` at `:551` is that string verbatim. | (none) |
| Step 2: "**minimap2...** (the default), **BWA-MEM2...**, **Bowtie2...**, or **BBMap...**" | true | `FASTQOperationDialogState.swift:1279` returns `[.minimap2, .bwaMem2, .bowtie2, .bbmap, .viralRecon]` for `.mapping`; titles at `:1984-1988`; `MainMenu.swift:808` appends `\u{2026}` to each. | (none) |
| Step 2: "The wizard opens, titled 'Map Reads (minimap2)' for the default choice." | true | `MappingWizardSheet.swift:576` renders `Text("Map Reads (\(initialTool.displayName))")` and `MappingTool.displayName` returns "minimap2". | (none) |
| Step 3: "the path of the one you picked appears beneath it" | true | `MappingWizardSheet.swift:616-624` renders `selectedReferencePathDisplay` in monospaced secondary text under the picker. | (none) |
| Step 3: "click **Browse...** below the picker and select the file instead" | true | `MappingWizardSheet.swift:628-632` places `Button("Browse...")` after the picker block. | (none) |
| Step 4: "The wizard already chose it for you by reading the first reads in the file, and it stops guessing the moment you change the setting yourself." | true | `MappingWizardSheet.swift:885-890` `autoSelectPreferredModeIfNeeded` opens with `guard !modeWasChangedByUser else { return }`, called from `inspectInputs` at `:877-881`. | (none) |
| The quoted Input Compatibility block, all four lines | true | `MappingWizardSheet.swift:673-687` prints "Detected format: FASTQ", "Detected reads: \(displayName)", "Observed max read length: \(n) bp". `MappingCompatibilityPresentation.swift:89-91` builds "Ready: \(tool.displayName) is compatible with \(readClass.displayName)." `MappingReadClass.displayName` is "Illumina short reads" (`MappingTool.swift:44`), and the fixture reads are 2x250bp so 250 bp is the observed max. | (none) |
| "the verdict line turns into a refusal instead of that Ready sentence, and the Run button stays disabled until you fix the mismatch" | true | `MappingCompatibilityPresentation.swift:94` returns the blocked message with `isReady: false`; `MappingWizardSheet.swift:439-441` gates `canRun` on `isReady`. | (none) |
| "a row appears in the Operations panel ... labelled `Map Reads (minimap2): HG002.chr20.10.0-10.5Mb`" | true | `AppDelegate+ToolsMenu.swift:1300-1304` starts the operation with `title: "Map Reads (\(request.tool.displayName)): \(request.sampleName)"`. | (none) |
| "Open the panel with **Operations > Show Operations Panel** (Cmd-Shift-P)" | true | `MainMenu.swift:867-874` adds "Show Operations Panel" with `keyEquivalent: "p"` and `[.command, .shift]`. | (none) |
| "For minimap2 that is five steps, the mapper itself, then `samtools view` to drop unwanted records, then `samtools sort`, `samtools index`, and `samtools flagstat`." | true | The minimap2 run's `mapping-provenance.json` records exactly five steps in that order. `ManagedMappingPipeline.swift:281-296` appends view then sort for a `.sam` input, `:334-343` appends index then flagstat. | (none) |
| "The mapper writes a SAM file that the filter step consumes and the pipeline then deletes" | true | Provenance step 1 writes `HG002.raw.sam`, step 2 reads it. `ManagedMappingPipeline.swift:346-351` removes the `.raw.sam` on success and `:352-354` removes the filtered intermediate. | (none) |
| "the new track sits under it, named 'minimap2 Mapping', the mapper's name followed by the word Mapping" | true | `AppDelegate+ToolsMenu.swift:1390` and `:1504` both build `"\(request.tool.displayName) Mapping"`. | (none) |
| "The mapping run's own output folder sits under the project's `Analyses/`, in a subfolder named for the mapper and the time it ran, such as `Analyses/minimap2-20260906-134512/`." | false | The folder is right, the example's timestamp shape is not. `AnalysesFolder.swift:121` documents `Analyses/{tool}-{yyyy-MM-dd'T'HH-mm-ss}/` and `:139-140` builds `"\(tool)-\(timestamp)"` from `formatTimestamp` (`:410-417`, whose inverse `parseTimestamp` is documented as parsing `yyyy-MM-dd'T'HH-mm-ss`). `AppDelegate+ToolsMenu.swift:749` calls it with `tool: "minimap2"`. CONSISTENCY.md agrees on the `Analyses/<tool>-<timestamp>/` shape. | "...such as `Analyses/minimap2-2026-09-06T13-45-12/`." |
| "**Reference.** ... It defaults to the first reference the app finds in the project ... accepts any reference sequence in the project or a FASTA you pick with Browse.... On the command line this is `--reference`." | true | `parameters.yaml` `map.minimap2` Reference entry gives that default and allowed set verbatim; `map.txt` lists `--reference <reference>  Reference FASTA file to align against`. | (none) |
| "**Preset.** ... It defaults to Short-read, and it offers Short-read, Assembly-to-assembly, Spliced CDS/cDNA, Oxford Nanopore, PacBio HiFi, and PacBio CLR." | true | `MappingMode.isValid(for: .minimap2)` (`MappingTool.swift:221-222`) admits exactly those six; display names at `:196-203`. Registry allowed list matches. Default is `availableModes(for:).first`, which is `.defaultShortRead` ("Short-read"). | (none) |
| "**Mode.** ... For BWA-MEM2 and Bowtie2 it defaults to Short-read and offers nothing else ... For BBMap it defaults to Standard and offers Standard and PacBio" | true | `MappingTool.swift:223-224` returns `self == .defaultShortRead` for both; `:225-226` admits `.bbmapStandard` and `.bbmapPacBio`, display names "Standard" and "PacBio" (`:202-203`). Registry entries for all three agree. | (none) |
| "**Run Mode.** ... It defaults to Run separately per bundle ... the alternative is Combine all inputs and run once. This setting has no command-line flag" | true | `MappingWizardSheet.swift:159-162` `MultiBundleRunPolicy(allowedModes: [.perBundle, .combined], defaultMode: .perBundle)`. All four registry entries carry `cli_flag: null` for Run Mode, and `map.txt` has no such flag. | (none) |
| The five read-group labels "ID (--rg-id)", "Sample (--rg-sm)", "Library (--rg-lb)", "Platform (--rg-pl)", "Platform unit (--rg-pu)" | true | `MappingWizardSheet.swift:704-708` passes exactly those five strings as `label:` to `readGroupField`. All five appear as registry labels and as `map.txt` OPTIONS. | (none) |
| "It defaults to the sample name taken from the input file" for ID, Sample, Library, and Platform unit | true | `MappingWizardSheet.swift:129-134` seeds all five from `Self.defaultReadGroup(sampleName:modeID:)` where `sampleName` is the input file's display name. `map.txt` marks `--rg-id`, `--rg-sm`, `--rg-lb`, `--rg-pu` each "(default: sample name)". | (none) |
| "**Platform (--rg-pl).** ... a short-read preset writes `ILLUMINA`, Oxford Nanopore writes `ONT`, the PacBio presets write `PACBIO`, splice mode writes `CDNA`, and assembly mode writes `ASSEMBLY`." | true | `MappingRunRequest.swift:47-61` `defaultPlatform(forModeID:)` maps exactly these five outcomes. `map.txt` `--rg-pl` reads "(default: mapper preset platform)". | (none) |
| "changing the preset rewrites this field automatically as long as it still holds the old preset's default" | true | `MappingWizardSheet.swift:485-490` on `selectedModeID` change compares `readGroupPlatformText` to the old mode's default and only then overwrites it. | (none) |
| "**Threads:.** ... It defaults to the number of processor cores in your Mac and the stepper will not let you set more" | true | `MappingWizardSheet.swift:131` initialises `threads` to `ProcessInfo.processInfo.processorCount`; `:731` bounds the stepper `1...ProcessInfo.processInfo.processorCount`. Label at `:727` is `"Threads:"` verbatim. | (none) |
| "which is why the fixture run below reports 14 threads on a 14-core machine" | true | The minimap2 provenance records `-t 14` in the mapper invocation and `threads: 14`. | (none) |
| "**Secondary alignments:.** ... The checkbox is off by default" | true | Label `"Secondary alignments:"` at `MappingWizardSheet.swift:741`; `:85` `includeSecondary = false`. `map.txt` `--secondary  Keep secondary alignments in the normalized BAM`. | (none) |
| "each mapper implements it differently, with Bowtie2 adding `-k 10` and BBMap adding `secondary=t`" | true | `MappingCommandBuilder.swift:142-144` appends `["-k", "10"]` for Bowtie2 when `includeSecondary`; `:176` builds `"secondary=\(request.includeSecondary ? "t" : "f")"` for BBMap. | (none) |
| "**Supplementary:.** ... The checkbox is on by default and ticking it keeps those supplementary alignments ... the command-line flag is the inverse of the checkbox. On the command line this is `--no-supplementary`, which drops them." | true | Label `"Supplementary:"` at `MappingWizardSheet.swift:751`; `:86` `includeSupplementary = true`. `map.txt` `--no-supplementary  Exclude supplementary alignments from the normalized BAM`. Registry records default `"true"` with `cli_flag: --no-supplementary`. | (none) |
| "**Min mapping quality:.** ... It defaults to 0 ... and the stepper accepts 0 to 60" | true | Label `"Min mapping quality:"` at `MappingWizardSheet.swift:759`; `:87` `minMappingQuality = 0`; `:762` bounds `0...60`. `map.txt` `--min-mapq` "(default: 0)". | (none) |
| "**Extra arguments.** ... It is empty by default ... LGE parses it live and a parse error both blocks Run and prints in the footer." | true | Field title `"Extra arguments"` at `MappingWizardSheet.swift:197` (no trailing colon, matching the chapter's colon-free bolding); `:95` `advancedOptionsText = ""`. `advancedOptionsParseError` at `:952-959` re-parses on every read, `canRun` at `:440` requires it nil, and the footer at `:783-785` shows it in orange. | (none) |
| "It reads `--eqx -N 5` for minimap2, `-M -Y` for BWA-MEM2, `--very-sensitive -N 1` for Bowtie2, and `minid=0.97 local=t` for BBMap." | true | `MappingWizardSheet.swift:199-210` `advancedOptionsPlaceholder(for:)` returns those four strings, and `:765` passes it as the TextField's placeholder. | (none) |
| "Use **File > Import Center...** (Cmd-Shift-I), open the Alignments tab, and drop the file on the BAM/CRAM Alignments card." | true | `MainMenu.swift:207-212` adds "Import Center…" with `keyEquivalent: "i"` and `[.command, .shift]`. `parameters.yaml` `import.bam` title is "BAM/CRAM Alignments" with entry point "File > Import Center... > Alignments > BAM/CRAM Alignments". | (none) |
| "That card has no settings beyond the file panel, so there is nothing to configure." | true | `parameters.yaml` `import.bam` has `settings: []` and notes "The card has no controls beyond a file panel". | (none) |
| "The importer indexes the alignment, collects per-chromosome and per-read-group statistics, and works out which reference assembly the file names from its own header without asking you to confirm it." | true | `BAMImportService.swift:427-436` runs `samtools index`; the registry's `import.bam` notes record the per-chromosome and per-read-group statistics database and header-based assembly inference. | (none) |
| "A CRAM is converted on the way in" | false | CRAM is not converted. `BAMImportService.swift:369-371` documents "CRAM -> sorted CRAM + CRAI", and `:391-393` sets `outputFormat = .cram` with `outputExt = "cram"`, sorting with `-O CRAM` (`:407-409`) and indexing with `--reference` (`:429-431`). Only SAM is normalised to BAM (`:384-386`). The registry note agrees that both formats are recorded rather than one replacing the other. | "A CRAM stays a CRAM, sorted and indexed in place, and a SAM is normalised to a sorted, indexed BAM." |
| "a SAM is normalised to a sorted, indexed BAM" | true | `BAMImportService.swift:383-386` sets `outputFormat = .bam` for a `.sam` source, with the comment "Normalize SAM input to BAM for indexed random access." | (none) |
| "On the command line this is `lungfish-cli import bam`, which takes `--name` to set the track's display name and `--output-dir` to name the project it lands in." | true | `cli-help/import.txt:50-53` lists `-o, --output-dir  Output project directory (default: current directory)` and `--name  Display name for the alignment track (default: filename)`. | (none) |
| "Five numbers sit at the top. Total Mapped ... Total Unmapped ... Mapped % ... Chromosomes ... Est. Coverage" | true | `ReadStyleSection.swift:1166-1192` renders exactly `statRow("Total Mapped")`, `statRow("Total Unmapped")`, `statRow("Mapped %")`, `statRow("Chromosomes")`, and `statRow("Est. Coverage")`. | (none) |
| "Mapped % is the first as a percentage of the two together, drawn as a bar beneath the figure." | true | `ReadStyleSection.swift:1169-1172` computes `mapped / (mapped + unmapped) * 100`; `:1174-1187` draws the green `Rectangle` progress bar directly beneath. | (none) |
| "Est. Coverage ... appears only when the alignment has a single contig, so a whole-genome reference shows no such row." | true | `ReadStyleSection.swift:1190-1192` guards the row on `viewModel.chromosomeStats.count == 1`. | (none) |
| "For the fixture mapped with minimap2 those five read 90,990, 213, 99.8%, 1, and 44.7x." | true | `minimap2-out/mapping-result.json` gives `mappedReads` 90990, `unmappedReads` 213, one contig, `meanDepth` 44.7234. The Inspector formats Mapped % as `%.1f%%` (`ReadStyleSection.swift:1172`), and 90990/91203 = 99.7665 renders "99.8%"; Est. Coverage formats `%.1fx` (`:1191`), so 44.7234 renders "44.7x". | (none) |
| "On the HG002 slice it is 44.7, which is what a human genome project aims for. A region under 10 is too thin to call a variant with confidence." | true (number) | The 44.7 figure is verified above. The 30x-to-50x target and the sub-10x caution are standard field guidance rather than app behavior, so only the number was checkable and it is right. | (none) |
| "Below the five sits a collapsed **Flag Stats** list." | false | The disclosure is titled "Flag Statistics", not "Flag Stats". `ReadStyleSection.swift:1101` reads `DisclosureGroup("Flag Statistics", isExpanded: $isFlagStatsExpanded)`. Collapsed-by-default and its position below the five rows are both right. Same error in the frontmatter caption for `alignment-inspector-stats` and in the table header "Flag Stats category", and in the What good looks like line "The Flag Stats total should equal...". | Rename every occurrence to "Flag Statistics", including the shot caption and the table header. |
| The Flag Stats table, all five rows, 91,203 / 91,148 / 55 / 90,935 / 90,414 | true | I reran `samtools flagstat` on the adopted minimap2 BAM: "91203 in total / 91148 primary / 0 secondary / 55 supplementary / 90990 mapped / 90935 primary mapped / 90414 properly paired". Every quoted figure matches to the digit, and matches the fixture README's Internal consistency section. | (none) |
| "The total of 91,203 is larger than the 91,148 reads you imported ... Fifty-five reads were split across two places on the reference and so contributed a second record each. Subtracting those gives 91,148" | true | Arithmetic holds against the flagstat above (91,203 - 55 = 91,148), and `samtools flagstat` reports the same 91,148 as "primary" directly. | (none) |
| "Of those, 90,935 were placed somewhere, which is 99.77%." | true | 90935/91148 = 99.7663%, and samtools prints "90935 primary mapped (99.77%)". | (none) |
| "The properly paired count of 90,414 ... which is 99.19% of the paired reads" | true | 90414/91148 = 99.1947%, and samtools prints "90414 properly paired (99.19%)". | (none) |
| "Every mapping run also writes a `mapping-result.json` beside the BAM" | true | Present in all four scratch output directories alongside `HG002.sorted.bam`. | (none) |
| "including coverage breadth ... For this run it is 99.994%" | true | `minimap2-out/mapping-result.json` `coverageBreadth` is 0.999938, which is 99.9938% and rounds to 99.994%. | (none) |
| Mapper table, minimap2 row: 91,203 / 90,990 / 99.77% / 44.7x / 60 | true | `minimap2-out/mapping-result.json`: totalReads 91203, mappedReads 90990, mappedReadPercent 99.7665, meanDepth 44.7234, medianMAPQ 60. | (none) |
| Mapper table, BWA-MEM2 row: 91,317 / 91,239 / 99.91% / 44.8x / 60 | true | `bwamem2-out/mapping-result.json`: 91317, 91239, 99.9146, meanDepth 44.7671, medianMAPQ 60. My own flagstat confirms 91317 total. | (none) |
| Mapper table, Bowtie2 row: 91,148 / 90,241 / 99.00% / 44.8x / 42 | true | `bowtie2-out/mapping-result.json`: 91148, 90241, 99.0049, meanDepth 44.8188, medianMAPQ 42. Flagstat confirms 91148 total, 0 supplementary. | (none) |
| Mapper table, BBMap row: 91,148 / 90,658 / 99.46% / 45.0x / 45 | true | `bbmap-out/mapping-result.json`: 91148, 90658, 99.4624, meanDepth 45.0472, medianMAPQ 45. Flagstat confirms 91148 total, 0 supplementary. | (none) |
| "minimap2 emitted 55 supplementary records and BWA-MEM2 emitted 169, while Bowtie2 and BBMap emitted none at all" | true | My four flagstat runs report supplementary counts of 55, 169, 0, and 0 respectively. | (none) |
| "mean depth is the same to within a third of a read" | true | The four meanDepth values span 44.7234 to 45.0472, a range of 0.3238. | (none) |
| "For human reads against the matching human reference, Mapped % should sit above 99%, and all four mappers clear that on the fixture." | true | The four mapped percentages are 99.77, 99.91, 99.00, and 99.46. Bowtie2's 99.0049 clears 99% and, as the chapter's own table already rounds it to 99.00%, the claim holds on the underlying value. | (none) |
| "The Flag Stats total should equal the reads you imported plus however many supplementary records the mapper emitted, and never less." | true (with the label fix above) | Holds for all four runs: 91,148 + 55, + 169, + 0, + 0 give the four observed totals exactly. | (none) |
| "The fixture runs above used minimap2 2.31 with samtools 1.24." | true | `minimap2-out/mapping-provenance.json` records `mapperVersion` 2.31 and `samtoolsVersion` 1.24. `third-party-tools-lock.json:31` pins `minimap2=2.31` and `:13` pins `samtools=1.24`. | (none) |
| "LGE records the resolved tool version in the provenance sidecar of every mapping run" | true | `mapping-provenance.json` exists in all four scratch output directories and carries `mapperVersion` plus `samtoolsVersion`. | (none) |
| The `lungfish-cli map` example's flags: `--reference`, `--paired`, `--mapper`, `--preset`, `--sample-name`, `-o` | true | All six appear in `cli-help/map.txt` OPTIONS, including `-o, --output-dir` and `--sample-name  Sample name for BAM read groups and output naming`. | (none) |
| The `bam adopt-mapping` example's flags: `--bundle`, `--mapping-result`, `--name` | true | `cli-help/bam.txt:253` USAGE requires exactly those three, described at `:256-260`. | (none) |
| "For the fixture it reported 91,203 total reads, 90,990 mapped at 99.77%, 213 unmapped, and a 4.8 second runtime." | true (as a report of what the CLI prints) | `MapCommand.swift:291-303` prints "Total reads" = `result.totalReads`, "Mapped reads" with `%.2f%%`, "Unmapped reads", and "Runtime" with `%.1fs`. The JSON gives 91203, 90990, 213, and `wallClockSeconds` 4.812, which formats as "4.8s" and 99.77%. See the defect note below on the "Total reads" label itself. | (none) |
| "The second command prints one line, `Attached alignment track 'minimap2 Mapping' (aln_86981CC7) to bundle.`" | true | `BAMAdoptMappingSubcommand.swift:84` prints `"Attached alignment track '\(trackName)' (\(outputTrackID)) to bundle."`. The scratch bundle holds `aln_86981CC7.bam`, `aln_86981CC7.bam.bai`, and `aln_86981CC7.stats.db`, confirming that exact id. | (none) |
| Preset token table, all five rows (`sr`, `map-ont`, `map-hifi`, `map-pb`, `asm5`) with their wizard labels | true | `MappingMode.displayName` (`MappingTool.swift:196-203`) and `commandPresetValue` (`:209-216`) pair exactly these. `map.txt` `--preset` lists "minimap2: sr, asm5, splice, map-ont, map-hifi, map-pb". | (none) |
| "`bbmap-standard` is the Standard mode and the one BBMap falls back to when you omit the flag, and `bbmap-pacbio` is its PacBio mode." | true | Raw values at `MappingTool.swift:187-188`; `map.txt` lists "bbmap: bbmap-standard, bbmap-pacbio". `availableModes(for: .bbmap).first` is `.bbmapStandard`, and `preferredMode` returns it for Illumina input. The author's BBMap run recorded `modeID: bbmap-standard` with no preset passed. | (none) |
| "minimap2 also offers `splice` for spliced transcript alignment, shown in the wizard as Spliced CDS/cDNA." | true | `MappingTool.swift:181` raw value `splice`, display "Spliced CDS/cDNA" (`:199`), admitted for minimap2 at `:221-222`. | (none) |
| "`--reference` wants a FASTA file rather than a bundle, and while a `.lungfishref` path works whenever LGE can pull the bundle's primary FASTA out of it, the reliable move when a bundle path is rejected is to pass the FASTA inside it instead." | true | `map.txt` describes `--reference` as "Reference FASTA file to align against" with no bundle mention. `MapCommand.swift:318-322` resolves inputs through `SequenceInputResolver.resolvePrimarySequenceURL` and throws `unreadableSequenceInput` when it cannot, which is exactly the hedged behavior described. | (none) |
| "Multiple input files are treated as one sample's reads, so run the command once per sample" | true | `map.txt` overview: "Multiple inputs are treated as one sample's reads (--paired binds exactly two files as R1/R2 of that sample); invoke once per sample for per-sample results." | (none) |
| "`--format json` and `--format tsv` print the run summary for a script to read" | true | `map.txt` `--format <format>  Output format: text, json, tsv (default: text)`. | (none) |
| "`--mapper` chooses among `minimap2`, `bwa-mem2`, `bowtie2`, and `bbmap`" | true | `map.txt` `--mapper` banner names exactly those four; `MappingTool` raw values match (`bwaMem2 = "bwa-mem2"`). | (none) |
| "The adoption step mints its own track identifier, of the form `aln_` followed by eight characters taken from a fresh UUID" | true | `BAMAdoptMappingSubcommand.swift:46` reads `let outputTrackID = trackIDOverride ?? "aln_\(UUID().uuidString.prefix(8))"`. The author's wording correctly avoids calling it hex. This settles reality-map claim 46, which was unverifiable there. | (none) |
| "Pass `--track-id` to set that identifier yourself" | true | `cli-help/bam.txt:261` `--track-id <track-id>  Override the auto-generated alignment track identifier`, and `:46` uses `trackIDOverride` in preference to the generated id. | (none) |
| "Both routes write the same provenance sidecar, so a run made in the window and a run made from the command line with identical inputs record identical methods." | unverifiable | The GUI and CLI both reach `ManagedMappingPipeline`, and both write `mapping-provenance.json`, so the mechanism is shared. But I could only produce CLI sidecars in this review, and the sidecar carries `runtimeIdentity` and absolute paths that differ between hosts, so "identical" is untested at the file level. Settled by running the same inputs through the wizard in the Preview app and diffing the two sidecars field by field, excluding paths and timestamps. | (none) |
| Shot caption: "showing its five items, minimap2..., BWA-MEM2..., Bowtie2..., BBMap..., and Viral Recon...." | true | `FASTQOperationDialogState.swift:1279` returns those five tool ids for `.mapping`, `:1984-1988` gives those five titles, `MainMenu.swift:806-808` appends an ellipsis to each. The Viral Recon item survives the `workflowOperations` capability filter at `MainMenu.swift:806` since it is reached this way in practice. | (none) |
| Shot caption: "showing Total Mapped, Total Unmapped, Mapped %, Chromosomes, and Est. Coverage above the collapsed Flag Stats list" | false (label only) | The five row names are exactly right (`ReadStyleSection.swift:1166-1192`). "Flag Stats" is the wrong title, per the row above. | "...above the collapsed Flag Statistics list." |

## Front matter

`parameters_refs: [map.minimap2, map.bwa-mem2, map.bowtie2, map.bbmap, import.bam]`
matches roster row 22 at `DRIFT.md:3748` exactly, in the same order.

Every registry setting has a Settings paragraph. `map.minimap2` declares twelve
settings and the chapter carries a paragraph for each, with the four
colon-terminated labels quoted verbatim including the colon ("Threads:",
"Secondary alignments:", "Supplementary:", "Min mapping quality:") and "Extra
arguments" correctly quoted without one, matching
`MappingWizardSheet.swift:197`. The three other mapping registries differ only
in the Preset-versus-Mode label and the per-mapper allowed sets, both of which
the chapter's separate **Mode.** paragraph covers, and in the BWA-MEM2 and
Bowtie2 Platform default of `ILLUMINA`, which follows from the **Platform**
paragraph's rule since Short-read is their only mode. `import.bam` declares
`settings: []`, which the chapter states outright. Every default and allowed
value in the chapter's Settings section agrees with the registry.

All four `<!-- SHOT -->` markers in the body (`tools-mapping-submenu`,
`mapping-wizard-overview`, `mapping-wizard-advanced`,
`alignment-inspector-stats`) have a captioned `shots` entry, and there are no
extra entries. One caption needs the Flag Statistics fix above.

All seventeen `glossary_refs` anchors resolve to exactly one `{#anchor}` in
`GLOSSARY.md`, the six new ones included.

## The author's two defect claims

**"Total reads" mislabels a record count.** Confirmed. `MapCommand.swift:298`
prints `("Total reads", String(result.totalReads))`, and `totalReads` is the
flagstat total, which counts supplementary records. On the fixture that prints
91,203 for a 91,148-read input. The mismatch is real and reaches the user with
no explanation. The fixture README already records the same trap. This is a
labeling defect, not a miscount, and "Total records" plus a separate primary
line would settle it.

**Orphaned Tools menu selectors.** Confirmed for both. `showFASTQMappingOperations`
is declared at `MainMenu.swift:1164` and implemented at
`AppDelegate+ToolsMenu.swift:113`; `showWorkflowOperations` is declared at
`MainMenu.swift:1180` and implemented at `AppDelegate+ToolsMenu.swift:1731`.
Neither name appears in any `#selector(...)` in `MainMenu.swift`. Every
per-tool Tools item is built with `#selector(ToolsMenuActions.launchFASTQOperationToolFromMenu(_:))`,
which is the only `ToolsMenuActions` selector used for those items in the whole
file. So both selectors are dead entry points, and the first is the reason the
old chapter documented a `Tools > FASTQ/FASTA Operations > Mapping…` path.
This also settles reality-map claim 52, which was unverifiable there. The
rewritten chapter makes no claim about either, so nothing in it depends on this.

## Verdicts

81 true, 4 false, 1 unverifiable.
