# Reader 1 report: Subsetting and Extraction

Persona: sophomore, one genetics course, no lab time, never opened a terminal.

| Location | What stopped me | What would have helped |
|---|---|---|
| What it is, "Subsetting means making a smaller read" | The word "bundle" is defined as "the folder LGE treats as one object", but I do not know what is inside that folder or why reads live in a folder instead of a file. | One clause saying a bundle is the reads plus the notes about them. |
| What it is, "a read bundle carries the extension" | I have never seen `.lungfishfastq` and I do not know if that is a file or a folder, since the sentence just called it a folder. | Say whether a bundle looks like one icon in the Finder. |
| What it is, "That is subsampling, drawing reads" | I did not know reads could be drawn at random and still be trustworthy. My genetics course never covered sampling from sequencing data. | One line saying why a random draw still represents the whole sample. |
| What it is, "Select Reads by Sequence matches an adapter" | I do not know what an adapter is. The glossary link is there but the sentence assumes I already know why an adapter would be inside a read. | A half-sentence gloss saying an adapter is a lab-added sequence stuck to the fragment. |
| What it is, "or barcode at one end" | Barcode is used with no gloss and no glossary link, unlike adapter beside it. | Gloss barcode the same way adapter is glossed. |
| Why you would do this, "Any measure that grows with depth" | I do not know what "depth" means here. I guessed it means number of reads, but the chapter never says. | Define depth on first use. |
| Why you would do this, "Which reads came from the run" | I did not know one sample's reads could come from more than one sequencing run. This is treated as obvious. | One clause saying a sample is often sequenced more than once. |
| Why you would do this, "a pair of Illumina files holding 45,574" | It says 45,574 read pairs here, but the results table later says "Share of the 45,574 in" as if that were reads. I could not tell whether the file has 45,574 reads or 91,148. | Use the same unit in both places, or say which is which. |
| Why you would do this, "They came off five separate sequencing" | Five runs on two instruments, but the results section only ever names two instruments and never the five runs. I expected the five to come back. | Drop the five or use it later. |
| Before you start, "Download the files `HG002.chr20.10.0-10.5Mb_R1.fastq.gz`" | I do not know how to get a file off GitHub. The link goes to a folder page and I would not know which button downloads it. | One sentence on what to click on that page. |
| Before you start, "These operations use tools from" | I do not know what a pack is or what "Required Setup" means. It sounds like something I might have skipped during install. | Say a pack is a set of tools LGE installs, and that this one is already there. |
| Before you start, "No optional pack and no Docker" | Docker Desktop appears once with no explanation and never again. It made me worry I was missing an install step. | Cut it or say it is only needed for other chapters. |
| Before you start, "Every result lands under `Analyses/`" | I do not know what `<tool>` gets replaced with for these five operations, so I would not recognise the folder when I looked. | Show one real folder name as an example. |
| Procedure, "There is no wizard with multiple" | Wizard is used without explanation. I only guessed from context that it means a multi-step dialog. | Say "step-by-step dialog" instead. |
| Procedure step 3, "Type `10000` into the Count" | The step packs the empty field, the disabled button, and the message text into one sentence and I had to read it twice. | Split it into two sentences. |
| Procedure step 4, "Leave Output Strategy on Per" | "Per Input" means nothing to me at this point. The explanation is far below in Settings and I did not know to look ahead. | Point forward to the Settings entry here. |
| Procedure, "The other four operations differ only" | Four operations are summarised in one dense paragraph after a numbered list for the fifth. I could not tell whether I was supposed to be able to run them from this alone. | Say plainly that the numbered steps apply to all five and only the fields change. |
| Paired reads stay paired, "A paired-end sample is stored" | I knew paired-end from lecture but not that the two mates get merged into one file. I had to reread to see that the two files I downloaded become one inside the bundle. | Say explicitly that importing the two files makes one interleaved file. |
| Paired reads stay paired, "The two search operations find the" | This sentence has four clauses and a "so", and I lost track of what goes back to the original file. | Break it into two sentences. |
| Paired reads stay paired, "so asking for 10,000 reads gives" | I could not tell whether the Count field is asking me for reads or for pairs. The sentence says both things about the same number. | State once whether Count is a read count on paired data. |
| Settings, "This setting has no command-line flag" | I do not know what a command-line flag is. This is the first mention and I have never opened a terminal. | Move this to the command line section or gloss it. |
| Subsample by Proportion, "which is 9.81 percent rather than" | I do not know how far off is too far off. Is 9.81 fine but 9 percent a problem? The chapter says it is normal but not where the edge is. | Give a rough tolerance. |
| Subsample by Proportion, "returned 4,473, which is 9.81" | If the file has 45,574 pairs, ten percent should be about 4,557 pairs or 9,115 reads, and 4,473 fits neither cleanly. I could not check the arithmetic. | Say whether these counts are reads or pairs. |
| Extract Reads by ID, "The text matched against each read's" | I have never seen the inside of a FASTQ file, so "name line" and the `@` line mean nothing visually. | Show two lines of a real header. |
| Extract Reads by ID, "Read the next paragraph before using" | A warning whose whole content is that a warning follows made me stop and check I had not skipped something. | Put the warning content in this sentence. |
| Extract Reads by ID, "Searching for the complete identifier `HISEQ1:93:H2YHMBCXX`" | I cannot tell which part of that string is the instrument and which is the lane, so I would not know how to build my own query. | Label the parts once. |
| Extract Reads by ID Field, "Description is everything after the first" | I do not know what would be in a description or why anyone puts free text into a read name. | Give one example of a description. |
| Extract Reads by Motif, "returned 521 reads, which is 260" | 260 plus 261 is 521, but I do not know why the chapter expects those two to be nearly equal, beyond the one clause about reading from either end. | Say the near-equality is the check, not the doubling. |
| Extract Reads by Motif regex, "Writing `GCCTCCCAAAGTGCTGGGATTACAG[GA]` means" | The bracket example is the only pattern syntax given, and I would not know how to write a pattern for any other degenerate position. | Add one more example, or say brackets are all you need. |
| Extract Reads by Motif regex, "picking up 44 reads whose Alu" | 565 minus 521 is 44, but I could not see whether reverse complements are counted the same way in both numbers, so I could not trust the subtraction. | State that both numbers include both strands. |
| Select Reads by Sequence, "It allows a stated number of" | The Settings entries give an error rate as a fraction and never a number of mismatches, so this opening promise did not match the controls I was shown. | Use the same wording as the Error Rate entry. |
| Sequence or FASTA Path, "LGE decides which you meant by" | I could not tell how LGE decides, so I would not know how to keep it from guessing wrong. | Say what makes text look like a path. |
| Search End, "Chooses which end of the read" | I know 5 prime and 3 prime from lecture as ends of a strand, but not what they mean for a read as written in a file. "The start of the read as written" felt like a different idea. | Say the read is written 5 prime to 3 prime. |
| Min Overlap, "It defaults to 16 in the" | I do not know how long the fixture's reads are at this point. The 250-base figure only appears two paragraphs later. | Give the read length before using it as a reason. |
| Min Overlap, "40,507 reads at an overlap of" | The lesson is clear but I still do not know what overlap I should pick for my own data, only that 8 is bad and 16 worked here. | Say to start at the default and only lower it with a reason. |
| Error Rate, "so a 16-base match tolerates two" | 0.15 times 16 is 2.4, and I could not tell whether that rounds down to 2 or whether the sentence was approximating. | Say the allowance is rounded down. |
| Keep Matched Reads, "On the command line this is" | I read this three times. The window default, the command line default, and the word "off" all point in different directions inside one sentence. | Split the window behaviour and the command line behaviour. |
| Reading the results, "Subsample by Count, 10000" | The share column mixes two meanings. On the subsample rows it is just what I asked for, and on the extraction rows it is a fact about the data. I read the column as one thing at first. | Split the table or label the column differently. |
| Reading the results, "the remaining 34,952 came off the" | The chapter never names the second instrument even though it named HISEQ1. | Name both instruments. |
| Reading the results, "about one read in ninety carries" | I have no idea whether one in ninety is what Alu abundance predicts. "Roughly what a human shotgun library should show" gives me no number to compare against. | Give the expected percentage. |
| Reading the results, "a recognisable Alu right-arm end" | Right-arm end names a structure inside an Alu element that was never introduced. | Drop it or gloss it. |
| Reading the results, "which for 250-base reads off a" | I do not know what a typical fragment size is, so "healthy" is a judgement I cannot make for my own data. | Give the fragment size range. |
| Subset bundles hold no reads, "Only a preview of about a" | I was alarmed that my subset is not really there. The rest of the section reassured me, but I still do not know what happens if I copy that bundle to another computer. | Say whether a virtual bundle survives being moved. |
| Subset bundles hold no reads, "Reveal the bundle in the Finder" | I do not know how to reveal a bundle in the Finder from inside LGE. | Name the menu item or the right-click item. |
| Subset bundles hold no reads, "A bundle produced by these operations" | "Usually a virtual bundle" implies sometimes it is not, but the chapter never says when. | Say which operations produce a real file. |
| Subset bundles hold no reads, "If you need the reads as" | The only route to an ordinary file is a terminal command, and I have never opened a terminal. This is the one thing in the chapter I could not do at all. | Say whether the window can export a plain FASTQ. |
| What good looks like, "should land within a percent or" | Earlier the 0.1 draw was off by 0.19 percentage points and called normal. Here the tolerance is "a percent or so". I could not tell if those are the same standard. | Use one tolerance figure in both places. |
| What good looks like, "Its reads are 76.7 percent from" | 10,622 of 45,574 is 23.3 percent, so the split is stated in reads here, but the file was called 45,574 pairs earlier. The same confusion caught me twice. | Fix the unit once and it fixes both. |
| On the command line, "The `lungfish-cli` program ships inside the" | I do not know what a program shipping inside an application means or how I would reach it. The section says it is optional, which I took as permission to skip. | Say a reader with no terminal can skip safely. |
| On the command line, "--search-end right --min-overlap 16" | The window said 3' End and this command says `right`, and nothing tells me those are the same thing. | Say right means the 3 prime end. |
| On the command line, "bbduk refuses an edit distance above" | Edit distance is a new term and the arithmetic that turns overlap and error rate into it is not shown, so I could not check my own settings against the limit. | Show the multiplication once. |
| On the command line, "fails with a Java error rather" | Java appears once, unexplained, and I would not recognise such an error if I saw one. | Say the message will look like a crash. |
| Extracting reads from a list, "Note that the output is gzip-compressed" | I do not know what gzip-compressed means for whether I can open the file afterwards. | Say it is a compressed file most tools read directly. |
| Extracting reads from a list, "`--by-db` queries a metagenomics database by" | Metagenomics database and taxonomy both arrive with no gloss, in a chapter that has been about human reads throughout. | Cut them or gloss them. |
| Extracting reads from a list, "A parallel command, `lungfish-cli extract contigs`" | Contigs are never explained and this is the last line before the Next section. | Gloss contig or drop the sentence. |

The one thing I learned: a subset bundle normally stores a recipe rather than a second copy of the reads, which is why ten test slices cost almost no disk.

The one thing I still could not do: get a plain FASTQ file out of a subset bundle, because the only route given is a terminal command.

The sentence I liked most: "There are not forty thousand adapter-contaminated reads in this fixture."
