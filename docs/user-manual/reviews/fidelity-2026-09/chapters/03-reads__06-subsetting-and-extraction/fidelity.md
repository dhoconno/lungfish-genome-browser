# Fidelity review: 03-reads/06-subsetting-and-extraction

Reviewer: manual-fidelity-reviewer. Date: 2026-09-07. Target build: Preview
2026.9.13.

Method. Every GUI claim was checked against `FASTQOperationDialogState.swift`,
`FASTQOperationToolPanes.swift`, `ToolsMenuModel.swift`, and the derivative
pipeline. Every CLI claim was checked against `cli-help/fastq.txt`,
`cli-help/extract.txt`, and the subcommand sources. Every quoted count was
recomputed from the author's outputs under the session scratchpad
(`.../scratchpad/fastq-subset/`), and two runs were reissued independently
against fixture copies to test the two defect claims that depend on runtime
behaviour. Nothing was written into the repository or into `~/Desktop/lge-docs`.

`LUNGFISH_MANUAL_STRICT=1 bash docs/user-manual/build/scripts/lint-chapter.sh`
reports `no issues found`.

## Claims

| Claim | Verdict | Evidence | Corrected wording |
|---|---|---|---|
| "Tools > Search & Subsetting > Subsample by Proportion..." (front matter and body, all five paths) | true | `ToolsMenuModel.swift:68` returns the category title "Search & Subsetting". `ToolsMenuModel.build` (`:24-55`) emits one `WorkflowEntry` per tool inside each category, so these are five sibling menu items and not one dialog with a list. Operation titles at `FASTQOperationDialogState.swift:1975-1979` are exactly "Subsample by Proportion", "Subsample by Count", "Extract Reads by ID", "Extract Reads by Motif", "Select Reads by Sequence". | no change |
| "CLI: lungfish-cli fastq subsample, search-text, search-motif, sequence-filter" | true | `fastq.txt` banners at lines 94, 1371, 1391, 1453. | no change |
| "a read bundle carries the extension `.lungfishfastq`" | true | `FASTQBundle.directoryExtension`, used at `FASTQOperationOutputImporter.swift:1053`. | no change |
| "Every operation in this chapter reads one bundle and writes a new one beside it. The bundle you started from is never altered" | true | All five build a `.derivative` request against `selectedInputURLs` and write to a fresh output bundle (`FASTQOperationDialogState.swift:743-797`; `FASTQDerivativeService+Core.swift:217-222` creates a new output bundle URL). | no change |
| The five-row summary table (what each operation takes and returns) | true | Matches the five request cases at `FASTQOperationDialogState.swift:743-797` and the pane controls at `FASTQOperationToolPanes.swift:566-604`. | no change |
| "45,574 read pairs from a well-characterized human genome" | true | `docs/user-manual/fixtures/hg002-chr20/README.md:86` states 45,574 read pairs, 91,148 reads. Recounted: R1 holds 45,574 records. | no change |
| "They came off five separate sequencing runs on two instruments" | true | Recounted from R1 headers: `@HISEQ1:93:H2YHMBCXX` 10,622, `@D00360:94:H2YT5BCXX` 9,998, `@D00360:97:H2YVMBCXX` 9,680, `@D00360:96:H2YLYBCXX` 8,049, `@D00360:95:H2YWMBCXX` 7,225. Five run IDs, two instrument names. | no change |
| "a small number of them run through into the sequencing adapter" | true | 218 of 45,574 reads match the TruSeq adapter at 3' end, overlap 16 (recounted from `adapter-matched.fastq`). | no change |
| "**File > New Project** (Cmd-N), or click Create Project on the Welcome window" | true | Campaign-standard phrasing fixed by CONSISTENCY.md:127. | no change |
| "These operations use tools from the Required Setup pack, the one pack LGE installs by itself. No optional pack and no Docker Desktop is needed." | true | `PluginPack.swift:424-435` builds the Required Setup pack from every tool in the lock manifest; `third-party-tools-lock.json` lines 10, 16, 17 carry bbtools (which supplies `reformat.sh` and `bbduk.sh`), seqkit, and cutadapt. None of the four engines is containerized. | no change |
| "Every result lands under `Analyses/<tool>-<timestamp>/` in your project folder, the same place every other analysis result goes. The timestamp is what keeps a second run of the same operation from overwriting the first." | **false** | A FASTQ operation does not get a timestamped tool subfolder. `FASTQOperationDialogState.defaultOutputDirectory` (`:1607-1613`) returns `<project>/Analyses`, and `FASTQOperationOutputImporter.bundleNameStem` (`:1011-1021`) names the bundle `<input stem>-<operation>` directly in that directory. Collisions are resolved by an appended counter, not a timestamp (`uniqueBundleURL`, `:1052-1062`). This is exactly the case CONSISTENCY.md:47-58 settled on 2026-09-07. | "Every result lands directly under `Analyses/` in your project folder, as a bundle named for the input and the operation, such as `HG002.chr20.10.0-10.5Mb-subsampleCount.lungfishfastq`. Running the same operation twice adds a counter to the second name rather than overwriting the first." |
| "There is no wizard with multiple pages and no dialog to pick the operation from once you are inside. The menu item you choose is the operation you get." | true | `ToolsMenuModel.build` carries `toolID` on each entry, so the chosen item selects the tool directly. The window is one pane, not a paged wizard (`FASTQOperationToolPanes.swift:95-130` renders sections in a single scroll). | no change |
| "Type `10000` into the **Count** field. The field starts empty, and the Run button stays disabled until you enter something, showing 'Enter a positive read count.' until you do." | true | Label "Count" at `FASTQOperationToolPanes.swift:571`; default `nil` at `FASTQOperationDialogState.swift:260`; message verbatim at `:1492` and `:1494`. | no change |
| "Leave **Output Strategy** on Per Input" | true | Picker labelled "Output Strategy" at `FASTQOperationToolPanes.swift:109`; segment titles "Per Input" and "Grouped Result" at `:992` and `:994`; default `.perInput` via `defaultOutputMode` (`FASTQOperationDialogState.swift:2107-2113`, `:204`). | no change |
| "**Operations > Show Operations Panel** (Cmd-Shift-P)" | true | `MainMenu.swift:866-872`, key equivalent "p" with `[.command, .shift]`. | no change |
| "Extract Reads by ID takes a **Query** plus a **Field** choice and a regular-expression checkbox. Extract Reads by Motif takes a **Pattern** and the same checkbox. Subsample by Proportion takes a **Proportion** between 0 and 1." | true | `FASTQOperationToolPanes.swift:573-587` and `:567-568`. | no change |
| "A paired-end sample is stored inside its bundle as one interleaved file" | true | CONSISTENCY records this for imports, and the pipeline branches on `isInterleavedBundle(sourceBundleURL)` throughout (`FASTQDerivativeService+Transformations.swift:32`, `:71`, `:150`, `:180`). | no change |
| "The two search operations find the matching reads, collect their names, and then go back to the original file to pull both mates of every name they found, so a hit on either mate brings its partner along." | true | `FASTQDerivativeService+Transformations.swift:150-157` and `:180-187` route interleaved input through `runPairedAwareSearch`, whose three documented steps (`FASTQDerivativeService+SubsetHelpers.swift:44-94`) are exactly search, extract deduplicated base IDs, re-extract both mates from the source. | no change |
| "Subsampling draws pairs, so a pair is either kept entire or dropped entire." | true | Interleaved subsampling runs `reformat.sh` with `interleaved=t` (`Transformations.swift:34-46` and `:73-87`), which samples pairs. | no change |
| "Subsample by Count on a paired bundle halves your number to get a pair count ... Asking for an odd number rounds down." | true | `Transformations.swift:73`: `let pairCount = max(1, count / 2)`. Swift integer division truncates, so an odd count rounds down. | no change |
| "**Output Strategy.** ... It defaults to Per Input ... This setting has no command-line flag, because the command line takes one input file at a time." | true | Default `.perInput`; options `[.perInput, .groupedResult]` (`FASTQOperationDialogState.swift:1066-1072`); `supportsConfigurableOutput` (`:2135-2141`) is true for all five. `cli_flag: null` in all five registry entries, and every CLI subcommand takes a single `<input>` argument (`fastq.txt`). | no change |
| "**Proportion.** ... The field starts empty and accepts a fraction between 0 and 1, and the Run button reads 'Enter a proportion between 0 and 1.' ... On the command line this is `--proportion`." | true | Default `nil` (`:259`); message verbatim at `:1486` and `:1488`; guard `> 0, <= 1` at `:744`; `--proportion` in `fastq.txt:103`. Matches registry `fastq.subsample-by-proportion`. | no change |
| "Asking for 0.1 of the fixture's 45,574 reads returned 4,473, which is 9.81 percent rather than 10 percent." | true | Recounted `subset-prop-0.1.fastq` = 4,473. 4473/45574 = 9.81 percent. | no change |
| "**Count.** ... accepts a whole number above 0, showing 'Enter a positive read count.' ... On the command line this is `--count`." | true | Guard `> 0` at `:754`; message at `:1492`; `--count` in `fastq.txt:105`. Matches registry `fastq.subsample-by-count`. | no change |
| "Asking for 10,000 reads from the fixture returned exactly 10,000." | true | Recounted `subset-count-10000.fastq` = 10,000. | no change |
| "If the bundle holds fewer reads than you asked for, you get all of them rather than an error." | unverifiable | This is standard `seqkit sample2 -n` behaviour and the registry says the same, but no run in the evidence set asked for more reads than the input holds, and no guard in `FastqSubsampleSubcommand` (`FastqCommand.swift:320-350`) or `Transformations.swift:68-105` caps or errors on the case. A run with `--count 100000` against the 45,574-read fixture would settle it. | keep, or verify with an over-count run before publication |
| "**Query.** ... the field starts empty with 'Enter a read ID or search pattern.' shown until you fill it. ... On the command line this is `--query`." | true | Message at `:1498`; `--query` at `fastq.txt:1383`. Matches registry `fastq.extract-reads-by-id`. | no change |
| "Searching the fixture for `HISEQ1` with the box unticked returns zero reads, even though 10,622 reads have identifiers beginning with `HISEQ1`" | true | Recounted: `hiseq1-plain.fastq` = 0 records; 10,622 R1 headers begin `@HISEQ1`. Mechanism confirmed: without `-r`, `buildSearchArgs` (`SubsetHelpers.swift:17-27`) and the CLI path (`Transformations.swift:159-168`) pass `seqkit grep -p`, which anchors to the whole ID. | no change |
| "Searching for the complete identifier `HISEQ1:93:H2YHMBCXX:1:1101:1457:14988` returns the one read that carries it." | true | Recounted `exact-one.fastq` = 1 record, and exactly one R1 header matches that identifier. | no change |
| "**Field.** ... offering ID and Description. The default is ID, which is the first word after the `@`, and Description is everything after the first space. ... On the command line this is `--field`, which takes `id` or `description`." | true | Segments "ID" and "Description" at `FASTQOperationToolPanes.swift:575-578`; default `.id` at `FASTQOperationDialogState.swift:263`; `--field` with values `id`/`description`, default `id`, at `fastq.txt:1384-1385`. Description maps to seqkit's `-n` (whole name line) at `SubsetHelpers.swift:19-21`. | no change |
| "Many FASTQ files, the HG002 fixture among them, have no description at all ... Searching either field gives the same answer on such a file" | true | Recounted: zero of 45,574 R1 headers contain a space. The `--field description` run (`desc.fastq`) returned the same 10,622 as the ID run. | no change |
| "**Use Regular Expression.** ... It is off by default ... On the command line this is `--regex`." | true | Default `false` at `:264`; toggle label at `FASTQOperationToolPanes.swift:580`; `--regex` at `fastq.txt:1386`. | no change |
| "Plain text with this box ticked already means 'contains this anywhere' ... Searching the fixture for `HISEQ1` with the box ticked returns all 10,622 reads from that instrument." | true | Recounted `hiseq1.fastq` = 10,622, matching the header count. | no change |
| "**Pattern.** ... The field starts empty and shows 'Enter a motif or search pattern.' until filled. ... On the command line this is `--pattern`." | true | Message at `:1503`; `--pattern` at `fastq.txt:1403`. Matches registry `fastq.extract-reads-by-motif`. | no change |
| "This operation searches both strands. A read whose sequence contains the reverse complement of your pattern matches just as a read containing the pattern itself does" | true | Both surfaces pass seqkit's by-sequence grep, which searches both strands by default. GUI: `buildMotifSearchArgs` (`SubsetHelpers.swift:30-36`) emits `grep -s`; non-interleaved GUI path emits `grep -s` (`Transformations.swift:190-196`); CLI emits `grep --by-seq` (`FastqSearchMotifSubcommand.swift:31`). Measured below. | no change |
| "Searching the fixture for the 26-base Alu motif `GCCTCCCAAAGTGCTGGGATTACAGG` returned 521 reads, which is 260 reads carrying the motif as written plus 261 carrying its reverse complement." | true | Independently recomputed on the fixture: 260 R1 reads contain the motif, 261 contain `CCTGTAATCCCAGCACTTTGGGAGGC`, zero contain both, and `alu-motif.fastq` holds 521 records. 260 + 261 = 521 exactly. | no change |
| "Writing `GCCTCCCAAAGTGCTGGGATTACAG[GA]` means 'either G or A in the final position', and that pattern returned 565 reads from the fixture against the literal pattern's 521, picking up 44 reads" | true | Recounted `alu-regex.fastq` = 565. 565 - 521 = 44. | no change |
| "**Sequence or FASTA Path.** ... the single field accepts either kind of input ... LGE decides which you meant by looking at whether the text is shaped like a path. It starts empty and shows 'Enter a literal sequence or FASTA path.' ... On the command line these are two separate options, `--sequence` and `--fasta-path`." | true | Label at `FASTQOperationToolPanes.swift:590`; `isPathLikeSequenceFilterValue` (`FASTQOperationDialogState.swift:1892-1896`) tests for a slash or a `.fa`/`.fasta` suffix and routes to `fastaPath` at `:784`; message at `:1508`; both CLI options at `fastq.txt:1465-1467`. | no change |
| "**Search End.** ... offering 5' End and 3' End. It defaults to 5' End" | true | Segments verbatim at `FASTQOperationToolPanes.swift:592-593`; default `.fivePrime` at `FASTQOperationDialogState.swift:268`. cutadapt mapping confirms the semantics: `-g` for 5', `-a` for 3' (`FASTQDerivativeService+BBTools.swift:602`). | no change |
| "`--search-end` accepts `both` and defaults to it, while the window offers only the two ends and starts on 5' End." | true | `fastq.txt:1468-1470`: values left, right, both, default both. The picker has no third segment. This is the DRIFT "Missing" row, now covered. | no change |
| "**Min Overlap.** ... It defaults to 16 in the window ... On the command line this is `--min-overlap`, which defaults to 8 rather than 16." | true | GUI default 16 at `FASTQOperationDialogState.swift:269`; CLI default 8 at `fastq.txt:1471-1472`. Matches registry `fastq.select-reads-by-sequence`. | no change |
| "matched 218 reads at an overlap of 16, 12,058 reads at an overlap of 12, and 40,507 reads at an overlap of 8" | true | Recounted `ov-16.fastq` = 218, `ov-12.fastq` = 12,058, `ov-8.fastq` = 40,507. | no change |
| "**Error Rate.** ... It defaults to 0.15 in the window, so a 16-base match tolerates two mismatched bases. ... On the command line this is `--error-rate`, which defaults to 0.1 rather than 0.15." | true | GUI default 0.15 at `:270`; CLI default 0.1 at `fastq.txt:1473-1475`. 0.15 x 16 = 2.4, so two whole bases, which is what cutadapt's `-e` fraction permits. | no change |
| "**Keep Matched Reads.** ... It starts on ... On the command line this is `--keep-matched`, which is off unless you pass it, the opposite of the window's default." | true | GUI default `true` at `:271`; toggle label at `FASTQOperationToolPanes.swift:600`; CLI `--keep-matched` is a bare flag (`fastq.txt:1476`), and `FastqSequenceFilterSubcommand.swift:52-56` sends `outm=` only when it is passed. GUI inversion mechanism at `BBTools.swift:592-596`. | no change |
| "keeping matched reads returned 218 and turning the setting off returned 45,356, and those two numbers sum to the 45,574 reads the file holds." | true | Recounted `adapter-matched.fastq` = 218 and `adapter-removed.fastq` = 45,356. 218 + 45,356 = 45,574. | no change |
| "**Search Reverse Complement.** Also looks for the reverse complement of your sequence, at the opposite end of the read from the one you chose ... It is off by default. ... On the command line this is `--search-rc`." | true | Default `false` at `FASTQOperationDialogState.swift:272`. The opposite-end behaviour is exact: `BBTools.swift:602-608` adds `oppositeFlag` plus the reverse complement, with the code comment stating the same rule. `--search-rc` at `fastq.txt:1477`. | no change |
| "Turning it on for the fixture's TruSeq adapter changed nothing, returning the same 218 reads" | true | The author's `--search-rc` run is recorded at 218, matching `ov-16.fastq` = 218. | no change |
| "The new bundle appears in the sidebar under `Analyses/`" | true | `defaultOutputDirectory` returns `<project>/Analyses` (`:1607-1613`). Only the timestamped-subfolder detail in Before you start is wrong. | no change |
| Results table: 4,473 at 9.8%, 10,000 at 21.9%, 10,622 at 23.3%, 521 at 1.1%, 218 at 0.5% | true | Every count recounted above. Percentages recomputed against 45,574: 9.81, 21.94, 23.31, 1.14, 0.48. All five round as printed. | no change |
| "10,622 of the fixture's reads came off the HiSeq instrument and the remaining 34,952 came off the other one" | true | 45,574 - 10,622 = 34,952, and all four remaining run IDs carry the `D00360` instrument name. | no change |
| "about one read in ninety carries a recognisable Alu right-arm end" | true | 45,574 / 521 = 87.5, which "about one in ninety" covers. | no change |
| "The fifth says a half of one percent of reads ran into the adapter, which for 250-base reads ... is a healthy figure" | true | 218/45,574 = 0.48 percent. The fixture is NIST Illumina 2x250bp (`fixtures/hg002-chr20/README.md`), and the modal recomputed read length is 250. | no change |
| "Every operation writes a provenance record inside the new bundle recording the tool, its version, the exact parameters, and checksums of the input and the output ... whether you ran the operation from the window or from the command line." | true | `requiresProvenance` is true for every FASTQ operation (`FASTQOperationDialogState.swift:2143-2149`), and every CLI run in the evidence set emitted a `.lungfish-provenance.json` sidecar beside its output. | no change |
| "A bundle produced by these operations is usually a virtual bundle ... Only a preview of about a thousand reads sits on disk. Reveal the bundle in the Finder and you find a `preview.fastq`" | true | None of the five is an `isFullOperation` case (`FASTQDerivativeServiceModels.swift:133-142`), so all five take the subset branch at `FASTQDerivativeService+Core.swift:320-341`, which writes `read-ids.txt` plus `preview.fastq`. `writePreviewFASTQ` defaults to `readLimit: 1_000` (`SubsetHelpers.swift:301-310`). The hedge "usually" is the right strength, since a FASTA-output variant stores differently. | no change |
| "The read count in the viewport is the real count, not the preview's." | true | Statistics are computed over the full transformed output before the preview is written (`FASTQDerivativeService+Core.swift:203-213`), so the stored `readCount` is the subset's true size. This settles DRIFT row 20's read-count half. | no change |
| "LGE performs materialization, rebuilding the full file as the first step of that job and clearing it away at the end. You never ask for this and there is no button for it." | true | `materializeInputFilesIfNeeded()` runs as the first pipeline step and cleans up its temporaries; `AssemblyConfigurationViewModel.swift:570` carries the same comment for the assembly path. No user-facing materialize control exists in the operations window. | no change |
| "`lungfish-cli fastq materialize SampleA.lungfishfastq --output SampleA.fastq`" | true | `fastq.txt:651-669`: takes `<input>` as a `.lungfishfastq` bundle path and requires `-o, --output`. | no change |
| "Its reads are 76.7 percent from one instrument and 23.3 percent from the other, and the 0.1 subsample came back 76.5 percent and 23.5 percent." | true | Recomputed: parent 34,952/45,574 = 76.7 and 10,622/45,574 = 23.3. Subsample 3,422/4,473 = 76.5 and 1,051/4,473 = 23.5. | no change |
| "Add `--force` to overwrite an output that already exists ... and add `--compress` to gzip what it writes." | true | Both flags on all four subcommands (`fastq.txt:107-108`, 1381-1382, 1401-1402, 1463-1464). | no change |
| The five-command code block (`fastq subsample --proportion/--count`, `search-text --query --regex`, `search-motif --pattern`, `sequence-filter --sequence --search-end right --min-overlap 16 --error-rate 0.15 --keep-matched`) | true | Every flag and every value is legal per `fastq.txt`, and `right` is the correct CLI spelling for the window's 3' End. The `sequence-filter` line's edist is 0.15 x 16 = 2.4, rounding to 2, so it stays inside bbduk's cap and runs. | no change |
| "`sequence-filter` runs a different program than the window does. The window uses cutadapt and the command line uses bbduk" | true | GUI: `Transformations.swift:420-446` calls `runCutadaptAdapterPresenceFilter` and stamps `toolUsed: "cutadapt"`. CLI: `FastqSequenceFilterSubcommand.swift:84` runs `.bbduk`. | no change |
| "It converts your error rate and overlap into an edit distance, and bbduk refuses an edit distance above 2, so `--min-overlap 18 --error-rate 0.15` fails with a Java error rather than a readable message." | true | `FastqSequenceFilterSubcommand.swift:67`: `edist=\(max(1, Int((errorRate * Double(minOverlap)).rounded())))`. Reproduced independently on a fixture copy: that exact flag pair exits non-zero with `Exception in thread "main" java.lang.AssertionError: edit distance must be between 0 and 2; default is 0.` | no change |
| "Keep the product of the two at or below 2, as `--min-overlap 20 --error-rate 0.1` does" | true | 20 x 0.1 = 2.0, and `ov20e10.fastq` = 179 records, so that combination completed. | no change |
| "The command line draws the same reads every time you run it, so the two runs of `--proportion 0.1` behind this chapter returned identical sets of 4,473 reads. The window seeds its draw afresh on each run" | true | The CLI passes no `-s` to seqkit (`FastqCommand.swift:328-342`), so seqkit's fixed default seed applies. The GUI generates `UInt64.random(in: 0...UInt64(Int64.max))` per run and passes it as `-s` or `sampleseed=` (`Transformations.swift:31`, `:70`). Recounted: `subset-prop-0.1.fastq` and `subset-prop-run2.fastq` are both 4,473; `subset-count-10000.fastq` and `subset-count-run2.fastq` are both 10,000. | no change |
| "Neither surface offers a seed you can set" | true | `fastq.txt` `==== fastq subsample ====` exposes only `--proportion`, `--count`, `--output`, `--force`, `--compress`. The pane shows only Proportion or Count (`FASTQOperationToolPanes.swift:567-571`). | no change |
| "`lungfish-cli extract reads --by-id` reads a text file of identifiers, one per line, and pulls exactly those reads." | true | `extract.txt:78-81` and `:107`. | no change |
| "Running that against a 20-name list drawn from the fixture returned 20 reads." | true | Reissued the command independently against a fixture copy with the author's 20-line `read-ids.txt`. Output holds exactly 20 records. | no change |
| "Note that the output is gzip-compressed and named `picked.fastq.gz` whatever extension you gave `--output`." | true | Reproduced independently: `--output rev-picked.fastq` produced `rev-picked.fastq.gz`, and the command's own summary block printed `Output files : rev-picked.fastq.gz`. | no change |
| "Repeat `--source` for paired data, passing R1 and R2, and the command writes a matching pair of files with `_R1` and `_R2` in their names." | true | `extract.txt:108-109` documents the repeatable `--source`. The author's two-source run produced `paired-default_R1.fastq.gz` and `paired-default_R2.fastq.gz`, recounted at 20 records each. | no change |
| "Pass `--keep-read-pairs` to bring both mates along when either one matches, and `--no-keep-read-pairs` to emit only the exact reads whose names were listed." | true | `extract.txt:110-115`, both flags with those meanings. The chapter states each flag's effect without claiming which is the default, which is exactly what DRIFT changed row 18 asked for. | no change |
| "Add `--bundle`, or `--bundle-name <name>` which implies it, to wrap the result as a `.lungfishfastq` bundle" | true | `extract.txt:151-152`. | no change |
| "`--by-region` pulls the reads aligned to a genomic region out of an indexed BAM, `--by-db` queries a metagenomics database by taxonomy, and `--by-classifier` takes a selection you made in a classifier result" | true | `extract.txt:83-97`. | no change |
| "A parallel command, `lungfish-cli extract contigs`, does the same job for assemblies." | true | `extract.txt:169-173`. | no change |
| Cross-links to `01-importing-fastq.md`, `07-ont-runs.md`, `../04-alignments/01-mapping-reads-to-a-reference.md`, `../06-classification/02-running-kraken2.md`, `../appendices/cli-reference.md` | true | All five files exist. | no change |
| Fixture download URL `https://github.com/dhoconno/lungfish-genome-explorer/tree/main/docs/user-manual/fixtures/hg002-chr20` | unverifiable | The local path and both named files exist under `docs/user-manual/fixtures/hg002-chr20/`, but whether the GitHub tree is public at that owner and path cannot be checked without network access, which this role does not use. | verify the public URL resolves before publication |

## Front matter

| Item | Verdict | Evidence |
|---|---|---|
| `parameters_refs` matches roster row 19 | true | DRIFT.md:3745 lists exactly `[fastq.subsample-by-proportion, fastq.subsample-by-count, fastq.extract-reads-by-id, fastq.extract-reads-by-motif, fastq.select-reads-by-sequence]`, in that order. The chapter's line 10 is identical. |
| Every registry setting has a Settings paragraph with the exact label, default, and allowed values | true | All fourteen distinct settings across the five entries are covered. Proportion, Count, Query, Field, Use Regular Expression (twice, once per operation), Pattern, Sequence or FASTA Path, Search End, Min Overlap, Error Rate, Keep Matched Reads, Search Reverse Complement each have a paragraph whose bolded label, stated default, and stated allowed values match the registry exactly. Output Strategy appears once with an explicit note that it is shared, which the campaign template permits and which the author declared. |
| `tools: [seqkit, cutadapt, bbduk, reformat]` | true | seqkit backs both search operations and the CLI subsample; `reformat.sh` backs the GUI subsample on interleaved input (`Transformations.swift:36`, `:75`); cutadapt backs the GUI Select Reads by Sequence (`:421`); bbduk backs the CLI `sequence-filter` (`FastqSequenceFilterSubcommand.swift:84`). All four are reached. |
| `shots` lists every `<!-- SHOT -->` marker with a caption | true | Body markers at lines 85, 95, 101 are `search-subsetting-menu`, `subsample-by-count-pane`, `select-reads-by-sequence-pane`. Front matter lines 18-24 carry all three ids with captions, and no extra. |
| Shot caption "showing Search End on 5' End, Min Overlap 16, Error Rate 0.15, and Keep Matched Reads on" | true | Matches `FASTQOperationDialogState.swift:268-271`. |
| Every `glossary_refs` anchor resolves in GLOSSARY.md | true | All nineteen anchors resolve to exactly one `{#anchor}` each: fastq, bundle, sidebar, inspector, provenance, adapter, reverse-complement, interleaved-fastq, paired-end, seqkit, cutadapt, bbduk, subsampling, virtual-bundle, materialization, sequence-motif, alu-element, read-identifier, regular-expression. The author's seven additions are present and in the existing entry shape. |
| `entry_points` menu paths | true | Same evidence as the menu-path row above. |
| `fixtures_refs: [hg002-chr20]` | true | Fixture directory exists with both named FASTQ files. |

## The author's three defect claims

**1. bbduk edit-distance assertion crash.** Confirmed by independent
reproduction. `--min-overlap 18 --error-rate 0.15` on a fixture copy exits
non-zero with the raw Java assertion the author quoted. The arithmetic in
`FastqSequenceFilterSubcommand.swift:67` is as described, and the failure is
unguarded, so it surfaces as a stack trace rather than a validation error. The
severity argument holds: the window's own default error rate of 0.15 crashes
the CLI at any overlap of 18 or more, and the window has no such cap because it
runs cutadapt.

**2. The registry note for `fastq.extract-reads-by-motif` is wrong.** Confirmed.
The note reads "The motif is matched on the read as stored, so the reverse
complement is not searched unless you write it into the pattern." Both surfaces
pass seqkit's by-sequence grep (`SubsetHelpers.swift:30-36` emits `grep -s`;
`FastqSearchMotifSubcommand.swift:31` emits `grep --by-seq`), which searches
both strands, and the measurement is decisive: 260 forward plus 261
reverse-complement equals the 521 returned, with no read carrying both. The
same wrong belief is carried by ground-truth row 24 ("Extract Reads by Motif
matches the pattern exactly on the read as written", verdict true) and row 10's
"no strand option" clause. Those two ground-truth rows should be corrected
alongside the registry note. The chapter itself is right.

**3. `extract reads --output` always writes `.gz`.** Confirmed by independent
reproduction. `--output rev-picked.fastq` produced `rev-picked.fastq.gz`. The
command's summary block does print the real name, so the chapter's framing of it
as a surprise rather than data loss is accurate.

## Verdict counts

True 62, false 1, unverifiable 2.
