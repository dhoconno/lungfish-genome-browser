# Reader 3 report: Extracting Contigs

Persona: pre-med undergraduate reading in English as a second language. Biology vocabulary is strong from textbooks. Idioms, figures of speech, and long sentences slow me down. I have never opened a terminal.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| What it is, "An assembler hands you every contig it managed to build" | "Hands you" is an idiom. A program does not have hands. I stopped to check whether something is given to me physically or on screen. Also "managed to build" suggests effort or difficulty, which confused me about whether some contigs failed. | Say the assembler produces every contig it could build. |
| What it is, "the thing you were after" | "After" used this way is idiomatic. I know "after" as time or sequence. I read the sentence twice. | Say the contig you want. |
| What it is, "short fragments the assembler could not extend" | "Extend" is not explained. I know the biology word for elongation of a strand, but here it seems to mean something about the software. I could not tell which. | Say the assembler found no more overlapping reads to add. |
| What it is, "MEGAHIT produced three contigs" | MEGAHIT appears with no explanation of what it is. I guessed it is a program only from context much later. | Name it as an assembler at first mention. |
| What it is, "holds essentially the whole mitochondrial genome" | "Essentially" is vague to me. I cannot tell if this means all of it, or most of it, or if something is missing that matters. | Give the fraction or say what is absent. |
| What it is, "together under 4% of the assembly" | I do not know how to judge 4%. Is 4% small enough to ignore always, or only here? The text does not say what number would worry me. | Say what a normal leftover share looks like before giving this number. |
| What it is, "derives a new reference bundle" | "Derives" is an unusual verb here. In chemistry a derivative is a modified compound. I could not tell if the bundle is a copy, a modification, or a link. | Say the operation creates a new bundle containing copies of those contigs. |
| What it is, "the folder LGE uses to keep a sequence together with its indexes and its history" | This sentence is long and carries three new ideas at once, bundle, index, and history. I had to read it three times. Also "history" is not explained. | Split into two sentences and say what history means. |
| What it is, "builds a FASTA index beside it so tools can jump to any position without reading the whole file, assembles a `.lungfishref` bundle around the pair, and writes a provenance record naming where the sequence came from" | This is one sentence with four actions joined together. It is the longest sentence in the chapter and I lost the subject halfway. "Jump" is also figurative. | Break into a short numbered list of what LGE writes. |
| What it is, "The FASTA inside a derived bundle is left uncompressed" | I do not know what compression does here or whether this changes anything I must do. It is stated without consequence. | Say whether this affects anything the reader does. |
| What it is, "The point of doing this is narrower than it first appears" | "Narrower than it first appears" is a figure of speech. I did not know what "it" refers to, the point or the operation. | State plainly that extraction has one main use. |
| What it is, "any reference picker in LGE will accept it whole" | "Reference picker" is not explained, and "accept it whole" is idiomatic. I did not know a picker is a part of the interface. | Say the menus where you choose a reference will list the whole assembly. |
| What it is, "So what should you do with this?" | A rhetorical question. I stopped and reread the paragraph thinking I had missed an instruction addressed to me. | Cut the question and give the advice directly. |
| What it is, "decide whether the fragments are helping or distracting" | "Distracting" describes a person, not data. I could not tell what a distracting contig does to my result. | Say the fragments can take reads away from the contig you want. |
| Why you would do this, "the available reference is too distant to map against comfortably" | "Too distant" has no number, and "comfortably" describes a feeling, not a measurement. I cannot judge when this applies to my sample. | Give a rough percent identity below which mapping struggles. |
| Why you would do this, "let reads scatter across the short fragments" | "Scatter" is figurative. Reads do not move by themselves. I understood only after rereading. | Say some reads would align to the fragments instead. |
| Why you would do this, "which muddies the depth numbers you use to judge a call" | Three problems in one clause. "Muddies" is a metaphor, "depth" is not glossed, and "a call" is not defined at first use. | Gloss depth and variant call, and replace "muddies" with a plain verb. |
| Why you would do this, "gives every read a single place to go" | Figurative again. Reads do not go places. | Say each read then has only one matching sequence. |
| Why you would do this, "as a sequence rather than as a table row" | I did not understand this contrast until I read the next sentence. The contrast is stated before either thing is described. | Describe the sequence viewport first, then the contrast. |
| Why you would do this, "move along it by coordinate, translate it, or search it" | "Translate" is ambiguous for me. In my textbooks translation makes protein. Here it might mean shifting the view sideways. | Say which meaning is intended. |
| Why you would do this, "Deriving a reference bundle from that contig is the route across" | "The route across" is a metaphor with no stated destination. I did not know what I am crossing. | Say this is how you open a contig in the sequence viewport. |
| Why you would do this, "The third case is tidying." | "Tidying" is informal and I was not sure what is being tidied, the project folder or my thinking. | Name what is being organised. |
| Why you would do this, "months later the bundle can still tell you what it is" | A bundle cannot tell me anything. I understood the meaning, but it slowed me. | Say you can still read where the bundle came from. |
| Why you would do this, "Keeping the full assembly costs you nothing" | "Costs you nothing" is idiomatic, and it is not true for disk space, which I worry about on a laptop. | Say the assembly can stay and can be extracted from again. |
| Before you start, "You need a project open." | This is the first instruction in the chapter and it assumes the app is already running. Nothing tells me to launch Lungfish Genome Explorer. | Add a first step that says open the app. |
| Before you start, "choose **File > New Project** (Cmd-N)" | I do not know what Cmd-N means or where to press it. I have used Windows keyboards in my classes. | Say Cmd is the command key on a Mac keyboard, once, at first use. |
| Before you start, "Download the folder `human-mito` from the manual's fixtures" | "Fixtures" is not explained. In my English this word means something fixed to a wall. | Say these are example files provided for practice. |
| Before you start, "GitHub offers no download for a single folder" | I have never used GitHub. I do not know what a repository or a front page is, and the instruction has six steps in one sentence. | Turn this into a short numbered list and say what GitHub is. |
| Before you start, "double-click the downloaded file to unpack it" | I am not told how large the download is or where it lands, so I could not tell whether it worked. | Say the approximate size and that it goes to the Downloads folder. |
| Before you start, "Run [Running SPAdes](02-running-spades.md) against those reads first" | The link name says SPAdes but the next sentence tells me I need the MEGAHIT run. I could not tell which one to actually perform. | Say inside that chapter choose MEGAHIT, not SPAdes. |
| Before you start, "leaves nothing to choose between" | Idiomatic. I read it twice to see that it means there is only one contig. | Say the SPAdes run gives one contig so no selection is needed. |
| Before you start, "The assembly lands in a timestamped folder under `Analyses/`" | "Lands" is figurative and "timestamped" is not shown, so I would not recognise the folder in the sidebar. | Show an example name such as the one used later in the chapter. |
| Before you start, "There is no `Assemblies/` folder." | I never expected one. Being told a folder does not exist confused me about whether I did something wrong. | Remove, or move to a note for people who used an older version. |
| Procedure, "The steps are the same whether you extract one contig or several." | This sits before the list with no heading, so I was not sure if it was a step. | Keep, but it reads better as part of step 1's lead-in. |
| Procedure step 1, "Open the assembly bundle from `Analyses/` in the sidebar so its result viewport appears" | "Sidebar" and "result viewport" both appear here for the first time with no description of where they are on screen. I could not perform this step. | Say where the sidebar and the viewport sit in the window. |
| Procedure step 1, "its rank, name, length in bases, GC percent, share of the assembly" | "Rank" is not explained. I do not know what is ranked or in which direction. | Say contigs are ranked by length, longest first. |
| Procedure step 1, "There is no coverage column." | Coverage is not explained at this point in the chapter, and I did not know to expect the column. | Explain coverage where it is first named, or drop this sentence. |
| Procedure step 2, "The action bar along the bottom reads the count back to you" | "Reads the count back to you" is idiomatic. An action bar does not read aloud. I had to reread. | Say the action bar shows how many rows are selected. |
| Procedure step 2, "shows 'Select contigs to materialize'" | "Materialize" is not explained anywhere in the chapter. I know the word only from physics and from films. | Gloss it, or note that it is the app's word for building the bundle. |
| Procedure step 2, "select the 16,711-base contig and leave the two short fragments alone" | I am told the length but not how to find that row. Contig names appear only much later in the command-line section. | Say select the top row, which is the longest. |
| Procedure step 3, "with exactly one contig selected the first reads **BLAST Contig** instead" | BLAST is never explained in this chapter. I have heard the name but do not know what pressing it would do. | Add one clause saying what BLAST does, or link the glossary. |
| Procedure step 4, "Watch the run in the Operations Panel" | I do not know whether opening this panel is required or optional. The step is written as a command so I assumed required. | Say it is a progress list and that opening it is optional. |
| Procedure step 4, "It finishes quickly, because the only work is subsetting and indexing." | "Quickly" gives me no number, and "subsetting" is not a word I have met. I could not tell whether to wait five seconds or five minutes. | Give an approximate time and say subsetting means copying the chosen contigs. |
| Procedure, "Right-clicking a selected row opens the same actions as a menu, plus three the action bar does not carry" | "Carry" is figurative for a menu bar. Also I use a trackpad and do not know how to right-click on it. | Say Control-click for trackpad users, and use "does not include". |
| Procedure, "**Align with MAFFT...** builds a multiple sequence alignment" | MAFFT is not explained. I know what a multiple sequence alignment is, but not what MAFFT is. | Say MAFFT is an alignment program. |
| Procedure, "so it stays greyed out on a single row" | "Greyed out" is interface slang I had to guess. | Say the item is disabled and cannot be clicked. |
| Settings, "Everything it does is decided by which rows are selected and what the bundle ends up called" | Long sentence and "ends up called" is idiomatic. I read it twice. | Split and say the bundle's name. |
| Settings, "they matter only if you read the optional section at the end of this chapter" | I did not know at this point that eight of the eleven entries below are command-line only, so I tried to find them in the window. | Add a subheading separating window settings from command-line flags. |
| Settings, Contig selection, "is the whole of the operation's input" | Abstract phrasing. I could not tell whether this means nothing else is needed. | Say the selection is the only input the operation takes. |
| Settings, Bundle name, "suggests something like `NODE_1_length_16697_cov_121.957333`" | This example comes from SPAdes but the chapter's procedure uses MEGAHIT, whose contig is named `k141_1` later. I was confused about which name I would see. | Use the MEGAHIT example to match the procedure. |
| Settings, `--assembly`, "Points the command at the assembly run folder" | The whole flag block assumes I know what a command line and a flag are. Terminal is only explained later, in the optional section. | Move the flag entries into the command-line section, or forward-reference it here. |
| Settings, `--output`, "The default is standard output, so the FASTA prints to the terminal" | "Standard output" is not explained. I have never opened a terminal. | Gloss standard output as the text the terminal prints on screen. |
| Settings, `--output`, "which is what makes the command pipe cleanly" | "Pipe" is used as a verb with no explanation, and "cleanly" is vague. | Gloss piping, or drop the clause. |
| Settings, `--bundle`, "so a bare command prints FASTA rather than creating anything" | "Bare command" is not explained. | Say a command without this flag. |
| Settings, `--line-width`, "which is the width most tools and most readers expect" | "Readers" here means people reading the file, but I first read it as a program. | Say people reading the file. |
| Reading the results, "A successful extraction is uneventful, and that is the intended experience." | I understood this only on the second reading. It tells me nothing will happen visibly, which contradicts the next sentence saying things appear. | State what appears on success. |
| Reading the results, "at 44.3% GC" | I cannot judge whether 44.3% is right. The chapter never says what GC percent I should expect for human mitochondria. | Say the expected value for this genome before giving the observed one. |
| Reading the results, "a length that does not match is a sign you selected a different row than you thought" | Long sentence with a double negative feel. I read it twice. | Split into two sentences. |
| Reading the results, "That block is what lets the bundle answer 'where did you come from?'" | The bundle is given a voice. I understood, but it slowed me and it is not how the rest of the chapter speaks. | Say the block records the origin of the sequence. |
| Reading the results, "Moving or renaming the source assembly afterwards is what makes that record harder to follow." | This warns me about a problem without telling me what to do. I do not know if the bundle stops working. | Say what breaks and whether it can be fixed. |
| Reading the results, "Two failures are worth recognising." | I could not tell whether these are common or rare, or whether I should do something to prevent them. | Say how likely each is and what to do. |
| Reading the results, "Could not resolve the enclosing Lungfish project root" | "Enclosing" and "project root" are not explained. I would not know what to change. | Say the assembly must sit inside a project folder, and how to move it. |
| What good looks like, "the strongest cheap signal that the assembly worked" | "Cheap signal" is a metaphor I did not understand. Nothing here has a price. | Say this is the quickest check available. |
| What good looks like, "a little over the reference by roughly the length of the repeated stretch at the point where a circular molecule gets cut open and written down as a line" | This is the hardest sentence in the chapter. It is very long, and it explains circular genome linearisation without naming it. I read it four times and still am not sure whether 142 extra bases is expected or a problem. | Split into two sentences, name the effect, and say whether the difference is acceptable. |
| What good looks like, "The long HG002 mitochondrial contig is 96.01% of its assembly" | I can see the number but the threshold is only implied. I do not know if 80% would also be fine. | Give a rough cut-off for what counts as dominant. |
| What good looks like, "Several contigs of comparable share means the assembly fragmented, or that the sample genuinely held several sequences, and you should work out which before extracting" | Long sentence, and it tells me to work something out without telling me how. | Split, and name one way to distinguish the two cases. |
| What good looks like, "If coverage against the extracted contig comes back patchy" | "Patchy" is not defined, and coverage is only glossary-linked here after being mentioned twice earlier. | Gloss coverage at first mention and say what patchy looks like. |
| What good looks like, "the assembly probably collapsed or fragmented the genome" | "Collapsed" is a technical meaning I do not know. In my English collapse means falling down. | Say what a collapsed assembly is. |
| What good looks like, "a name already in use gets a counter rather than overwriting anything" | "Gets a counter" is unclear until the command-line section shows an example much later. | Show the renamed example here. |
| On the command line, "The whole procedure runs headless, meaning with no window at all, by typing commands into the Terminal application" | This is the first sentence in the manual that tells me Terminal is an application, but it does not say where to find it or how to open it. I have never opened a terminal. | Say Terminal is in Applications, Utilities, and that the section can be skipped. |
| On the command line, "A run started from the window leaves one under `Analyses/`" | "Leaves one" is idiomatic and "one" refers back two sentences. I lost the referent. | Repeat the noun, run folder. |
| On the command line, the first code block | I do not know what the backslashes at the end of the lines do, or whether I type them. I also do not know what `./` means. | Add one sentence saying the backslash continues one command over several lines. |
| On the command line, "printed the path of the bundle it created on standard output and the line ... on standard error" | Standard output and standard error are both used without explanation, and the difference is not said to matter. | Gloss both, or say both appear in the terminal window. |
| On the command line, "stored in a file whose spaces become underscores" | I could not tell if I need to do this conversion or if the app does it. | Say the app does this automatically. |
| On the command line, "which is where it becomes scriptable" | "Scriptable" is not explained and this is the argument for the whole paragraph. | Gloss scripting as running the command automatically from a file. |
| On the command line, "One difference between the two sources is worth knowing, because it shows up in the bundle rather than in the run" | Long sentence and "shows up" is idiomatic. The contrast between bundle and run is not clear on first reading. | Split into two sentences. |
| On the command line, "The **Create Bundle** button takes the `--contigs` route internally" | This says the button in the window gives worse provenance than the command line, but the earlier sections told me the window records the assembler. I had to go back and check. | Say plainly in the Reading the results section that a window-made bundle records Unknown. |
| Next, "Continue to Mapping Reads to a Reference ... or to Calling Variants from Amplicons" | I do not know which of the two I should choose, or whether I need both. | Say which one follows this chapter for a first-time reader. |

One thing I learned: extraction only copies the contigs you select into a new folder and never changes the assembly it came from.

One thing I still could not do: judge whether the 16,711-base contig is correct, because the sentence explaining the difference from the 16,569-base reference is too long and never says whether the difference is acceptable.

The sentence I liked most: "Extraction is cheap to redo, and a name already in use gets a counter rather than overwriting anything."
