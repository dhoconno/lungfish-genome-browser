# Reader 2 report: Extracting Contigs

Persona: senior undergraduate, two years of wet-lab pipetting, library prep and PCR fluent, no data analysis experience, has never opened a terminal.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| What it is, "An assembler hands you every contig" | "Assembler" is used before it is explained. I know what a contig is by the end of the sentence, but I do not know what an assembler is or that it is a program. I had to read to the MEGAHIT sentence to guess. | Gloss assembler at first use as the program that builds contigs from reads. |
| What it is, "the overlaps between reads" | "Reads" is never glossed in this chapter. In the lab a read is what comes off the sequencer, but I do not know whether the manual means one sequence, one file, or one sample. | Gloss read at first use. |
| What it is, "MEGAHIT produced three contigs" | MEGAHIT appears with no explanation. I cannot tell if it is a setting, a file format, or a program until much later. | Say it is an assembler at first mention. |
| What it is, "The other two are 362 and 332 bases, together under 4%" | I cannot judge whether 4% is good, bad, or expected. It is stated as a fact with no interpretation. | Say what a small leftover share means before giving the number. |
| What it is, "builds a FASTA index beside it so tools can jump" | I do not know what an index is in this sense, and "jump to any position" does not tell me why I should care or whether I ever have to make one myself. | Add one sentence saying the index is built for you and you never touch it. |
| What it is, "the extension `.lungfishref`" | I do not know what "extension" means for a folder. Everything I have seen with an extension is a file. A folder with an extension is confusing. | Say the bundle is a folder that macOS shows as a single item. |
| What it is, "left uncompressed, unlike the one inside an assembly bundle" | I do not know what compression is doing here or whether this matters to me. It reads as a warning without telling me what to do. | Say whether this affects anything I do, or drop it from the primer. |
| What it is, "So what should you do with this?" | The rhetorical question made me stop and reread the paragraph to check whether I had missed instructions. | Cut the question and state the advice directly. |
| Why you would do this, "which muddies the depth numbers you use to judge a call" | "Depth" is not glossed and "judge a call" assumes I already do variant calling. I have never called a variant. | Gloss depth, and say what a call is before saying how to judge one. |
| Why you would do this, "too distant to map against comfortably" | "Too distant" is not defined. I have no sense of how different is too different. | Give a rough sense of what distant means here. |
| Why you would do this, "move along it by coordinate, translate it, or search it" | "Translate" is ambiguous. In genetics class translation is making protein, but here it might mean moving the view. | Say which meaning is intended. |
| Before you start, "choose **File > New Project** (Cmd-N)" | This is the first mention of a menu path and I do not know if the app is already open or how to open it. Nothing earlier in the chapter tells me to launch anything. | Add a first step that says open Lungfish Genome Explorer. |
| Before you start, "Download the folder `human-mito` from the manual's fixtures" | "Fixtures" is not explained. I could not tell whether this is example data, test data, or something I must have. | Say plainly that these are example files for practising. |
| Before you start, "double-click the downloaded file to unpack it" | The ZIP is the whole repository. I do not know how large that is or whether I have room, and I am not told what to do with the rest of it afterwards. | Say roughly how big the download is and that the rest can be deleted. |
| Before you start, "Run [Running SPAdes](02-running-spades.md) against those reads first" | The link says SPAdes but the paragraph then says I need the MEGAHIT run. I could not tell which one to actually do. | Say directly that inside that chapter I should choose MEGAHIT. |
| Before you start, "The assembly lands in a timestamped folder" | I do not know what "timestamped" will look like in the sidebar, so I would not recognise the folder. | Show an example folder name. |
| Before you start, "There is no `Assemblies/` folder" | I never expected one, so this correction confused me more than it helped. | Drop it, or move it to a note for people upgrading. |
| Procedure step 1, "Open the assembly bundle from `Analyses/` in the sidebar" | I do not know what "the sidebar" is or where it sits in the window. This chapter is the first place it appears. | Name where the sidebar is on screen at first mention. |
| Procedure step 1, "its rank, name, length in bases, GC percent, share of the assembly" | "Rank" is not explained. I do not know what it is ranking by. | Say what the ranking is based on. |
| Procedure step 1, "There is no coverage column" | Coverage is a glossary link elsewhere in the chapter but is not glossed here, and being told a column is absent is confusing when I did not know to look for it. | Move this to a note, or say why someone might have expected it. |
| Procedure step 2, "shows 'Select contigs to materialize'" | "Materialize" is not explained anywhere. It is the app's word, not one I know. | Gloss materialize, or note that it is the app's wording for building the bundle. |
| Procedure step 2, "select the 16,711-base contig" | I am told which contig to pick but not how to tell which row it is once I am looking at the real table. The names are not given until the command-line section. | Name the row, for example the top row by length. |
| Procedure step 3, "with exactly one contig selected the first reads **BLAST Contig**" | BLAST is never explained. I have heard the word but do not know what pressing it would do, and this chapter never says. | One clause saying what BLAST does, or a glossary link. |
| Procedure step 4, "Watch the run in the Operations Panel" | I do not know what the Operations Panel is for or whether I must open it. The step reads as required but sounds optional. | Say it is a progress list and that opening it is optional. |
| Procedure step 4, "It finishes quickly, because the only work is subsetting and indexing" | "Subsetting" and "indexing" are jargon here, and "quickly" gives me no way to know whether ten seconds means something is wrong. | Give a rough time so I know when to worry. |
| After step 5, "**Extract Sequence...** takes a sub-range of a contig" | "Sub-range" is not explained and I do not know how I would specify one. | Say it lets you type start and end positions. |
| After step 5, "**Align with MAFFT...** builds a multiple sequence alignment" | MAFFT is unexplained and multiple sequence alignment is not glossed. I do not know why I would want one. | Gloss both briefly. |
| After step 5, "hands the selection to the FASTQ/FASTA Operations dialog" | This dialog is named with no explanation and no link. I have no idea what it does. | Link it or say in one clause what the dialog is for. |
| After step 5, "BLAST is capped at 50 selected sequences" | The 50 number arrives with no context and I still do not know what BLAST does. | Move this beside the BLAST explanation. |
| Settings, "they matter only if you read the optional section at the end" | I had to read this sentence twice to work out that the next several entries do not apply to me at all. The list then runs on for eight entries with no visual break. | Put the command-line-only settings under their own heading. |
| Settings, "so a SPAdes selection suggests something like `NODE_1_length_16697_cov_121.957333`" | The example is from SPAdes but the chapter's worked run is MEGAHIT, so I cannot match it to what I will see. | Show the MEGAHIT name instead, since that is the run I did. |
| Settings, "**`--assembly`.** Points the command at the assembly run folder" | Everything from here to `--format` is written for someone at a terminal, but I am still in the Settings section that promised to describe the window. I kept trying to find these in the dialog. | Move these under the command-line heading. |
| Settings, "The default is standard output, so the FASTA prints to the terminal" | "Standard output" means nothing to me. | Gloss it, or keep it in the terminal section only. |
| Reading the results, "one sequence of 16,711 bases at 44.3% GC" | I cannot judge 44.3%. I do not know what GC content near this value tells me. | Say what GC percent is for and what range is expected. |
| Reading the results, "a Derived Subset block of metadata" | "Metadata" is not glossed, and I do not know where in the app I would look at this block. | Say where to see it. |
| Reading the results, "Moving or renaming the source assembly afterwards is what makes that record harder to follow" | I read this twice. It is a warning phrased as an observation, so I was not sure whether I am being told not to do it. | State it as a direct instruction. |
| Reading the results, "'Could not resolve the enclosing Lungfish project root'" | The fix is implied but never stated. I am told what the message means but not what to do next. | Say what action fixes it. |
| What good looks like, "by roughly the length of the repeated stretch at the point where a circular molecule gets cut open" | I read this three times. I know mitochondrial DNA is circular but this clause packs the explanation of the extra 142 bases into one dependent phrase. | Split into two sentences. |
| What good looks like, "the reference record `NC_012920.1`" | I do not know what a reference record is or where that identifier comes from. | Say it is the standard human mitochondrial sequence in a public database. |
| What good looks like, "The long HG002 mitochondrial contig is 96.01% of its assembly" | I can see 96% is high, but the chapter never says what threshold counts as one contig dominating. | Give a rough cutoff. |
| What good looks like, "If coverage against the extracted contig comes back patchy" | I do not know how I would check coverage or where. This assumes I have already done a mapping run. | Point to the mapping chapter for how to check. |
| What good looks like, "the assembly probably collapsed or fragmented the genome" | "Collapsed" is jargon I do not know in this context. | Gloss or drop it. |
| What good looks like, "If annotation later shows the contig is host or vector" | "Annotation", "host", and "vector" are all unexplained, and this is the only mention of any of them. | Gloss vector at least, since it is the least guessable. |
| On the command line, "by typing commands into the Terminal application" | I have never opened Terminal and the chapter does not say how. It also never says how to install or find `lungfish-cli`. | Say where the tool comes from, or say this section needs setup covered elsewhere. |
| On the command line, "`lungfish-cli assemble --output <dir>`" | The angle brackets are not explained. I would not know whether to type them. | Say once that angle brackets mark something you replace. |
| On the command line, the first code block, "`--contig k141_1`" | This is the first time a real contig name appears. In the Procedure I was told to pick the 16,711-base contig but never told it is called `k141_1`, so I could not connect the two. | Name the contig in the Procedure section too. |
| On the command line, "printed the path ... on standard output and the line ... on standard error" | Standard output and standard error are both unexplained and the distinction means nothing to me. | Drop the distinction or gloss it. |
| On the command line, "a file whose spaces become underscores" | I had to read this twice to work out that the name shown in the app and the name on disk differ. | Say plainly that the sidebar name and the folder name differ. |
| On the command line, "`genome/sequence.fa` holds the selected contigs" | "bgzip-compressed" appears here with no explanation. | Gloss or drop it. |
| On the command line, "The **Create Bundle** button takes the `--contigs` route internally" | This says the button I was told to use in the Procedure produces worse provenance than the terminal. Since I cannot use a terminal, I do not know whether my bundle is now wrong. | Say in the Procedure section that the window records the assembler as Unknown, and whether that matters. |

The one thing I learned: extracting contigs makes a new reference from your own assembly so that later mapping and variant work aims at one sequence instead of every fragment.

The one thing I still could not do: tell which row in the real contig table is the one I am supposed to select, because the chapter gives lengths in the Procedure but names only in the terminal section.

The sentence I liked most: "Keeping the full assembly costs you nothing, since you can extract from it again as often as you like, and nothing about extracting consumes or alters the original."
