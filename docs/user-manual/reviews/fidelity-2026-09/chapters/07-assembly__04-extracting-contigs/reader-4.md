# Reader 4 report, Extracting Contigs

Persona. Undergraduate. One semester of Geneious in a course. Comfortable
with a graphical sequence viewer. Expects a dialog for every operation.
Has never opened a terminal.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| What it is, "An assembler hands you every contig" | "Assembler" is used before it is explained. I know Geneious has an assembler button but I do not know what the word means here, and the chapter names MEGAHIT and SPAdes as if I already know they are assemblers. | Gloss assembler at first use as the program that joins reads into contigs, and name MEGAHIT and SPAdes as two of them. |
| What it is, "reconstructed from the overlaps between reads" | "Reads" is never glossed in this chapter. Every other term in the paragraph got a definition, so the gap made me stop and wonder if I had missed something. | Gloss read at first use the way contig is glossed. |
| What it is, "the other two are 362 and 332 bases, together under 4% of the assembly" | I could not judge whether 362 bases is bad. In Geneious short contigs still get shown, and nobody told me a length below which a contig is junk. | Say what a fragment that short usually is, and that there is no fixed cutoff. |
| What it is, "builds a FASTA index beside it" | I do not know what an index is in this sense, and "so tools can jump to any position" did not land because I have never thought about a program reading a file. | One added clause saying the index is a small companion file listing where each sequence starts. |
| What it is, "The FASTA inside a derived bundle is left uncompressed, unlike the one inside an assembly bundle." | Read twice. It arrives with no reason attached, and I could not tell whether it is something I must act on. | Say why it matters, or move it to the command-line section. |
| What it is, "So what should you do with this?" | The question is addressed to me but the paragraph had already told me the answer, so I reread the paragraph looking for the part I missed. | Cut the question and keep the sentence after it. |
| Why you would do this, "which muddies the depth numbers you use to judge a call" | "Depth" is not glossed and I do not use depth numbers to judge anything yet. Geneious showed me a coverage graph but never asked me to judge it. | Gloss depth and say which number in LGE it is. |
| Why you would do this, "it will not open a contig in the sequence viewport" | This is the opposite of what Geneious does, where a contig is just a document I can double-click. Nothing warns me that the obvious action fails. | State plainly that double-clicking a contig row does not open it, unlike a sequence viewer you may have used. |
| Before you start, "choose File > New Project (Cmd-N)" | Fine, but the chapter never says what a project is or why extraction needs one until a failure message mentions it eight sections later. | One sentence saying a project is the folder LGE keeps everything in. |
| Before you start, "Download the folder human-mito" | I could not do this from the text on my first read. The first sentence tells me to download a folder, the next says GitHub cannot download a folder. I followed the wrong instruction before reaching the correction. | Lead with the ZIP route and drop the instruction that does not work. |
| Before you start, "Run Running SPAdes against those reads first" | The link is titled Running SPAdes but the paragraph then tells me to use the MEGAHIT run. I did not know whether that chapter covers MEGAHIT too or whether I need something else. | Say that the linked chapter covers both assemblers. |
| Before you start, "There is no `Assemblies/` folder." | I never expected one, so the denial made me hunt for where the idea came from. | Cut it, or attach it to something a reader would actually look for. |
| Procedure step 1, "with its rank, name, length in bases, GC percent, share of the assembly" | I do not know what rank means here. Rank by length? By quality? Geneious sorted by name. | Say the table is ranked by length, longest first. |
| Procedure step 1, "There is no coverage column." | Coverage is linked in the glossary list at the top and discussed later under What good looks like, so being told it is absent before being told what it is left me unsure whether I needed it. | Move this note next to the coverage discussion, or gloss coverage here. |
| Procedure step 2, "shows 'Select contigs to materialize' while nothing is chosen" | "Materialize" is app wording I have never met, and the chapter never explains it. It also does not match the button, which says Create Bundle. | Add a half-sentence saying materialize is the app's word for writing the selection out to a file. |
| Procedure step 3, "The others are BLAST Contigs, Copy FASTA, and Export FASTA" | BLAST is not glossed anywhere in the chapter and it is not in the glossary list in the front matter. I have clicked BLAST in Geneious but could not say what LGE will do or where it sends my sequence. | Gloss BLAST at first use, and say whether it searches over the internet. |
| Procedure step 3, "the first reads BLAST Contig instead" | Read twice. I had to work out that "the first" means the first of the three buttons just listed. | Name the button rather than its position. |
| Procedure step 4, "Watch the run in the Operations Panel" | This is the step where a Geneious user expects a dialog with options, and there is none. The Settings section says so, but not until after the procedure. | Say in step 3 that Create Bundle runs immediately with no dialog. |
| Procedure, after step 5, "Extract Sequence... takes a sub-range of a contig" | This sounds like exactly what "extracting contigs" should mean, so I could not tell which of the two operations this chapter is about. | One clause contrasting it with Create Bundle. |
| Procedure, after step 5, "BLAST is capped at 50 selected sequences" | A number I cannot judge and cannot reach, since my assembly has three contigs. It also arrives attached to the wrong paragraph. | Move it to wherever BLAST is actually explained. |
| Settings, "Everything it does is decided by which rows are selected and what the bundle ends up called" | The text says there are two settings, then lists ten bold entries in the same style. I counted them twice trying to find the window settings. | Put the command-line flags under their own heading. |
| Settings, "The command-line flags that have no counterpart in the window follow them, and they matter only if you read the optional section" | Read twice. I do not use a terminal, so I could not tell whether I was allowed to skip eight of the ten entries. | Say outright that a window user can stop reading after Bundle name. |
| Settings, Bundle name, "suggests something like NODE_1_length_16697_cov_121.957333" | The example is a SPAdes name but the chapter's run is MEGAHIT, whose contig is called k141_1 in the command-line section. I could not tell what my own suggestion will look like. | Show the MEGAHIT suggestion this chapter's reader will actually see. |
| Settings, `--assembly`, "that holds `assembly-result.json`, not at the `.lungfishref` bundle sitting inside it" | I do not know how to see either of these, because I have never looked inside a bundle and the sidebar shows me names not files. | Note that these are visible in Finder, not in the sidebar. |
| Settings, `--line-width`, "pass 0 for one unbroken line per record" | I cannot judge when 60 versus 80 versus 0 matters, and the sentence assumes I know what downstream means. | Gloss downstream, or say most readers should leave this alone. |
| Reading the results, "gives a bundle with one sequence of 16,711 bases at 44.3% GC" | I could not judge 44.3%. Nothing says what GC percent a human mitochondrial genome should have, so the number is only useful if I already know the answer. | Say that 44.3% is the expected value for this genome. |
| Reading the results, "Alongside it the bundle's source information records the source assembly's path" | I do not know where in the window to find the Derived Subset block or the source information. The chapter tells me what they contain but not how to look at them. | Name the panel or inspector these appear in. |
| Reading the results, "Moving or renaming the source assembly afterwards is what makes that record harder to follow." | Read twice. It warns me about something without telling me what breaks or how to fix it. | Say what the bundle shows once the source has moved. |
| What good looks like, "roughly the length of the repeated stretch at the point where a circular molecule gets cut open" | Long sentence, read three times. I know mitochondrial DNA is circular but I do not know why cutting it open would repeat anything. | Split it, and say the assembler overlaps the two ends. |
| What good looks like, "If coverage against the extracted contig comes back patchy" | Coverage is linked here but still not explained, and "patchy" is not a number I can check. I would not know where in LGE to look. | Gloss coverage inline and name the view that shows it. |
| What good looks like, "a name already in use gets a counter rather than overwriting anything" | Read twice. In Geneious a duplicate name just sits there, so I could not tell whether LGE renames my new bundle or my old one. | Say the new bundle is the one that gets the counter. |
| On the command line, "The whole procedure runs headless, meaning with no window at all, by typing commands into the Terminal application." | I have never opened Terminal and the chapter does not say how, so this section is closed to me even though it holds the only route to correct provenance. | Link to wherever the manual first explains opening Terminal. |
| On the command line, "The **Create Bundle** button takes the `--contigs` route internally, so a bundle made in the window records `Unknown`" | This is the most important thing in the chapter for me and it is buried in the optional section I was told to skip. My bundles will record the wrong assembler and I would never have found out. | Move this warning into Reading the results. |

One thing I learned. A derived bundle keeps a record of which contigs it
came from, so I can still answer that question months later.

One thing I still could not do. Get a bundle that records MEGAHIT as its
assembler, because the only route is the terminal.

The sentence I liked most. "A contig that lands near the length you
expected is the strongest cheap signal that the assembly worked."
