# Reader 4 report, Novel Virus Diagnostics

Persona. Undergraduate who used Geneious for one semester. Comfortable with a
graphical sequence viewer. Expects every operation to have a dialog. Has never
opened a terminal.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| What it is, "stitches the reads into contigs first" | The word "assembled" appears inside the contig gloss but assembly itself is never explained. In Geneious I clicked De Novo Assemble and got a consensus, so I half know it, but the chapter never says whether NVD's assembly is the same idea or who ran it. | Add one clause saying assembly is the step that overlaps reads into contigs and that NVD does it for you before the BLAST search. |
| What it is, "searches each contig against a database of known nucleotide sequences" | Which database is never named. Geneious made me choose nt or a custom database every single time, so my first question is which one this is, and whether the pipeline operator picked it. | Name the database the NVD pipeline searches, or say plainly that the pipeline operator chooses it and it is not visible in LGE. |
| What it is, "written in Snakemake" | Snakemake is glossed as a system for running multi-step analyses outside LGE, but I still cannot tell whether I could ever run it. Nothing in the chapter says I would need a terminal to do so, which is the fact that decides it for me. | State that running NVD itself requires command-line tooling and that this chapter covers only the import. |
| What it is, "it lives in a folder named 05_labkey_bundling/" | I do not know what labkey is and the folder name is the only place it appears. I read the sentence twice looking for the explanation. | Say the folder name is just the pipeline's own label and carries no meaning for the import. |
| Why you would do this, "a large comma-separated table" | Comma-separated is used before CSV appears, and CSV is never spelled out anywhere in the chapter. | Gloss CSV at first use as a plain-text table with one comma between columns. |
| Before you start, "Download the folder nvd-demo from the manual's fixtures on GitHub" | The link points at a GitHub tree page for a folder. GitHub does not offer a download button for a folder, only for single files or the whole repository. I could not perform this step. | Give a downloadable zip link, or say explicitly to download the whole repository and navigate to that path. |
| Before you start, "No plugin pack is needed" | Plugin pack is used with no explanation and never returns. In Geneious plugins were things I installed from a menu, so I do not know if LGE expects the same of me elsewhere. | Gloss plugin pack at first use, or drop the sentence and say the import needs nothing extra. |
| Step 1, "Unlike most cards in the Import Center, this one does not open a plain file panel" | The contrast is with behaviour I have not seen, since this may be my first import. The sentence only makes sense to someone who already knows the other cards. | Drop the comparison and simply say the card opens a wizard sheet that scans before writing. |
| Step 2, "select the run directory, which for the fixture is nvd-demo/results" | The Before you start section said to point at `nvd-demo/results` and Step 2 repeats it, but neither says what else is inside `nvd-demo`, so when I open the picker and see two folders I cannot tell which is which. | List the one or two folders the fixture contains so the picker is unambiguous. |
| Step 2, "the panel counts rows as it reads them" | On a 10-hit file this will flash past. I waited for a counter I never saw and thought the scan had failed. | Note that on a file this small the counting is instantaneous. |
| Step 2, "the preview reads experiment 100" | I cannot judge whether `100` is a good value or even what an experiment identifier is. It is the only number in the chapter with no interpretation attached. | Say the experiment identifier is a label the pipeline operator assigned and that any value is fine. |
| Step 3, "which is not where classification runs started inside LGE land" | I read this three times. "LGE land" reads as a typo or a missing word, and the sentence carries the most important navigational fact in the chapter. | Rewrite as a plain statement that Import Center results go to `Imports/` while runs started in LGE go to `Analyses/`. |
| Step 3, "The command line writes wherever --output-dir points" | I have never opened a terminal. This is offered as the remedy for the folder-location problem, so the fix to a problem I have is one I cannot use. | Say whether the folder can be moved in the sidebar after import, which is the fix a graphical user needs. |
| Step 4, "an outline list of contigs the right" | The sentence drops the verb and I had to reread it. Also "outline" is Geneious's word for a different thing, so I expected a tree of folders. | Restore the verb and gloss outline as the expandable table of contigs. |
| Step 4, "Six metric pills sit under that" | Pill is not a word I know as an interface element. I looked for something round and clickable. | Gloss pill at first use as a small labelled badge showing one number. |
| Step 4, "embeds the full alignment viewer over the reads that built the contig" | This is the one part of LGE closest to what I did in Geneious, and it is the part the demo cannot show. I could not tell whether it is a read pileup like Geneious's alignment view or something else. | Say in one sentence what the embedded viewer shows, since the fixture cannot demonstrate it. |
| Step 5, "hovering it explains why in a tooltip" | A disabled button with an explanatory tooltip is not a Geneious pattern, where menu items simply grey out silently. I would not have thought to hover. | Say directly that hovering a disabled control in LGE reveals the reason. |
| Step 5, "220 points tall to begin with ... down to 160 points" | I do not know what a point is here and I cannot judge whether 220 is large or small. The two numbers told me nothing. | Drop the point figures or describe the drawer's size relative to the window. |
| Step 5, "This step cannot be performed on the demo results" | The whole section is written as instructions I should follow, and only the last paragraph says I cannot. I clicked through it before finding out. | Move the warning to the top of the section. |
| Settings, "a reader arriving from the registry will look for them here" | Registry is not explained anywhere in the chapter and I do not know what it refers to. | Remove the reference or gloss registry at first use. |
| Settings, "--name. Overrides the name of the folder" | Both settings are command-line flags presented in a Settings section. As a graphical user I have no way to set either, and the section never says the wizard has no equivalent for them. | State once that these two options exist only on the command line and the wizard offers no substitute. |
| Settings, "the current working directory, which is almost never what you want" | Current working directory assumes terminal knowledge I do not have. | Gloss it, or mark the whole paragraph as command-line only. |
| Settings, "All Samples. Opens a popover" | Popover is unexplained interface vocabulary. Geneious used dropdowns and dialogs, not popovers. | Gloss popover at first use as a small panel that appears next to the button. |
| Reading the results, "Contigs are listed longest first by default, and there is no sort control" | In Geneious every column header sorts on click. Here it does not, and the text says so only as a passing clause. This is a real behaviour difference and it needs to be prominent. | Say plainly that clicking a column header does not sort, unlike most sequence viewers. |
| Reading the results, "the demo results run from 0.0, meaning too small for the pipeline to represent" | An e-value of `0.0` reading as "too small to represent" is counterintuitive, and `1e-90` is scientific notation which is never explained. | Gloss the `1e-90` notation at first use as one followed by ninety zeros in the denominator. |
| Reading the results, "Anything at or under 1e-30 is effectively certain not to be chance" | I have no idea whether the values in front of me are at or under this. The comparison of `1e-90` against `1e-30` requires knowing that the more negative exponent is smaller, which is the exact thing I would get wrong. | Say which way the comparison runs, for example that `1e-90` is far smaller than `1e-30`. |
| Reading the results, "SampleA's SARS-CoV-2 contig carries 50 mapped reads out of 1,000,000 total" | The total read count of 1,000,000 appears here for the first time and is nowhere in the summary cards, the preview, or the CLI output. I could not check it. | Say where the sample's total read count is visible in the app, or that it comes from the pipeline. |
| Reading the results, "Unique Reads ... when the pipeline did not report a separate figure the column repeats the mapped-read count" | This means the two columns on the demo look identical, and a reader checking numbers by eye would think the app is broken. The explanation is buried mid-paragraph. | Flag it where the demo numbers are first described, not only in the column gloss. |
| Reading the results, "one reads clade, the Norovirus GII row" | Clade is used as a taxonomic rank and glossed only as sitting between genus and species. I learned the ranks as a fixed ladder and clade was not on it. | Gloss clade as a named group of related organisms that does not correspond to a fixed rank. |
| Reading the results, "Extract Sequence…, Verify with BLAST…, Copy FASTA, Export FASTA…, Create Bundle…, and Run Operation… all act on the contig's own sequence" | Six commands in one sentence with no description of what any does, and Run Operation… in particular is opaque. Earlier the chapter said the demo has no contig sequences, so I cannot try them either. | Split out Run Operation… with a one-sentence description, and note these need a run that shipped contig sequences. |
| Reading the results, "Verify with BLAST…" versus Step 5's "BLAST Verify" | The right-click item and the button are named differently for what reads like the same action. I could not tell whether they are the same thing. | Say they are the same operation reached two ways, or use one name. |
| Reading the results, "shows an em dash in grey" | Em dash is typography vocabulary. I would not recognise the character by that name in a table cell. | Describe it as a short horizontal line meaning no value. |
| Reading the results, "Use Import Metadata… in the Inspector" | The Inspector is never located. I do not know whether it is a panel, a menu, or something I have to open, and no menu path or shortcut is given, unlike the Operations panel which got both. | Give the menu path or shortcut that opens the Inspector. |
| What good looks like, "with hits at least equal to contigs since every contig has at least one match" | I had to read this twice to see that hits counts matches and contigs counts sequences, so the ratio runs the way it does. | Add the ratio explicitly, for example that 4 contigs and 10 hits means about two and a half matches per contig. |
| What good looks like, "sits well below ninety percent identity" | Earlier the chapter set the interesting threshold at "the seventies or low eighties" and here it says below ninety. The two numbers disagree and I do not know which to use. | Use one threshold in both places. |
| What good looks like, "bit scores step down gently from 750 to 660" | I cannot judge whether a drop of 90 is gentle or steep, because no example of a clean drop is given. The chapter tells me the shape matters and then gives me only one shape. | Add the bit-score drop for the clean SARS-CoV-2 case as a contrast. |
| What good looks like, "a deep and evenly distributed pileup supports the contig" | Pileup is not glossed. Geneious drew stacked reads and I think this is that, but the chapter never says so, and I cannot see it on the demo anyway. | Gloss pileup at first use as the stack of reads covering a position. |
| On the command line, whole section | Every command begins `lungfish-cli`, and the chapter never says where that program is or how a graphical user would reach it. The section is unusable for me and does not say so. | Open the section by saying it requires a terminal and is optional for readers using the app. |
| On the command line, "Run against the demo results it exits with status 1" | Exit status is terminal vocabulary. I do not know what status 1 means or whether it is bad. | Say it fails with an error message rather than naming the status code. |
| Whole chapter, no Troubleshooting section | Three failure modes are described in passing, which are the preview warning, the greyed-out Run button, and the missing BAM. They are scattered across four sections and I could not find any of them again when I hit one. | Collect the three into one short troubleshooting list. |

Row count 38.

The three worst issues. The fixture cannot be downloaded from the GitHub tree
link as written, so the entire chapter cannot be followed. Step 3's "inside LGE
land" sentence garbles the most important navigational fact in the chapter and
then offers a command-line-only remedy to a graphical reader. The identity
threshold for an interesting contig is given as the seventies or low eighties in
one section and below ninety percent in another.
