# Reader 2 report, Novel Virus Diagnostics

Persona: senior, two years of wet-lab work, comfortable with library prep and PCR, has never analyzed sequencing data and has never opened a terminal.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| What it is, "reaches the same goal as a read classifier" | The chapter opens by comparing NVD to a read classifier as if I already know what one is. I have run PCR, not pipelines. The prereq link is at the top of the file, not in the prose. | Say in one clause what a read classifier is before comparing to it. |
| What it is, "stitches the reads into contigs first" | "Stitches" is doing the work of the word "assembly," which never appears here. I did not know an assembler was involved until the Length column much later. | Name the step assembly at first use. |
| What it is, "a database of known nucleotide sequences" | Which database? NCBI nt, a local copy, something the pipeline builds? I cannot tell whether my lab needs to supply one. | Name the database the NVD pipeline searches. |
| What it is, "one BLAST result on a contig carries far more evidence" | I believe it, but nothing tells me why length equals evidence. This is the central claim of the chapter and it is asserted. | Add one clause tying longer query length to less chance matching. |
| What it is, "reports the nearest relative with unwarranted confidence" | Read twice. I could not tell whether this is a flaw in classifiers generally or in a specific tool LGE ships. | Say which behaviour belongs to the classifier and which to the operator reading it. |
| What it is, "matches it at low sequence agreement" | "Sequence agreement" appears here, "percent identity" appears much later, and I did not connect them until the Reading the results section. | Use percent identity here or say the two are the same thing. |
| What it is, "written in Snakemake" | Glossed as a system for multi-step analyses, which is fine, but I still cannot tell whether I could run it, whether it needs a terminal, or who does run it. | Say plainly that running NVD is a terminal task done outside LGE. |
| What it is, "it lives in a folder named `05_labkey_bundling/`" | I have no idea what LabKey is or why a folder is named for it. I worried I needed a LabKey account. | Note that the folder name comes from the pipeline and needs nothing from you. |
| Why you would do this, "a large comma-separated table" | I know spreadsheets, not "comma-separated." CSV is used later in the same breath as the filename without ever being spelled out. | Gloss CSV at first use. |
| Why you would do this, "the rows are ranked matches rather than independent records" | Read three times. I did not understand what "independent records" meant until the next sentence rescued it. | Lead with the plain version, that several rows describe the same contig. |
| Why you would do this, "one disclosure triangle away" | I did not know what a disclosure triangle was until step 4 described clicking one. It is used here first. | Gloss it here or drop the term until step 4. |
| Why you would do this, "a checksum of the input file" | Checksum is not glossed anywhere I could find. I guessed it is a fingerprint. | Gloss checksum at first use. |
| Why you would do this, "10 BLAST hit rows across 3 samples and 4 contigs" | I could not tell whether 10 hits for 4 contigs is normal, sparse, or dense until the What good looks like section, six sections later. | Say here that most contigs carry several ranked matches. |
| Before you start, "Download the folder `nvd-demo` from the manual's fixtures on GitHub" | I could not perform this. The GitHub page for a folder has no download button for that folder, and I do not know how to clone. This is the one step that blocks the whole chapter. | Give a direct download link or a click-by-click path. |
| Before you start, "No plugin pack is needed." | "Plugin pack" arrives with no explanation. I do not know what one is or where they live in the app. | Gloss plugin pack or drop the sentence. |
| Before you start, "the BAM alignment files a full NVD run produces" | The BAM gloss says where reads landed on a reference sequence, but NVD assembles contigs rather than using a reference, so I could not reconcile the two. | Say the reads are aligned back to the assembled contigs. |
| Procedure step 1, "click the **Classification Results** tab" | I could not tell whether the Import Center opens on that tab or whether I must hunt for it among others. | Say how many tabs there are or which one opens first. |
| Procedure step 2, "which for the fixture is `nvd-demo/results`" | Earlier the chapter says to download `nvd-demo`, then the run directory is `nvd-demo/results`, then the CSV is under `05_labkey_bundling/`. Three levels, and I lost track of which one to click in the Browse panel. | Show the three-level folder tree once. |
| Procedure step 2, "the preview reads experiment `100`" | An "experiment identifier" of `100` looks like a count of something. I read it as 100 experiments at first. | Say it is a label, not a number of anything. |
| Procedure step 3, "which is not where classification runs started inside LGE land" | Read twice. "Inside LGE land" is a strange phrase and I first parsed "land" as a verb, which is in fact what it means. | Rephrase without the noun-verb ambiguity. |
| Procedure step 3, "The command line writes wherever `--output-dir` points" | The Procedure is the part I follow by clicking. A flag I cannot use appears in the middle of it and made me think I had missed a field in the sheet. | Move the command-line aside out of the click-through steps. |
| Procedure step 4, "the same four the command-line summary prints" | Same problem. I have never opened a terminal, so this cross-check is not available to me. | Note that the check is optional and terminal-only. |
| Procedure step 4, "Six metric pills sit under that" | "Pills" as a UI element was new to me. I looked for a medical reading before the screenshot caption made sense. | Say "six small labelled badges" once. |
| Procedure step 4, "RPB" | RPB appears in the pill list and in the column list before it is expanded to reads per billion in Reading the results. | Expand RPB at this first appearance. |
| Procedure step 4, "embeds the full alignment viewer over the reads that built the contig" | I do not know what the alignment viewer shows or what I am meant to look for. It is the one part of the detail pane with no description. | Add one sentence on what the viewer displays, or link the alignment chapter. |
| Procedure step 5, "220 points tall to begin with ... down to 160 points" | I do not know what a point is on screen and I cannot judge whether 220 is a lot. These numbers told me nothing. | Drop the point sizes or say roughly a fifth of the window. |
| Procedure step 5, "A taxon group heading in the By Taxon grouping is not itself a hit" | By Taxon grouping has not been introduced at this point. It is defined in Settings, which comes after. | Introduce the grouping control in step 4 before referring to it. |
| Settings, "The NVD import has no settings." | The section then lists four things that look exactly like settings. I could not work out which of them I can actually change from the window. | Split the terminal-only flags from the window controls with a clearer heading. |
| Settings, "--name" and "--output-dir" | These are typed as flags with two dashes and no explanation of what a flag is. I have never opened a terminal and cannot use either. | State once that these belong to the command line only. |
| Settings, "the current working directory, which is almost never what you want" | I do not know what a working directory is, so I cannot tell what goes wrong if I leave it. | Gloss working directory, or say the file may be hard to find afterwards. |
| Settings, "it searches best matches only" | Important and easy to miss at the end of a paragraph. I would have hunted for a virus that only appears in secondary hits and concluded it was absent. | Lead the entry with the limitation. |
| Reading the results, "The columns, left to right, are Sample, Contig, Length ..." | Fourteen columns in one run-on sentence. I lost my place twice and could not map them onto the six pills from step 4. | Break into a short table, or say which columns match the pills. |
| Reading the results, "Contigs are listed longest first by default, and there is no sort control" | I could not tell whether the missing sort control is a limitation to work around or a deliberate design. | State it as intended, since What good looks like relies on the ordering. |
| Reading the results, "A contig of 5,000 bases aligning over 400" | I can see that this is bad, but I do not know at what ratio to start worrying. 500 over 498 and 5000 over 400 are the two extremes with nothing between. | Give the fraction that marks a partial match worth chasing. |
| Reading the results, "A figure in the seventies or low eighties" | This is the one threshold I most needed, and the range between low eighties and high nineties is left undefined. That is a wide gap. | Say what the ninety-ish band means. |
| Reading the results, "the demo results run from `0.0`, meaning too small ... down to `1e-90`" | `1e-90` is scientific notation I can read, but "run from 0.0 down to 1e-90" is backwards to me, since 1e-90 is larger than 0. I read it twice and still am unsure. | Say the smallest reported value is 0.0 and the largest is 1e-90. |
| Reading the results, "Anything at or under `1e-30` is effectively certain" | A threshold arrives with no sense of the scale it sits on. I do not know if 1e-20 is borderline or hopeless. | Give one contrasting value that would be weak. |
| Reading the results, "Bit Score ... the raw strength of the alignment on a scale that does not shift with database size" | I understand the words but have no anchor. Is 750 good? The section says only to compare within a contig. | Say plainly that bit scores are comparable within a contig, not across contigs. |
| Reading the results, "a large drop from the first row to the second" | "Large" is never quantified, yet What good looks like calls a 750-to-660 step "gentle." I cannot tell where large begins. | Give an approximate drop that counts as clean. |
| Reading the results, "when the pipeline did not report a separate figure the column repeats the mapped-read count" | This means Unique Reads can silently be a duplicate rather than a real measurement, and I would have believed it. | Say how to tell a real unique count from a repeated one. |
| Reading the results, "SampleA's SARS-CoV-2 contig carries 50 mapped reads out of 1,000,000" | The RPB worked example is clear, but 50,000 per billion is a number I have no feel for. Is that abundant? | Say whether an RPB of 50,000 is high or low for wastewater. |
| Reading the results, "one reads `clade`, the Norovirus GII row" | I know species and genus from genetics. "Clade" as a rank between them was new, and the explanation is a half-clause. | Gloss clade properly at first use. |
| Reading the results, "**Extract Reads…** opens the shared extraction dialog" | "Shared extraction dialog" implies a dialog documented somewhere else, but there is no link, so I do not know where to read about it. | Link the chapter that documents the extraction dialog. |
| Reading the results, "**Create Bundle…**, and **Run Operation…**" | Eight right-click items are listed in one sentence with no description of what any of them produces. Run Operation in particular sounds consequential. | Say what Run Operation does, or link it. |
| Reading the results, "A sample with no value for a column shows an em dash in grey" | "Em dash" is typography vocabulary I had to look up to picture the screen. | Say a long dash. |
| What good looks like, "the number of libraries that went into the run" | This is the one place my wet-lab vocabulary connected, but "library" appears nowhere else in the chapter, where the word is "sample." I paused to check they were the same. | Use one of the two words throughout. |
| What good looks like, "sits well below ninety percent identity" | Ninety is a new threshold. Reading the results used high nineties and seventies to low eighties. Three different boundaries across two sections. | Use one identity threshold across the chapter. |
| What good looks like, "bit scores step down gently from 750 to 660" | This is called both ambiguous and reassuring in the same paragraph, and I had to read it three times to see that the reassurance comes from the names, not the scores. | Split into two sentences, shape first and verdict second. |
| What good looks like, "a deep and evenly distributed pileup" | "Pileup" is not glossed and I had to guess it from context. I also do not know how to judge depth by eye. | Gloss pileup and say what thin looks like. |
| What good looks like, "A contig assembled from very few reads may be an assembly artifact" | "Very few" is the whole test and it is never given a number. Mapped Reads on the demo runs 10 to 100. | Give the read count below which you would distrust a contig. |
| On the command line, "The whole procedure runs headless." | "Headless" was new to me and is not glossed. I guessed it means without the window. | Gloss headless, or say without opening the app. |
| On the command line, "`lungfish-cli nvd summary /path/to/nvd-demo/results --top 20`" | I have never opened a terminal. Nothing in this section says where to type this or that I may skip it, so I stalled here wondering if it was required. | Open the section by saying the window steps already did this work. |
| On the command line, "the TSV form's columns are `sample_id`, `qseqid`, `qlen` ..." | These names do not match the window's column names, and nothing maps `qseqid` or `pident` back to Contig and Identity %. | Map the two names for at least qseqid and pident. |
| On the command line, "it exits with status 1" | Exit status is a terminal idea I do not have. I could not tell whether status 1 is a crash or an ordinary refusal. | Say it stops with an error message rather than naming the status. |

Three closing lines.

The one thing I learned: a long contig that matches a known virus over only part of its length is the interesting result, not a failure.

The one thing I still could not do: get the demo data, because I do not know how to download a folder from GitHub, which stopped every step after Before you start.

The sentence I liked most: "SampleB's herpesvirus contig carries 100 out of 2,000,000, and its RPB is also 50,000, so those two contigs are equally abundant in their own samples despite the raw counts differing twofold."
