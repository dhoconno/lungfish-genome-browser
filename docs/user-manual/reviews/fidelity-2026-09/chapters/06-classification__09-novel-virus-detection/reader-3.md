# Reader 3 — Novel Virus Diagnostics

Reader persona: pre-med student, English is my second language. I know biology
words from my textbooks. I do not know idioms, and I have never opened a
terminal. I read the chapter from the top and wrote down everything that
stopped me.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| What it is — "reaches the same goal as a read classifier by a different road" | A road metaphor. I had to stop and decide whether "road" meant something technical. Also I do not yet know what "the same goal" is, because the chapter has not said it yet. | Say the goal plainly, for example that both methods name which organisms are in a sample. |
| What it is — "a few hundred bases long" | "Bases" is used before it is explained. I know bases from genetics class, but the letters A, C, G, T are never connected to the word here, and "150-base read" later assumes the same. | Gloss base at first use as one letter of DNA sequence. |
| What it is — "searches each contig against a database of known nucleotide sequences" | The name of the database is never given. I do not know if this is one specific database or any database, so I cannot tell what "known" means. | Name the database the pipeline searches. |
| What it is — "one BLAST result on a contig carries far more evidence than one match on a single 150-base read" | The 150 appears with no warning. Earlier the text said "a few hundred bases". I could not tell whether 150 is a rule, an example, or a contradiction. | State that 150 bases is a typical Illumina read length, or reuse the earlier phrasing. |
| What it is — "rather than a census tool" | "Census" is a government word for counting people. I read the sentence twice before I understood the comparison. | Replace with a plain phrase such as a tool for counting what is already known. |
| What it is — "it either reports the nearest relative with unwarranted confidence" | Long sentence, and "unwarranted" is a hard word for me. I understood it only on the second reading. | Split the sentence and use a simpler word such as too much confidence. |
| What it is — "at low sequence agreement" | Later the chapter calls this percent identity. Here it has a third name. I did not know they were the same thing until the Reading the results section. | Use percent identity here, or say the two names mean the same number. |
| What it is — "written in Snakemake" | The gloss says it is a system for running multi-step analyses. It does not say whether I must install it or learn it. As a person who has never opened a terminal, this frightened me. | Add one sentence saying the reader never runs Snakemake. |
| What it is — "the practice of sequencing sewage" | I understood this, but I could not tell why a chapter about sewage is in a manual where I expected human samples. The reason comes one sentence later and I had already been confused. | Put the reason for the viral example before the wastewater definition. |
| What it is — "The pipeline's output file is named `*_blast_concatenated.csv`" | The star character is never explained. I did not know if the file is literally named with a star. | Say that the star stands for any text at the start of the file name. |
| What it is — "optionally gzip-compressed" | "Gzip" is not glossed. I guessed it is a zip file but I was not sure the `.gz` is the same thing. | Gloss gzip at first use. |
| What it is — "the earlier stage folders of an NVD run do not need to be present" | I do not know what a stage folder is, because I have never seen an NVD run. I could not judge whether this sentence gives me permission to delete something. | Say briefly that an NVD run makes numbered folders and only `05_labkey_bundling/` is read. |
| What it is — "The mental model to carry into the window" | "Mental model" and "carry into" are both figures of speech. I read the sentence three times and I am still unsure whether "the window" is the app window or a metaphor. | Rewrite as a plain statement about how to think about the display. |
| Why you would do this — "rather than the log files" | Log files are never explained, and they are not part of anything I do in this chapter. | Drop the mention or gloss it. |
| Why you would do this — "a table of that shape" | "Of that shape" is vague. I did not know if shape means size, or number of columns, or something else. | Say what property of the table causes the problem. |
| Why you would do this — "one disclosure triangle away" | I did not know what a disclosure triangle is until step 4 explained it. Here it is used with no gloss and inside an idiom. | Gloss disclosure triangle at this first use. |
| Why you would do this — "and that comparison is the thing a flat table hides" | "Flat table" is not defined. It sounds like the opposite of something, but I do not know of what. | Define flat table, or say a table with no grouping. |
| Why you would do this — "a checksum of the input file" | "Checksum" is not glossed and has no glossary link, unlike provenance which does. I do not know what it is or why it proves anything. | Gloss checksum at first use. |
| Why you would do this — "a small synthetic run holding 10 BLAST hit rows across 3 samples and 4 contigs" | I could not judge whether 10 rows is small or large for a real run. Nothing tells me what a real run holds. | Give a typical real-run size for comparison. |
| Why you would do this — "checked by eye" | An idiom. I understood it eventually but it slowed me. | Say checked by hand against the source file. |
| Before you start — "Download the folder `nvd-demo` ... on GitHub" | I could not perform this step. The link goes to a folder page. GitHub does not have a download button for a folder, and the chapter does not say how to get one. This is where I stopped being able to follow along. | Give the exact download step, or link a zip file. |
| Before you start — "The folder you point the importer at is `nvd-demo/results`" | I could not tell whether `results` is inside the folder I downloaded or beside it. The GitHub link ends at `nvd-demo`. | Show the folder layout as a small tree. |
| Before you start — "No plugin pack is needed" | "Plugin pack" is not explained here. I do not know what one is or why it would be needed, so the reassurance means nothing to me. | Add a short gloss or a link. |
| Before you start — "finishes in under a second on a run this size" | I cannot judge this because I do not know what a normal size is. Also it does not tell me how long a real run takes. | Give the timing for a real run too. |
| Before you start — "this chapter says plainly where that boundary falls" | "Where the boundary falls" is a metaphor and the sentence talks about the chapter itself, which confused me about who is speaking. | Say directly that step 5 and the extraction command cannot be run on the demo. |
| Procedure 1 — "Choose **File > Import Center...**" | The three dots after the name are never explained. In my experience with menus I did not know if I must type them. | Say once that three dots mean the item opens a dialog. |
| Procedure 1 — "a wizard sheet titled NVD Import" | "Wizard sheet" is app vocabulary I do not have. I did not know if it is a window, a panel, or a page. | Gloss sheet at first use. |
| Procedure 2 — "The path readout to the left of the button is a display of the chosen path and is not editable" | I read this twice. The sentence tells me what it is not, which made me look for a place to type that does not exist. | State positively that the path is shown for confirmation only. |
| Procedure 2 — "the preview reads experiment `100`" | I do not know what an experiment identifier is or where the number 100 comes from. Later the Settings section says it comes from the CSV's first column, which is too late. | Explain the experiment identifier at first use. |
| Procedure 2 — "While the scan runs the panel counts rows as it reads them" | I could not perform this observation. On a file this small the scan is under a second, so I would never see the counting. | Say the counting is visible only on large runs. |
| Procedure 3 — "writes them into a small database inside the new result folder" | I do not know what a database file means here or whether I ever need to open it. This made me worry I need extra software. | Add that the database is internal and never opened by hand. |
| Procedure 3 — "which is not where classification runs started inside LGE land" | I read this sentence three times. "Started inside LGE land" is an idiom and "LGE land" is not a place I know. The sentence is also long and has three clauses. | Split into two short sentences and remove the idiom. |
| Procedure 3 — "The command line writes wherever `--output-dir` points" | I have never opened a terminal. This sentence appears in the middle of a mouse procedure and I could not tell if I was supposed to do something. | Move the command-line note out of the numbered step. |
| Procedure 4 — "which are the same four the command-line summary prints" | Again the command line appears without warning in a step I am doing with the mouse. | Move to the On the command line section. |
| Procedure 4 — "contig `NODE_2_length_300_cov_5.0`" | These names are used as if I can read them. I guessed length 300 and cov 5.0, but "cov" is never explained and I do not know if it matters. | Explain the contig-name parts once, including cov. |
| Procedure 4 — "opens to reveal four further HIV-1 matches under its best one" | I counted five matches in the What good looks like section for this same contig, and four here. I had to work out that four plus the best one equals five. | State the total once so the two sections agree in wording. |
| Procedure 4 — "Six metric pills sit under that" | "Pills" is app jargon and a metaphor. I pictured medicine. | Say six small labelled boxes. |
| Procedure 4 — "RPB" | The abbreviation appears with no expansion here. It is expanded only much later in Reading the results. | Expand reads per billion at this first use. |
| Procedure 4 — "embeds the full alignment viewer over the reads that built the contig" | I do not know what the alignment viewer is or what I would see. "Over the reads" is unclear grammar to me. | Say in one sentence what the viewer shows. |
| Procedure 5 — "The button is deliberately fussy about selection" | "Fussy" is an idiom about a person. I had to read the next sentence to understand. | Say the button requires exactly one row. |
| Procedure 5 — "220 points tall to begin with ... down to 160 points" | I do not know what a point is as a unit, and I cannot judge whether 220 is big or small. | Say the drawer opens at roughly one fifth of the window, or drop the numbers. |
| Procedure 5 — "A taxon group heading in the By Taxon grouping is not itself a hit" | By Taxon grouping is not introduced until the Settings section. I did not know what it was at this point. | Introduce By Sample and By Taxon in step 4 before referring to them. |
| Settings — "**--name.**" and "**--output-dir.**" | These two settings are written as command-line flags with two dashes, in a section a mouse user reads. I did not know if I could set them in the app. | Say at the top of each that the setting exists only on the command line. |
| Settings — "the current working directory, which is almost never what you want" | "Current working directory" is a terminal concept I do not have. | Gloss it or remove it for the app reader. |
| Settings — "Search contigs…." | The description says it searches "best matches only, so a term appearing solely in a secondary hit will not surface its contig". "Surface" as a verb is unusual for me and the sentence is long and negative. | Rewrite as a short positive statement plus one short limitation. |
| Reading the results — "The columns, left to right, are Sample, Contig, Length ..." | Fourteen column names in one sentence. I lost my place twice reading it. | Present the columns as a table or list. |
| Reading the results — "Contigs are listed longest first by default, and there is no sort control" | I could not tell whether this is a limitation I should work around or simply how it is. | Say plainly that the order cannot be changed. |
| Reading the results — "aligns over 498 of them, which is a match covering essentially the whole contig" | I can see 498 out of 500 is nearly all. But I cannot judge the threshold. Is 450 out of 500 still good? | Give the rough cut-off for a good coverage fraction. |
| Reading the results — "A figure in the seventies or low eighties says the contig is related to something known" | The numbers 99.5, 96.0, 99.0, 97.5 are all in the nineties, and then the advice jumps to the seventies. Nothing tells me how to judge the gap between, for example 88 percent. | Describe the middle range as well. |
| Reading the results — "from `0.0`, meaning too small for the pipeline to represent, down to `1e-90`" | I could not follow the direction. It says "from 0.0 down to 1e-90", but 1e-90 is larger than 0. This is backwards to me and I read it three times. | Say the range runs from 1e-90 up to 0.0, or explain that 0.0 means smaller than any number shown. |
| Reading the results — "`1e-90`" | Scientific notation with the letter e is never explained. I know 10 to the power of minus 90 from chemistry, but not this way of writing it. | Explain the e notation once. |
| Reading the results — "Anything at or under `1e-30` is effectively certain not to be chance" | I cannot judge this because the previous sentence said 1e-90 is the smallest value in the demo, so every demo value passes. The rule never gets tested on an example. | Give one example of an e-value that fails the test. |
| Reading the results — "so on contigs the e-value rarely does much work. It earns its place on short contigs" | Two idioms in a row, "does much work" and "earns its place", both about a number as if it were a worker. | Say the e-value matters mainly for short contigs. |
| Reading the results — "a scale that does not shift with database size" | I do not know why database size would change a score, so I cannot appreciate why this is good. | Add a half sentence saying that a bigger database makes chance matches more likely. |
| Reading the results — "a large drop from the first row to the second means the call is clean" | I could not judge "large". Later the chapter calls a drop from 750 to 660 gentle. So 90 is small. But I still have no number for large. | Give a rough number or ratio for a large drop. |
| Reading the results — "**RPB** ... divided by the sample's total read count and multiplied by a billion" | The worked example uses 50 reads out of 1,000,000 to get 50,000. I did the arithmetic and it is right, but the phrase "puts contigs from libraries of different sizes on one scale" uses "library", which is never glossed in this chapter. | Gloss library, or say total reads in the sample. |
| Reading the results — "despite the raw counts differing twofold" | "Twofold" and "raw counts" both slowed me. I understood only after checking 100 against 50. | Say although one has twice as many reads. |
| Reading the results — "one reads `clade`, the Norovirus GII row, because that name sits at a level between genus and species" | "Clade" is a real biology word I know, but the claim that it sits between genus and species is not how my textbook defines it, and there is no glossary link. | Link clade or explain what the pipeline means here. |
| Reading the results — "A broad rank is not a defect" | Understood, but the paragraph never tells me what to do differently with a broad rank. | Say what action a broad rank should change. |
| Reading the results — "**Extract Sequence…**, **Verify with BLAST…**, **Copy FASTA**, **Export FASTA…**, **Create Bundle…**, and **Run Operation…** all act on the contig's own sequence" | Six menu items are named and none is explained. I do not know what any of them do, and Create Bundle and Run Operation sound important. | Add one short clause per item, or point to where each is described. |
| Reading the results — "Verify with BLAST…" | Step 5 calls the same action "BLAST Verify". Here it is "Verify with BLAST". I could not tell if these are two different things. | Use one name, or say the two labels are the same action. |
| Reading the results — "The information button at the right end" | I do not know what the information button looks like. Nothing describes the icon. | Describe the icon, for example a lowercase i in a circle. |
| Reading the results — "shows an em dash in grey rather than dropping the column" | "Em dash" is a typography word I do not know, and the manual elsewhere avoids em dashes. | Say shows a dash. |
| What good looks like — "with hits at least equal to contigs since every contig has at least one match" | I had to read this twice because of the double "at least". | Split into two sentences. |
| What good looks like — "sits well below ninety percent identity" | Earlier the advice was the seventies or low eighties. Now it is below ninety. The two numbers disagree and I do not know which to use. | Use one threshold in both places. |
| What good looks like — "the shape to worry about is the same gentle step down across matches naming different organisms" | Long sentence with a metaphor about shapes, and it is the most important rule in the section. I read it three times. | Split into two short sentences. |
| What good looks like — "A contig assembled from very few reads may be an assembly artifact" | "Very few" gives me no number. The demo has contigs with 10, 20, 50, and 100 mapped reads and I cannot tell which of those is too few. | Give a rough minimum read count. |
| What good looks like — "a deep and evenly distributed pileup supports the contig and a thin or lopsided stack does not" | "Pileup", "lopsided", and "stack" are all new to me in one sentence, and pileup is not glossed. | Gloss pileup and replace lopsided. |
| On the command line — "The whole procedure runs headless." | "Headless" is not explained. I guessed it means without the app window, but only from context. | Gloss headless, or say without opening the app. |
| On the command line — "lungfish-cli nvd summary /path/to/nvd-demo/results --top 20" | I have never opened a terminal. Nothing tells me where to type this, what `lungfish-cli` is, or how to replace `/path/to/`. I could not perform any command in this section. | Add one sentence pointing to the chapter that explains how to run commands. |
| On the command line — "`--format tsv` or `--format json`" | TSV and JSON are never explained. The chapter glosses FASTQ and BAM but not these. | Gloss both, or say machine-readable file formats. |
| On the command line — "the TSV form's columns are `sample_id`, `qseqid`, `qlen`, `adjusted_taxid_name` ..." | These names do not match the column names I just learned in the viewport. I could not map `qseqid` to Contig or `pident` to Identity %. | Show the mapping to the viewport column names. |
| On the command line — "so the two produce byte-identical output for the same selection" | "Byte-identical" is a computing term I do not know. | Say exactly the same file. |
| On the command line — "it exits with status 1" | "Exit status 1" means nothing to me. | Say the command reports an error and stops. |
| Throughout — screenshot placeholders such as "SHOT: nvd-import-card" | Five places in the chapter show only a comment where a picture should be. As a visual learner following an unfamiliar app, the missing pictures are exactly where I needed them, especially at the column menu and the drawer. | Add the screenshots before this chapter is published. |

The one thing I learned: RPB lets me compare two contigs from samples that were
sequenced to different depths, because 50 reads out of one million and 100 reads
out of two million give the same number.

The one thing I still could not do: download the demo folder from GitHub, so I
could not follow a single step of the procedure on real data.

The sentence I liked most: "A contig assembled from very few reads may be an
assembly artifact rather than a real sequence, and no BLAST number can tell you
that."
