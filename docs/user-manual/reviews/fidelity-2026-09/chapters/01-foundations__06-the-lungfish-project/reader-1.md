# Reader report, reader 1 (sophomore, genetics course, no lab time)

Chapter: `01-foundations/06-the-lungfish-project.md`

| Location | What stopped me | What would have helped |
|---|---|---|
| What it is, "A Lungfish Genome Explorer (LGE) project keeps..." | "provenance" is used before it is explained. The link goes somewhere else and I do not want to leave the page on sentence one. | Gloss it in the sentence, one clause saying it is the record of where a file came from. |
| What it is, "Double-click it and the app..." | I did not know what "Show Package Contents" was or that a folder could pretend to be a file. I read the paragraph twice. | Say first that macOS lets a folder be displayed as one file, then give the right-click step. |
| What it is, "What you see there is an ordinary set..." | "the project is a folder that has been given a costume" is cute but I could not tell if it was literal. Is it a folder or not? | State it plainly, it is a folder and Finder displays it as one item. |
| What it is, "`.project.db` is a SQLite database..." | I do not know what a "sequence catalog" is or what "version history" is versioning. | Say what the catalog lists and what the history records. |
| What it is, "its format version, and the dates..." | "format version" was not defined. Later the chapter says "schema version". I could not tell if these are the same thing. | Use one term, or say the two are the same. |
| What it is, "A small number of analyses lean on reference databases tens of gigabytes in size." | I have no sense of what these databases are or which analyses need them. Tens of gigabytes sounds alarming and I could not judge if my laptop is fine. | Name one example database and say the disk requirement is checked for you. |
| What it is, "the same plugin packs, and the same shared databases" | "plugin packs" appears here for the first time with no gloss. It is glossed two sections later in Before you start. | Gloss it here at first use. |
| What it is, "such as a sequence track, an alignment, a variants table" | I have taken genetics and I still do not know what a "sequence track" is, nor a "variants table" as a thing on a screen. | One short parenthetical for each, or a pointer to the chapter that shows them. |
| Why you would do this, "Reads land under `Imports/`" | "Reads" is used as a bare noun. I know reads are sequencing output but I did not know if a read is one file or many. | Say a read file, or gloss reads once as the sequence fragments a sequencer produces. |
| Why you would do this, "a checksum of the bytes" | I do not know what a checksum is or why it matters. | Gloss it, a short fingerprint that proves the file did not change. |
| Why you would do this, "an assembly, an alignment with its tree, and a classification" | Four analysis words in one clause, none glossed. I could not picture the demo project. | Cut the list or gloss each in two or three words. |
| Before you start, "Build it by following the instructions in the manual's fixtures on GitHub" | I could not perform this. The link goes to a folder listing and I do not know what to do when I get there. Also I have never opened a terminal and I suspect this needs one. | Say explicitly whether this needs a terminal, and give the one file to open. |
| Before you start, "Docker Desktop is a separate free application" | I do not know what Docker is or why an analysis app would need it. | One clause saying what it does for LGE. |
| Before you start, "Only the app creates the project store" | I could not tell whether "project store" is the same as the `.project.db` file. The sentence says "which is" so probably yes, but then I met "store" alone in What good looks like and hesitated again. | Pick one name and use it every time. |
| Before you start, "`lungfish-cli` never does." | The CLI was mentioned once in What it is and I was told the chapter stays in the window. Then a whole paragraph and a whole section are about it. | Say up front that the CLI appears only as an optional aside. |
| Procedure step 2, "whether the Required Setup pack is installed" | I did not know if I have to click Install before I can continue, or if I can skip it. The paragraph says opening waits during an install, which sounds like I must. | Say plainly whether the reader should click Install now. |
| Procedure step 2, "moves the shared tool and database root somewhere with more room" | "root" as a noun was unfamiliar. I thought it meant a root directory of the disk. | Say the folder where LGE keeps its shared tools and databases. |
| Procedure step 3, "The wording differs between the two surfaces and the actions do not." | I had to read this twice to work out that "surfaces" meant the Welcome window and the menu bar. | Name the two things instead of calling them surfaces. |
| Procedure step 5, "for a full-width look at a coverage track" | I do not know what a coverage track is. It is the second example I could not picture. | Gloss or drop the example. |
| Procedure step 5, "Bring back any pane that is missing, then open" | Five shortcuts in one step, two of them three-key and four-key chords. I could not hold them and I could not tell which ones I actually need now. | Move the rarely used two out of the step. |
| A tour of the sidebar, "The sidebar is the canonical view of the project." | "canonical" stopped me. I know it from a genetics context, canonical sequence, and it does not mean the same thing here. | Say the sidebar is the authoritative view. |
| Sidebar table, `Primer Schemes/` row | "Amplicon primer-scheme bundles" is three unfamiliar words together. I know primers from PCR but not what a scheme or an amplicon bundle is. | Gloss amplicon and primer scheme in the row or before the table. |
| Sidebar table, `Haplotype Definitions/` row | "MHC genotyping chapters" assumes I know MHC. I met it in lecture but not as a data type. | Say what a haplotype definition file holds. |
| A tour of the sidebar, "LGE prepends it to the top of the tree" | "prepends" is programmer vocabulary. Also I could not follow "assembled from the project rather than read straight off the folder it points at" and read it three times. | Say LGE builds the group from the project's results rather than showing one folder. |
| A tour of the sidebar, "De novo assemblies are results, so they" | "De novo" was not glossed anywhere in the chapter. | Gloss it, built from the reads with no reference. |
| What "bundle" means, "a `genome/` folder with the bgzip-compressed FASTA" | "indexes" plural was never explained. I do not know what an index of a FASTA is or why there are several. | One sentence on what an index does. |
| What "bundle" means, "Bgzip is a block-compressed form of gzip" | "seek into" is not everyday English. I guessed it means jump to the middle. | Say jump straight to one part without unpacking the rest. |
| Sharing a project, "A project can be opened by more" | I did not know what happens to me if I hit a lock. The message is named later but I did not know if I would lose work. | Say the second person is only prevented from writing, not from opening. |
| Sharing a project, "The record names the user, the host" | "the process" as a thing that can be named confused me. | Drop it, or say the running copy of the app. |
| Sharing a project, "On the command line, `lungfish-cli project lock`" | I could not perform this. `--mode` records what kind of what, and what values are allowed? The code block later shows `--mode exclusive` with no list of alternatives. | List the allowed modes, or say exclusive is the only one you need. |
| Sharing a project, "It scans the bundles inside the project" | "transformer" was opaque and "legacy schema" is jargon on jargon. | Say it reports an old format it cannot safely convert. |
| Saving and exporting, "Project changes are stored when an" | I could not tell whether anything I do is at risk. There is no Save command, which made me nervous, and the reassurance arrives three sentences later. | Lead with the reassurance, LGE saves for you, then explain. |
| Saving and exporting, "Editing tools may ask you to" | "draft" was undefined and I could not tell which tools these are. | Name one editing tool that does this. |
| Saving and exporting, "Sequence and annotation exports use an explicit" | I read this three times. I could not work out what the rule is or what I should click. | State the rule as a step, select the item first, otherwise LGE exports what is on screen. |
| Searching the project, "Type into it and LGE searches" | "background index" was new and I did not know if it needs to finish building before search works. | Say the index is built for you and search works immediately. |
| Searching the project, "A Scope selector narrows the search" | Six of these seven are tool names or formats I have never seen. I could not choose one. | Say these are named after the tools that produced the data, and point to the chapter that introduces them. |
| Searching the project, "Below it, fields filter by Keywords" | I do not know how to judge Min Unique Reads or Min and Max Total Reads. What is a reasonable minimum? | Give one worked value, or say leave them blank until a later chapter. |
| Searching the project, "A \"High-confidence pathogens only\" checkbox restricts results" | I do not know what flags a pathogen as high confidence or who decides. | Say what the flag is based on. |
| The Inspector, "Select the `HG002` paired-end FASTQ bundle" | "paired-end" was not glossed. I have heard it but could not say what the pairing is. | Gloss it once, two reads from opposite ends of the same fragment. |
| The Inspector, "Select the `HG002` paired-end FASTQ bundle" (quality) | I do not know what a per-base quality is or what a good one looks like. This is a number I cannot judge. | Say what the number measures and what range is normal. |
| The Inspector, "Select an alignment track inside a" | Three numbers, none of which I can judge. Mean coverage especially, I have no idea if 5 or 500 is good. | One sentence giving a typical range. |
| The Inspector, "Click a row in the Variants" | `INFO` and `FORMAT` look quoted from a file format I have never seen. I could not tell if they are LGE features or something external. | Say they come from the VCF file format and point to that chapter. |
| The Inspector, "Click a row in the Variants" (strand) | "strand" here is not the DNA-strand idea I learned, or if it is I could not see why it splits the count. | One clause on why the two strands are counted separately. |
| The Inspector, "Variants live in the table drawer" | "table drawer" appears with no introduction and no earlier mention. I did not know where to look for it. | Introduce the drawer, or say which chapter opens it. |
| The Operations Panel, "Click the disclosure triangle to expand it" | I did not know why the app would build a command, since I am clicking buttons. | One clause saying LGE runs command-line tools underneath. |
| When things go wrong, "The menu is assembled from what" | I could not predict what I would see when I right-click, so I did not know if a missing item meant a problem. | Say a missing item just means that row does not support it. |
| When things go wrong, "Cancellation is cooperative, so the tool" | "cooperative" as a technical term stopped me. I read it as the app being polite. | Say the tool is asked to stop and may take a moment. |
| What good looks like, "Confirm the window title carries the" | Two very different causes in one clause, and I could not tell how to distinguish them. | Say how to tell which one it is. |
| On the command line, "This section is optional." | I have never opened a terminal, so I could not perform any of it, but the section also holds the only description of `--mode exclusive`. | Say what a reader who never uses a terminal loses by skipping it. |
| On the command line, code block, `lungfish-cli import fasta ~/Downloads/chr20.fasta` | The first command uses `--output-dir` and the second uses `--project` for the same project path. I could not tell whether that difference is real. | Say why the two flags differ, or use one. |
| On the command line, "The CLI rejects a `--project` path" | I could not tell if this matters to me in the app, or only on the command line. | Say the app never lets you get this wrong. |

One thing I learned: the folder a file sits in is itself a record of where the file came from, so `Downloads/` versus `Imports/` is information and not just tidiness.

One thing I still could not do: build the demo project, because the instructions point at a GitHub folder and I could not tell whether following them requires a terminal I have never used.

The sentence I liked most: "A bundle under `Reference Sequences/` was published by somebody else and a bundle under `Analyses/` was built here."
