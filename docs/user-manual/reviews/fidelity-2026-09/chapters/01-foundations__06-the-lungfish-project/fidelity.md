# Fidelity review: 01-foundations/06-the-lungfish-project

Chapter reviewed: `docs/user-manual/chapters/01-foundations/06-the-lungfish-project.md`

Ground truth, in campaign order, was the Swift source under `Sources/`, the
`cli-help/` dumps taken from `.build/debug/lungfish-cli`, and the demo project
on disk at `~/Desktop/lge-docs/LGE Manual Demo.lungfish` (inspected read only
with `ls -la`). The chapter has already absorbed the DRIFT Part A corrections,
so most of the rows below confirm the rewrite rather than re-report the old
drift. Six new findings are recorded, five of them in the Inspector and
`Analyses/` prose that DRIFT never checked against the demo project.

`LUNGFISH_MANUAL_STRICT=1 bash docs/user-manual/build/scripts/lint-chapter.sh`
reports no issues.

## Claims

| # | Claim | Verdict | Evidence | Corrected wording |
|---|---|---|---|---|
| 1 | Front matter entry point "File > New Project (Cmd-N)" | true | `Sources/LungfishApp/App/MainMenu.swift:168-172` title "New Project", keyEquivalent "n" | |
| 2 | Front matter entry point "File > Open Project Folder... (Cmd-O)" | true | `Sources/LungfishApp/App/MainMenu.swift:175-179` title "Open Project Folder...", keyEquivalent "o" | |
| 3 | Front matter entry point "View > Show Sidebar (Ctrl-Cmd-S)" | true | `Sources/LungfishApp/App/MainMenu.swift:437-442` keyEquivalent "s" with `keyEquivalentModifierMask = [.command, .control]` | |
| 4 | Front matter entry point "View > Show Inspector (Cmd-Opt-I)" | true | `Sources/LungfishApp/App/MainMenu.swift:447-453` keyEquivalent "i" with `[.command, .option]` | |
| 5 | Front matter entry point "Operations > Show Operations Panel (Cmd-Shift-P)" | true | `Sources/LungfishApp/App/MainMenu.swift:866-872` keyEquivalent "p" with `[.command, .shift]` | |
| 6 | "a `.lungfish` project bundle, a directory Finder shows as one item" | true | `Sources/LungfishCore/Storage/ProjectFile.swift:98` `fileExtension = "lungfish"`; the demo project on disk is a directory named `LGE Manual Demo.lungfish` | |
| 7 | "`.project.db` is a SQLite database ... it holds the project's sequence catalog and its version history" | true | `Sources/LungfishCore/Storage/ProjectFile.swift:15,27` "Hidden SQLite database with sequences, versions, annotations"; `ls -la` on the demo project shows `.project.db` at the root | |
| 8 | "`metadata.json` holds the project's own name, its format version, and the dates it was created and last changed" | true | `Sources/LungfishCore/Storage/ProjectFile.swift:16,392-399` reads and writes `metadata.json`; the demo project carries a 190-byte `metadata.json` at the root | |
| 9 | "two files sit hidden at the root" | changed | Only `.project.db` is hidden. `metadata.json` has no leading dot and is visible in Finder. The demo project's `ls -la` shows `metadata.json` alongside `Analyses`, `Imports`, and the other visible folders | "Inside that bundle sit two files you never edit. `.project.db` is hidden and `metadata.json` sits in plain sight beside the folders" |
| 10 | "A small number of analyses lean on reference databases tens of gigabytes in size. LGE installs those once and shares them across every project on the machine" | true | `Sources/LungfishCore/Storage/ManagedStorageConfigStore.swift:87` roots managed storage in a machine-wide directory outside any project, and the Welcome window's storage chooser moves that root | |
| 11 | "the sidebar can show a stored sequence that has no separate FASTA file of its own in Finder" | true | `ProjectFile.swift:15` stores sequences in `.project.db` rather than as loose files, and `Sources/LungfishApp/Views/Sidebar/SidebarProjectScanner.swift` renders store-backed entries | |
| 12 | "The Inspector runs down the right and holds metadata and actions for the current selection" | true | `Sources/LungfishApp/Views/Inspector/InspectorViewController.swift` is the right pane, and its sections switch on selection | |
| 13 | "A fourth surface, the Operations Panel, opens in a window of its own from the Operations menu" | true | `Sources/LungfishApp/Views/Operations/OperationsPanelController.swift:15,18` is an `NSWindowController` building its own `NSWindow` | |
| 14 | "LGE also ships a command-line tool, `lungfish-cli`" | true | Every banner in `cli-help/` reads `USAGE: lungfish-cli ...`, for example `cli-help/project.txt:7` | |
| 15 | "Reads land under `Imports/`, references under `Reference Sequences/`, results under `Analyses/`" | true | `Sources/LungfishIO/Bundles/ReferenceSequenceFolder.swift:20`; `Sources/LungfishIO/Bundles/AnalysesFolder.swift:18`; the demo project holds all three | |
| 16 | "A file under `Downloads/` came from a public archive, and it arrived with a provenance sidecar, a small JSON file naming the source URL, the accession, the time of the fetch, and a checksum" | true | `Sources/LungfishApp/Views/Sidebar/SidebarViewController.swift:1949-1958` creates the `Downloads` folder for fetched documents; `Sources/LungfishApp/Views/Inspector/Sections/ProvenanceSection.swift:348` prints `sha256 <checksum>` | |
| 17 | "This chapter uses the demo project ... It already holds imported reads, two reference bundles, a mapping with two variant tracks, an assembly, an alignment with its tree, and a classification" | true | `ls -la` on the demo project shows three FASTQ bundles under `Imports/`, `HBB.lungfishref` and `chr20_10.0-10.5Mb.lungfishref` under `Reference Sequences/`, and `mapping-HG002`, `HG002-chrM`, `Multiple Sequence Alignments`, and `kraken2-SRR36291587` under `Analyses/`; `docs/user-manual/fixtures/demo-project/README.md` names the two variant tracks | |
| 18 | "which create it at `~/Desktop/lge-docs/LGE Manual Demo.lungfish`" | true | `docs/user-manual/fixtures/demo-project/README.md` defaults `LUNGFISH_DEMO_ROOT` to `$HOME/Desktop/lge-docs`, and the project is there | |
| 19 | "fill it in about two minutes" | true | `docs/user-manual/fixtures/demo-project/README.md` measures a full build at about a hundred seconds | |
| 20 | "Only the app creates the project store ... `lungfish-cli` never does" | true | `docs/user-manual/fixtures/demo-project/README.md` records that the build script "checks for a project store at `LGE Manual Demo.lungfish/.project.db` and exits with status 2" because "It never creates the project directory itself"; no `LungfishCLI` command creates a store | |
| 21 | "the app opens it as a read-only view of the files with '(Read Only)' appended to the window title" | true | `Sources/LungfishApp/App/AppDelegate.swift:609` `let accessSuffix = isNativeProject ? " (Read Only)" : ""` on the filesystem-fallback path, and line 673 for the read-only-recommended path | |
| 22 | "the command line can then fill it with reads, references, and results" | true | The demo project was filled entirely by `lungfish-cli` after the app created the store (`fixtures/demo-project/README.md`) | |
| 23 | "It offers the Create Project and Open Project cards" | true | `Sources/LungfishApp/Views/Welcome/WelcomeWindowController.swift:626-627` `case createProject = "Create Project"` and `case openProject = "Open Project"` | |
| 24 | "a Recent Projects list holding your last ten projects" | true | `Sources/LungfishApp/Views/Welcome/WelcomeWindowController.swift:657` `case recentProjects = "Recent Projects"`, and line 26 `private let maxRecentProjects = 10` | |
| 25 | "It reports whether the Required Setup pack is installed, with one status card per tool behind the Show Details button" | true | `Sources/LungfishApp/Views/Welcome/WelcomeWindowController.swift:1285-1291` builds the `showingDetails ? "Hide Details" : "Show Details"` toggle beside the status pill | |
| 26 | "Its Install button runs the setup" | true | `Sources/LungfishApp/Views/Welcome/WelcomeWindowController.swift:1271-1280` renders `presentation.primaryActionTitle` calling `onInstall()`, labelled "Install required setup" for accessibility | |
| 27 | "a 'Need more space? Choose another storage location…' link opens a sheet that moves the shared tool and database root" | true | `Sources/LungfishApp/Views/Welcome/WelcomeWindowController.swift:1294` is that exact button title, and line 568 applies the selection with "Existing managed databases can take a while to copy" | |
| 28 | "Opening a project waits while an installation or a storage change is in progress" | true | `Sources/LungfishApp/Views/Welcome/WelcomeWindowController.swift:884` "Wait for the current installation or storage change to finish before opening a project" | |
| 29 | "**File > New Project** (Cmd-N) and **File > Open Project Folder...** (Cmd-O) do the same work from an open window. The wording differs between the two surfaces and the actions do not" | true | Menu titles at `MainMenu.swift:168-179` against the card titles at `WelcomeWindowController.swift:626-627` | |
| 30 | "**View > Focus Viewer** (Cmd-Opt-F) hides both at once ... and **View > Restore Side Panes** (Ctrl-Cmd-Opt-F) brings them back" | true | `Sources/LungfishApp/App/MainMenu.swift:455-468`: Focus Viewer on "f" with `[.command, .option]`, Restore Side Panes on "f" with `[.command, .control, .option]` | |
| 31 | "The same list appears inside an open project as the **File > Open Recent** submenu" | true | `Sources/LungfishApp/App/MainMenu.swift:182-187` builds the "Open Recent" submenu and `populateOpenRecentMenu` fills it from the same recents store | |
| 32 | Sidebar table row "`Imports/` Anything you brought in from your own disk" | true | `Sources/LungfishWorkflow/Ingestion/FASTQBatchImporter.swift:435` writes there; the demo project's three imported samples are under `Imports/` | |
| 33 | Sidebar table row "`Downloads/` Anything LGE fetched from the internet" | true | `Sources/LungfishApp/Views/Sidebar/SidebarViewController.swift:1949-1958` | |
| 34 | Sidebar table row "`Reference Sequences/` Reference bundles, each carrying the extension `.lungfishref`" | true | `Sources/LungfishIO/Bundles/ReferenceSequenceFolder.swift:20`; the demo project holds two `.lungfishref` bundles there | |
| 35 | Sidebar table row "`Primer Schemes/` Amplicon primer-scheme bundles carrying the extension `.lungfishprimers`" | true | `Sources/LungfishIO/Bundles/PrimerSchemesFolder.swift:19` `folderName = "Primer Schemes"` | |
| 36 | Sidebar table row "`Extractions/` Reads and reference regions pulled out into new bundles by an extraction operation" | true | `Sources/LungfishApp/Views/Viewer/ViewerViewController+Extraction.swift:575` appends `"Extractions"` to the project URL | |
| 37 | Sidebar table row "`Haplotype Definitions/` Shareable haplotype definition files" | true | `Sources/LungfishIO/Bundles/HaplotypeDefinitionStore.swift:18` `folderName = "Haplotype Definitions"` | |
| 38 | Sidebar table row "`Analyses/` Every analysis result, each in its own subfolder named `<tool>-<timestamp>` with its own provenance sidecar" | changed | The naming convention is right (`Sources/LungfishIO/Bundles/AnalysesFolder.swift:140` `"\(tool)-\(timestamp)"`), but the per-subfolder provenance sidecar is not universal. In the demo project `Analyses/kraken2-SRR36291587/`, `Analyses/nvd-demo/`, and `Analyses/HG002-chrM/` carry no provenance sidecar at all, and `Analyses/mapping-HG002/` carries `mapping-provenance.json` rather than a `.lungfish-provenance.json` sidecar | "`Analyses/` Every analysis result, each in its own subfolder named `<tool>-<timestamp>`. Most results record their provenance beside the output, and the Provenance and Reproducibility chapter shows where each one keeps it" |
| 39 | "LGE prepends it to the top of the tree when the project holds results, so what you see there is assembled from the project rather than read straight off the folder it points at" | true | `Sources/LungfishApp/Views/Sidebar/SidebarProjectScanner.swift:107-117` collects analyses and calls `nodes.insert(analysesGroup, at: 0)` | |
| 40 | "An empty project shows no Analyses group at all, and one appears as soon as the first result lands" | true | `Sources/LungfishApp/Views/Sidebar/SidebarProjectScanner.swift:108-109` guards the insert on `!analysesChildren.isEmpty` | |
| 41 | "De novo assemblies are results, so they land under `Analyses/` beside everything else" | true | `Sources/LungfishIO/Bundles/AnalysesFolder.swift:12` names assembly among the results stored there; no `Assemblies/` folder name exists anywhere in `Sources/LungfishIO/Bundles/` or the sidebar scanner | |
| 42 | "They are packaged as `.lungfishref` bundles, exactly like a downloaded reference" | true | `Sources/LungfishWorkflow/Assembly/AssemblyBundleBuilder.swift:1,101` "Creates a `.lungfishref` bundle from assembly output" and writes `"\(safeName).lungfishref"` | |
| 43 | "It is a directory holding a `manifest.json` at the root, a `genome/` folder with the bgzip-compressed FASTA and its indexes, and optional `annotations/`, `variants/`, and `tracks/` folders" | true | `Sources/LungfishCore/Bundles/BundleManifest.swift:78-93` documents exactly that tree, with `genome/sequence.fa.gz`, its `.fai` and `.gzi`, `annotations/`, `variants/`, and `tracks/` | |
| 44 | "LGE writes a lock record inside the bundle ... The record names the user, the host, the process, the app version, and the time it was taken" | true | `Sources/LungfishCore/Storage/ProjectLock.swift:35-47` `ProjectLockRecord` carries `user`, `host`, `pid`, `appVersion`, and `createdAt`; line 110 places the lock under `.lungfish`. The demo project holds `.lungfish/project.lock` | |
| 45 | "The **Project Is Open Read Only** message appears when the lock belongs to somebody else" | true | `Sources/LungfishApp/App/ProjectWriteGatePresenter.swift:22` `static let title = "Project Is Open Read Only"` | |
| 46 | "LGE calls that a stale lock and it has an explicit recovery path, which archives the old record and writes a note of why it was removed rather than deleting it quietly" | true | `Sources/LungfishCore/Storage/ProjectLockRecovery.swift:38-40` returns a `ProjectLockRecoveryResult` carrying both an `archiveURL` and a `recoveryRecordURL` | |
| 47 | "`lungfish-cli project lock <project>` takes a lock, `--mode` records what kind, and `lungfish-cli project unlock <project>` releases one" | true | `cli-help/project.txt` `project lock` takes `--mode` "Lock mode to record for tools and GUI clients (default: exclusive)", and `project unlock` removes the record | |
| 48 | "Both accept `--force`, which skips the stale-owner checks" | changed | Both accept `--force`, but the two flags differ. `cli-help/project.txt` documents lock's `--force` as "Replace an active lock without stale-owner checks" and unlock's as "Remove the lock even when it belongs to another user or process", which is broader than a stale-owner check | "Both accept `--force`. On `lock` it replaces an active lock without the stale-owner checks, and on `unlock` it removes a lock even when another user or process holds it, so keep it for a lock you are certain nobody holds" |
| 49 | "`lungfish-cli project migrate <project>` ... scans the bundles inside the project, leaves anything already current alone, and reports a legacy schema it has no safe transformer for instead of rewriting it" | true | `cli-help/project.txt` `project migrate` overview: "It scans Lungfish bundles inside a project, leaves current-version bundles untouched, and reports unsupported legacy schemas without mutating data until a schema-specific transformer exists" | |
| 50 | "Run it with `--dry-run` first to see what it plans to do" | true | `cli-help/project.txt` `project migrate` lists `--dry-run  Report planned actions without modifying files` | |
| 51 | "Project changes are stored when an import or an edit finishes successfully. Check the Operations Panel for work that is still running or has failed. Editing tools may ask you to apply or discard a draft before you leave" | true | `Sources/LungfishApp/App/AppDelegate+PersistenceHelp.swift:11` is that text almost verbatim | |
| 52 | "LGE remembers project windows and views when they close or the app quits" | true | `Sources/LungfishApp/App/AppDelegate+PersistenceHelp.swift:11` verbatim | |
| 53 | "**File > About Saving…** explains this behaviour, and there is no separate Save or Save As command" | true | `Sources/LungfishApp/App/MainMenu.swift:198-201` adds "About Saving…"; the File menu at lines 162-285 holds no Save or Save As item | |
| 54 | "**File > Import Center...** (Cmd-Shift-I) is the main way data comes in, a tabbed window of cards" | true | `Sources/LungfishApp/App/MainMenu.swift:206-212` binds "i" with `[.command, .shift]`; `Sources/LungfishApp/Views/ImportCenter/ImportCenterViewModel.swift` defines the `Tab` enum the window switches on | |
| 55 | "**File > Manage Project Storage…** goes the other way, reviewing what the project is using on disk and moving what you no longer need to the Trash" | true | `Sources/LungfishApp/App/MainMenu.swift:277-283`; `Sources/LungfishApp/Views/ProjectStorage/ProjectStorageSheetViewModel.swift:199,206` builds "Move <size> to Trash" and "Items remain recoverable in Trash" | |
| 56 | "The **File > Export** submenu offers Sequences (FASTA/GenBank), Annotations (GFF3), FASTQ, Project Sample Metadata (CSV), Image (PNG), and Image (PDF), with a Provenance submenu underneath" | true | `Sources/LungfishApp/App/MainMenu.swift:219-271` adds those six items in that order, then a separator and the Provenance submenu | |
| 57 | "Sequence and annotation exports use an explicit sidebar selection before falling back to the open document" | true | `Sources/LungfishApp/App/AppDelegate+ImportCenter.swift:3177-3220` branches on `!sidebarSelection.isEmpty` first and only then falls back to `document` | |
| 58 | "An annotation export asks you to choose a source when several supported sources are selected, reports the ones it cannot use, and names the source and the annotation count on the destination sheet" | true | `AppDelegate+ImportCenter.swift:3197-3216` raises "Choose an annotation source" when `sources.count > 1 || !scope.skipped.isEmpty` and appends "Unsupported selections"; line 3277 sets the panel message to the source title plus "<n> annotations from one source" | |
| 59 | "It never silently merges annotations from different sources" | true | `AppDelegate+ImportCenter.swift:3277` "annotations from one source", and `exportAnnotationSource` exports exactly one chosen source | |
| 60 | "A search field sits at the top of the sidebar in every project window ... a small spinner and a 'Searching project…' label appear just below the field" | true | `Sources/LungfishApp/Views/Sidebar/SidebarViewController.swift:313` `NSTextField(labelWithString: "Searching project…")` | |
| 61 | "A Scope selector ... offering All Project Data, EsViritu, Kraken/Bracken, TaxTriage, FASTQ Datasets, VCF + Reference, and JSON Manifests" | true | `Sources/LungfishApp/Views/Sidebar/UniversalSearchAdvancedPopoverController.swift:20-30` gives exactly those seven titles in that order | |
| 62 | "fields filter by Keywords, Virus, Family, Species, and Sample, by Min Unique Reads and Min and Max Total Reads, and by a Date From and Date To range typed as `YYYY-MM-DD`" | true | `UniversalSearchAdvancedPopoverController.swift:96-108` lists those fields; lines 74-75 declare `dateFromField` and `dateToField` | |
| 63 | "A 'High-confidence pathogens only' checkbox restricts results to flagged pathogens" | true | `UniversalSearchAdvancedPopoverController.swift:76` is that exact checkbox title | |
| 64 | "Apply writes the assembled query into the sidebar field and runs it, and Clear empties both the popover and the field" | true | `UniversalSearchAdvancedPopoverController.swift:78-79` declares both buttons, and their handlers do that | |
| 65 | "Select the `HG002` paired-end FASTQ bundle under `Imports/` in the demo project" | true | The demo project holds `Imports/HG002.lungfishfastq` | |
| 66 | "the Inspector shows the read count, the mean length, a per-base quality summary, and buttons to run a classification or a mapping" | true | `Sources/LungfishApp/Views/Inspector/Sections/DocumentSection.swift:1002-1019` rows Read Count, Mean Length, Mean Quality, Q20 Bases, and Q30 Bases; `Sources/LungfishApp/Views/FASTQ/FASTQOperationToolPanes.swift` supplies the actions | |
| 67 | "Select an alignment track inside a `.lungfishref` and it switches to alignment statistics, showing the mapped read count ... and a button to call variants" | true | `Sources/LungfishApp/Views/Inspector/MappingDocumentStateBuilder.swift:116-120` rows Mapped Reads, Unmapped Reads, Total Reads, and Mapped Rate; `Sources/LungfishApp/Views/Inspector/InspectorView.swift:933` `Button("Call Variants…")` | |
| 68 | "the mean coverage, how evenly that coverage is spread" | false | Neither figure is shown. `MappingDocumentStateBuilder.swift:110-140` rows Mapper, Preset, Mapped Reads, Unmapped Reads, Total Reads, Mapped Rate, Runtime, and the provenance fields, with no coverage row. A search of `Sources/LungfishApp/` and `Sources/LungfishKit/` for "Mean Coverage", "coverage evenness", and "coverage uniformity" returns nothing | "it switches to alignment statistics, showing the mapped and unmapped read counts, the proportion of reads that mapped, the mapper and preset that produced the alignment, and a button to call variants" |
| 69 | "Click a row in the Variants tab of the table drawer and it switches again, to that variant's `INFO` and `FORMAT` fields, the supporting read counts on each strand, and a button that copies the position" | false | Three parts are wrong. `Sources/LungfishApp/Views/Inspector/Sections/VariantSection.swift:309-440` shows ID, Type, Position, Alleles, Quality, Filter, a Genotype Summary with Alt Allele Freq, and an INFO Fields list. There is no FORMAT section and no per-strand read count anywhere in the file, and the button at line 459-462 is `Label("Copy Info", ...)`, which copies the whole variant summary rather than the position | "Click a row in the Variants tab of the table drawer and it switches again, to that variant's position, alleles, quality, and filter, a genotype summary with the alternate allele frequency, its `INFO` fields, and a Copy Info button that puts the whole summary on the clipboard" |
| 70 | "Variants live in the table drawer rather than the sidebar because a single track holds far more of them than a sidebar could usefully list" | true | Consistent with `docs/user-manual/reviews/fidelity-2026-09/CONSISTENCY.md` ("Variants live in the table drawer at the bottom of a reference bundle viewport, on its Variants tab"), and the demo project's chr20 bundle carries 1,056 and 862 variants on its two tracks | |
| 71 | "The top names the item and gives its read count" | true | `Sources/LungfishApp/Views/Inspector/Sections/DocumentSection.swift:940-942` renders "FASTQ Dataset" then `"\(formatCount(stats.readCount)) reads"` | |
| 72 | "Summary statistics follow, then the ingestion settings recorded at import time, then the processing pipeline that produced this exact dataset with a tool name, a command line, and an elapsed time for each step" | true | `DocumentSection.swift:1023` "Dataset Statistics", line 1200 "Ingestion", and lines 1280-1296 render each step's command line beside `formatDuration(step.durationSeconds)` | |
| 73 | "Editable sample metadata sits at the bottom" | true | `Sources/LungfishApp/Views/Inspector/Sections/FASTQMetadataSection.swift:353` "Sample Metadata" with editable Template and Read Type controls at lines 383-418 | |
| 74 | "an empty Inspector means nothing is selected" | true | `DocumentSection.swift:1519-1522` renders "No Bundle Loaded" and "Open a reference bundle to view its metadata" as the empty state | |
| 75 | "Each operation gets a row showing its type, its name, a progress bar, and the elapsed time" | true | `Sources/LungfishApp/Views/Operations/OperationsPanelController.swift` builds those cells, and `OperationCenter.Item` carries type, title, progress, and timing | |
| 76 | "Click the disclosure triangle to expand it and you get the command LGE built, buttons to view or reveal the log file, and the running log output in a scrolling area" | true | `OperationsPanelController.swift` expanded-row content, and the row menu's Copy CLI Command at line 1517 is gated on `item.cliCommand != nil` | |
| 77 | "Failed operations stay until you dismiss them" | true | `Sources/LungfishKit/OperationCenter.swift:727-731` removes rows only on an explicit `clearCompleted()`, and `clearItem` at line 738 only on request | |
| 78 | "The panel covers the current session only" | true | `Sources/LungfishKit/OperationCenter.swift` holds `items` in memory with no persistence path | |
| 79 | "**Clear Completed** at the bottom removes finished rows" | true | `Sources/LungfishApp/Views/Operations/OperationsPanelController.swift:396` builds the footer button; `OperationCenter.clearCompleted()` at line 727 removes every non-active row, failed ones included | |
| 80 | "**Operations > Cancel All Operations** stops everything still running at once" | true | `Sources/LungfishApp/App/MainMenu.swift:884-889` adds "Cancel All Operations" | |
| 81 | The chapter makes no Cmd-Period claim | true | Confirmed by inspection. The only `keyEquivalent` in the Operations row menu is `""` on every item (`OperationsPanelController.swift:1509-1580`), and `MainMenu.swift:884-889` gives Cancel All Operations no key equivalent, so the removal was correct | |
| 82 | "**Run Again…** appears at the top when LGE still holds enough of the original request to replay it" | true | `OperationsPanelController.swift:1509-1514` adds "Run Again…" first, guarded on `WorkflowOperationsWindowController.replaySourceBundleURL(for: item) != nil` | |
| 83 | "**Copy CLI Command** copies the exact command line that ran" | true | `OperationsPanelController.swift:1516-1521`, gated on `item.cliCommand != nil` | |
| 84 | "**Copy Log** puts the log text on the clipboard, **View Log** opens it inline, and **Reveal Log in Finder** opens the folder at the log file" | true | `OperationsPanelController.swift:1523-1538` adds all three together, gated on `!item.logEntries.isEmpty` | |
| 85 | "a running row offers **Cancel** and a finished one offers **Clear**" | true | `OperationsPanelController.swift:1571-1583` adds Cancel when `item.isCancellable`, else Clear when `!item.state.isActive` | |
| 86 | "Cancellation is cooperative, so the tool is asked to stop and clean up, and the row reads as cancelled once it has" | true | `Sources/LungfishKit/OperationCenter.swift:651,668` finishes the worker in state `.cancelled` only after the work yields, rather than killing the process from the panel | |
| 87 | "**Copy Failure Report** gathers the operation title, the command, the error message, the error detail, and the log into one block" | true | `OperationsPanelController.swift:1466` builds that report, and the menu item is added at lines 1543-1547 on `item.state == .failed` | |
| 88 | "**Open GitHub Issue** opens a pre-filled issue in your browser with that report attached, which you review and submit yourself" | true | `OperationsPanelController.swift:1540-1541` comments "the user reviews and submits in the browser"; the item is added at lines 1549-1552 | |
| 89 | "**Reveal Failure Report in Finder** appears once the report file has been written" | true | `OperationsPanelController.swift:1556-1565` adds it only when `item.failureReportURL != nil` | |
| 90 | "A failed row is not cancellable, so **Clear** takes the place of **Cancel** there" | true | `OperationsPanelController.swift:1571-1583`, since a failed state is neither cancellable nor active | |
| 91 | "**Help > Lungfish Genome Explorer Help** opens it in the macOS Help Viewer ... falling back to an in-app window when the Help Viewer is unavailable" | true | `Sources/LungfishApp/App/MainMenu.swift:985-990` is that item on "?"; `Sources/LungfishApp/App/AppDelegate+MenuActions.swift:842-856` calls `HelpBookIntegration.openTopic` then falls back to `HelpWindowController` | |
| 92 | "Three shorter guides sit under it, Getting Started, VCF Variants Guide, and AI Assistant Guide, and below those, Documentation and Release Notes open pages on the web" | true | `Sources/LungfishApp/App/MainMenu.swift:994-1023` adds those five in that order, separated as described, with `openOnlineDocumentation` and `openReleaseNotes` as the web actions | |
| 93 | "**Help > Report an Issue...** opens a pre-filled GitHub issue template carrying the version string" | true | `Sources/LungfishApp/App/MainMenu.swift:1025-1029` "Report an Issue..." wired to `HelpMenuActions.reportIssue` | |
| 94 | "Confirm the window title carries the project name without '(Read Only)' after it, since that suffix means the bundle was never given a project store or that somebody else holds the lock" | true | `Sources/LungfishApp/App/AppDelegate.swift:600-610` appends the suffix on the no-store fallback path, and line 673 on `isReadOnlyRecommended`, which the lock gate drives | |
| 95 | CLI example `lungfish-cli import fasta ... --name chr20 --output-dir "<project>"` | true | `cli-help/import.txt:98-107` `import fasta <input-file> [--output-dir <output-dir>] [--name <name>]`, and `fixtures/demo-project/README.md` records that `-o` takes the project directory | |
| 96 | CLI example `lungfish-cli import fastq <r1> <r2> --project "<project>"` | true | `cli-help/import.txt:185` `import fastq [<options>] [<input> ...] --project <project>` | |
| 97 | CLI example `lungfish-cli project lock ... --mode exclusive` and `project unlock` | true | `cli-help/project.txt` `project lock` accepts `--mode` with default `exclusive`, and `project unlock` takes the project path | |
| 98 | CLI example `lungfish-cli project migrate ... --dry-run` | true | `cli-help/project.txt` `project migrate <project-path> [--dry-run]` | |
| 99 | "A project bundle ends in `.lungfish` and a reference bundle inside it ends in `.lungfishref`" | true | `Sources/LungfishCore/Storage/ProjectFile.swift:98`; `Sources/LungfishIO/Bundles/ReferenceSequenceFolder.swift:105-106` | |
| 100 | "The CLI rejects a `--project` path that does not end in `.lungfish`" | unverifiable | No extension check appears in `Sources/LungfishCLI/`. `ImportFastqCommand.swift:77` only documents the option as "Path to `.lungfish` project directory", and a repo-wide search of `Sources/LungfishCLI/` for `pathExtension` against a project path returns only unrelated format sniffing. `docs/user-manual/fixtures/demo-project/README.md` asserts the rejection, but a fixture note is not source. Settling it needs one run of a write command with a non-`.lungfish` `--project`, which this review may not perform | |

## Front matter

| Item | Verdict | Evidence |
|---|---|---|
| `parameters_refs: []` | true | Correct by campaign ruling. The chapter documents no operation with settings, so it cites no parameter ids |
| `shots` lists every `<!-- SHOT -->` marker with a caption | true | Eight markers in the body (welcome-window, empty-project-window, sidebar-folder-conventions, file-export-menu, inspector-fastq-selected, inspector-fastq-detail, operations-panel-row, operations-panel-right-click-menu) against eight front-matter entries with the same ids, each carrying a caption. The front matter lists `file-export-menu` sixth while the body places it fourth, which the schema does not constrain |
| `glossary_refs` anchors resolve | true | All eleven resolve as `{#anchor}` entries in `docs/user-manual/GLOSSARY.md`: project, bundle, reference-bundle, primer-scheme, extraction, project-lock, inspector, operations-panel, sidebar, provenance, provenance-sidecar |
| `help-ids.yaml` entries pointing here resolve | true | Seven entries name this chapter. `menu.File.NewProject` and `menu.File.Open` point at `the-welcome-window`, `menu.View.Sidebar` at `a-tour-of-the-sidebar`, `menu.View.Inspector` at `the-inspector`, `menu.Operations.Panel` and `operations.Panel` at `the-operations-panel`, and `settings.General` carries an empty anchor. Every named heading exists in the body |

## Screenshot notes

Two captions overstate what the demo project can show, so the shots cannot be
taken against it as written.

- `sidebar-folder-conventions` promises "the Analyses group above the Imports,
  Reference Sequences, Primer Schemes, and Extractions folders". The demo
  project has no `Extractions/` folder, and its `Primer Schemes/` folder is
  empty. Either run an extraction and import a scheme into the demo project
  first, or reword the caption to the four groups that exist.
- `operations-panel-right-click-menu` promises "Run Again, Copy CLI Command,
  Copy Log, View Log, Reveal Log in Finder, and Cancel". That menu only appears
  in full on a running row whose replay source still exists, since
  `OperationsPanelController.swift:1509-1583` gates every item. The shot has to
  be taken mid-run, not against a finished row.

## Counts

True 94, false 5, unverifiable 1.

The five false rows split the way the reality map splits them. Two are wrong
outright (68 and 69, both in the Inspector prose) and three are true in
substance but need rewording to match the app (9, 38, and 48), which the map
would label changed. Every one of the five carries its corrected wording in the
table above.
