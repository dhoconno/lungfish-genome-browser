# Reader 1 report, Running Freyja

Persona: sophomore, one intro genetics course, never opened a terminal, never used sequencing software.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "Kraken 2, EsViritu, TaxTriage, and the importers" | Four names dropped with no hint of what they are. I have not read the earlier chapters and nothing here says these are read classifiers until two sentences later. | Say "the read classifiers Kraken 2, EsViritu, and TaxTriage" so the category comes before the names. |
| What it is, "BQ.1 and BA.5.3.2 are both SARS-CoV-2" | I could not tell whether these letter-and-number strings are official names, gene names, or made-up examples, and nothing says how to read them. | Add one sentence saying these are Pango lineage names and each added number marks a further split. |
| What it is, "a few dozen positions out of nearly thirty thousand" | I do not know if a few dozen differences is a lot or a little for telling two things apart, so I cannot judge how hard this problem is. | Say whether a few dozen is enough to identify a lineage confidently. |
| What it is, "At every position where the sample differs from the reference genome, mapping tells you what fraction" | "Mapping" arrives as a bare verb doing the work, and I do not know what mapping is or that it is a separate step I have to run. | Gloss mapping at first use and link it here, not only in Before you start. |
| What it is, "solves for the mixture of lineages whose combined mutation profile comes closest" | Had to read twice. "Solves for" sounds like algebra and I could not tell if this is exact or a best guess. | Say it searches for the mixture that best fits, since no exact answer exists. |
| Why you would do this, "a consensus genome cannot" | "Consensus genome" is used before it is explained, and the explanation comes in the following sentence inside a link title. | Gloss consensus genome in the same sentence where it first appears. |
| Why you would do this, "The SRR36291587 reads come from a single clinical sample" | I did not know what an SRR number is or where it comes from until much later in Before you start. | Say at first use that SRR36291587 is an accession number in a public sequence database. |
| Before you start, "choose File > New Project (Cmd-N)" | I have never used this app. I do not know if a project is a folder, a file, or a workspace, and nothing says what picking a folder does. | Add a half sentence saying a project is a folder where LGE keeps the results of your work. |
| Before you start, "Build a reference bundle from MN908947.3.fasta and map the reads to it with minimap2" | This is the single biggest stopping point. It is three separate multi-step operations compressed into one sentence, each pointed at a different chapter, and I cannot tell in what order to do them or how long they take. | Break this into three numbered prerequisite steps, one per linked chapter. |
| Before you start, "MN908947.3.fasta" | The name is never explained. I could not tell whether it is a gene, a strain, or a database record. | Say once that MN908947.3 is the accession for the SARS-CoV-2 reference genome. |
| Before you start, "the virus was copied in numbered overlapping pieces by PCR" | I know PCR from lecture but I could not picture why the piece ends carry primer bases rather than sample bases, so the reason for trimming did not land. | Add one sentence saying the primer sequence is synthetic DNA added by the experimenter, not read from the sample. |
| Before you start, "Leaving them in fakes allele frequencies at the amplicon edges" | "Fakes" is doing a lot of work. I could not tell if the numbers get too high, too low, or just unreliable. | Say the frequencies come out wrong in a direction you cannot predict. |
| Before you start, "Open Tools > Plugin Manager... (Cmd-Shift-B) and install it" | No indication of how long the download takes or how much disk it needs, only that it is "not instant" and "large enough". I cannot plan around that. | Give an approximate size or time range for the pack download. |
| Before you start, "iVar and minimap2 for the mapping and variant summarising steps" | iVar is named with no gloss, and the sentence pairs two tools to two steps without saying which does which. | Gloss iVar in one clause and say which tool does which step. |
| Before you start, "Pangolin and Nextclade for naming a lineage" | Two more unexplained tool names in a chapter I am told not to use them in. | Say in the same sentence that these are not used in this chapter. |
| Before you start, "LGE pins version 2.0.3 of it" | "Pins" is jargon. I read it as a pushpin at first. | Say LGE always installs exactly version 2.0.3. |
| Before you start, "The runs in this chapter used a barcode file dated 22 March 2026" | I could not judge whether a barcode from that date is fine or badly out of date for me reading this now, and the chapter tells me refreshing happens outside the app without saying whether I need to. | State how stale a barcode can get before results suffer. |
| Before you start, "Freyja has its own freyja update command ... which LGE does not wrap" | "Wrap" is jargon, and this sends me to a terminal command with no instruction on how to run it. This is the one prerequisite I genuinely could not perform. | Either give the full steps for running freyja update or say plainly that a beginner should skip it. |
| Procedure, "it never appears in the Tools category submenus" | A whole paragraph explains why a menu item is absent. As a first-time reader I did not need the reasoning and had to reread it to find the actual instruction. | Lead with "Freyja runs from the command line only" and move the reasoning after the steps. |
| Procedure, "[The Command Line](../appendices/cli-reference.md) covers where it lives" | I have never opened a terminal. One link is all I get for opening a terminal, finding the tool, and getting into the right folder. Everything below this line was unperformable for me. | Add two or three lines here on opening Terminal and changing to the folder holding your files. |
| Procedure Step 1, "it runs from the pack's environment rather than through lungfish-cli" | I do not know what an environment is or how to get into one, so I cannot run the very first command. | Show the exact command that activates the pack environment before the freyja variants line. |
| Procedure Step 1, the code block with trailing backslashes | Nothing explains the backslash at line ends or whether I type this as one line or four. | Add a note that the backslash continues one command onto the next line. |
| Procedure Step 1, "srr36291587.trimmed.bam" | The filename appears from nowhere. The prerequisites never said the trimmed BAM would be called this or where it would be saved. | Say the name is an example and to substitute your own trimmed BAM path. |
| Procedure Step 1, "an allele frequency of 50 percent means something quite different when 4,000 reads covered the position than when 4 did" | Best sentence in the chapter, but it stops short. I still do not know what depth counts as enough. | Add the rough depth below which a frequency should not be trusted. |
| Procedure Step 2, "16,779 rows" | I have no way to judge whether my own count of, say, 300 rows would be normal or a sign something broke. | Say whether this count varies widely between samples and what a suspiciously low one means. |
| Procedure Step 2, "Write the command plan without running anything" | I did not understand what a command plan is for until Step 3. It reads like a step I could skip. | Say in the step line that the plan lets you check the command before it runs. |
| Procedure Step 2, "with the three leading dots standing in for the full directory path" | Had to read twice. I first thought I should type the dots. | Say the dots are shortening for print and your output shows full paths. |
| Procedure Step 3, "took 16.7 seconds of wall time" | "Wall time" is unexplained jargon. | Say "16.7 seconds of real time". |
| Procedure Step 3, "Passing both --dry-run and --execute does not run Freyja" | This warns about a mistake I have no reason to make yet, placed before I have run anything successfully. | Move this note to the end of the Procedure section. |
| Settings, "Freyja demix registers no dialog settings" | "Registers" is developer phrasing and "dialog settings" is undefined. | Say Freyja demix has no dialog, so every setting is a command-line flag. |
| Settings, Extra args, "such as --eps to change the minimum abundance ... which Freyja defaults to 0.001" | I could not tell what units 0.001 is in, and the later text calls the same idea one percent, which took a second pass to reconcile. | State once that abundances are proportions, so 0.001 is one tenth of one percent. |
| Settings, Extra args, "passed through unchecked" | I did not know what "unchecked" risks. Nothing tells me what goes wrong beyond a refusal. | Say a bad value here is Freyja's error, not LGE's, and the run stops. |
| Reading the results, "`.lungfish-provenance.json`" | The filename begins with a dot and the chapter never says such files are hidden in Finder, so I would think the file was missing. | Note that files beginning with a dot are hidden by default. |
| Reading the results, the code block starting with a blank-looking first line | The first line has an invisible leading tab and a file path with no label. I could not tell what field it was. | Say the first line is an unlabelled row holding the input file path. |
| Reading the results, "summarized [('Omicron', 0.9885371959826457)]" | This looks like raw program output with brackets and parentheses I cannot parse, and nothing says why it is formatted this way. | Add a clause saying the brackets are the tool's own formatting and to read only the name and the number. |
| Reading the results, "no other group reached the threshold to be listed" | Which threshold? None was named, and I could not tell if it is the eps value from Settings or something else. | Name the threshold or link back to the eps setting. |
| Reading the results, "the solver can split one real lineage's evidence between them" | "Solver" appears for the first time here as if already introduced. | Call it the solver at first use in What it is, or say "Freyja" here. |
| Reading the results, "coverage is the percentage of genome positions covered by at least ten reads" | The number is clear but I still could not judge my own. "In the tens" is bad, 99.51 is good, and the whole middle is undefined. | Give a rough dividing line, for example below what value the abundances stop being usable. |
| Reading the results, "resid ... This run gives 12.29" | Told smaller is better, told zero is perfect, told no threshold exists, and told to compare within a batch. I have one sample, so I cannot judge my number at all. | Say plainly that a single sample's residual cannot be judged on its own. |
| Reading the results, "a SHA-256 checksum and byte size for each input table" | Checksum is not explained. I know it is a number that identifies a file but not why it proves anything. | Gloss checksum as a fingerprint that changes if the file changes. |
| What good looks like, "The abundances should sum to close to 1" | "Close to" is not a number. 0.9885 is given as good, but I do not know if 0.90 is a problem. | Give the value below which the total is a warning sign. |
| What good looks like, "the symptom is a poor residual or a low abundance total rather than an error message" | "Poor residual" contradicts the earlier statement that no residual threshold exists, so I could not act on this. | Say the residual is high relative to your other samples. |
| On the command line, "cat demix-run/freyja-demix.tsv" | `cat` is used without explanation and it is the only command here that is not one of the two tools. | Say cat prints a file to the screen. |
| Next, "the pipeline pins a Freyja container built for Intel processors only, while the ... pack installs a native build that runs on Apple Silicon" | Container, native build, Intel, and Apple Silicon all arrive at once in the last sentence. I could not follow it and it does not seem to matter for what I just did. | Cut this to one clause or move it into the Viral Recon chapter. |

Row count: 43.

One thing I learned. A sample can be a blend of several lineages of the same virus, and taking the most common base at each position quietly erases everything but the biggest one.

One thing I still could not do. Run anything at all. The chapter is command-line only and I have never opened a terminal, so Step 1 was already out of reach.

The sentence I liked most. "Both are needed, because an allele frequency of 50 percent means something quite different when 4,000 reads covered the position than when 4 did."
