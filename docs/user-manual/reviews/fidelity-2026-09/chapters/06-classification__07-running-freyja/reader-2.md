# Reader report, Running Freyja

Persona: senior, two years of pipetting, knows library prep and PCR, no data analysis, never opened a terminal.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "Kraken 2, EsViritu, TaxTriage, and the importers" | Four tool names in the first paragraph, and I have not read the earlier chapters in this part. I could not tell whether I needed them first. | Say these are the earlier chapters' tools and I do not need them to follow this one. |
| What it is, "The difference between them lives in a few dozen positions" | I could not judge whether a few dozen out of thirty thousand is a lot or almost nothing for telling lineages apart. | Say plainly that this is a tiny fraction, which is why ordinary classifiers cannot separate them. |
| What it is, "At every position where the sample differs from the reference genome, mapping tells you what fraction" | I have never done mapping and do not know it produces per position fractions. This sentence assumes I know the output of a step I have never seen. | One clause saying mapping stacks reads on the reference so each position has a count. |
| What it is, "solves for the mixture of lineages whose combined mutation profile comes closest" | "Solves for" is mathematics language and I had to read the sentence twice to see that the computer is guessing and checking. | Say the tool searches for the combination that best matches. |
| Why you would do this, "so a sample that is 60 percent one lineage" | The earlier section said sixty and thirty-five percent, this one says 60 and 40. I stopped to check whether these were the same example. | Use one set of numbers for the running example. |
| Why you would do this, "The SRR36291587 reads come from a single clinical sample" | An accession number appears as if I should recognize it. I did not know it was a database identifier until the next section. | Say it is an SRA accession at first mention. |
| Before you start, "choose File > New Project (Cmd-N)" | Fine, but nothing tells me where the project folder should sit relative to the files I am about to download. I guessed. | Say the downloads do not need to go inside the project folder. |
| Before you start, "Download the reference file MN908947.3.fasta" | The chapter never says what a reference file is for or why this particular one. I know PCR primers, not references. | One clause saying the reference is the genome the reads get compared against. |
| Before you start, "fetch them from the Sequence Read Archive as accession SRR36291587" | I do not know how big this download is or how long it takes, so I could not plan around it. | State the approximate download size. |
| Before you start, "Build a reference bundle from MN908947.3.fasta and map the reads to it with minimap2, then trim the primers" | Three whole chapters of work compressed into one sentence with three links. I could not tell how long this takes or whether I can do it in one sitting. | Say this is roughly an hour of prerequisite work across three chapters. |
| Before you start, "Leaving them in fakes allele frequencies at the amplicon edges" | This is the one part I understood immediately from bench work, but "fakes" as a verb made me read it twice. | Say the primer bases produce false frequencies. |
| Before you start, "Open Tools > Plugin Manager... (Cmd-Shift-B) and install it" | No wait time or download size given for a pack the text calls large. I did not know whether to start it and walk away. | Give an approximate size or minutes. |
| Before you start, "Pangolin and Nextclade for naming a lineage from a finished consensus sequence" | Two more tool names introduced and never used again in this chapter. I spent time deciding whether I needed them. | Say the last two are not used in this chapter. |
| Before you start, "LGE pins version 2.0.3 of it" | "Pins" is software jargon. I read it as a physical pin the first time. | Say LGE installs a fixed version, 2.0.3. |
| Before you start, "The runs in this chapter used a barcode file dated 22 March 2026" | I could not tell how to check the date of the barcode file that my own install shipped with. The paragraph says nothing asks me for it, which left me with no way to look. | Say where to find the barcode file's date. |
| Before you start, "Freyja has its own freyja update command for refreshing it, which LGE does not wrap" | "Wrap" is jargon, and "outside the app" is the only guidance for someone who has never opened a terminal. Dead end. | Say plainly that refreshing barcodes is beyond this chapter. |
| Procedure, "It runs from the command line only" | This is the sentence where I realized I cannot do this chapter at all. It arrives after two pages of setup work I would already have done. | Put this warning in the first paragraph of the chapter. |
| Procedure, "so it never appears in the Tools category submenus" | Three sentences explaining why a menu item is absent. I do not need the internal reason and reread them looking for an instruction. | Cut to one sentence. |
| Procedure, "The command line tool is lungfish-cli" | The appendix link is my entire onboarding for a terminal I have never opened. Nothing here tells me whether that appendix is ten minutes or an afternoon. | Say the appendix is required reading before Step 1 for anyone new to the terminal. |
| Procedure step 1, "it runs from the pack's environment rather than through lungfish-cli" | I do not know what an environment is or how to get into one. I cannot perform Step 1 from this text. | Give the exact command that puts me in the pack's environment. |
| Procedure step 1, "freyja variants srr36291587.trimmed.bam" | The file is named as if I already have it under that exact name, but the mapping chapter never promised that name. I could not tell what my own file is called. | Say to substitute your own trimmed BAM's name. |
| Procedure step 1, code block backslashes | The trailing backslashes and indented lines are unexplained. I did not know whether to type all four lines or one. | One line saying the backslash continues the command onto the next line. |
| Procedure step 1, "16,779 rows and a depths table with 29,903 rows" | I do not know whether my variants row count should be near 16,779 or wildly different, since only the depths count is explained as fixed. | Say the variants count varies by sample and is not a check. |
| Procedure step 2, "Write the command plan without running anything" | I did not understand what a command plan is or why I would want one until after reading Step 3. | Gloss the command plan as a preview of what will run. |
| Procedure step 2, "--output-dir demix-dry" | Two different output directories across Steps 2 and 3 with no explanation of why. I wondered whether I had made a typo. | Say the dry run gets its own directory so it does not overwrite the real one. |
| Procedure step 3, "The recorded run took 16.7 seconds of wall time" | "Wall time" is undefined. I guessed it means clock time. | Gloss wall time at first use. |
| Procedure step 3, "Passing both --dry-run and --execute does not run Freyja" | I had to read this three times. The paragraph explains the trap before saying what to do, and "the dry run wins" reads as an opinion. | Lead with the instruction, then the explanation. |
| Settings, "Freyja demix registers no dialog settings" | "Registers" is developer language, and this whole section is flags for a terminal I cannot use. | Say every setting here is a command-line flag. |
| Settings, Extra args, "--eps to change the minimum abundance a lineage must reach" | "eps" is never expanded and 0.001 is given without saying it is a proportion, not a percent. | Say 0.001 is a proportion, so one tenth of one percent. |
| Reading the results, ".lungfish-provenance.json" | The leading dot means the file is hidden on a Mac and the chapter never says so. I would have concluded the run failed. | Say the provenance file is hidden in Finder. |
| Reading the results, result code block | The output block is wrapped across lines with no marker, and the first line is a bare indented path with no label. I could not tell where one line ended. | Mark the wrapped lines or say the first line is unlabelled. |
| Reading the results, "no other group reached the threshold to be listed" | A threshold is mentioned but never given a number, so I could not judge what was suppressed. | State the threshold. |
| Reading the results, "Those two together are 96.9 percent" | 0.623 plus 0.346 is 96.9 percent, but the summarized line says 98.85 percent for Omicron. I could not reconcile the two figures without doing the arithmetic myself. | Say the summarized figure includes all twelve lineages, not just the top two. |
| Reading the results, "when two lineages differ at only a handful of positions the solver can split one real lineage's evidence" | This is the most important warning in the chapter and it is buried in the middle of a paragraph. I nearly missed it. | Give the warning its own opening sentence. |
| Reading the results, "coverage is the percentage of genome positions covered by at least ten reads" | The number 99.51 is explained well, but "a sample where coverage drops into the tens" gives no cutoff I could act on. | Give a number below which the result should not be used. |
| Reading the results, "resid is the residual" | 12.29 is given with no scale at all, and then the text says no threshold exists. I finished the section unable to judge my own number. | Say the residual has no absolute scale before quoting 12.29. |
| Reading the results, "a SHA-256 checksum and byte size" | Checksum is undefined. I could not tell whether it is something I need to check by hand. | Gloss checksum as a fingerprint that changes if a file changes. |
| What good looks like, "The abundances should sum to close to 1" | "Close to" is not quantified, and 0.9885 is the only example. I could not tell whether 0.95 passes. | Give a floor for the total. |
| What good looks like, "the symptom is a poor residual or a low abundance total" | "Poor residual" contradicts the earlier statement that no threshold exists for the residual. I stopped to decide which sentence to believe. | Drop the residual from this check. |
| On the command line, "cat demix-run/freyja-demix.tsv" | `cat` appears once with no explanation. I did not know it was a command to display a file. | Say `cat` prints the file to the screen. |
| Next, "the pipeline pins a Freyja container built for Intel processors only" | Container, Intel, and Apple Silicon all arrive in one clause at the very end. I could not tell whether this affects my machine. | Cut this to a link. |

One thing I learned. A single sample can hold several lineages of the same virus at once, and a consensus sequence quietly erases all but the most common one.

One thing I still could not do. Anything in the Procedure. The chapter is command-line only and I have never opened a terminal, and I only found that out after two pages of setup.

The sentence I liked most. "Sewage from a city carries virus shed by everyone in it, and different people are infected with different lineages, so one wastewater sample is a blend."
