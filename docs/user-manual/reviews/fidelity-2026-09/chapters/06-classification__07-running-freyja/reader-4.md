# Reader 4 report, Running Freyja

Persona. An undergraduate who used Geneious for one semester, is comfortable
with a graphical sequence viewer, expects every operation to have a dialog,
and has never opened a terminal.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "Kraken 2, EsViritu, TaxTriage, and the importers" | Four tool names arrive in the first paragraph with no gloss. I have used Geneious, not any of these. I do not know whether TaxTriage is a competitor of Kraken 2 or a step after it. | Say "the read classifiers of the earlier chapters" and name them in a parenthesis. |
| What it is, "solves for the mixture of lineages" | "Solves for" is algebra vocabulary applied to biology. I read it twice trying to work out what is being solved and by what. | Add one sentence saying Freyja tries many candidate mixtures and keeps the one that best matches. |
| What it is, "the difference between them lives in a few dozen positions out of nearly thirty thousand" | I could not judge this. Is a few dozen a lot or a little for telling lineages apart? The number is given but not calibrated. | Say plainly that this is why a read classifier cannot separate them but a whole-genome frequency method can. |
| Why you would do this, "cross-contaminated in the laboratory" | Not explained. I do not know if this means two samples touching in a rack or something during library prep. | Gloss it as one sample's DNA getting into another during handling. |
| Before you start, "Download the reference file MN908947.3.fasta ... from the manual's practice data files on GitHub" | I could not do this step. I have never used GitHub. The link goes to a folder, and I do not know how to get a single file out of a GitHub folder view. | Add the one click that downloads the file, or link to a chapter that shows it. |
| Before you start, "fetch them from the Sequence Read Archive as accession SRR36291587" | The reads are said to be too large for GitHub but no size is given, so I cannot tell whether this will take a minute or an afternoon on my laptop. | Give the approximate download size and time. |
| Before you start, "Build a reference bundle ... and map the reads to it with minimap2 ... then trim the primers" | Three whole chapters are folded into one sentence as prerequisites. Coming from Geneious, where mapping is one dialog, I could not tell how long this detour is before I get back here. | Say how many operations this is and that each has its own dialog. |
| Before you start, "minimap2" | Named without gloss at first use here. I know Geneious "Map to Reference", not minimap2. | Gloss it once as the read mapper LGE uses. |
| Before you start, "the primer bases at each piece's ends are copies of the primer scheme rather than of the sample" | I had to read this three times. The idea that a base can be a copy of a primer rather than of the sample is not something my genetics course covered. | Add one sentence saying the primer sequence was added by the lab, so it tells you nothing about the sample. |
| Before you start, "Open Tools > Plugin Manager... (Cmd-Shift-B) and install it" | I could not perform this. It does not say what to click once the Plugin Manager is open. Geneious plugin install is a named button, so I looked for one here and the text does not name it. | Name the control that starts the install. |
| Before you start, "the pack is large enough that the download is not instant" | I cannot judge this. No size and no time. I do not know whether to start it and wait or start it and go to lunch. | Give the download size in gigabytes and a rough time. |
| Before you start, "marked Experimental in the Plugin Manager" | I did not know whether Experimental means I should avoid it for coursework. The paragraph reassures me about the programs but not about whether results are safe to report. | State whether results from an Experimental pack are usable in a report. |
| Before you start, "LGE pins version 2.0.3 of it" | "Pins" is jargon. I guessed it means locks to that version, but I was not sure. | Say "installs and locks to version 2.0.3". |
| Before you start, "Freyja has its own freyja update command ... which LGE does not wrap, so refreshing the barcodes is something you would do outside the app" | This is a dead end for me. I have never opened a terminal, and "outside the app" is the only instruction. If my barcodes are stale I cannot fix it from this chapter. | Point to the appendix that shows how to run a command outside LGE, or say plainly that this task needs help from someone who uses a terminal. |
| Before you start, "a barcode file dated 22 March 2026" | I could not judge how stale that is relative to today, or how quickly barcode files go out of date. | Say how often new lineages appear, so I can tell whether a six-month-old barcode matters. |
| Procedure, "It runs from the command line only." | This is the point where the chapter stops working for me. Everything before it reads like an app chapter with menus and dialogs, and then the whole operation turns out to be terminal-only. Coming from Geneious I expected a dialog for every operation. | Say this in the first paragraph of "What it is" so a reader without a terminal knows before investing sixteen minutes. |
| Procedure, "Freyja is not a FASTQ or FASTA operation, so it never appears in the Tools category submenus" | This explains the app's internals rather than telling me what to do. I read it twice looking for an action. | Cut to one sentence, or move it to a note. |
| Procedure, "The command line tool is lungfish-cli" | I could not perform any step from here on. The chapter links to an appendix but does not say I will need to open Terminal, find a folder, and type. | Say in one sentence that the rest of this chapter requires a terminal, and what the appendix will teach. |
| Procedure Step 1, the `freyja variants` block | I could not run this. The command is not `lungfish-cli`, and the text says it "runs from the pack's environment", which I do not know how to enter. No instruction is given for getting into that environment. | Give the command that enters the pack environment, or give an LGE wrapper. |
| Procedure Step 1, "srr36291587.trimmed.bam" | I do not know where the primer-trimmed BAM lives after the trimming chapter, or what LGE named it. The command assumes the file is beside me. | Say where in the project folder the trimmed BAM is written and under what name. |
| Procedure Step 1, "a variants table with 16,779 rows" | I cannot judge this number. Is 16,779 rows of disagreement with the reference a lot for a 29,903 base genome? It sounds like half the genome is mutated, which cannot be right. | Explain that the table has several rows per position, so the count is not a mutation count. |
| Procedure Step 2, "--dry-run" and the "..." in the printed command | The three dots are explained after the block, but I read the block first and thought the command was truncated or broken. | Put the note before the block. |
| Procedure Step 3, "The recorded run took 16.7 seconds of wall time" | "Wall time" is not glossed. I guessed it means real elapsed time. | Say "elapsed time". |
| Procedure Step 3, "Passing both --dry-run and --execute does not run Freyja" | I had to read this paragraph twice. It describes a mistake before I have made it, using flag names I have only just met. | Move it to a note after Step 4. |
| Settings, "Freyja demix registers no dialog settings, because it has no dialog." | Every other chapter's Settings section describes controls I can see. Here the section is a list of things I have to type exactly right with no field to check my work against. | Say once that a typo in a flag is the equivalent of a wrong value in a field, and how the error appears. |
| Settings, Output dir, "a second run pointed at the same directory overwrites the first run's plan and result without warning" | This differs sharply from Geneious, which never silently overwrote my results. Nothing flags it as a difference. | Mark it as a warning rather than a clause in the middle of an entry. |
| Settings, Extra args, "--eps to change the minimum abundance a lineage must reach to be reported, which Freyja defaults to 0.001" | I could not judge whether 0.001 is a sensible floor or when I would want to change it. The example later raises it tenfold with no reason given beyond tidiness. | Say when a beginner should leave it alone. |
| Settings, Extra args, "passed through unchecked" | I do not know what happens on failure. Does the run stop, or does it produce a wrong answer quietly? The sentence says Freyja refuses it, but not what I see. | Say what the error looks like in the terminal. |
| Settings, Dry run, "Reach for it in a script where --execute is fixed" | I do not write scripts. This entire entry describes a situation I cannot be in. | Say the flag is not needed for manual runs. |
| Reading the results, the five-line output block | I could not match names to numbers by eye. Twelve names wrap across two lines and twelve numbers wrap across two more, and counting across a wrapped block to find the fourth of each is genuinely hard. | Show the first three pairs as a small two-column table beneath the block. |
| Reading the results, "[('Omicron', 0.9885371959826457)]" | This looks like code, not a result. Nothing explains the brackets, the quotes, or why the number has sixteen digits. | Add one sentence saying to read only the name and the first four digits. |
| Reading the results, "no other group reached the threshold to be listed" | The threshold is not named here. Earlier the Settings section mentioned 0.001 for lineages, but I could not tell whether the same threshold applies to the summarized line. | Name the threshold, or say it is the same one. |
| Reading the results, "Those two together are 96.9 percent" | I checked. 0.623 plus 0.346 is 0.969, but the chapter elsewhere gives the total as 0.9885, and I could not reconcile the two figures on a first pass. | Say the 96.9 percent is the top two only, not the total. |
| Reading the results, "the solver can split one real lineage's evidence between them" | "Solver" appears here for the first time as a noun for the thing doing the work. I did not connect it to "solves for" many pages earlier. | Use one word for the method throughout. |
| Reading the results, "resid is the residual" | I could not judge this at all. It is 12.29, smaller is better, zero is perfect, and no scale is given. I do not know if 12.29 is close to zero or far from it, and the chapter says no threshold exists. | Give the range this figure takes on the sample set the team has run, even loosely. |
| Reading the results, "coverage is the percentage of genome positions covered by at least ten reads" | This meaning of coverage differs from the one the alignment chapter uses, where coverage was reads per position. The chapter does not say the word has changed meaning. | Say Freyja's coverage is a percentage of positions, not a depth. |
| Reading the results, "SHA-256 checksum" | Not glossed. I know the word checksum vaguely, not SHA-256. | Gloss it once as a fingerprint of the file's contents. |
| Reading the results, "you cannot prove from the sidecar alone which result came out" | I did not know what to do with this. It reads as a caution with no action attached. | Say what to do instead, such as keeping the output directory intact. |
| What good looks like, "The abundances should sum to close to 1." | I could not do this check by hand from the wrapped output block, and no tool is offered for it. | Say the summarized line gives the same total when all lineages share one group. |
| What good looks like, "processed the same way" | This assumes I have a batch. My coursework is one sample. The only absolute check offered is coverage. | Say what a lone-sample user should do. |
| On the command line, "cat demix-run/freyja-demix.tsv" | `cat` is never introduced. It is the only command in the chapter I could have guessed at, and it is unexplained. | Add a three-word gloss. |
| Next, "the pipeline pins a Freyja container built for Intel processors only, while the Wastewater Surveillance pack installs a native build that runs on Apple Silicon" | Container, Intel, native build, and Apple Silicon all arrive at once in a closing sentence. I do not know what a container is here. | Cut to the fact that the wizard's Freyja step is skipped and this chapter is the way to run it. |

The one thing I learned. A single sample can hold several varieties of one virus at once, and averaging them into a consensus sequence throws the smaller ones away.

The one thing I still could not do. Run anything at all. Every step from the Procedure onward needs a terminal I have never opened, and Step 1 needs an environment the chapter does not tell me how to enter.

The sentence I liked most. "Freyja starts after that question is already answered."
