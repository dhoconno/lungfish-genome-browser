# Reader report: Running TaxTriage

Reader 4. Undergraduate. I used Geneious for one semester in a class where we
aligned some sequences and built a tree. I have never opened a terminal.

| Location | What stopped me | What would have helped |
|---|---|---|
| What it is / "It takes a FASTQ file, classifies" | I get that a read is a stretch of sequence, but nobody told me what a FASTQ file actually is before it was used. In Geneious I just dragged in a file and it worked. | One clause saying a FASTQ file is the instrument's output holding every read plus its quality scores. |
| What it is / "gathers additional evidence for each one" | "Additional evidence" is vague. I could not picture what a second pass over the same reads would even find. | Name the evidence in that sentence instead of waiting a paragraph. |
| What it is / "which Lungfish Genome Explorer's (LGE's) own" | The possessive with the parenthetical abbreviation inside it made me stop and re-read the whole sentence twice to find the subject. | Introduce LGE in its own short sentence. |
| What it is / "The upstream project does not publish" | "Upstream project" assumes I know there is a separate group publishing TaxTriage. I thought TaxTriage was part of this app. | Say who makes TaxTriage before calling them upstream. |
| What it is / "LGE pins one specific commit of" | I do not know what a commit is. I know what a version is. | Gloss commit as a specific saved version of the pipeline's code. |
| What it is / "and it records that commit in" | Which run folder? At this point in the chapter no folder has been mentioned. | Point forward to the result folder section. |
| Why you would do this / "A survey classifier hands you a" | "Survey classifier" appears here for the first time and is never glossed. Is that a category, or the name of a tool? | Say plainly that this means a tool like Kraken 2 used on its own. |
| Why you would do this / "an organism whose reads all pile" | I could not judge why piling onto a repetitive stretch is bad without being told what a repetitive stretch is. | One clause saying repeats occur in many genomes so reads there could belong to anything. |
| Why you would do this / "and mean depth alongside the count" | Mean depth is used before it is explained. The explanation only arrives much later as "how thickly they stacked", which I still find fuzzy. | Define depth here as the average number of reads covering each position. |
| Why you would do this / "a QIAseq Direct amplicon library of" | Three terms I do not know in one phrase. QIAseq Direct, amplicon library, read pairs. I could not tell whether any of them matter for following the steps. | Gloss amplicon library at minimum, since the chapter later leans on "tiled amplicon library" to explain the result. |
| Why you would do this / "86,281 paired-end Illumina read pairs taken" | Later the Procedure says 83,591 read pairs and the results say 82,234 read pairs assigned. Three different numbers for what I thought was the same thing. I could not tell whether I had misread or whether reads get dropped somewhere. | Say once, near the first number, that filtering removes some pairs before classification. |
| Before you start / "which arrives with the Required Setup" | I do not know what the Required Setup pack is or how to check whether I have it. The chapter says LGE installs it on first launch, but not how to confirm. | Point at where in the app I can see that pack is present. |
| Before you start / "so Docker Desktop must be installed" | The chapter says Docker must be installed and running but never says how to get it or how to tell whether it is running. This is the one prerequisite most likely to stop me and it gets no instructions. | A line saying where Docker Desktop comes from and how to tell it is running. |
| Before you start / "On macOS 26 and later, Apple" | I do not know what Apple Containerization is, whether I have it, or whether I should prefer it. | One sentence saying whether I need to do anything for this. |
| Before you start / "from 0.5 GB for Viral up" | I could not decide which database to pick from size alone, and the paragraph does not say what the others contain. It only tells me this chapter used Viral. | Say in one clause what PlusPF holds that Viral does not. |
| Procedure / "with the pinned pipeline revision `e10bfebda32a62711f38a4e23ab03b61725a9675`" | A forty-character string with no explanation of what I would ever do with it. I did not know whether I had to type it anywhere. | Say I never need to enter it, it is recorded for me. |
| 1. Check the prerequisites / "Click the FASTQ bundle holding the" | "Bundle" is used as if I already know it. In Geneious my reads were just documents in a folder. I did not know whether one bundle means one file or one sample. | Gloss bundle at first use in this chapter. |
| 1. Check the prerequisites / "so picking TaxTriage opens the FASTQ/FASTA" | The menu item and the window title do not match, which made me think I had clicked the wrong thing. The chapter does explain it, but only after I had already been confused. | Put the reassurance before the window name. |
| 1. Check the prerequisites / "one labelled with the name of" | I could not tell what the second indicator would say on my machine before I opened it. The sentence hedges instead of telling me. | Just say it reads Docker unless you are on macOS 26. |
| 1. Check the prerequisites / "The same check runs from the" | I have never opened a terminal, so I did not know whether I am supposed to be able to do this or whether skipping it leaves me worse off. | Mark it optional in the sentence itself. |
| 2. Name the samples / "Rows you add by hand are" | I did not know why I would ever add a row by hand at this point, since I selected my bundle in the sidebar already. | Say what adding a row is for before saying what it is named. |
| 2. Name the samples / "one line for a single-ended sample" | Single-ended is used here but only paired-end gets a glossary link. I had to infer single-ended from the contrast. | Gloss both, or link single-ended too. |
| 2. Name the samples / "It offers Clinical Sample, Negative Control" | Five role choices and only Negative Control is explained. I would not know whether my blank is an Extraction Blank or a Negative Control, and the chapter says choosing wrong costs me the contamination flagging. | One line saying which roles feed the contamination check. |
| 3. Choose the database / "which are the same collections the" | I could not tell whether "the Kraken2 tool" is a different item in the same app or the pipeline step inside TaxTriage. | Name it as the separate Kraken 2 tool in the same menu. |
| 3. Choose the database / "The other two segments are Oxford" | "Segments" for the parts of a picker is app jargon I had to guess at. | Say the control offers three choices. |
| 4. Run it and find / "because Nextflow has to pull the" | I have no idea how long a first run takes. The chapter gives me 72.2 seconds for a cached run and nothing at all for the uncached one, which is the one I will actually hit. | A rough first-run figure, even an order of magnitude. |
| 4. Run it and find / "It is a folder named `taxtriage-<timestamp>`" | I did not know whether the angle brackets are literally part of the folder name. | Show one real example name. |
| Settings / "where their labels carry a trailing" | I read this three times. Explaining a punctuation quirk of the screenshots before any setting is described made me think it mattered more than it does. | Move it to a note after the entries. |
| Settings / "**K2 Confidence:.** Sets how much of" | "How much of a read must match" does not tell me whether that is a fraction of bases or of something else, so I could not judge what moving the slider by 0.05 does. | Say what the fraction is a fraction of. |
| Settings / "Raise it when the organism list" | I do not know what an implausible species looks like in my own data. This assumes I already know what should be there. | Give the SARS-CoV-2 run as the concrete case. |
| Settings / "**Max memory:.** Caps how much memory" | The default is 16 GB and the range goes to 256 GB, but I do not know how to find out how much memory my Mac has, and the chapter warns that setting it too high makes steps fail. | Name where the machine's memory is shown. |
| Settings / "**Extra arguments:.** Passes text straight through" | Since I have never used a terminal, I could not tell whether this field is something I am ever expected to touch. | Say plainly that most users leave it empty. |
| Reading the results / "High Confidence counts the organisms whose" | The card counts organisms above 0.80, but the chapter later says every TASS value is 0.000 on this version. So this card must always read zero and nobody says so. | Say what this card shows while the gap persists. |
| Reading the results / "**Coverage Breadth** and **Coverage Depth** report" | "How thickly they stacked" is the only definition of depth I get, and it is a metaphor. I could not turn it into a number I could judge. | Give depth a unit, as in reads per position. |
| Reading the results / "**Abundance** gives the proportion of the" | Proportion of the library by reads, or by organisms, or by something else? And is it the whole library or only the classified part? | Name what the proportion is taken over. |
| What the TASS score means / "and agreement between the pipeline's steps" | I could not picture what it means for steps to agree or disagree with each other. | One example of two steps disagreeing. |
| What the TASS score means / "0.80 and above, High, A" | The whole band table is unusable on this version because every score is 0.000, and I did not learn that until two sections later. | Flag the gap before the table, not after. |
| The reference run / "at 97.52% of reads, with 163,031" | Earlier I was told there were about 83,000 read pairs. Now 163,031 reads are aligned. I think pairs are being counted as two reads, but I had to work that out myself and I am still not certain. | Say once that a pair counts as two reads in this column. |
| The reference run / "and left 1 read unclassified" | One single unclassified read out of tens of thousands looked like a typo. I could not tell whether that is a real result or a rounding artefact. | A short remark that this is expected for a pure amplicon library. |
| A reporting gap / "while LGE builds the viewport's table" | I understood that the number is broken. I could not tell whether anything else in the viewport is affected, since the summary cards and the batch overview both use the score. | State which views are affected by the gap. |
| A reporting gap / "read the score from the pipeline's" | I am told to open a report in the result folder but not what the file is called or how to find it from inside the app. | Name the file and say Open Report reaches it. |
| Working with a single row / "That view shares the general alignment" | No pointer to where the alignment viewer is documented, and I have never used this app's alignment view. | A link, the way other sections link out. |
| Comparing several samples / "either TASS Score, Total Reads, Unique" | The facet offers "Coverage" but the table has two coverage columns, breadth and depth. I did not know which one the facet shows. | Say which coverage the facet uses. |
| Comparing several samples / "An organism carrying a real score" | Given that all scores are 0.000, I could not tell whether contamination flagging still works on this version. | Say whether the flag survives the reporting gap. |
| What good looks like / "A breadth in the low single" | Low single digits is given as bad and 100 percent as good. I have no idea how to judge 40 percent, which is where real data will probably land. | One sentence on the middle range. |
| What good looks like / "The reference run's row reported 165,500" | The section above said 163,031 reads aligned. Here it is 165,500. Two different numbers for the same run, and I could not tell which one to believe. | Reconcile the two figures. |
| What good looks like / "against 5,690 unique after deduplication, which" | Deduplication appears here for the first time, in the middle of a number, and Reading the results defined Unique Reads as reads that mapped nowhere else. Those are two different meanings for the same column. | Pick one definition of Unique Reads and use it in both places. |
| On the command line / "The whole procedure runs headless" | Headless is never glossed. I guessed it means without the window, but I was guessing. | Gloss it or drop it. |
| On the command line / "`--db ~/.lungfish/databases/kraken2/viral`" | I would not know how to find that path on my own machine, and nothing in the GUI section showed it to me. | Say where the app displays the database path. |
| On the command line / "one of D, P, C, O" | Seven single letters with only S expanded. I know the rank names from genetics but I had to reconstruct the mapping myself and I was unsure about D. | Expand all seven, or say they are the initials of the ranks. |
| On the command line / "reported \"Parsed 15 taxonomy rows, 2" | Fifteen taxonomy rows when the reference run returned exactly one organism. I could not reconcile those two statements. | Say the rows include the parent ranks. |

Learned. Coverage breadth is the number that separates a real detection from a
pile of reads on one repetitive patch, and it is the thing to read first.

Could not do. I could not get Docker installed and running, because the chapter
requires it and never says where it comes from or how to check it.

Liked most. "It is evidence that nothing was asked."
