# Reader report: Running TaxTriage

Reader 3. Pre-med student, English is my second language, I have taken genetics and I have never opened a terminal.

| Location | What stopped me | What would have helped |
|---|---|---|
| What it is, "TaxTriage is a pathogen-detection workflow." | "Workflow" is used here as a thing you run, but in my English a workflow is a way of working, not a program. I did not know if TaxTriage is software I click or a procedure I follow. | Say in the first sentence that it is a program made of several programs run one after another. |
| What it is, "then goes back over the organisms" | I had to read this sentence twice. It is very long and holds four actions plus two glossary terms, so I lost which noun "it" pointed to. | Split the sentence at "read classification". |
| What it is, "The point of the second pass is" | "Pass" as a noun meaning one round of work is new to me. I first read it as a passing grade. | Write "second round", or gloss "pass" at first use. |
| What it is, "in settings where somebody has to decide" | "Settings" here means situations, but two sections later "Settings" is the name of a section about dialog controls. The same word doing two jobs confused me. | Use "situations" here. |
| What it is, "measures how much of that genome the reads reached and how deeply" | I do not know what "how deeply" means for a genome. Deep is a direction, not a measurement I can picture. | One clause saying depth is how many reads sit over the same position. |
| What it is, "treat the acronym as LGE names it" | I could not tell what action this asks of me. It warns me about something but I do not know what I would do wrong. | State the risk plainly, for example that a score of 0.90 does not mean 90 percent certain. |
| What it is, "LGE pins one specific commit of the pipeline" | "Pins" and "commit" are both unknown to me. I have never used version control. | Gloss "commit" as one saved version of the pipeline's code. |
| Why you would do this, "an organism whose reads tile its whole genome" | "Tile" as a verb was unclear. I guessed it means spread evenly but I was not sure. | Gloss it, or write "spread across". |
| Why you would do this, "A survey classifier hands you a list" | "Survey classifier" appears with no introduction. I did not know if this is a category of tool or one specific tool. | Say "a classifier that only surveys, such as Kraken 2". |
| Why you would do this, "a QIAseq Direct amplicon library of 86,281" | I do not know what QIAseq Direct is, and "amplicon library" is not glossed here even though the chapter leans on "amplicon" four more times later. | Gloss "amplicon" once here. |
| Before you start, "which arrives with the Required Setup pack" | I could not tell whether I must do anything. If it installs on first launch, why is it listed as something I need? | Say directly that I normally already have it and only need to check the indicator. |
| Before you start, "On macOS 26 and later, Apple Containerization" | I did not know how to find which macOS I have, and whether Apple Containerization is something I install or something already present. | One sentence saying where to check, and whether any action is needed. |
| Before you start, "from 0.5 GB for Viral up to 72 GB" | I do not know how to judge these numbers. 72 GB sounds enormous for a laptop, but the text does not say whether that is a problem. | Say how much free disk space the large databases need. |
| Procedure lead-in, "the pinned pipeline revision `e10bfebda32a62711f38a4e23ab03b61725a9675`" | I could not do anything with this string, and I did not know whether I must check that mine matches. | Say whether the reader needs to verify this or can ignore it. |
| Step 1.2, "That is the title of the dialog window" | I had to read this twice. It corrects a misunderstanding I had not yet formed, so it created confusion instead of removing it. | Drop it, or move it to a note. |
| Step 1.3, "A filled green dot means the item was found" | I am red-green colour deficient, so I would rather not depend on colour alone, and the text does not say whether another cue exists. | Say whether the indicator also carries text or a symbol. |
| Step 1.4, "Install the missing piece and reopen the dialog" | This is the step I could not perform. Installing Docker Desktop is a whole task, and the chapter gives no link, no download location, and no description of what a working install looks like. | A link to an install page, or a pointer to the chapter that covers it. |
| Step 1 closing, "The same check runs from the command line" | I have never opened a terminal. I did not know whether this line was something I had to do or something optional. | Mark the command-line lines as optional where they first appear. |
| Step 3, "the setting tells the pipeline what error profile to expect" | "Error profile" is unknown to me. I know sequencers make mistakes, but not that the pattern of mistakes has a name. | Gloss it as the typical pattern of sequencing mistakes for that machine. |
| Step 3, "Leave **Skip assembly (faster)** ticked" | "Assemble genomes" and "de novo assembly" are used before either is explained. Only the Settings entry explains it, and that comes later than the step where I must choose. | Gloss assembly here, where the choice is actually made. |
| Step 4, "The reference run of these 83,591 read pairs" | Earlier the chapter said 86,281 read pairs for the same sample. Two different numbers for one library stopped me, and I could not tell which to expect. | Reconcile the two numbers, or explain what removed the difference. |
| Settings lead-in, "their labels carry a trailing colon on screen" | I read this twice. The entries below then appear as "**K2 Confidence:.**" with a colon and a full stop together, which looks like a typing mistake. | Drop the on-screen colon in the manual and note it once. |
| Settings, "**Max memory:.** The default is 16 GB" | I do not know how much memory my Mac has or how to find out, so I cannot judge whether 16 is safe. | Say where to look up the machine's memory. |
| Settings, "**Extra arguments:.** Passes text straight through" | I could not do anything with this. I do not know what an argument is in this sense. | Say plainly that most readers should leave it empty. |
| Reading the results, "High Confidence counts the organisms whose TASS score reached 0.80" | The chapter later says every TASS score is 0.000 on this version, so I could not tell whether this card is always zero. | Say what this card shows while the reporting gap lasts. |
| Reading the results, "**Abundance** gives the proportion of the library" | Proportion of the library measured how, by reads or by something else, and is it a percentage or a fraction? | Name the units. |
| What the TASS score means, "agreement between the pipeline's steps" | I do not know what it means for steps to agree or disagree with each other. | One example of a disagreement. |
| TASS table, "0.40 up to 0.80, Medium" | I could not tell whether exactly 0.80 is Medium or High. The first row says "0.80 and above" and this row says "up to 0.80", so 0.80 appears in both. | Write the boundary once, for example "0.40 to below 0.80". |
| The reference run, "at 97.52% of reads, with 163,031 reads aligned" | The numbers do not fit together for me. 86,281 pairs cannot obviously give 163,031 aligned reads and also 82,234 assigned pairs, and I could not work out which counts are of pairs and which of single reads. | State for each number whether it counts reads or pairs. |
| The reference run, "a mean depth of 1249.7" | I do not know whether 1249.7 is good, ordinary, or suspiciously high. The chapter judges breadth for me but never judges depth. | Say what depth range is normal for an amplicon run. |
| A reporting gap, "the pipeline's own report scored at 99" | Everywhere else the score runs 0 to 1, and here 99 appears. I had to stop and work out that these are two different scales. | Write "99 out of 100" the first time. |
| A reporting gap, "read the score from the pipeline's own report in the result folder" | This is the second thing I could not do. I do not know how to open the result folder, which file inside it is the report, or where in that file the score sits. | Name the file and say how to open it. |
| Working with a single row, "That view shares the general alignment viewer's controls." | No link is given, so I did not know where those controls are described. | Link to the alignment chapter. |
| Working with a single row, "the whole row as TSV" | TSV is not glossed here. I have met CSV in class, TSV I have not. | Gloss TSV at first use. |
| Comparing several samples, "a value-facet control above it" | "Value-facet" means nothing to me and I could not picture the control. | Describe it as a picker that chooses what the cells show. |
| Comparing several samples, "TaxTriage flags such rows rather than subtracting them" | "Subtracting them" was unclear. Subtracting what from what? | Say that it does not remove the reads or the row automatically. |
| What good looks like, "reported 165,500 reads aligned against 5,690 unique" | This disagrees with the reference run section above, which said 163,031 reads aligned for the same row. Two numbers for one row stopped me completely. | Use one number in both places. |
| What good looks like, "which for a PCR-amplified library is expected" | The section had just told me a small unique fraction means the evidence is shared and the call is weak, then says here that a small unique fraction is expected and fine. I could not reconcile the two rules. | Say that the unique-read rule does not apply to amplicon libraries. |
| On the command line, "A single sample goes in behind `--input`" | I have never opened a terminal, so this whole section was unusable for me, including where I would type any of it. | A single line saying the section is optional and needs the Terminal app. |
| On the command line, "--input extract_1_R1.fastq.gz" | The file is called `extract_1_R1.fastq.gz` here, but the sample is SRR36291587 and Before you start told me to download SRR36291587. I did not know where `extract_1` came from. | Use the same file names throughout the chapter. |

The one thing I learned is that a read count on its own cannot tell a real detection from reads piled on one repetitive stretch, and that coverage breadth is the number that separates them.

The one thing I still could not do is install Docker Desktop, so I could never get past the Prerequisites row to run the worked example at all.

The sentence I liked most is "It is evidence that nothing was asked."
