# Reader report: Running EsViritu

Reader 1. Sophomore, one genetics course, no lab time, never opened a terminal.

| Location | What stopped me | What would have helped |
|---|---|---|
| What it is / "It takes the reads from a sequencing run" | "Sequencing run" is never explained. I know what a read is now because you told me, but I do not know if a run is one sample, one machine load, or one day of work. | Say in half a sentence that a sequencing run is one machine load producing the reads from one or more samples. |
| What it is / "curated collection of viral genomes" | "Curated" sounds important but I cannot tell what it excludes. Curated by whom, and does that mean it is complete? | Say who curated it and that curation means hand-checked rather than complete. |
| What it is / "The collection EsViritu compares against" | I do not know whether 19,925 assemblies is a lot. Are there twenty thousand viruses known, or two million? The number means nothing without a denominator. | Add a clause saying roughly what fraction of known viruses that covers. |
| What it is / "The tool that does the mapping" | I could not tell whether minimap2 is something I install, something already inside EsViritu, or something I run myself. | Say that minimap2 runs inside EsViritu and you never call it yourself. |
| What it is / "The second is what a shared" | "Off-target PCR product" is new. I have heard of PCR in lecture but I could not picture what makes it look like a spike. | One clause saying the PCR accidentally amplified the wrong stretch, so reads pile on that one stretch. |
| What it is / "The second is what a shared" | Read twice. I know conserved means similar across species, but I did not follow why that produces a spike on one genome rather than spreading out. | Say the reads truly come from a relative but land on the only stretch the two genomes share. |
| What it is / "A run writes a table of" | "Indexed alignment file" was not explained and I did not know if I have to do the indexing myself. | Say indexed means it carries a lookup table so the viewer can jump around, and LGE makes it for you. |
| Why you would do this / "That is often as far as" | I did not know databases had a size budget that trades off this way. I read this twice before it landed. | State plainly that a database of fixed size must drop viral detail to hold bacteria and human. |
| Before you start / "The reads themselves are too" | I do not know what the Sequence Read Archive is on first meeting it here. It is not glossed and the link goes to a procedure, not a definition. | Gloss the SRA in one clause as NCBI's public store of raw sequencing reads. |
| Before you start / "A plugin pack is a" | "Conda environment" is a link but the sentence gives me nothing. I have never installed software from a command line and cannot picture a private environment. | One clause saying each tool gets its own isolated copy of its software so tools cannot break each other. |
| Before you start / "Open Tools > Plugin Manager..." | I could not perform this. It does not say what to click once the Plugin Manager is open, though step 1 below names a tab and a button for the database. | Name the tab and the button for installing the pack, the way step 1 does for the database. |
| 1. Install the viral database / "Plan for at least 8" | I do not know how to find out how much memory my Mac has, and I do not know if 8 GB is the whole machine or free space. | Say where to check it, for example About This Mac, and whether the 8 GB is total or free. |
| 1. Install the viral database / "Outside the app, lungfish-cli esviritu db-status" | I have never opened a terminal, so I could not tell whether this paragraph is something I am expected to do or something I may skip. | Say explicitly that this line is optional and the app already shows the same facts. |
| 2. Open the dialog / "Pairing comes from the way" | I could not act on this. It tells me to fix the selection in the sidebar but not what a correct paired selection looks like. | Say what to select so a pair is recognised, for example both mate files together in one bundle. |
| 2. Open the dialog / "A warning may appear under" | This paragraph arrives after step 5 and mixes a memory warning with a batch mode I did not ask for. I had to reread it to see it was two unrelated notes. | Split the batch note out under its own lead-in line. |
| Settings / "Min read length:. Discards reads" | The doubled punctuation in "length:." made me stop and check whether text was missing. | Drop the stray period after the colon, or explain the label convention once. |
| Settings / "The default is 100 bases" | I did not know what a stepper is. I guessed a box with little arrows but I was not sure. | Gloss stepper once as a number field with up and down arrows. |
| Settings / "Untick it when the reads" | I cannot tell whether my downloaded SRA reads were already trimmed. Nothing tells me how to check. | Say that reads fetched from the SRA are usually untrimmed, so leave the box ticked. |
| Settings / "Extra arguments:. Passes text straight" | I do not know where the EsViritu documentation lives, so I could not use this even if I wanted to. | Point at that documentation, or say plainly to leave this empty. |
| Reading the results / "RPKMF is reads per kilobase" | Read three times. I followed each division separately but I still cannot say what 32,022.4 means or what counts as high. | Give a rough anchor, for example what an RPKMF near zero versus in the tens of thousands implies. |
| Reading the results / "Identity is the average percent" | The definition uses the word identity to define Identity. I worked it out from a later paragraph, not from here. | Say identity is the percent of bases that match, averaged over the mapped reads. |
| The reference run / table row "Segment" | The cell literally reads "em dash" rather than showing a dash. I thought it was a placeholder someone forgot to fill in. | Show the character, or write "no segments" in the cell. |
| The reference run / "Detecting viruses in the SRR36291587" | The table has no Unique Reads value, but the column list promised one and "What good looks like" tells me to compare Reads against Unique Reads. I could not do the comparison the chapter asks for. | Add the Unique Reads figure to the reference run table. |
| The reference run / "The per-window coverage file divided" | I did not know a window was a chunk of the genome until this sentence, and it is the first use. | Gloss window once as an equal slice of the reference. |
| The reference run / "The per-window coverage file divided" | I do not know how to judge 319.4 reads deep. Is 30 fine? Is 3 fatal? The chapter tells me to read depth but never gives a floor. | Give a rough threshold below which a window counts as thin. |
| The detail pane / "For a segmented virus, one" | I could not picture the completeness grid, and there is no screenshot. The worked example deliberately has no segments, so I never see one. | A one-line description of what a filled versus an empty cell looks like. |
| Auditing a detection / "The viewer opens the indexed" | "Read packing" is listed as if I already know it, and it is not glossed. | Gloss read packing as stacking reads into rows so they do not overlap. |
| Auditing a detection / "It reads Structurally validated reference" | "M5" is never expanded and "sequence checksums" is new. I could not tell what to do if I saw the weaker of the two states. | Expand M5 once and say whether the weaker state needs any action from me. |
| Acting on a row / "A batch result covering several" | I could not tell whether I should press Recompute Unique Reads, or whether missing values are normal and harmless. | Say whether recomputing is routine or only needed when an ellipsis appears. |
| On the command line / "The whole procedure runs headless." | I have never opened a terminal, so I could not tell where this section stops being optional. The earlier CLI aside at least sat inside a step. | Open the section with one line saying the app already did all of this. |
| On the command line / "Its toolVersion field, and the" | This is described as a current defect, but I could not tell whether the app's provenance panel, which I was told to trust earlier, shows the same wrong version. | Say whether the in-app provenance carries the same wrong version. |

One thing I learned. Depth and breadth are different, and a big coverage number can still hide a genome that was mostly never read, which is why the little sparkline matters more than the number beside it.

One thing I still could not do. Judge whether a detection's numbers are good, because the chapter gives one worked example where everything is excellent and no threshold anywhere for what counts as too few reads, too thin a window, or too low an identity.

The sentence I liked most. "Two hundred reads spread evenly along a 30,000-base viral genome and two hundred reads stacked on one 300-base stretch produce the same read count and mean completely different things."
