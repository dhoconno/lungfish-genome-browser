# Reader 3 report, Variants and VCF Files

Persona. Pre-med student, English is my second language, I took genetics, I have never opened a terminal.

| Location | What stopped me | What would have helped |
|---|---|---|
| What it is, "A variant is a position on..." | "Disagree with the reference base" sounds like an opinion. I did not know if the reads are wrong or the reference is wrong. | Say in one sentence that the reference is only one person's genome and difference does not mean error. |
| What it is, "It might be an insertion of..." | I know insertion and deletion from lecture, but the chapter never gives the short word "indel" here, and later the FILTER table uses `min_indelqual_20` and the chip is called **Indel**. I had to guess they are the same thing. | Give the word indel in the first paragraph where insertion and deletion appear. |
| What it is, "walks the reference position by position" | I could not picture "walks". Is this one position at a time in order, or all at once? | Replace "walks" with a plain verb such as "moves along". |
| What it is, "examines the pileup at each one" | Pileup is linked but never glossed inside this chapter. I did not know what a pileup is while reading. | Add four words after it, such as "the stack of reads covering that position". |
| What it is, "applies its evidence thresholds" | Threshold is used many times before it is explained. I know the English word but not what a threshold is here, a number or a rule. | One clause saying a threshold is the minimum value a number must reach. |
| What it is, "The meaning of a field name like" | `AF` and `GT` appear before either is defined. `GT` is only explained two sections later. | Move this caveat after the column tables, or spell out the two names once here. |
| Why you would do this, "from consensus building to lineage" | Consensus building and lineage assignment are both new to me. Neither is glossed or linked. | Gloss both in half a sentence, or drop them. |
| Why you would do this, "the Genome in a Bottle project has" | I did not know Genome in a Bottle, or what characterised means for a sample. | One clause saying it is a public project that produced a trusted answer set for this person's genome. |
| Why you would do this, "a 500,001-base slice of human" | The odd number 500,001 made me read twice and wonder whether it was a typo. | Say why it is one base more than 500,000, or round the description. |
| Before you start, "the demo project's chr20 10.0-10.5Mb bundle" | A demo project is mentioned as if I already have one. I did not know if it installs with LGE or I must build it myself. | Say where the demo project comes from. |
| Before you start, "Calling variants needs the variant-calling plugin" | Plugin pack is explained but not how I get one. The sentence never tells me where to click. | Point to the chapter or the menu where a pack is installed. |
| Before you start, "The two caller VCFs are rebuilt by" | I could not tell whether this means the files are missing when I download, so I cannot follow along. | Say plainly whether the reader can open these two files after downloading. |
| What a VCF file looks like, "Header lines begin with two hash" | Later the FILTER section says flags "are declared in each file's header". I did not know how to look at a header without a terminal. | One sentence saying whether LGE shows the header anywhere. |
| What a VCF file looks like, "The compression is bgzip, a block" | Block form of gzip and "jump into the middle of the file" was the hardest sentence in the chapter for me. | Split it into two sentences and say why jumping matters. |
| What a VCF file looks like, "a CSI index serves the same purpose" | I did not know how long "too long" is, so I cannot tell which index my own file needs. | Give the approximate length limit, or say LGE picks for you. |
| What a VCF file looks like, "An optional SQLite sidecar sits alongside" | Sidecar and SQLite are both unknown to me, and "optional" made me worry I have to choose. | Say it is a helper file LGE makes by itself. |
| The eight standard columns, "QUAL is a Phred-scaled confidence" | I could follow that 20 means 1 percent and 30 means 0.1 percent, but then bcftools writes 222.235 and LoFreq writes 1093 for the same variant. If it is a probability, why are they so different? The text says not comparable but never why. | One clause saying the two callers scale the number differently even though both call it Phred. |
| The eight standard columns, "A real indel row from the fixture's" | I counted `CTTTTT` as six and `CTTTTTT` as seven and the text agrees, but I still could not see which T is the inserted one. | Say the anchor base is the leading C and the extra base is at the end. |
| The eight standard columns, caller table, "none at all" | The cell says none at all under a column headed FORMAT and sample columns. For a moment I thought the file was broken. | Write "no FORMAT column and no sample column" in the cell. |
| The eight standard columns, "DP, AF, SB, and DP4 in INFO" | `SB` appears in this table before strand bias is explained, which happens only in the FILTER section. | Gloss `SB` where it first appears. |
| The eight standard columns, "iVar adds two more keys, MERGED_AF" | The codon-merging step is new, and the sentence assumes I know what a codon merge does. | Say in one clause that it joins changes inside one codon into one row. |
| The eight standard columns, "LGE's iVar lane writes the bare" | "Lane" is used with no explanation. I do not know if a lane is a menu, a pipeline, or a sequencing lane. | Use a plain word such as "LGE's iVar pipeline". |
| Walking through one row, "Take position 250,527, the heterozygous" | The `C>T` notation for a change is used here for the first time without saying what the arrow means. | Say "C changed to T" the first time. |
| Walking through one row, code block starting "chr20_10.0-10.5Mb 250527" | The row is split across three lines in the block and I could not tell where a real tab ended and the wrapping began. | A note in the block, or call-out numbers, showing where the columns actually break. |
| Walking through one row, "The INFO field packs a dozen keys" | Nine of the twelve INFO keys are never explained and the chapter says only three matter. I did not know whether I may ignore the rest forever. | One sentence saying the others are caller internals a reader may ignore. |
| Walking through one row, "PL holds Phred-scaled likelihoods for" | For `PL` the middle value of 0 marks the best genotype, so lower is better, but for `QUAL` higher is better. I read this three times. | Say explicitly that for `PL` the smallest number is the best. |
| Walking through one row, "Twenty plus thirty-three is 53 rather" | The `DP4` counts 8,12,16,17 also sum to 53, but `AD` says 20 and 33. I could not tell whether `DP4` and `AD` count the same evidence in two ways. | One clause linking `DP4` to `AD`. |
| Walking through one row, "an allele frequency of AF=0.571429" | For a heterozygous site I expected 0.5. The chapter never says a small departure from 0.5 is normal. | Say sampling makes the value scatter around 0.5. |
| Walking through one row, "a strand-bias score of SB=0 where" | I do not know the range of `SB`, so I cannot judge any value that is not zero. | Give the direction, that a higher number means more bias. |
| FILTER, "Every one of the 1,056 rows in" | If a default LGE bcftools run judges nothing, I did not know what I am supposed to do next inside the app. | Name the chip or the setting that applies a filter afterwards. |
| FILTER, "min_snvqual_73, The substitution's QUAL fell" | SNV appears here and later as a chip but is never spelled out. I guessed single nucleotide variant. | Spell out SNV at first use. |
| FILTER, "sb_fdr, The row failed a strand-bias" | "A correction for testing many positions at once" was too compressed, and FDR is never expanded. | Expand FDR once and keep the plain explanation. |
| FILTER, "ft marks a row that failed a" | A p-value above 0.05 means fail here, but in my statistics class a p-value above 0.05 means we do not reject. This felt backwards and I could not resolve it. | One clause saying what the test's null hypothesis is. |
| FILTER, "Amplicon protocols pile strand-imbalanced reads near" | Amplicon and primer are used as known words. I have never done library preparation. | Gloss amplicon in four words. |
| Where a VCF comes from, "a primer-trimmed BAM and an untrimmed" | Primer trimming is assumed. I did not know it is a step somebody performs on purpose. | Point to the chapter that covers trimming. |
| Where a VCF comes from, "the row count grows by an" | Order of magnitude and "noise" are both idioms I had to translate. | Say "about ten times as many rows, and most of them are false". |
| Where a VCF comes from, "954 of bcftools' 1,053 distinct positions" | Earlier the chapter says 1,056 rows and here it says 1,053 distinct positions. I could not tell if this is an error or if three rows share positions. | Say why three rows do not add a new position. |
| Where a VCF comes from, "a real accuracy assessment needs a" | `hap.py` is named with no explanation and I cannot run it without a terminal. | Say whether LGE offers this or it lives outside the app. |
| Where a VCF comes from, "Medaka and Clair3 for Oxford Nanopore" | Oxford Nanopore is not glossed here. An earlier chapter may cover it, but I was told every chapter glosses its own terms. | Four words saying it is a long-read sequencing platform. |
| Reading a variant track in LGE, "Variants live in the Variants tab" | Table drawer, reference bundle, and viewport all appear in one line and none of them is glossed here. | Gloss viewport and drawer once, or put the screenshot directly above this sentence. |
| Reading a variant track in LGE, "and the protein consequence where iVar's" | Protein consequence is a new idea and I did not know what values it can take. | Give one example value. |
| Reading a variant track in LGE, "High Impact and Moderate+ filter on" | Impact and Moderate+ are category names from some tool I do not know, and the chapter never says which categories exist. | Name the levels once, or say where they come from. |
| Reading a variant track in LGE, "Rare (<1%), Qual >= 30, DP" | The word "obvious" told me I should already understand. Rare below 1 percent of what population? ClinVar is never glossed. | Do not say obvious, and gloss ClinVar. |
| Reading a variant track in LGE, "and the sheet combines the rules" | I did not know whether Match All is fixed behaviour or a control I could change to Match Any. | Say whether any other choice exists. |
| What good looks like, "Across this fixture the mean is" | The mean depth is 44.7 and the threshold is about 10. I could not tell whether 44.7 itself is a good number for a project of my own. | Say what mean depth a human sample should reach. |
| What good looks like, "A position under about 10 is" | The words "in a human sample" imply a different number for other samples, but the chapter never gives it. | Give the other case, or drop the qualifier. |
| What good looks like, "If the first fails, sort the" | I did not know what I am looking for in that distribution once I have sorted. | Say what a healthy distribution looks like. |
| On the command line, "running against a bundle-owned alignment track" | Bundle-owned is a new compound and I could not tell it apart from the plain word bundle. | Use "an alignment track stored inside a bundle". |
| On the command line, "This section is optional and nothing" | The section says nothing depends on it, yet the two VCFs the whole chapter quotes were produced here. That felt contradictory. | Say the reader can obtain the files another way, or drop "nothing depends on it". |

One thing I learned. A bare dot in the FILTER column does not mean the variant passed. It means nobody judged it, and the fixture shows a whole file of 1,056 rows in exactly that state.

One thing I still could not do. Take a variant track of my own inside LGE and decide whether a flagged row is a real difference or an artifact, because I do not know how to open the alignment viewport at that position or what I should be looking at when I get there.

The sentence I liked most. "Read that 0.57 as the answer to 'how much of the evidence backs `T`', and the `0/1` in the bcftools row as the answer to 'how many chromosome copies carry `T`'."
