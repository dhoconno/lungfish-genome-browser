# Fidelity review: 01-foundations/05-variants-and-vcf.md

Reviewed 2026-09-06 against the committed HG002 chr20 fixture VCFs, the Swift
source under `Sources/`, the `variants` CLI help dump, and `GLOSSARY.md`.
`LUNGFISH_MANUAL_STRICT=1 lint-chapter.sh` reports no issues.

Note on the fixture. `git ls-files docs/user-manual/fixtures/hg002-chr20/expected`
returns four files, so `HG002.bcftools.vcf.gz`, `HG002.lofreq.vcf.gz`, and both
`.tbi` indexes ARE committed as of today. Any sentence in the chapter or the
fixture README saying they are gitignored or must be regenerated is stale.

| Claim | Verdict | Evidence | Correction if false |
|---|---|---|---|
| "The fixture's bcftools VCF carries 29 header lines" | false | `bcftools view -h ... \| grep -c '^##'` returns 30. Counting the `#CHROM` line too gives 31. Neither is 29 | "carries 30 header lines" |
| "Its LoFreq VCF carries 18 header lines" | false | `grep -c '^##'` returns 19. With `#CHROM` it is 20 | "carries 19 header lines" |
| "The Genome in a Bottle VCF for this slice carries 235 header lines above its 961 rows" | true | `grep -c '^##'` returns 235; `bcftools view -H \| wc -l` returns 961 | |
| "1,056 variant rows" (bcftools) | true | `bcftools view -H` returns 1056 | |
| "862 variant rows" (LoFreq) | true | `bcftools view -H` returns 862 | |
| The quoted bcftools row at 250,527 (`C`, `T`, `222.235`, `.`, the full INFO string, `GT:PL:AD`, `0/1:255,0,255:20,33`) | true | Byte-for-byte identical to line 604 of the decompressed stream | |
| "the author reports the bcftools row at line 604" | true | `gzcat ... \| sed -n '604p'` is that exact row | |
| The quoted LoFreq row `chr20_10.0-10.5Mb 250527 . C T 1093 PASS DP=63;AF=0.571429;SB=0;DP4=13,14,17,19` | true | Byte-for-byte identical to line 483 of the decompressed stream | |
| The indel "with `REF` reading `CTTTTT` and `ALT` reading `CTTTTTT` at position 5,839" | true | The record is `chr20_10.0-10.5Mb 5839 . CTTTTT CTTTTTT 221.501 . INDEL;...` | |
| "the indel at line 33" (author's line reference) | false | The indel record is at line 43 of the decompressed stream, not 33. Line 33 is the SNV `chr20_10.0-10.5Mb 2333 . T C`. The chapter body does not print a line number, so the prose is unaffected; only the author's note is wrong | Author's note should read line 43 |
| "The benchmark VCF calls it too, with `GT` `0/1` and its own depth of 1,247" | true | Line 734 of the decompressed benchmark stream ends `GT:PS:DP:ADALL:AD:GQ 0/1:.:1247:175,167:335,335:553` | |
| "Every one of the 1,056 rows in the bcftools VCF has `FILTER` set to `.`, and none say `PASS`" | true | FILTER tally over column 7 is exactly `{. : 1056}` | |
| "Every one of the 862 rows in the LoFreq VCF says `PASS`" | true | FILTER tally is exactly `{PASS : 862}` | |
| "954 of bcftools' 1,053 distinct positions ... match a position in the benchmark" | true | 1053 distinct POS values; `comm -12` against the 961 benchmark positions gives 954 | |
| "808 of LoFreq's 861 ... match a position in the benchmark" | true | 861 distinct POS values; `comm -12` gives 808 | |
| The four LoFreq FILTER names `min_snvqual_73`, `min_indelqual_20`, `min_dp_10`, `sb_fdr` | true | The file's five `##FILTER` lines are `PASS`, `min_dp_10`, `sb_fdr`, `min_snvqual_73`, `min_indelqual_20`. The four named are the four non-PASS ones | |
| "`min_indelqual_20` The same test for an insertion or deletion, at a fixed threshold of 20" | unverifiable | The header reads `Minimum Indel Quality (Phred) 20`. Nothing in `Sources/` sets or varies this value, so whether 20 is fixed by LoFreq or merely happened to be 20 on this run cannot be settled from the repo. Running LoFreq on a second dataset and comparing the declared threshold would settle it | |
| "`sb_fdr` The row failed a strand-bias check after a correction for testing many positions at once" | true | Header description reads `Strand-Bias Multiple Testing Correction: fdr corr. pvalue > 0.001000` | |
| iVar's `ft` "fires when that test returns a p-value above 0.05" and is "a statistical test, not a plain frequency cut-off" | true | `IVarTSVToVCFConverter.swift:122` declares `ft` as `Fisher's exact test of variant frequency compared to mean error rate, p-value > 0.05` | |
| iVar's `bq` "where the average quality of the alternate-supporting bases fell below 20" | true | `IVarTSVToVCFConverter.swift:123` and `:225-227` gate on `row.altQual < options.badQualityThreshold`, whose default is 20 (`BundleVariantCallingModels.swift:61`). The chapter states 20 as a flat fact where it is a user-settable default, but 20 is what ships | |
| "Inside an LGE bundle the variant track is stored as a BCF ... with a CSI index beside it under the bundle's `variants/` folder" | false | `BundleVariantTrackAttachmentService.swift:71-74` writes `variants/<name>.vcf.gz`, `variants/<name>.vcf.gz.tbi`, and `variants/<name>.db`. `VariantTrackInfo` (`BundleTracks.swift:380-405`) carries `path`, `indexPath`, and an optional `databasePath`. The committed fixture output is `HG002.bcftools.vcf.gz` + `.tbi` + provenance, with no `.bcf` anywhere. The BCF/CSI layout appears only in an aspirational doc comment at `BundleManifest.swift:88-91` and in a `ReferenceBundle` error string for imported BCF-only tracks | "Inside an LGE bundle the variant track is stored under the bundle's `variants/` folder as a bgzip-compressed VCF with a tabix index, alongside a SQLite database that indexes the same rows so the Variants tab can filter a large track quickly. LGE can also read an imported BCF with a CSI index, though it cannot yet query one by region" |
| "An optional SQLite sidecar sits alongside them and indexes the same rows so the Variants tab can filter a large track quickly" | true | `databasePath` is optional in `VariantTrackInfo`, and `ReferenceBundle.getVariants` (line 464 onward) queries the SQLite sidecar as its fast path. The sidecar is only optional for imported bundles; every track LGE writes gets one | |
| "When an operation needs a VCF ... LGE writes one from the stored BCF" | false | Follows from the storage error above. The stored payload is already a VCF, so nothing is written from a BCF | "When an operation needs a loose VCF, for an export or a downstream tool or a colleague, LGE copies or filters the stored one. On import it reads VCFs in through the same path" |
| "LGE offers five variant callers. LoFreq, iVar, and bcftools for short reads, and Medaka and Clair3 for Oxford Nanopore reads" | true | `BundleVariantCallingModels.swift:6-10` defines exactly `lofreq`, `ivar`, `medaka`, `bcftools`, `clair3`, and `cli-help/variants.txt` lists the same five under `--caller` | |
| "The variant-calling dialog exposes a minimum depth, a minimum allele frequency, a Medaka model ..., iVar's consensus and codon-merge thresholds, and an advanced-arguments field" | true | `BundleVariantCallingRequest` carries `minimumDepth`, `minimumAlleleFrequency`, `medakaModel`, `ivarConsensusAF`, `ivarMergeAFThreshold`, `advancedArguments`; the CLI mirrors each | |
| "LGE's iVar lane writes the bare haploid `1` for a called variant rather than the diploid-shaped `1/1`" | true | `IVarTSVToVCFConverter.swift:201` builds the sample field as `"1:\(row.totalDP):..."` | |
| iVar FORMAT `GT:DP:REF_DP:REF_RV:REF_QUAL:ALT_DP:ALT_RV:ALT_QUAL:ALT_FREQ` | true | `IVarTSVToVCFConverter.swift:189` is that exact string | |
| "iVar adds two more keys, `MERGED_AF` and `MERGED_DP`, when its codon-merging step fires" | true | `IVarTSVToVCFConverter.swift:192-198` appends `:MERGED_AF:MERGED_DP` only when `variant.kind == .merged` and `rows.count > 1` | |
| "bcftools ... `GT:PL:AD` with one sample column" and "LoFreq ... none at all" | true | The bcftools fixture row carries `GT:PL:AD`; the LoFreq fixture's `#CHROM` line ends at `INFO` with no FORMAT and no sample | |
| "iVar disables strand-bias filtering by default ... and the LGE iVar dialog ships with 'Ignore strand bias' already switched on" | true | `BundleVariantCallingModels.swift:62` defaults `ivarIgnoreStrandBias: Bool = true`; the toggle at `BAMVariantCallingToolPanes.swift:179` reads "Ignore strand bias (recommended for amplicons)". The chapter's short label omits the parenthetical, which is acceptable paraphrase | |
| "Variants live in the Variants tab of the table drawer at the bottom of a reference bundle viewport" | true | `AnnotationTableDrawerView+Columns.swift:78` gates on `activeTab == .variants`; matches the CONSISTENCY.md required phrasing | |
| "one row per VCF row and one column per VCF field, plus derived columns for the source caller, the gene name from any attached GFF3 annotation, and the protein consequence" | false | `variantColumnDefs` (`AnnotationTableDrawerView+Columns.swift:312-323`) is ID, Type, Chrom, Position, Ref, Alt, Quality, Filter, Samples, Source, Consequence, AA Change. There is no column per VCF field, only promoted INFO keys added dynamically. The Gene column is a promoted INFO key (`promotedInfoKeyPatterns` at :528 matches `GENE`, `ANN_Gene`, `CSQ_SYMBOL`), so it comes from the VCF's own INFO, not from an attached GFF3 annotation | "one row per VCF row across a fixed set of columns, ID, Type, Chrom, Position, Ref, Alt, Quality, Filter, Samples, Source, Consequence, and AA Change, with extra columns added for the INFO keys the loaded track declares. `AF`, `Gene`, and `Impact` are promoted to the front of those when the file carries them" |
| "The genome track along the top draws each variant as a tick at its `POS`" | unverifiable | `VariantTrackRenderer` draws a summary bar and genotype rows (`SequenceViewerView+Rendering.swift:268,298`), but whether the top track renders one tick per POS is a visual property this review cannot settle from source alone. A screenshot of the Variants tab on the fixture would settle it | |
| "the fastest way to reach confident calls is the **PASS** chip, which hides every row whose `FILTER` is anything but `PASS`" | true | `SmartToken.passOnly` has label `"PASS"` (`SmartFilterTokens.swift:52`) and is always available because "FILTER column always exists" (:121) | |
| "Filter chips sit above the table" | false | The chips are behind a disclosure button. `AnnotationTableDrawerView+Columns.swift:69` sets `presetFiltersToggleButton.title` to `"Presets ▾"` or `"Presets ▸"`, and the button itself is hidden entirely when the toolbar is at minimal density, when `infoColumnKeys` is empty, or when the database is in materialized-only mode (:104) | "A **Presets** button above the table reveals the filter chips" |
| "**SNV** and **Indel** split by variant type and only one can be on at a time" | true | Both return `exclusivityGroupKey` `"variant-type"` (`SmartFilterTokens.swift:99-101`) | |
| "**High Impact** and **Moderate+** filter on a predicted-consequence field" | true | Labels confirm at `SmartFilterTokens.swift:55-56`; both gate on `impactKeys` (IMPACT, ANN_IMPACT, CSQ_IMPACT) at :129. They are also mutually exclusive (`"impact-tier"`), which the chapter does not mention but does not misstate | |
| "**Rare (<1%)**, **Qual >= 30**, **DP >= 10**, and **ClinVar Path.**" | true | Labels at `SmartFilterTokens.swift:57-60` are `Rare (<1%)`, `Qual ≥ 30`, `DP ≥ 10`, `ClinVar Path.` | |
| "A chip appears only when the track carries the field it needs, so a file with no annotation shows no impact chips" | true | `isAvailable(...)` at `SmartFilterTokens.swift:112-146` gates each token on `infoKeys` / `variantTypes`, and `unavailabilityReason` returns "Requires SnpEff/VEP annotation (IMPACT field not found)" | |
| The chip list as a complete enumeration (nine chips) | false | `SmartToken` has fourteen cases (`SmartFilterTokens.swift:15-30`). The chapter omits **Het Only**, **Bookmarked**, and the three within-sample frequency chips **Minor (≤20%)**, **Mixed (20-80%)**, **Dominant (≥80%)**. **Het Only** never appears (`isAvailable` returns `false` for `.heterozygous` at :139) so omitting it is defensible, but Bookmarked and the three frequency chips do appear and are undocumented | Add the bookmarked chip and the three within-sample frequency chips, which appear for haploid organisms with genotype data |
| "Four saved combinations ship built in ... **Clinical** combines PASS with ClinVar pathogenic. **Research** turns on the rare-variant chip alone. **QC** pairs quality at or above 30 with depth at or above 10. **High Confidence** combines PASS, quality at or above 30, and high impact." | true | `FilterProfileManager.swift:38-70` defines exactly those four with exactly those token sets, and `builtInProfiles` holds four | |
| "Any set of chips you assemble yourself can be saved as a named custom profile, stored per bundle" | true | `FilterProfileStore.saveCustomProfiles` (:88) writes to a UserDefaults key suffixed with the bundle identifier (:77-80) and filters out built-ins | |
| "the Variant Query Builder sheet ... Each rule picks a category, then a field inside it, then an operator and a value" | true | `QueryCategory.fields` and `.operators(for:)` in `QueryRule.swift:113-157` supply exactly that cascade | |
| "the sheet combines the rules with Match All" | true | `VariantQueryBuilderSheet.swift:27` initialises `logic = .matchAll`; `QueryLogic.allCases` exposes only `matchAll` (`QueryRule.swift:167`); `applyQuery` joins clauses with `"; "` and logs a warning if Match Any is ever requested (:245) | |
| "The categories are Location, Variant Identity, Biological Effect, Population/Frequency, Call Quality, Sample/Genotype, and INFO Field" | true | `QueryCategory.displayName` (`QueryRule.swift:103-112`) gives those seven strings, and `allCases` (:96) returns them in that order | |
| "the last of those offers every `INFO` key the loaded track actually declares" | true | `effectiveFields(for:)` sets `fields = availableInfoKeys.sorted()` for `.infoField` (`VariantQueryBuilderSheet.swift:503`) | |
| "A query you expect to reuse can be saved as a preset from the same sheet" | true | `savedPresets` and `SavePresetDialogView` in the same file | |
| "Filtering is a view and never a rewrite. Rows hidden by a chip or a query stay in the underlying BCF" | false | Only the "BCF" word is wrong, and it follows from the storage error. The behaviour claim is true, since filtering runs as SQLite queries and display-state updates | "stay in the underlying track" |
| Inspector "shows the identifier and the variant type, the position as `contig:position`, the alleles as `REF` to `ALT`, the quality and the filter value" | true | `VariantSection.swift` `variantIdentity` renders ID, Type badge, `"\(variant.chromosome):\(variant.start + 1)"`, and `"\(ref) → \(alt)"`; `qualityAndFilter` renders Quality and Filter | |
| Inspector shows "a genotype summary with the alternate allele frequency where the file carries genotypes" | true | `genotypeSummary` is rendered only `if viewModel.hasGenotypes`, and shows Hom Ref / Het / Hom Alt / No Call badges plus an "Alt Allele Freq" bar from `alleleFrequency` | |
| Inspector shows "the `INFO` fields broken out one row per key" | true | `infoSection` iterates `viewModel.infoFields` one `HStack` per key. It caps display at the first 20 keys and then prints "... and N more", which the chapter does not mention | |
| "Long `INFO` strings such as the benchmark VCF's are far easier to read here than in the table" | true | The benchmark row at 250,527 carries eight long INFO keys including `callsetnames` and `callable`, and the Inspector breaks them one per row | |
| "`lungfish-cli variants call --bundle <bundle> --alignment-track hg002-minimap2 --caller bcftools --name 'HG002 bcftools'" | true | Every flag matches `cli-help/variants.txt`. `--name` is documented as `--name, --output-track-name` | |
| "`--caller` picks one of `lofreq`, `ivar`, `medaka`, `bcftools`, or `clair3`" | true | Verbatim from the CLI help's `--caller` line | |
| "The same command group also holds `variants query` ... and `variants extract-sample`" | false | The group holds four subcommands, not three. `cli-help/variants.txt` lists `call`, `phase`, `extract-sample`, and `query`. The chapter omits `variants phase`, the GATK HaplotypeCaller plus WhatsHap command plan | "The same command group also holds `variants query`, which applies a smart filter and writes the matching rows to a VCF, `variants extract-sample`, which pulls one sample's calls out of a multi-sample track, and `variants phase`, which builds a phase-aware GATK and WhatsHap command plan" |
| "`variants query`, which applies a smart filter and writes the matching rows to a VCF" | true | CLI help: "Query bundle variants with a per-sample smart filter", with `--filter` and `-o, --output` | |
| "`variants extract-sample`, which pulls one sample's calls out of a multi-sample track" | true | CLI help: "Extract one sample's variant calls from a bundle variant database" | |
| "The versions that produced these files were bcftools 1.24 and LoFreq 2.1.5, both recorded in the provenance sidecar written beside each VCF" | false | The sidecars exist beside each VCF, and bcftools 1.24 is recorded. LoFreq 2.1.5 is NOT. `HG002.lofreq.lungfish-provenance.json` step 3 records `toolVersion` as `FATAL(lofreq_main.c\|main:336): Unrecognized command '--version' ...`. The fixture README says the 2.1.5 came from running `lofreq version` by hand | "The versions that produced these files were bcftools 1.24, recorded in the provenance sidecar written beside each VCF, and LoFreq 2.1.5, which had to be read from the tool directly because the LoFreq binary rejects `--version`" |
| "The two caller VCFs are rebuilt by the fixture's `regenerate.sh` script rather than committed, because they are larger than the manual's per-file cap" | false | `git ls-files docs/user-manual/fixtures/hg002-chr20/expected` returns both VCFs and both `.tbi` indexes. They are committed. The bcftools VCF is 46 KB and the LoFreq VCF is 16 KB, far under the 10 MB per-file cap | "The two caller VCFs and their indexes are committed alongside the rest of the fixture, and `regenerate.sh` reproduces them" |
| "its reference is a 500,001-base slice of human chromosome 20" | true | `GRCh38.chr20.10.0-10.5Mb.fasta.fai` reads `chr20_10.0-10.5Mb 500001 19 50 51` | |
| "`CHROM` ... `chr20_10.0-10.5Mb`" | true | Same `.fai` line and every fixture VCF record | |
| "Across this fixture the mean is 44.7, and the row at 250,527 sits at 63" | true | README records mean depth 44.7x from a real `regenerate.sh` run; the quoted row carries `DP=63` | |
| "The 16 and 17 alternate reads at 250,527 are the balance a real difference produces" | true | The bcftools row's `DP4=8,12,16,17` gives alternate-forward 16 and alternate-reverse 17 | |
| "`AD` gives the per-allele counts ..., 20 reference and 33 alternate" and "Twenty plus thirty-three is 53 rather than the 63 of `DP`" | true | The sample field is `0/1:255,0,255:20,33`, and `DP=63` in INFO. The arithmetic holds | |
| "`MQ=59` is the average mapping quality ..., close to the maximum of 60" | true | The row carries `MQ=59` | |
| "Calling variants needs the variant-calling plugin pack" | true | `PluginPack.swift:565` defines a pack with `id: "variant-calling"` | |
| "LGE records that in the track's provenance, visible in the Inspector" | true | A `.lungfish-provenance.json` is written beside each VCF (`writeWorkflowProvenance` in the attachment service), and the fixture folder contains one per caller | |
| "The Inspector's provenance carries all of it", meaning alignment, caller, and thresholds | unverifiable | The sidecar JSON does carry the caller, its command line, and the input BAM. Whether the Inspector surfaces the thresholds specifically was not settled. Opening a bundle track in the running Preview app and reading the provenance section would settle it | |
| "a `QUAL` of `.` means the caller did not score the row at all" | true | Standard VCF semantics, and consistent with the FILTER treatment elsewhere in the chapter | |
| "`QUAL` is a Phred-scaled confidence ..., so 20 means a 1 percent chance the call is wrong and 30 means 0.1 percent" | true | Phred definition, and consistent with `GLOSSARY.md#phred-score` | |
| Front matter `parameters_refs: []` | true | Correct by campaign ruling. The chapter documents no operation settings, and says so explicitly ("this chapter documents none of the settings on the variant-calling dialog") | |
| Front matter `shots` lists every `<!-- SHOT -->` marker with a caption | true | Three markers in the body at lines 172, 178, 190 (`variants-tab-hg002-bcftools`, `variants-pass-chip-and-tokens`, `inspector-variant-selected`), and three `shots` entries with those exact ids, each carrying a caption | |
| Every `glossary_refs` anchor resolves in `GLOSSARY.md` | true | All nineteen anchors resolve. `strand-bias` is linked from the body at line 152 and also resolves, though it is not listed in `glossary_refs` | Add `strand-bias` to `glossary_refs` |
| Glossary **FILTER** entry | true | Matches the fixture exactly. `min_dp_10` and `sb_fdr` are real LoFreq header names; `ft` and `bq` are real iVar names from `IVarTSVToVCFConverter.swift:122-123` | |
| Glossary **FORMAT** entry | true | `GT:PL:AD` is the bcftools fixture's FORMAT, and the LoFreq fixture's `#CHROM` line really does end at `INFO` with no FORMAT and no sample column | |
| Glossary **Genotype** entry | true | The diploid notation is standard, and "Lungfish's iVar lane writes the bare haploid `1`" matches `IVarTSVToVCFConverter.swift:201` | |
| Glossary **Filter profile** entry naming the four built-ins | true | `FilterProfileManager.swift:38-70` gives exactly Clinical, Research, QC, High Confidence, saved per bundle | |

## Counts

10 false, 4 unverifiable, 49 true.
