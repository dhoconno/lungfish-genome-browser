# Reader 2 report, Importing NAO-MGS Results

Persona. Senior, two years of wet-lab pipetting, comfortable with library prep and PCR, has never analyzed sequencing data, has never opened a terminal.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| What it is, "NAO-MGS is a metagenomic surveillance pipeline" | The name NAO-MGS is never unpacked. I do not know what the letters stand for or who SecureBio is, so I cannot tell whether this is a tool I would ever meet. | Spell out the acronym and say in one clause what SecureBio is. |
| What it is, "surveillance pipeline built by SecureBio" | The word pipeline is used from the first sentence and never glossed. In my lab a pipeline is a vague word for a protocol. Here it seems to mean a specific program someone else runs. | Gloss pipeline at first use as a fixed chain of programs run end to end. |
| What it is, "screens every read against a broad reference collection" | I do not know what screening a read against a collection means mechanically. Is this alignment, is it a search, is it BLAST. | Say in one clause that the pipeline aligns or searches each read against a set of known genomes. |
| What it is, "give it a viewport, which is this manual's word for the window" | Viewport is defined here as a window, but later the chapter says the viewport is a split view with four regions inside one window. I had to read twice to decide whether a viewport is the app window or a panel inside it. | Define viewport once as the panel inside the app that displays one result. |
| What it is, "The file LGE reads is `virus_hits_final.tsv.gz`" | I do not know what `.tsv.gz` is. I have never opened a compressed file on purpose, and nothing tells me not to unzip it first. | Say that `.tsv` is a plain text table and `.gz` means it is compressed, and that LGE reads it compressed. |
| What it is, "no `samples/` folder, `metadata.tsv`, or `manifest.json` structure to assemble first" | This tells me what does not exist, using three filenames I have never seen. It relieves an anxiety I did not have and creates one instead. | Cut the list of absent files or reduce it to a single clause saying no directory layout is required. |
| Why you would do this, "with its own bit score, edit distance, and reference position" | Three terms in one clause. Bit score is linked, edit distance and reference position are not, and I do not know either. | Gloss edit distance and reference position, or drop them from the sentence. |
| Why you would do this, "Thirty columns wide, one row per matching read" | This is a fragment, not a sentence, and I read it twice looking for the verb. | Make it a full sentence. |
| Why you would do this, "Ten reads that are ten copies of one amplified fragment" | This is the one place my PCR knowledge would have helped, but the link to PCR duplicates comes four paragraphs later. Here it is only implied. | Name PCR duplicates in this sentence, where the idea is introduced. |
| Before you start, "an NAO-MGS wastewater run covering five sampling sites" | The chapter's whole worked example uses numbers from a file I am told I cannot download. I cannot follow along, and I cannot check my own numbers against anything. | Say up front, before the procedure, that the reader will run this on their own file and that quoted numbers will differ. |
| Before you start, "produced by an external `securebio/nao-mgs-workflow` run" | This is the actual prerequisite and I cannot meet it. I have no cluster and no idea who would run this for me. Nothing tells me whether that is fine. | Add one sentence saying this file normally arrives from a collaborator or a scheduled job, not from the reader. |
| Before you start, "no plugin pack, no conda environment, and no Docker container" | Three things I have never heard of, listed as things I do not need. I still do not know what they are, and the sentence made me worry that other chapters will need them. | Reduce to a single sentence saying nothing needs installing for this import. |
| Procedure step 1, "The **NAO-MGS Results** card carries an NM badge" | I cannot picture a badge. The figure is a placeholder comment, so nothing shows me what NM looks like or where on the card it sits. | Say the badge is a small two-letter label on the card. |
| Procedure step 2, "select either the pipeline output directory or the `virus_hits_final.tsv(.gz)` file itself" | The notation `.tsv(.gz)` is programmer shorthand I had to decode. I also cannot tell which of the two choices I should make. | Write the two filenames out and say which choice to prefer. |
| Procedure step 2, "When the run wrote one file per sequencing lane rather than one combined table" | Sequencing lane is not glossed. I know lanes from a flow cell, but I cannot tell whether that is what is meant, or what I should do differently when I see this row. | Gloss lane and say plainly that the importer handles the multi-file case with no action from the reader. |
| Procedure step 3, "Commit the import." | This is the only instruction in the step and it does not tell me what to click. Every other step names a button. I do not know whether there is an Import button or a Run button. | Name the button. |
| Procedure step 3, "resolves the numeric taxonomy identifiers into organism names by looking them up at NCBI" | This means the import needs the internet, which contradicts the Settings paragraph telling me I can work offline. I could not tell whether an offline import would leave every name blank. | State whether taxonomy lookup needs the network and what happens offline. |
| Procedure step 3, "the import reported 35 hits across 4 distinct taxa" | I have no way to judge 35. Is that a busy sample or an empty one. Nothing tells me the scale a real run lands in. | Say what range of hit counts a typical wastewater run produces. |
| Procedure step 3, "named `naomgs-<sample>` after the first sample" | Later the CLI output shows the folder named `naomgs-virus_hits_final`, which is a filename, not a sample. The two do not agree and I read both twice. | Reconcile the naming rule with the example output. |
| Settings, "The three settings below therefore exist only on the command line" | The Settings section is written entirely for someone with a terminal. I was told in the introduction that everything is complete without the command line, so I did not know whether to read this section or skip it. | Add one sentence saying GUI readers can skip this section. |
| Settings, Fetch references, "Downloads a reference FASTA from NCBI for each accession" | FASTA is not glossed here and is not in this chapter's glossary list, unlike FASTQ, which is glossed later. I do not know the difference between the two. | Gloss FASTA at first use and contrast it with FASTQ in the same clause. |
| Settings, Fetch references, "The registry records reference fetching as on by default" | The registry is named as if I know it. I do not know what the registry is or why it is the authority on a default. | Say the default is on, without naming an internal registry. |
| Settings, Output directory, "It defaults to the current directory, which is almost never what you want" | The current directory is a terminal idea. I have no current directory. | Gloss current directory as the folder the terminal is pointed at. |
| Reading the results, "The viewport is a split view. A summary bar runs along the top" | Four regions are named in one sentence with no figure rendered. I could not build the picture, and I could not tell whether the detail pane on the left is larger or smaller than the table on the right. | Land the screenshot, or describe the layout in the order the eye meets it. |
| The summary bar, "reads `N of M samples` when you have filtered" | N and M are algebra standing in for numbers. I understood it on the second read, but it is the first time the manual asks me to do that. | Give a concrete example such as 2 of 5 samples. |
| The taxon table, "**Hits** is the number of reads assigned to that taxon" | Hits and reads are used as near-synonyms throughout, and the Why section said one row is one read's alignment against one reference. So one read matching three references could be three hits. I could not settle whether Hits counts reads or alignments. | State plainly whether a hit is a read or a read-to-reference alignment. |
| The taxon table, "12 hits from 8 unique reads, meaning a third of the evidence was duplicate copies" | The arithmetic does not land for me. Four of twelve is a third of the hits, but I could not tell whether a third of the evidence means the same thing. | Restate the fraction in terms of one column, not of evidence. |
| The taxon table, "right-click a header to filter on that column's values" | I cannot perform this from the text. Nothing says what the filter control looks like or how to clear a filter once set. | Say what the right-click menu offers and how to remove a filter. |
| The taxon table, "the Inspector's **Import Metadata...**" | The Inspector appears here for the first and only time with no explanation of what it is or how to open it. | Name the Inspector's location, or link to the chapter that covers it. |
| The detail pane, "a `Top Taxa by Read Count` bar chart of the top fifteen taxa" | The example run has only 4 taxa, so I could not tell what the chart shows when there are fewer than fifteen. | Say the chart shows all taxa when there are fewer than fifteen. |
| The detail pane, "`Taxid 28875  •  26 unique / 28 total reads  •  7 accessions`" | These numbers do not match the 28 hits from 26 unique reads quoted two sections earlier for a different site, and I spent time trying to work out whether they were the same row. | Use one consistent worked example throughout the chapter. |
| The detail pane, "`Top 5: 5 of 12 accessions` when the list was capped" | I cannot tell whether the cap is something I control or something fixed, and there is no setting for it in the Settings section. | Say whether the cap is adjustable. |
| The detail pane, "Drag a panel's handle downward to make it taller" | I do not know what a handle looks like or where on the panel it sits. | Say where the handle is. |
| The detail pane, "a compact read-pileup view of the reads that landed on one accession" | Read pileup is the central object of this section and it is not glossed. I have never seen one and cannot picture it, so the whole What good looks like advice about reads spread along the reference means nothing concrete to me. | Gloss pileup as a stacked display of reads against reference position. |
| The action bar, "**BLAST Verify** submits a sample of the selected taxon's reads" | A sample of the reads, not all of them. I could not tell how many, or whether that number matters to the verdict. | Say how many reads are submitted. |
| The action bar, "the export records those choices in its own provenance sidecar" | Sidecar is not glossed. I pictured a motorcycle. | Gloss sidecar as a companion file written beside the export. |
| What good looks like, "a row whose unique count is under half its hit count deserves the read pileups opened" | This is the only number the chapter gives me to judge with, and it arrives as a hedged rule after saying there is no threshold. I could not tell whether to treat half as a rule or as an opinion. | Keep the number and drop the hedge, or drop the number. |
| What good looks like, "Sort by Taxon rather than Hits to see this" | This tells me to sort by name to group rows, which works only if the same organism resolves to the same string in every sample. The Taxon column falls back to `Taxid N` when lookup fails, so I could not tell whether the grouping survives that. | Note what happens to grouping when a name fails to resolve. |
| What good looks like, "Reads stacked on one short stretch are what a conserved region shared with a relative looks like" | Conserved region is not glossed. I half know it from lecture, but the sentence packs two different failure explanations into one clause and I read it twice. | Gloss conserved region and split the two explanations. |
| What good looks like, "A row naming a broad group such as `Cressdnaviricota sp.`" | This example is opaque. I do not know the group, and `sp.` is not explained. | Choose a familiar group, or gloss `sp.` as species not determined. |
| On the command line, "This section is optional." | Good that it says so, but the three settings I actually need to understand were pushed into the Settings section above and written in terminal terms. So the optional section is not really optional. | Move the GUI-relevant part of Fetch references up into the Procedure. |
| On the command line, "One defect is worth knowing before you rely on that summary" | A known bug is documented in the middle of a how-to. I could not tell whether the app has the same bug or only the command line does. | State plainly that the defect affects only the command-line summary. |
| On the command line, "`--min-bitscore`, which drops hits below a bit-score floor and defaults to 0" | I do not know what bit score values look like, so a floor of 0 tells me nothing about what a non-zero floor would be. | Give a typical bit-score range before naming the floor. |
| On the command line, "MU-CASPER-2026-03-31-a-IL_CHI_StickneyWS_20260308" | This identifier runs off the edge of the block and I could not tell which part is the sample name. | Shorten the example identifier. |
| On the command line, "`--by-db` ... does not work against a result the importer produced" | A whole paragraph explains a command that never works for me, ending in an error string. I read it twice before realizing the advice was to not use it. | Lead with the instruction to use `--by-classifier` and cut the explanation to one sentence. |

One thing I learned. Counting reads twice because PCR copied the same fragment is the difference between a real signal and a fake one, and the Unique Reads column is where that shows.

One thing I still could not do. Get a `virus_hits_final.tsv.gz` file, so I could not perform a single step in the Procedure.

The sentence I liked most. "Ten reads that are ten copies of one amplified fragment and ten reads from ten independent fragments produce the same count and mean entirely different things."
