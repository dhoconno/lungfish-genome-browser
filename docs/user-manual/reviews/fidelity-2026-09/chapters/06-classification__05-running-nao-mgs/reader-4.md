# Reader 4 report, Importing NAO-MGS Results

Persona. An undergraduate who used Geneious for one semester, is at home in a
graphical sequence viewer, expects every operation to open a dialog with
settings, and has never opened a terminal.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| What it is, "NAO-MGS is a metagenomic surveillance pipeline built by SecureBio" | NAO-MGS is never spelled out anywhere in the chapter. I do not know what the letters stand for, and neither does the sentence. SecureBio is also a bare name. | Give the expansion once at first use, and say in half a sentence who SecureBio is. |
| What it is, "The pipeline itself runs elsewhere, on a cluster or in the cloud" | I do not know what a cluster is in this sense, and this is the first hint that the thing I am reading about is not something I can do. In Geneious every workflow in the menu is one I can actually run. | Say plainly that this chapter is about a file someone else made for you, and gloss cluster as a shared multi-computer system you submit jobs to. |
| What it is, "screens every read against a broad reference collection" | Reference collection is not glossed. I do not know whether it means a folder of FASTA files, a database, or a curated list, or how I would find out what is in it. | Gloss reference collection at first use and say it is a fixed set of viral genomes chosen by the pipeline authors. |
| What it is, "The pipeline's main output is a table of the reads that matched a virus, one row per read." | I read this twice. On the first pass I thought it meant one row per virus, because that is the shape a results table normally has. | Put the contrast up front, for example say the table has one row per matching read and not one row per virus. |
| What it is, "there is no way to start one from inside the window" | Which window? The chapter has not shown me any window yet, and I do not know if this means the project window, the Import Center, or the app in general. | Name the thing, for example say there is no way to start an NAO-MGS run from anywhere in LGE. |
| What it is, "The file LGE reads is `virus_hits_final.tsv.gz`" | I do not know what `.tsv.gz` is. I have never had to know a file extension in Geneious, where I drag a file in and it works out the format. | Gloss `.tsv` as a tab-separated text table and `.gz` as a compressed file, and say I do not need to unzip it first. |
| What it is, "there is no `samples/` folder, `metadata.tsv`, or `manifest.json` structure to assemble first" | This reassures me about a worry I did not have, and it uses three filenames I have never seen. It made me wonder whether some other import in this manual does need that structure. | Cut the list of filenames, or say only that no folder layout has to be prepared. |
| Why you would do this, "with its own bit score, edit distance, and reference position" | Edit distance is not glossed and is not in the glossary list at the top. Bit score is linked but the link does not tell me here whether a big number is good. | Gloss edit distance as the number of differences between the read and the reference, and say which direction is better. |
| Why you would do this, "Thirty columns wide, one row per matching read" | This is not a sentence. It has no verb and I stopped on it. | Attach it to the sentence before it. |
| Why you would do this, "Ten reads that are ten copies of one amplified fragment" | Amplified is used before PCR is introduced. The PCR duplicate link does not appear until much later, in the taxon table section. | Say PCR here and link the glossary entry at this first use instead of later. |
| Before you start, "You need a `virus_hits_final.tsv.gz` produced by an external `securebio/nao-mgs-workflow` run" | I cannot do this chapter. I have no cluster, no such file, and the linked GitHub page is for people who run pipelines from a terminal. This is the point where I had to stop following along. | Say at the top of the chapter, not here, that a file from an external run is required and that no downloadable example exists yet. |
| Before you start, "This manual does not yet ship a public NAO-MGS fixture, so the figures quoted below come from a five-sample surveillance run" | Fixture is a word from software testing, not from biology. I guessed it means example dataset. | Say example dataset. |
| Procedure step 1, "The Import Center is a tabbed grid of cards, one card per kind of thing LGE can bring into a project" | I expected to import a file with File > Import, as in Geneious. A separate Import Center with tabs and cards is a real difference and the text does not flag it as one. | Add one sentence saying LGE routes all imports through this one window rather than a plain file-open dialog. |
| Procedure step 1, "carries an NM badge" | I do not know what a badge is here or why NM matters. If I am hunting the grid for the right card, the badge is what I would look at, and I cannot picture it. | Say the badge is the small two-letter tag on the card corner, and that NM stands for NAO-MGS. |
| Procedure step 2, "It holds no settings at all, only a source picker and a validation readout" | This contradicts everything I expect. Every Geneious operation opens a dialog where I set parameters. Then the Settings section below lists three settings, which made me think I had missed a control. | Say once, in this step, that the three settings named later are command-line only and never appear in the sheet. |
| Procedure step 2, "select either the pipeline output directory or the `virus_hits_final.tsv(.gz)` file itself" | The `(.gz)` notation is not explained. I did not know whether that meant a file literally named with parentheses or an optional part. | Write both filenames out, or say the `.gz` ending is optional. |
| Procedure step 2, "When the run wrote one file per sequencing lane rather than one combined table" | Sequencing lane is not glossed and is not in the glossary list. I do not know why a run would be split this way or whether it matters to my result. | Gloss lane as one physical channel on the sequencer, and say the importer combines them for you. |
| Procedure step 2, "on a row labelled **Files found** whose value counts the per-lane files" | I cannot judge this number. If it says 8, is that fine, or does it mean something went wrong? Nothing tells me what a normal value looks like. | Say any count above zero is normal and that the number is informational only. |
| Procedure step 3, "Commit the import." | This is the one instruction I could not perform. I do not know what to click. Every other step names a button. | Name the button, for example Import. |
| Procedure step 3, "resolves the numeric taxonomy identifiers into organism names by looking them up at NCBI" | This needs the internet and the text does not say so here. It also does not say what happens if I am offline, though a later column mentions a `Taxid N` fallback. | Say the step needs a network connection and that names fall back to `Taxid N` without one. |
| Procedure step 3, "the import reported 35 hits across 4 distinct taxa" | I cannot judge whether 35 hits is a lot or almost nothing for five wastewater samples. The number appears three more times later with no scale attached. | Say in one clause whether this is a small run or a typical one. |
| Procedure step 3, "named `naomgs-<sample>` after the first sample the table names" | The angle-bracket notation is not explained, and later the example folder is `naomgs-virus_hits_final`, which is a filename and not a sample name at all. The two disagree and I read both twice. | Reconcile the two examples, and explain the angle brackets as a placeholder once. |
| Settings, "The three settings below therefore exist only on the command line" | I have never opened a terminal. This tells me a Settings section exists that I can never use, but does not tell me whether anything is lost by ignoring it. | Add one sentence saying the sheet's defaults are correct for GUI users and nothing here needs action. |
| Settings, Fetch references, "The registry records reference fetching as on by default" | Registry is a word for developers. I do not know what registry this is or why it, rather than the app, decides the default. | Say simply that reference fetching is on by default. |
| Settings, Output directory, "It defaults to the current directory, which is almost never what you want" | Current directory is a terminal idea. I have no mental model of a directory the program is currently in. | Gloss it as the folder the command was typed in, or move this whole entry into the command-line section. |
| Reading the results, "the detail pane fills the left side, the taxon table fills the right" | The screenshot caption at the top of the file describes the bar chart as "filling the detail pane" and the table with columns, but I could not build a picture of which half is which from prose alone. Two paragraphs later the detail pane is described again. | Say left and right once with the summary bar and action bar, and drop the repeat. |
| The summary bar, "or reads \"N of M samples\" when you have filtered to a subset" | N and M as placeholders are never introduced. I first read this as literal letters on screen. | Show a concrete example such as 2 of 5 samples. |
| The taxon table, "**Refs** is how many reference accessions the taxon's hits spread across" | I cannot judge this number. Is one reference better than seven, or worse? Later the chapter uses 7 accessions and 12 accessions with no verdict either way. | Say what a high Refs value suggests and what a low one suggests. |
| The taxon table, "right-click a header to filter on that column's values" | I could not perform this. It does not say what the filter looks like, whether it is a checklist or a typed value, or how to clear it once set. | Say what appears on right-click and how to clear a filter. |
| The taxon table, "12 hits from 8 unique reads, meaning a third of the evidence was duplicate copies" | I read this twice. Twelve minus eight is four, which is a third of twelve, but the sentence sits next to "a third" and I had to work out whether the fraction was of hits or of the difference. | State the arithmetic, for example four of the twelve hits were duplicates. |
| The detail pane, "a **Top Taxa by Read Count** bar chart of the top fifteen taxa" | The example run has only 4 taxa, so I cannot tell what I would actually see. Nothing says what the chart does when there are fewer than fifteen. | Say the chart shows all taxa when there are fewer than fifteen. |
| The detail pane, "`Taxid 28875  •  26 unique / 28 total reads  •  7 accessions`" | These figures do not match the table example a few paragraphs earlier, which said 28 hits from 26 unique reads for the same organism at one site, and the import summary said 35 hits total. I could not tell whether this is the same row or a different one. | Use one consistent example run throughout, or label the different sources. |
| The detail pane, "reading `All: 3 of 3 accessions` when every one fits and `Top 5: 5 of 12 accessions` when the list was capped" | I do not know where the cap comes from or whether I can change it. It is not in the Settings section, so I assume I cannot, but the text does not say. | Say the cap is fixed and cannot be changed. |
| The detail pane, "Drag a panel's handle downward to make it taller" | I could not find this from the text. Handle is not described, so I do not know what to look for or where on the panel it sits. | Say where the handle is, for example along the panel's bottom edge. |
| The detail pane, "A miniBAM panel is a compact read-pileup view" | Pileup is not glossed, and it is the thing the whole "What good looks like" section asks me to interpret. In Geneious I would call this an alignment view. | Gloss pileup as a stack of reads drawn at the positions they matched on the reference. |
| The action bar, "Each accession above its panel is a clickable link to that record at GenBank" | GenBank appears here for the first time with no gloss, and NCBI has been used four times already without one. I do not know if they are the same thing. | Gloss NCBI at first use and say GenBank is its sequence database. |
| The action bar, "**BLAST Verify** submits a sample of the selected taxon's reads to NCBI BLAST" | A sample, not all of them. I do not know how many reads that is, and the number would change how much I trust the verdict. | Say how many reads are sent, or point to where that number is set. |
| The action bar, "the export records those choices in its own provenance sidecar" | Sidecar is not glossed. I pictured a motorcycle. | Say a small companion file written beside the export. |
| The action bar, "**Extract FASTQ** pulls the reads behind your selection out" | It is not clear what "your selection" means when no row is selected. In Geneious an export with nothing selected exports everything, and I do not know if that holds here. | Say what the button does when no taxon row is selected. |
| What good looks like, "a row whose unique count is under half its hit count deserves the read pileups opened" | This is the only number in the chapter I can act on, but the paragraph opens by saying there is no threshold and then gives one. I read the paragraph twice to settle which I was meant to use. | Present the half as a rule of thumb in one sentence without the preceding disclaimer. |
| What good looks like, "Reads stacked on one short stretch are what a conserved region shared with a relative looks like" | Conserved region is not glossed. I know the words separately but not what to look for on the pileup, and this is the visual judgement the section is asking me to make. | Gloss conserved region as a stretch of sequence that several related viruses share. |
| What good looks like, "A row naming a broad group such as `Cressdnaviricota sp.`" | I cannot read this name. Nothing tells me that `sp.` means an unnamed species, and phylum is used without a gloss in the same sentence. | Gloss `sp.` and phylum. |
| On the command line, "This section is optional." | I stopped reading here and I think that was correct, but the Settings section above already sent me to the command line for three settings. The two sections disagree about whether I can skip terminal work. | Say in Settings that GUI users lose nothing, so this section really is optional. |
| On the command line, "One defect is worth knowing before you rely on that summary." | Even skimming, this alarmed me. A known defect that reports 34 hits where the real answer is 35 makes me wonder whether the viewport numbers I was just taught to read are trustworthy. | Say explicitly that the defect is confined to `nao-mgs summary` and does not affect the imported result. |

Three closing lines.

The one thing I learned. Two reads that look identical can be one fragment
copied twice, so the unique count is the honest one and the hit count is not.

The one thing I still could not do. Any of it. I have no NAO-MGS file, no way
to make one, and no example dataset to download, so I could not follow a single
step of the Procedure.

The sentence I liked most. "Ten reads that are ten copies of one amplified
fragment and ten reads from ten independent fragments produce the same count and
mean entirely different things."
