# Reader 1 review — Importing NAO-MGS Results

Persona: sophomore, one genetics course, never opened a terminal, never used sequencing software.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| Title / What it is, "Importing NAO-MGS Results" | The title says "Importing" but the chapter id and the sidebar phrase everywhere else say "Running NAO-MGS". I spent a while wondering whether I was in the wrong chapter, because I expected to run something. | Say in the first line that this chapter imports rather than runs, since the file name says running. |
| What it is, "built by SecureBio" | SecureBio is never explained. I do not know whether it is a company, a university lab, or a consortium, and that changes how much I trust the reference collection. | Add three words identifying SecureBio as a biosecurity research organization. |
| What it is, "NAO-MGS" | The letters are never expanded. I could not guess what NAO or MGS stand for, so the name stayed a meaningless label the whole chapter. | Expand the acronym at first use. |
| What it is, "screens every read against a broad reference collection" | "Screens against" is doing a lot of work. I do not know if this is alignment, exact matching, or something else, and later the chapter talks about bit scores and edit distances that imply alignment. | Say the pipeline aligns each read to the references. |
| What it is, "reference collection" | Never defined, but the whole "What good looks like" section depends on knowing what is in it and what is not. | Gloss it as the set of viral genomes the pipeline compares reads against. |
| What it is, "`virus_hits_final.tsv.gz`" | I do not know what `.tsv` or `.gz` mean. Nothing in the chapter says these are a tab-separated text table and a compressed file. | Gloss tsv and gz once at first use. |
| What it is, "Point the importer at the results directory" | "Directory" appears here for the first time and "folder" is used elsewhere for what I think is the same thing. I stopped to check whether these were two different things. | Use one word for the concept throughout. |
| What it is, "there is no `samples/` folder, `metadata.tsv`, or `manifest.json` structure to assemble first" | This tells me what I do not need without ever telling me those things existed. It reads like an answer to a question I had not asked, and I read it twice. | Drop it or say that other importers need such a structure and this one does not. |
| Why you would do this, "its own bit score, edit distance, and reference position" | Three terms in one clause. Bit score is glossary-linked but not glossed in the text, and edit distance and reference position are not explained at all. The chapter later says the bit score is "length-aware alignment strength", but that arrives 130 lines too late. | Gloss bit score here and drop or gloss edit distance. |
| Why you would do this, "Thirty columns wide, one row per matching read" | This is not a sentence and it read as a fragment I had to reparse. | Make it a full sentence. |
| Why you would do this, "Ten reads that are ten copies of one amplified fragment" | "Amplified" is used before PCR is mentioned. In my genetics class amplification was a PCR step, but I could not tell if the pipeline is amplifying or the wet lab did. | Say the amplification happened during library preparation before sequencing. |
| Why you would do this, "the same read extraction you use for the classifiers LGE runs itself" | I have no idea which classifiers LGE runs itself, and this is the first classification chapter I opened. | Name one, or link to the classification overview. |
| Before you start, "an NAO-MGS wastewater run covering five sampling sites" | I cannot get this file. The chapter then quotes exact numbers from it for the rest of the chapter and I have nothing to compare against, so every worked number is unverifiable for me. | Say up front that the reader will follow along on their own file with different numbers. |
| Before you start, "which the pipeline's own documentation ... explains how to produce" | This sends me to a GitHub page for a cluster pipeline. I have never opened a terminal, so I cannot produce this file at all and cannot do the chapter. | Say plainly that the file comes from a collaborator or a computing core, not from the reader. |
| Before you start, "no plugin pack, no conda environment, and no Docker container is involved" | Three terms I do not know, used only to say they are not needed. Naming things I have never heard of made me worry I had skipped a prerequisite chapter. | Say only that nothing needs installing. |
| Procedure step 2, "the `virus_hits_final.tsv(.gz)` file itself" | The parenthesis notation is a convention I had to decode. I was not sure whether that meant two file names or one optional suffix. | Write both file names out. |
| Procedure step 2, "recognisable NAO-MGS virus-hits table" | I have no way to check the header myself, so if the warning triangle appears I cannot tell whether the file is wrong or the app is wrong. | Say what a valid header looks like, or name one column the checker requires. |
| Procedure step 2, "one file per sequencing lane rather than one combined table" | A lane is never explained. I do not know if a lane is a machine part, a sample, or a run, and the Files found row's number therefore means nothing to me. | Gloss a lane as one physical channel on the sequencer. |
| Procedure step 3, "Commit the import." | This is the only instruction and it names no button. Every other step tells me what to click. I did not know whether to press Return, click Import, or click OK. | Name the button. |
| Procedure step 3, "merges the per-sample databases, and resolves the numeric taxonomy identifiers ... by looking them up at NCBI" | This implies the import needs the internet, but "Before you start" said nothing needs installing and later a setting exists for working offline. I could not tell whether the import works offline at all. | State whether the import requires a network connection. |
| Procedure step 3, "the import reported 35 hits across 4 distinct taxa" | I cannot judge whether 35 hits across five wastewater sites is a lot or almost nothing. No sense of scale is given anywhere. | Say what a typical run's totals look like. |
| Procedure step 3, "named `naomgs-<sample>` after the first sample" | Later the CLI output shows `naomgs-virus_hits_final`, which is a file name and not a sample name. The two do not match and I read both twice. | Reconcile the two examples. |
| Settings, "The three settings below therefore exist only on the command line" | I have never opened a terminal, and the reader description says these are the settings. A whole required section of the chapter is for someone else. | Say that GUI readers can skip this section. |
| Settings, "Fetch references", "The registry records reference fetching as on by default" | "The registry" appears once, unexplained. I do not know what registry this is or why it, rather than the app, decides a default. | Say the default is on, without naming the registry. |
| Settings, "Output directory", "It defaults to the current directory, which is almost never what you want" | The current directory of what? I have no terminal and no mental model of a working directory. | Gloss the current directory as wherever the terminal is pointed. |
| Settings, "Point it at the `.lungfish` project folder" | I do not know where my project folder is or what its name looks like. I made one in step "Before you start" by picking a folder and was never told the app made a `.lungfish` thing inside it. | Say the project folder is the one ending in `.lungfish` that New Project created. |
| Reading the results, "The viewport is a split view. A summary bar runs along the top, the detail pane fills the left side, the taxon table fills the right" | The screenshot caption for the same screen says the bar chart fills "the detail pane" and puts the taxon table under the sample-filter button, which I pictured on the left. I could not build a consistent mental picture. | Make the caption and the prose agree on which side is which. |
| The summary bar, "reads \"N of M samples\"" | N and M are algebra placeholders. It took me a moment to realise the app shows real numbers there, not the letters. | Show a real example such as 2 of 5 samples. |
| The taxon table, "falling back to `Taxid N` when the lookup found no name" | I do not know what it means for the lookup to fail or what I should do when it does. It sounds like something is broken. | Say a bare Taxid means NCBI had no name and the row is still usable. |
| The taxon table, "right-click a header to filter on that column's values" | I could not perform this. Nothing says what the filter looks like, whether it is a list of checkboxes or a typed value, or how to clear it afterwards. | Say what the filter control offers and how to remove it. |
| The taxon table, "the Inspector's **Import Metadata...**" | The Inspector is never introduced. I do not know where it is on screen or how to open it. | Say where the Inspector lives, or link to the chapter that introduces it. |
| The taxon table, "12 hits from 8 unique reads, meaning a third of the evidence was duplicate copies" | 8 of 12 is two thirds unique, so a third duplicate is right, but I had to do the arithmetic twice because "a third of the evidence" reads at first like a third remains. | Say four of the twelve were duplicates. |
| The taxon table, "28 hits from 26 unique reads, which is a much stronger claim from a similar-looking count" | 12 and 28 are not similar-looking counts to me, they differ by more than a factor of two, so the comparison confused rather than clarified. | Pick example numbers whose totals actually match. |
| The taxon table, "the taxon is leaning on a small number of fragments that PCR amplified" | PCR is linked to a glossary entry about duplicates but never spelled out or explained in the text. This is the crux of the whole chapter's judgement and it rests on one unexplained acronym. | Explain in one sentence how PCR duplicates arise and why they inflate counts. |
| The detail pane, "a card naming the sample or counting the samples in view" | I could not tell which of the two I would see or what decides it. | Say the card names the sample when one is selected and counts them otherwise. |
| The detail pane, "`Taxid 28875  •  26 unique / 28 total reads  •  7 accessions`" | Earlier the chapter said 28875 is Rotavirus A with 12 hits from 8 unique reads at one site and 28 from 26 at another. Here the same taxid shows 26 of 28 with 7 accessions, and I could not tell if this is the same site or a total. | Say which sample the example line comes from. |
| The detail pane, "`All: 3 of 3 accessions`" and "`Top 5: 5 of 12 accessions`" | These two examples do not match the 7 accessions in the line just above, so I could not tell what my own screen would say for a taxon with 7. | Use one taxon consistently through the section. |
| The detail pane, "Drag a panel's handle downward" | I do not know where the handle is or what it looks like. | Say where on the panel the handle sits. |
| The detail pane, miniBAM panels | The chapter never says what I am looking at inside a panel. "Read pileup view" is the only description, and I have never seen one. "What good looks like" then asks me to judge whether reads are spread or stacked, which I cannot do without knowing what the picture shows. | Describe what the panel draws, with the reference along one axis and reads stacked above it. |
| The action bar, "submits a sample of the selected taxon's reads to NCBI BLAST" | "A sample of" is vague and it matters. If BLAST only sees some reads, I do not know how much of my evidence was checked. | Say how many reads are submitted. |
| The action bar, "records those choices in its own provenance sidecar" | "Sidecar" is not explained. I guessed it is a second file next to the export but I was not sure. | Gloss a sidecar as a companion file written beside the export. |
| The action bar, "**Extract FASTQ** pulls the reads behind your selection out" | I could not tell what "your selection" means here. A taxon row, a sample, or the highlighted rows in the table. | Say which selection the button acts on. |
| What good looks like, "a row whose unique count is under half its hit count deserves the read pileups opened" | This is the only number in the chapter I can act on, but it contradicts the worked example, where 8 of 12 is above half and the chapter still called it weak. | Reconcile the rule with the example. |
| What good looks like, "Reads stacked on one short stretch are what a conserved region shared with a relative looks like" | "Conserved region" was in my genetics course but the link to a false positive is not explained. I could not see why a shared region makes the name wrong. | Add half a sentence saying reads from a relative pile onto the shared stretch. |
| What good looks like, "`Cressdnaviricota sp.`, an unclassified member of a large viral phylum" | This is the only example given for a broad name, and it is a phylum I have never heard of. The campaign asks for human or macaque examples first, and even a viral chapter could use something I know. | Use a familiar broad name such as an unclassified enterovirus. |
| On the command line, whole section | Fifty lines of terminal commands in a chapter whose reader has never opened a terminal. The section says it is optional, which helped, but it is longer than the procedure it supplements. | Move it behind a collapsed block or to an appendix. |
| On the command line, "One defect is worth knowing" ... "it printed one sample name, 34 hits" | The chapter tells me a shipped command reports wrong numbers, 34 against the correct 35. Being told a tool is broken and to use it anyway for a "shape check" left me unsure whether to trust any of the numbers in this chapter. | State plainly that the summary command is unreliable on multi-sample files and should not be used for them. |
| On the command line, "`--min-bitscore`, which drops hits below a bit-score floor and defaults to 0" | I do not know what bit score value would be a sensible floor, and the chapter never gives a range. | Give a typical bit-score range so the floor means something. |
| On the command line, "A fourth route, `extract reads --by-db` ... It does not work against a result the importer produced." | An entire paragraph documents a command that cannot work in the situation the chapter is about. I read it three times looking for the case where it does apply. | Cut it to one sentence saying not to use it. |
| Next, "[Novel Virus Diagnostics](09-novel-virus-detection.md)" | The link text and the file name disagree, diagnostics against detection, so I could not tell which chapter I would land on. | Make the link text match the target chapter's title. |

Three lines:

One thing I learned: a hit count and a unique read count can be the same number of rows and mean completely different amounts of evidence.

One thing I still could not do: get an NAO-MGS file at all, so I could not perform a single step of the procedure.

The sentence I liked most: "Ten reads that are ten copies of one amplified fragment and ten reads from ten independent fragments produce the same count and mean entirely different things."
