# Reader 2 report

Persona: a senior who has spent two years pipetting in a wet lab. Knows library prep and PCR well. Has never analyzed data and has never opened a terminal.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "Every operation in this chapter runs from the command line in a terminal." | I have never opened a terminal. This first sentence tells me the whole chapter happens somewhere I have never been, and it does not tell me whether I should stop and go elsewhere first. The pointer to CLI Reference does not arrive until two sections later, under Before you start. | Move the pointer to CLI Reference into this first paragraph. |
| What it is, "the five GATK steps that turn that raw table" | GATK is never expanded or explained. It is used as a bare name here, in the tags, and in the Before you start section, and I do not know if it is a company, a program, or a file format. | Gloss GATK at first use as the toolkit the commands run. |
| What it is, "a tab-separated table listing the positions" | VCF is glossed as a tab-separated table, but two paragraphs later the chapter says `variants-to-table` "flattens the VCF into a spreadsheet." If it was already a table, I cannot tell what flattening adds. | Say in the VCF gloss that most of the information sits packed inside single columns. |
| What it is, "Its FILTER column, the seventh of the standard columns" | I have never seen a VCF and the chapter never shows me one. Being told the seventh column exists does not let me picture the file or find the column. | Show two or three lines of a real VCF with the columns labelled. |
| What it is, "the ones supported by evidence a careful reader would reject" | I had to read this twice. It says evidence a reader would reject, which sounds like the reader rejects the evidence, but the point is that the row should be rejected. | Say the rows whose supporting evidence is too weak. |
| What it is, "`leftalign` rewrites insertions and deletions in a standard position" | Indel is in the glossary list at the top but is not glossed here, and "a standard position" means nothing to me until the Settings entry much later explains leftmost equivalent position. | Gloss indel and say leftmost position here. |
| Why you would do this, "It is a fixed list of arithmetic tests applied to statistics the caller already wrote into each row" | Which statistics? I have no idea what a variant caller writes into a row, so I cannot picture what is being tested. The names QD, FS, MQ, MQRankSum, ReadPosRankSum, SOR appear one page later in a wall of command text with no introduction. | Name two or three of the statistics here before the command block shows all six. |
| Why you would do this, "GATK itself recommends a trained approach for large projects" | Trained approach is not explained and I do not know what counts as a large project. I cannot tell whether my own experiment is one. | Say trained approach means a model fitted to your data, and give a rough cohort size. |
| Why you would do this, "built from the HG002 chromosome 20 slice" | HG002 is explained one sentence later but the slice is not. I do not know whether a slice is a subset I have to make or a file I download. | Say a slice is a small region of the genome, supplied as the fixture. |
| Why you would do this, "so this is a human example with an answer key" | Answer key is a metaphor I liked, but it is never cashed out into what the file actually is until Step 4 calls it a benchmark and Settings calls it known variants. Three names for one thing. | Use one name for the file throughout and state it here. |
| Before you start, "None of the five operations has a dialog, a menu item, or an entry anywhere in the LGE window" | This repeats What it is almost word for word, and I read it twice looking for the new information. | Delete one of the two statements. |
| Before you start, "read CLI Reference ... first" | This is the only preparation offered for someone who has never opened a terminal, and it is a link out of the chapter. I do not know whether reading it takes ten minutes or an afternoon, or whether I can come back and succeed. | Say what the appendix covers and roughly how long it takes. |
| Before you start, "You need a project open." | Then the very next sentence says the files do not need to sit in the project folder and the CLI writes wherever you point it. So I cannot tell why I need a project at all. | State what the open project is actually used for here. |
| Before you start, "Download the file `GRCh38.chr20.10.0-10.5Mb.fasta`" | I download this FASTA and never use it in the Procedure. It appears only in the On the command line section, in the `leftalign` block. Meanwhile Step 4 asks for a `.dict` file I was never told to download or make until one paragraph later. | Say which step each downloaded file is used in. |
| Before you start, "and its `.tbi` index from the same folder" | Index is not explained. I do not know what it is for, whether the tool builds it, or what happens if I forget it. | Gloss index as a companion file the tools need to jump into the file quickly. |
| Before you start, "remember where you saved it" | I cannot perform the commands from this. Every command below names a bare file like `cohort.vcf.gz` with no folder, which only works if everything is in one place and I am standing in that place in the terminal. Nobody tells me that, and I do not know how to stand anywhere. | Tell the reader to put all files in one folder and say how to point the terminal at it. |
| Before you start, "This feature is experimental." | I do not know what experimental costs me. Can it lose data, give wrong answers, or disappear in an update? | Say what experimental means for the results. |
| Before you start, "Installing it from the command line does not work and answers `Unknown tool pack: gatk-core`, which is a defect" | I had to read this twice to work out that I am being warned off something I was never told to try. It reads as an instruction until the end. | Cut this to a short note that the Plugin Manager is the only route. |
| Before you start, "the `.dict` sequence dictionary you make once with GATK's own `CreateSequenceDictionary`" | This is a step I cannot perform. No command is given, and `CreateSequenceDictionary` is not one of the five `lungfish-cli` subcommands, so I do not know how I would run it. Step 4 then requires the resulting file. | Give the exact command that makes the `.dict` file. |
| Before you start, "Each of the six recorded runs below finished in about 1.2 seconds" | I count five operations in the chapter and the Procedure has five steps. Six is never accounted for, so I read the section again looking for the sixth. | Say which six runs, or drop the count. |
| Procedure, "a backslash at the end of a line continues one command onto the next line" | I do not know how to paste a multi-line block into a terminal, or whether the indentation matters. This is my first terminal command ever. | Say the block can be pasted as one piece and the leading spaces do not matter. |
| Procedure, "Every subcommand prints the GATK command it composed and stops." | Composed is doing a lot of work. It took me two readings to understand that `lungfish-cli` is writing a GATK command for me rather than doing the analysis itself. | Say `lungfish-cli` builds a GATK command and shows it to you before running. |
| Step 1 code block, `--preset best-practices-both` | Step 1 hands me a preset value with no explanation. The allowed values are only defined in Settings, twenty paragraphs later. | Say in one clause what this preset does. |
| Step 1 preview output, "`--filter-expression 'QD < 2.0' --filter-name QD2 ... 'MQRankSum < -12.5'" | The chapter tells me this preview is the only place the tests are written out and I should read it, but six of the ten statistics are never defined anywhere in the chapter. FS, MQ, MQRankSum, and ReadPosRankSum appear here and are never mentioned again. Only QD and SOR are explained. | Add a short table naming all six statistics and what a failure means. |
| Step 1 preview output, "with the directory part of each path shortened to three dots" | I could not judge whether my own output should look like this. If my paths appear in full I do not know if something is wrong. | Say the three dots are the manual's abbreviation and your output shows full paths. |
| Step 2, "GATK execution completed with exit code 0." | Exit code is never explained. I do not know that 0 means success until much later, where exit codes 2 and 3 are described as failures and I have to infer the rule backwards. | Say exit code 0 means the run succeeded. |
| Step 3, "Split the filtered cohort by variant class" | Two commands run and each needs `--sample HG002`, but nowhere does the chapter tell me how to find out what sample names are in my file. If my sample is not called HG002 I am stuck. | Say the sample name comes from the VCF header and how to see it. |
| Step 3, no leftalign or variants-to-table | The Procedure has five steps but only runs three of the five operations. `leftalign` and `variants-to-table` are described in What it is and then appear only in the final code dump. Following the Procedure straight through does not produce the table the chapter's task line promises. | Add the two missing operations as Procedure steps. |
| Step 4, "The `--output-prefix` value is a path stem rather than a file name" | Path stem is a term I do not know. The Settings entry later explains the suffixes, but here I only learn it is not a file name. | Gloss path stem as the start of a file name that the tool completes. |
| Step 4, "Make the folder first, because the command will not create it." then `mkdir -p metrics` | `mkdir -p` is a terminal command I have never seen, and the `-p` is not explained. It is also the only command in the chapter that is not a `lungfish-cli` command, which confused me about whether I typed it in the same place. | Say `mkdir` makes a folder and it is typed in the same terminal. |
| Step 4, "the recorded run needed the benchmark VCF rebuilt so that its list of contigs matched" | The chapter tells me my Step 4 will probably fail and sends me forward to a later section for the fix, but that later section only tells me to rewrite a file header. I cannot rewrite a VCF header and no command is given. So Step 4 is a step I cannot complete. | Give the command that rebuilds the benchmark header, or supply a matching fixture. |
| Step 4, "contigs" | Contig is used four times in the chapter and never glossed. It is not in the glossary list in the front matter either. | Gloss contig as one named sequence in the reference. |
| Settings, "a command-line flag, meaning a word beginning with two dashes" | This was the most useful gloss in the chapter, but it arrives after twenty flags have already been used in the Procedure. | Move this gloss to the start of the Procedure. |
| Settings, Preset, "`custom`, is accepted and applies no filter expressions at all ... Avoid it" | I cannot tell whether this is a feature I might one day want or a bug I should never touch. Later the chapter calls similar behaviour a defect, but not here. | Say plainly whether `custom` is unfinished. |
| Settings, Type, "`MIXED` for a position carrying both at once" | I could not picture a position carrying a substitution and a length change at once until Reading the results gave the `A` to `G,AGG` example. The example should come first. | Put the `A` to `G,AGG` example in this entry. |
| Settings, Fields, "`CHROM,POS,REF,ALT,QUAL,AF,DP`" | Seven field names, and only the plain-English list that follows tells me what five of them mean. REF and ALT are called "the two alleles" together so I cannot tell which is which, and I do not know what QUAL's units are or what a good value is. | Map each field name to its meaning one to one, and say what QUAL measures. |
| Settings, Max indel length, "The default is 200, which covers the great majority of indels" | I cannot judge this. Great majority could be 90 percent or 99.9 percent, and I have no idea how long a typical indel is. | Give a typical indel length so 200 can be judged. |
| Settings, Max leading bases, "treat both this number and the one above as load-bearing for clinical work" | Load-bearing is a metaphor I had to stop and decode, and the warning says an indel outside the window is left alone with no warning, but does not tell me how to find out whether that happened. | Say plainly that a wrong value silently leaves indels unnormalized. |
| Settings, dbSNP, "conventionally dbSNP, which is the public catalogue of human variation" | The flag is called `--dbsnp` and requires dbSNP, but every command in the chapter passes the HG002 benchmark instead. I could not tell whether I am doing something irregular. | Say the flag accepts any known-variants VCF and the fixture stands in for dbSNP. |
| Settings, GVCF input, "carries reference confidence at every position" | GVCF and reference confidence are both new terms in one sentence, and neither is glossed. | Gloss GVCF as a VCF that also records the positions where nothing differs. |
| Settings, Dry run, "on its own the flag changes nothing" | A setting that does nothing unless paired with the setting it cancels took me three readings. | Say its only use is cancelling `--execute`. |
| Settings, "An unrecognised `--preset` value falls back to `best-practices-both` rather than failing" | This is the scariest thing in the chapter and it is the last paragraph of a long section. A typo silently changes my analysis and I would never know. | Move this warning to the Procedure, beside Step 1. |
| Reading the results, "The two counts below `PASS` add to 14 rather than 13 because one row failed both tests" | I had to read this twice and count. The table shows 12 and 2, and the arithmetic 1013 plus 13 equals 1026 is left for me to do. | State the 1,013 plus 13 sum explicitly. |
| Reading the results, "quality score divided by the depth of reads supporting the variant" | Depth is used throughout and never glossed, though it is the DP column and a key number. | Gloss depth as how many reads cover the position. |
| Reading the results, "had quality scores of 32.64 and 32.6, both barely above the caller's own floor" | I cannot judge these. The test is QD below 2.0 but these numbers are 32, so they are not QD values, and the caller's floor is never given a number. | Give the floor value and say which statistic 32.64 is. |
| Reading the results, "a lopsided count suggests an artefact of the chemistry" | I know PCR and library prep, so I would understand this if told which step. Chemistry alone is too vague to act on. | Name the step, such as PCR or the sequencing chemistry. |
| Reading the results, "The recorded runs pulled 842 SNP rows and 182 indel rows out of the 1,026" | 842 plus 182 plus 2 is 1,026, which the text says adds up exactly, but the metrics table later reports TOTAL_SNPS 835 and TOTAL_INDELS 159 for the same file. The chapter never reconciles the two pairs of numbers and I assumed one was wrong. | Say the metrics counts cover `PASS` rows only and the select counts cover all rows. |
| Reading the results, "the SNP file holds 836 `PASS` rows and the indel file 175" | 836 plus 175 is 1,011, not 1,013, and the missing two are never mentioned. I checked this three times. | Account for the two `MIXED` rows in the sum. |
| Reading the results, "All 19 of the input's multi-allelic rows became two rows each" | 1,026 plus 19 is 1,045, which works, but the text says 19 rows became two rows each, which sounds like 38 rows from 19 and I had to work out the difference. | Say the split adds 19 rows. |
| Reading the results, "Position 29,224 shows the effect plainly." | I cannot find position 29,224 in my own file from this text, because no command for looking inside a VCF is given anywhere in the chapter. | Point to the exported table as the way to look at a position. |
| Reading the results, "`AF` ... the allele frequency" | Allele frequency is glossed only by its name. In one sample I do not know whether 1.00 means every read carried the variant or something about the population. | Say what AF is a fraction of. |
| The metrics files, "`DBSNP_TITV` 2.248 ... `NOVEL_TITV` 1.769" | The table gives no guidance in the What it means column, and the expected range only appears in the paragraph below. I judged the numbers wrong on first read. | Put the expected range in the table row. |
| The metrics files, "`HET_HOMVAR_RATIO` 1.459" | I know what heterozygous means but I do not know what value I should expect, and the paragraph below unpacks only two metrics and not this one. | Give the expected range for this ratio. |
| The metrics files, "`NUM_IN_DB_SNP` 799 ... `NOVEL_SNPS` 36" | 799 plus 36 is 835, which matches, but `TOTAL_SNPS` 835 does not match the 842 SNP rows from `select` or the 836 `PASS` rows. Three different SNP counts in one chapter. | Reconcile the three SNP counts in one sentence. |
| When the metrics step fails, "Rewrite its header so it names only the contigs your calls use" | This is the step I most need and least can do. No tool, no command, no example header. My Step 4 will fail and I cannot get past it. | Give the command or supply a fixture whose header already matches. |
| When the metrics step fails, "the reference is 500,001 bases long" | The fixture is named 10.0-10.5Mb, which is 500,000, so the extra base looks like a mistake in the manual rather than a real off-by-one. | Say why the slice is 500,001 bases. |
| The provenance record, "press Cmd-Shift-Period to show hidden files" | This was genuinely useful and I did not know it. But the section never says how to open the JSON file once I can see it, and JSON is not glossed. | Say the file opens in TextEdit and gloss JSON. |
| The provenance record, "the path of the conda environment GATK ran from" | Conda environment is not explained and appears nowhere else in the chapter. | Gloss conda environment or cut it. |
| The provenance record, "running two of these steps into one folder leaves you with the second one's record and no trace of the first" | This is a real data-loss trap buried in the last third of the chapter, and the Procedure's Steps 2 and 3 both write into the same folder, so following the chapter as written destroys the filter's record. | Warn about this at Step 3, where it first bites. |
| What good looks like, "A double-digit percentage points at a problem upstream in the mapping or the calling" | Mapping is not glossed and calling is used loosely throughout. I know what PCR is but not what mapping means here. | Gloss mapping as lining reads up against the reference. |
| On the command line, `--reference GRCh38.chr20.10.0-10.5Mb.fasta` | This is the only place the downloaded FASTA is used, and it is the only command in the chapter carrying a `--reference` flag, which has no Settings entry. | Add a Settings entry for `--reference`. |
| On the command line, "point `--intervals` at a BED file. A recorded `select` run restricted to the first 100 kb" | The command uses `first100kb.bed` but nothing tells me how to make that file or what is inside a BED file. I cannot run this. | Show the two or three lines the BED file contains. |
| On the command line, "`lungfish-cli gatk markdup` ... It is a different tool from the top-level `lungfish-cli markdup`" | Two subcommands with the same name doing different things, dropped in at the end with a list of flags none of which have Settings entries. I stopped reading here. | Split these two subcommands into their own short section. |

One thing I learned. A filtered VCF carries the arithmetic of its own filters in its header lines, so somebody who receives the file later can find out what `SOR3` meant without asking me.

One thing I still could not do. Run Step 4. The chapter says the metrics step needs a `.dict` file I am told to make with a command that is never given, and a benchmark header I am told to rewrite with a method that is never given.

The sentence I liked most. "Thirteen rows is not many, and finding out that it is only thirteen is itself the result."
