# Reader 3 report

Chapter: `06-human-germline-variants/03-filtering-selecting-and-metrics.md`

Persona: pre-med student, English is my second language. I know biology words
from my textbooks. I have never opened a terminal. Idioms and long sentences
slow me down.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "Every operation in this chapter runs from the command line in a terminal." | I do not know what a terminal is or where it is on my computer. The chapter tells me twice that everything happens there, but the first help arrives much later in Before you start. I stopped at the first sentence of the chapter. | Move the pointer to the CLI Reference appendix into this first paragraph. |
| What it is, "GATK" | GATK appears in the tags, in the second paragraph, and all through the chapter, and it is never said what the letters mean or who makes it. I guessed it is a program. | Say at first use that GATK is a variant analysis toolkit from the Broad Institute. |
| What it is, "That file is a raw answer rather than a finished one." | A file is not an answer, so I read this twice before I understood it is a metaphor for incomplete results. | Say the file contains calls that have not yet been judged for quality. |
| What it is, "including the ones supported by evidence a careful reader would reject" | "a careful reader would reject" is a long way to say "bad evidence", and the sentence has four clauses. I read it three times. | Split into two sentences and say "weak evidence" plainly. |
| What it is, "holds a bare full stop on every row" | "bare full stop" is a British phrase for the dot character. I know the symbol as a period. I did not know if this meant a literal dot in the file. | Write "a single dot character (.)". |
| What it is, "the seventh of the standard columns" | I have never seen a VCF file, so counting to column seven means nothing to me and I cannot check it. | Add one sentence saying a VCF row has fixed columns in a fixed order, or show one example row. |
| What it is, "`filter` marks the suspect rows without deleting them." | Five commands are named in one paragraph in code font. I could not tell whether `filter` is a GATK word, an LGE word, or a general word, because the actual command is `lungfish-cli gatk filter`. | Write the full subcommand name at first use for each of the five. |
| What it is, "or like an artefact" | "artefact" here does not mean an archaeological object, which is the only meaning I knew. The chapter uses it three more times without explaining. | Gloss it at first use as a false signal produced by the laboratory or the machine rather than by the sample. |
| What it is, "Four of the five leave the row count alone" | "leave the row count alone" is an idiom. I understood only after reading the next sentence about adding rows. | Write "do not change the number of rows". |
| Why you would do this, "a variant caller is deliberately generous" | "generous" describing a program is a figure of speech. I paused. | Write that the caller reports every position with any supporting evidence. |
| Why you would do this, "leaves the judgement ... to somebody downstream" | "downstream" is used as a metaphor for a later analysis step and is never explained. It appears again in Settings twice. | Gloss "downstream" at first use as a later step in the analysis. |
| Why you would do this, "a handful of positions where the reads were lopsided or shallow" | Three unexplained words in one clause. "a handful" is vague, "lopsided" I know only from objects, "shallow" I did not connect to read depth until the Reading the results section. | Say "a few positions with unbalanced strand support or low read depth". |
| Why you would do this, "It is a fixed list of arithmetic tests applied to statistics the caller already wrote into each row" | I do not know which statistics, or that a VCF row carries per-variant annotations at all. This is the core idea of the chapter and I could not picture it. | Show one INFO field from a real row before this sentence. |
| Why you would do this, "GATK itself recommends a trained approach for large projects" | "a trained approach" is not explained and no name is given, so I cannot look it up or decide whether my project is large. | Name the method (VQSR) and say roughly how many samples counts as large. |
| Why you would do this, "built from the HG002 chromosome 20 slice" | HG002 is introduced with no explanation of what a "slice" is, and I did not know 500 kb of chromosome 20 was meant until the Before you start section. | Say at first use that the fixture is a 500 kb region of chromosome 20. |
| Why you would do this, "so this is a human example with an answer key" | "answer key" is a school idiom. I worked it out but it took a second read. | Write "a trusted list of the true variants". |
| Why you would do this, "the filter marked 13 rows out of 1,026 as suspect. Thirteen rows is not many" | I am told 13 is not many and 300 is bad, but nothing tells me where the boundary is. The What good looks like section says a double-digit percentage is bad, which is a different scale from a row count, so I could not judge my own future run. | Give one threshold in percent here and use percent consistently. |
| Before you start, "choose **File > New Project** (Cmd-N)" | This is the only step in the chapter I could actually perform, but the chapter has just said nothing happens inside the app, so I did not understand why I need a project at all. The next sentence says the files do not need to be in the project folder. | Say in one sentence what the open project is needed for. |
| Before you start, "Download the file `GRCh38.chr20.10.0-10.5Mb.fasta`" | I could follow the click instructions, but the FASTA file is never used in the Procedure section, only in the On the command line section for `leftalign`. I downloaded a large file and could not tell if I needed it. | Say which step uses the FASTA. |
| Before you start, "the `.fai` index the fixture ships and the `.dict` sequence dictionary you make once with GATK's own `CreateSequenceDictionary`" | Step 4 of the Procedure requires `GRCh38.chr20.10.0-10.5Mb.dict`, but I am never given the command that makes it. The chapter says only "you make once". I could not complete the procedure. | Show the exact command that creates the `.dict` file. |
| Before you start, "`.fai` index" | Never explained. I do not know what it is for or how to check I have it. | Gloss "index file" once and say it must sit beside the data file. |
| Before you start, "This feature is experimental." | I do not know what risk "experimental" carries. Should a pre-med student use this on real data or not? | Say in one sentence what experimental means for the user. |
| Before you start, "answers `Unknown tool pack: gatk-core`, which is a defect rather than a design choice" | "defect rather than a design choice" made me stop, because I could not tell if I was being told to report a bug or to just use the other route. | Write "this is a known bug, so use the Plugin Manager". |
| Before you start, "Each of the six recorded runs below finished in about 1.2 seconds" | I count five commands in the Procedure section, not six. I could not reconcile the number and wondered if a step was missing. | Match the count to the numbered steps or say which runs are counted. |
| Procedure, "a backslash at the end of a line continues one command onto the next line" | I have never typed in a terminal and do not know what a backslash key looks like or whether the indentation on the next line matters. | Add one sentence saying to copy the whole block, including line breaks, exactly. |
| Procedure, Step 1 | Nothing tells me how to get to the folder where `cohort.vcf.gz` lives, and every command uses a bare file name with no folder. As someone who has never opened a terminal, I could not run step 1 at all. | Add one line telling the reader to change to the folder holding the file, or use full paths. |
| Procedure, "Every subcommand prints the GATK command it composed and stops." | I did not understand "composed" as a verb here on the first read, and "stops" made me think it crashed. | Write "prints the GATK command it would run, then exits without running it". |
| Procedure, after Step 1, "with the directory part of each path shortened to three dots" | The printed block shows `.../cohort.vcf.gz` and I did not know if I should type three dots myself. | Say the three dots stand for your own folder and will not appear in your output. |
| Procedure, printed command block | The block is one very long unwrapped line with ten filter expressions. I could not read it on my screen and gave up, even though the text tells me this preview is the only place the tests are written out. | Show the tests as a small table of name, statistic, and threshold. |
| Procedure, Step 1, "`QD < 2.0'`, `FS > 60.0`, `MQ < 40.0`, `MQRankSum`, `ReadPosRankSum`" | Of the six statistics in the default preset, only QD and SOR are explained anywhere in the chapter. FS, MQ, MQRankSum, and ReadPosRankSum are never defined, so I cannot judge four of the ten tests being applied to my data. | Add the four missing statistics to the table suggested above, one line each. |
| Procedure, Step 2, "GATK execution completed with exit code 0." | "exit code 0" is never explained. Later the chapter shows exit code 3 and exit code 2 as failures, so I inferred 0 means success, but only much later. | Say once that exit code 0 means success and anything else means failure. |
| Procedure, Step 3, "`--sample HG002`" | The commands pass `--sample HG002`, but I was never told the sample inside `cohort.vcf.gz` is named HG002, or how to find out the name in my own file. | Add one sentence on how to see the sample names in a VCF. |
| Procedure, Step 4, "`mkdir -p metrics`" | This is a terminal command I have never seen and it is not explained, unlike the `lungfish-cli` ones. I did not know if it was part of LGE. | Say in the lead-in that `mkdir -p` is the terminal command that creates a folder. |
| Procedure, Step 4, "The `--output-prefix` value is a path stem rather than a file name" | "path stem" is not a phrase I know and it is not glossed. | Write "the first part of a file name, which the tool completes for you". |
| Procedure, Step 4 | The command needs `GRCh38.chr20.10.0-10.5Mb.dict`, which the chapter never makes. Also the recorded run needed the benchmark VCF rebuilt, and the fix is deferred to a later section. So this step, as written, cannot succeed. I stopped here. | Either ship a working `.dict` and benchmark, or give the two preparation commands inside step 4. |
| Procedure, "The reason and the fix are in Reading the results, under the heading about the metrics step failing." | The pointer describes the heading instead of naming it, and there is no link. I had to search the page. | Link directly to the "When the metrics step fails" heading. |
| Procedure, Step 5, "Read the two metrics files" | I am not told how to open them. They are described later as plain text, but at this point I did not know whether to double-click, use a spreadsheet, or type something. | Say they are plain text and can be opened in any text editor or spreadsheet. |
| Procedure, Steps 1 to 5 | The `leftalign` and `variants-to-table` steps are never in the numbered procedure at all, only in the closing code section, yet both are described in What it is as core steps of the chapter. I expected seven steps. | Either add them as steps or say the procedure covers three of the five operations. |
| Settings, "meaning a word beginning with two dashes that you add to the command" | This is a good gloss, but it arrives long after Step 1 used four flags. | Move the gloss to the Procedure lead-in. |
| Settings, "The flags belonging to the joint-genotyping subcommand are in the previous chapter" | Three chapters are cross-referenced in one sentence and I could not tell which flags I still need to learn to finish this chapter. | Split into two sentences. |
| Settings, Preset, "which is the more careful order" | "order" confused me because the paragraph is about which value to choose, not about sequence. I read it twice. | Write "which is the more careful way to work". |
| Settings, Preset, "a test tuned for substitutions is not the right test for a length change" | This is the clearest sentence about why SNPs and indels differ, but "substitutions" and "length change" are used as informal synonyms for SNP and indel without saying so. | Pair the informal term with the abbreviation once. |
| Settings, Preset custom, "printed `gatk VariantFiltration -V cohort.vcf.gz -O x.vcf.gz` and nothing more, so the command copies the file and marks nothing" | I could not follow how printing a short command proves the file is copied. That is an inference an expert makes and I cannot. | State the outcome directly and drop the reasoning step. |
| Settings, Sample, "which is the right behaviour for a cohort file whose whole purpose is holding several" | "holding several" has no noun. Several what. I re-read to find "samples" in the earlier clause. | Write "holding several samples". |
| Settings, Type, "`MIXED` for a position carrying both at once" | I could not picture a position that is both a substitution and an insertion until the Reading the results section explained alternative alleles. The order should be reversed. | Add the phrase "as two different alternate alleles" here. |
| Settings, Intervals, "a BED file, a Picard-style interval list, or a bare contig name" | Three formats named, none explained, and BED is not in the glossary list at the top. Later a command uses `first100kb.bed` which is never created. | Gloss BED once and show how the example BED file was made. |
| Settings, Fields, "`CHROM,POS,REF,ALT,QUAL,AF,DP`" | Seven abbreviations in one string. Only AF and DP get a plain name in the same sentence and QUAL is called "the quality score" with no scale. I do not know what a good QUAL is. | Add one short table of the seven defaults. |
| Settings, Fields, "LGE splits your string on the commas and passes each name to GATK separately" | This is implementation detail. I could not tell what I am supposed to do differently because of it. | Reduce to "spaces after commas are ignored". |
| Settings, Left align, "rewrites each indel in its leftmost equivalent position" | "leftmost equivalent position" assumes I know that an indel in a repeat can be written several ways. The reason follows in the next clause, but the term comes first. | Put the repeat explanation before the term. |
| Settings, Split multi-allelics, "keeps GATK's own per-allele statistics attached to a single row" | I do not understand what breaks when the row is split, so I cannot weigh the trade-off the sentence is setting up. | Say what is lost when the row is split. |
| Settings, Max indel length, "The default is 200, which covers the great majority of indels" | I cannot judge 200 because I do not know the typical size of a human indel. "great majority" is not a number. | Give the typical indel size range in bases. |
| Settings, Max leading bases, "treat both this number and the one above as load-bearing for clinical work" | "load-bearing" is an architecture metaphor. I did not know what it asks me to do. | Write "check both numbers carefully before clinical use". |
| Settings, Sequence dictionary, "when your VCF headers are thin" | "thin" describing a header is a figure of speech and I could not tell how to check if mine are thin. | Write "when your VCF header does not list the contigs". |
| Settings, Dry run, "on its own the flag changes nothing, because not executing is already the default" | I read this three times. A setting that does nothing, unless combined with another, is a surprising idea and the sentence buries the useful part at the end. | Lead with the override behaviour. |
| Settings, Extra args, "expect LGE to split that quoted string into separate arguments rather than passing it as one word" | I do not know what "splitting into arguments" means without terminal experience, so I could not tell whether this is good or a warning. | Say it means multiple GATK options fit in one pair of quotes. |
| Settings, "Two of these flags are parsed leniently" | "parsed leniently" is jargon and the paragraph is the most important warning in the chapter, since a typo silently changes my results. I nearly skipped it because the opening words meant nothing. | Open with the consequence, such as a typo is accepted silently. |
| Settings, "a recorded run with `--type SUBSTITUTION` printed a `SelectVariants` command carrying no `-select-type` at all" | I have to notice that a flag is missing from a long printed command to catch this. I do not know what `-select-type` looks like when present. | Show the correct and the broken preview side by side. |
| Reading the results, FILTER table | The table has PASS, SOR3, and QD2, but the preview listed ten tests. I assumed the other eight caught nothing, but the text never says so and I could not be sure the table was complete. | Say the remaining tests marked no rows. |
| Reading the results, "a semicolon-separated list being how a VCF records more than one failure" | The clause is grammatically detached and I read it twice to attach it to the sentence. | Make it its own sentence. |
| Reading the results, "the row's quality score divided by the depth of reads supporting the variant" | Good definition, but "quality score" itself is never defined in this chapter, and it is the numerator. | Gloss quality score once, with its rough range. |
| Reading the results, "had quality scores of 32.64 and 32.6, both barely above the caller's own floor" | I do not know the caller's floor, so "barely above" cannot be checked. Also QD is a ratio, so I could not tell if 32.64 is a QUAL or a QD value. | Give the floor number and say which statistic 32.64 is. |
| Reading the results, "A real variant should be seen about equally from both, so a lopsided count suggests an artefact of the chemistry" | "artefact of the chemistry" assumes I know how strand bias arises in sequencing. I have never done a sequencing run. | Name the cause in a few words, such as an error introduced during library preparation. |
| Reading the results, "the SNP file holds 836 `PASS` rows and the indel file 175" | 836 plus 175 is 1,011, but the filter marked 13 of 1,026, leaving 1,013. I could not make the numbers close and did not know if I had misunderstood or found an error. | Show where the remaining 2 PASS rows are, presumably the MIXED file. |
| Reading the results, "All 19 of the input's multi-allelic rows became two rows each, and none remained." | 1,026 plus 19 is 1,045, so this works, but "none remained" was ambiguous to me, as if the original rows were deleted entirely. | Write "so no multi-allelic rows are left in the output". |
| Reading the results, "which surprises people the first time" | Telling me I will be surprised before telling me why made me stop and look for what I had missed. | State the cause first. |
| Reading the results, "chr20_10.0-10.5Mb" | The contig in the example output has a strange name that does not match the file name `GRCh38.chr20.10.0-10.5Mb.fasta`. I could not tell if this was my expected output. | Say the fixture renames the contig. |
| The metrics files, "TOTAL_SNPS 835" | 835 does not match the 842 SNP rows or the 836 PASS SNP rows given earlier. The table says "PASS rows only", so I expected 836. I could not resolve the difference. | Explain the difference between 836 and 835. |
| The metrics files, "`PCT_DBSNP` 0.9569, The known fraction, 95.7 percent" | The value is a fraction and the explanation is a percent, so I had to convert in my head to check. Also 0.9569 rounds to 95.69, shown as 95.7. | Show both forms in the value column. |
| The metrics files, "`FILTERED_SNPS` 6 ... `FILTERED_INDELS` 7" | Earlier the FILTER table gave 12 SOR3 rows and 2 QD2 rows. 6 and 7 do not obviously relate to those. The text says they sum to 13, which is right, but I could not connect the two breakdowns. | Say one breakdown is by test and the other by variant class. |
| The metrics files, "A transition swaps a base for the other one of the same shape" | "shape" is not the word my textbook uses. I know purine and pyrimidine and would have understood immediately. | Use purine and pyrimidine, with the plain words in parentheses. |
| The metrics files, "genuine human variation runs at roughly 2 to 3, while random sequencing error has no such preference and runs near 0.5" | Good numbers, but the What good looks like section later says "near 2", a narrower target, and I could not tell which to use. | Use the same range in both places. |
| The metrics files, "`HET_HOMVAR_RATIO` 1.459" | The only metric in the table with no guidance on what value is good. I do not know if 1.459 is normal. | Give the expected range or say it is informational. |
| The metrics files, "the detail file adds a few per-sample figures the summary leaves out" | "a few" and no names. I do not know what to look for. | Name one or two of them. |
| When the metrics step fails, "Picard insists that the known-variants file and the input describe exactly the same contigs" | This section explains a failure clearly, but the fix, "Rewrite its header", is not a step I can perform. No command is given and I have never edited a VCF header. | Give the command that rewrote the header. |
| When the metrics step fails, "`Sequence dictionaries are not the same size (195, 1)`" | I could not tell which number belongs to which file until I read the next sentence twice. | Label the two numbers in the sentence. |
| When the metrics step fails, "the rebuilt header claimed a length of 500,000 where the reference is 500,001 bases long" | An off-by-one that I would have no way to predict or check on my own. Nothing tells me how to read the true length from the reference. | Show how to read the contig length from the `.fai` or `.dict`. |
| The provenance record, "press Cmd-Shift-Period to show hidden files" | This I could do. But the file is JSON and I am not told how to open or read it. | Say it opens in any text editor. |
| The provenance record, "the path of the conda environment GATK ran from" | "conda environment" is not explained anywhere in the chapter and is not in the glossary list. | Gloss it or drop it. |
| The provenance record, "running two of these steps into one folder leaves you with the second one's record and no trace of the first" | This is a data-loss warning and it appears near the end of a long section. The Procedure section has me run two `select` commands into the same folder in Step 3 with no warning. | Add the warning to Step 3. |
| What good looks like, "A double-digit percentage points at a problem upstream in the mapping or the calling" | "points at" is an idiom and "upstream" appears again unexplained. Also this is the only place I get a numeric threshold for the marked share. | Write "means there is a problem in the mapping or calling step". |
| On the command line, "`--reference GRCh38.chr20.10.0-10.5Mb.fasta`" | The `leftalign` command in this block is the only place the FASTA is used, and it is not in the Procedure section at all, so I would not have known to download it if I skipped Before you start. | Add `leftalign` to the numbered procedure. |
| On the command line, "point `--intervals` at a BED file. A recorded `select` run ... returned 258 rows ending at position 99,174." | `first100kb.bed` is used in the command but never created and its contents are never shown. I cannot run this. | Show the one line the BED file contains. |
| On the command line, "Two further GATK subcommands prepare BAM files rather than VCFs" | A dense final paragraph listing twelve flags with no line breaks, and BAM is not explained in this chapter. I skipped it. | Move to a table or its own short section. |
| Next, "the base quality score recalibration step that runs before calling rather than after it" | Base quality score recalibration is a new term in the last sentence of the chapter with no gloss. | Gloss it in one clause. |

One thing I learned. A variant caller writes down every position the reads
could support, and deciding which of those to believe is a separate step
that a person has to run on purpose.

One thing I still could not do. I could not finish the procedure, because
Step 4 needs a `.dict` file the chapter never shows me how to make and a
benchmark VCF the chapter says had to be rebuilt without giving the command.

The sentence I liked most. "A reader who receives the file six months later
can find out what `SOR3` meant without asking you."
