# Reader 4 report

Persona. Undergraduate, one semester of Geneious in a course, comfortable with a
graphical sequence viewer, expects every operation to have a dialog, has never
opened a terminal.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "Every operation in this chapter runs from the command line" | This is the first chapter in the manual that tells me there is nothing to click. In Geneious every tool is a menu item with a dialog. I do not know what "the command line" is, where it lives, or whether I already have it. The sentence tells me what I cannot do but not what to do instead. | Add one sentence saying the command line is the macOS Terminal app and pointing to the CLI Reference right here, not eight paragraphs later. |
| What it is, "a tab-separated table listing the positions" | The chapter says the previous one ends with a cohort VCF, but I do not know whether the file I have is a "cohort" VCF or an ordinary one. Nothing tells me how to check. | Say a cohort VCF is simply the VCF produced by joint genotyping, and that it is the file named `cohort.vcf.gz`. |
| What it is, "Its FILTER column, the seventh of the standard columns" | I have never opened a VCF as text. I do not know what the other six columns are, and counting to the seventh in a file I cannot see is not something I can do. | Show two or three lines of a real VCF with the columns labelled, once, before naming the seventh. |
| What it is, "the five GATK steps" | GATK is used as if I know it. It is never expanded or glossed anywhere in the chapter, and it is not in the glossary list at the top. | Gloss GATK at first use as the Genome Analysis Toolkit, the standard package for human variant work. |
| What it is, "`filter` marks the suspect rows" | Backticked words like `filter`, `select`, `leftalign` are introduced as if they are things, but I cannot tell whether they are buttons, files, or words I type. Only much later do I learn they are subcommands. | Say once that these five are words you type after `lungfish-cli gatk`. |
| Why you would do this, "a variant caller is deliberately generous" | I do not know what a variant caller is. In Geneious I clicked "Find Variations". The term is used three times before any explanation. | Gloss variant caller at first use as the program that reads the alignment and writes down each difference it sees. |
| Why you would do this, "statistics the caller already wrote into each row" | I do not know that a VCF row carries statistics, or where in the row they sit. This makes the later QD and SOR discussion feel like it is about something invisible. | Say the caller stores these numbers in the INFO column of each row. |
| Why you would do this, "the filter marked 13 rows out of 1,026" | I cannot judge 13. The chapter later says 1.3 percent is fine, but at this point I have no scale, and it also says three hundred would be alarming without saying where between 13 and 300 I should worry. | Give the range here, for example anything under a few percent is normal. |
| Why you would do this, "built from the HG002 chromosome 20 slice" | "Slice" is used without explanation. I assumed it meant a physical piece of DNA before working out it means a region of the file. | Say a 500 kb region of chromosome 20 rather than "slice". |
| Before you start, "read CLI Reference first" | This is the step I actually need, and it arrives after I have already read two sections written as though I could follow along. I could not perform a single step before this point. | Move the pointer to CLI Reference into the first paragraph of What it is. |
| Before you start, "You need a project open." | This contradicts the sentence two lines above saying no step happens inside the app. If nothing runs in LGE, I do not understand why I must open a project, and the same paragraph then says the files need not live in the project folder. | Say plainly whether the project is required or only conventional. |
| Before you start, "Download the file `GRCh38.chr20.10.0-10.5Mb.fasta`" | The download instructions are clear, but I am never told where to put the downloaded files, and every later command names them with no folder. I do not know how the command finds a file in my Downloads folder. | Say the commands assume you are working in the folder holding these files, and how to get there. |
| Before you start, "Turn on **Show Experimental Features**" | This is the only part of the chapter that happens in a window, and it is buried in a paragraph. I nearly missed it. Also, "experimental" makes me unsure whether my results are trustworthy for a class report. | Split the Plugin Manager steps onto their own lines and say what experimental means for the reliability of the numbers. |
| Before you start, "the `.dict` sequence dictionary you make once with GATK's own `CreateSequenceDictionary`" | This is a required file and the chapter tells me to make it but never shows the command. Step 4 then uses the `.dict` file, so I am stuck. The previous chapter is cited generally, but not for this specific command. | Give the one command that builds the `.dict`, or cite the exact section of the previous chapter. |
| Before you start, "the `.fai` index the fixture ships" | `.fai` is named without explanation and is not in the glossary list. | Gloss it as the index that lets tools jump into the FASTA without reading it all. |
| Procedure, "a backslash at the end of a line continues one command" | I had to read this twice. I do not know what a backslash looks like on my keyboard versus a forward slash, and if I mistype it the command breaks in a way I cannot diagnose. | Show the character itself and say it is the one above Return on a US keyboard. |
| Procedure, "Every subcommand prints the GATK command it composed and stops." | This is the opposite of Geneious, where pressing OK runs the job. I read this twice before believing the tool does nothing by default. | Say explicitly that running a command without `--execute` is safe and changes no files. |
| Step 1, the printed preview block | The block is one enormous unwrapped line. On my screen it runs off the edge and I cannot read the end of it. I also cannot tell whether the tests apply in order or all at once. | Wrap the preview across lines, one filter expression per line. |
| Step 1, "`QD < 2.0`", "`FS > 60.0`", "`MQ < 40.0`" | Six statistics appear here with no explanation. Only QD and SOR are explained later, and only after the results. FS, MQ, MQRankSum, and ReadPosRankSum are never explained at all, so I cannot tell what a row labelled `MQ40` did wrong. | Add one short clause per statistic, or say plainly that only the two you meet in the results are explained. |
| Step 1, "with the directory part of each path shortened to three dots" | I read this twice. I expected my own output to look like the book and it will not. | Say the three dots stand for whatever folder you are working in. |
| Step 2, "Provenance: .../.lungfish-provenance.json" | The word provenance is used here with no gloss. It is glossed only in a much later subsection, after I have already seen it twice. | Gloss it at this first appearance as a record of exactly what was run. |
| Step 3, "`--sample HG002`" | I do not know where the sample name comes from or how to find it in my own file. If my VCF calls the sample something else, the command silently produces nothing useful. | Say the sample name is the column heading in the VCF and how to look it up. |
| Step 3 | The step splits by class but never runs `leftalign` or `variants-to-table`, which the chapter promised as two of the five operations. They appear only in the final code block. I followed Procedure top to bottom and produced no table. | Add the two missing steps to Procedure, or say the Procedure is a subset. |
| Step 4, "`mkdir -p metrics`" | This is a terminal command that is not a `lungfish-cli` command, and it is not explained. I do not know what it does or where the folder appears. | Say it creates a folder named metrics in the folder you are working in. |
| Step 4, "the recorded run needed the benchmark VCF rebuilt" | The chapter tells me the file I was told to download will not work, then defers the fix to a later section, and the later section describes the problem without giving me commands to fix it. So I cannot complete Step 4. | Give the rebuild commands, or ship a benchmark file that already matches. |
| Settings, "**Preset.**" | The Settings section is nineteen bold paragraphs with no headings. Finding the flag I want means reading all of it. In Geneious the settings are laid out in the dialog next to the thing they affect. | Group the entries under one subheading per subcommand. |
| Settings, "A fourth value, `custom`, is accepted and applies no filter expressions" | I had to read this twice to understand that a documented value does nothing useful. It reads as a bug report inside a settings list. | Say in the first sentence that `custom` is not usable from the command line. |
| Settings, "**Fields.** ... The default is `CHROM,POS,REF,ALT,QUAL,AF,DP`" | AF and DP are never expanded. The results section calls them allele frequency and depth, but only in passing, and I do not know what allele frequency means for a single person's genome. | Expand AF and DP where the default is given, and say what allele frequency means in one sample. |
| Settings, "**Max leading bases.** ... treat both this number and the one above as load-bearing for clinical work" | "Load-bearing" is a metaphor I had to stop and parse. I also do not know how to tell whether my indels sit inside long repeats, so I cannot decide whether to change 1000. | Say what happens concretely and give a symptom that means the default is too small. |
| Settings, "**GVCF input.**" | GVCF appears with no explanation and is not in the glossary list. The previous chapter may cover it, but this entry does not say so. | Gloss GVCF at first use here. |
| Settings, "An unrecognised `--preset` value falls back to `best-practices-both` rather than failing" | This frightened me. A typo silently changes my analysis and nothing tells me. Coming from Geneious, where a dialog cannot be mistyped, this is the largest behaviour difference in the chapter and it is one paragraph near the end of a long list. | Raise this into a warning near Step 1, where I first type a preset. |
| Reading the results, "one row failed both tests and carries `QD2;SOR3`" | The arithmetic here needed two readings. 12 plus 2 is 14, then 13 distinct rows, and I had to work out that the double-counted row is the difference. | State the overlap first, then the counts. |
| Reading the results, "quality scores of 32.64 and 32.6, both barely above the caller's own floor" | I do not know what the caller's floor is, so "barely above" means nothing to me, and the QD threshold in the filter is 2.0, not 32. The two numbers seem unrelated. | Give the floor value, and say whether 32.64 is a QUAL or a QD. |
| Reading the results, "the SNP file holds 836 `PASS` rows and the indel file 175" | 836 plus 175 is 1,011, but the PASS total was 1,013. I could not account for the missing two and stopped to check my arithmetic. | Say the two MIXED rows carry the remainder. |
| Reading the results, "Position 29,224 shows the effect plainly" | Every other position in the chapter is given without a contig, and the table row shows `chr20_10.0-10.5Mb` and position 2078. I could not tell whether 29,224 is a position in the slice or in chromosome 20. | Say positions are counted from the start of the slice. |
| The metrics files, "`TOTAL_SNPS` 835" | The results section earlier reported 842 SNP rows and 836 PASS SNP rows. Now the metric is 835. Three near-identical numbers with no explanation of why they differ made me distrust all of them. | Explain in one sentence why Picard's count differs from the select count. |
| The metrics files, "`DBSNP_TITV` 2.248" | The good range 2 to 3 is given only a paragraph later, and separately I am told 0.5 means error. I do not know what 1.769 for the novel set means for whether I should worry. | Say directly whether a novel TiTv of 1.769 is acceptable. |
| The metrics files, "`HET_HOMVAR_RATIO` 1.459" | This is the only metric in the table with no interpretation anywhere in the chapter. I do not know what a good value is. | Give the expected range, or drop the row. |
| When the metrics step fails, "The fix in both cases is the same" | The fix is described in words ("rewrite its header") but no command is given. Rewriting a header of a compressed VCF is not something I can do from this text. | Give the commands, or say plainly this is beyond the chapter and the step can be skipped. |
| The provenance record, "running two of these steps into one folder leaves you with the second one's record" | The Procedure steps I was told to run do exactly this, so following the chapter destroys my own provenance and the chapter never warns me at the step. | Warn at Step 3, where two runs write into one folder. |
| On the command line, the closing block | This block runs `leftalign` and `variants-to-table`, which Procedure never covered, and it repeats commands I already ran. I could not tell whether this is a summary to read or a script to run. | Label it as a recap of the whole chapter rather than new steps. |
| On the command line, "point `--intervals` at a BED file" | The chapter tells me to point at `first100kb.bed` but never says how to make one or what is inside it. | Show the one line the BED file contains. |
| On the command line, "`lungfish-cli gatk markdup` ... It is a different tool from the top-level `lungfish-cli markdup`" | Two commands with the same name doing different things, described in one dense paragraph at the very end, with eleven flags listed inline. I read this twice and still could not tell which one I would want. | Split the two paragraphs and say in one sentence when to use each. |

The one thing I learned. A filtered VCF carries its own `##FILTER` header lines,
so somebody reading the file later can find out what a label meant without
asking me.

The one thing I still could not do. Finish Step 4, because the benchmark file I
was told to download does not match the cohort VCF and the chapter never gives
the commands that fix it.

The sentence I liked most. "Thirteen rows is not many, and finding out that it is
only thirteen is itself the result."
