# Reader 1 — Filtering, Selecting, and Metrics

Persona: sophomore, one genetics course, never opened a terminal, never used sequencing software.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "Every operation in this chapter runs from the command line in a terminal." | I have never opened a terminal. The chapter opens by telling me the one thing I cannot do, and I did not know whether to keep reading or stop. | Add one sentence saying to read the CLI Reference appendix first if you have never used a terminal, right here instead of two sections later. |
| What it is, "the five GATK steps" | GATK is never spelled out or explained anywhere in the chapter. I do not know if it is a company, a program, or a file format. | Gloss GATK at first use as the toolkit the steps come from. |
| What it is, "`filter` marks the suspect rows" | The five words in code font (`filter`, `select`, `leftalign`, `variants-to-table`, `collect-metrics`) are subcommands, but nothing says so until the Procedure section. I read them as file names. | Say once that these are the five subcommand names you type. |
| What it is, "including the ones supported by evidence a careful reader would reject" | I do not know what evidence a careful reader would reject. That is exactly the thing I came to the chapter to learn, so using it as a known quantity stopped me. | Name one concrete example, such as a position where only two reads support the change. |
| What it is, "Its FILTER column, the seventh of the standard columns" | I do not know the standard columns or their order, and counting to seven does not help me find it. | Say that the FILTER column is the one headed FILTER, and drop the ordinal. |
| Why you would do this, "a variant caller is deliberately generous" | The term "variant caller" appears here for the first time in this chapter with no gloss. I guessed it is the program from the previous chapter, but I was not sure. | Gloss variant caller at first use in this chapter. |
| Why you would do this, "It is a fixed list of arithmetic tests applied to statistics the caller already wrote into each row" | I do not know that the caller writes statistics into each row, or where in the row they live, so I could not picture what is being tested. | Add a half sentence saying these statistics sit in the INFO field of each row. |
| Why you would do this, "GATK itself recommends a trained approach for large projects" | "Trained approach" is unexplained. I do not know what it would be trained on or why size matters. | Name it (machine-learning recalibration) and say it needs many samples. |
| Why you would do this, "built from the HG002 chromosome 20 slice" | I did not know what a slice is, and "500 kb fixture slice" later did not clear it up. | Say once that the slice is a 500,000 base stretch of chromosome 20 rather than the whole chromosome. |
| Why you would do this, "the filter marked 13 rows out of 1,026" | I could not judge 13 out of 1,026 until the number 1.3 percent appeared much later in Reading the results. At this point I had no scale. | Give the percentage here, at first mention. |
| Before you start, "Turn on **Show Experimental Features** in **Settings > Advanced**" | Settings is not on the menu bar of most Mac apps under that name. I did not know whether to look in the app menu or in a Preferences window. | Name the menu path fully, for example LGE > Settings, and give the shortcut. |
| Before you start, "Installing it from the command line does not work and answers `Unknown tool pack: gatk-core`, which is a defect" | I could not tell whether I should worry that a documented defect will also break the steps I am about to run. | Add four words saying the defect affects only installation, not the runs. |
| Before you start, "the `.fai` index the fixture ships and the `.dict` sequence dictionary you make once with GATK's own `CreateSequenceDictionary`" | This is a required step with no command given. I could not perform it. The Procedure never mentions it either, yet Step 4 passes a `.dict` file I was never told how to build. | Give the actual command that makes the `.dict`, in a code block, before the Procedure. |
| Before you start, "Everything in this chapter runs from the command line" | This repeats the first sentence of the chapter almost word for word. I read it twice checking whether it said something new. | Delete one of the two. |
| Before you start, "Download the file `GRCh38.chr20.10.0-10.5Mb.fasta`" | I download the FASTA here, but the Procedure never uses it. Only the On the command line section does, in the `leftalign` block that the Procedure skips entirely. I wondered if I had missed a step. | Say what the FASTA is for, or move the download next to the step that uses it. |
| Procedure, "Every subcommand prints the GATK command it composed and stops." | I had to read this twice. On first pass I thought "stops" meant it crashed. | Say it prints the command and exits without doing anything. |
| Procedure, Step 1, "--preset best-practices-both" | The preset is passed explicitly here, but the Settings section says it is already the default. I did not know whether leaving it out would change anything. | Note that this is the default and is shown for clarity. |
| Procedure, Step 1 output block, "`QD < 2.0` ... `FS > 60.0` ... `MQ < 40.0`" | Four of the six SNP tests (FS, MQ, MQRankSum, ReadPosRankSum) are never explained anywhere in the chapter. Only QD and SOR get an explanation, and only later. I cannot read the preview the chapter tells me is the only place the tests are written out. | Add one line each for FS, MQ, MQRankSum, and ReadPosRankSum, or say plainly that only QD and SOR are explained here. |
| Procedure, Step 1, "Read it, because this preview is the only place the tests are written out." | The instruction is to read something I have no way of understanding at this point in the chapter. The explanation of QD and SOR is two sections later. | Point forward to Reading the results for what the labels mean. |
| Procedure, Step 2, "GATK execution completed with exit code 0." | I do not know what an exit code is or that 0 means success. It appears again as codes 2 and 3 in the failure section. | Gloss exit code 0 as success at first appearance. |
| Procedure, Step 3, "--sample HG002" | I did not know where the sample name comes from or how I would find it in my own file. The chapter uses HG002 as if I had chosen it. | Say the sample name is the column header in the VCF and name the way to list them. |
| Procedure, Step 3 | `leftalign` is one of the five steps the chapter promises, but it has no step in the Procedure. It only appears in the final code block. I went looking for a Step and did not find one. | Either add it as a numbered step or say in the Procedure lead-in that leftalign is optional and shown at the end. |
| Procedure, Step 4, "The `--output-prefix` value is a path stem rather than a file name" | "Path stem" is not a phrase I know. The example `metrics/cohort` has no file extension, which made me think I had mistyped something. | Say a stem is the start of a file name that the tool finishes for you. |
| Procedure, Step 4, "the recorded run needed the benchmark VCF rebuilt so that its list of contigs matched" | This says the step I am about to run will fail and points to a section that tells me to rewrite a VCF header. It never tells me how to rewrite a VCF header, so I cannot finish the chapter. | Give the actual rebuild command, or supply an already-fixed benchmark file in the fixtures. |
| Procedure, Step 4, "contigs" | Contig is used repeatedly (Step 4, the failure section, the sequence-dictionary setting) and is never glossed. It is also not in the glossary list at the top of the file. | Gloss contig at first use as one named sequence in the reference, such as a chromosome. |
| Settings, "meaning a word beginning with two dashes that you add to the command" | This is the only place the chapter teaches command-line syntax, and it arrives after I have already been asked to type six commands. | Move this gloss to the Procedure lead-in. |
| Settings, Preset, "a test tuned for substitutions is not the right test for a length change" | This says the careful order is to split first and then filter, but the Procedure does the opposite, filtering first and then splitting. I could not tell which order I should follow. | Say why the chapter's own order is fine for this example. |
| Settings, Type, "`MIXED` for a position carrying both at once" | I could not picture a position that is a substitution and a length change at once until the example at position 29,224 appeared much later. | Move a one-line example here. |
| Settings, Fields, "the allele frequency, and the depth" | Allele frequency and depth are named as if I know how to read them. A value of 1.00 in the example row means nothing to me. | Gloss both, and say what AF 1.00 means for one sample. |
| Settings, Max leading bases, "treat both this number and the one above as load-bearing for clinical work" | "Load-bearing" is a metaphor from building construction. I stopped on it. | Say the numbers matter and that a wrong one fails silently. |
| Settings, "An unrecognised `--preset` value falls back to `best-practices-both` rather than failing" | This means I cannot tell from the output whether my command worked as intended, which frightened me more than the paragraph seems to intend. | State plainly that the composed command is the check, in the same sentence. |
| Reading the results, "The two counts below `PASS` add to 14 rather than 13" | I had to read the table, then this sentence, then the table again to see that the double-failure row is counted in both the 12 and the 2. | Mark the double-failure row in the table itself. |
| Reading the results, "quality scores of 32.64 and 32.6, both barely above the caller's own floor" | I do not know the caller's floor, so I cannot see that 32.64 is barely above anything. | Give the floor value. |
| Reading the results, "one strand of the DNA" | I know DNA has two strands from class, but I do not know why a sequencing read comes from one strand or why that would be lopsided. | Add half a sentence connecting reads to strands. |
| Reading the results, "836 `PASS` rows and the indel file 175" | 836 plus 175 is 1,011, but the filtered file has 1,013 PASS rows. The missing two are presumably the MIXED rows, but the text does not say so and I spent time on the arithmetic. | Say the remaining two PASS rows are the MIXED ones. |
| Reading the results, "All 19 of the input's multi-allelic rows became two rows each" | 1,026 plus 19 is 1,045, which works, but the text never says the row count rose by exactly the number of multi-allelic rows. I checked by hand. | State the arithmetic. |
| The metrics files, "`TOTAL_SNPS` 835" | The select step reported 842 SNP rows and 836 PASS SNPs, and the metrics report 835. Three different SNP counts appear with no explanation of why they differ. I could not tell which is the real answer. | Explain the difference between the counts, or say plainly they count slightly different things. |
| The metrics files, "`DBSNP_TITV` 2.248" | The row says only "Transition to transversion ratio among the known ones" with no indication of whether 2.248 is good until a paragraph later. | Put the expected range in the table row. |
| The metrics files, "`HET_HOMVAR_RATIO` 1.459" | This is the only metric in the table that is never explained or judged. I do not know if 1.459 is good, and heterozygous versus homozygous is the one bit of genetics I do know. | Say what value to expect, or drop the row. |
| The metrics files, "`FILTERED_SNPS` 6" and "`FILTERED_INDELS` 7" | Earlier the chapter said 12 rows failed SOR3 and 2 failed QD2. I could not reconcile 6 and 7 with 12 and 2 without being told these count different things. | Note that the earlier split is by test and this one is by variant class. |
| The metrics files, "A transition swaps a base for the other one of the same shape" | "Same shape" is not the vocabulary my genetics course used. We said purine and pyrimidine. I had to work out that shape meant ring structure. | Use purine and pyrimidine, glossed in the same sentence. |
| When the metrics step fails, "Sequence dictionaries are not the same size (195, 1)" | I could not work out what to do with this. The fix says rewrite the header, but no command or tool is named, so I stopped here. | Give the rebuild command. |
| When the metrics step fails, "the rebuilt header claimed a length of 500,000 where the reference is 500,001 bases long" | The off-by-one is stated but not explained, and the file name says 10.0-10.5Mb, which is 500,000. I read this three times. | Say where the extra base comes from. |
| The provenance record, "the path of the conda environment GATK ran from" | Conda is never explained and is not in the glossary list. | Gloss conda, or drop the mention. |
| The provenance record, "a SHA-256 checksum" | SHA-256 is unexplained. The next sentence explains checksum but not the SHA-256 part, so I did not know if they were the same thing. | Say SHA-256 is the recipe used to compute the checksum. |
| What good looks like, "A double-digit percentage points at a problem upstream in the mapping or the calling" | "Upstream" is used here and in Why you would do this without a gloss. I guessed it means an earlier step. | Gloss upstream at first use. |
| On the command line, "point `--intervals` at a BED file" | The block uses `first100kb.bed`, but nothing tells me how to make a BED file or what one contains. I could not run this example. | Show the two-line contents of the BED file. |
| On the command line, "`--validation-stringency`" | Six flags for markdup and five for validate-sam are listed in one dense paragraph with no defaults or explanations for several of them. I could not use any of them. | Either drop the paragraph or give each flag its own line. |
| On the command line, "It is a different tool from the top-level `lungfish-cli markdup`" | Two commands with the same name doing different things, mentioned once in passing near the end. I had to read it twice to believe it. | Keep this, but set it off so it is not lost in a paragraph. |

Row count: 44.

The one thing I learned: a filter here marks bad rows instead of deleting them, so the row count staying the same is the sign it worked.

The one thing I still could not do: build the `.dict` sequence dictionary or rebuild the benchmark VCF header, so I could never finish Step 4.

The sentence I liked most: "A reader who receives the file six months later can find out what `SOR3` meant without asking you."
