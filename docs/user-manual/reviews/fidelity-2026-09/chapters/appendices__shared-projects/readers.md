# Merged reader report - Shared Projects and Bundle Migration

Four readers reviewed this chapter. Reader 1 is a sophomore fresh from one genetics course who has never opened a terminal. Reader 2 is a senior undergraduate with two years of wet-lab experience in library prep and PCR, comfortable at the bench but new to data analysis and to the terminal. Reader 3 is a pre-med student reading English as a second language, strong on biology vocabulary but slow on idiom and long sentences, and has never opened a terminal. Reader 4 has used Geneious for one semester, expects a dialog for every action, and has never opened a terminal. Reader 1 submitted 42 rows, reader 2 submitted 50 rows, reader 3 submitted 42 rows, and reader 4 submitted 54 rows, for 188 source rows in total.

| Location | Issue | Suggested fix | Readers |
|---|---|---|---|
| What it is, first command name, and Before you start, "Every command below is a command-line command" | All four readers, none of whom had opened a terminal, hit the first command name or the first command-line paragraph with nothing telling them what app to open or where typing happens. | Name Terminal once, near the first command name, and say plainly that the rest of the chapter is typed there. | 1, 2, 3, 4 |
| Locking a project from the command line, "On success it prints three lines and exits 0" (and every later exit code) | All four readers met "exits 0" and later "exits 1" with no gloss, and could not tell success from failure in the app's own output. | Gloss the exit code once, at first use, saying 0 means success and any other number means the command refused or failed. | 1, 2, 3, 4 |
| What it is, `.lungfish/project.lock`, Reading the window's read-only state, the `lock-recovery` folder, and Migrating older bundles, `.manifest.json.backup` and `.lungfish/migrations/` | All four readers went looking in Finder for a file or folder whose name starts with a dot and found nothing, because the chapter never says dot-prefixed names are hidden in Finder. | State once, at the first dot-prefixed name, that such names are hidden in Finder, and repeat the reminder briefly wherever a new hidden file or folder is introduced. | 1, 2, 3, 4 |
| Why you would do this, "a project catalog inconsistent" | All four readers met "project catalog" with no gloss and could not tell whether it is a file, a window, or a list, or whether they would ever see it. | Gloss the project catalog at first use, or drop the term and say "the project's own index of its contents." | 1, 2, 3, 4 |
| Reading the window's read-only state, "when the lock is not from a live local process" | All four readers met "live local process" describing when Recover and Open appears, before the four lock states that would explain it are defined two sections later. | Move the four-state list before this section, or gloss "live local process" here in plain words. | 1, 2, 3, 4 |
| Reading the window's read-only state, "the owner and host and process number" | All four readers did not know what a host or a process number is, or what either field tells them to do next. | Gloss host as the computer's name and process number as the app's id for that run, and say what each is for. | 1, 2, 3, 4 |
| Reading the window's read-only state, "a checksum of the archived bytes" | All four readers met "checksum" with no gloss, in a list of recovery-file details a student would never need to read by hand. | Gloss checksum once as a short fingerprint that changes if the file changes. | 1, 2, 3, 4 |
| Reading the window's read-only state, "has no project store" | All four readers met "project store" as the deciding clue for the no-banner, read-only case, with no definition anywhere in the chapter. | Gloss project store in the same sentence, as the app's own index file inside the project. | 1, 2, 3, 4 |
| Locking a project from the command line, the backslash before a space in the example path | All four readers stopped at the backslash before "Manual" in the example command and took it for a typo in the manual, since escaping a space was never explained. | Add one sentence at the first command saying the backslash protects the space in the folder name. | 1, 2, 3, 4 |
| Locking a project from the command line, "The record it writes holds twelve fields" | All four readers hit a single sentence naming twelve fields in a row and lost their place, especially since the JSON block right after already shows them. | Cut the prose list and let the example JSON block speak for itself, or shorten to the three fields a reader actually uses. | 1, 2, 3, 4 |
| Locking a project from the command line, "`machineIdentifier` is an opaque identifier for the Mac itself" | All four readers read "opaque identifier" as meaning unclear or unclear-in-a-worrying-way rather than simply not human-readable. | Say plainly that it is a meaningless code for the computer, unrelated to any personal information, that stays the same over time. | 1, 2, 3, 4 |
| Locking a project from the command line, "Verified on a copy..." asides | All four readers read these verification asides as notes the author left for another author, not instruction meant for a reader, and could not tell if they had to reproduce the test. | Move the verification notes out of the reader's path, into a footnote or an admin-only note. | 1, 2, 3, 4 |
| Locking a project from the command line, "a script that needs an unbroken hold must either keep an LGE process alive" | All four readers do not write scripts and could not act on this advice, since keeping a process alive is never explained as something they could do. | Mark this paragraph as being for script authors, or state plainly that a window-only reader can skip it. | 1, 2, 3, 4 |
| Unlocking a project, the two conditions for who may remove a lock | All four readers hit one long sentence stating two allowed cases in a single conditional and had to work out the logic by hand. | Split the two cases into two short sentences or a short list. | 1, 2, 3, 4 |
| Migrating older bundles, "Bundles inspected: 4 / Current: 1 / Unsupported: 2" | All four readers tried to add up the reported counts, found they did not reach the total, and thought their own run was broken, since the explanation comes only later in the chapter. | Put the caution that the counts do not add up immediately above or below the example block, not paragraphs later. | 1, 2, 3, 4 |
| Provenance expectations, the sentence listing everything a migration record holds | All four readers hit a single sentence naming ten or eleven recorded items and lost the thread partway through. | Break the list into a short bulleted list, or cut it to what a reader would actually look up. | 1, 2, 3, 4 |
| Provenance expectations, "The wider rule is a requirement on migrations LGE has not written yet" | All four readers could not tell that this paragraph describes a rule for a future version rather than the app they have, and read it two or three times trying to reconcile it with what came before. | Open the paragraph with a plain flag, such as "this describes a planned rule, not today's app." | 1, 2, 3, 4 |
| Before you start, "Every command below is a command-line command, because the window has no equivalent...", What good looks like, the four-item checklist, and Migrating older bundles, opening paragraph | Three readers found that the chapter's most important fact for a window-only reader, that migration and locking from the window has no equivalent, is buried mid-paragraph, never repeated where later sections need it, and never followed by what such a reader should do instead. | State at the top of the chapter, in its own short paragraph, that the window has no equivalent for these actions, say what a window-only reader does instead, for example asking the other person to quit and reopen, and repeat the reminder at Migrating older bundles and in the checklist. | 2, 3, 4 |
| What it is, "The lock is advisory..." and "It does not ask the operating system to refuse anyone access" | Three readers understood that the lock is advisory but could not tell whether their real risk, a labmate's Finder copy, Dropbox sync, or rsync job, would be stopped by it or not. | State plainly that a sync client or a plain Finder copy is not stopped by the lock. | 1, 2, 4 |
| Why you would do this, "a cached summary the window uses to draw a reference quickly" | Three readers met "cached summary," "draw," and "reference" together in one phrase and could not picture what breaks, or what they would notice, if the cache were missing. | Say what the reader would actually see if this cache were missing, for example the chromosome list taking longer to appear. | 1, 2, 3 |
| Before you start, "Build it by following the instructions in the manual's fixtures on GitHub" | Three readers could not perform this step from the chapter text alone, since it points to an external GitHub page and never says what a fixture is or whether an account is needed. | Summarize the demo-project build in two or three sentences in the chapter itself, and say plainly that no account is needed. | 1, 2, 4 |
| Reading the window's read-only state, the banner listing five to seven fields | Three readers could read the read-only banner but could not judge any field in it, and most wanted to know only which field names the person to go ask. | Say in one sentence which field tells the reader who to contact. | 1, 2, 4 |
| Locking a project from the command line, JSON mentioned or shown with no gloss | Three readers met "JSON" used and shown in a code block with no definition anywhere in the chapter. | Gloss JSON once, at first use, as a plain-text format for structured data. | 1, 2, 4 |
| Locking a project from the command line, the hostname anecdote about `raven.local` | Three readers found the aside about the same Mac reporting two different hostnames read like the author doubting the app, or a private test note, rather than something to act on. | Cut the anecdote to one clause stating that hostnames can change, or move it to a note. | 2, 3, 4 |
| Locking a project from the command line, "so `exclusive` and `maintenance` block writes in exactly the same way" | Three readers met `maintenance` as an allowed value of `--mode` for the first and only time here, with no earlier statement of the full list of accepted values. | List the accepted values of `--mode` where the flag is first introduced. | 1, 2, 4 |
| Locking a project from the command line, "Now the part that surprises people." | Three readers had to read the paragraph twice to find the rule, since it is told as a story that ends with its conclusion instead of leading with it. | Lead with the rule, that a lock taken from the command line marks intent and does not hold, then explain why. | 2, 3, 4 |
| Unlocking a project, the doubled period in the quoted error message | Three readers hit "isn't in the correct format.." with two periods and assumed it was a typo in the manual rather than the app's real output. | Note once that the doubled punctuation is what the app actually prints. | 1, 3, 4 |
| Migrating older bundles, folder extension used to detect a bundle | Three readers do not know what a folder's extension is, especially since Finder hides extensions by default, and could not check this rule against their own files. | Give one example filename with its extension marked, and note that Finder may be hiding it. | 1, 2, 3 |
| Migrating older bundles, `unreadable` bundle status | Three readers read `unreadable` as meaning damage to their lab's data, and were only reassured a paragraph or more later that it is expected for some bundle types. | State in the status list itself, not later, that unreadable is expected and harmless for certain bundle types. | 2, 3, 4 |
| Migrating older bundles, the reference-bundle manifest cache holding the chromosome list | Three readers could not say what they would gain by filling this cache, since the benefit is buried one clause deep inside a nested definition. | Lead with the payoff, for example that a filled cache makes the reference open faster. | 1, 2, 4 |
| Migrating older bundles, the `--format` option and its `json`/`tsv`/`text` values | Three readers do not script and could not tell whether this paragraph, full of machine-readable and tab-separated-values language, applied to them at all. | Mark the paragraph plainly as being for script authors, or say readers who do not script can skip it. | 2, 3, 4 |
| What good looks like, "suspect the project before the app" | Three readers understood the sentence but were left with no next step, since no action follows naming what to suspect. | Name the first concrete thing to check, for example reopening the project or asking a labmate. | 1, 3, 4 |
| What it is, "is a `.lungfish` folder holding one analysis" | Two readers with data-analysis backgrounds could not tell what "one analysis" means as a unit, whether it is one sample, one run, or one whole study. | Add a clause giving one concrete example of what a single project holds, for example all the samples and results from one sequencing run. | 1, 2 |
| What it is, "a `.lungfish` folder" first introduced as a bundle | Two readers, coming from a single-icon mental model of a project, did not know how they would ever look inside the folder, or whether they needed to. | Say once that the reader normally never opens the folder directly, and name Show Package Contents only if they ever need to. | 3, 4 |
| What it is, "Take a lock before any scripted maintenance" | Two readers do not write scripts and could not tell whether this whole paragraph of advice applied to them at all. | Say plainly who this advice is for, for example "if you run batch jobs," and that a window-only reader can skip it. | 2, 4 |
| Before you start, "`--force` in particular destroys a record" | Two readers met `--force` used before flags were introduced as a concept, and could not tell whether it was something typed, a checkbox, or a setting. | Introduce flags, including the double-dash form, as things typed after a command, at first use. | 2, 4 |
| Before you start, "Duplicate the demo project in Finder" | Two readers could not tell whether duplicating a `.lungfish` folder in Finder is safe, or whether the working copy would still function. | State plainly that duplicating in Finder copies the whole bundle intact and it will still work. | 1, 3 |
| Before you start / Locking a project, "point the commands at the duplicate" | Two readers did not know how to turn a duplicated folder into a path typed into a command. | Show the duplicate's path used in one worked example command. | 2, 4 |
| Locking a project from the command line, "It is stale when that process has exited, or when the process number has been reused" | Two readers were unsettled by process-number reuse, a detail that made them distrust the whole locking mechanism, inside a sentence already carrying two conditions. | Split into two sentences, and note that reuse is rare. | 2, 3 |
| Locking a project from the command line, "process id" versus "process number" | Two readers met both terms used for what turned out to be the same thing and thought they were two different values. | Use one term for this value throughout the chapter. | 2, 4 |
| Unlocking a project, the warning that `--force` does not stop the other process from writing | Two readers found the most alarming sentence in the chapter, that the other process keeps writing even after a forced unlock, placed after the instructions for using `--force` rather than before. | Move this warning before the flag is introduced, not after. | 2, 4 |
| Locking a project, "a cleanup step" | Two readers do not write scripts and could not tell who this phrase was written for. | Say plainly this is for script authors, or move it to a note. | 3, 4 |
| Migrating older bundles, "schema" used bare after being glossed as "schema version" | Two readers could not tell whether bare "schema" and "schema version" were the same idea, since the terms do not match on reuse. | Use "schema version" consistently throughout the chapter. | 1, 3 |
| Migrating older bundles, "`migration-available` under `--dry-run` means a fillable gap was found" | Two readers met "fillable gap" as a metaphor with the actual missing thing named only paragraphs later. | Name the missing thing directly here instead of calling it a gap. | 3, 4 |
| Provenance expectations, "writes no provenance sidecar" | Two readers met "provenance sidecar" linked but never glossed in a section that is entirely about it. | Gloss sidecar at first use here as a small companion file stored next to the data. | 1, 4 |
| Provenance expectations, "stderr where useful, and wall time" | Two readers met "stderr" and "wall time" with no gloss and no idea what either meant. | Gloss both once, or cut them for this audience. | 2, 4 |
| What good looks like, "A `(Read Only)` title with a yellow banner means a lock. Without a banner it means no project store." | Two readers singled this out as the one check in the section they could actually perform without a terminal, and the clearest sentence in the chapter, though "project store" is still undefined at this point. | Keep this check as written, and add the project-store gloss here since it is a window reader's clearest anchor. | 3, 4 |
| Why you would do this / Provenance expectations, "a project catalog inconsistent" and "Locks are coordination metadata rather than scientific output" | Two readers met "coordination metadata" as two abstract words together, familiar from sequencing sample sheets but not from this use. | Say plainly that locks are bookkeeping, not a scientific result, so no record is written for them. | 2, 3 |
| What good looks like, "a second attempt against a live owner exits 1" | One reader found this contradicted an earlier finding, that a second `project lock` succeeds because the first owner is already dead, and could not tell which behavior to expect. | Add the qualifier that a second attempt exits 1 only while the first owner is still running. | 1 |
| What it is, "The lock is advisory" | One reader found "advisory" itself a hard word, known only from "academic advisor," and had to reread the sentence to attach the gloss that followed it to the right idea. | Put the plain meaning first, then attach the word "advisory" as the label afterward. | 3 |
| What it is, "no operation that writes into the project will start" | One reader met "operation" as a technical noun for the first time, with no gloss, and could not tell whether opening a viewport counts as one. | Gloss "operation" at first use as a named piece of work the app runs. | 1 |
| What it is, "The lock is advisory, meaning it works only because every LGE process agrees to check it." | One reader met "process" used for the first time with no gloss and read it as "program," which caused confusion later when one program turned out to have many processes. | Gloss "process" at first use as one running copy of a program. | 2 |
| What it is, "and two copies of LGE can open it together" | One reader read "two copies of LGE" as two installations on one Mac rather than two people each running the app. | Write "two people running LGE" instead of "two copies of LGE." | 3 |
| What it is, "every bundle can be inspected" | One reader met "bundle" used here but not explained until two paragraphs later, and had to scroll ahead to find the gloss. | Move the bundle gloss earlier, or gloss it at this first use. | 1 |
| What it is, "Every viewport opens" | One reader met "viewport" used as already known, with no gloss and no link, in a chapter a reader could reach before the one that defines it. | Gloss viewport at first use, or link it. | 2 |
| What it is, "a small file at its root naming what the bundle holds" | One reader read "at its root" as filesystem language and pictured a tree rather than a folder location. | Say "at the top level of the bundle folder" instead. | 2 |
| What it is / Migrating older bundles, "Manifests carry a schema version" | One reader could not judge the schema-version number, since nothing in the chapter says what the current version is or where to see their own project's value. | State the current schema version once, and say where a reader can see their own. | 2 |
| What it is, "run `project migrate` with `--dry-run` first" | One reader met "--dry-run" used before it was explained, and read the two leading dashes as part of the word itself. | Gloss "--dry-run" at this first use as a mode that reports without changing files. | 1 |
| What it is, "Both the window and the command line write this record" | One reader met "the window" and "the command line" introduced as two named routes with neither defined. | Gloss "command line" at first use as typing commands in the Terminal app rather than clicking. | 4 |
| Why you would do this, "the loser of a race between two writers" | One reader read "the loser of a race" as a competition between people in the lab rather than a figure of speech about which write happens second. | Replace the figure of speech with plain wording about which write happens second. | 3 |
| Reading the window's read-only state, "with a dialog titled with the project's own name" | One reader had to read this sentence twice because of its awkward phrasing. | Simplify to "a dialog whose title is the project name." | 2 |
| Reading the window's read-only state, "Recovery is safe when the other session is genuinely gone" | One reader had no way to test whether the other session is really gone, which is the actual decision this sentence asks the reader to make. | Give one practical check, for example asking the person named in the banner. | 2 |
| Reading the window's read-only state, the read-only section placed fourth | One reader, whose own situation is exactly the read-only banner, had to read three sections of background before reaching the one section describing what they actually saw on screen. | Move this section first for window readers, or add a one-line pointer at the top of the chapter. | 4 |
| Reading the window's read-only state, "Try to run something that writes and a sheet appears" | One reader could not tell which operations count as writing and which are safe, despite being told every viewport opens. | Name two or three examples of writing operations and one safe example. | 4 |
| Locking a project, "It is unknown when LGE cannot tell, which is the state for every lock written on a different machine." | One reader, whose lab shares project storage on a server, realized every lock they would ever see reads as unknown, a conclusion the chapter never draws for that situation. | Add one sentence saying that on shared lab storage nearly every lock will read as unknown. | 4 |
| Locking a project from the command line, "an opaque identifier for the Mac itself" followed by the earlier command | One reader found the whole passage read as author-side testing evidence rather than reader instruction, contradicting the earlier advice to take a lock before scripted maintenance. | State the conclusion first, then the supporting reason, and cut evidence that reads as a private test note. | 1 |
| Locking a project, "so a lock taken from the command line marks intent, not possession" | One reader had to parse this abstract pair twice and only understood it from the sentences that followed. | Say plainly that the lock is a message to other sessions, not a live hold. | 3 |
| Unlocking a project, "Error: Refusing to remove lock ... pid 97287; pass --force to override." | One reader could not tell whether the semicolon-joined `pid 97287` in the quoted error was something to type back. | Say the numbers in a quoted error message are read-only information, not something to retype. | 2 |
| Migrating older bundles, "`migration-available` is counted in no line at all" | One reader read this as a bug described as a quirk and could not tell whether to report it or ignore it. | Say plainly whether this is expected to change. | 1 |
| Migrating older bundles, "Those bundle types store their metadata under a different filename" | One reader found "metadata" never glossed anywhere in the chapter. | Gloss metadata once as information about the data rather than the data itself. | 1 |
| Migrating older bundles, "A reference manifest at format version 1.0" | One reader could not tell whether "format version," "schema version," and bare "schema" name one idea or three. | Use one term for this version number throughout the chapter. | 1 |
| Migrating older bundles, "`tsv` is accepted but currently prints the same text report" | One reader learned a documented option does not work and could not tell whether to avoid it forever or only for now. | Say whether this is a known defect and whether it is being fixed. | 1 |
| Migrating older bundles, "an interruption leaves the bundle with its old manifest rather than half of a new one" | One reader stopped at "half of a new one," picturing a broken file, before reaching the reassurance buried at the end of the clause. | Split into a short sentence stating plainly that an interrupted run is safe. | 3 |
| Migrating older bundles, "`unsupported` means no transformer exists for that schema" | One reader met "transformer" used four times with no gloss anywhere. | Gloss transformer once as the code that rewrites one layout into another. | 4 |
| Migrating older bundles, "which is why 1 current plus 2 unsupported plus 0 migrated does not reach the 4 inspected" | One reader had to work through arithmetic embedded in the middle of a sentence. | Show the sum as a short standalone line rather than inside a sentence. | 4 |
| Provenance expectations, "or a bundle copied without its provenance accounts for most of what looks at first like a missing feature" | One reader did not know how to avoid copying a bundle without its provenance in the first place. | Say how to copy a bundle safely, or link to where that is covered. | 2 |
| Whole chapter, "shots: shared-projects-read-only-banner" | One reader noted the chapter has only one screenshot, of the banner, while the three-button dialog the reader actually faces has no picture at all. | Add a screenshot of the "may already be open" dialog. | 2 |
| See also, "for how a read-only project refuses a workflow run" | One reader met "workflow" used throughout the chapter with no gloss and never learned what one is. | Gloss workflow at first use, in the read-only section. | 1 |
| See also, the pointer to the Workflow Builder chapter for the read-only fix | One reader wanted the one-line fix for the commonest window case stated directly in this chapter rather than sent elsewhere. | Add the one-line fix for a read-only project directly in this chapter. | 4 |
| Whole chapter, no section on what to do right now | One reader, locked out of a shared project and unable to use the terminal, found no simple non-terminal answer, which is to ask the labmate to close it and reopen. | Add one short paragraph for the reader who is locked out and cannot use the terminal. | 3 |

Eighty merged rows total, from 188 source rows across the four reports.

## Consensus

Thirty-two merged rows were hit by three or more readers.

- Neither the first command name nor the first command-line paragraph tells a reader what app to open or where typing happens.
- The exit code is never glossed at first use, so readers cannot tell success from failure in the app's own output.
- Dot-prefixed hidden names appear in three sections with no statement anywhere that Finder hides them.
- Project catalog is never glossed at its first use.
- Live local process is used to explain when Recover and Open appears, two sections before the four lock states that would define it.
- Host and process number appear in the read-only banner with no gloss and no statement of what either tells the reader to do.
- Checksum is never glossed in the recovery-file description.
- Project store is never glossed where it first decides the no-banner, read-only case.
- The backslash that protects a space in example paths is never explained and reads as a typo.
- The twelve-fields sentence runs the list in prose when the JSON block right after it already shows the same fields.
- Opaque identifier is never glossed as a meaningless, stable code for the machine.
- The verification asides read like the author's own testing notes rather than instruction for the reader.
- The unbroken-hold script advice is never marked as being for script authors only.
- The two allowed cases for who may remove a lock are stated as one long conditional instead of two sentences.
- The migration counts block is never preceded by a warning that the figures will not add up.
- The provenance record's list of what gets written runs ten or eleven items into one sentence.
- The future-rule paragraph never states up front that it describes a planned rule rather than the current app.
- The chapter never states in one place that the window has no equivalent for locking or migrating, and never says what a window-only reader should do instead.
- The advisory gloss for the lock never spells out that a Finder copy or a sync client is not stopped by it.
- The cached-summary aside piles three unfamiliar ideas, cached summary, drawing, and reference, into one phrase with no picture of what breaks if it is missing.
- The demo-project build instructions live only behind an external GitHub link and cannot be followed from the chapter text alone.
- The read-only banner lists several fields with no indication of which one names the person to contact.
- JSON is used and shown in a code block with no gloss anywhere in the chapter.
- The hostname anecdote about a Mac reporting two different names reads as private testing evidence rather than guidance for the reader.
- The accepted values of `--mode` are never listed where the flag is first introduced.
- The command-line-lock warning is told as a story with its rule at the end instead of the start.
- The doubled period in the quoted unlock error looks like a typo in the manual rather than the app's real output.
- A folder's extension is used to detect a bundle with no gloss, despite Finder hiding extensions by default.
- The `unreadable` bundle status reads as damage before the chapter clarifies, a paragraph or more later, that it is often expected.
- The chromosome-cache paragraph buries its own payoff, what the reader gains by filling it, one clause deep inside a nested definition.
- The `--format` option's machine-readable values are never marked as being for script authors only.
- Suspect the project before the app never names a first concrete action for the reader to take.
