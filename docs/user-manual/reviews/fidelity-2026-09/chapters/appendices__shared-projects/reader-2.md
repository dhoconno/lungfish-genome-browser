# Reader 2 report, appendices/shared-projects

Persona. Senior undergraduate, two years of wet-lab work, comfortable with library prep and PCR, has never analyzed data and has never opened a terminal. My lab shares one project folder on a server, and I just opened it and got a window that says Read Only.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| What it is, "is a `.lungfish` folder holding one analysis" | I do not know what "one analysis" means as a unit. In the lab an analysis is a whole experiment. Does the folder hold one sample, one run, one figure? | Say what a project holds in concrete terms, for example all the samples and results from one sequencing run. |
| What it is, "The record lives at `.lungfish/project.lock`" | A folder name starting with a dot is invisible in Finder on my Mac. I looked and could not find it, and the chapter never says it is hidden. | Add one sentence saying the `.lungfish` folder is hidden in Finder. |
| What it is, "The lock is advisory, meaning it works only because every LGE process agrees to check it." | "Process" is used here for the first time with no gloss, and I had to read this twice. I read it as "program" and was then confused later when one program has many processes. | Gloss "process" at first use as one running copy of a program. |
| What it is, "It does not ask the operating system to refuse anyone access" | This told me what does not happen but not what does. I could not tell whether my lab mate copying the folder in Finder while I work is safe or not. | Name the real risk, for example Finder copies and Dropbox sync are not stopped by the lock. |
| What it is, "Every viewport opens" | "Viewport" is not explained here and I do not know the word from the app yet. This is an appendix, so I may arrive here before reading the chapter that defines it. | Gloss viewport at first use, or link it. |
| What it is, "a small file at its root naming what the bundle holds" | "At its root" is filesystem language. I pictured a tree. | Say "at the top level of the bundle folder". |
| What it is, "Manifests carry a schema version, a number saying which layout the file was written to." | I do not know how to judge this number. Is my project's number 1 or 7? Nothing in the chapter tells me where to look or what number is current. | Say what the current schema version is and where a reader can see their own. |
| What it is, "Take a lock before any scripted maintenance" | "Scripted maintenance" assumes I write scripts. I do not, and I do not know whether this advice applies to me at all. | Say plainly that a reader who only uses the window never needs to take a lock. |
| Why you would do this, "finds a result folder half-written or a project catalog inconsistent" | "Project catalog" appears nowhere else in the chapter and I do not know what it is. | Gloss or drop "project catalog". |
| Why you would do this, "a cached summary the window uses to draw a reference quickly" | Three unexplained things at once, cached summary, draw, reference. I read it twice and still could not picture what breaks if it is missing. | Say what the reader would actually see, for example the chromosome list takes longer to appear. |
| Before you start, "Every command below is a command-line command" | This is the first mention that the whole second half needs a terminal. I have never opened one. The chapter never says how to open Terminal or what "exit 0" is going to mean. | Add one line early saying which app to open and that a reader can skip to the window sections. |
| Before you start, "Build it by following the instructions in the manual's fixtures on GitHub" | I could not perform this step. It sends me to a GitHub URL and says it takes two minutes, but I do not know what a fixture is or whether I need a GitHub account. | Say what the demo project contains and that no account is needed. |
| Before you start, "`--force` in particular destroys a record" | `--force` is used before it is introduced. I did not know it was something I type. | Introduce flags as things typed after the command, at first use. |
| Before you start, "Duplicate the demo project in Finder and point the commands at the duplicate" | "Point the commands at" is not a step I can perform. I do not know how to turn a duplicated folder into text in a command. | Show the duplicate's path in one example command. |
| Reading the window's read-only state, "with a dialog titled with the project's own name" | I had to read this twice because of "titled with the project's own name". It parses oddly. | Simplify to "a dialog whose title is the project name". |
| Reading the window's read-only state, "**Recover and Open** appears only when recovery is possible, meaning when the lock is not from a live local process" | I cannot judge whether my case qualifies. I do not know if my lab mate's session on the server counts as local. | State the common case plainly, for example a lock from another Mac always allows recovery. |
| Reading the window's read-only state, "the owner and host and process number" | "Host" and "process number" are not glossed. Host to me is a host organism. | Gloss host as the computer name at first use. |
| Reading the window's read-only state, "Recovery is safe when the other session is genuinely gone" | I have no way to check whether it is gone. This is the decision the chapter actually asks me to make and it gives me no test. | Give one practical check, for example ask the person named in the banner. |
| Reading the window's read-only state, "writes a `recovery.json` beside it holding the reason, the time, the original path, a checksum of the archived bytes" | "Checksum" is not explained, and the whole sentence is a list of eight things I cannot use. As a student I would never open this file. | Gloss checksum, or move this paragraph to a note for administrators. |
| Reading the window's read-only state, "naming the lock's tool, its status, its mode, its owner and host, the process number, and the creation time" | Seven fields and I do not know which one tells me who to go ask. That is the only thing I want from the banner. | Say which field names the person. |
| Reading the window's read-only state, "A folder built only by `lungfish-cli`, never opened by the app, has no project store" | "Project store" appears once, unglossed, and this is the paragraph that explains my most likely confusion. | Gloss project store in the same sentence. |
| Locking a project from the command line, "It is **stale** when that process has exited, or when the process number has been reused" | Process numbers being reused was new to me and I read the sentence three times. It also made me distrust the whole mechanism. | Split into two sentences and say reuse is rare. |
| Locking a project from the command line, "It is **corrupted** when the lock file exists but cannot be read as valid JSON." | "JSON" is never explained and appears again later in a code block I cannot read. | Gloss JSON once as a plain-text data format. |
| Locking a project from the command line, "```sh\nlungfish-cli project lock ~/Desktop/..." | I could not perform this. The backslash before "Manual" is unexplained, and I would have deleted it as a typo. The `~` is also unexplained. | Add one line saying the backslash escapes the space in the folder name. |
| Locking a project from the command line, "On success it prints three lines and exits 0." | "Exits 0" is used here and then eleven more times. I never learned what it means and assumed at first that something had failed. | Gloss exit 0 as success and exit 1 as refusal, once, at first use. |
| Locking a project from the command line, "The record it writes holds twelve fields" | Twelve fields listed in one sentence, then repeated in the JSON block. I read the sentence twice and got nothing from it. | Cut the prose list and let the example block speak. |
| Locking a project from the command line, "`machineIdentifier` is an opaque identifier for the Mac itself" | "Opaque identifier" is jargon. I could not tell whether it identifies me personally, which matters for a shared lab machine. | Say it is a random code for the computer that contains no personal information. |
| Locking a project from the command line, "the same Mac reported `raven.local` for one command and a long provider-assigned name for the next" | This anecdote made me doubt the app. It also assumes I know what a hostname is and why a provider assigns one. | Cut the anecdote or reduce it to one clause. |
| Locking a project from the command line, "so `exclusive` and `maintenance` block writes in exactly the same way" | `maintenance` appears here for the first and only time and I did not know it was an allowed value. | List the allowed values of `--mode` where the flag is introduced. |
| Locking a project from the command line, "Now the part that surprises people." | I had to read the next four sentences twice. The chapter first teaches me to take a lock, then tells me the lock does not really hold. I was left unsure whether I should use the command at all. | Lead with the conclusion, that a CLI lock marks intent only. |
| Locking a project from the command line, "the process id in the record belongs to that one short-lived command and is dead before your next command runs" | "Process id" here versus "process number" earlier. I thought they were two different things. | Use one term throughout. |
| Locking a project from the command line, "Verified on a copy of the demo project, both calls exited 0" | This reads like a lab notebook entry, not instruction. I did not know whether I was supposed to reproduce it. | Move verification notes out of the reader's path. |
| Locking a project from the command line, "a script that needs an unbroken hold must either keep an LGE process alive for the duration" | I cannot act on this. I do not write scripts and cannot keep a process alive on purpose. | Mark this paragraph as being for script authors. |
| Unlocking a project, "Either the lock belongs to the running process itself" | On a command line that exits immediately, "the running process itself" cannot ever be me. I read this three times trying to make it apply. | Say plainly that this case covers the app, not the command line. |
| Unlocking a project, "Error: Refusing to remove lock ... pid 97287; pass --force to override." | The error output uses a semicolon and `pid`, neither of which the chapter has explained, and I could not tell if `pid 97287` was something I had to type back. | Say the numbers in the message are read-only information. |
| Unlocking a project, "It does not stop the process that wrote it, which will carry on writing into the project believing it still holds the lock" | This is the most alarming sentence in the chapter and it arrives after the instruction to use `--force`. I stopped reading and went back. | Put the warning before the flag is offered. |
| Migrating older bundles, "reports the schema each one is written to" | Same schema-version problem as earlier. I still cannot judge whether any number in the report is good or bad. | State the current schema version once. |
| Migrating older bundles, "```\nProject migration report ... Bundles inspected: 4\nCurrent: 1\nUnsupported: 2" | I tried to add these up and they did not reach 4, which made me think my run was broken. The chapter explains this two paragraphs later. | Put the caution about the counts immediately after the block. |
| Migrating older bundles, "any folder whose extension begins with `lungfish` and which contains a `manifest.json`" | "Extension" is filesystem vocabulary I half know, and Finder hides extensions by default, so I could not check this. | Give one example filename with its extension marked. |
| Migrating older bundles, "`unreadable` means the manifest could not be decoded at all" | I read `unreadable` as damage and got worried about my lab's data. The correction comes in the next paragraph, but the word had already scared me. | Say in the status list that unreadable is expected for some bundle types. |
| Migrating older bundles, "a cache in a reference bundle's manifest holding the chromosome list the window draws from" | Nested definition inside a definition. I read it twice and still cannot say what I would gain by filling it. | Say what improves, for example the reference opens faster. |
| Migrating older bundles, "a `.manifest.json.backup` holding the manifest exactly as it was" | Both files start with a dot and so are invisible in Finder. I would look for them and conclude the migration did not run. | Note that these files are hidden. |
| Migrating older bundles, "The `--format` option accepts `text`, `json`, and `tsv`." | This paragraph is for people writing scripts and I could not use any of it. "Machine-readable" and "tab-separated values" are both unexplained. | Mark the paragraph as being for script authors. |
| Provenance expectations, "Locks are coordination metadata rather than scientific output" | "Coordination metadata" stopped me cold. I know metadata from sequencing sample sheets, but not this use. | Say locks are bookkeeping, not results. |
| Provenance expectations, "It records the tool name and version, the reproducible command, the input files with their checksums and sizes, the output files including the backup, the exit status, the wall time, the host and user, and the parameters" | One sentence with eleven items. I lost the thread halfway and had to restart it. | Break into a short list. |
| Provenance expectations, "The wider rule is a requirement on migrations LGE has not written yet rather than a description of what runs today." | I had to read this three times. It is a sentence about a rule about software that does not exist, and I could not tell whether the paragraph after it described the app I have. | Open the paragraph with a plain flag such as "This does not apply to today's app". |
| Provenance expectations, "runtime identity, exit status, stderr where useful, and wall time" | "Stderr" and "wall time" are unexplained and I have no idea what runtime identity means. | Gloss or cut for this audience. |
| What good looks like, "3. `project lock` on a free project exits 0 and prints three lines" | Two of the four checks need a terminal, so half the checklist is closed to me. | Split the checklist into window checks and command-line checks. |
| What good looks like, "or a bundle copied without its provenance accounts for most of what looks at first like a missing feature" | Long sentence, and "copied without its provenance" is a thing I do not know how to avoid doing. | Say how to copy a bundle safely, or link to where that is covered. |
| Whole chapter, "shots: shared-projects-read-only-banner" | The single screenshot is of the banner, and it is the only picture. The dialog with three buttons is the moment I actually face, and there is no picture of it. | Add a screenshot of the "may already be open" dialog. |

One thing I learned. A yellow banner means someone else has the project, and no banner with the same Read Only title means the folder was never opened by the app.

One thing I still could not do. Anything in the second half. I have never opened a terminal and the chapter never says where to start one or what "exits 0" means.

The sentence I liked most. "The lock turns a silent corruption into a visible refusal, which is the whole point of it."
