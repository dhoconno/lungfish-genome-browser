# Reader 4 report, appendices/shared-projects

Persona. Undergraduate who used Geneious for one semester. Comfortable with a
sequence viewer, expects a dialog for everything, has never opened a terminal.
My lab shares one project folder on a server, and I have just seen the window
open read-only.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| What it is, "a `.lungfish` folder holding one analysis" | I have only ever seen the project as one icon I double-click. The chapter tells me it is really a folder, but never says how I would look inside one. In Geneious I never saw a folder at all. | Add a sentence saying you see the inside with Finder's Show Package Contents, or say that you never need to. |
| What it is, "The record lives at `.lungfish/project.lock`" | A path starting with a dot. In Finder I cannot see anything starting with a dot, so I went looking for it and found nothing and thought the app was broken. | Say in one clause that names beginning with a dot are hidden in Finder. |
| What it is, "The lock is advisory, meaning it works only because" | Read twice. The gloss is there but the consequence is not spelled out for my case. My lab also uses Dropbox and rsync, and I could not tell whether those count as "a program that does not read the record". | Say plainly that a sync client copying the folder is not stopped by the lock. |
| What it is, "Both the window and the command line write this record" | This is the first appearance of "the window" and "the command line" as two named routes. I do not know what the command line is, and the chapter never says. | Gloss "command line" at first use as typing commands in the Terminal app rather than clicking. |
| What it is, "`project migrate` is the command that reports on them" | A command name is dropped in with no explanation of how one runs a command. This is the first place I felt shut out. | Name the tool and where you type it, once, before the first command name appears. |
| What it is, "Take a lock before any scripted maintenance" | "Scripted maintenance" is not explained. I do not write scripts, so I could not tell whether this whole paragraph is advice for me or for someone else. | Say who this advice is for, for example "if you run batch jobs". |
| Why you would do this, "a project catalog inconsistent" | "Project catalog" appears once and is never defined. I do not know if it is a file, a window, or a list. | Gloss it, or drop the phrase and say the project's index of its own contents. |
| Before you start, "Every command below is a command-line command, because the window has no equivalent for taking, releasing, or migrating a lock on demand." | This is the single most important sentence for me and it is buried mid-section as an aside. I got to the bottom of the chapter still hoping for a dialog. It also does not tell me what to do instead. My whole lab has no terminal user. | Move this to the very top of the chapter as its own short paragraph, and add what a window-only reader should do, for example quit LGE on the other Mac and reopen. |
| Before you start, "Build it by following the instructions in the manual's fixtures on GitHub at https://..." | The steps are not in the chapter, they are behind a link, and I do not know what "the manual's fixtures" are. I could not do this step from the text. | Give the two or three clicks inline, or say the demo project is optional for reading. |
| Before you start, "`--force` in particular destroys a record" | "Flag" and the double-dash form are never explained. I could not tell whether `--force` is something I type, a checkbox, or a setting. | Say once that words beginning with two dashes are options typed after the command. |
| Before you start, "Duplicate the demo project in Finder and point the commands at the duplicate" | "Point the commands at" assumed I know how a command is given a folder. I do not. | Say the folder path is typed after the command, and show the one example. |
| Reading the window's read-only state, "a dialog titled with the project's own name and the words \"may already be open\"" | Good, this is my part of the chapter, but this section sits fourth. I met three sections of background before the one thing I actually saw on screen. | Put this section first for window readers, or add a one-line pointer at the top. |
| Reading the window's read-only state, "**Recover and Open** appears only when recovery is possible, meaning when the lock is not from a live local process" | "Live local process" is used before the four lock states are defined two sections later. I had to read forward and come back. | Move the four-state list before this section, or gloss "live local process" here. |
| Reading the window's read-only state, "the owner and host and process number" | I do not know what a host or a process number is, or how I would use either. The details block is offered as evidence but I cannot read it. | Gloss host as the computer's name and process number as the app's id on that computer. |
| Reading the window's read-only state, "It moves it into a `lock-recovery` folder under the project's `.lungfish` directory" | I cannot reach a hidden folder from Finder, and no window route is offered, so this evidence is not available to me. The text does not say that. | Say this is reachable only outside the window, or give the Finder route. |
| Reading the window's read-only state, "writes a `recovery.json` beside it holding the reason, the time, the original path, a checksum of the archived bytes" | "Checksum" is not glossed anywhere in the chapter. | Gloss it once as a short fingerprint that changes if the file changes. |
| Reading the window's read-only state, "titled \"Project opened read-only\", with a detail line naming the lock's tool, its status, its mode, its owner" | Five fields listed, none explained, and this is the banner I am actually looking at right now. I could read the banner but could not judge any of it. | Say what a reader should do with each field, or at minimum which one tells you who to go ask. |
| Reading the window's read-only state, "Try to run something that writes and a sheet appears" | I could not tell which operations write. Does opening a viewport write? Does exporting? The chapter says every viewport opens, but I still did not know where the line is. | Name two or three examples of writing operations and one of a safe one. |
| Reading the window's read-only state, "A folder built only by `lungfish-cli`, never opened by the app, has no project store" | "Project store" is used here and never defined. This is also the one case a window-only reader is most likely to hit, because someone else in the lab made the folder. | Gloss "project store" and say what a window reader does about it. |
| Locking a project, "It is **unknown** when LGE cannot tell, which is the state for every lock written on a different machine." | Our shared folder is on a server and everyone opens it from their own Mac, so every lock I will ever see is unknown. The chapter never draws that conclusion for my situation. | Add one sentence saying that on shared lab storage nearly every lock reads as unknown. |
| Locking a project, "```sh" code block | I do not know where to type this. The chapter shows commands from the second page on and never once says what to open. | State the app to open and that the text is typed at a prompt, once, before the first block. |
| Locking a project, "`~/Desktop/lge-docs/LGE\ Manual\ Demo.lungfish`" | The tilde and the backslash before the space are both unexplained, and the backslash looks like a typo. | Gloss the tilde as your home folder and the backslash as what protects a space. |
| Locking a project, "On success it prints three lines and exits 0." | "Exits 0" appears with no gloss and is then used a dozen more times. I guessed it means fine, but I guessed. | Gloss it once as the code a command leaves behind, with 0 meaning success. |
| Locking a project, "The record it writes holds twelve fields" | Twelve items in one sentence with no way to see them until the JSON block below. I read this twice and gained nothing from the count. | Cut the count and let the JSON block speak, or shorten to the three fields a reader uses. |
| Locking a project, "Passing `--format json` prints that record instead" | "JSON" is not glossed, and the block below is my first sight of it. | Gloss JSON once as a plain-text format for structured data. |
| Locking a project, "`machineIdentifier` is an opaque identifier" | "Opaque" in this sense is not the everyday word. I read it as "unclear" and was confused about why an unclear identifier was better. | Say it is a meaningless string that never changes. |
| Locking a project, "and during the session that produced these examples the same Mac reported `raven.local` for one command and a long provider-assigned name for the next" | Long sentence about the author's own testing that broke the flow. I could not tell if this was something I should worry about. | Cut to the fact that hostnames change, or move it to a note. |
| Locking a project, "so `exclusive` and `maintenance` block writes in exactly the same way" | `maintenance` appears here for the first and only time, with no earlier statement that it is a legal value of `--mode`. | List the accepted values of `--mode` where the option is introduced. |
| Locking a project, "The command exits 1 and prints the owner." | I now have exit 0 and exit 1 and still no gloss for either. | See above, gloss the exit code once and cross-reference. |
| Locking a project, "Now the part that surprises people." | This paragraph is the most important warning in the section and it is written as a story with the conclusion at the end. I had to read the whole paragraph twice to find the rule. | Lead with the rule, that a lock taken from the command line does not last. |
| Locking a project, "the process id in the record belongs to that one short-lived command and is dead before your next command runs" | "Process id" is used here where "process number" was used earlier. Two names for one thing threw me. | Use one term throughout. |
| Locking a project, "Verified on a copy of the demo project, both calls exited 0" | Sentence starts with a verification note rather than a fact, which reads oddly and made me stop. Three sentences in the chapter start this way. | Move the verification into a note or drop the leading participle. |
| Locking a project, "a script that needs an unbroken hold must either keep an LGE process alive for the duration" | I do not know what "keep a process alive" means as an action I could take. No window equivalent is offered. | Say concretely, for example leave the project open in LGE on one Mac. |
| Unlocking a project, "Either the lock belongs to the running process itself, or it belongs to the current user on this machine and its owning process has exited" | Two conditions in one long sentence with three unglossed terms. Read three times. | Split into two short sentences or a two-item list. |
| Unlocking a project, "```\nError: Project lock file is corrupted ... The data couldn't be read because it isn't in the correct format.." | Two full stops in a row in the quoted error. I assumed a typo and then could not tell whether the rest of the quote was accurate. | Note that the doubled full stop is what the app really prints. |
| Unlocking a project, "It does not stop the process that wrote it, which will carry on writing into the project believing it still holds the lock" | This is the scariest sentence in the chapter for someone sharing a folder, and it arrives after the instructions for using `--force`. | Put the warning before the flag, not after. |
| Unlocking a project, "which makes it safe to put in a cleanup step" | "Cleanup step" assumes I have a script with steps. I do not. | Say who this is for, or drop it. |
| Migrating older bundles, "`project migrate` walks a project, reads the manifest of every bundle it finds" | This whole half has no window route at all and the chapter never says so plainly here. Section three said the window has no equivalent, but by page four I had forgotten and looked for a menu item. | Repeat in one clause that migration is command-line only. |
| Migrating older bundles, "Bundles inspected: 4 / Current: 1 / Unsupported: 2 / Migrated: 0" | I could not judge these numbers at all. The chapter later says the counts are wrong, which left me not knowing whether anything on my project is broken. | Say up front, above the block, that the counts are unreliable and the lines are the answer. |
| Migrating older bundles, "`migration-available` under `--dry-run` means a fillable gap was found" | "Fillable gap" is vague and comes before "browser summary" is defined two paragraphs down. | Name the gap here. |
| Migrating older bundles, "`unsupported` means no transformer exists for that schema" | "Transformer" is used four times and never glossed. | Gloss it once as the code that rewrites one layout into another. |
| Migrating older bundles, "The two `unreadable` lines in the demo project are not damage." | Relief, but I could not tell whether unreadable in MY project would be damage. The rule is given as a fact about the demo only. | State the general rule, that unreadable on alignments and trees is expected. |
| Migrating older bundles, "which is why 1 current plus 2 unsupported plus 0 migrated does not reach the 4 inspected" | Arithmetic mid-sentence that I had to work through on paper. | Show the sum as a short line rather than inside a sentence. |
| Migrating older bundles, "a cache in a reference bundle's manifest holding the chromosome list the window draws from" | "Cache" is not glossed. Also this is the only place the chapter tells me what I would actually gain, and it is one clause deep in a sentence. | Lead the section with what the user gets, a reference that draws faster. |
| Migrating older bundles, "under its own `.lungfish/migrations/` folder, the run left two timestamped files" | Hidden folder again, no window route, and no statement that I cannot see it. | Say the backups are not visible in Finder. |
| Migrating older bundles, "so `json` is the format to script against" | "Script against" assumes scripting. I do not script and could not tell whether I need this paragraph. | Mark the paragraph as being for scripted use. |
| Provenance expectations, "taking and releasing one writes no [provenance sidecar]" | "Provenance sidecar" is linked but not glossed in the chapter, and this section is entirely about it. | Gloss it in one clause at first use here. |
| Provenance expectations, "It records the tool name and version, the reproducible command, the input files with their checksums and sizes, the output files including the backup, the exit status, the wall time, the host and user, and the parameters naming the transformer and the source and target schemas." | Eleven items in one sentence. I lost the thread halfway and could not have said what was recorded. | Break into a short list. |
| Provenance expectations, "The wider rule is a requirement on migrations LGE has not written yet rather than a description of what runs today." | Read three times. I could not work out whether the paragraph that follows describes the app or a wish. | Say plainly that the next paragraph describes a rule for future work. |
| Provenance expectations, "must preserve the existing sidecars, write new migration provenance covering tool name and version, options, inputs, outputs, checksums, file sizes, runtime identity, exit status, stderr where useful, and wall time" | Another eleven-item sentence, plus "stderr" which is never glossed and which I have never seen. | Break up and gloss or cut "stderr". |
| What good looks like, "3. `project lock` on a free project exits 0 and prints three lines" | Two of the four checks are command-line only, so the checklist I was hoping to use is half unavailable to me and does not say so. | Mark the window checks and the command-line checks apart. |
| What good looks like, "A `(Read Only)` title with a yellow banner means a lock. Without a banner it means no project store." | This is the one check I can perform, and it is the clearest thing in the chapter, but "project store" is still undefined here. | Gloss it, or say "made outside the app". |
| What good looks like, "suspect the project before the app" | I do not know what suspecting a project would lead me to do next. No action follows. | Give the first thing to check. |
| See also, "[The Workflow Builder](...) for how a read-only project refuses a workflow run." | The chapter told me a sheet appears when I try to write, but sends the detail elsewhere. I wanted the fix here. | Add the one-line fix in this chapter, since it is the commonest window case. |

Three closing lines.

The one thing I learned. A yellow banner under a `(Read Only)` title means another
person holds the project, and no banner means the folder was never opened by the app.

The one thing I still could not do. Release a lock, or tell my labmate how, because
every route in this chapter is a command typed somewhere the chapter never names,
and it does not tell a window user what to do instead.

The sentence I liked most. "The lock turns a silent corruption into a visible
refusal, which is the whole point of it."
