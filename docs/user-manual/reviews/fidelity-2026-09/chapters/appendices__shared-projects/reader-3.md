# Reader 3 report, Shared Projects and Bundle Migration

Reader. Pre-med student, English is my second language, strong on biology
words from textbooks, slow on idioms and long sentences, never opened a
terminal. My lab shares one project folder and my window just opened
read-only.

| Location | Issue | Suggested fix |
| --- | --- | --- |
| What it is, "is a `.lungfish` folder holding one analysis" | I do not know what a folder ending in `.lungfish` looks like in Finder. It sounds like a file name, not a folder. The later section says a bundle is a folder Finder shows as one icon, but that comes six paragraphs later, so at first reading I pictured a document. | Say at first use that the folder appears in Finder as a single icon. |
| What it is, "and two copies of LGE can open it together" | "Two copies of LGE" confused me. I thought it meant two copies of the program installed on my Mac, not two people running it. | Write "two people running LGE" instead of "two copies". |
| What it is, "The lock is advisory, meaning it works only because" | "Advisory" is a word I know from "academic advisor", not from software. The gloss after it helped, but I still had to read the sentence twice to attach the gloss to the right idea. | Put the plain meaning first, then the word "advisory" as the label. |
| What it is, "The record lives at `.lungfish/project.lock` inside the project folder." | I cannot find this. A name starting with a dot is hidden in Finder and nobody told me. I opened the project folder and saw nothing. | Add one sentence that names starting with a dot are hidden in Finder. |
| What it is, "So what should you do with this? Take a lock before" | This paragraph tells me to take and release a lock, but at this point I have not been told that taking a lock means typing a command. I thought there was a menu item. | Say here that taking a lock is a command-line action only. |
| Why you would do this, "the loser of a race between two writers" | "The loser of a race" is a figure of speech. I read it as a competition between people in my lab. It took a second reading to understand it means whichever save happened second. | Replace with plain wording about which write happens second. |
| Why you would do this, "a project catalog inconsistent" | I do not know what the project catalog is. It has not been named anywhere before this. | Gloss the project catalog at first use, or drop the term. |
| Why you would do this, "a cached summary the window uses to draw a reference quickly" | Three unfamiliar ideas in one phrase, cached summary, the window drawing, and "a reference". I could not picture what is missing or what I would lose without it. | Say what the reader actually sees when this cache is missing. |
| Before you start, "Every command below is a command-line command" | I have never opened a terminal. The chapter never says how to open one, where to type, or what "exits 0" will look like on my screen. I could not perform any step in the second half. | Add one line pointing to whichever chapter explains opening Terminal and running `lungfish-cli`. |
| Before you start, "Duplicate the demo project in Finder" | I do not know how to duplicate a bundle safely, and I do not know whether duplicating copies the hidden lock too. | Name the Finder step and say whether the copy carries the lock. |
| Reading the window's read-only state, "when the lock is not from a live local process" | "Live local process" is not explained yet. The four lock states are explained in the next section, after I already needed them here. | Move the four-state list before this section, or gloss "live local process" here. |
| Reading the window's read-only state, "the owner and host and process number" | I do not know what a host is or what a process number means, and I cannot judge them. Seeing "pid 97287" tells me nothing about whether that person is still working. | Say in one sentence what each of these tells the reader to do next. |
| Reading the window's read-only state, "It moves it into a `lock-recovery` folder" | I cannot follow this step, because I still do not know how to see a hidden `.lungfish` directory in Finder. | Give the Finder keystroke for showing hidden items once, early. |
| Reading the window's read-only state, "a checksum of the archived bytes" | "Checksum" is not explained. I know it is from computing, not biology, and I cannot tell if I am supposed to check it myself. | Gloss checksum at first use and say the reader never reads it by hand. |
| Reading the window's read-only state, "has no project store, and the app opens it read-only" | "Project store" appears here for the first time with no explanation, and it is the deciding clue for the whole no-banner case. | Gloss project store where it first appears. |
| Locking a project, "It is **stale** when that process has exited, or when the process number has been reused" | Long sentence with two conditions and a word I do not know. I read it three times. I also cannot understand how a number gets reused by a different program. | Split into two sentences and drop the number-reuse detail into a note. |
| Locking a project, "lungfish-cli project lock ~/Desktop/lge-docs/LGE\\ Manual\\ Demo.lungfish" | The backslash before the space stopped me completely. I thought it was a typo in the manual. | Add one sentence saying the backslash marks the space in the folder name. |
| Locking a project, "On success it prints three lines and exits 0." | I do not know what "exits 0" means or where I would see it. Nothing on the screen shows a zero in the printed output. | Say once that exit 0 means success and is not printed. |
| Locking a project, "The record it writes holds twelve fields, which are the schema version, the tool name" | This is one sentence with twelve items in it. I lost track by the sixth. The JSON block right after already shows them. | Cut the sentence and let the JSON block show the fields. |
| Locking a project, "`machineIdentifier` is an opaque identifier for the Mac itself" | "Opaque" here does not mean not transparent in the physical sense, which is the only meaning I knew. | Say the identifier is meaningless to read and only used for comparison. |
| Locking a project, "during the session that produced these examples the same Mac reported `raven.local`" | I could not tell whether this is advice for me or a note about how the manual was tested. It reads like the author talking about their own work. | Move this evidence into a note, or state it as a general fact about hostnames. |
| Locking a project, "Now the part that surprises people." | Idiom, and it tells me a surprise is coming without telling me what. I had to read the whole paragraph before I understood the point. | State the fact first, then say it surprises people. |
| Locking a project, "Verified on a copy of the demo project, both calls exited 0" | Again I cannot tell if this is an instruction to me or a record of the author's test. It appears three times in the chapter in that same shape. | Mark these verification notes consistently as testing notes. |
| Locking a project, "so a lock taken from the command line marks intent, not possession" | "Marks intent, not possession" is an abstract pair I had to parse twice. I understand the idea only from the sentences after it. | Say plainly that the lock is a message to other sessions, not a live hold. |
| Locking a project, "must either keep an LGE process alive for the duration" | I do not know how to keep a process alive, and the chapter never says how. This is the one place it tells me to do something with no step at all. | Either give the step or say plainly this is out of scope. |
| Unlocking a project, "the lock belongs to the running process itself" | Which running process? I have not been told that unlock knows about a process of its own. I read the sentence twice and still guessed. | Name the case concretely, such as a lock this same command took. |
| Unlocking a project, "The data couldn't be read because it isn't in the correct format.." | Two periods at the end. I thought I had misread the sample, and I stopped to check. | Fix the doubled period, or say the message quotes a system error verbatim. |
| Unlocking a project, "which makes it safe to put in a cleanup step" | "Cleanup step" assumes I write scripts. I do not. | Say who this sentence is for, or move it to a note for script writers. |
| Migrating older bundles, "reads the manifest of every bundle it finds, and reports the schema" | "Schema" was glossed earlier as schema version, but here it is used bare and I was not sure they are the same thing. | Use "schema version" consistently. |
| Migrating older bundles, "Bundles inspected: 4 / Current: 1 / Unsupported: 2" | I tried to judge these numbers and they do not add up. The text explains this later, but I spent time thinking I had misread the output. | Warn before the output block that the counts do not add up. |
| Migrating older bundles, "The scan counts as a bundle any folder whose extension begins with `lungfish`" | "Extension" and the idea of a folder having one are not explained, and this is the same hidden-in-Finder problem again. | Gloss file extension once at first use. |
| Migrating older bundles, "`migration-available` under `--dry-run` means a fillable gap was found" | "Fillable gap" is a metaphor. I could not tell what the gap is until three paragraphs later when the browser summary is explained. | Name the missing thing here instead of calling it a gap. |
| Migrating older bundles, "The two `unreadable` lines in the demo project are not damage." | I was relieved, but I cannot generalise. If I see `unreadable` on my lab's project, I still do not know whether to worry. | Say which bundle types are the harmless case. |
| Migrating older bundles, "a `.manifest.json.backup` holding the manifest exactly as it was" | Another dot-prefixed hidden name, and I do not know whether I should keep or delete these files over time. | Say whether the reader ever needs to remove these backups. |
| Migrating older bundles, "an interruption leaves the bundle with its old manifest rather than half of a new one" | Long sentence, and "half of a new one" made me stop and picture a broken file. The reassurance is buried at the end of a clause. | Split into a short sentence saying an interrupted run is safe. |
| Migrating older bundles, "so `json` is the format to script against" | Assumes I script. I do not, and I could not tell whether I should care about `--format` at all. | Say plainly that readers who do not script can ignore this option. |
| Provenance expectations, "Locks are coordination metadata rather than scientific output" | "Coordination metadata" is two abstract words together. I know metadata from a library class, not from this. | Say the lock is not a scientific result, so no record is written. |
| Provenance expectations, "It records the tool name and version, the reproducible command, the input files" | A list of eleven things inside one sentence. I could not hold it and I could not judge whether I need any of them. | Turn into a short list, or say the reader never reads this by hand. |
| Provenance expectations, "The wider rule is a requirement on migrations LGE has not written yet" | I read this three times. It says a rule exists for something that does not exist. I could not tell what I am supposed to do with the paragraph. | Label the paragraph as a note about future versions. |
| What good looks like, "A `(Read Only)` title with a yellow banner means a lock." | This is the one check I can actually do, and it is the fourth item after three command-line checks I cannot run. | Put the two window checks first for readers who never use the terminal. |
| What good looks like, "When one disagrees, suspect the project before the app." | "Suspect the project before the app" is an idiom about blame. I understood it only after the next sentence. | Say the cause is usually in the project folder, not a bug in LGE. |
| Whole chapter, no section on what to do right now | I opened my lab's project and it says (Read Only). The chapter tells me what the state means, but never gives me the simple non-terminal answer, which is to ask my labmate to close it and then reopen. | Add one short paragraph for the reader who is locked out and cannot use the terminal. |

The one thing I learned. A lock is only a note that other LGE sessions agree
to read, so it cannot stop a program that ignores it.

The one thing I still could not do. Run any command in this chapter, because
I have never opened a terminal and the chapter never shows me where to type.

The sentence I liked most. "The lock turns a silent corruption into a visible
refusal, which is the whole point of it."
