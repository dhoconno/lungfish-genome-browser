# Merged reader report - Primer Scheme Bundles

Four readers reviewed this chapter. Reader 1 is a sophomore fresh from a genetics course who has never opened a terminal. Reader 2 is a senior with two years of wet-lab library-prep and PCR experience but no data-analysis or terminal background. Reader 3 is a pre-med student reading English as a second language, working from biology-textbook vocabulary. Reader 4 has used Geneious and expects every operation to have a dialog. Reader 1 submitted 44 rows, reader 2 submitted 41 rows, reader 3 submitted 53 rows, and reader 4 submitted 58 rows, for 196 source rows in total.

| Location | Issue | Suggested fix | Readers |
|---|---|---|---|
| What it is, "A bundle is a folder that macOS shows as a single item" | Readers understood the words but not how to look inside a bundle, and later steps assume that skill. | Add the right-click Show Package Contents step at first mention. | 1, 2, 3, 4 |
| Shipped schemes, path `Sources/LungfishApp/Resources/PrimerSchemes/` | Readers read the source-code path as a place they were meant to go look, with no way to open it and no reason to. | Say plainly the path is internal to the app and the reader never needs to browse it. | 1, 2, 3, 4 |
| Shipped schemes, "canonical accession" | Readers met canonical used before or apart from its gloss, which arrives sentences or sections later. | Gloss canonical at the same first use as accession. | 1, 2, 3, 4 |
| Shipped schemes, "which suits short-read Illumina sequencing" | Readers could not judge why amplicon length has to match Illumina versus Nanopore, since the platform-length reasoning is never spelled out. | Add one clause saying each sequencing platform reads only up to a certain fragment length. | 1, 2, 3, 4 |
| Bundle layout, code-block comments naming `--fasta` and `--attachment` flags | Readers met command-line flags inside the bundle-layout diagram before the command-line section exists, and could not tell whether the window route produces these files. | Describe the two optional items in words instead of by flag name, and say which route produces them. | 1, 2, 3, 4 |
| Bundle layout, "a fourth message naming the parse failure" | Readers found three required-file messages implied but not all quoted, then a fourth message counted with no earlier count to follow. | Quote all four messages, or drop the counting language. | 1, 2, 3, 4 |
| Manifest fields, "Its keys are snake_case" | Readers met snake_case as unglossed jargon. | Gloss it as lowercase words joined by underscores. | 1, 2, 3, 4 |
| Manifest fields, "That fallback is silent" | Readers could not picture what silent costs them, since nothing states the consequence of the wrong accession being picked, or how to check it happened. | State plainly that nothing warns the reader and the wrong genome may be used silently. | 1, 2, 3, 4 |
| BED expectations, "tab characters between the columns" | Readers could not tell tabs from spaces in the printed example and had no way to verify their own file used tabs. | Say how to confirm tabs in a text editor, for example a show-invisibles view. | 1, 2, 3, 4 |
| BED expectations, "drops a trailing dash followed by up to three digits" | Readers found the three-digit cutoff arbitrary and could not judge their own primer names against it. | Give one example of a name that fails the rule, and say what happens at four digits. | 1, 2, 3, 4 |
| BED expectations, "`lungfish-cli bam primer-trim --target-reference` overrides" | Readers found the only stated fix for the silent zero-trim trap is a terminal command, with no window-route equivalent offered. | State whether the window route can fix this, and say so plainly if it cannot. | 1, 2, 3, 4 |
| In the LGE window, "Fill the Name field with a file-safe identifier" | Readers were told only that a slash becomes an underscore, with no statement of what else counts as unsafe, such as spaces or accented letters. | Say plainly which characters are allowed, or give a good example name. | 1, 2, 3, 4 |
| In the LGE window, "Type the canonical accession exactly as the alignment...spells its contig" | Readers could not perform this step, since nothing in the chapter says where to find how their own alignment spells its contig name. | Name the place in LGE, such as the Inspector, where the alignment's contig name is shown. | 1, 2, 3, 4 |
| Provenance, "a SHA-256 checksum...as fetched from NCBI" | Readers met checksum used in its harder SHA-256 form before its plain-language gloss, which arrives paragraphs later. NCBI is never glossed at all. | Move the checksum gloss to this first use, and gloss NCBI. | 1, 2, 3, 4 |
| Provenance, "It holds the argv as an array, a durable replay command" | Readers met argv as unglossed programmer jargon, with durable replay command equally unpicturable. | Gloss argv as the command split into its parts, and say what a replay command does. | 1, 2, 3, 4 |
| Provenance, "the runtime identity of the machine" | Readers could not tell what this records about their computer, and some found it worth knowing before sharing a bundle. | Name the one or two fields the runtime identity actually holds. | 1, 2, 3, 4 |
| Provenance, "Nothing in LGE compares them for you, so the comparison is yours to make" | Readers were told to compare checksums but never shown how, with no window-route method given. | Say which file to open and which two lines to compare, or say plainly this needs a terminal. | 1, 2, 3, 4 |
| Keeping a scheme reproducible, section not marked optional | Readers found this section assumes a scripted project and version control with no opt-out line, unlike the command-line section's own opt-out. | Add the same opt-out line the command-line section has, marking this section as for scripted projects. | 1, 2, 3, 4 |
| Front matter, "audience: power-user" | Readers could not tell whether they were supposed to be able to finish a chapter labelled for a power user, with no guidance on what a beginner can skip. | Say in the first paragraph who the appendix is for and what a first-time reader can skip. | 1, 2, 4 |
| Shipped schemes table, primer and amplicon counts | Readers met the raw counts before the roughly-twice rule that would let them judge the numbers, one paragraph or more later. | Put the roughly-twice rule in a lead-in line above the table. | 1, 2, 3 |
| Shipped schemes, menu order versus table order | Readers had to sort the table's second column themselves to predict the menu order, since the table is not ordered the way the menu is. | Order the table the way the menu orders it, or state the sort key plainly. | 1, 3, 4 |
| Shipped schemes, "restore coverage the original design lost as the virus drifted" | Readers met coverage and drifted both unglossed, and could not tell whether coverage meant read depth or genome reach, or whether drifted meant mutation. | Gloss coverage as the parts of the genome the amplicons reach, and drifted as the virus mutating where primers bind. | 1, 2, 3 |
| Manifest fields, BED column references before the BED section | Readers met BED column 4 and non-comment BED rows referenced before the BED expectations section explains them. | Move the BED section before Manifest fields, or gloss comment rows and column 4 here. | 1, 2, 3 |
| Manifest fields, "up to two boolean role flags" | Readers met boolean unglossed and had to infer true-or-false from the JSON example below. | Gloss boolean as a true-or-false value at first use. | 1, 2, 3 |
| BED expectations, worked-example placement for zero-based half-open coordinates | Readers read the sentence itself as the hardest in the chapter, with unfamiliar terms and the example arriving in the same breath as the definition. | Put the worked example on its own line, immediately after the definition. | 1, 2, 4 |
| Auditing a detection, checking the amplicon-count-equals-primer-count tell | Readers liked the check but did not know where in LGE to see the two counts or how to act once they diverge. | Name the Inspector as the place the counts are shown, and say what to do when they differ. | 1, 2, 4 |
| BED expectations, iVar and operation log unglossed | Readers met iVar and operation log named with no gloss and no sense of what a healthy trim rate looks like. | Gloss iVar as the trimming program, name where the operation log opens, and give a rough healthy trim-rate range. | 1, 2, 4 |
| In the LGE window, "Open the project that will own the scheme" | Readers did not know how to open or create a project, and no link back to a projects chapter is given. | Link to the chapter that covers creating a project. | 1, 2, 3 |
| On the command line, opening the Terminal and installing the CLI | Readers could not begin the command-line route because nothing says how to open Terminal or how lungfish-cli gets onto their machine. | Link to the chapter that installs and opens the CLI. | 1, 3, 4 |
| On the command line, "exited 0" | Readers met exit code 0 as unglossed jargon and only guessed it meant success from later context. | Gloss exit code 0 as success at its first use. | 1, 3, 4 |
| On the command line, DemoPanel versus MinimalPanel mismatch | Readers found the worked example built DemoPanel while the shown error names MinimalPanel, and reread thinking they had lost track. | Make the example and the error message use the same name throughout. | 1, 3, 4 |
| What the provenance records, "The importer writes nine lines" | Readers counted the block against the stated nine and could not reconcile the count because of the heading line. | Say nine fields below the heading, or recount so the stated number matches what is shown. | 2, 3, 4 |
| What it is, "shearing" | Readers met shearing used with no gloss and could not tell whether it is a machine step or an enzyme step. | Gloss shearing at first use as breaking DNA into random fragments. | 1, 3 |
| What it is, "the thing a trimming program needs" | Readers met trimming used before its meaning, cutting primer bases from reads, is defined. | Gloss trimming at its first use in the chapter. | 1, 2 |
| What it is, "the Viral Recon wizard read the bundle" | Readers met Viral Recon named with no gloss or link at first mention. | Say in half a sentence what Viral Recon is, or link it, at first mention. | 1, 2 |
| Shipped schemes, ARTIC V3's primer-to-amplicon ratio | Readers noticed V3's 218-over-98 ratio exceeds the roughly-twice rule, with the stated exceptions naming only QIAseq and V4.1. | Say why V3 also carries extra primers, or state how much slack the rule tolerates. | 1, 4 |
| Shipped schemes, "not a judgement call" reversal | Readers found the paragraph spends several sentences comparing platforms as if a choice were needed, only to reverse with though into the kit-box rule. | Put the kit-box rule first, ahead of the platform comparison. | 3, 4 |
| Bundle layout, "refuses to load rather than loading partially" | Readers could not tell where they would see this refusal, whether in a dialog or a log. | Say where the error message appears. | 2, 3 |
| Bundle layout, "someone who assembled a bundle by hand" | Readers found hand-assembling a bundle introduced as an option with no earlier mention that it exists. | State plainly that hand assembly is possible but discouraged, and the importer is the supported route. | 1, 4 |
| Manifest fields, "`schema_version`...currently `1`" | Readers found this entry defines schema version using the word schema, teaching nothing. | Say the field records which version of the file format the bundle follows. | 1, 2 |
| Manifest fields, created versus imported timestamps identical in the example | Readers found the manifest table asserts created and imported are different events, but the minimal manifest example shows both fields with the identical timestamp. | Explain why the two fields match on a fresh import. | 3, 4 |
| BED expectations, subtraction for 30-to-54 covering 24 bases | Readers could not reproduce the arithmetic, expecting 25 from counting inclusively, since the subtraction itself is never shown. | Show the subtraction explicitly, 54 minus 30 equals 24. | 1, 3 |
| Building a scheme, "Both routes below run the same code" contradicted later | Readers found this reassurance contradicted later, when the window route turns out to be missing attachments and several manifest fields. | Say the two routes share the same writer but the window route offers fewer options. | 2, 4 |
| Keeping a scheme reproducible, version-control terms unglossed | Readers met committing, version control, and repository all used with no gloss, in a section written for a reader who already knows them. | Gloss the terms, or mark the section as for readers who already use version control. | 2, 4 |
| Keeping a scheme reproducible, comparing files with no named tool | Readers were told to compare files by hand but never shown a tool or method to do it, in the window or otherwise. | Name a tool or chapter that shows how to compare two files. | 3, 4 |
| Next, "Return to Primer Trimming an Alignment" | Readers arrived at this appendix without having read that chapter, so return assumed a path they had not taken. | Say "Go to" instead of "Return to." | 2, 4 |
| In the LGE window, three FASTA extensions listed together | Readers saw .fasta, .fa, and .fna listed with no statement that they are the same format. | Say the three extensions are interchangeable. | 3, 4 |
| In the LGE window, leaving the equivalent-accessions field empty | Readers did not know whether an equivalent accession is required for their own reference, or whether the field may be left blank. | Say leaving the field empty is normal. | 3, 4 |
| In the LGE window, editing manifest.json with no reachable editor | Readers were offered text-editor editing as the only fix, but the file sits inside a bundle already described as looking like a single item, with no route shown to reach it. | Say how to reach the file inside the bundle, or say to re-import instead. | 3, 4 |
| On the command line, backslash line continuations | Readers did not know the trailing backslashes at line ends were continuations rather than characters to type. | Say the backslashes join the command across lines. | 3, 4 |
| Front matter, prereqs listed in the header block | The prerequisite chapters are listed in a block not normally read, and nothing in the body names them or says the reader should have read them first. | Name the two prerequisite chapters in the opening prose. | 4 |
| What it is, "the thing a trimming program needs" (which program) | The reader was never told which trimming program is meant until iVar appears eight sections later. | Name iVar at this first mention of a trimming program. | 4 |
| What it is, "so the name shown in the menu, the counts shown beside it, and the coordinates...can never drift apart" | The reader had to reread this long sentence three times to connect its subject to its verb. | Split the sentence in two. | 2 |
| What it is, "can never drift apart" | The reader read drift apart as a figure of speech about people, and found it clashed with a later, different use of drifted for the virus. | Replace with "cannot disagree." | 3 |
| What it is, "So what should you do with this?" | The reader read this as a question addressed to them in the middle of reference text and looked for something to answer. | Make it a statement instead of a question. | 3 |
| What it is, "which menu" (Primer Trim dialog not yet named) | The reader had not yet opened LGE in this chapter and did not know which dialog held the referenced menu. | Say the menu is the Primer Trim dialog's scheme picker. | 4 |
| What it is, "A human or macaque amplicon panel imports through the same route" | The reader was reassured by this line, but no non-viral example, row, or command ever appears afterward. | Show one non-viral example, or say plainly that none is shipped. | 4 |
| Shipped schemes, "the key both pickers sort on" (picker jargon) | The reader met picker as the first mention of any picker, with no statement of what it is or where the two instances are. | Name the two places, or gloss picker as a dropdown menu of schemes. | 2 |
| Shipped schemes, "Booster A spike-in primers" | The reader met three unexplained terms at once, with spike-in read in a different, conflicting sense from their own vocabulary. | Gloss spike-in as extra primers added on top of the base design. | 3 |
| Shipped schemes, "kilobase" | The reader met kilobase used without a gloss and had to work out the meaning unaided. | Gloss kilobase as a thousand bases at first use. | 1 |
| Shipped schemes, "None of the eight ships a primer FASTA" stated before Bundle layout | The reader was told what shipped bundles lack before being told what a full bundle contains, since the layout section comes next. | Move this sentence after the Bundle layout section. | 4 |
| Manifest fields, "the loader accepts a manifest without them" | The reader met loader as a new noun for whatever reads a bundle, after importer, trimming program, and LGE were already used for the same thing. | Use one consistent name for the part that reads a bundle. | 4 |
| Manifest fields, JSON block punctuation unexplained | The reader had never seen JSON before and could not tell which punctuation was structure and which was content. | Label the parts of the example in one following sentence. | 4 |
| BED expectations, "which in a tiling scheme alternates between 1 and 2" | The reader knew PCR but not tiling, and the gloss link sits mid-sentence after the concept has already been used. | Define tiling as overlapping amplicons covering the genome end to end before using the term. | 1 |
| Manifest fields, pool numbering must match vendor kit | The reader found column 5's PCR-reaction numbering clear in itself, but was not told it must match their own kit's pool numbers. | State that column 5 must follow the vendor's own pool numbering. | 2 |
| BED expectations, "cannot compete with one another" | The reader read amplicon competition as a metaphor and did not know what happens when amplicons compete or why it is bad. | Say plainly that overlapping amplicons in one tube amplify poorly. | 3 |
| BED expectations, "stripping a trailing `_LEFT` or `_RIGHT`" | The reader read stripping a trailing as programmer language, understood only once the example arrived a sentence later. | Say "removing the ending" instead. | 3 |
| BED expectations, vendor naming other than LEFT/RIGHT | The reader could not tell what happens if their vendor uses FWD and REV instead of LEFT and RIGHT. | Say what happens with other naming conventions and how to rename primers. | 4 |
| BED expectations, "the tell" idiom | The reader did not know the card-playing idiom "the tell." | Replace with "the sign." | 3 |
| Building a scheme, "I did not know whether I needed both" routes | The reader did not know whether both routes were required, since the reassurance that the window route alone suffices arrives only later. | State here that the window route alone is complete. | 1 |
| Building a scheme, "Both routes...run the same code" read positively | The reader read this line as reassuring on its own terms and reported no problem with it. | No change needed. | 3 |
| In the LGE window, "shows fewer fields than a shipped scheme's does" | The reader could not tell whether the missing fields were cosmetic or affected trimming results. | Say the missing fields are labels only and do not change results. | 1 |
| On the command line, "The whole procedure runs headless" read positively | The reader noted headless is glossed and the section's own opt-out line let them skip ahead without difficulty. | No change needed. The opt-out sentence already suffices. | 2 |
| Provenance, ISO timestamp "T" and "Z" | The reader met the T and Z in the timestamp unexplained, alongside the unexplained identical created and imported values. | Say the timestamp is in UTC, and that a fresh import sets both fields at once. | 1 |
| Keeping a scheme reproducible, "the choice is between storage and trust" | The reader found the summary word trust did not match the paragraph that followed, which compared repository size and review instead. | Replace trust with a word drawn from the paragraph itself. | 3 |
| Keeping a scheme reproducible, "will show a difference that means nothing" read twice | The reader had to reread this line to see the difference was harmless rather than a warning sign. | Lead with the reassurance before the explanation. | 1 |
| Keeping a scheme reproducible, "Regenerating has one wrinkle worth planning around" | The reader did not know the idiom "wrinkle" and understood it only from the following sentence. | Replace with "one catch." | 3 |
| In the LGE window, "Two limits are worth knowing before you start" placed after the steps | The reader read the fill-in steps first and only afterward learned what the window route could not do, despite the heading "before you start." | Move this paragraph above the fill-in steps. | 3 |
| In the LGE window, "the whole of what inspecting a scheme means" | The reader had to reread this wordy phrase twice. | Shorten to "inspecting is read-only." | 3 |
| On the command line, "the directory you are standing in" | The reader read standing in as a metaphor for a terminal concept they did not have. | Say "the folder the terminal is currently using." | 3 |
| On the command line, "against the same 58-primer BED" | The reader had to cross-reference the shipped-schemes table to work out that 58 primers meant the Midnight scheme. | Name the scheme directly in the sentence. | 3 |
| On the command line, "the exact command as typed" shown truncated | The reader found the promised exact command shown ending in an ellipsis, so it was not actually visible. | Note that the example command is shortened. | 3 |
| In the LGE window, "choose File > Import Center...and click the Reference Sequences tab" | The reader, coming from Geneious, would not have thought to look for primer schemes under a Reference Sequences tab. | Add a clause saying primer schemes live on that tab. | 4 |
| In the LGE window, "Identity has four text fields" with no example values | The reader, expecting a dialog that prefills from the file the way Geneious does, found no defaults or example values shown. | Give one filled-in example set of the four values. | 4 |
| In the LGE window, "leave it empty to let the name serve as both" | The reader read this as conflicting with an earlier line saying Import stays disabled until name and accession are set. | State that display name is optional in the same sentence as the enable rule. | 4 |
| On the command line, "This section is optional" contradicted by earlier traps | The reader found earlier trap paragraphs already sent them to a CLI-only fix, making the optional claim not quite true. | Say plainly which two things only the command line can do. | 4 |
| On the command line, "`--fasta` copies a FASTA of the primer sequences" | The reader was warned off using a reference genome by mistake but still did not know where a primer FASTA would come from. | Say the vendor supplies the primer FASTA, or say to omit the flag. | 4 |
| On the command line, "A minimal import shows the defaults at work" as a third example | The reader found the minimal manifest a third, confusingly named example after Demo and Minimal, with an ambiguous "the same" BED reference. | Name the BED file explicitly rather than calling it "the same." | 4 |
| On the command line, JSON key order differs from the field table | The reader compared the alphabetical JSON keys against the field table's different order before giving up on reconciling them. | Say the file's key order does not matter. | 4 |
| Provenance, "not provided" case never shown | The reader saw only the FASTA-source-path example, never the "not provided" case a window import would actually produce. | Show the window-import version of the provenance file. | 4 |
| On the command line, "`--output DemoPanel` above produced" (stem gloss) | The reader met stem used without a gloss and could not connect it to DemoPanel. | Gloss stem as the filename without its extension. | 2 |
| What it is, "copies a genome in numbered pieces rather than shearing it at random" (PCR not yet named) | The reader met the copying reaction described before PCR itself is named, many sections later. | Name PCR at this first mention of the copying reaction. | 3 |
| Shipped schemes, "The primer count should run to roughly twice the amplicon count" | The reader could not tell how far off from twice is too far, since the shipped schemes range from exactly 2x to 2.52x with no stated boundary. | Give a number, such as more than ten percent above twice, as the boundary worth a second look. | 4 |

93 merged rows total, covering 196 source rows from four readers.

## Consensus

32 merged rows were hit by three or more readers.

- All four readers could not tell how to look inside a primer scheme bundle, since it looks like a single item in macOS.
- All four readers read the shipped-schemes source-code path as somewhere they were meant to browse, though it is internal to the app.
- All four readers met canonical accession used before or apart from its own gloss.
- All four readers could not judge why amplicon length has to match the sequencing platform, since the reasoning is never spelled out.
- All four readers met command-line flags inside the bundle-layout diagram before the command-line section exists.
- All four readers found a fourth error message counted but not fully quoted, with no earlier count to follow.
- All four readers met snake_case used as unglossed jargon.
- All four readers could not tell what a silent fallback costs them or how they would know it had happened.
- All four readers could not tell tabs from spaces in the printed BED example.
- All four readers found the three-digit trailing-dash rule arbitrary and could not judge their own primer names against it.
- All four readers found the only stated fix for the silent zero-trim trap a terminal command with no window equivalent.
- All four readers were told only that a slash gets replaced in the Name field, with no full list of unsafe characters.
- All four readers could not perform the canonical-accession step because nothing says where to find their own alignment's contig name.
- All four readers met the checksum term used in its harder SHA-256 form before its plain-language gloss, with NCBI never glossed at all.
- All four readers met argv and durable replay command as unglossed jargon.
- All four readers could not tell what the runtime machine identity actually records.
- All four readers were told to compare checksums by hand with no method given for doing so.
- All four readers found the reproducibility section assumes version control with no opt-out line, unlike its command-line neighbor.
- Three readers could not tell whether a chapter labelled for a power user was meant for them, with no guidance on what a beginner can skip.
- Three readers read the shipped-schemes primer and amplicon counts before the rule that would let them judge those numbers.
- Three readers had to sort the shipped-schemes table themselves to predict the actual menu order.
- Three readers met coverage and drifted both used without a gloss in the same sentence.
- Three readers met BED-file terms referenced in the manifest section before the BED section explains them.
- Three readers met boolean used without a gloss.
- Three readers found the worked example for the zero-based half-open coordinates placed awkwardly relative to its own definition.
- Three readers liked the amplicon-count-equals-primer-count check but did not know where to see it or what to do when it fails.
- Three readers met iVar and the operation log named with no gloss and no sense of a healthy trim rate.
- Three readers did not know how to open or create the project that owns a new scheme, with no link given.
- Three readers could not begin the command-line route because nothing says how to open Terminal or install the CLI.
- Three readers met exit code 0 as unglossed jargon.
- Three readers found the DemoPanel example and the MinimalPanel error message do not match.
- Three readers could not reconcile the stated count of nine provenance lines against what the example block actually shows.
