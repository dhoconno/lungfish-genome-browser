# Reader 3 report, Primer Scheme Bundles

Reader persona. Pre-med student, English is my second language. I know
biology words from my textbooks. I have never opened a terminal.

| Location | Issue | Suggested fix |
|---|---|---|
| What it is, "copies a genome in numbered pieces rather than shearing it at random" | "Shearing" is not explained. I know shear from physics class, not from DNA. I could not tell whether shearing is a machine step or an enzyme step. | Gloss shearing at first use as breaking DNA into random fragments. |
| What it is, "starts the copying reaction" | Which copying reaction? The word PCR never appears until much later, in the primer pool row of the BED table. The reader meets the machine before the name. | Name PCR here at first use. |
| What it is, "a folder that macOS shows as a single item" | I read this twice. If it is a folder but looks like one item, I did not know whether I can open it, or how. The chapter later says to edit `manifest.json` inside it, so I must open it somehow, but no step tells me the way. | Add the right-click Show Package Contents step here. |
| What it is, "can never drift apart" | "Drift apart" is a figure of speech about people. I had to stop and work out that it means the two values stop agreeing. Later the chapter uses "drifted" again for the virus, in a different meaning, which confused me more. | Replace with "cannot disagree". |
| What it is, "So what should you do with this?" | A question addressed to me in the middle of reference text. I looked for something I was supposed to answer. | Make it a statement. |
| Shipped schemes, "in `Sources/LungfishApp/Resources/PrimerSchemes/`" | This looks like a place I should go and look, but I do not know where it is on my computer, and nothing tells me. It appears to be inside the program itself. | Say plainly that this path is inside the app and the reader does not need to visit it. |
| Shipped schemes, "canonical accession `MN908947.3` and equivalent accession `NC_045512.2`" | "Canonical" is used before it is explained. The accession sentence that follows explains accession but not canonical. I learned what canonical means only in the Manifest fields section, many paragraphs later. | Gloss canonical here as the one the coordinates were measured against. |
| Shipped schemes table, "Primers 218, Amplicons 98" | I could not judge these numbers on my own. The rule comes one paragraph after the table, so on first reading the whole table was just numbers to me. | Put the roughly-twice rule in a lead-in sentence before the table. |
| Shipped schemes, "Booster A spike-in primers" | Three unexplained terms at once, Booster A, spike-in, and the idea that a kit adds extra primers. I know spike-in from a different context, adding a control sample. | Gloss spike-in here as extra primers added on top of the base design. |
| Shipped schemes, "restore coverage the original design lost as the virus drifted" | "Coverage" is never explained in this chapter, and "drifted" is a metaphor. I guessed drift means mutation, but I was not sure. | Say the virus mutated where the primers bind. |
| Shipped schemes, "the order the second column reads alphabetically rather than the order this table uses" | Long sentence, read three times. I still cannot predict the menu order without sorting the second column myself. | Sort the table by the second column so the two orders match. |
| Shipped schemes, "which suits short-read Illumina sequencing" | Illumina and Oxford Nanopore are brand names with no gloss. I have heard of them, but I could not tell from this text why short amplicons suit one and long amplicons suit the other. | Add one clause saying each machine reads pieces of a certain length. |
| Shipped schemes, "read off the kit box rather than picked from the menu" | I liked the advice but the phrase "not a judgement call" before it made me read twice, because the previous paragraph spent five sentences comparing the schemes as if I should choose. | Move the kit-box rule to the front of the paragraph. |
| Bundle layout, "`primers.fasta` # only when you supplied `--fasta`" | The code block explains itself using command-line flags I have not read yet, and the window route does not offer them. I could not tell whether a bundle I make in the window has this file. | Annotate the block in words, not flags. |
| Bundle layout, "a bundle missing any one of them refuses to load rather than loading partially" | I could not perform anything from this. It says the message names the file, but I do not know where a message like that appears in the app. | Say where the error is shown. |
| Bundle layout, "A manifest that is present but unreadable gives a fourth message naming the parse failure" | "Parse failure" is not explained, and "a fourth message" counts messages I was never shown. Only one message text is quoted. | Quote the fourth message or drop the count. |
| Manifest fields, "Its keys are snake_case." | "Keys" and "snake_case" are both unexplained. I guessed snake_case from the field names in the table below, but only after reading the table. | Gloss key as the name before the value, and snake_case as words joined by underscores. |
| Manifest fields, "`primer_count` Number of non-empty, non-comment BED rows" | I did not know a BED file could have comment rows until this line, and the BED section below never says how a comment row is marked. | Say a comment row starts with a hash. |
| Manifest fields, "`created` Timestamp written when the bundle was authored" vs "`imported`" | The row for `imported` says the two are different events, but the minimal manifest example later shows both with the identical value `2026-09-07T14:10:05Z`. I read both twice and could not resolve it. | Explain why the two match on a fresh import. |
| Manifest fields, "up to two boolean role flags" | "Boolean" is a programming word. I worked out from the JSON that it means true or false. | Gloss boolean as true or false. |
| Manifest fields, "That fallback is silent, so mark the flags rather than relying on it." | "Fallback" and "silent" are both used in a technical sense I had to infer. The point is good, but I read the sentence twice. | Say LGE gives no warning when it guesses. |
| BED expectations, "zero-based and half-open" | This is the hardest sentence in the chapter. The explanation is given, and the 30 to 54 example helped, but I still had to count on my fingers to reach 24 and I am not confident I would get the next one right. | Show the subtraction, 54 minus 30 equals 24. |
| BED expectations, "tab characters between the columns" | I cannot see a tab in the example block. It looks like spaces to me. Nothing tells me how to make a tab in a text editor, or that a spreadsheet export might use commas instead. | Warn that spaces will not work and say a spreadsheet must be saved as tab-separated. |
| BED expectations, "cannot compete with one another" | Competition between amplicons is a metaphor I had to think about. I do not know what happens when they compete or why it is bad. | Say overlapping amplicons in one tube amplify poorly. |
| BED expectations, "it counts amplicons by stripping a trailing `_LEFT` or `_RIGHT`" | "Stripping a trailing" is programmer language. I understood it from the example, but the example came in the next sentence. | Say removing the ending. |
| BED expectations, "It also drops a trailing dash followed by up to three digits" | I read this three times. The example `nCoV-2019_72_LEFT-1` has dashes in two places, in `nCoV-2019` and at the end, and I could not tell why only the last one is dropped. | Say only a dash at the very end of the name. |
| BED expectations, "which is the tell that the naming did not parse" | "The tell" is an idiom from card playing. I did not know it. | Replace with "the sign". |
| BED expectations, "can trim zero primers and raise no visible error at all" | This is called a trap and it worries me, but I cannot perform the check. The fix offered is a command-line flag, and the other check is "the trim rate iVar prints in the operation log", where iVar, trim rate, and operation log are all new words. | Give a window-route check for this trap, since the whole section assumes a terminal. |
| Building a scheme, "Both routes below run the same code and write the same bundle." | I liked this sentence, it told me I could skip the terminal half. | No change needed. |
| In the LGE window, "Open the project that will own the scheme" | I do not know how to open a project, and no earlier step in this chapter creates one. The chapter's prerequisites do not include a chapter on projects. | Link to the chapter that creates a project. |
| In the LGE window, "with a file hint reading `.bed (+ optional .fasta/.fa/.fna)`" | Three extensions with no explanation of the difference. I assumed they are the same format with different names, but the text does not say so. | Say the three endings mean the same format. |
| In the LGE window, "Fill the Name field with a file-safe identifier" | "File-safe identifier" is not defined. The sentence tells me only that a slash becomes an underscore, which does not tell me what else is unsafe, such as spaces or accented letters. My own name has an accent in it. | Say which characters are allowed. |
| In the LGE window, "Type the canonical accession exactly as the alignment you will trim spells its contig" | I could not perform this step. I do not know how to find out how my alignment spells its contig, and no place in the app is named where I could look. | Name the Inspector field or menu that shows the alignment's contig name. |
| In the LGE window, "type any equivalent accessions into the last field separated by commas" | I do not know how to find an equivalent accession for my own panel, or whether I may leave the field empty. The shipped-scheme example is a virus deposited twice, which does not tell me what to do for a macaque panel. | Say the field may be left empty. |
| In the LGE window, "Two limits are worth knowing before you start." | This paragraph comes after the Import step, not before it, although it says "before you start". I read the sheet steps first and only then learned what the sheet cannot do. | Move it above the fill-in paragraph. |
| In the LGE window, "which is the whole of what inspecting a scheme means in the window" | Long, and the phrase "the whole of what" made me read twice. | Shorten to "inspecting is read-only". |
| In the LGE window, "means editing `manifest.json` in a text editor" | I have never edited a JSON file, and I do not know how to reach one inside a bundle that macOS shows as a single item. This is the only route the chapter offers for changing a field. | Give the Show Package Contents step and name a text editor. |
| On the command line, "The whole procedure runs headless, meaning with no window at all, by typing commands into the Terminal application." | I appreciate the gloss of headless. I still cannot use this section, because nothing says how to open Terminal or how `lungfish-cli` gets onto my computer. The chapter's own opt-out sentence saved me. | Link to whatever chapter installs the CLI. |
| On the command line, "```bash lungfish-cli primers import \" | The backslashes at the ends of lines are not explained. I did not know whether to type them. | Say the backslash continues one command across lines. |
| On the command line, "That run printed one line and exited 0." | "Exited 0" is not explained until much later, where "Exit status: 0" appears in the provenance block, also unexplained. I guessed 0 means success only because the failure case says "exits 1". | Say 0 means success. |
| On the command line, "the directory you are standing in" | "Standing in" is a metaphor for a terminal idea I do not have. | Say the folder the terminal is currently using. |
| On the command line, "Error: A primer scheme bundle already exists at ... MinimalPanel.lungfishprimers." | The command above used `--output DemoPanel`, but the error names `MinimalPanel`. I read both twice, thinking I had misread the flag. | Make the two examples use the same name. |
| On the command line, "against the same 58-primer BED" | I had to go back to the shipped-schemes table to work out that 58 primers means the Midnight scheme. The `--bed midnight.bed` in the earlier block is the only clue. | Name the scheme in the sentence. |
| What the provenance records, "the exact command as typed" | The provenance example shows the command ending in "...", so I cannot see the exact command, even though the sentence promises it. | Note that the example is shortened. |
| What the provenance records, "The importer writes nine lines into it" | I counted the block and found nine content lines plus a heading, so the count works, but I counted twice because the `# PROVENANCE` heading made me unsure. | Drop the count or say nine after the heading. |
| What the provenance records, "a SHA-256 checksum of each declared accession's sequence as fetched from NCBI" | Checksum is glossed here, but two paragraphs later, at "A checksum is a short string computed from a file's exact bytes". I needed the gloss at this first use. NCBI is never glossed anywhere in the chapter. | Move the checksum gloss to first use and gloss NCBI. |
| What the provenance records, "It holds the argv as an array, a durable replay command" | "Argv" is a programming word with no gloss, and "durable replay command" is three words I cannot picture. This is the longest sentence in the chapter, listing eight things. | Gloss argv and split the sentence. |
| What the provenance records, "the runtime identity of the machine" | I cannot tell what this records. My computer's name? Its serial number? This matters to me for privacy if I share a bundle. | Say what fields it holds. |
| What the provenance records, "Nothing in LGE compares them for you, so the comparison is yours to make." | The chapter tells me to compare checksums but never shows a way to do it in the window. Every route it hints at is a terminal route. | Say a window route does not exist, or give one. |
| Keeping a scheme reproducible, "the choice is between storage and trust" | Read twice. The paragraph that follows compares repository size and review, not trust, so the summary word did not match what came after. | Replace trust with a word from the paragraph. |
| Keeping a scheme reproducible, "Committing the finished ... folder to version control" | "Committing", "version control", and "repository" are all unexplained. I have never used git. The section is written for someone who has. | Say this section is for readers who use version control. |
| Keeping a scheme reproducible, "Regenerating has one wrinkle worth planning around." | "Wrinkle" is an idiom. I understood it only from the sentence after it. | Replace with "one catch". |
| Keeping a scheme reproducible, "a byte-for-byte comparison of `manifest.json` will show a difference that means nothing" | I do not know how to do a byte-for-byte comparison, and the fix, comparing `primers.bed` instead, is the same problem again. | Name the tool that compares two files. |

Three closing lines.

One thing I learned. A primer scheme bundle keeps the coordinates, the
name shown in the menu, and the record of where the scheme came from in
one item, so the label and the numbers cannot disagree.

One thing I still could not do. Build a scheme for a macaque panel. I
could not find out how my alignment spells its contig name, so I could
not fill in the canonical accession field the sheet requires.

The sentence I liked most. "The right scheme is whichever one your
wet-lab protocol actually used, read off the kit box rather than picked
from the menu."
