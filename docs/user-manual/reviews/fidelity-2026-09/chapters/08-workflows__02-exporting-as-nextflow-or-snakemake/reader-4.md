# Reader 4 report

Persona: undergraduate who used Geneious for one semester, expects a dialog for every operation, has never opened a terminal.

| Location | Issue | Suggested fix |
|---|---|---|
| Title and front matter, "Exporting as Nextflow or Snakemake" | I do not know what Nextflow or Snakemake are before I start reading, and the chapter never says in plain words. It says they are things a collaborator "already uses" and that a file "validates", but I never learn that they are systems for running a chain of analysis programs. Geneious had no equivalent, so I have no mental slot to put this in. | Add one sentence in What it is glossing Nextflow and Snakemake as systems that run a chain of analysis steps on another computer. |
| What it is, "Every time Lungfish Genome Explorer (LGE) runs a tool for you" | I do not know what "a tool" means here. In Geneious everything was a plugin inside the program. I could not tell whether this means a separate program on my Mac or something inside LGE. | Gloss "tool" once as a separate analysis program LGE runs for you. |
| What it is, "the exact command line, the tool and its version" | "Command line" is used without a gloss, and I have never opened a terminal. The chapter waits until the very last section to say a command line is something typed into Terminal. | Gloss command line at this first use, not in the optional section. |
| What it is, "they sit on a spectrum from 'executable on somebody else's cluster'" | "Cluster" is not explained. I picture a group of samples, which is what cluster meant in my genetics course. | Gloss cluster once as a shared multi-computer system at a university. |
| What it is, table row "Python Script... A subprocess driver where a Python entry point is more convenient" | I had to read this three times and still cannot say what it means. "Subprocess driver" and "entry point" are both unexplained, and neither tells me when I would pick this row over the shell row. | Replace the cell with a plain statement of who would choose Python over shell. |
| What it is, "Each one writes a folder rather than a single file." | This contradicts my Geneious expectation, where Export produced one file I could attach to an email, and the text does not say what to do with a folder. I did not know whether I can email a folder. | Add a sentence saying to compress the folder before sending it. |
| What it is, "LGE walks the provenance chain backwards from it" | "Walks the chain backwards" is a metaphor I cannot cash out. I do not know how far back it goes or whether it stops at the file I imported. | State plainly that it collects every earlier step that fed into the selected file. |
| Why you would do this, "which minimap2 build produced a BAM file" | minimap2, "build", and BAM are all used before any gloss. I know BAM vaguely from a lecture but not "build" as distinct from version. | Gloss BAM at first use and say build means a specific compiled copy of a version. |
| Why you would do this, "not, in the build tested for this chapter, pipelines that execute unmodified" | "The build tested for this chapter" gave me no way to know whether it applies to the copy I have. I do not know how to find my build number or whether mine differs. | Name the version tested and say where the reader finds their own. |
| Before you start, "its `Analyses/mapping-HG002` run, which mapped human reads" | I could not perform this step. The chapter says "the demo project" as if I already have it. I do not know where the demo project comes from or how to open it. | Say in one sentence how to obtain and open the demo project, or link the chapter that does. |
| Before you start, "a piece of the human reference genome with minimap2 and then processed the alignment with samtools" | samtools is never glossed anywhere in the chapter although it appears about ten times, including in an error message I am told to recognise. | Gloss samtools at this first use. |
| Before you start, "a mapping result, an assembly, a classification, or a download all qualify" | I could not judge my own files against this list. My course files were FASTA and FASTQ I downloaded from a website by hand, which sounds like the disqualified case, but "a download" is listed as qualifying, so I could not tell which side I am on. | Say the download must be one LGE performed, not one you did in a browser. |
| Procedure step 1, "LGE looks at the visible viewport first and the sidebar selection second" | "Viewport" is used without a gloss. Geneious called this the document view. I also could not predict which one wins if I have one thing open and a different thing clicked. | Gloss viewport at first use in this chapter. |
| Procedure step 1, "select `Analyses/mapping-HG002` in the demo project" | I could not perform this. I do not know whether Analyses is a folder I expand in the sidebar or a tab, and the chapter has no picture yet, only a planned one. | Name where in the window the Analyses folder appears. |
| Procedure step 2, "The submenu is nested two levels deep inside File" | I had to read this twice against the entry point above it, which shows three levels after File. Two levels deep and File > Export > Provenance > Nextflow did not obviously agree. | Say the item sits under Export, then Provenance. |
| Procedure step 3, "choose a location outside the project" | This differs from Geneious, where exports naturally went anywhere and nothing warned me. I could not tell what breaks if I ignore the advice and save inside the project. | Say what goes wrong when the export lands inside the project folder. |
| Procedure step 3, "Exporting onto a path that already exists and is not a folder is refused" | I had to read this twice. The double negative around "exists and is not a folder" left me unsure what happens in the ordinary case where a folder of that name already exists. Does it overwrite my earlier export? | State plainly what happens when a folder of the same name already exists. |
| Settings, "Every entry below closes with the flag that does the same job on the command line" | "Flag" is not glossed, and it appears here before the optional terminal section explains anything. As someone who will never type a command, I could not tell whether these sentences are instructions for me. | Say once that flag means a typed option and that these closing sentences are for terminal users only. |
| Settings, "**Provenance.** Chooses what the export renders" | The setting is named Provenance but I never see a control called Provenance in the save panel, and the entry itself says there is no control. Calling it a setting when it is a menu choice made me reread the whole paragraph. | Name the entry after the menu choice rather than calling it a setting with no control. |
| Settings, "**Save As.**" and "**Where.**" | These are described as two of three settings, but in the save panel I know from Geneious the name field and the location are one dialog. I could not tell whether Where is a separate control I must hunt for. | Say both are parts of the standard macOS save panel. |
| Settings, "**--signature.** Names the signature artifact that `provenance verify` should check" | I do not know what a signature artifact is, what signing a record means, or why I would want one. The word artifact is also used two different ways in this chapter, once for my data file and once for a signature file. | Gloss digital signature once, and use a different word for the signature file. |
| Settings, "The default is `<sidecar>.pub`" | I cannot read the angle bracket notation. I do not know if I literally see a file named sidecar.pub or something else. | Show one real filename as an example. |
| Reading the results, "`provenance/source/external/` with their original path preserved as a folder tree" | I had to read this twice and still cannot picture the result. "Original path preserved as a folder tree" does not tell me what I will see when I open the folder. | Show two lines of the folder listing instead of describing it. |
| Reading the results, "including the records inside any enclosing `.lungfishref` reference bundle" | ".lungfishref reference bundle" appears here with no gloss and no earlier mention in this chapter. | Gloss it or drop it from this sentence. |
| The Nextflow export, "the LGE version, the original run time, the host operating system" | "Run time" is ambiguous to me. It could be the clock time when the run happened or how long the run took. Both readings appear elsewhere in the chapter, where "wall time" is used for duration. | Say when the run started, if that is the meaning. |
| The Nextflow export, "with names derived from the filenames by turning each dot into an underscore" | I checked the example and could not make it work. `HG002.chr20.10.0-10.5Mb.R1.fastq.gz` has a hyphen and capitals too, and the result is `hg002_chr20_10_0_10_5mb_r1_fastq_gz`. The rule as stated does not predict lowercasing or the hyphen. | State the full rule, or say the exact transformation is not worth predicting and read the file. |
| The Nextflow export, "`MINIMAP2_1` followed by `SAMTOOLS_2` through `SAMTOOLS_5`" | I could not judge whether five steps is normal. My Geneious mapping felt like one operation. Nothing tells me whether a run with two steps or twenty is expected. | Say the step count simply equals the number of recorded operations, which varies. |
| The Nextflow export, "`root /Users/dho/.lungfish/conda`" | This shows another person's username inside what I am told my export will contain. I could not tell whether my export would say dho or my own name. | Use a placeholder username in the example. |
| The Nextflow export, "conda environment minimap2" | conda is never glossed although it is used five times, including in the sentence explaining why the container manifest is empty. | Gloss conda once as the system LGE uses to install analysis programs. |
| The Nextflow export, "a run whose steps are all byte-copy replays of a retained selection" | I did not understand a single term in this sentence. Byte-copy replay, retained selection, and REPLAY_RETAINED_SELECTION are all new, and I cannot tell whether my run is one of these. | Say plainly which kind of operation produces this and how the reader recognises it. |
| The Nextflow export, "`docker.enabled = true`" and "note what it is for" | I am shown a config that turns Docker on for a run whose tools came from conda, and the text does not say whether that line will cause a problem. Then "note what it is for" is a sentence I read twice without extracting an instruction. | Say whether the docker line matters when no containers were used. |
| The Nextflow export, "adding an executor or a profile block to this file yourself" | I could not perform this. Executor and profile block are unexplained, and I would not know where to start editing. | Point to the Nextflow documentation for this edit rather than implying it is a small task. |
| The Snakemake export, "a usage comment reading `snakemake --cores 8 --use-singularity`" | Singularity appears once, never explained, and I cannot tell whether I need it installed. The number 8 is also unjudgeable. Is 8 a requirement or a suggestion? | Say the 8 is an editable suggestion and gloss or drop the singularity flag. |
| The Snakemake export, "A collaborator overrides one with `--config`" | I could not perform this and I could not tell whether I was meant to. The sentence gives a flag with no example. | Show the whole command or say this is a note for the recipient. |
| The other four exports, "`set -euo pipefail`" | Shown as the opening of a file I am encouraged to read for understanding, but this line means nothing to me and is not explained. | Say in half a sentence that the line makes the script stop on the first error. |
| The other four exports, "with that file's SHA-256 in a trailing comment" | SHA-256 appears here for the first time. The chapter glossed checksum nicely at the top but never connects that gloss to this name. | Say SHA-256 is the kind of checksum described earlier. |
| The other four exports, "Both files are written with the executable bit set, so `./run.sh` and `./reproduce.py` work without a `chmod` first." | Executable bit, the `./` prefix, and chmod are three terminal concepts in one sentence, in a section that is not the optional terminal section. | Move this sentence to the command line section. |
| What good looks like, "confirm none of them reads `unknown`" | I could not perform this without being told where the versions appear in each of the six targets. The chapter shows a version inside a comment in main.nf but not in methods.md or provenance.json. | Name the place to look in one or two representative targets. |
| What good looks like, "validate the file before you send it" then a `bash` code block | Here I stop entirely. I have never opened a terminal, the chapter told me at the start that the command line section is optional, and now the only way to complete the third of three required checks is to type two commands. That is a contradiction I read three times. | Say that this check needs the terminal, and say what a reader who will not use one should do instead. |
| What good looks like, "`nextflow lint main.nf`" | I could not perform this because nothing tells me how to get Nextflow onto my Mac. Before you start says running an export needs Nextflow "on the machine that will run it", which sounded like the collaborator's machine, not mine. | Say the person validating needs Nextflow installed locally, and link the install instructions. |
| What good looks like, "reported one error and eleven warnings" | I cannot judge eleven warnings. I do not know whether warnings are fatal, cosmetic, or somewhere between, and the paragraph only explains the error. | Say warnings do not stop a run and the error does. |
| What good looks like, "`CyclicGraphException in rule samtools_5`" | I had to read the explanation twice. "Lists `HG002.sorted.bam` as both an input and an output of the same rule" is clear, but I could not see why samtools flagstat would do that, and the chapter does not say what flagstat is. | Add three words saying flagstat only reads the file and writes a summary. |
| What good looks like, "These are defects in LGE's exporter rather than mistakes you made, and they are reported below." | I looked below and could not find where they are reported. The remaining sections are On the command line and Next, and neither carries a bug list or a link. | Point to the actual bug report or drop the promise. |
| On the command line, "The whole procedure runs headless, meaning with no window at all, by typing commands into the Terminal application." | This is the first and only place Terminal is named, and it arrives after four earlier sections have already used command line, flag, chmod, and executable bit as if known. | Move the Terminal gloss to the first mention of the command line. |
| On the command line, "`lungfish-cli provenance export ./Analyses/mapping-HG002`" | I could not perform this. Nothing says where lungfish-cli lives or how it gets on my computer, and the leading `./` is unexplained. | Add one sentence saying how to install or locate lungfish-cli. |
| On the command line, "LGE pins Nextflow 26.04.6 and Snakemake 9.25.2" | I could not judge these. "Pins" is unexplained, and I do not know how a collaborator finds out which version they have or what counts as "much older". | Say pins means LGE installs that exact version, and name the oldest Nextflow that works. |
| On the command line, "`--from-lockfile` reconstruction is unsupported" | I read this twice. The paragraph tells me a file cannot be fed back in, then names a flag that does not work, and I could not tell whether the flag exists and fails or does not exist. | Drop the flag name and keep the plain statement. |
| Next, "whose own Export menu writes Nextflow and Snakemake through a separate code path" | "Separate code path" is developer language. I could not tell whether the Workflow Builder export has the same defects described in this chapter or different ones, which is the only thing I wanted to know. | Say whether the builder export has the same validation problems. |

The one thing I learned is that LGE keeps a written record of every command it ran for me, and that record is separate from and more trustworthy than the pipeline it generates.

The one thing I still could not do is complete the third check in What good looks like, because it requires the terminal that the chapter told me I could skip.

The sentence I liked most is "A checksum is a short fingerprint computed from a file's exact bytes, so two people can confirm they are holding the identical file."
