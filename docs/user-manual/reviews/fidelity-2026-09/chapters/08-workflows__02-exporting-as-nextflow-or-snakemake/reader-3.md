# Reader 3 report

Chapter: `docs/user-manual/chapters/08-workflows/02-exporting-as-nextflow-or-snakemake.md`

Reader: pre-med student, English is my second language, biology vocabulary is fine, idioms and long sentences are slow, I have never opened a terminal.

| Location | Issue | Suggested fix |
|---|---|---|
| Title and metadata, "Exporting as Nextflow or Snakemake" | The title uses two words I have never seen, and the chapter body never says plainly what Nextflow and Snakemake are. I read the whole chapter and I still only infer that they are programs that run a series of steps. | Add one sentence at first use of each, saying a workflow manager is a program that runs a chain of analysis steps in order on a computer or a cluster. |
| What it is, "a small JSON file that sits beside" | JSON is used without explanation here and again in the target table and in `provenance.json`. I do not know if I can open it or if I need a program. | Gloss JSON at first use as a plain text format that programs read easily and a person can open in a text editor. |
| What it is, "they sit on a spectrum from" | Two figures of speech in one sentence, a spectrum and quoted phrases like "executable on somebody else's cluster". I had to read this twice to see it just means the six targets are ordered from runnable to readable. | Replace the spectrum image with a plain statement that the first four targets can be run and the last two are for reading. |
| What it is, "on somebody else's cluster" | Cluster is never explained. I do not know if this is a machine, a room of machines, or a service. | Gloss cluster at first use as a shared group of computers that a university or institute runs for large jobs. |
| What it is table, "Best for" column, "A subprocess driver where a Python entry point" | Subprocess, driver, and entry point are three unexplained terms in one cell. I could not tell how the Python target differs from the shell one. | Replace the cell with a plain reason such as, for a group that prefers Python to shell. |
| What it is, "So the sentence to remember is this." | This reads as spoken English and made me stop and look for a sentence after it. The instruction that follows is fine, but the lead-in wasted a re-read. | Delete the lead-in and give the instruction directly. |
| What it is, "LGE walks the provenance chain backwards from it" | Walking a chain backwards is a metaphor. I guessed it means LGE looks at what made this file, then what made those files, but I was not sure how far back it goes. | State it plainly and say where it stops, for example that it follows inputs back until it reaches files LGE did not create. |
| Why you would do this, "which minimap2 build produced a BAM file" | Build is used in a software sense that I only know from construction. Also minimap2 and BAM appear here with no gloss in this chapter. | Say version instead of build, and gloss BAM at first use as the compressed file that holds reads aligned to a reference. |
| Why you would do this, "There is an honest limit worth stating at the top rather than burying." | An idiom, burying, and an unusual sentence shape. I read it twice before I understood that the paragraph is a warning. | Open the paragraph with a plain warning sentence such as, one limit matters before you choose a target. |
| Why you would do this, "in the build tested for this chapter" | I do not know what build means here, or how to find out whether my copy of LGE is that build. This matters because the whole warning depends on it. | Name the version number and say how to check it, for example in the About window. |
| Before you start, "its `Analyses/mapping-HG002` run, which mapped human reads ... and then processed the alignment with samtools" | One long sentence carrying HG002, chromosome 20 slice, reference genome, minimap2, and samtools. I know the biology words but not the two tool names, and the sentence is too long to hold at once. | Split into two sentences and gloss minimap2 and samtools as the aligner and the alignment toolkit. |
| Before you start, "a mapping result, an assembly, a classification, or a download all qualify" | I could not confirm which of my own files qualify without already knowing what LGE calls each of these. There is no way to check a file before trying. | Add a sentence saying the way to check is to try the export and see whether the No Provenance Available alert appears. |
| Before you start, "Running one needs Nextflow or Snakemake on the machine that will run it" | I cannot perform this from the text. Nothing tells me how to get Nextflow or Snakemake, or whether LGE installs them for me. The later section says LGE pins versions, which confused me further. | Say clearly whether LGE provides these tools or whether the recipient installs them separately, and point to the chapter that covers it. |
| Procedure step 1, "or open it so its viewport is showing" | Viewport is not explained in this chapter. I do not know which part of the window is the viewport. | Gloss viewport at first use as the main panel on the right that shows the selected file. |
| Procedure step 3, "`<artifact>-provenance-<format>`" | The angle bracket placeholder style is not explained. On first reading I thought the brackets were typed literally. | Say once that the angle brackets stand for a name LGE fills in and are not typed. |
| Procedure step 3, "Exporting onto a path that already exists and is not a folder is refused" | Path is unexplained, and the double negative shape of this sentence made me read it twice to work out that the problem case is an existing file with the same name. | Say plainly that if a file of that name already exists, LGE refuses and names it. |
| Settings, "Every entry below closes with the flag that does the same job on the command line" | Flag is used here before the optional command line section explains anything. I have never opened a terminal, so I did not know whether I needed this. | Add a half sentence saying a flag is an option typed after a command, and that these can be skipped if you work in the window. |
| Settings, Provenance, "which is the choice you make by picking one of the six submenu items rather than by setting a control" | Long sentence with a contrast built into a relative clause. I read it three times. The point, that there is no control to set, is simple. | Split it into two short sentences. |
| Settings, "--signature" and "--public-key" | These two entries describe signing, signature artifacts, and public keys, none of which are explained anywhere in the chapter. I could not judge whether I need them. | Add one sentence saying a signature is an optional cryptographic seal, that most users have none, and that these two settings can be ignored unless a signer was set up. |
| Settings, Where, "which takes the folder path and the folder name together as one argument" | Argument is unexplained, and I could not see why two settings in the window become one thing on the command line. | Gloss argument, or simply say the command line takes the full location including the name. |
| Reading the results, "the records inside any enclosing `.lungfishref` reference bundle" | Enclosing, reference bundle, and the `.lungfishref` extension are all new here. I could not picture what contains what. | Gloss reference bundle at first use and say plainly that a bundle can hold records for the files inside it. |
| Reading the results, "That transitive record, not just the last command, is what a reproducibility reviewer wants." | Transitive is a mathematics word I did not expect. The sentence is the summary of the paragraph, so missing the word cost me the point. | Replace transitive with the whole chain of steps, not only the last one. |
| The Nextflow export, "turning each dot into an underscore and sanitizing what is left" | Sanitizing is used in a software sense. In biology it means cleaning, which sent me the wrong way. | Say replacing any other character that is not a letter or a number. |
| The Nextflow export, "`MINIMAP2_1` followed by `SAMTOOLS_2` through `SAMTOOLS_5`" | I could not judge this. Five steps is stated as fact but I have no idea whether five is normal, or what would be wrong with a different number. | Say that the count simply matches the number of steps the run recorded, so any number is fine. |
| The Nextflow export, "then a `publishDir` directive pointing at `params.outdir`" | Directive is unexplained, and this sentence stacks five things joined by then. I lost track by the third. | Split the sentence and gloss directive as an instruction inside the file. |
| The Nextflow export, "Step 1's comment reads `minimap2 2.31 (managed conda environment ...)`" | Conda and managed environment are not explained. I could not tell whether this is something on my machine or something LGE made. | Gloss conda environment at first use as a self-contained folder of installed programs that LGE creates and manages. |
| The Nextflow export, "A run whose steps are all byte-copy replays of a retained selection" | I did not understand a single clause of this. Byte-copy, replay, and retained selection are all new, and I do not know how to tell whether my run is one of these. | Say plainly what kind of run this is, for example a run that only copied a saved subset of reads rather than analyzing them. |
| The Nextflow export, "`docker.enabled = true`" and the surrounding text | Docker is never explained, and the container gloss arrives two paragraphs later. I read a config that turns something on without knowing what. | Move the container gloss earlier, or gloss Docker here as the most common container program. |
| The Nextflow export, "so do not pass `-profile`, and reaching a cluster scheduler means adding an executor or a profile block" | Profile, cluster scheduler, executor, and block are four unexplained terms in one sentence, and the instruction is a negative one about something I did not know existed. | Cut this to one sentence saying the file has no cluster settings and a person familiar with Nextflow must add them. |
| The Nextflow export, "Nextflow's restart-from-the-failed-step behaviour needs `nextflow run main.nf -resume`" | I could not perform this. There is no explanation of where such a command is typed, and this is not in the optional terminal section. | Move the command into the optional command line section, or add that it is typed in Terminal. |
| The Nextflow export, "the process in `main.nf` still names its image by the recorded reference rather than by that digest" | Digest and reference are both used as nouns for identifiers I do not know. The contrast between them is the whole point of the sentence and I missed it. | Gloss digest as an exact fingerprint of an image and reference as its human readable name and tag. |
| The Snakemake export, "a usage comment reading `snakemake --cores 8 --use-singularity`" | Cores is a number I cannot judge. I do not know if 8 is a recommendation, a copy of my machine, or a fixed default. Singularity is also unexplained. | Say what the 8 means and whether the recipient should change it, and gloss Singularity as another container program. |
| The Snakemake export, "A collaborator overrides one with `--config`." | Override is used without saying what it does to the run, and this is a command line action dropped into a section for people reading files. | Say plainly that this lets the recipient point a key at a different file on their own machine. |
| The other four exports, "`run.sh` opens with `set -euo pipefail`" | This is presented as if I should recognize it. I cannot tell whether it matters to me or whether it is only for the recipient. | Add a half sentence saying this line makes the script stop at the first failure, and that you do not need to change it. |
| The other four exports, "with that file's SHA-256 in a trailing comment" | SHA-256 appears with no gloss, although checksum was glossed earlier. I did not connect the two until the second reading. | Say the checksum, in SHA-256 form, so the link to the earlier gloss is explicit. |
| The other four exports, "Both files are written with the executable bit set, so `./run.sh` and `./reproduce.py` work without a `chmod` first." | Executable bit, the `./` form, and `chmod` are three terminal concepts in one sentence, and I have never opened a terminal. | Move this into the optional command line section. |
| What good looks like, "The exporter falls back to that string when a version was never recorded" | Falls back to is an idiom. Combined with that string referring back to `unknown`, I had to re-read to see that LGE writes the word unknown itself. | Say LGE writes the word unknown when it never recorded a version. |
| What good looks like, "validate the file before you send it" and the code block | I cannot perform this step. Two commands are given with no statement that they are typed in Terminal, and the terminal section is later and labelled optional. | Add a sentence saying these are typed in the Terminal application, and cross-reference the last section. |
| What good looks like, "In the build tested for this chapter, neither emitted pipeline validates as written" | Emitted is an unusual word for produced, and I could not judge how bad this is. Does it mean the pipeline never runs, or that it needs small edits? | Say plainly that both need edits before they will run, and name the size of the editing job. |
| What good looks like, "`nextflow lint` reported one error and eleven warnings" | I cannot judge eleven warnings. I do not know whether warnings block a run or are advisory. | Say whether the warnings matter, for example that only the single error stops the run. |
| What good looks like, "`CyclicGraphException in rule samtools_5`, because the recorded `samtools flagstat` step lists ... as both an input and an output" | A very long sentence carrying an error name, a tool subcommand, and a graph concept. I understood the biology but lost the sentence twice. | Split into two sentences, the error first and the cause second. |
| What good looks like, "a duplicated `params.hg002_sorted_bam` line appears twice" | Duplicated and twice say the same thing, which made me re-read to check whether it meant four occurrences in total. | Remove one of the two words. |
| What good looks like, "These are defects in LGE's exporter rather than mistakes you made, and they are reported below." | Reported below sent me looking for a bug list further down the chapter. I could not find one. | Say where the defects are reported, or remove the promise. |
| On the command line, "The whole procedure runs headless, meaning with no window at all" | Headless is glossed, which helped, but the section still assumes I know how to open Terminal and where to type. As someone who has never used one, I could not start. | Add one sentence saying how to open the Terminal application and that commands are typed at the prompt and confirmed with Return. |
| On the command line, "lungfish-cli provenance export ./Analyses/mapping-HG002 \" | The backslash at the end of the line is not explained. I did not know whether to type it. | Say once that the backslash continues one command over several lines. |
| On the command line, "The input argument takes a provenance sidecar file, a bundle, or an output directory" | Three accepted inputs given with no way to tell which one I have. I could not choose. | Say that pointing at the run folder covers all three cases, which the next clause almost says already. |
| On the command line, "Two neighbouring subcommands have no menu equivalent at all." | Neighbouring is a figure of speech for commands, and subcommand is unexplained. | Say two related commands, and gloss subcommand as the second word after lungfish-cli. |
| On the command line, "each with authors, title, journal, year, DOI, and project URL" | DOI is not glossed, although this is a chapter aimed partly at people writing papers. | Gloss DOI at first use as the permanent identifier of a published article. |
| On the command line, "LGE signs each generated export artifact, writes a `.signature.json` and a `.pub` beside it" | Signing appears again with no explanation of what it protects against or how a signer is configured. I could not act on this. | Add one sentence on what signing proves, and point to where a signer is set up. |
| On the command line, "LGE pins Nextflow 26.04.6 and Snakemake 9.25.2" | Pins is unexplained, and I cannot judge these numbers. The sentence then warns about a much older Nextflow without saying how old is old. | Gloss pins as fixes to one version, and give a lowest version that still works. |
| On the command line, "the emitted `main.nf` is DSL2" | DSL2 is given with no explanation at all, and the risk it describes depends entirely on knowing what it is. | Gloss DSL2 as the second version of Nextflow's pipeline language, introduced in newer releases. |
| On the command line, "this is a requested environment specification and not a resolved lock" | Requested, resolved, and lock are used as a contrast I do not understand, and the paragraph repeats the point three times without ever explaining the difference. | Explain the difference once, that a requested list names packages while a resolved lock names exact versions and builds. |
| On the command line, "since `--from-lockfile` reconstruction is unsupported" | A flag is named for something that does not work, which confused me about whether I should try it. | Drop the flag name and say LGE cannot rebuild the environment from this file. |
| Next, "the other direction of travel" | An idiom. I read it twice before understanding that the next chapter is about importing rather than exporting. | Say the opposite direction, importing rather than exporting. |
| Next, "through a separate code path from the provenance export this chapter describes" | Code path is developer language. As a reader I only need to know that the results may differ. | Say the Workflow Builder writes these files a different way, so the output may not match. |

One thing I learned. LGE writes a small record beside every file it makes, holding the exact command, the tool version, and a fingerprint of every input and output.

One thing I still could not do. Check an export the way the chapter tells me to, because the validation commands are typed in a terminal I have never opened and the chapter does not say how.

The sentence I liked most. "A checksum is a short fingerprint computed from a file's exact bytes, so two people can confirm they are holding the identical file."
