# Reader 1 report, undergraduate

Chapter: `docs/user-manual/chapters/08-workflows/02-exporting-as-nextflow-or-snakemake.md`

Persona: sophomore, one genetics course, never opened a terminal, never used sequencing software.

| Location | Issue | Suggested fix |
|---|---|---|
| Title, "Exporting as Nextflow or Snakemake" | I did not know what Nextflow or Snakemake are before I started, and the title assumes I do. The first place either word is explained at all is much later, and even then only as "a group that already uses Snakemake". | Add one sentence at the top of "What it is" saying a workflow manager is a program that runs a chain of analysis steps in order on another computer. |
| What it is, "The record it writes is a provenance sidecar" | "Sidecar" is glossed as a JSON file, but "JSON" itself is never explained. I do not know what a JSON file looks like or whether I can open it. | Gloss JSON at first use as a plain-text format that both people and programs can read. |
| What it is, "the exact command line, the tool and its version" | "Command line" is used as if I know it. I have never opened a terminal, so I do not know what a command line is or what one looks like. | Gloss "command line" at first use as the exact line of text a program was started with. |
| What it is, "from 'executable on somebody else's cluster'" | "Cluster" is not explained. I pictured a cluster of cells from genetics class before I realized it must mean computers. | Gloss cluster at first use as a shared set of computers a lab submits big jobs to. |
| What it is, table row "Shell Script... `run.sh`, `provenance/`" | I cannot tell what a shell script is, and the trailing slash on `provenance/` was not explained as meaning a folder. I had to guess. | Say in the lead-in that a name ending in a slash is a folder and the others are files. |
| What it is, table row "A subprocess driver where a Python entry point" | I read this three times and still cannot say what it means. "Subprocess driver" and "entry point" are both unexplained, and the row does not tell me when I would want this over the shell one. | Replace with a plain statement, such as that this one is for people who already write Python. |
| What it is, "The submenu draws a separator after the fourth item" | I do not know what a separator is in a menu, and I could not tell why the split matters to me until the next clause. Also I could not check this, because there is no screenshot yet, only a placeholder. | Say the menu shows a thin dividing line, and land the screenshot. |
| What it is, "LGE walks the provenance chain backwards from it" | "Provenance chain" appears here for the first time without a gloss. I know provenance is the record, but not what makes a chain of them. | Gloss the chain as the series of earlier files each output was made from. |
| Why you would do this, "which minimap2 build produced a BAM file" | Three unexplained things in one clause. I do not know what minimap2 is, what a "build" of it is, or what a BAM file is. | Gloss minimap2 as a read alignment program and BAM as the file that holds aligned reads. |
| Why you would do this, "They are not, in the build tested for this chapter, pipelines that execute unmodified" | I had to read this twice because of the clause wedged in the middle, and "the build tested for this chapter" is vague. Which version is that, and how would I know if mine is different? | Name the version number that was tested. |
| Before you start, "its `Analyses/mapping-HG002` run, which mapped human reads" | I do not know what HG002 is or why chromosome 20 was sliced. I also cannot find this run, because the chapter never says how to open the demo project. | Say in one sentence what HG002 is and where the demo project comes from. |
| Before you start, "and then processed the alignment with samtools" | "Samtools" and "alignment" are both unglossed. Later the chapter says four samtools steps ran, and I have no idea what any of them did. | Gloss samtools at first use as the standard toolkit for handling alignment files. |
| Before you start, "a **No Provenance Available** alert instead of a save panel" | I could not tell from this whether that alert is an error I caused or normal. It says what happens but not what to do next. | Add what the reader should do when that alert appears. |
| Procedure step 1, "or open it so its viewport is showing" | "Viewport" is used without a gloss in this chapter. I am not sure whether the viewport is the big panel on the right or something else. | Gloss viewport at first use, or point to the chapter that does. |
| Procedure step 3, "in the form `<artifact>-provenance-<format>`" | The angle brackets are never explained. I first thought I had to type the brackets. | Say the angle brackets stand for names LGE fills in. |
| Procedure step 3, "choose a location outside the project" | I do not know where "outside the project" is on my own computer, and no example location is given. Desktop? Documents? | Name one concrete safe location, such as the Desktop. |
| Procedure step 3, "Exporting onto a path that already exists and is not a folder is refused" | Read twice. I could not picture the situation the sentence is guarding against. | Give the one case plainly, such as when a file of that name is already there. |
| Settings, "**Provenance.** Chooses what the export renders" | The setting is named "Provenance" but the entry says it is not a control at all, it is the menu item I picked. I read this three times trying to find a control on screen. | Say up front that this setting has no control and is set by the menu choice. |
| Settings, "the raw JSON for an auditor" | "Auditor" appears twice in the chapter with no explanation of who that is in a lab. I do not know if I will ever meet one. | Say who this means, such as a journal reviewer checking the analysis. |
| Settings, "**Where.** ... On the command line this is `--output`, which takes the folder path and the folder name together" | Both "Save As" and "Where" say they map to `--output`, and I could not work out how one flag does two things until this trailing clause. | Merge the two mappings into one statement so the reader is not surprised twice. |
| Settings, "**--signature.** Names the signature artifact that `provenance verify` should check" | I do not know what a signature over a file is, what signing means here, or why I would want one. The two flags are given without ever saying what the feature is for. | Add one sentence saying a signature proves the record was not altered after it was written. |
| Settings, "The default is `<sidecar>.pub`" | "Public key" is unglossed, and I cannot judge whether receiving a key "separately" is something I need to care about. | Gloss public key in one clause, or say most readers can skip both flags. |
| Reading the results, "including the records inside any enclosing `.lungfishref` reference bundle" | I have no idea what a `.lungfishref` reference bundle is, and this is its only mention. | Gloss it, or drop it from this chapter. |
| Reading the results, "That transitive record, not just the last command" | "Transitive" stopped me cold. I know the word from math class but not what a transitive record is. | Say "the whole chain of earlier steps" instead. |
| The Nextflow export, "by turning each dot into an underscore and sanitizing what is left" | "Sanitizing" is not explained, so I cannot predict what a parameter name will be from a filename. | Say what else is changed, or drop the word. |
| The Nextflow export, code block `params.hg002_chr20_10_0_10_5mb_r1_fastq_gz = ...` | I do not read Groovy and the block is not introduced as code I only need to skim. I spent a while trying to decide if I had to type this. | Say before the block that this is generated output the reader only reads. |
| The Nextflow export, "so `main.nf` holds `MINIMAP2_1` followed by `SAMTOOLS_2` through `SAMTOOLS_5`" | Five steps are counted but never named, so I cannot tell what the four samtools steps did or judge whether five is a normal number. | Name the four samtools operations in one line. |
| The Nextflow export, "a `publishDir` directive pointing at `params.outdir`" | Three Nextflow-specific terms in one clause, none glossed. I could not follow the sentence. | Say plainly that each process states where its results are written. |
| The Nextflow export, "`minimap2 2.31 (managed conda environment minimap2; ... root /Users/dho/.lungfish/conda)`" | "Conda environment" is not explained, and this quoted string is somebody's personal folder path. I could not tell whether mine would look the same. | Gloss conda environment, and say the path will show the reader's own user name. |
| The Nextflow export, "A run whose steps are all byte-copy replays of a retained selection" | I could not parse this at all. "Byte-copy replay" and "retained selection" are both new and neither is defined. | Say plainly what kind of run this is, such as one that only copied files rather than analyzing them. |
| The Nextflow export, "`errorStrategy = 'terminate'` ... needs `nextflow run main.nf -resume`" | I do not know what a restart-from-the-failed-step behaviour is or who types this command. This paragraph seems written for somebody who already runs Nextflow. | Mark this paragraph as being for the recipient rather than the reader. |
| The Nextflow export, "the tool name, version, image, and image digest" | "Image digest" is not glossed even though container is. I cannot judge what a digest tells an auditor. | Gloss digest as a fingerprint of the exact container version. |
| The Nextflow export, "Pointing a run at a local image tarball means editing the config yourself" | "Tarball" is unexplained, and I do not know what editing the config would involve. | Cut this sentence or gloss tarball. |
| The Snakemake export, "a usage comment reading `snakemake --cores 8 --use-singularity`" | I cannot judge the number 8. Is that a requirement, a guess, my computer's core count? "Singularity" is also unglossed. | Say what the 8 is and that it should match the recipient's machine. |
| The Snakemake export, "A collaborator overrides one with `--config`" | Read twice. The sentence names no object, so I could not tell what is being overridden. | Say what gets overridden, such as an input file path. |
| The other four exports, "`run.sh` opens with `set -euo pipefail`" | This means nothing to me and the chapter does not say whether I need to understand it. | Say it makes the script stop on the first error, or drop it. |
| The other four exports, "that file's SHA-256 in a trailing comment" | "SHA-256" appears here for the first time. Checksum was glossed at the top, but I did not connect the two names. | Say SHA-256 is the kind of checksum used. |
| The other four exports, "Both files are written with the executable bit set, so `./run.sh` ... work without a `chmod` first" | Executable bit, `./`, and `chmod` are three terminal ideas in one sentence, and I have never opened a terminal. | Move this to the command line section at the end. |
| What good looks like, "confirm none of them reads `unknown`" | I do not know how many versions to expect, so I cannot tell whether I have checked all of them. The chapter says the HG002 run has none, but not how many entries I should be scanning. | Say where the version list lives in each target so the reader knows what to scan. |
| What good looks like, "`nextflow lint main.nf`" and "`snakemake --dry-run --cores 1`" | The chapter tells me to run these but I have never opened a terminal, and this is before the section that says the command line is optional. I do not know how to run them or whether I can skip them. | Say who runs these and point forward to the command line section. |
| What good looks like, "reported one error and eleven warnings" | I cannot judge these numbers. Is eleven warnings a lot? Do the warnings matter, or only the error? | Say whether the warnings matter to the reader. |
| What good looks like, "`CyclicGraphException in rule samtools_5`" | Read twice. I followed the explanation about a rule depending on itself, but not why listing a file as both input and output is wrong when the tool really did read and write it. | Add half a sentence on why that is a problem for Snakemake but not for LGE. |
| What good looks like, "These are defects in LGE's exporter ... and they are reported below" | Nothing below reports them that I could find. I looked for a bug list and there is none. | Point to where the defects are actually filed, or drop the promise. |
| On the command line, "The whole procedure runs headless, meaning with no window at all, by typing commands into the Terminal application" | This is the only help I get, and it is not enough to actually open Terminal or know what directory I would be in. The commands use `./Analyses/...`, which assumes I know where I am. | Point to the chapter that introduces the terminal, or say the reader can skip this section entirely. |
| On the command line, "each with authors, title, journal, year, DOI, and project URL" | "DOI" is not glossed. I have seen it on papers but could not say what it is. | Gloss DOI at first use. |
| On the command line, "`verify` reports `Signature artifact is missing`" | I could not judge whether this is a failure. The chapter says it is not, but the message reads like an error and I would have worried. | Keep, but say the reader will see this by default. |
| On the command line, "LGE pins Nextflow 26.04.6 and Snakemake 9.25.2" | I cannot judge these version numbers. I do not know what counts as "much older" for a collaborator. | Say roughly how old is too old. |
| On the command line, "this is a requested environment specification and not a resolved lock" | "Requested specification" versus "resolved lock" is the crux of the warning and neither term is explained. I read the paragraph twice and still am not confident I understand the risk. | Gloss the difference in one sentence, such as the list of what was asked for versus exactly what got installed. |
| On the command line, "`--from-lockfile` reconstruction is unsupported" | A flag is named that appears nowhere else in the chapter, so I do not know what it would have done. | Drop the flag name and say the file cannot be used to rebuild the environment. |
| Next, "through a separate code path from the provenance export" | "Code path" is developer language, and I could not tell what the practical difference is for me. | Say the two exports produce different files, if that is what it means. |

One thing I learned. A provenance sidecar records the exact command, the tool version, and file fingerprints, so an analysis can be described without relying on memory.

One thing I still could not do. Judge whether an export is usable, because checking it means running `nextflow lint` in a terminal I have never opened.

The sentence I liked most. "And six months from now you will not remember which minimap2 build produced a BAM file, but the export will."
