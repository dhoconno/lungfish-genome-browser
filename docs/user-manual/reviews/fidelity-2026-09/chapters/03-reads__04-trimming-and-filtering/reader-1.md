# Reader 1 report: Trimming and Filtering Reads

Persona: sophomore, one genetics course, no lab time, never opened a terminal.

| Location | What stopped me | What would have helped |
|---|---|---|
| What it is / "Trimming means cutting bases off" | "Reads" is used as a noun from the first line and never told me what a read actually is. I inferred it is one stretch of sequence the machine output, but I was guessing. | One clause saying a read is one stretch of sequence the sequencer produced from one fragment. |
| What it is / "The second is adapter sequence, meaning" | I do not know what "library preparation" is. It is used as if I have done one at a bench. | A four-word gloss on library preparation as the bench steps that turn DNA into machine-ready fragments. |
| What it is / "When a fragment is shorter than" | I could not picture this. Why does reading through a fragment land you in the adapter on the far side? | A clause saying adapter is attached at both ends, so running off the real DNA lands you in the other one. |
| What it is / "The third is primer sequence, present" | Amplicon is linked but not glossed inline, and the other three kinds of baggage all got an inline gloss. I had to guess it means a PCR product. | The same one-clause inline gloss the other three received. |
| What it is / "Six operations sit under Tools" | I learned that six exist but never saw the six names as a list, so later I could not tell which pane belonged to which. | One line naming the six operations once. |
| What it is / "A fourth program, cutadapt, takes over" | I did not learn what cutadapt does differently, only that it takes over. It reads like a footnote but turns out to matter later. | Half a sentence saying what cutadapt is for. |
| What it is / "Trim only what quality control told" | I have not run QC yet, so I do not know what QC "told me was there" looks like or where to read it. | Name the one QC item that tells you adapter is present. |
| Why you would do this / "An untrimmed low-quality tail produces" | "Variant caller" appears with no gloss and no link, and I only half know what one is. | A gloss like "a program that lists where your sample differs from the reference". |
| Why you would do this / "This chapter works through the" | I did not know HG002 is a benchmark sample, and "shotgun" is used for this same data much later without ever being introduced. | One clause saying HG002 is a reference human sample whose true sequence is known. |
| Before you start / "Download the files HG002.chr20.10.0-10.5Mb_R1.fastq.gz" | Nothing says why there are two files or that R1 and R2 are the two ends of one fragment. I nearly downloaded only the first. | A clause saying R1 and R2 are the two ends of each fragment and both are needed. |
| Before you start / "These operations use tools from" | Required Setup pack, optional pack, and Docker Desktop all arrive in one sentence and I know none of the three. | A pointer to where packs are explained, or drop the two I do not need. |
| Procedure / "Every number quoted below came from" | "The fixture's R1 file" reads as jargon. I did not connect "fixture" to the files I had just downloaded. | Say "the R1 file you downloaded". |
| The combined adapter and quality trim / step 3 "Leave Adapter Mode on Auto-Detect" | Four settings are named at once with no meaning attached, and their explanations are pages below. I could perform the click but not judge it. | A note in the step that these are explained in Settings below. |
| The combined adapter and quality trim / "Before you click, glance at the" | This paragraph sits after step 5 has already told me to click Run, so I had clicked before being told to look first. | Move the readiness-line advice above the Run step. |
| The combined adapter and quality trim / "in a folder named for the tool" | I could not predict what "the tool" resolves to. Would the folder say fastp, or trim, or something else? I could not find my output confidently. | Show the real folder name from the worked run. |
| The length filter / step 1 "Click the trimmed bundle in the" | I do not know what the trimmed bundle is called in the sidebar. The previous section only gave a path on disk. | Give the name the new bundle shows in the sidebar. |
| The length filter / step 3 "Set Min Length to 50" | I could not tell where 50 came from. Its justification only appears much later, in Reading the results. | One clause saying reads shorter than 50 map ambiguously. |
| Trimming a fixed number of bases / "such as a UMI, which is" | I read this three times. The operation, the condition, the example, and a long definition are all stacked into one sentence. | Split the UMI definition into its own sentence. |
| Primer trimming at the read level / "Primer schemes in LGE are viral" | I could not tell whether viral-only is a limitation of the app or a fact about biology, and it made me wonder if the feature works on my data at all. | Say the bundled schemes are viral, not that the feature is. |
| Primer trimming at the read level / "instead uses each read's mapped position" | This compares FASTQ and BAM, but I have not mapped anything yet and BAM is never defined in this chapter. | A gloss on BAM as the file of reads after mapping. |
| Quality trimming and adapter removal on their own / "it carries an Extra arguments field" | I could not tell why only this pane has that field. It reads like an accident rather than a design choice. | One clause on why the combined pane omits it. |
| FASTA input / "The quality-based operations have nothing to" | I could not tell whether those operations are hidden, greyed out, or simply fail when I click Run. | Say what the pane actually does with them on FASTA input. |
| Settings / fastp Adapter + Quality Trim / Threshold "The default is 20, which means" | I follow one-in-a-hundred, but I cannot judge 20 against 30 without knowing the scale is logarithmic. That is never said. | State that each ten points means ten times fewer expected errors. |
| Settings / fastp Adapter + Quality Trim / Mode "offering Cut Right, Cut Front, Cut Tail" | Four options are listed and only two are explained. Also "Cut Right" walking from the start of the read confused me for a while. | One clause each for Cut Front and Cut Tail. |
| Settings / fastp Adapter + Quality Trim / Adapter Sequence "and the IUPAC ambiguity codes that" | I have not met IUPAC codes. The gloss says what they do but not that they are letters. | Name one example code. |
| Settings / Output Strategy "Chooses whether each selected dataset is" | This identical paragraph appears six times. By the third I stopped reading it, so I may have missed a real difference somewhere. | Say once that it behaves identically in all six panes. |
| Settings / Primer Trimming / Primer Source "along with .lungfishref" | I have no idea what a .lungfishref file is or where I would get one. | A gloss or a cross-reference. |
| Settings / Primer Trimming / k "The default is 15, which is" | I could not check the claim that a fifteen-base match is unlikely by chance, and I did not know whether that depends on how big the genome is. | Anchor the claim to the number of possible fifteen-base words, or drop it. |
| Settings / Primer Trimming / hdist "which is the Hamming distance between" | I did not know Hamming distance, and the gloss just restates the sentence before it. | Say it counts the positions where the two strings differ. |
| Settings / Filter by Read Length / Min Length "when short fragments are only adapter dimer" | "Adapter dimer" is new here and never explained anywhere in the chapter. | A short gloss on adapter dimer. |
| Reading the results / "Its top pane holds the nine" | Nine cards and three charts are counted but not named, and I have to find specific ones two sections later. | Name the cards this chapter actually uses. |
| Reading the results / table row "Shortest read 50 bp 1 bp" | A one-base read looked like a bug to me. Even after the paragraph below explains it I do not know whether that is normal or alarming. | Say plainly that a one-base read is expected and that the filter removes it. |
| Reading the results / "so the trim cost seven percent" | The line above gives 93.0 percent, so the cost is 7.0 percent. Saying "seven percent" right after two-decimal figures made me recheck the arithmetic. | Match the precision to the numbers above it. |
| Reading the results / "Six hundred and seventy-seven reads came" | Every other count in the chapter is in digits. Spelling this one out made me stop and convert it, and I could not quickly reconcile it against the 44,857 kept figure. | Use digits and show the subtraction. |
| Reading the results / "which is 98.51 percent of what" | Two percentages against two different denominators in one sentence. If someone asked me how good the filter was I would not know which to quote. | Give one number and say which denominator it uses. |
| Reading the results / "kept 44,916 reads but only 8,947,205" | The word "but" led me to expect more reads and fewer bases. Both actually fell. I read the sentence three times. | State the direction of both changes explicitly. |
| Reading the results / "read the durable record in the" | I do not know how to open a folder inside a bundle, and I have never opened a terminal. | Say whether a menu item reveals that folder. |
| What good looks like / "The Q / Position sparkline, which" | I do not know what a sparkline is, and reading it is one of the four checks I am told to perform. | A four-word gloss on sparkline. |
| What good looks like / "expect 90 to 99 percent of" | The next clause calls 99.91 percent "the healthy end of that", but 99.91 sits outside the 90 to 99 range as stated. I could not tell which half is wrong. | Fix the range or the wording. |
| What good looks like / "Confirm the base survival separately from" | I could not turn this into an action. Nothing says where to read the input bundle's base count once the viewport has switched to the output. | Say which card holds the base count and that you select each bundle in turn. |
| When a trim goes wrong / "Re-run at a Threshold of" | One in 32 does not follow from anything I was told. If 20 is one in 100, I have no rule that gets me to 32 at 15. | Give the formula once, or drop the derived figure. |
| When a trim goes wrong / "Long-read data sits far lower" | Long reads appear here and twice in Settings, but the chapter never says these operations accept long reads or what such data looks like. | Say once, early, that these operations also accept long reads. |
| When a trim goes wrong / "Drop Window Size from 4 to" | The Settings entry says use a smaller window for a sharp quality cliff, while this says use 1 for isolated bad bases. I cannot tell which case I am in from the chart. | Say what the chart looks like in each case. |
| On the command line / "This section is optional. Everything above" | It says optional, then assumes I can find and run a program. I have never opened a terminal and did not know whether to try. | Say plainly that a reader who has not used a terminal can skip the whole section. |
| On the command line / "--literal GCTGGGATTACAGGCATGAGCCACC" | The chapter already said this dataset has no primers, so I could not tell what this command would do or whether running it was safe. | Note that the example shows shape only and trims nothing on this data. |
| On the command line / "--error-rate, which defaults to 0.12" | I do not know what 0.12 is a rate of. Twelve percent of the primer, of the read, or of something else? | Say it is the fraction of the primer allowed to mismatch. |

The one thing I learned: a healthy trim shows up as almost every read surviving while the total base count drops by several percent, and the gap between those two survival figures is the thing to look at.

The one thing I still could not do: judge whether a trim on my own data had been too harsh, because the survival benchmark in "What good looks like" and the worked example's own number contradict each other.

The sentence I liked most: "The input bundle is never modified, so a trim you regret costs you disk space and nothing else."
