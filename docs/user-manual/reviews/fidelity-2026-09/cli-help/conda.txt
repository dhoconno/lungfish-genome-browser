==== conda ====
OVERVIEW: Manage bioconda tool plugins via micromamba

Install bioinformatics tools from bioconda and conda-forge using micromamba.
Each tool is installed in its own isolated environment to prevent dependency
conflicts. Tools are stored in /Users/dho/.lungfish/conda.

Use 'lungfish conda lock --pack <pack> --output lockfile.yml' to write
a Lungfish requested environment specification (JSON). This is not a
resolved artifact lock; exact --from-lockfile reconstruction is unsupported.

USAGE: lungfish-cli conda <subcommand>

OPTIONS:
  --version               Show the version.
  -h, --help              Show help information.

SUBCOMMANDS:
  install                 Install a tool or plugin pack from bioconda
  remove                  Remove a conda environment and its tools
  list                    List installed packages in an environment
  search                  Search for packages in bioconda and conda-forge
  run                     Run a tool from a conda environment
  envs                    List conda environments
  setup                   Download and set up micromamba
  lock                    Export a requested environment specification for a
                          plugin pack (not a resolved lock)
  packs                   List available plugin packs
  export-pack             Export installed conda environments for offline
                          transfer
  offline-export          Export installed conda environments for offline
                          transfer
  offline-install         Install conda environments from an offline pack
  classify                Run Kraken2 taxonomic classification on FASTQ or
                          FASTA inputs
  db                      Manage metagenomics reference databases
  extract                 Extract reads by taxonomy from Kraken2 classification
                          output

  See 'lungfish-cli help conda <subcommand>' for detailed help.

==== conda install ====
OVERVIEW: Install a tool or plugin pack from bioconda

USAGE: lungfish-cli conda install [<options>] [<packages> ...]

ARGUMENTS:
  <packages>              Package name(s) to install (e.g., 'samtools'
                          'bwa-mem2')

OPTIONS:
  -e, --env <env>         Environment name (default: package name)
  --pack                  Install a plugin pack instead of individual packages
  --offline               Install environments from an offline conda pack bundle
  --from-bundle <from-bundle>
                          Offline conda pack directory, .tar, .tgz, or .tar.gz
                          archive
  --from-lockfile <from-lockfile>
                          Unsupported exact reconstruction input; requested
                          specifications cannot be installed as resolved locks
  --conda-root <conda-root>
                          Conda root to install into when using --offline or
                          --from-lockfile (default: managed storage conda root)
  --overwrite             Replace existing environments when using --offline
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.


==== conda remove ====
OVERVIEW: Remove a conda environment and its tools

USAGE: lungfish-cli conda remove <environments> ... [--format <format>] [--verbose ...] [--quiet] [--progress] [--no-progress] [--debug] [--log-file <log-file>] [--no-color] [--threads <threads>]

ARGUMENTS:
  <environments>          Environment name(s) to remove

OPTIONS:
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.


==== conda list ====
OVERVIEW: List installed packages in an environment

USAGE: lungfish-cli conda list [--env <env>] [--format <format>] [--verbose ...] [--quiet] [--progress] [--no-progress] [--debug] [--log-file <log-file>] [--no-color] [--threads <threads>]

OPTIONS:
  -e, --env <env>         Environment name (lists all envs if omitted)
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.


==== conda search ====
OVERVIEW: Search for packages in bioconda and conda-forge

USAGE: lungfish-cli conda search <query> [--format <format>] [--verbose ...] [--quiet] [--progress] [--no-progress] [--debug] [--log-file <log-file>] [--no-color] [--threads <threads>]

ARGUMENTS:
  <query>                 Search query

OPTIONS:
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.


==== conda run ====
OVERVIEW: Run a tool from a conda environment

USAGE: lungfish-cli conda run [--env <env>] <tool-and-args> ... [--format <format>] [--verbose ...] [--quiet] [--progress] [--no-progress] [--debug] [--log-file <log-file>] [--no-color] [--threads <threads>]

ARGUMENTS:
  <tool-and-args>         Tool name followed by its arguments

OPTIONS:
  -e, --env <env>         Environment name (default: tool name)
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.


==== conda envs ====
OVERVIEW: List conda environments

USAGE: lungfish-cli conda envs [--format <format>] [--verbose ...] [--quiet] [--progress] [--no-progress] [--debug] [--log-file <log-file>] [--no-color] [--threads <threads>]

OPTIONS:
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.


==== conda setup ====
OVERVIEW: Download and set up micromamba

USAGE: lungfish-cli conda setup [--format <format>] [--verbose ...] [--quiet] [--progress] [--no-progress] [--debug] [--log-file <log-file>] [--no-color] [--threads <threads>]

OPTIONS:
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.


==== conda lock ====
OVERVIEW: Export a requested environment specification for a plugin pack (not a
resolved lock)

USAGE: lungfish-cli conda lock --pack <pack> --output <output> [--format <format>] [--verbose ...] [--quiet] [--progress] [--no-progress] [--debug] [--log-file <log-file>] [--no-color] [--threads <threads>]

OPTIONS:
  --pack <pack>           Built-in tool pack ID to export
  -o, --output <output>   Requested specification JSON output path
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.


==== conda packs ====
OVERVIEW: List available plugin packs

USAGE: lungfish-cli conda packs [--format <format>] [--verbose ...] [--quiet] [--progress] [--no-progress] [--debug] [--log-file <log-file>] [--no-color] [--threads <threads>]

OPTIONS:
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.


==== conda export-pack ====
OVERVIEW: Export installed conda environments for offline transfer

USAGE: lungfish-cli conda export-pack --pack <pack> --output <output> [--conda-root <conda-root>] [--format <format>] [--verbose ...] [--quiet] [--progress] [--no-progress] [--debug] [--log-file <log-file>] [--no-color] [--threads <threads>]

OPTIONS:
  --pack <pack>           Built-in tool pack ID to export
  -o, --output <output>   Offline pack output directory, .tar, .tgz, or .tar.gz
                          archive
  --conda-root <conda-root>
                          Conda root to export from (default: managed storage
                          conda root)
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.


==== conda offline-export ====
OVERVIEW: Export installed conda environments for offline transfer

USAGE: lungfish-cli conda offline-export --pack <pack> --output <output> [--conda-root <conda-root>] [--format <format>] [--verbose ...] [--quiet] [--progress] [--no-progress] [--debug] [--log-file <log-file>] [--no-color] [--threads <threads>]

OPTIONS:
  --pack <pack>           Built-in tool pack ID to export
  -o, --output <output>   Directory where the offline pack directory will be
                          written
  --conda-root <conda-root>
                          Conda root to export from (default: managed storage
                          conda root)
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.


==== conda offline-install ====
OVERVIEW: Install conda environments from an offline pack

USAGE: lungfish-cli conda offline-install <pack-directory> [--conda-root <conda-root>] [--overwrite] [--format <format>] [--verbose ...] [--quiet] [--progress] [--no-progress] [--debug] [--log-file <log-file>] [--no-color] [--threads <threads>]

ARGUMENTS:
  <pack-directory>        Path to an offline pack directory created by 'conda
                          offline-export'

OPTIONS:
  --conda-root <conda-root>
                          Conda root to install into (default: managed storage
                          conda root)
  --overwrite             Replace existing environments with matching names
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.


==== conda classify ====
OVERVIEW: Run Kraken2 taxonomic classification on FASTQ or FASTA inputs

Classify metagenomic reads or assembled sequences using Kraken2 with an
installed database.
Databases are managed via `lungfish conda db` or downloaded from the
built-in catalog. Results include a kreport file, per-read output,
and an optional Bracken abundance profile.

USAGE: lungfish-cli conda classify [<options>] <fastq-files> ... --db <db>

ARGUMENTS:
  <fastq-files>           Input sequence file(s). Provide two files for
                          paired-end FASTQ.

OPTIONS:
  --db <db>               Database name (e.g., 'Viral', 'Standard-8')
  --preset <preset>       Sensitivity preset: sensitive, balanced, precise
                          (default: balanced) (values: sensitive, balanced,
                          precise; default: balanced)
  -o, --output-dir <output-dir>
                          Output directory (default: current directory)
  --paired                Input files are paired-end reads
  --recursive             When an input is a directory, include eligible
                          FASTQ/FASTA files in subfolders
  --profile               Run Bracken abundance profiling after classification
  --confidence <confidence>
                          Override confidence threshold (0.0-1.0)
  --min-hit-groups <min-hit-groups>
                          Override minimum hit groups
  --memory-mapping        Use memory-mapped I/O (slower, less RAM)
  --quick                 Use Kraken2 quick mode
  --bracken-read-length <bracken-read-length>
                          Read length for Bracken -r flag (default: 150)
                          (default: 150)
  --bracken-level <bracken-level>
                          Bracken taxonomic level: D,P,C,O,F,G,S (default:
                          automatic for the selected database)
  --bracken-threshold <bracken-threshold>
                          Bracken minimum read threshold (default: 10)
                          (default: 10)
  --extra-args <extra-args>
                          Additional kraken2 arguments passed verbatim
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.


==== conda db ====
OVERVIEW: Manage metagenomics reference databases

Manage Kraken2 databases for taxonomic classification. Databases may
be downloaded or prepared from managed upstream sources and are stored
in Lungfish's managed database storage.

USAGE: lungfish-cli conda db <subcommand>

OPTIONS:
  --version               Show the version.
  -h, --help              Show help information.

SUBCOMMANDS:
  list                    List available and installed databases
  info                    Show installed database version and update status
  download                Download or prepare a database from the catalog
  remove                  Remove a database from the registry
  recommend               Show recommended database for this system
  update                  Update an installed database to the pinned version
  install-managed         Install a managed user-data database by identifier

  See 'lungfish-cli help conda db <subcommand>' for detailed help.

==== conda db list ====
OVERVIEW: List available and installed databases

USAGE: lungfish-cli conda db list [--format <format>] [--verbose ...] [--quiet] [--progress] [--no-progress] [--debug] [--log-file <log-file>] [--no-color] [--threads <threads>]

OPTIONS:
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.


==== conda db info ====
OVERVIEW: Show installed database version and update status

USAGE: lungfish-cli conda db info <name> [--format <format>] [--verbose ...] [--quiet] [--progress] [--no-progress] [--debug] [--log-file <log-file>] [--no-color] [--threads <threads>]

ARGUMENTS:
  <name>                  Database name (e.g., 'Viral', 'Standard-8', 'PlusPF')

OPTIONS:
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.


==== conda db download ====
OVERVIEW: Download or prepare a database from the catalog

USAGE: lungfish-cli conda db download <name> [--format <format>] [--verbose ...] [--quiet] [--progress] [--no-progress] [--debug] [--log-file <log-file>] [--no-color] [--threads <threads>]

ARGUMENTS:
  <name>                  Database name (e.g., 'Viral', 'SILVA', 'Greengenes')

OPTIONS:
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.


==== conda db remove ====
OVERVIEW: Remove a database from the registry

USAGE: lungfish-cli conda db remove <name> [--delete-files] [--format <format>] [--verbose ...] [--quiet] [--progress] [--no-progress] [--debug] [--log-file <log-file>] [--no-color] [--threads <threads>]

ARGUMENTS:
  <name>                  Database name to remove

OPTIONS:
  --delete-files          Also delete database files from disk
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.


==== conda db recommend ====
OVERVIEW: Show recommended database for this system

USAGE: lungfish-cli conda db recommend [--format <format>] [--verbose ...] [--quiet] [--progress] [--no-progress] [--debug] [--log-file <log-file>] [--no-color] [--threads <threads>]

OPTIONS:
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.


==== conda db update ====
OVERVIEW: Update an installed database to the pinned version

Replaces an installed database with the version pinned in the dependency
manifest.

Name one database by its catalog identifier ('kraken2-viral') or by the display
name it is listed under ('Viral'), or pass --all to update every installed
database that has an update available. Either way --yes is required, because
the update replaces the installed copy.

Databases that are built locally rather than downloaded cannot be updated in
place; they are reported as skipped and are refreshed by reinstalling instead.
A run in which every selected database was skipped updated nothing, so it
exits non-zero rather than reporting success.

Exit codes:
  0   nothing to update, or at least one selected database updated
  2   usage error, such as a missing target or --yes
  1   a database failed to update, --all found updates it could not apply, or
      every selected database was skipped

Examples:
  lungfish conda db update kraken2-viral --yes
  lungfish conda db update Viral --yes
  lungfish conda db update --all --yes

USAGE: lungfish-cli conda db update [<catalog-id>] [--all] [--yes] [--format <format>] [--verbose ...] [--quiet] [--progress] [--no-progress] [--debug] [--log-file <log-file>] [--no-color] [--threads <threads>]

ARGUMENTS:
  <catalog-id>            Catalog identifier or display name of the database to
                          update (e.g., 'kraken2-viral', 'Viral')

OPTIONS:
  --all                   Update every installed database with an update
                          available
  --yes                   Confirm the update (required)
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.


==== conda db install-managed ====
OVERVIEW: Install a managed user-data database by identifier

Installs one of the managed user-data databases declared in the
dependency manifest's managedData section, such as the Deacon
panhuman host-depletion index.

Already-installed databases are reported and left alone unless
--reinstall is given. Use --list to print the known identifiers
without installing anything.

Exit codes:
  0   the database is installed (or was already)
  2   usage error, such as an unknown identifier
  1   the installation failed

Examples:
  lungfish conda db install-managed --list
  lungfish conda db install-managed deacon-panhuman

USAGE: lungfish-cli conda db install-managed [<database-id>] [--list] [--reinstall] [--format <format>] [--verbose ...] [--quiet] [--progress] [--no-progress] [--debug] [--log-file <log-file>] [--no-color] [--threads <threads>]

ARGUMENTS:
  <database-id>           Managed database identifier (e.g., 'deacon-panhuman')

OPTIONS:
  --list                  List the known managed database identifiers and exit
  --reinstall             Reinstall even if the database is already present
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.


==== conda extract ====
OVERVIEW: Extract reads by taxonomy from Kraken2 classification output

Filter FASTQ reads that were classified to specific taxa by Kraken2.
Requires the per-read output file (--output from kraken2) and the
source FASTQ(s). When --include-children is set, a kreport file is
needed to build the taxonomy tree for descendant lookup.

USAGE: lungfish-cli conda extract [<options>] --kraken-output <kraken-output> --source <source> ... --output <output> ... --taxid <taxid> ...

OPTIONS:
  --kraken-output <kraken-output>
                          Kraken2 per-read output file (.kraken)
  --source <source>       Source FASTQ file(s). Repeat for paired-end.
  --output <output>       Output FASTQ file(s). Must match source count.
  --taxid <taxid>         Taxonomy ID(s) to extract (comma-separated or
                          repeated)
  --include-children      Include reads classified to descendant taxa
  --kreport <kreport>     Kreport file for taxonomy tree (required with
                          --include-children)
  --no-read-pairs         Extract only exact read IDs (don't pair /1 and /2
                          mates)
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.

