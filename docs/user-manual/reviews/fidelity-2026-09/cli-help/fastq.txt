==== fastq ====
OVERVIEW: FASTQ read processing and quality control

Process FASTQ files using bundled bioinformatics tools (seqkit, fastp,
bbtools). All tools are embedded — no external installations required.

Operations include subsetting, quality/adapter trimming, contaminant
and low-complexity filtering, error correction, primer removal, and
paired-end utilities.

Examples:
  lungfish fastq subsample --proportion 0.1 reads.fastq -o subset.fastq
  lungfish fastq quality-trim --threshold 20 reads.fastq -o trimmed.fastq
  lungfish fastq contaminant-filter --mode phix reads.fastq -o clean.fastq
  lungfish fastq entropy-filter --entropy 0.6 reads.fastq -o complex.fastq
  lungfish fastq error-correct reads.fastq -o corrected.fastq

USAGE: lungfish-cli fastq <subcommand>

OPTIONS:
  --version               Show the version.
  -h, --help              Show help information.

SUBCOMMANDS:
  subsample               Subsample reads by proportion or count
  length-filter           Filter reads by length
  trim                    Trim adapters and low-quality bases in one fastp pass
  quality-trim            Trim low-quality bases using fastp
  adapter-trim            Remove adapter sequences using fastp
  fixed-trim              Trim fixed number of bases from read ends
  contaminant-filter      Remove contaminant reads using bbduk
  entropy-filter          Remove low-complexity reads using the bbduk entropy
                          filter
  primer-remove           Remove primer sequences from FASTQ reads
  error-correct           Correct sequencing errors using tadpole
  merge                   Merge overlapping paired-end reads using bbmerge
  repair                  Repair desynchronized paired-end reads using repair.sh
  deinterleave            Split interleaved FASTQ into separate R1/R2 files
  interleave              Interleave separate R1/R2 files into one FASTQ
  deduplicate             Remove duplicate reads using clumpify.sh (BBTools)
  demultiplex             Demultiplex reads by internal barcodes
  ont-fluidigm-samples    Materialize counted per-sample ONT Fluidigm amplicon
                          FASTQs
  ont-pacbio-barcode-demux
                          Demultiplex full-length MHC ONT amplicons with PacBio
                          barcode pairs
  scout                   Scout barcode assignments before FASTQ demultiplexing
  import-ont              Import ONT output directory into per-barcode bundles
  materialize             Materialize a virtual FASTQ bundle to a physical
                          FASTQ file
  qc-summary              Compute a JSON QC summary for FASTQ input files
  pbaa-cluster            Cluster PacBio HiFi amplicon reads with pbAA in
                          pinned containers
  savont-cluster          Cluster FASTQ reads into counted consensus sequences
                          with Savont
  full-length-ont-mhc-genotype
                          Run full-length ONT MHC genotyping from per-sample
                          FASTQ bundles using Savont clusters
  genotype                Run platform-aware exact+indel amplicon genotyping
                          for ONT or Illumina reads
  genotype-cohort         Run cohort amplicon genotyping across multiple
                          prepared per-sample FASTQ bundles
  update-current-workbook Apply genotype haplotype review edits to a
                          .lungfishgenotype current.xlsx workbook
  ont-genotype            Genotype ONT amplicon FASTQ bundles by short-read
                          mapping and pysam filtering
  ont-barcode-genotype    Deprecated ONT barcode demux genotyping workflow
  mhc-reference-bundle    Create a .lungfishmhcref bundle for MHC amplicon
                          genotyping
  12s-reference-metadata  Prepare structured taxonomy metadata for a
                          deduplicated 12S reference FASTA
  12s-reference-bundle    Create a .lungfish12sref bundle for 12S amplicon
                          matching
  12s-match               Match merged 12S amplicon FASTQ reads to a
                          deduplicated reference FASTA
  12s-export              Export 12S amplicon result species rows to CSV, TSV,
                          or Excel
  12s-export-unresolved   Export unresolved 12S sequence clusters above a read
                          threshold to FASTA
  search-text             Search FASTQ reads by ID or description field
  search-motif            Search FASTQ reads by sequence motif
  orient                  Orient reads against a reference sequence
  scrub-human             Remove human reads from FASTQ
  sequence-filter         Filter reads by sequence presence (adapter/barcode
                          matching)
  deacon-ribo             Detect and remove ribosomal RNA sequences with Deacon
                          and BBMap ribokmers
  reverse-complement      Reverse-complement FASTQ reads and reverse their
                          quality scores
  translate               Translate FASTQ reads to protein FASTA

  See 'lungfish-cli help fastq <subcommand>' for detailed help.

==== fastq subsample ====
OVERVIEW: Subsample reads by proportion or count

USAGE: lungfish-cli fastq subsample <input> [--proportion <proportion>] [--count <count>] --output <output> [--force] [--compress]

ARGUMENTS:
  <input>                 Input FASTQ file

OPTIONS:
  --proportion <proportion>
                          Fraction of reads to keep (0-1)
  --count <count>         Number of reads to keep
  -o, --output <output>   Output file path (required)
  --force                 Overwrite existing output file
  --compress              Compress output with gzip
  --version               Show the version.
  -h, --help              Show help information.


==== fastq length-filter ====
OVERVIEW: Filter reads by length

USAGE: lungfish-cli fastq length-filter <input> [--min <min>] [--max <max>] --output <output> [--force] [--compress]

ARGUMENTS:
  <input>                 Input FASTQ file

OPTIONS:
  --min <min>             Minimum read length
  --max <max>             Maximum read length
  -o, --output <output>   Output file path (required)
  --force                 Overwrite existing output file
  --compress              Compress output with gzip
  --version               Show the version.
  -h, --help              Show help information.


==== fastq trim ====
OVERVIEW: Trim adapters and low-quality bases in one fastp pass

USAGE: lungfish-cli fastq trim <input> [--threshold <threshold>] [--window <window>] [--mode <mode>] [--adapter-trimming] [--no-adapter-trimming] [--adapter <adapter>] [--extra-args <extra-args>] --output <output> [--force] [--compress]

ARGUMENTS:
  <input>                 Input FASTQ file

OPTIONS:
  --threshold <threshold> Quality threshold (default: 20) (default: 20)
  --window <window>       Sliding window size (default: 4) (default: 4)
  --mode <mode>           Quality trim mode: cut-right, cut-front, cut-tail,
                          cut-both (default: cut-right) (default: cut-right)
  --adapter-trimming/--no-adapter-trimming
                          Run fastp adapter trimming in the same pass (default:
                          enabled) (default: --adapter-trimming)
  --adapter <adapter>     Adapter sequence (omit for auto-detect)
  --extra-args <extra-args>
                          Additional fastp arguments passed verbatim
  -o, --output <output>   Output file path (required)
  --force                 Overwrite existing output file
  --compress              Compress output with gzip
  --version               Show the version.
  -h, --help              Show help information.


==== fastq quality-trim ====
OVERVIEW: Trim low-quality bases using fastp

USAGE: lungfish-cli fastq quality-trim <input> [--threshold <threshold>] [--window <window>] [--mode <mode>] [--extra-args <extra-args>] --output <output> [--force] [--compress]

ARGUMENTS:
  <input>                 Input FASTQ file

OPTIONS:
  --threshold <threshold> Quality threshold (default: 20) (default: 20)
  --window <window>       Sliding window size (default: 4) (default: 4)
  --mode <mode>           Trim mode: cut-right, cut-front, cut-tail, cut-both
                          (default: cut-right) (default: cut-right)
  --extra-args <extra-args>
                          Additional fastp arguments passed verbatim
  -o, --output <output>   Output file path (required)
  --force                 Overwrite existing output file
  --compress              Compress output with gzip
  --version               Show the version.
  -h, --help              Show help information.


==== fastq adapter-trim ====
OVERVIEW: Remove adapter sequences using fastp

USAGE: lungfish-cli fastq adapter-trim <input> [--adapter <adapter>] --output <output> [--force] [--compress]

ARGUMENTS:
  <input>                 Input FASTQ file

OPTIONS:
  --adapter <adapter>     Adapter sequence (omit for auto-detect)
  -o, --output <output>   Output file path (required)
  --force                 Overwrite existing output file
  --compress              Compress output with gzip
  --version               Show the version.
  -h, --help              Show help information.


==== fastq fixed-trim ====
OVERVIEW: Trim fixed number of bases from read ends

USAGE: lungfish-cli fastq fixed-trim <input> [--front <front>] [--tail <tail>] --output <output> [--force] [--compress]

ARGUMENTS:
  <input>                 Input FASTQ file

OPTIONS:
  --front <front>         Bases to trim from 5' end (default: 0) (default: 0)
  --tail <tail>           Bases to trim from 3' end (default: 0) (default: 0)
  -o, --output <output>   Output file path (required)
  --force                 Overwrite existing output file
  --compress              Compress output with gzip
  --version               Show the version.
  -h, --help              Show help information.


==== fastq contaminant-filter ====
OVERVIEW: Remove contaminant reads using bbduk

USAGE: lungfish-cli fastq contaminant-filter <input> [--mode <mode>] [--ref <ref>] [--kmer <kmer>] [--hdist <hdist>] --output <output> [--force] [--compress]

ARGUMENTS:
  <input>                 Input FASTQ file

OPTIONS:
  --mode <mode>           Filter mode: phix, custom (default: phix) (default:
                          phix)
  --ref <ref>             Reference FASTA for custom mode
  --kmer <kmer>           K-mer size (default: 31) (default: 31)
  --hdist <hdist>         Hamming distance tolerance (default: 1) (default: 1)
  -o, --output <output>   Output file path (required)
  --force                 Overwrite existing output file
  --compress              Compress output with gzip
  --version               Show the version.
  -h, --help              Show help information.


==== fastq entropy-filter ====
OVERVIEW: Remove low-complexity reads using the bbduk entropy filter

Discards reads whose Shannon entropy falls below a threshold. This
catches homopolymer runs and tandem repeats (for example ATCATCATC…)
that per-read complexity metrics miss, and that otherwise inflate
apparent mapping depth.

Defaults are entropy 0.6, window 50, k-mer 5.

USAGE: lungfish-cli fastq entropy-filter <input> [--entropy <entropy>] [--window <window>] [--kmer <kmer>] [--threads <threads>] --output <output> [--force] [--compress]

ARGUMENTS:
  <input>                 Input FASTQ file

OPTIONS:
  --entropy <entropy>     Entropy threshold, 0.3-0.9 (default: 0.6) (default:
                          0.6)
  --window <window>       Entropy sliding window in bases (default: 50)
                          (default: 50)
  --kmer <kmer>           K-mer length for entropy estimation (default: 5)
                          (default: 5)
  --threads <threads>     bbduk thread count (default: 4) (default: 4)
  -o, --output <output>   Output file path (required)
  --force                 Overwrite existing output file
  --compress              Compress output with gzip
  --version               Show the version.
  -h, --help              Show help information.


==== fastq primer-remove ====
OVERVIEW: Remove primer sequences from FASTQ reads

USAGE: lungfish-cli fastq primer-remove <input> [--literal <literal>] [--ref <ref>] [--kmer <kmer>] [--mink <mink>] [--hdist <hdist>] [--engine <engine>] [--minimum-overlap <minimum-overlap>] [--error-rate <error-rate>] --output <output> [--force] [--compress]

ARGUMENTS:
  <input>                 Input FASTQ file

OPTIONS:
  --literal <literal>     Primer sequence (IUPAC nucleotides)
  --ref <ref>             Primer reference FASTA file
  --kmer <kmer>           K-mer size (default: 23) (default: 23)
  --mink <mink>           Minimum k-mer size (default: 11) (default: 11)
  --hdist <hdist>         Hamming distance tolerance (default: 1) (default: 1)
  --engine <engine>       Primer trimming engine: bbduk or cutadapt-linked
                          (default: bbduk)
  --minimum-overlap <minimum-overlap>
                          Minimum overlap for cutadapt-linked primer matching
                          (default: 12)
  --error-rate <error-rate>
                          Maximum error rate for cutadapt-linked primer
                          matching (default: 0.12)
  -o, --output <output>   Output file path (required)
  --force                 Overwrite existing output file
  --compress              Compress output with gzip
  --version               Show the version.
  -h, --help              Show help information.


==== fastq error-correct ====
OVERVIEW: Correct sequencing errors using tadpole

USAGE: lungfish-cli fastq error-correct <input> [--kmer <kmer>] --output <output> [--force] [--compress]

ARGUMENTS:
  <input>                 Input FASTQ file

OPTIONS:
  --kmer <kmer>           K-mer size for correction (default: 50, max: 62)
                          (default: 50)
  -o, --output <output>   Output file path (required)
  --force                 Overwrite existing output file
  --compress              Compress output with gzip
  --version               Show the version.
  -h, --help              Show help information.


==== fastq merge ====
OVERVIEW: Merge overlapping paired-end reads using bbmerge

USAGE: lungfish-cli fastq merge <input> [--min-overlap <min-overlap>] [--strict] [--count-duplicates] --output <output> [--force] [--compress]

ARGUMENTS:
  <input>                 Input interleaved FASTQ file

OPTIONS:
  --min-overlap <min-overlap>
                          Minimum overlap (default: 12) (default: 12)
  --strict                Use strict merge mode
  --count-duplicates      Collapse identical output sequences after merge and
                          encode support as size=N
  -o, --output <output>   Output file path (required)
  --force                 Overwrite existing output file
  --compress              Compress output with gzip
  --version               Show the version.
  -h, --help              Show help information.


==== fastq repair ====
OVERVIEW: Repair desynchronized paired-end reads using repair.sh

USAGE: lungfish-cli fastq repair <input> --output <output> [--force] [--compress]

ARGUMENTS:
  <input>                 Input interleaved FASTQ file

OPTIONS:
  -o, --output <output>   Output file path (required)
  --force                 Overwrite existing output file
  --compress              Compress output with gzip
  --version               Show the version.
  -h, --help              Show help information.


==== fastq deinterleave ====
OVERVIEW: Split interleaved FASTQ into separate R1/R2 files

USAGE: lungfish-cli fastq deinterleave <input> --out1 <out1> --out2 <out2>

ARGUMENTS:
  <input>                 Input interleaved FASTQ file

OPTIONS:
  --out1 <out1>           Output R1 file (required)
  --out2 <out2>           Output R2 file (required)
  --version               Show the version.
  -h, --help              Show help information.


==== fastq interleave ====
OVERVIEW: Interleave separate R1/R2 files into one FASTQ

USAGE: lungfish-cli fastq interleave --in1 <in1> --in2 <in2> --output <output> [--force] [--compress]

OPTIONS:
  --in1 <in1>             Input R1 file (required)
  --in2 <in2>             Input R2 file (required)
  -o, --output <output>   Output file path (required)
  --force                 Overwrite existing output file
  --compress              Compress output with gzip
  --version               Show the version.
  -h, --help              Show help information.


==== fastq deduplicate ====
OVERVIEW: Remove duplicate reads using clumpify.sh (BBTools)

USAGE: lungfish-cli fastq deduplicate <input> [--subs <subs>] [--optical] [--dupedist <dupedist>] --output <output> [--force] [--compress]

ARGUMENTS:
  <input>                 Input FASTQ file

OPTIONS:
  --subs <subs>           Substitution tolerance (0=exact, 2=default) (default:
                          0)
  --optical               Optical duplicate mode (patterned flowcells)
  --dupedist <dupedist>   Pixel distance for optical duplicates (default: 40)
                          (default: 40)
  -o, --output <output>   Output file path (required)
  --force                 Overwrite existing output file
  --compress              Compress output with gzip
  --version               Show the version.
  -h, --help              Show help information.


==== fastq demultiplex ====
OVERVIEW: Demultiplex reads by internal barcodes

Splits multiplexed FASTQ reads into per-barcode output files using
embedded cutadapt. Supports single- and dual-indexed Illumina kits,
Fluidigm Access Array indexes, custom barcode definition files, and
terminally anchored barcode location (5', 3', or both ends) for
cutadapt demultiplexing.

Useful for internal Illumina barcodes within ONT reads, re-demultiplexing,
or demultiplexing with custom barcode sets.

Built-in kits: truseq-single-a, truseq-single-b, truseq-ht-dual, nextera-xt-v2,
idt-ud-indexes, fluidigm-access-array, pacbio-sequel-16-v3,
pacbio-sequel-96-v2, pacbio-sequel-384-v1, m13-universal-primers, ont-nbd104,
ont-nbd114, ont-nbd104-114, ont-nbd114-96, ont-pbc096, ont-rbk004,
ont-rbk114-24, ont-rbk114-96, ont-16s114-24, ont-rab204-214.

Custom definitions can be CSV, TSV, or whitespace-delimited text:
  id,sequence[,secondary_sequence][,sample_name]
  FLD0001<TAB>GTATCGTCGT

Engines:
  cutadapt    Established fuzzy adapter matcher; supports error rate and
indels.
  exact-bare  Swift-native exact matching for bare A/C/G/T barcodes; scans
whole reads,
              searches reverse complements, and preserves reads.

Examples:
  lungfish fastq demultiplex reads.fastq.gz --kit truseq-single-a -o demux-out/
  lungfish fastq demultiplex reads.fastq.gz --kit fluidigm-access-array -o
demux-out/ --engine exact-bare
  lungfish fastq demultiplex reads.fastq.gz --kit custom.csv -o demux-out/
--location bothends

USAGE: lungfish-cli fastq demultiplex <input> --kit <kit> --output <output> [--location <location>] [--max-distance-5prime <max-distance-5prime>] [--max-distance-3prime <max-distance-3prime>] [--error-rate <error-rate>] [--overlap <overlap>] [--engine <engine>] [--no-trim] [--discard-unassigned] [--threads <threads>]

ARGUMENTS:
  <input>                 Input FASTQ file or .lungfishfastq bundle

OPTIONS:
  --kit <kit>             Barcode kit: truseq-single-a, truseq-single-b,
                          truseq-ht-dual, nextera-xt-v2, idt-ud-indexes,
                          fluidigm-access-array, pacbio-sequel-16-v3,
                          pacbio-sequel-96-v2, pacbio-sequel-384-v1,
                          m13-universal-primers, ont-nbd104, ont-nbd114,
                          ont-nbd104-114, ont-nbd114-96, ont-pbc096,
                          ont-rbk004, ont-rbk114-24, ont-rbk114-96,
                          ont-16s114-24, ont-rab204-214, or path to custom
                          CSV/TSV/text barcode definition. Custom definitions
                          can be CSV, TSV, or whitespace text with columns
                          id,sequence[,secondary_sequence][,sample_name];
                          header optional. Example: FLD0001<TAB>GTATCGTCGT.
  -o, --output <output>   Output directory for per-barcode bundles
  --location <location>   Cutadapt barcode location: 5prime, 3prime, bothends
                          (default: bothends) (default: bothends)
  --max-distance-5prime <max-distance-5prime>
                          Cutadapt max bases from 5' terminus where barcodes
                          may start (default: 0) (default: 0)
  --max-distance-3prime <max-distance-3prime>
                          Cutadapt max bases from 3' terminus where barcodes
                          may end (default: 0) (default: 0)
  --error-rate <error-rate>
                          Cutadapt maximum error rate for barcode matching
                          (default: 0.15) (default: 0.15)
  --overlap <overlap>     Cutadapt minimum overlap length (default: 3)
                          (default: 3)
  --engine <engine>       Demultiplexing engine: cutadapt or exact-bare
                          (default: cutadapt) (default: cutadapt)
  --no-trim               Cutadapt only: keep barcode sequences in output reads
                          (exact-bare always preserves reads)
  --discard-unassigned    Discard reads that do not match any barcode
  --threads <threads>     Cutadapt thread count (default: 4) (default: 4)
  --version               Show the version.
  -h, --help              Show help information.


==== fastq ont-fluidigm-samples ====
OVERVIEW: Materialize counted per-sample ONT Fluidigm amplicon FASTQs

Assigns ONT reads to samples with the same exact Fluidigm barcode matching
used by amplicon genotyping, extracts the CS1-CS2 amplicon insert, then
writes one physical .lungfishfastq bundle per sample containing unique
insert exemplars. Payloads are gzip-compressed and duplicate support is
encoded in FASTQ headers as size=N.

USAGE: lungfish-cli fastq ont-fluidigm-samples <input> --barcodes <barcodes> --output <output> [--threads <threads>] [--primer-mismatches <primer-mismatches>] [--minimum-insert-length <minimum-insert-length>] [--canonicalize-reverse-complements] [--no-canonicalize-reverse-complements] [--force]

ARGUMENTS:
  <input>                 Input FASTQ file, directory, or .lungfishfastq bundle

OPTIONS:
  --barcodes <barcodes>   CSV/TSV file with sample and Fluidigm barcode
                          sequence columns
  -o, --output <output>   Output directory for per-sample .lungfishfastq bundles
  --threads <threads>     Worker count reserved for future parallel
                          materialization; currently recorded for provenance
                          (default: 1)
  --primer-mismatches <primer-mismatches>
                          Maximum mismatches allowed when detecting CS1/CS2
                          primer boundaries (default: 2) (default: 2)
  --minimum-insert-length <minimum-insert-length>
                          Minimum CS1-CS2 insert length to retain (default: 20)
                          (default: 20)
  --canonicalize-reverse-complements/--no-canonicalize-reverse-complements
                          Canonicalize exact reverse-complement insert
                          duplicates after orienting CS1-CS2 reads (default:
                          disabled) (default:
                          --no-canonicalize-reverse-complements)
  --force                 Replace an existing output directory
  --version               Show the version.
  -h, --help              Show help information.


==== fastq ont-pacbio-barcode-demux ====
OVERVIEW: Demultiplex full-length MHC ONT amplicons with PacBio barcode pairs

Validates a PacBio barcode-pair sample sheet, resolves built-in bc*
barcode IDs, and writes one materialized .lungfishfastq bundle per
sample. Barcode definitions must contain sample_id, barcode_1, and
barcode_2 columns, or headerless rows in that order.

Repeated sample IDs are numbered _1, _2, and so on.
Unique sample IDs remain unchanged.

USAGE: lungfish-cli fastq ont-pacbio-barcode-demux <input> --barcodes <barcodes> --output <output> [--threads <threads>] [--chunk-jobs <chunk-jobs>] [--max-reads-per-slice <max-reads-per-slice>] [--max-bytes-per-cutadapt <max-bytes-per-cutadapt>] [--force]

ARGUMENTS:
  <input>                 Input ONT FASTQ file, barcode directory, run
                          directory, or .lungfishfastq bundle

OPTIONS:
  --barcodes <barcodes>   CSV/TSV file with sample_id, barcode_1, and barcode_2
                          columns, or headerless rows in that order
  -o, --output <output>   Output directory for per-sample .lungfishfastq bundles
  --threads <threads>     Compatibility option for legacy chunked demux paths
                          (default: 1) (default: 1)
  --chunk-jobs <chunk-jobs>
                          Compatibility option for legacy chunked demux paths
                          (default: active cores) (default: 14)
  --max-reads-per-slice <max-reads-per-slice>
                          Compatibility option for legacy chunked demux paths;
                          0 disables sub-slicing (default: 100000) (default:
                          100000)
  --max-bytes-per-cutadapt <max-bytes-per-cutadapt>
                          Compatibility option for legacy chunked demux paths
                          (default: 536870912) (default: 536870912)
  --force                 Replace an existing output directory
  --version               Show the version.
  -h, --help              Show help information.


==== fastq scout ====
OVERVIEW: Scout barcode assignments before FASTQ demultiplexing

Scans a subset of reads against a barcode kit and writes a
scout-result.json file with per-barcode hit counts and suggested
accept/reject dispositions.

USAGE: lungfish-cli fastq scout [<options>] <input> --kit <kit> --output <output>

ARGUMENTS:
  <input>                 Input FASTQ file or .lungfishfastq bundle

OPTIONS:
  --kit <kit>             Barcode kit: truseq-single-a, truseq-single-b,
                          truseq-ht-dual, nextera-xt-v2, idt-ud-indexes,
                          fluidigm-access-array, pacbio-sequel-16-v3,
                          pacbio-sequel-96-v2, pacbio-sequel-384-v1,
                          m13-universal-primers, ont-nbd104, ont-nbd114,
                          ont-nbd104-114, ont-nbd114-96, ont-pbc096,
                          ont-rbk004, ont-rbk114-24, ont-rbk114-96,
                          ont-16s114-24, ont-rab204-214, or path to custom
                          CSV/TSV/text barcode definition. Custom definitions
                          can be CSV, TSV, or whitespace text with columns
                          id,sequence[,secondary_sequence][,sample_name];
                          header optional. Example: FLD0001<TAB>GTATCGTCGT.
  -o, --output <output>   Output scout-result.json path
  --read-limit <read-limit>
                          Maximum reads to scan (default: 10000) (default:
                          10000)
  --accept-threshold <accept-threshold>
                          Minimum hits to auto-accept a barcode (default: 10)
                          (default: 10)
  --reject-threshold <reject-threshold>
                          Maximum hits to auto-reject a barcode (default: 3)
                          (default: 3)
  --error-rate <error-rate>
                          Override barcode matching error rate
  --overlap <overlap>     Override minimum barcode overlap
  --source-platform <source-platform>
                          Source platform: illumina, ont, pacbio, element,
                          ultima, mgi
  --no-indels             Disallow indels in barcode matching
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.


==== fastq import-ont ====
OVERVIEW: Import ONT output directory into per-barcode bundles

Imports Oxford Nanopore sequencing output directories into per-barcode
.lungfishfastq bundles. Concatenates chunked FASTQ files within each
barcode directory and generates a demultiplex manifest.

Accepts either a fastq_pass/ parent directory or a single barcode
directory (e.g., fastq_pass/barcode01/).

Examples:
  lungfish fastq import-ont fastq_pass/ -o imported/
  lungfish fastq import-ont fastq_pass/barcode13/ -o imported/
  lungfish fastq import-ont fastq_pass/ -o imported/ --include-unclassified

USAGE: lungfish-cli fastq import-ont <input> --output <output> [--include-unclassified] [--concurrency <concurrency>] [--storage-mode <storage-mode>] [--optimize-storage] [--quality-binning <quality-binning>]

ARGUMENTS:
  <input>                 ONT output directory (fastq_pass/ or single barcode
                          directory)

OPTIONS:
  -o, --output <output>   Output directory for .lungfishfastq bundles
  --include-unclassified  Include unclassified reads (default: skip)
  --concurrency <concurrency>
                          Max concurrent barcode imports (default: 4) (default:
                          4)
  --storage-mode <storage-mode>
                          How to store ONT chunks inside each bundle: chunked
                          or flattened (default: chunked) (values: chunked,
                          flattened; default: chunked)
  --optimize-storage      Run clumpify on flattened per-barcode FASTQ payloads
                          to improve compression
  --quality-binning <quality-binning>
                          Quality binning for --optimize-storage: none,
                          illumina4, or eightLevel (default: none) (values:
                          illumina4, eightLevel, none; default: none)
  --version               Show the version.
  -h, --help              Show help information.


==== fastq materialize ====
OVERVIEW: Materialize a virtual FASTQ bundle to a physical FASTQ file

Reads the derived bundle manifest, resolves the root FASTQ, and applies
payload-specific materialization (subset read IDs, trim positions, or
copy full payload). Produces a single output FASTQ file.

Examples:
  lungfish-cli fastq materialize myreads.lungfishfastq -o output.fastq
  lungfish-cli fastq materialize trimmed.lungfishfastq -o reads.fastq
--temp-dir /tmp/work

USAGE: lungfish-cli fastq materialize <input> --output <output> [--force] [--compress] [--temp-dir <temp-dir>]

ARGUMENTS:
  <input>                 Input .lungfishfastq bundle path

OPTIONS:
  -o, --output <output>   Output file path (required)
  --force                 Overwrite existing output file
  --compress              Compress output with gzip
  --temp-dir <temp-dir>   Temporary directory for intermediate files
  --version               Show the version.
  -h, --help              Show help information.


==== fastq qc-summary ====
OVERVIEW: Compute a JSON QC summary for FASTQ input files

USAGE: lungfish-cli fastq qc-summary <inputs> ... --output <output> [--force] [--compress]

ARGUMENTS:
  <inputs>                Input FASTQ file(s)

OPTIONS:
  -o, --output <output>   Output file path (required)
  --force                 Overwrite existing output file
  --compress              Compress output with gzip
  --version               Show the version.
  -h, --help              Show help information.


==== fastq pbaa-cluster ====
OVERVIEW: Cluster PacBio HiFi amplicon reads with pbAA in pinned containers

USAGE: lungfish-cli fastq pbaa-cluster <input> --guide <guide> --output-dir <output-dir> [--output-name <output-name>] [--threads <threads>] [--seed <seed>] [--extra-args <extra-args>]

ARGUMENTS:
  <input>                 Input FASTQ file or .lungfishfastq bundle

OPTIONS:
  --guide <guide>         Guide FASTA file or .lungfishref bundle
  --output-dir <output-dir>
                          Directory for raw outputs and the .lungfishref result
  --output-name <output-name>
                          Output bundle name and pbAA prefix (default:
                          pbaa-clusters)
  --threads <threads>     Threads for pbAA (default: 14)
  --seed <seed>           pbAA random seed (default: 1984)
  --extra-args <extra-args>
                          Advanced pbAA arguments
  --version               Show the version.
  -h, --help              Show help information.


==== fastq savont-cluster ====
OVERVIEW: Cluster FASTQ reads into counted consensus sequences with Savont

USAGE: lungfish-cli fastq savont-cluster <input> --output <output> [--threads <threads>] [--quality-value-cutoff <quality-value-cutoff>] [--min-cluster-size <min-cluster-size>] [--min-read-length <min-read-length>] [--max-read-length <max-read-length>] [--single-strand]

ARGUMENTS:
  <input>                 Input FASTQ file or .lungfishfastq bundle

OPTIONS:
  --output <output>       Output counted-cluster FASTA file
  --threads <threads>     Threads for Savont (default: 14)
  --quality-value-cutoff <quality-value-cutoff>
                          Savont quality-value cutoff (default: 90)
  --min-cluster-size <min-cluster-size>
                          Minimum reads per cluster (default: 3)
  --min-read-length <min-read-length>
                          Optional minimum read length
  --max-read-length <max-read-length>
                          Optional maximum read length
  --single-strand         Run Savont in single-strand mode
  --version               Show the version.
  -h, --help              Show help information.


==== fastq full-length-ont-mhc-genotype ====
OVERVIEW: Run full-length ONT MHC genotyping from per-sample FASTQ bundles
using Savont clusters

USAGE: lungfish-cli fastq full-length-ont-mhc-genotype [<options>] <inputs> ... --reference <reference> --output-dir <output-dir>

ARGUMENTS:
  <inputs>                One or more per-sample ONT FASTQ files or
                          .lungfishfastq bundles

OPTIONS:
  --reference <reference> MHC allele FASTA, .lungfishref bundle, or
                          .lungfishmhcref bundle
  --orient-reference <orient-reference>
                          Optional FASTA used by vsearch --orient
  --forward-primer <forward-primer>
                          Optional forward primer FASTA for 5-prime bbduk
                          trimming
  --reverse-primer <reverse-primer>
                          Optional reverse primer FASTA for 3-prime bbduk
                          trimming
  --output-dir <output-dir>
                          Output .lungfishgenotype bundle directory
  --output-name <output-name>
                          Output report stem (default:
                          full-length-ont-mhc-genotyping)
  --project <project>     Optional Lungfish project root for provenance context
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --sample-jobs <sample-jobs>
                          Concurrent sample workflows. Defaults to an automatic
                          sample-level parallel strategy.
  --savont-threads-per-sample <savont-threads-per-sample>
                          Savont threads per concurrently processed sample.
                          Defaults to an automatic batch-aware value.
  --min-length <min-length>
                          Minimum post-primer read length retained for Savont
                          (default: 2000)
  --max-length <max-length>
                          Maximum post-primer read length retained for Savont
                          (default: 4000)
  --savont-quality-value-cutoff <savont-quality-value-cutoff>
                          Minimum estimated read accuracy percent retained for
                          Savont clustering (default: 90)
  --savont-min-cluster-size <savont-min-cluster-size>
                          Minimum number of reads required to keep a Savont
                          cluster (default: 3)
  --min-unmatched-reads <min-unmatched-reads>
                          Minimum cluster read count written to unmatched FASTA
                          (default: 5)
  --cdna-threshold <cdna-threshold>
                          Alleles shorter than this length are treated as cDNA
                          references (default: 2000)
  --keep-intermediates    Preserve regenerable full-length ONT MHC workflow
                          intermediates for debugging
  --reuse-compatible-checkpoints
                          Reuse compatible full-length ONT MHC sample
                          checkpoints when present
  --haplotype-min-sample-percent <haplotype-min-sample-percent>
                          Drop genotype rows below this percent of retained
                          genotyping reads for the sample; 0 disables (default:
                          0.0)
  --haplotype-min-locus-percent <haplotype-min-locus-percent>
                          Drop genotype rows below this percent of retained
                          genotyping reads for the sample/locus; 0 disables
                          (default: 0.0)
  --haplotype-min-locus-percent-override <haplotype-min-locus-percent-override>
                          Per-locus percent override such as MHC-A=10; may be
                          repeated
  --haplotype-assay <haplotype-assay>
                          Assay/amplicon set for haplotyping; disambiguates
                          --haplotype-definition
  --haplotype-species <haplotype-species>
                          Species code used to restrict compatible haplotype
                          definitions, such as MCM or MAMU
  --haplotype-definition-scope <haplotype-definition-scope>
                          Haplotype definition scope: project
  --haplotype-definition <haplotype-definition>
                          Optional assay-scoped haplotype definition set ID;
                          omit to skip haplotyping
  --version               Show the version.
  -h, --help              Show help information.


==== fastq genotype ====
OVERVIEW: Run platform-aware exact+indel amplicon genotyping for ONT or
Illumina reads

USAGE: lungfish-cli fastq genotype [<options>] <inputs> ... --output-dir <output-dir>

ARGUMENTS:
  <inputs>                Input FASTQ file, folder, or .lungfishfastq bundle.
                          Sample-bundle modes accept multiple prepared
                          per-sample bundles.

OPTIONS:
  --mode <mode>           Genotyping mode: auto, ont-sample-bundles,
                          illumina-paired, or deprecated ont-barcode-demux
                          (default: auto)
  --read-type <read-type> Read type override: auto, ont, or illumina (default:
                          auto)
  --reference <reference> Reference FASTA file, .lungfishref bundle, or
                          .lungfishmhcref bundle (FASTA + paired haplotype
                          definitions) used as the mapping target
  --preset <preset>       Locked genotyping preset. Supported value:
                          mcm-mhc-miseq.
  --barcodes <barcodes>   Deprecated. CSV/TSV file containing sample ID and
                          Fluidigm barcode sequence columns for ONT
                          barcode-demux mode
  --demux-manifest <demux-manifest>
                          Optional demux-manifest.json with total input/sample
                          read counts for ONT barcode-demux mode
  --output-dir <output-dir>
                          Directory for genotype CSV summaries, workbook,
                          stats, and provenance
  --output-name <output-name>
                          Output filename stem (default: amplicon-genotyping)
  --analysis-name <analysis-name>
                          Label for this analysis in the workbook; defaults to
                          --output-name
  --comparison-workbook <comparison-workbook>
                          Optional Excel workbook whose first sheet provides
                          comparison layout and expected calls
  --comparison-name <comparison-name>
                          Label for the comparison workbook sheet (default:
                          Illumina-31262)
  --project <project>     Project directory; external FASTA references are
                          imported here before mapping
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --sort-threads <sort-threads>
                          Threads for samtools sort (default: 4)
  --min-support <min-support>
                          Minimum retained unique-read support required for a
                          genotype row in the report and workbook (default: 1)
  --keep-intermediates    Keep regenerable workflow intermediates for
                          troubleshooting
  --haplotype-min-sample-percent <haplotype-min-sample-percent>
                          Drop genotype rows below this percent of retained
                          genotyping reads for the sample; 0 disables (default:
                          0.0)
  --haplotype-min-locus-percent <haplotype-min-locus-percent>
                          Drop genotype rows below this percent of retained
                          genotyping reads for the sample/locus; 0 disables
                          (default: 0.0)
  --haplotype-min-locus-percent-override <haplotype-min-locus-percent-override>
                          Per-locus percent override such as MHC-DQ=10; may be
                          repeated
  --haplotype-assay <haplotype-assay>
                          Assay/amplicon set for haplotyping; disambiguates
                          --haplotype-definition
  --haplotype-species <haplotype-species>
                          Species code used to restrict compatible haplotype
                          definitions, such as MCM or MAMU
  --haplotype-definition-scope <haplotype-definition-scope>
                          Haplotype definition scope: project
  --haplotype-definition <haplotype-definition>
                          Optional assay-scoped haplotype definition set ID;
                          omit to skip haplotyping
  --extra-args <extra-args>
                          Advanced minimap2 arguments passed after the mapping
                          preset
  --version               Show the version.
  -h, --help              Show help information.


==== fastq genotype-cohort ====
OVERVIEW: Run cohort amplicon genotyping across multiple prepared per-sample
FASTQ bundles

USAGE: lungfish-cli fastq genotype-cohort [<options>] <inputs> ... --output-dir <output-dir>

ARGUMENTS:
  <inputs>                Input .lungfishfastq bundles. Each bundle must
                          contain one prepared per-sample FASTQ.

OPTIONS:
  --mode <mode>           Genotyping mode: auto, ont-sample-bundles,
                          illumina-paired, or deprecated ont-barcode-demux
                          (default: illumina-paired)
  --read-type <read-type> Read type override: auto, ont, or illumina (default:
                          illumina)
  --reference <reference> Reference FASTA file, .lungfishref bundle, or
                          .lungfishmhcref bundle (FASTA + paired haplotype
                          definitions) used as the mapping target
  --preset <preset>       Locked genotyping preset. Supported value:
                          mcm-mhc-miseq.
  --barcodes <barcodes>   Deprecated. CSV/TSV file containing sample ID and
                          Fluidigm barcode sequence columns for ONT
                          barcode-demux mode
  --demux-manifest <demux-manifest>
                          Optional demux-manifest.json with total input/sample
                          read counts for ONT barcode-demux mode
  --output-dir <output-dir>
                          Directory for genotype CSV summaries, workbook,
                          stats, and provenance
  --output-name <output-name>
                          Output filename stem (default: amplicon-genotyping)
  --analysis-name <analysis-name>
                          Label for this analysis in the workbook; defaults to
                          --output-name
  --comparison-workbook <comparison-workbook>
                          Optional Excel workbook whose first sheet provides
                          comparison layout and expected calls
  --comparison-name <comparison-name>
                          Label for the comparison workbook sheet (default:
                          Illumina-31262)
  --project <project>     Project directory; external FASTA references are
                          imported here before mapping
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --sort-threads <sort-threads>
                          Threads for samtools sort (default: 4)
  --min-support <min-support>
                          Minimum retained unique-read support required for a
                          genotype row in the report and workbook (default: 1)
  --keep-intermediates    Keep regenerable workflow intermediates for
                          troubleshooting
  --haplotype-min-sample-percent <haplotype-min-sample-percent>
                          Drop genotype rows below this percent of retained
                          genotyping reads for the sample; 0 disables (default:
                          0.0)
  --haplotype-min-locus-percent <haplotype-min-locus-percent>
                          Drop genotype rows below this percent of retained
                          genotyping reads for the sample/locus; 0 disables
                          (default: 0.0)
  --haplotype-min-locus-percent-override <haplotype-min-locus-percent-override>
                          Per-locus percent override such as MHC-DQ=10; may be
                          repeated
  --haplotype-assay <haplotype-assay>
                          Assay/amplicon set for haplotyping; disambiguates
                          --haplotype-definition
  --haplotype-species <haplotype-species>
                          Species code used to restrict compatible haplotype
                          definitions, such as MCM or MAMU
  --haplotype-definition-scope <haplotype-definition-scope>
                          Haplotype definition scope: project
  --haplotype-definition <haplotype-definition>
                          Optional assay-scoped haplotype definition set ID;
                          omit to skip haplotyping
  --extra-args <extra-args>
                          Advanced minimap2 arguments passed after the mapping
                          preset
  --version               Show the version.
  -h, --help              Show help information.


==== fastq update-current-workbook ====
OVERVIEW: Apply genotype haplotype review edits to a .lungfishgenotype
current.xlsx workbook

Updates artifacts/workbooks/current.xlsx inside a .lungfishgenotype bundle
from a displayed/effective haplotype-call JSON snapshot and the bundle's
annotations.json audit sidecar.

USAGE: lungfish-cli fastq update-current-workbook [<options>] <bundle> --calls-json <calls-json>

ARGUMENTS:
  <bundle>                Input .lungfishgenotype bundle

OPTIONS:
  --calls-json <calls-json>
                          JSON array of displayed/effective haplotype calls
  --annotations <annotations>
                          Annotation sidecar to write Overrides and Audit Log
                          worksheets; defaults to bundle annotations.json when
                          present
  --included-locus <included-locus>
                          Haplotype locus included in the displayed call
                          snapshot; repeat for multiple loci
  --annotation-only       Apply annotations while preserving the
                          manifest-attested scientific workbook projection
  --haplotype-projection-mode <haplotype-projection-mode>
                          Typed haplotype source: haplotyped or
                          manual-genotype-only (values: haplotyped,
                          manual-genotype-only; default: haplotyped)
  --input-fingerprint <input-fingerprint>
                          Lowercase SHA-256 fingerprint of the immutable
                          current-workbook inputs
  --input-fingerprint-schema <input-fingerprint-schema>
                          Schema version for --input-fingerprint
  --reviewable-row-catalog-path <reviewable-row-catalog-path>
                          Manifest-relative path attested by --input-fingerprint
  --reviewable-row-catalog-size <reviewable-row-catalog-size>
                          Byte size attested for the reviewable-row catalog
  --reviewable-row-catalog-sha256 <reviewable-row-catalog-sha256>
                          SHA-256 attested for the reviewable-row catalog
  --reviewable-row-catalog-schema <reviewable-row-catalog-schema>
                          Document schema version attested for the
                          reviewable-row catalog
  --sync-intent <sync-intent>
                          Synchronization intent: automatic-idle,
                          bundle-switch, or update-and-view
  --version               Show the version.
  -h, --help              Show help information.


==== fastq ont-genotype ====
OVERVIEW: Genotype ONT amplicon FASTQ bundles by short-read mapping and pysam
filtering

USAGE: lungfish-cli fastq ont-genotype [<options>] <inputs> ... --reference <reference> --output-dir <output-dir>

ARGUMENTS:
  <inputs>                Input FASTQ files or .lungfishfastq bundles. Multiple
                          demultiplexed bundles are supported.

OPTIONS:
  --reference <reference> Reference FASTA file, .lungfishref bundle, or
                          .lungfishmhcref bundle (FASTA + paired haplotype
                          definitions) used as the mapping target
  --output-dir <output-dir>
                          Directory for filtered BAMs, indexes, report CSV, and
                          workflow provenance
  --output-name <output-name>
                          Report filename stem (default: ont-genotyping-report)
  --project <project>     Project directory; external FASTA references are
                          imported here before mapping
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --min-support <min-support>
                          Minimum filtered read support required for a genotype
                          row in the report (default: 1)
  --extra-args <extra-args>
                          Advanced minimap2 arguments passed after the simple
                          mapping settings
  --version               Show the version.
  -h, --help              Show help information.


==== fastq ont-barcode-genotype ====
OVERVIEW: Deprecated ONT barcode demux genotyping workflow

Deprecated: demultiplexing now belongs in FASTQ import recipes. Use those
recipes to create per-sample .lungfishfastq bundles, then run `lungfish-cli
fastq genotype` or `lungfish-cli fastq genotype-cohort`.

USAGE: lungfish-cli fastq ont-barcode-genotype [<options>] <input> --barcodes <barcodes> --output-dir <output-dir>

ARGUMENTS:
  <input>                 Original ONT FASTQ file or multi-file .lungfishfastq
                          bundle

OPTIONS:
  --reference <reference> Reference FASTA file, .lungfishref bundle, or
                          .lungfishmhcref bundle (FASTA + paired haplotype
                          definitions) used as the mapping target
  --preset <preset>       Locked genotyping preset. Supported value:
                          mcm-mhc-miseq.
  --barcodes <barcodes>   CSV/TSV file containing sample ID and Fluidigm
                          barcode sequence columns
  --demux-manifest <demux-manifest>
                          Optional demux-manifest.json with total input/sample
                          read counts; defaults to the input bundle manifest
  --output-dir <output-dir>
                          Directory for mapped BAM, retained BAM, CSV
                          summaries, stats, and provenance
  --output-name <output-name>
                          Output filename stem (default: ont-barcode-genotyping)
  --analysis-name <analysis-name>
                          Label for this ONT analysis in the workbook; defaults
                          to --output-name
  --comparison-workbook <comparison-workbook>
                          Optional Excel workbook whose first sheet provides
                          comparison layout and expected calls
  --comparison-name <comparison-name>
                          Label for the comparison workbook sheet (default:
                          Illumina-31262)
  --project <project>     Project directory; external FASTA references are
                          imported here before mapping
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --sort-threads <sort-threads>
                          Threads for samtools sort (default: 4)
  --min-support <min-support>
                          Minimum retained unique-read support required for a
                          genotype row in the report and workbook (default: 1)
  --keep-intermediates    Keep regenerable workflow intermediates for
                          troubleshooting
  --haplotype-min-sample-percent <haplotype-min-sample-percent>
                          Drop genotype rows below this percent of retained
                          genotyping reads for the sample; 0 disables (default:
                          0.0)
  --haplotype-min-locus-percent <haplotype-min-locus-percent>
                          Drop genotype rows below this percent of retained
                          genotyping reads for the sample/locus; 0 disables
                          (default: 0.0)
  --haplotype-min-locus-percent-override <haplotype-min-locus-percent-override>
                          Per-locus percent override such as MHC-DQ=10; may be
                          repeated
  --haplotype-assay <haplotype-assay>
                          Assay/amplicon set for haplotyping; disambiguates
                          --haplotype-definition
  --haplotype-species <haplotype-species>
                          Species code used to restrict compatible haplotype
                          definitions, such as MCM or MAMU
  --haplotype-definition-scope <haplotype-definition-scope>
                          Haplotype definition scope: project
  --haplotype-definition <haplotype-definition>
                          Optional assay-scoped haplotype definition set ID;
                          omit to skip haplotyping
  --extra-args <extra-args>
                          Advanced minimap2 arguments passed after --MD and
                          before reference/input paths
  --version               Show the version.
  -h, --help              Show help information.


==== fastq mhc-reference-bundle ====
OVERVIEW: Create a .lungfishmhcref bundle for MHC amplicon genotyping

USAGE: lungfish-cli fastq mhc-reference-bundle --reference-fasta <reference-fasta> [--haplotype-definition <haplotype-definition> ...] [--default-haplotype-definition <default-haplotype-definition>] --output <output> [--name <name>] [--source-file <source-file> ...] [--source-directory <source-directory> ...] [--force]

OPTIONS:
  --reference-fasta <reference-fasta>
                          MHC amplicon reference in FASTA, GenBank, or EMBL
                          format
  --haplotype-definition <haplotype-definition>
                          Haplotype definition JSON file to embed in the bundle
  --default-haplotype-definition <default-haplotype-definition>
                          Default embedded haplotype definition set ID
  --output <output>       Output .lungfishmhcref bundle
  --name <name>           Display name stored in the bundle manifest
  --source-file <source-file>
                          Additional source file to copy into the bundle
  --source-directory <source-directory>
                          Additional source directory to copy into the bundle
  --force                 Replace an existing .lungfishmhcref bundle
  --version               Show the version.
  -h, --help              Show help information.


==== fastq search-text ====
OVERVIEW: Search FASTQ reads by ID or description field

USAGE: lungfish-cli fastq search-text <input> --output <output> [--force] [--compress] --query <query> [--field <field>] [--regex]

ARGUMENTS:
  <input>                 Input FASTQ file path

OPTIONS:
  -o, --output <output>   Output file path (required)
  --force                 Overwrite existing output file
  --compress              Compress output with gzip
  --query <query>         Search query string
  --field <field>         Field to search: "id" or "description" (default: id)
                          (default: id)
  --regex                 Treat query as a regular expression
  --version               Show the version.
  -h, --help              Show help information.


==== fastq search-motif ====
OVERVIEW: Search FASTQ reads by sequence motif

USAGE: lungfish-cli fastq search-motif <input> --output <output> [--force] [--compress] --pattern <pattern> [--regex]

ARGUMENTS:
  <input>                 Input FASTQ file path

OPTIONS:
  -o, --output <output>   Output file path (required)
  --force                 Overwrite existing output file
  --compress              Compress output with gzip
  --pattern <pattern>     Sequence motif pattern to search for
  --regex                 Treat pattern as a regular expression
  --version               Show the version.
  -h, --help              Show help information.


==== fastq orient ====
OVERVIEW: Orient reads against a reference sequence

USAGE: lungfish-cli fastq orient <input> --output <output> [--force] [--compress] --reference <reference> [--word-length <word-length>] [--db-mask <db-mask>] [--extra-args <extra-args>]

ARGUMENTS:
  <input>                 Input FASTQ file path

OPTIONS:
  -o, --output <output>   Output file path (required)
  --force                 Overwrite existing output file
  --compress              Compress output with gzip
  --reference <reference> Reference FASTA file path
  --word-length <word-length>
                          Word length for orientation matching (default: 12)
                          (default: 12)
  --db-mask <db-mask>     Database masking method (default: dust) (default:
                          dust)
  --extra-args <extra-args>
                          Additional vsearch arguments passed verbatim
  --version               Show the version.
  -h, --help              Show help information.


==== fastq scrub-human ====
OVERVIEW: Remove human reads from FASTQ

USAGE: lungfish-cli fastq scrub-human <input> --output <output> [--force] [--compress] --database-id <database-id> [--remove-reads]

ARGUMENTS:
  <input>                 Input FASTQ file path

OPTIONS:
  -o, --output <output>   Output file path (required)
  --force                 Overwrite existing output file
  --compress              Compress output with gzip
  --database-id <database-id>
                          Human read removal database identifier
  --remove-reads          Deprecated compatibility flag; ignored because Deacon
                          always removes matched reads
  --version               Show the version.
  -h, --help              Show help information.


==== fastq sequence-filter ====
OVERVIEW: Filter reads by sequence presence (adapter/barcode matching)

USAGE: lungfish-cli fastq sequence-filter <input> --output <output> [--force] [--compress] [--sequence <sequence>] [--fasta-path <fasta-path>] [--search-end <search-end>] [--min-overlap <min-overlap>] [--error-rate <error-rate>] [--keep-matched] [--search-rc]

ARGUMENTS:
  <input>                 Input FASTQ file path

OPTIONS:
  -o, --output <output>   Output file path (required)
  --force                 Overwrite existing output file
  --compress              Compress output with gzip
  --sequence <sequence>   Literal sequence to match against reads
  --fasta-path <fasta-path>
                          Path to FASTA file containing sequences to match
  --search-end <search-end>
                          Which end to search: "left", "right", or "both"
                          (default: both) (default: both)
  --min-overlap <min-overlap>
                          Minimum overlap length (default: 8) (default: 8)
  --error-rate <error-rate>
                          Allowed error rate as fraction (default: 0.1)
                          (default: 0.1)
  --keep-matched          Keep matched reads instead of discarding them
  --search-rc             Also search the reverse complement of the sequence
  --version               Show the version.
  -h, --help              Show help information.


==== fastq deacon-ribo ====
OVERVIEW: Detect and remove ribosomal RNA sequences with Deacon and BBMap
ribokmers

USAGE: lungfish-cli fastq deacon-ribo [<options>] <inputs> ... --output <output>

ARGUMENTS:
  <inputs>                Input FASTA/FASTQ file, or paired R1/R2 FASTQ files

OPTIONS:
  --retain <retain>       Read classes to retain: norrna, rrna, or both
                          (default: norrna)
  --database-id <database-id>
                          Managed Deacon database ID (default: deacon-ribokmers)
  --absolute-threshold <absolute-threshold>
                          Minimum absolute minimizer hits for an rRNA match
                          (default: 1)
  --relative-threshold <relative-threshold>
                          Minimum relative minimizer-hit proportion for an rRNA
                          match (default: 0.0)
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  -o, --output <output>   Output directory
  --version               Show the version.
  -h, --help              Show help information.


==== fastq reverse-complement ====
OVERVIEW: Reverse-complement FASTQ reads and reverse their quality scores

USAGE: lungfish-cli fastq reverse-complement <input> --output <output> [--force] [--compress]

ARGUMENTS:
  <input>                 Input FASTQ file

OPTIONS:
  -o, --output <output>   Output file path (required)
  --force                 Overwrite existing output file
  --compress              Compress output with gzip
  --version               Show the version.
  -h, --help              Show help information.


==== fastq translate ====
OVERVIEW: Translate FASTQ reads to protein FASTA

USAGE: lungfish-cli fastq translate <input> [--frame <frame>] [--table <table>] --output <output> [--force] [--compress]

ARGUMENTS:
  <input>                 Input FASTQ file

OPTIONS:
  --frame <frame>         Reading frame: 1-3 forward, 4-6 reverse (default: 1)
                          (default: 1)
  --table <table>         Genetic code table ID (default: 1) (default: 1)
  -o, --output <output>   Output file path (required)
  --force                 Overwrite existing output file
  --compress              Compress output with gzip
  --version               Show the version.
  -h, --help              Show help information.

