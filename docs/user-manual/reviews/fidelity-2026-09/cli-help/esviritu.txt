==== esviritu ====
OVERVIEW: Run EsViritu viral metagenomics detection on FASTQ files

Detect and characterize viruses from metagenomic sequencing data
using EsViritu. Requires the EsViritu conda package and its curated
viral reference database.

Install the tool: lungfish conda install esviritu
Download the database: lungfish esviritu download-db

USAGE: lungfish-cli esviritu <subcommand>

OPTIONS:
  --version               Show the version.
  -h, --help              Show help information.

SUBCOMMANDS:
  detect (default)        Run EsViritu viral detection on FASTQ files
  download-db             Download the EsViritu viral reference database
  db-status               Check EsViritu database installation status

  See 'lungfish-cli help esviritu <subcommand>' for detailed help.

==== esviritu detect ====
OVERVIEW: Run EsViritu viral detection on FASTQ files

USAGE: lungfish-cli esviritu detect [<options>] --sample <sample>

OPTIONS:
  -i, --input <input>     Input FASTQ file(s). Provide two files for paired-end.
  -s, --sample <sample>   Sample name for output file prefixes
  --db <db>               Path to EsViritu database directory (default:
                          auto-detect)
  -o, --output <output>   Output directory (default: current directory)
  --paired                Input files are paired-end reads
  --recursive             When an input is a directory, include eligible FASTQ
                          files in subfolders
  --no-qc                 Skip quality filtering (fastp)
  --min-read-length <min-read-length>
                          Minimum read length after filtering (default: 100)
                          (default: 100)
  --extra-args <extra-args>
                          Additional EsViritu arguments passed verbatim
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.


==== esviritu download-db ====
OVERVIEW: Download the EsViritu viral reference database

USAGE: lungfish-cli esviritu download-db [--force] [--format <format>] [--verbose ...] [--quiet] [--progress] [--no-progress] [--debug] [--log-file <log-file>] [--no-color] [--threads <threads>]

OPTIONS:
  --force                 Re-download even if the database is already installed
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.


==== esviritu db-status ====
OVERVIEW: Check EsViritu database installation status

USAGE: lungfish-cli esviritu db-status [--format <format>] [--verbose ...] [--quiet] [--progress] [--no-progress] [--debug] [--log-file <log-file>] [--no-color] [--threads <threads>]

OPTIONS:
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.

