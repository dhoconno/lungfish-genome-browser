==== workflow ====
OVERVIEW: Execute and manage bioinformatics workflows

Run Nextflow and Snakemake workflows using Apple Containerization
for isolated execution of bioinformatics tools.

Requires macOS 26 or later for container support.

USAGE: lungfish-cli workflow <subcommand>

OPTIONS:
  --version               Show the version.
  -h, --help              Show help information.

SUBCOMMANDS:
  run (default)           Execute a workflow pipeline
  builder-run             Run a native Workflow Builder graph
  list                    List available workflows
  validate                Validate a workflow definition
  diff                    Compare two saved Lungfish workflows

  See 'lungfish-cli help workflow <subcommand>' for detailed help.

==== workflow run ====
OVERVIEW: Execute a workflow pipeline

Run a Nextflow or Snakemake workflow with the specified parameters.
Workflows are executed using Apple Containerization (macOS 26+).
The only built-in nf-core workflow supported by this command is
nf-core/viralrecon, also accepted as viralrecon.

Examples:
  lungfish workflow run pipeline.nf --param reads=data/*.fastq.gz
  lungfish workflow run Snakefile --cpus 8 --memory 16.GB
  lungfish workflow run nf-core/viralrecon --input samplesheet.csv --param
platform=illumina

USAGE: lungfish-cli workflow run [<options>] <workflow>

ARGUMENTS:
  <workflow>              Workflow file (*.nf, Snakefile) or supported nf-core
                          workflow: nf-core/viralrecon

OPTIONS:
  --repeat-from <repeat-from>
                          Validate an original local run bundle before starting
                          a fresh attempt
  --results-dir <results-dir>
                          Output directory for results (default: ./results)
  --executor <executor>   Execution profile for nf-core workflows: docker,
                          conda, or local (default: docker)
  --input <input>         Input file selected for the workflow; repeat for
                          multiple inputs
  --expected-output <expected-output>
                          Final output bundle or file path; required for
                          executed runs and repeatable for every scientific
                          output that must receive provenance
  --bundle-root <bundle-root>
                          Directory where the .lungfishrun bundle should be
                          created
  --bundle-path <bundle-path>
                          Exact .lungfishrun bundle path to create or update
  --version <version>     nf-core workflow version or tag
  -w, --workdir <workdir> Working directory for execution
  --param <param>         Workflow parameter (key=value, can be repeated)
  --params-file <params-file>
                          Parameters from JSON/YAML file
  --cpus <cpus>           Maximum CPUs per process
  --memory <memory>       Maximum memory per process (e.g., 8.GB)
  --resume                Resume from last checkpoint
  --dry-run               Validate workflow without executing
  --prepare-only          Create the Lungfish run bundle and command preview
                          without launching Nextflow
  --timeout <timeout>     Maximum execution time in minutes
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.


==== workflow builder-run ====
OVERVIEW: Run a native Workflow Builder graph

USAGE: lungfish-cli workflow builder-run --workflow <workflow> --project <project> [--run-directory <run-directory>] [--threads <threads>] [--dry-run]

OPTIONS:
  --workflow <workflow>   Workflow Builder .lungfishflow bundle or graph JSON
  --project <project>     Active .lungfish project directory
  --run-directory <run-directory>
                          Directory for workflow run state and intermediate
                          files
  --threads <threads>     CPU threads available to native FASTQ tools (default:
                          4)
  --dry-run               Compile the executable plan and print JSON without
                          running tools
  --version               Show the version.
  -h, --help              Show help information.


==== workflow list ====
OVERVIEW: List available workflows

USAGE: lungfish-cli workflow list [--nf-core] [--format <format>] [--verbose ...] [--quiet] [--progress] [--no-progress] [--debug] [--log-file <log-file>] [--no-color] [--threads <threads>]

OPTIONS:
  --nf-core               List the supported nf-core Viral Recon pipeline
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.


==== workflow validate ====
OVERVIEW: Validate a workflow definition

USAGE: lungfish-cli workflow validate <workflow> [--format <format>] [--verbose ...] [--quiet] [--progress] [--no-progress] [--debug] [--log-file <log-file>] [--no-color] [--threads <threads>]

ARGUMENTS:
  <workflow>              Workflow file to validate

OPTIONS:
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.


==== workflow diff ====
OVERVIEW: Compare two saved Lungfish workflows

USAGE: lungfish-cli workflow diff <first> <second> [--format <format>]

ARGUMENTS:
  <first>                 First workflow file or .lungfishflow bundle
  <second>                Second workflow file or .lungfishflow bundle

OPTIONS:
  --format <format>       Output format: text, json, or tsv (values: text,
                          json, tsv; default: text)
  --version               Show the version.
  -h, --help              Show help information.

