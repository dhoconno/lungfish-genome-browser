==== ops ====
OVERVIEW: Inspect completed operation history and resource usage

USAGE: lungfish-cli ops <subcommand>

OPTIONS:
  --version               Show the version.
  -h, --help              Show help information.

SUBCOMMANDS:
  stats                   Summarize runtime and peak RAM from provenance
                          sidecars

  See 'lungfish-cli help ops <subcommand>' for detailed help.

==== ops stats ====
OVERVIEW: Summarize runtime and peak RAM from provenance sidecars

USAGE: lungfish-cli ops stats <project> [--format <format>] [--verbose ...] [--quiet] [--progress] [--no-progress] [--debug] [--log-file <log-file>] [--no-color] [--threads <threads>]

ARGUMENTS:
  <project>               Project or bundle directory containing
                          .lungfish-provenance.json sidecars

OPTIONS:
  --format <format>       Output format: text, json, tsv (default: text)
                          (values: text, json, tsv; default: text)
  -v, --verbose           Increase output verbosity (can be repeated: -v, -vv,
                          -vvv)
  -q, --quiet             Suppress non-essential output (errors only)
  --progress              Show progress bar (default: auto-detect TTY)
  --no-progress           Disable progress bar
  --debug                 Enable debug output with detailed logging
  --log-file <log-file>   Write detailed logs to file
  --no-color              Disable colored output
  -t, --threads <threads> Number of threads to use (default: auto)
  --version               Show the version.
  -h, --help              Show help information.

