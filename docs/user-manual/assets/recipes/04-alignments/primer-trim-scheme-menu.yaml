# Captured from Lungfish Preview 2026.9.13 with capture-window.sh (2026-09 campaign).
# App state: Preview build 4673. Interface-only configuration on human chr20 alignment. No matching scheme chosen and no scientific run.
id: primer-trim-scheme-menu
chapter: 04-alignments/03-primer-trimming
caption: The Primer Trim dialog with the Primer Scheme menu open, showing the eight schemes under the
  Built-in heading.
viewport_class: none
app_state:
  fixture: '{demo_project}'
  open_files: []
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Select the human chr20 reference bundle. Inspector > Analysis > Primer Trim > Primer-trim BAM. Open Primer Scheme to show the eight built-in scheme names. Capture the menu only, then dismiss it. No trimming run.
crop:
  mode: region
  region:
  - 790
  - 428
  - 540
  - 480
post:
  retina: true
  format: png
