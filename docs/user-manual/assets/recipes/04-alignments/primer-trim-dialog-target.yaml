# Captured from Lungfish Preview 2026.9.13 with capture-window.sh (2026-09 campaign).
# App state: the Primer Trim dialog Target section, opened against the chr20_10.0-10.5Mb HG002 minimap2 alignment track (no SARS-CoV-2 track was available in this session)
id: primer-trim-dialog-target
chapter: 04-alignments/03-primer-trimming
caption: The Primer Trim dialog's Target section, showing the Alignment Track menu, the pre-filled Output
  Track Name field, and the note that reads without matching primers are retained.
viewport_class: alignment
app_state:
  fixture: '{demo_project}'
  open_files:
  - reference: Reference Sequences/chr20_10.0-10.5Mb.lungfishref
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Selected the chr20_10.0-10.5Mb reference bundle in the sidebar
  # PROSE-ONLY: Clicked the Analysis tab, then the Primer Trim sub-tab
  # PROSE-ONLY: Clicked Primer-trim BAM...
crop:
  mode: region
  region:
  - 465
  - 210
  - 539
  - 479
post:
  retina: true
  format: png
