# Captured from Lungfish Preview 2026.9.13 with capture-window.sh (2026-09 campaign).
# App state: Preview build 4673. Unconfigured human alignment target, no scheme selected, Output Track Name empty.
id: primer-trim-dialog-target
chapter: 04-alignments/03-primer-trimming
caption: The Primer Trim dialog opened on the human demo alignment before choosing a matching scheme,
  showing the Alignment Track menu, empty Output Track Name field, and note that reads without matching
  primers are retained.
viewport_class: none
app_state:
  fixture: '{demo_project}'
  open_files: []
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Select chr20 reference. Inspector > Analysis > Primer Trim > Primer-trim BAM. Capture before choosing a matching scheme, then Cancel without running.
crop:
  mode: region
  region:
  - 790
  - 428
  - 540
  - 480
post:
  retina: true
  format: png
