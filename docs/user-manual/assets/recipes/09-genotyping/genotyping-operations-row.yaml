# Captured from Lungfish Preview 2026.9.13 with capture-window.sh (2026-09 campaign).
# App state: Preview build 4673. Native GUI simulated MHC genotype-only batch completed in 2 seconds with annotated reference and Keep Intermediates.
id: genotyping-operations-row
chapter: 09-genotyping/02-running-genotyping
caption: The Operations panel after the simulated two-sample MHC teaching batch completes, with its command,
  output files, log, and Completed status visible.
viewport_class: none
app_state:
  fixture: '{demo_project}'
  open_files: []
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Select SIMULATED-MHC-A-pairs and SIMULATED-MHC-B-pairs, open miSeq genotyping, choose SIMULATED-MHC-annotated-reference and Genotype only, enable Keep Intermediates, and name the report SIMULATED-MHC-GUI-annotated.
  # PROSE-ONLY: Run, open Operations Panel, expand the completed row, and capture window-relative crop 0,1,1200,570. This tiny fixture finishes before a running-stage capture, so the caption describes the completed state.
crop:
  mode: region
  region:
  - 0
  - 1
  - 1200
  - 570
post:
  retina: true
  format: png
