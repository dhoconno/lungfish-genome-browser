# Captured from Lungfish Preview 2026.9.13 with capture-window.sh (2026-09 campaign).
# App state: The Remove Human Reads pane with HG002 as input. On this profile the managed human database is not installed, so the Database row reads Required before this tool can run with a single Choose... button rather than the installed-database state the chapter describes.
id: human-scrub-dialog
chapter: 03-reads/05-decontamination
caption: "The Remove Human Reads pane, with the Database row in the Inputs section offering Choose... because no database is selected, and the settings pane below it holding no controls."
viewport_class: fastq
app_state:
  fixture: '{demo_project}'
  open_files:
  - fastq: Imports/HG002.lungfishfastq
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Selected Imports/HG002 in the sidebar.
  # PROSE-ONLY: Chose Tools > Decontamination > Remove Human Reads...
crop:
  mode: region
  region:
  - 242
  - 102
  - 982
  - 696
post:
  retina: true
  format: png
