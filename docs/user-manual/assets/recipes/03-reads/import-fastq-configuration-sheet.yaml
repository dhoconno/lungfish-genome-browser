# Captured from Lungfish Preview 2026.9.13 with capture-window.sh (2026-09 campaign).
# App state: Import FASTQ configuration for the HG002 chr20 R1 and R2 pair, Illumina and Paired-end detected.
id: import-fastq-configuration-sheet
chapter: 03-reads/01-importing-fastq
caption: The Import FASTQ configuration sheet for the HG002 chromosome 20 pair, with the summary reading
  R1 and R2 on separate lines above the Platform, Pairing, Quality Binning, and compression controls.
viewport_class: none
app_state:
  fixture: '{demo_project}'
  open_files: []
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Open Import Center > Sequencing Reads > Sequencing Read Files.
  # PROSE-ONLY: Choose both HG002 chr20 R1 and R2 FASTQ files from the hg002-chr20 fixture and click Open.
  # PROSE-ONLY: Capture configuration with R1 and R2 separate, then Cancel without importing duplicate reads.
crop:
  mode: region
  region:
  - 780
  - 523
  - 560
  - 520
post:
  retina: true
  format: png
