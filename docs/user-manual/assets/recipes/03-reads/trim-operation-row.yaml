# Captured from Lungfish Preview 2026.9.13 with capture-window.sh (2026-09 campaign).
# App state: Finished GUI fastp trim of Imports/HG002,7.7s,expanded Operations row with full wrapping command and log. Window-point crop. Local Preview2026.9.13 build4673,candidate9b767d3e4.
id: trim-operation-row
chapter: 03-reads/04-trimming-and-filtering
caption: The Operations Panel with the finished trim row expanded, showing its state, elapsed time, and
  command line.
viewport_class: none
app_state:
  fixture: '{demo_project}'
  open_files: []
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Select Imports/HG002 and choose Tools > Trimming & Filtering > fastp Adapter + Quality Trim.
  # PROSE-ONLY: Use threshold20,window4,Cut Right,Auto-Detect adapter mode,Per Input output and click Run.
  # PROSE-ONLY: Open Operations panel,expand finished trim row,and resize panel to1200×700points.
  # PROSE-ONLY: Capture window-point crop0,1,1200,280,keeping title,state,elapsed,full command and log.
crop:
  mode: region
  region:
  - 0
  - 1
  - 1200
  - 280
post:
  retina: true
  format: png
