# Captured from Lungfish Preview 2026.9.13 with capture-window.sh (2026-09 campaign).
# App state: The fastp Adapter + Quality Trim pane at its defaults: Threshold 20, Window Size 4, Mode Cut Right, Adapter Mode Auto-Detect.
id: trimming-dialog
chapter: 03-reads/04-trimming-and-filtering
caption: The FASTQ/FASTA Operations window on the fastp Adapter + Quality Trim pane at its defaults, with
  Threshold 20, Window Size 4, Mode on Cut Right, and Adapter Mode on Auto-Detect.
viewport_class: fastq
app_state:
  fixture: '{demo_project}'
  open_files:
  - fastq: Imports/HG002.lungfishfastq
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Selected Imports/HG002 in the sidebar.
  # PROSE-ONLY: Chose Tools > Trimming & Filtering > fastp Adapter + Quality Trim...
  # PROSE-ONLY: Left all controls on their defaults.
crop:
  mode: region
  region:
  - 242
  - 102
  - 982
  - 696
post:
  retina: true
  format: png
