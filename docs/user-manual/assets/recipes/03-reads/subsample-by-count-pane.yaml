# Captured from Lungfish Preview 2026.9.13 with capture-window.sh (2026-09 campaign).
# App state: The Subsample by Count pane with its Count field and Output Strategy picker.
id: subsample-by-count-pane
chapter: 03-reads/06-subsetting-and-extraction
caption: The Subsample by Count pane with its single Count field and the Output Strategy picker below
  it.
viewport_class: fastq
app_state:
  fixture: '{demo_project}'
  open_files:
  - fastq: Imports/HG002.lungfishfastq
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Selected Imports/HG002 in the sidebar.
  # PROSE-ONLY: Chose Tools > Search & Subsetting > Subsample by Count...
  # PROSE-ONLY: Left the Count field empty.
crop:
  mode: region
  region:
  - 242
  - 102
  - 982
  - 696
post:
  retina: true
  format: png
