# Captured from Lungfish Preview 2026.9.13 with capture-window.sh (2026-09 campaign).
# App state: Preview build 4673. HG002 selected, Remove Duplicates preset popup open with Exact PCR selected.
id: remove-duplicates-preset-picker
chapter: 03-reads/05-decontamination
caption: The Remove Duplicates pane with the Preset picker open on its six choices, Exact PCR selected.
viewport_class: none
app_state:
  fixture: '{demo_project}'
  open_files: []
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Select Imports/HG002 and choose Tools > Decontamination > Remove Duplicates.
  # PROSE-ONLY: Open Preset and capture all six choices with Cancel and Run visible. Escape dismisses the popup, then Cancel closes the configuration.
crop:
  mode: region
  region:
  - 570
  - 318
  - 980
  - 700
post:
  retina: true
  format: png
