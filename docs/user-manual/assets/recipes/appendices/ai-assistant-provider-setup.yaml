# Captured from Lungfish Preview 2026.9.13 with capture-window.sh (2026-09 campaign).
# App state: The AI Services settings tab with the Enable AI-powered search toggle off, the Default provider picker set to Anthropic Claude, and the Anthropic API Key field empty with its grey minus-sign status indicator and Model picker.
id: ai-assistant-provider-setup
chapter: appendices/ai-assistant
caption: The AI Services settings tab with the Enable AI-powered search toggle, the Default provider picker,
  and one provider's API Key field, key-status indicator, and Model picker.
viewport_class: none
app_state:
  fixture: none
  open_files: []
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Choose Lungfish Genome Explorer Preview > Settings...
  # PROSE-ONLY: Click the AI Services tab
  # PROSE-ONLY: Leave every API Key field empty
crop:
  mode: region
  region:
  - 0
  - 55
  - 820
  - 365
post:
  retina: true
  format: png
