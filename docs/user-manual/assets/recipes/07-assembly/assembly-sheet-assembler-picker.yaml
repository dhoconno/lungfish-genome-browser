# Captured from Lungfish Preview 2026.9.13 with capture-window.sh (2026-09 campaign).
# App state: The assembly sheet's Primary Settings section, cropped to the Assembler picker and the locked Read Type row, from the SPAdes sheet opened on Imports/HG002-chrM.
id: assembly-sheet-assembler-picker
chapter: 07-assembly/01-when-to-assemble
caption: The assembly sheet's Primary Settings, with the segmented Assembler picker above the Read Type
  row, which reads Illumina short reads with the note Locked from FASTQ header detection beneath it.
viewport_class: fastq
app_state:
  fixture: '{demo_project}'
  open_files:
  - bundle: Imports/HG002-chrM
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Select Imports/HG002-chrM in the sidebar
  # PROSE-ONLY: Choose Tools > Assembly > SPAdes...
crop:
  mode: region
  region:
  - 500
  - 327
  - 673
  - 189
post:
  retina: true
  format: png
