# Captured from Lungfish Preview 2026.9.13 with capture-window.sh (2026-09 campaign).
# App state: Preview build 4673. HiFi bundle, Hifiasm alone, Diploid profile.
id: assembly-sheet-hifiasm
chapter: 07-assembly/03-running-flye-or-hifiasm
caption: The same sheet opened from Tools > Assembly > Hifiasm... with the HiFi bundle selected, where
  the Assembler picker shows Hifiasm alone because it is the only assembler that accepts PacBio HiFi reads,
  and the Profile picker sits on Diploid.
viewport_class: none
app_state:
  fixture: '{demo_project}'
  open_files: []
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Select Imports/HG002.chrM.hifi and open Tools > Assembly > Hifiasm. Capture the initial settings, then Cancel.
crop:
  mode: region
  region:
  - 570
  - 318
  - 980
  - 700
post:
  retina: true
  format: png
