# Captured from Lungfish Preview 2026.9.13 with capture-window.sh (2026-09 campaign).
# App state: The Plugin Manager's Installed tab with the bcftools environment row expanded to list its 30 packages and versions, above which sits the Check for Tool Updates... button.
id: plugin-manager-installed-tab
chapter: 01-foundations/07-plugin-packs
caption: The Installed tab with one environment row expanded to list the packages and versions inside
  it, and the Check for Tool Updates button above the list.
viewport_class: none
app_state:
  fixture: none
  open_files: []
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Choose Tools > Plugin Manager...
  # PROSE-ONLY: Click the Installed tab
  # PROSE-ONLY: Click the bcftools row to expand it
crop:
  mode: window
post:
  retina: true
  format: png
