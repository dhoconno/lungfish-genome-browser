# Captured from Lungfish Preview 2026.9.13 with capture-window.sh (2026-09 campaign).
# App state: Kraken2 result kraken2-SRR36291587 open in the taxonomy viewport; this demo run has no Bracken sidecar so no Bracken column appears
id: kraken2-taxonomy-viewport
chapter: 06-classification/02-running-kraken2
caption: The taxonomy viewport after classifying SRR36291587, with the breadcrumb bar running across the
  top of both panes, the sunburst on the left, and the per-taxon table on the right showing its Filter
  taxa... field and its Bracken column.
viewport_class: taxonomy
app_state:
  fixture: '{demo_project}'
  open_files:
  - analysis: kraken2-SRR36291587
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Select kraken2-SRR36291587 under Analyses in the sidebar
crop:
  mode: window
post:
  retina: true
  format: png
