# Captured from Lungfish Preview 2026.9.13 with capture-window.sh (2026-09 campaign).
# App state: Preview build 4673. CZ-ID fixture validated, form scrolled to show complete Preview and Project Destination.
id: czid-import-sheet
chapter: 06-classification/08-importing-cz-id-results
caption: The CZ-ID Import sheet scrolled after a successful scan, showing the Preview panel with Sample,
  Project, Rows, Source, Report, Pipeline, NT DB, NR DB, and Top taxa, followed by the Project Destination
  readout and Cancel and Run buttons.
viewport_class: none
app_state:
  fixture: '{demo_project}'
  open_files: []
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Import Center > Classification Results > CZ-ID Results. Browse and select Tests/Fixtures/czid/minimal_taxon_report.tsv.
  # PROSE-ONLY: Scroll the form to the bottom and capture all preview values, complete destination readout, Cancel and Run. Cancel without importing a duplicate.
crop:
  mode: region
  region:
  - 934
  - 226
  - 520
  - 460
post:
  retina: true
  format: png
