# Captured from Lungfish Preview 2026.9.13 with capture-window.sh (2026-09 campaign).
# App state: Plugin Manager on the Databases tab, window resized to 800x1000 and scrolled to show all eleven Kraken2 Databases rows plus the recommended-database banner and storage readout
id: kraken2-databases-tab
chapter: 06-classification/02-running-kraken2
caption: The Plugin Manager on its Databases tab, with the recommended-database banner at the top and
  the eleven Kraken 2 rows showing which collections are installed and which still offer a Download button.
viewport_class: none
app_state:
  fixture: none
  open_files: []
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Tools > Plugin Manager...
  # PROSE-ONLY: Click Databases tab
  # PROSE-ONLY: Resize window to 800x1000
  # PROSE-ONLY: Scroll down to reveal all eleven rows
crop:
  mode: window
post:
  retina: true
  format: png
