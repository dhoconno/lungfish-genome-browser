# Captured from Lungfish Preview 2026.9.13 with capture-window.sh (2026-09 campaign).
# App state: Existing Kraken report taxon row context menu, Extract Reads highlighted only.
id: kraken2-extract-reads
chapter: 06-classification/02-running-kraken2
caption: The right-click menu on a taxon row, with Extract Reads... highlighted above the expansion, BLAST,
  NCBI lookup, and Copy Taxon Name actions.
viewport_class: none
app_state:
  fixture: '{demo_project}'
  open_files: []
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Filter sidebar to kraken2 and open existing result. Right-click a visible taxon row. Down highlights Extract Reads. Capture and Escape without extraction.
crop:
  mode: region
  region:
  - 1219
  - 409
  - 246
  - 293
post:
  retina: true
  format: png
