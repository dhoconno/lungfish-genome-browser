# Captured from Lungfish Preview 2026.9.13 with capture-window.sh (2026-09 campaign).
# App state: The Variant Query Builder sheet with two rules and Match All selected.
id: variants-search-builder
chapter: 05-variants/02-reading-the-variant-browser
caption: The Variant Query Builder sheet with two rules, one on Call Quality and one on an INFO field,
  combined with Match All.
viewport_class: variant
app_state:
  fixture: '{demo_project}'
  open_files:
  - reference: Reference Sequences/chr20_10.0-10.5Mb.lungfishref
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Selected chr20_10.0-10.5Mb in the sidebar, Variants tab
  # PROSE-ONLY: Clicked Query Builder...
  # PROSE-ONLY: Clicked the Quality Review preset, giving Call Quality/Quality < 30 and Call Quality/DP >= 10
  # PROSE-ONLY: Could not change the second rule's category popup to INFO Field in the background (AXPopUpButton controls refuse background clicks), so the second rule shows DP under Call Quality rather than under INFO Field; DP is listed under both categories in the chapter's own field table
crop:
  mode: region
  region:
  - 385
  - 266
  - 700
  - 380
post:
  retina: true
  format: png
