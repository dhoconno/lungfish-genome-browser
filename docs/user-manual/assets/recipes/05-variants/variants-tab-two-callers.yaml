# Captured from Lungfish Preview 2026.9.13 with capture-window.sh (2026-09 campaign).
# App state: The chr20 bundle open with its table drawer on the Variants tab, both the HG002 bcftools and HG002 LoFreq tracks loaded, 1,918 variants, columns ID through Source visible.
id: variants-tab-two-callers
chapter: 05-variants/01-calling-variants-from-amplicons
caption: The Variants tab of the table drawer with both the bcftools and LoFreq tracks loaded, and the
  Source column naming which track each row came from.
viewport_class: variant
app_state:
  fixture: '{demo_project}'
  open_files:
  - reference: Reference Sequences/chr20_10.0-10.5Mb.lungfishref
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Select chr20_10.0-10.5Mb under Reference Sequences
  # PROSE-ONLY: Click the drawer's Variants tab and wait for the rows to load
  # PROSE-ONLY: Capture the drawer region
crop:
  mode: region
  region:
  - 194
  - 620
  - 872
  - 258
post:
  retina: true
  format: png
