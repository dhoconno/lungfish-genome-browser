# Captured from Lungfish Preview 2026.9.13 with capture-window.sh (2026-09 campaign).
# App state: The Extract Sequence dialog with Save as Bundle selected by default, the other three destinations listed, and the Name field prefilled with the suggested consensus name.
id: consensus-destination-dialog
chapter: 05-variants/05-consensus-and-lineage
caption: The Extract Sequence dialog showing its four Destination choices, with Save as Bundle selected
  by default above Save to File..., Copy to Clipboard, and Share..., and the Name field prefilled with
  the suggested consensus name.
viewport_class: alignment
app_state:
  fixture: '{demo_project}'
  open_files:
  - reference: Reference Sequences/chr20_10.0-10.5Mb.lungfishref
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Clicked Extract Consensus... on the Consensus tab
  # PROSE-ONLY: Clicked Cancel without extracting
crop:
  mode: region
  region:
  - 520
  - 270
  - 425
  - 345
post:
  retina: true
  format: png
