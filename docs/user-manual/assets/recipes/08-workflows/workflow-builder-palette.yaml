# Captured from Lungfish Preview 2026.9.13 with capture-window.sh (2026-09 campaign).
# App state: The node palette below the Workflows list, showing the Filter nodes search field and the four category headers Input, Trimming & Filtering, Decontamination, and Read Processing, each expanded with its node types.
id: workflow-builder-palette
chapter: 08-workflows/01-the-workflow-builder
caption: 'The node palette with its Filter nodes search field and the four category headers it draws:
  Input, Trimming & Filtering, Decontamination, and Read Processing.'
viewport_class: none
app_state:
  fixture: human-mito
  open_files: []
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Workflow Builder open with the Mito read cleanup workflow created
crop:
  mode: region
  region:
  - 0
  - 240
  - 210
  - 380
post:
  retina: true
  format: png
