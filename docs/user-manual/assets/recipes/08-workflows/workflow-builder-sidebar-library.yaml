# Captured from Lungfish Preview 2026.9.13 with capture-window.sh (2026-09 campaign).
# App state: The Workflow Builder's left sidebar with the Workflows list empty (no workflow created yet), its plus, duplicate, and trash buttons beside the heading, and the node palette below it.
id: workflow-builder-sidebar-library
chapter: 08-workflows/01-the-workflow-builder
caption: The Workflow Builder's left sidebar, showing the Workflows list above the node palette with its
  plus, duplicate, and trash buttons.
viewport_class: none
app_state:
  fixture: human-mito
  open_files: []
  window_size:
  - 1400
  - 900
  appearance: light
steps:
- action: open_application
  app: Lungfish Preview
- action: wait_ready
  signal: main_window_visible
- action: resize_window
  size:
  - 1400
  - 900
  # PROSE-ONLY: Choose Tools > Workflow Builder (Experimental)...
  # PROSE-ONLY: Show Experimental Features already on in Settings > Advanced
crop:
  mode: region
  region:
  - 0
  - 0
  - 210
  - 772
post:
  retina: true
  format: png
